# v0.5.11+ Development Enhancement Work Plan

- 문서 목적: v0.5.10 baseline 의 단기 백로그 4개 (#1 Mavis hook, #2 회귀 test, #3 --no-interactive 가이드, #4 ADR 정식) 와 장기 2개 (#5 contract v2 streaming, #6 추가 하네스 overlay) 를 3 PR 로 분할하여 v0.5.11 milestone 으로 출시하기 위한 work plan.
- 범위: workflow_kit/contract_v1/ 확장, workflow_kit/upgrade_diff.py 후속, workflow-source/core/mcp_installation_by_harness.md 보강, docs/architecture/ 신규 ADR 3건, governance 갱신.
- 대상 독자: Sisyphus (orchestrator), maintainer, Mavis engine 사용자.
- 상태: stable (Momus 5 round 검수 완료, PASS)
- 최종 수정일: 2026-06-09
- 관련 문서: `.omo/plans/hotfix-smart-update.md` (v0.5.10.1 hotfix), `docs/planning/README.md` §3 (단기 백로그), `workflow-source/core/orchestrator_subagent_contract_v1.md`, `workflow-source/core/orchestrator_contract_v1_wire_guide.md`

- 작성일: 2026-06-09
- 작성자: Sisyphus (orchestrator)
- 적용 대상: `standard_ai_workflow` 저장소
- 기준선: `e927060 docs(v0.5.10-post): full documentation refresh + dev install guide`
- 사용자 선택 항목 6개를 단기(v0.5.11) / 장기(v0.6+) 마일스톤으로 분리
- **Revision 1 (2026-06-09)**: 3개 blocking issue 보정 (--no-interactive 베이스라인, DoD 정책, audit 절차)
- **Revision 2 (2026-06-09)**: 3개 blocking issue 보정 (hook 위치 의미 오류, 예외명, sub_id uniqueness 정책)
- **Revision 3 (2026-06-09)**: 3개 blocking issue 보정 (sub_id 한계 정직화, governance 옵션 A, audit 4-step) + 6개 비-blocker
- **Revision 4 (2026-06-09)**: 1 blocking issue 보정 (wire 가이드 ↔ `check_wire_guide_v059.py` stub 결합 명시) + 3개 advisory (PR-3 sequential 결정 + 7-day cool-down, fan-in test case 한정, Beta-v0.5.11.md §3 에 #5/#6 등재)
- **Revision 5 (2026-06-09)**: 1 final blocking 보정 (Strategy A 명시 + `except ValueError` catch 변경 1줄) — Plan PASS

---

## 0. Executive Summary

| 그룹 | 항목 | 마일스톤 | 우선순위 | 의존성 |
|---|---|---|---|---|
| 단기 | #1 Mavis engine hook 자동 enforce (wire 가이드 §2/§3 패턴 + stub 동기화) | v0.5.11 | P0 | — (선행) |
| 단기 | #2 회귀 test 강화 (parent_delegation_id / sub_id status inconsistency / contract_version) | v0.5.11 | P0 | #1 |
| 단기 | #3 `--no-interactive` 비대화형 가이드 보강 (`mcp_installation_by_harness.md`) | v0.5.11 | P2 | — (독립) |
| 단기 | #4 docs/architecture/ ADR-001/002/003 + governance 갱신 (옵션 A, sequential, 7-day cool-down) | v0.5.11 | P1 | — (독립) |
| 장기 | #5 contract v2 streaming/observability | v0.6+ | P2 | 별도 spec 단계 |
| 장기 | #6 추가 하네스 오버레이 (Claude Code, Cursor, Windsurf) | v0.6+ | P2 | 별도 PoC 단계 |

> **Rev 4 핵심 추가**:
> - #1 의 wire 가이드 갱신이 `check_wire_guide_v059.py` 라인 120-132 의 stub namespace (`OutputValidationFailed`, `log_violation`) 와 결합되어 있어, **PR-1 commit 시 stub 도 동시 갱신** 필요. 권장: wire 가이드 reference code 가 비표준 식별자 (`OutputValidationFailed`, `log_violation`) 를 제거하고 `enforce_subagent_response` / `enforce_fanin_response` + `validate_output` / `validate_fanin_output` + `OutputValidationResult` 의 **표준 contract_v1 public API 만 사용**. 그러면 stub 도 minimal (raise 안 함, validate 만).
> - #4 의 governance 갱신 PR-3 는 **sequential 머지 (governance commit 먼저 → 7-day cool-down → ADR commit) 권장**. 1인 dev 환경에서도 governance 변경에는 "fresh eyes" 효과.
> - #1 fan-in test 의 violation case 는 **"sub.delegation_id prefix 불일치" 만** 한정. `parent_delegation_id` 누락은 #2 의 `check_validate_fanin_parent_delegation_id_missing_raises` 가 cover.

---

## 1. 의존성 그래프 (DAG)

```
v0.5.11 마일스톤 (단기)
━━━━━━━━━━━━━━━━━━━━━━━
#1 Mavis hook (spec → test → impl + wire guide + stub 갱신)
                                                          ─► #2 회귀 test 강화 ─┐
                                                                                  │
#3 --no-interactive 가이드 보강 ────────────────────────────────────────────────┼─► v0.5.11 릴리스 노트 + PR
                                                                                  │
#4 ADR-001/002/003 + governance 갱신 (sequential, 7-day cool-down) ───────────────┘

v0.6+ 마일스톤 (장기) — 병렬 진행 가능
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#5 contract v2 streaming (별도 spec 단계)
#6 추가 하네스 오버레이 (별도 PoC 단계)
```

---

## 2. v0.5.11 마일스톤 — 단기 4개 항목

### 2.1. #1 Mavis engine hook 자동 enforce (P0) — Rev 4 stub 결합 명시

**문제**: `validate_output` / `validate_fanin_output` 가 `OutputValidationResult` 반환. wire 가이드 §2/§3 reference code 가 `OutputValidationFailed` / `log_violation` 같은 비표준 식별자 + `if not result.is_valid: raise ...` 패턴 사용. Mavis / consumer 가 빠뜨리면 hook 미적용.

**목표**:
- `enforce_subagent_response` / `enforce_fanin_response` helper 가 `ValueError` raise.
- wire 가이드 §2/§3 reference code 가 본 helper 사용 + **비표준 식별자 제거** (Rev 4 stub 결합 해결).
- `check_wire_guide_v059.py` 의 stub namespace 도 helper 호환 버전으로 갱신 (PR-1 의 동시 commit).

**대상 파일** (Rev 4 stub 결합 추가):
- `workflow-source/workflow_kit/contract_v1/output_validator.py` — helper 정의
- `workflow-source/workflow_kit/contract_v1/__init__.py` — re-export
- `workflow-source/tests/check_contract_v1_delegator.py` — 신규 test ≥ 2개
- `workflow-source/tests/check_contract_v1_multi_component.py` — 신규 test ≥ 1개 (Rev 4 N2: violation case = **sub.delegation_id prefix 불일치 만**)
- `workflow-source/core/orchestrator_subagent_contract_v1.md` — §6.x "Mavis engine hook" 절 신규 추가
- `workflow-source/core/orchestrator_contract_v1_wire_guide.md` — §2, §3 reference code 갱신 (**비표준 식별자 `OutputValidationFailed`, `log_violation` 제거 + helper 사용**)
- `workflow-source/tests/check_wire_guide_v059.py` — Rev 4 **필수**: stub namespace (라인 120-132) 가 helper 호환 버전으로 동시 갱신. `_OutputValidationFailed` / `log_violation` 제거, `enforce_subagent_response` / `enforce_fanin_response` 노출.

**구현 단계** (spec-first, 4단계):
1. **spec 확정**: `orchestrator_subagent_contract_v1.md` §6.x (또는 §6.4, 신규). 위치 = wire 가이드 §2/§3, opt-out 없음, helper 사용 의무.
2. **회귀 test 추가 (test-first)**:
   - `check_contract_v1_delegator.py`: `check_enforce_subagent_response_happy_path` + `check_enforce_subagent_response_violation_raises_value_error`
   - `check_contract_v1_multi_component.py`: `check_enforce_fanin_sub_delegation_id_prefix_mismatch_raises_value_error` (Rev 4: parent 누락은 #2 가 cover, prefix 만)
3. **impl**: `output_validator.py` 에 `enforce_subagent_response` / `enforce_fanin_response` 정의, `__init__.py` re-export.
4. **wire 가이드 + stub 동시 갱신 (Rev 4 + Rev 5 Strategy A commit)**:
   - **Strategy A 채택 (helper 직접 호출)** — wire 가이드 §2/§3 reference code 가 `enforce_subagent_response(response, decision.delegation_id)` / `enforce_fanin_response(fanin_payload, parent_decision.delegation_id)` 를 직접 호출. helper 가 `ValueError` raise.
   - wire 가이드 §2/§3 reference code 에서 `OutputValidationFailed`, `log_violation` 비표준 식별자 **완전 제거**.
   - `check_wire_guide_v059.py` 동시 갱신 (Rev 5 필수):
     - 라인 120-132 stub namespace: `_OutputValidationFailed` / `log_violation` 제거, helper 노출 (`enforce_subagent_response`, `enforce_fanin_response`).
     - **라인 263, 367**: `except _OutputValidationFailed` → **`except ValueError`** (helper 가 raise 하는 예외 타입이 `ValueError` 이므로 catch 절도 따라가야 함). 이 변경 누락 시 wire 가이드 walkthrough 가 `ValueError` 그대로 propagate → test fail.
     - 라인 135 의 `_OutputValidationFailed` 클래스 정의 제거.

**Definition of Done (Rev 4 audit 5-step)**:
- [ ] spec `orchestrator_subagent_contract_v1.md` §6.x 에 hook 규칙 명시
- [ ] `tests/check_contract_v1_delegator.py` 신규 test ≥ 2개
- [ ] `tests/check_contract_v1_multi_component.py` 신규 test ≥ 1개 (sub_id prefix mismatch 만, Rev 4 N2)
- [ ] `output_validator.py` 에 `enforce_subagent_response` / `enforce_fanin_response` 정의
- [ ] `__init__.py` 에서 둘 다 export
- [ ] wire 가이드 §2/§3 reference code 갱신 + `OutputValidationFailed` / `log_violation` 제거
- [ ] `check_wire_guide_v059.py` stub namespace 갱신 + `_OutputValidationFailed` / `log_violation` 제거
- [ ] **audit 5-step (Rev 4 확장, stub 결합 검증)**:
  1. `grep "validate_output\|validate_fanin_output" workflow_kit/contract_v1/delegator.py` → 0
  2. `grep "^def enforce_subagent_response\|^def enforce_fanin_response" workflow_kit/contract_v1/output_validator.py` → 각 ≥ 1
  3. `grep "enforce_subagent_response\|enforce_fanin_response" workflow_kit/contract_v1/__init__.py` → ≥ 2
  4. `grep "enforce_subagent_response\|enforce_fanin_response" workflow-source/core/orchestrator_contract_v1_wire_guide.md` → ≥ 1
  5. **Rev 4 신규**: `grep "OutputValidationFailed\|log_violation" workflow-source/core/orchestrator_contract_v1_wire_guide.md workflow-source/tests/check_wire_guide_v059.py` → 0 (비표준 식별자 모두 제거)
- [ ] smoke 52개 + 신규 3개 = 55개 전부 통과 (Rev 4 조정: #2 fanin parent 누락은 #2 에서 cover, #1 에선 prefix 만)

---

### 2.2. #2 회귀 test 강화 (parent_delegation_id / sub_id status inconsistency / contract_version) (P0) — Rev 4 조정

**문제**: `validate_fanin_output` 가 spec 위반 패턴 검증하나 raise path 회귀 test 부족. sub_id uniqueness = caller 책임 (Rev 3 옵션 a 유지, **시나리오 A: status 불일치 동반 시 indirect 검출**).

**대상 파일** (Rev 4 N2 조정 — 책임 분리):
- `workflow-source/tests/check_contract_v1_multi_component.py` — 추가 (Rev 4)
  - `check_validate_fanin_parent_delegation_id_missing_raises()` — fan-in payload 에 `parent_delegation_id` 누락 → `ValueError` raise
  - `check_validate_fanin_sub_id_duplicate_with_status_inconsistency_detected()` — caller 가 동일 sub_id 2개 + status 불일치 → aggregated mismatch 로 `ValueError` raise (시나리오 B 미검출 한계 주석 명시)
- `workflow-source/tests/check_contract_v1_output_validator.py` — 추가
  - `check_validate_output_contract_version_mismatch_marks_invalid()` — `contract_version != "1.0"` → `is_valid=False` (raise 안 함, 정책은 caller)
- ~~sub.delegation_id prefix mismatch test~~ — Rev 4 N2: **#1 의 `check_enforce_fanin_sub_delegation_id_prefix_mismatch_raises_value_error` 가 cover**. 본 #2 에선 중복 작성 안 함.

**구현 단계**: spec §4.2 에 sub_id uniqueness = caller 책임 + 시나리오 B 미검출 한계 1줄. test 3개 추가.

**Definition of Done (Rev 4 조정)**:
- [ ] `tests/check_contract_v1_multi_component.py` 에 신규 test **2개** (parent 누락 + sub_id status 불일치)
- [ ] `tests/check_contract_v1_output_validator.py` 에 신규 test 1개 (contract_version)
- [ ] test 3개 모두 `check_*` 컨벤션 + 시나리오 한계 주석
- [ ] spec §4.2 sub_id uniqueness = caller 책임 + 시나리오 B 미검출 한계 명시
- [ ] smoke 합계: 52 + #1 신규 3 + #2 신규 3 = **58개** (Rev 4 조정)
- [ ] `Beta-v0.5.11.md` §3 후속에 3줄 등재:
  - sub_id uniqueness 옵션 (b) — `choose_roles` dedup check
  - #5 contract v2 streaming PoC starter
  - #6 Claude Code/Cursor/Windsurf harness PoC starter

---

### 2.3. #3 `--no-interactive` 비대화형 가이드 보강 (P2) — Rev 3 유지

**대상 파일**:
- `workflow-source/core/mcp_installation_by_harness.md` — picker 섹션 끝에 비대화형 정책 단락 (1~2줄 + bash 예시 1개)
- `docs/INSTALLATION_AND_USAGE.md` — CI/스크립트 환경 호출 예시 ≥ 1개

**Definition of Done**: Rev 3 유지 (변경 없음).

---

### 2.4. #4 docs/architecture/ ADR-001/002/003 + governance 갱신 (P1) — Rev 4 sequential + 7-day cool-down

**Rev 4 결정**:
- PR-3 = **sequential 머지** (Rev 4 N1, squash 금지):
  1. commit 1: `docs/README.md` §1 governance 갱신
  2. **7-day cool-down** (Rev 4 N1, R7 강화)
  3. commit 2: ADR 3건 + `docs/architecture/README.md` 갱신

**7-day cool-down rationale**: 1인 dev 환경에서도 governance 변경은 본인이 fresh eyes 로 재검토하는 시간. 즉시 머지 금지. git tag `v0.5.11-governance-pending` 같은 임시 태그로 머지 전 상태 기록 (선택).

**대상 파일** (Rev 3 유지):
- 3개 ADR (신규)
- `docs/architecture/README.md` — ADR 링크
- `docs/README.md` — §1 governance 갱신 단락 (1인 dev 환경, 본인 = 문서 관리자)

**Definition of Done (Rev 4)**:
- [ ] 3개 ADR 표준 구조 + ADR-002 Consequences 1-2줄 압축
- [ ] `docs/architecture/README.md` §3 ADR 링크
- [ ] `docs/README.md` §1 governance 갱신 1줄
- [ ] **PR-3 = sequential 2 commit (Rev 4 N1)**: governance commit 먼저, **7-day cool-down** (Rev 4 N1), 그 후 ADR commit
- [ ] squash 금지 (audit trail 보존)

---

## 3. v0.5.11 릴리스 노트 + PR — Rev 4 갱신

**트리거**: #1, #2, #3, #4 모두 완료 시.

**대상 파일**:
- `workflow-source/releases/Beta-v0.5.11.md` — §1 변경 / §2 검증 / **§3 후속 3줄 등재** (Rev 4 N3: sub_id 옵션 b + #5 + #6)
- `docs/planning/README.md` — §2, §3 갱신
- `docs/README.md`, `README.md` — 버전 갱신

**PR 분할** (Rev 4 유지):
- PR-1: #1 (Mavis hook + spec + wire guide + stub 갱신) + #2 (회귀 test 강화)
- PR-2: #3 (가이드 보강)
- PR-3: #4 (ADR + governance) — **sequential + 7-day cool-down**

**검증 기준 (Rev 4)**:
- `check_docs.py` 통과
- smoke 합계 ≥ 58개 (52 + #1 신규 3 + #2 신규 3) 전부 green (Rev 4 조정)
- `Beta-v0.5.11.md` §3 후속 3줄 (sub_id 옵션 b, #5, #6)
- PR-3 = sequential 2 commit, 7-day cool-down 적용

---

## 4. v0.6+ 마일스톤 — Rev 4 갱신

(변경 없음, Rev 3 유지)

---

## 5. 위험 요소 (Risk Register) — Rev 4 갱신

| ID | 항목 | 영향 | 완화책 |
|---|---|---|---|
| R1 | hook / sub-agent 응답 깨짐 | 높음 | test-first. opt-out 없음. helper `ValueError`. smoke 58개 가드. |
| R2 | #3 가이드 충돌 | 낮음 | 문서만, picker 섹션만. |
| R3 | ADR 본문 불일치 | 낮음 | v0.5.2/5.4/5.7 PR cross-check, ADR-002 압축. |
| R4 | PR 충돌 | 중간 | 3 PR 분할, PR-3 sequential. |
| R5 | Momus 추가 issue | 낮음 | Rev 4 보정 완료. |
| R6 | sub_id uniqueness 옵션 (a) 시나리오 B 미검출 | 낮음 | spec §4.2 한계 명시. test 정직. 옵션 b v0.5.12+. |
| R7 | governance self-declaration 메타 순환 | 중간 | **Rev 4 강화**: 7-day cool-down for governance commit. squash 금지. `Beta-v0.5.11.md` §3 후속에 #5/#6 starter 등재. |
| R8 (Rev 4 신규) | wire 가이드 ↔ stub 결합 → PR-1 smoke 깨짐 | 높음 | **Rev 4 명시**: PR-1 commit 시 wire 가이드 + `check_wire_guide_v059.py` stub namespace 동시 갱신. 비표준 식별자 (`OutputValidationFailed`, `log_violation`) 모두 제거. audit 5-step 으로 검증. |

---

## 6. Definition of Done (v0.5.11 전체) — Rev 4

- [ ] #1, #2, #3, #4 모두 DoD 통과
- [ ] smoke 합계 ≥ 58개 (52 + #1 신규 3 + #2 신규 3) 전부 green
- [ ] audit 5-step (#1) 모두 PASS, 비표준 식별자 0
- [ ] `Beta-v0.5.11.md` §3 후속 3줄 (sub_id 옵션 b, #5, #6)
- [ ] PR-1/2/3 governance 따라 머지 (PR-3 = sequential + 7-day cool-down)
- [ ] self-dogfooding 1회 통과
- [ ] Momus Rev 4 → Rev 5 PASS

---

## 다음 단계

1. 본 plan 저장 (Rev 4, 현재)
2. Momus Rev 4 → Rev 5 검수
3. PASS 시 #1 Mavis hook spec-first 부터 구현 시작
4. #5, #6 은 v0.5.11 머지 후 별도 plan
