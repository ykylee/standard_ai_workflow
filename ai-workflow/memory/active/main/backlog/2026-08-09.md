# Backlog Index — 2026-08-09

- 문서 목적: 해당 날짜의 작업 항목(task) SSOT link 모음.
- 범위: 해당 일자(task 단위)의 모든 task.
- 대상 독자: AI agent (session-start / backlog-update), maintainer.
- 상태: stable (v0.14.0 append-only layout).
- 최종 수정일: 2026-08-09
- 관련 문서: [./tasks/](./tasks/) (per-task SSOT)

## Tasks

- **TASK-2026-08-09-main-001** [generic] memory 문서 정합성 정리 (v1.1.1-beta 세션 종료 잔여)
  - path: [`./tasks/TASK-2026-08-09-main-001.md`](./tasks/TASK-2026-08-09-main-001.md)
  - status: done
  - scope_creep_warnings: []
  - notes: 세션 시작 baseline 복원 중 발견한 어긋남 정리. backlog 인덱스 TASK-020 `planned` → `done` + notes 실제 결과 갱신 / 깨진 `path:` href 3건(018·019·020, `-08-main` 누락) 정정 / task 본문 `- 상태: planned` 7건(014~020) → `done` / state.json `backlog.done_items` 에 014~020 7건 + `recent_done_items` 에 018 추가 / handoff stale 4곳(§4 최근완료 10건 교체, §5 §0.8 잔존 3건 → close, §5 baseline `838b12f` 시점 명시, §6 `--force` 2-layer 반영). `check_self_application` **8/8 PASS** (이전 7/8 — `handoff_bloat` + `file_not_found` ×2). state.json 은 전면 재생성 안 함 — 재생성본이 `done_items` 를 10건으로 자르고 `current_focus` 를 끝난 TASK-014 로 잡아 정보 손실.

- **TASK-2026-08-09-main-002** [cli] CLI 化 B안 — 단일 dispatcher `wk`
  - path: [`./tasks/TASK-2026-08-09-main-002.md`](./tasks/TASK-2026-08-09-main-002.md)
  - status: done
  - scope_creep_warnings: []
  - notes: A안(v1.1.1) 의 29 binary 를 단일 `wk <name>` 으로. **새 dispatcher 를 만들지 않았다** — TASK-020 노트는 `tools/cli/dispatcher.py` 신설을 적었지만 `workflow_kit_cli.py` 가 이미 38 subcommand dispatcher 였고, 그대로 하면 진입점/`--help` 가 둘로 갈린다. 사용자 확인 후 기존 registry 확장. 정공법도 이미 있었다 — `score-wiki-trend`/`consumer-metrics` wrapper 의 *sys.argv 치환 + SystemExit → rc* 를 29개로 일반화. `TOOL_MODULES` 정본 ↔ `[project.scripts]` 일치를 smoke 가 강제. `main()` 시그니처 13:16 갈림은 `inspect.signature` 로 흡수 (29 파일 수정 회피). lazy 등록. 기존 wrapper 2건 미덮음. completion 은 `wk --list-commands` 소비. **10/10 PASS** + venv e2e (`wk` + 29 binary, 65 command). 발견 버그: `import importlib` 만으로 `importlib.util` 미부착 (우연히 동작 중이던 것을 mypy 가 잡음).

- **TASK-2026-08-09-main-003** [registry] registry HTTP server — federation *쓰기* 쪽
  - path: [`./tasks/TASK-2026-08-09-main-003.md`](./tasks/TASK-2026-08-09-main-003.md)
  - status: done
  - scope_creep_warnings: []
  - notes: TASK-016 이 닫은 pull 의 상대편. **구멍이 둘이었다** — 서빙하는 쪽이 없어 `http://` endpoint 가 문서상 형식일 뿐이었고, 더 조용하게는 `add_known_host()` API 가 TASK-015 부터 있었는데 **부르는 CLI 가 없어** 상대가 등록조차 못 했다. 둘 다 메움. 안전 기본값: loopback bind / read-only (쓰기 405) / 경로 2개만 (`SimpleHTTPRequestHandler` 미사용 — 디렉터리 노출 + traversal 회피) / 토큰은 **환경변수 이름** 으로 (`--token-env`, ps·history 회피) / registry 부재 → 빈 registry (404 면 상대가 호스트 사망으로 오해) / `Cache-Control: no-store` (오래된 뷰는 §0.8 #2 confidence 를 거짓말로 만듦). `KnownHost.token_env` additive (missing → "" 하위호환). **9/9 PASS** — 실제로 서버를 띄워 `pull_remote_registry()` 로 되받는다 (서버 단독 검사는 `_fetch_url` 계약 위반을 놓침).

- **TASK-2026-08-09-main-004** [safety] branch protection 자동 check — 3-layer defense 3rd layer
  - path: [`./tasks/TASK-2026-08-09-main-004.md`](./tasks/TASK-2026-08-09-main-004.md)
  - status: done
  - scope_creep_warnings: []
  - notes: layer 2 (pre-push hook) 는 로컬 설치형이라 hook 미설치 호스트는 통과한다. 그 구멍인 서버측 protection 이 *가이드* 로만 있었고 — 가이드는 확인하는 사람이 없으니 실질 layer 가 둘뿐이었다. 판정을 pure function 으로 분리해 gh 없이 fixture 로 검사 (실제 GitHub 응답 의존 검사는 네트워크/권한에 흔들려 회귀로 못 씀). **보호를 켜지 않는다 — 판정만** (§5D.4). **필드를 못 읽으면 통과로 치지 않는다** (권한 부족 `null` 과 실제 off 를 섞으면 거짓 안심). gh 부재는 graceful skip (모름 ≠ 없음), `--require-gh` 로 구분. **8/8 PASS**. 실측: 이 저장소 main 에 protection **없음**(404) — 켤지는 소유자 판단이라 보고만.

- **TASK-2026-08-09-main-005** [drift] title semantic drift v2 — §7.5 후속
  - path: [`./tasks/TASK-2026-08-09-main-005.md`](./tasks/TASK-2026-08-09-main-005.md)
  - status: done
  - scope_creep_warnings: []
  - notes: v1 은 TASK-ID *집합* 만 봐서 "TASK-001 계획 → TASK-001 완료" 면 내용이 통째로 바뀌어도 언제나 clean 이었다. v2 는 같은 ID 의 **제목** 을 `difflib` 로 비교해 후보를 고르고 판정은 LLM prompt 로 넘긴다 (`purpose_refresh` 와 같은 advisory 모델, API 직접 호출 ❌). 문자 기반 유사도 — 제목이 한국어+영어 식별자 혼합이라 공백 토큰화가 안 맞는다. `detect_scope_drift()` 에 `title_drift` **additive** (v1 필드 불변). **실측 함정**: 처음엔 ID *뒤* 만 제목으로 봤는데 handoff §5 는 `- **항목명** — ✅ 닫힘 (TASK-xxx, …)` 로 **ID 가 뒤에** 와서 `", 본 세션). 4-level enum + Panel 5"` 를 집었다 → ID 앞 텍스트 우선으로 수정 + 회귀 케이스 추가. **11/11 PASS**. 실측 후보 1건 (similarity 0.48) 은 같은 일의 표현 차이 — 임계 0.6 이 느슨한 쪽이라는 증거, advisory 라 그대로 둠.
