# Backlog Index — 2026-08-09

- 문서 목적: 해당 날짜의 작업 항목(task) SSOT link 모음.
- 범위: 해당 일자(task 단위)의 모든 task.
- 대상 독자: AI agent (session-start / backlog-update), maintainer.
- 상태: stable (v0.14.0 append-only layout).
- 최종 수정일: 2026-08-09
- 관련 문서: [./tasks/](./tasks/) (per-task SSOT)

## Tasks

- **TASK-2026-08-09-main-001** [generic] memory 문서 정합성 정리 (v1.1.1-beta 세션 종료 잔여)
  - path: [`./tasks/TASK-2026-08-09-main-001.md`](./tasks/TASK-2026-08-09-main-001.md)
  - status: done
  - scope_creep_warnings: []
  - notes: 세션 시작 baseline 복원 중 발견한 어긋남 정리. backlog 인덱스 TASK-020 `planned` → `done` + notes 실제 결과 갱신 / 깨진 `path:` href 3건(018·019·020, `-08-main` 누락) 정정 / task 본문 `- 상태: planned` 7건(014~020) → `done` / state.json `backlog.done_items` 에 014~020 7건 + `recent_done_items` 에 018 추가 / handoff stale 4곳(§4 최근완료 10건 교체, §5 §0.8 잔존 3건 → close, §5 baseline `838b12f` 시점 명시, §6 `--force` 2-layer 반영). `check_self_application` **8/8 PASS** (이전 7/8 — `handoff_bloat` + `file_not_found` ×2). state.json 은 전면 재생성 안 함 — 재생성본이 `done_items` 를 10건으로 자르고 `current_focus` 를 끝난 TASK-014 로 잡아 정보 손실.

- **TASK-2026-08-09-main-002** [cli] CLI 化 B안 — 단일 dispatcher `wk`
  - path: [`./tasks/TASK-2026-08-09-main-002.md`](./tasks/TASK-2026-08-09-main-002.md)
  - status: done
  - scope_creep_warnings: []
  - notes: A안(v1.1.1) 의 29 binary 를 단일 `wk <name>` 으로. **새 dispatcher 를 만들지 않았다** — TASK-020 노트는 `tools/cli/dispatcher.py` 신설을 적었지만 `workflow_kit_cli.py` 가 이미 38 subcommand dispatcher 였고, 그대로 하면 진입점/`--help` 가 둘로 갈린다. 사용자 확인 후 기존 registry 확장. 정공법도 이미 있었다 — `score-wiki-trend`/`consumer-metrics` wrapper 의 *sys.argv 치환 + SystemExit → rc* 를 29개로 일반화. `TOOL_MODULES` 정본 ↔ `[project.scripts]` 일치를 smoke 가 강제. `main()` 시그니처 13:16 갈림은 `inspect.signature` 로 흡수 (29 파일 수정 회피). lazy 등록. 기존 wrapper 2건 미덮음. completion 은 `wk --list-commands` 소비. **10/10 PASS** + venv e2e (`wk` + 29 binary, 65 command). 발견 버그: `import importlib` 만으로 `importlib.util` 미부착 (우연히 동작 중이던 것을 mypy 가 잡음).

- **TASK-2026-08-09-main-003** [registry] registry HTTP server — federation *쓰기* 쪽
  - path: [`./tasks/TASK-2026-08-09-main-003.md`](./tasks/TASK-2026-08-09-main-003.md)
  - status: done
  - scope_creep_warnings: []
  - notes: TASK-016 이 닫은 pull 의 상대편. **구멍이 둘이었다** — 서빙하는 쪽이 없어 `http://` endpoint 가 문서상 형식일 뿐이었고, 더 조용하게는 `add_known_host()` API 가 TASK-015 부터 있었는데 **부르는 CLI 가 없어** 상대가 등록조차 못 했다. 둘 다 메움. 안전 기본값: loopback bind / read-only (쓰기 405) / 경로 2개만 (`SimpleHTTPRequestHandler` 미사용 — 디렉터리 노출 + traversal 회피) / 토큰은 **환경변수 이름** 으로 (`--token-env`, ps·history 회피) / registry 부재 → 빈 registry (404 면 상대가 호스트 사망으로 오해) / `Cache-Control: no-store` (오래된 뷰는 §0.8 #2 confidence 를 거짓말로 만듦). `KnownHost.token_env` additive (missing → "" 하위호환). **9/9 PASS** — 실제로 서버를 띄워 `pull_remote_registry()` 로 되받는다 (서버 단독 검사는 `_fetch_url` 계약 위반을 놓침).

- **TASK-2026-08-09-main-004** [safety] branch protection 자동 check — 3-layer defense 3rd layer
  - path: [`./tasks/TASK-2026-08-09-main-004.md`](./tasks/TASK-2026-08-09-main-004.md)
  - status: done
  - scope_creep_warnings: []
  - notes: layer 2 (pre-push hook) 는 로컬 설치형이라 hook 미설치 호스트는 통과한다. 그 구멍인 서버측 protection 이 *가이드* 로만 있었고 — 가이드는 확인하는 사람이 없으니 실질 layer 가 둘뿐이었다. 판정을 pure function 으로 분리해 gh 없이 fixture 로 검사 (실제 GitHub 응답 의존 검사는 네트워크/권한에 흔들려 회귀로 못 씀). **보호를 켜지 않는다 — 판정만** (§5D.4). **필드를 못 읽으면 통과로 치지 않는다** (권한 부족 `null` 과 실제 off 를 섞으면 거짓 안심). gh 부재는 graceful skip (모름 ≠ 없음), `--require-gh` 로 구분. **8/8 PASS**. 실측: 이 저장소 main 에 protection **없음**(404) — 켤지는 소유자 판단이라 보고만.

- **TASK-2026-08-09-main-005** [drift] title semantic drift v2 — §7.5 후속
  - path: [`./tasks/TASK-2026-08-09-main-005.md`](./tasks/TASK-2026-08-09-main-005.md)
  - status: done
  - scope_creep_warnings: []
  - notes: v1 은 TASK-ID *집합* 만 봐서 "TASK-001 계획 → TASK-001 완료" 면 내용이 통째로 바뀌어도 언제나 clean 이었다. v2 는 같은 ID 의 **제목** 을 `difflib` 로 비교해 후보를 고르고 판정은 LLM prompt 로 넘긴다 (`purpose_refresh` 와 같은 advisory 모델, API 직접 호출 ❌). 문자 기반 유사도 — 제목이 한국어+영어 식별자 혼합이라 공백 토큰화가 안 맞는다. `detect_scope_drift()` 에 `title_drift` **additive** (v1 필드 불변). **실측 함정**: 처음엔 ID *뒤* 만 제목으로 봤는데 handoff §5 는 `- **항목명** — ✅ 닫힘 (TASK-xxx, …)` 로 **ID 가 뒤에** 와서 `", 본 세션). 4-level enum + Panel 5"` 를 집었다 → ID 앞 텍스트 우선으로 수정 + 회귀 케이스 추가. **11/11 PASS**. 실측 후보 1건 (similarity 0.48) 은 같은 일의 표현 차이 — 임계 0.6 이 느슨한 쪽이라는 증거, advisory 라 그대로 둠.

- **TASK-2026-08-09-main-006** [fix] rotate 도구 수정 (순서 규약 통일) + 사전 존재 red 검토
  - path: [`./tasks/TASK-2026-08-09-main-006.md`](./tasks/TASK-2026-08-09-main-006.md)
  - status: done
  - scope_creep_warnings: []
  - notes: **(1) rotate** — 두 문서가 같은 사실을 반대 순서로 들고 있었다: `state.json.recent_done_items` 는 최신-앞(`check_recent_done_items_order` 계약 1), handoff §4 writer 는 뒤-최신(`append`), **실제 문서는 최신-앞**. 문서와 state.json 이 일치하고 writer 만 반대라 writer 를 고쳤다 (`insert(0)` + `[:CAP]`, `check_handoff_done_cap` 계약 2 서술·기대값 반전). `rotation.py` 결함 2건: 섹션을 고정 문자열(`## 5.`/`## 6. 잔여 작업`)로 찾아 늘 error + `items[-max:]` 로 뒤를 남김 — **1번만 고쳤으면 도구가 동작하면서 최신을 지웠다**. 번호 무관 매칭 + 다음 `## ` 까지 + `newest_first=True`. baseline 오염 제거 (잘린 항목을 `- 현재 기준선:` 에 이어붙이던 것). `check_handoff_rotation.py` **9/9 PASS** 신규 — 이 도구엔 회귀 검사가 없었다. **(2) red 검토** — **결과를 두 번 잘못 셌다**: `[FAIL]` 패턴만 grep 해 7건이라 한 것과, 그다음 전체 실행 두 번(31/24건)이 **내가 편집하는 도중에** 돌아 반쯤 바뀐 트리를 본 것. 편집을 멈추고 돌린 최종 실행이 유일하게 유효하다 — **red 5건, 전부 사전 존재, 내가 만든 red 0건**. stamp 계열 7건은 전부 사전 존재(`git stash` 확인), 6건 해소: README/RELEASE/CODE_INDEX/INSTALLATION stamp + sample `tool_version` 24건 + mcp 는 환경 문제로 실증(설치 후 2/2 PASS). 신규 2건은 내가 만들어 같은 커밋에서 닫았다 — `check_migration_group_heading`(task 파일 `### (숫자)` heading) + `check_no_repo_write`(**`check_cli_wrappers` 가 저장소 실제 handoff 를 수정하고 있었다** — rotate 가 고장나 있어 드러나지 않던 것; 호출마다 tempdir 복사본 + `WATCHED_CHECKS` 등록). 1건 보류 — `check_smoke_trend_cross_v0_15_5` 는 v1.1.0/v1.1.1 노트에 *누적 smoke* 줄이 없어 v1.0.0 의 234 를 읽는다. 고치려면 발행된 노트를 사후 수정해야 해서 릴리스 사이클에 맡긴다. 배운 것: **회귀 검사 없는 도구는 "동작 안 함"조차 안 드러난다** — `check_cli_wrappers` 는 CLI↔MCP output 일치만 봐서 둘 다 `error` 를 내면 PASS 였다.

- **TASK-2026-08-09-main-007** [fix] 남은 red 4건 close (FULL mypy strict 복구 포함)
  - path: [`./tasks/TASK-2026-08-09-main-007.md`](./tasks/TASK-2026-08-09-main-007.md)
  - status: done
  - scope_creep_warnings: []
  - notes: TASK-006 이 "범위 밖"으로 남긴 4건을 전부 닫음 — 남긴 근거가 **내가 잘못 센 숫자**였다. (1) `check_source_without_runtime_layer` → `read_only_*.json` fixture 3건의 `tool_version` 이 `v1.0.0-beta`, TASK-006 stamp 6건과 **같은 뿌리인데 놓친 것**. 재생성. (2) `check_tempdir_leak_guard` → 정리 없는 `mkdtemp` 11건(6 파일에 `atexit` 헬퍼) + case_3 이 `"/var/tmp"` 를 문자열로 찾아 macOS(`/private/var/tmp`)에서 늘 red — **구현은 정상, 검사가 플랫폼을 못 넘긴 것**. (3) `check_wiki_url_validity` → worker 가 local function 이라 spawn(macOS)에서 `PicklingError`; **Linux 에서만 돌던 검사**였다. 모듈 레벨로 이동, 39/39. (4) `check_workflow_kit_cli` → `release-doctor` 의 mypy gate 가 red 였고 뿌리는 `workspace_registry.py` **mypy 24건** — **FULL mypy strict 가 깨져 있었다**. 전부 정리해 `workflow_kit/` **128 files clean** 복구. 부수: R3 위반 1건 — `survey()` 가 `repo_root` 를 받는데 브랜치는 `get_current_branch()`(모듈 앵커)로 얻어 `--repo-root` 지목 시 엉뚱한 브랜치를 가리켰다. `paths.branch_slug_for(repo_root)` 신설로 정정, 11/11. **효과**: Phase 13 P0-1 acceptance 실측 충족 + dashboard `guard_status` fail → **pass**. 잔여 red = 환경 3건(시스템 mypy 미설치, venv 에서 PASS) + `check_smoke_trend_cross` 1건(릴리스 대기).

- **TASK-2026-08-09-main-008** [release] v1.1.2-beta release
  - path: [`./tasks/TASK-2026-08-09-main-008.md`](./tasks/TASK-2026-08-09-main-008.md)
  - status: done
  - scope_creep_warnings: []
  - notes: 본 세션 TASK-001~007 을 묶어 발행. 릴리스 하나로 셋이 함께 닫혔다 — (1) **`check_smoke_trend_cross`** 마지막 실질 red (`cumulative_total` 이 *가장 최근 릴리스 노트* 에서 파싱되는데 v1.1.0/v1.1.1 노트에 *누적 smoke* 줄이 없어 v1.0.0 의 234 를 읽고 있었다 → 257), (2) **Phase 13 P0-1** (mypy strict venv 직접 verify — `workflow_kit/` 128 files clean 실측), (3) 문서 stamp 정합을 릴리스 절차 안에서 확정. 최종 전체 검사 **257/257 PASS**(308s) — 오늘 아침 baseline 31 red → 0. 노트 판정 문단은 이번 사이클 성격을 *"기능 추가보다 가려져 있던 사실의 노출"* 로 적었다. **발견한 도구 결함**: `release-bump --apply` 의 post-step `sync_release_hash` 가 `git add` 에 경로를 중복 prefix(`workflow-source/workflow-source/…`)로 넘겨 실패한다 — 버전 파일 2개는 정상 갱신되고 잘못된 amend 는 실행되지 않아 릴리스는 막히지 않지만 자동화가 반쪽. 릴리스 도중에 릴리스 도구를 고치지 않기로 하고 후속 분리. **일회성 red 1건**: `check_release_pipeline_lib` 의 dist skip 테스트가 버전 bump 직후 첫 실행에서만 실패(현재 버전 artifact 부재) → 재실행 9/9.

- **TASK-2026-08-09-main-009** [fix] 릴리스 도구 결함 2건 수정 + 회귀 검사 신설
  - path: [`./tasks/TASK-2026-08-09-main-009.md`](./tasks/TASK-2026-08-09-main-009.md)
  - status: done
  - scope_creep_warnings: []
  - notes: v1.1.2 발행 도중 드러난 2건. **(1) `git add` 경로 중복** — `release_pipeline.REPO_ROOT` 는 이름과 달리 `workflow-source/`(`parents[1]`) 인데 `git status --porcelain` 은 **저장소 루트 기준** 경로를 준다 → `workflow-source/workflow-source/…`. `_git_toplevel()` 신설 후 git 경로를 다루는 세 자리(status / add / amend)가 그걸 cwd 로 쓰게 함. `REPO_ROOT` 이름이 거짓인 이유는 주석으로 남김(다른 용도로는 그 값이 맞아 이름만 고치면 광범위). **(2) `cmd_verify` AttributeError 'dry_run'** — `_make_args` defaults 에 `dry_run: True`(안전측) 추가 + 조회가 목적인 `cmd_verify` 는 `dry_run=False` 명시(True 면 gh 명령 문자열만 반환). **(3) 회귀 검사 `check_release_wrapper_args.py` 8/8 신설** — 교훈이 *"릴리스 도구는 릴리스 때만 돌아 평소 검사에 안 걸린다"* 였으므로 릴리스 없이 잡히게: `_make_args` ↔ `args.X` AST 대조 / `_git_toplevel()` ≠ `REPO_ROOT` / dirty path 를 두 cwd 에서 `git add --dry-run` (toplevel `rc=0`, `workflow-source/` `rc=128` — **버그 자체를 회귀로 고정**) / read-only wrapper 4종 실호출. **검사 오탐 1건 고침**: 첫 버전이 `cmd_gen_schema` 의 `args.check` 를 결함으로 보고 — 그건 argparse 로만 불려 `_make_args` 를 안 거친다. **검사 범위가 실제 호출 경로보다 넓으면 없는 결함을 만든다.**
