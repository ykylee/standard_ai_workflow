---
id: TASK-2026-07-22-002
status: done
created_at: 2026-07-22
source_anchor: post-release-hardening-v1-0-0
source_path: backlog/2026-07-22.md
kind: generic
---

# TASK-2026-07-22-002 — **v1.0.0 발행 후 보강 — "지표가 무엇을 재고 있는가" 3층 점검** (노트 §2.15~§2.20). 릴리스 후 발견/보강 6건: (1) §2.15 auto-bump 이 dry-run 에서 version 을 write (릴리스 직후 발현); (2) §2.16 실행 표면 / 저장소 오염 메타 체크 2종 + `release_status` 의 `.rstrip("-beta")` 문자집합 strip 버그; (3) §2.17 memory-freeze 조건 충족 후 stable 승격 + wiki L2 계층 복구 (과정에서 lifecycle 지표가 잘못 정의돼 있었음이 드러나 `last_touched` 신선도로 재정의); (4) §2.18 maturity registry 정합 메타 체크 신규 + 드리프트 3건 해소 (디렉터리명 규약 이탈 / 등재됐으나 디스크 부재 / 디스크에 있으나 미등재); (5) **§2.19 Panel 1 의 두 지표가 구조적으로 무의미했다** — `maturity_stale` 을 달력(`last_updated != 오늘`)에서 **drift**(maturity surface commit > 선언)로, north-star `silent_failing_cycles_count` 를 freshness proxy 에서 **원장**(`drift_ledger.jsonl`, 원장이 비면 `미측정`)으로 재정의; (6) **§2.20 state.json 이 2026-07-21 이후 갱신되지 않고 있었다** — branch-scoped 전환에서 hint 만 옮겨지고 writer 가 legacy 경로에 남아, refresh 가 아무도 읽지 않는 파일을 만들면서 `"refreshed"` 를 보고하던 silent failing + `backlog-update` 가 `--apply` 없이 write 하던 dry-run 오염. 신규 smoke 5종 (누적 199 → 205/205). breaking change: ❌.

## 📝 Description

- 출처: `[[backlog/2026-07-22.md]] {#post-release-hardening-v1-0-0}`
- 분류: `generic` (릴리스 없이 main 에 누적된 보강 사이클)
- 작성일: 2026-07-22

## 🛠️ Implementation / Content

- commit `08391f2` §2.15 기록 · `95d3034` 메타 체크 2종 + suffix strip 버그 ·
  `dcbf2af` memory-freeze stable + wiki L2 복구 · `43079c0` registry 정합 메타 체크 ·
  `dbc4ec1` north-star / maturity_stale 재정의 · `ce81fa7` state cache 경로 결함.
- 신규 smoke: `check_executable_surface` · `check_repo_pollution` ·
  `check_maturity_registry` · `check_north_star_metric` ·
  `check_state_cache_branch_scoped_write`.

## ✅ Outcome

- 전량 smoke **205/205 PASS** (test case 219 PASS / 0 FAIL), mypy strict 117 files
  0 errors, 전량 실행 후 저장소 오염 0.
- 세 층의 메타 체크가 자리를 잡았다: **실행 가능한가 / 선언이 사실인가 /
  저장소를 오염시키지 않는가**, 그리고 그 위에 **지표가 무엇을 재고 있는가**.
- 모든 재발 방지 test 는 **버그 코드를 되돌려 주입해 실제로 FAIL 하는 것까지**
  확인했다.

## ⏭️ 후속 작업 (declared, not silently carried)

- ✅ **closed (`d285077`, 노트 §2.21)** — 아래 항목 해소. 정본 ID 패턴을
  `project_docs.TASK_ID_PATTERN` 단일 출처로 모으고, `--kind` flag 추가,
  `check_backlog_update_layout.py` 5 case 신규 (구 writer 에서 전부 FAIL 확인).
- **`backlog-update` skill 의 출력 layout 이 v0.14.0+ append-only 와 어긋난다.**
  `--apply` 가 legacy 인라인 daily index (`## TASK-… - 상태:` 본문 + `../work_backlog.md`
  링크) 와 `YYYY-MM-DD_TASK-….md` 파일명을 쓴다. 현행 layout 은 link-only index +
  `TASK-<date>-<NNN>.md` + frontmatter 다. stable 로 선언된 skill 이 governance 가
  규정한 layout 을 못 만드는 상태 — §2.18 의 "선언이 사실인가" 와 같은 부류.
  본 사이클의 메모리 파일은 그래서 layout 에 맞춰 **직접 작성**했다.
