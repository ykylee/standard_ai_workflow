---
id: TASK-2026-07-22-003
status: done
created_at: 2026-07-22
source_anchor: structural-defense-v1-0-0
source_path: backlog/2026-07-22.md
kind: generic
---

# TASK-2026-07-22-003 — **반복되던 결함의 구조적 방어 3층 + 같은 부류 전수 조사** (노트 §2.21~§2.25). TASK-2026-07-22-002 의 §2.19/§2.20 을 나란히 놓자 *같은 사실이 두 곳에 있는데 둘을 이어주는 기계적 장치가 없다* 는 하나의 결함으로 드러났고, 부품별 테스트는 전부 green 인데 조립하면 안 맞는 상태였다. **5 deliverable**: (1) **§2.21 backlog-update layout 정합** — stable 선언 skill 이 v0.14.0 append-only layout 을 만들지 못했다(task 파일명 / index 인라인 / `.bak` 재생성). 근본 원인은 task ID 정규식이 4곳에 복제돼 branch-scoped ID 를 셋이 인식 못 한 것 → `project_docs.TASK_ID_PATTERN` 단일 출처화 + `--kind` flag; (2) **§2.22 writer↔reader 왕복 계약 8쌍** — 프로덕션 writer 로 쓰고 프로덕션 reader 로 되읽는다. §2.20/§2.21 결함을 되돌려 주입해 해당 쌍이 FAIL 하는 것 확인; (3) **§2.23 판정 지표는 근거를 함께 낸다** — `JUDGMENT_METRICS` registry + `*_source` / `*_measured` 강제. 도입하며 Panel 6 north-star 도 같은 결함(git 실패를 삼키고 0 을 pass 로 보고)이었음이 드러나 `status: unknown` 도입; (4) **§2.24 규약 단일 출처 검사** — 정본 모듈이거나 정본 symbol 을 import 하거나. 도입 즉시 사본 7건 검출(4 skill 이 `purpose_digest` 없이 동작 / release validate 게이트 조용히 통과 / sync_release_hash 가 안 읽히는 파일에 기록); (5) **§2.25 같은 부류 전수 조사** — AST 로 문자열 상수 빈도를 내니 `ai-workflow`(28) > `memory`(21) > `active`(19) ≫ `state.json`(12) 로, 앞서 고친 것이 더 넓은 문제의 한 조각이었다. `memory_dir_for_workspace` / `memory_active_dir` 신규 + 14개 모듈 조립 교체 + 네 번째 규약 등록. 전량 smoke **203 → 209/209**, mypy strict 117 files 0 errors, 저장소 오염 0. breaking change: ❌.

## 📝 Description

- 출처: `[[backlog/2026-07-22.md]] {#structural-defense-v1-0-0}`
- 분류: `generic` (릴리스 없이 main 에 누적된 구조 보강)
- 작성일: 2026-07-22

## 🛠️ Implementation / Content

- `d285077` §2.21 backlog-update layout + task ID 단일 출처
- `02161a3` §2.22 왕복 계약 8쌍 (`check_writer_reader_roundtrip.py`)
- `d5be282` §2.23 판정 지표 근거 (`check_metric_source_contract.py`) + Panel 6 수정
- `fe6fcfa` §2.24 규약 단일 출처 (`check_convention_single_source.py`) + 사본 7건 해소
- `0116661` §2.25 memory 경로 조립 14곳 전수 교체 + 네 번째 규약 등록

## ✅ Outcome

- 세 겹 방어가 섰다: **조립이 맞는가(§2.22) / 근거를 말하는가(§2.23) / 애초에 사본이
  생기지 못하게(§2.24)**. 셋 다 **결함을 되돌려 주입해 실제로 FAIL 하는 것까지** 확인.
- 각 방어의 **한계를 노트와 docstring 에 명시**했다 — 근거 이름을 그럴듯하게 지으면
  §2.23 은 통과하고, §2.24 는 판정이 파일 단위라 같은 파일 다른 줄의 조립은 놓친다.
  과장하지 않는 대신 규약 등록 시 **한 번의 전수 조사**를 절차로 못박았다.
- 부수 발견: 4개 skill 이 `purpose_digest` 없이 동작하고 있었고(실측 확인),
  release validate 의 state.json freshness 게이트가 조용히 통과하고 있었다.

## ⏭️ 후속 작업 (declared, not silently carried)

- **파일명 상수 계열은 미등록**. 전수 조사에서 `work_backlog.md`(10 파일) /
  `PURPOSE.md`(6 파일) 등이 나왔으나, 디렉터리와 달리 파일명은 틀리면 같은 디렉터리
  안에서 눈에 띄게 깨지므로 이번 범위에서 제외했다. 필요해지면 다섯 번째 규약 후보.
- **north-star 는 아직 `미측정`**. `drift_ledger.jsonl` 은 다음 릴리스에서 release
  pipeline 이 첫 entry 를 쓸 때부터 채워진다. 그때 `미측정` → 실측 전환을 확인할 것.
