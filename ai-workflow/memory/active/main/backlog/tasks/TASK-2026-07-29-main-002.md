---
id: TASK-2026-07-29-main-002
status: done
created_at: 2026-07-29
source_anchor: generic-task-2026-07-29-main-002
source_path: backlog/2026-07-29.md
kind: generic
---

# TASK-2026-07-29-main-002 — ignore_missing_imports 가 사라진 optional dep 을 Any 로 덮는다 — 탐지층

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-07-29
- Owner: ykylee
- Host:
- Host IP:
- Affected documents:
  - 

- Description: 판정을 mypy 에서 런타임 import 로 옮겼다. optional dep import 정본 + 탐지 검사 신규.
- Completion criteria:

## 🛠️ Implementation / Content

- Progress: common/optional_deps.py 정본(required/alternative 구분) + check_optional_dep_imports.py 6건 + read_only_mcp_sdk 의 사본 제거.
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: 판정 기준을 먼저 정했다: ignore_missing_imports 를 좁히지 않는다. optional dep 은 실제로 optional 이라 mcp.* 만 빼면 SDK 미설치 로컬이 red 가 되고, 더 근본적으로 mypy 는 '안 깔림' 과 '깔렸는데 모듈이 사라짐' 을 구분할 수 없다 — 그 구분은 런타임 import 에서만 된다. 그래서 설정은 두고 판정을 옮겼다. import 대상 정본은 required(전부)/alternative(하나만) 두 종류로 나눴다 — 이 구분이 없으면 2.x 환경에서 검사가 틀린 실패를 낸다. skip 은 조용히 넘기지 않고 몇 건을 왜 건너뛰었는지 출력한다.
- Verification: 완료 기준 확인 — 이관 전 가정(fastmcp 를 required 로) 되주입 후 mcp 1.28.1 은 6/6 PASS, mcp 2.0.0 은 "'mcp.server.fastmcp' 모듈이 없다 (ModuleNotFoundError)" 로 실패. 같은 상황에서 mypy 가 냈던 no-any-return 과 대비된다. 되주입 4건이 각각 다른 증상으로 실패(이관 전 가정 / registry 누락 / note 없는 빈 extra / 소비자 사본). mcp 미설치 venv 에서 skip 6건이 출력으로 드러남. mypy strict 1.27.0/1.28.1/1.29.0/2.0.0 각 120 files 0 errors. 전량 smoke 223/223.
- Follow-up: smoke 가 requirements-dev.txt 설치 순서 때문에 1.x 로 도는 것은 여전히 우연 — 의도적 matrix 는 미결. dev/release extra 는 import 대상이 없어 note 로만 남겼다(subprocess 전용).
