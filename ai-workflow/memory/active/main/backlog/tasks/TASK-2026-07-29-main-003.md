---
id: TASK-2026-07-29-main-003
status: done
created_at: 2026-07-29
source_anchor: generic-task-2026-07-29-main-003
source_path: backlog/2026-07-29.md
kind: generic
---

# TASK-2026-07-29-main-003 — read_only_mcp_sdk lowlevel 이관 — decorator → add_request_handler

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-07-29
- 담당: ykylee
- 호스트명:
- 호스트 IP:
- 영향 문서:
  - 

- 작업 내용: 1.x decorator / 2.x handler 등록 양립 완료. 상한 핀 해제.
- 완료 기준:

## 🛠️ Implementation / Content

- 진행 현황: uses_handler_registration(hasattr add_request_handler)로 분기. 2.x 는 tools/list↔PaginatedRequestParams, tools/call↔CallToolRequestParams 등록 + ListToolsResult/CallToolResult 반환. check_mcp_lowlevel_sdk_compat.py 8건 신규. 상한 핀 해제.
- 다음 세션 시작 포인트:
- 남은 리스크:

## ✅ Outcome

- 작업 결과: 분기 기준을 버전 문자열이 아니라 계약의 존재로 잡았다. Tool/CallToolResult 는 2.x 가 snake_case 로 바꿨지만 camel alias 로 양쪽을 받아 payload 조립은 갈라 쓰지 않았다. 한 곳만 갈랐다 — result.isError 나중 대입은 2.x 에서 is_error 라 빗나가므로 생성 시점 force_error 로 이동(실패한 tool 호출이 성공으로 보고될 자리였다). import 문법 4가지로 전수 조사해 표면이 정확히 2개임을 먼저 확인한 뒤 핀 해제.
- 검증 결과: 프로토콜 왕복 실측 — 1.28.1/2.0.0 각각 인스펙터로 tools/list 13개 + tools/call 성공(isError=false)·실패(isError=true) 경로, 두 버전 wire 산출물이 JSON 동일. mypy strict 1.27.0/1.28.1/1.29.0/2.0.0 각 119 files 0 errors. 되주입 2건이 각각 다른 증상으로 실패(2.x 분기 제거 → 프로덕션과 동일한 AttributeError 로 5건, isError 나중 대입 → 해당 1건). 전량 smoke 222/222.
- 후속 작업: TASK-2026-07-29-main-002(ignore_missing_imports 탐지 구멍) 미착수. smoke 가 requirements-dev.txt 때문에 1.27.0 으로 도는 것은 여전히 설치 순서 의존 — 다만 핀 해제로 mypy-strict/mcp-inspector 는 2.x 를 밟아 두 major 가 CI 에서 동시에 덮인다.
