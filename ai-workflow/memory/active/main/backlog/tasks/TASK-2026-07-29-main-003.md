---
id: TASK-2026-07-29-main-003
status: planned
created_at: 2026-07-29
source_anchor: generic-task-2026-07-29-main-003
source_path: backlog/2026-07-29.md
kind: generic
---

# TASK-2026-07-29-main-003 — read_only_mcp_sdk lowlevel 이관 — decorator → add_request_handler

## 📝 Description

- 상태: planned
- 우선순위: high
- 요청일: 2026-07-29
- 담당: ykylee
- 호스트명:
- 호스트 IP:
- 영향 문서:
  - `workflow-source/workflow_kit/server/read_only_mcp_sdk.py`
  - `workflow-source/pyproject.toml`
  - `.github/workflows/mcp-inspector.yml`

- 작업 내용: mcp 2.0.0 의 mcp.server.lowlevel.Server 가 list_tools/call_tool decorator 를 없애고 add_request_handler(method, params_type, handler) 로 바꿨다. read_only_mcp_sdk.py 의 build_lowlevel_server() 가 2.0.0 에서 AttributeError 로 죽는다 (로컬 재현: 1.28.1 정상 / 2.0.0 실패). mcp-inspector workflow 만 잡는 파손이고, 그 workflow 는 server/** 가 바뀔 때만 돈다.
- 완료 기준: 1.x/2.x 양쪽에서 initialize + tools/list + tools/call 왕복이 성공하고, tools/list 결과가 커밋된 descriptor 와 일치하며(mcp-inspector 가 보는 그 대조), 상한 핀을 풀어도 mcp-inspector 포함 CI 4종이 green

## 🛠️ Implementation / Content

- 진행 현황: `2026-07-29 06:03` 기준 mcp 2.0.0 의 mcp.server.lowlevel.Server 가 list_tools/call_tool decorator 를 없애고 add_request_handler(method, params_type, handler) 로 바꿨다. read_only_mcp_sdk.py 의 build_lowlevel_server() 가 2.0.0 에서 AttributeError 로 죽는다 (로컬 재현: 1.28.1 정상 / 2.0.0 실패). mcp-inspector workflow 만 잡는 파손이고, 그 workflow 는 server/** 가 바뀔 때만 돈다.
- 다음 세션 시작 포인트:
- 남은 리스크: get_capabilities/InitializationOptions/CallToolResult 의 2.x 계약이 미확인. handler 반환 타입(ServerResult 계열) 확인 필요. 프로토콜 왕복을 실제로 돌려 보지 않으면 '그럴듯하지만 틀린' 구현이 통과할 수 있다 — 파일 모양 검사(check_mcp_tool_descriptors)는 이 층을 대신하지 못한다.

## ✅ Outcome

- 작업 결과:
- 후속 작업:
