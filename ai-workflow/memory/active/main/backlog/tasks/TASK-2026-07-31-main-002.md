---
id: TASK-2026-07-31-main-002
status: done
created_at: 2026-07-31
source_anchor: generic-task-2026-07-31-main-002
source_path: backlog/2026-07-31.md
kind: generic
---

# TASK-2026-07-31-main-002 — 파생물의 상한과 포인터 — 쓰는 쪽이 규약을 모르고 있었다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-07-31
- Owner: ykylee
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/common/project_docs.py`
  - `workflow-source/workflow_kit/common/workflow_writes.py`
  - `workflow-source/workflow_kit/common/state/builder.py`
  - `workflow-source/workflow_kit/common/linter.py`
  - `workflow-source/skills/workflow-linter/scripts/run_workflow_linter.py`
  - `workflow-source/tests/check_handoff_done_cap.py`
  - `workflow-source/tests/check_state_backlog_block.py`
  - `workflow-source/releases/Beta-v1.0.0.md`
  - `docs/CODE_INDEX.md`
  - `docs/INSTALLATION_AND_USAGE.md`

- Description: handoff §4 상한을 쓰는 쪽에 적용하고, append-only layout 에서 latest_backlog_path / task_count 가 사실을 적게 한다.
- Completion criteria: 쓰는 쪽이 상한을 적용하고(가장 오래된 것부터 버림), 상한 정본이 한 곳이며, append-only layout 에서 latest_backlog_path 가 실재 파일을 가리키고 task_count 가 그 파일의 task 수와 일치한다. 되주입 각각이 다른 신호로 실패한다.

## 🛠️ Implementation / Content

- Progress: 상한 정본을 common/project_docs.RECENT_DONE_ITEMS_CAP 으로 옮기고 writer/builder/linter 가 같은 이름을 읽게 했다. sync_handoff_status 가 §4 에만 상한 적용(가장 오래된 것부터 버림). builder 의 경로 해석을 (명시 인자 / legacy index / daily 디렉터리 최신) 세 갈래로 분리하고 실재할 때만 채택. current_focus fallback 은 미완료만 고른다. run_workflow_linter 는 workspace root 기준으로 해석.
- Next session starting point: CI 실측(smoke·mypy-strict·mkdocs·actionlint)
- Remaining risks: 린터 경로 해석은 실재 확인 가드가 실질적으로 막아 준다 — 기준 경로만 되돌리면 아무 검사도 실패하지 않는다(둘을 함께 되돌려야 재현).

## ✅ Outcome

- Result: 두 결함 다 '파생물을 만드는 쪽이 규약을 모른다' 였다. (1) 상한을 아는 자리가 셋인데 쓰는 쪽만 몰라 close-out 마다 사람이 11번째 줄을 지웠다(2회 연속 재발). (2) 경로 해석 세 갈래가 legacy_index_present 하나에 매달려, 명시한 --latest-backlog-path 인자까지 버려지고 backlog block 이 통째로 죽어 있었다(task 107건 저장소에서 task_count: 0). 죽은 필드가 소비자까지 얼려 두고 있었다 — 살리자 current_focus 가 완료 task 를 집는 것과 린터의 경로 겹침 2건이 곧바로 드러나 같이 고쳤다.
- Verification: 전량 smoke 226/226 PASS(dev,release,mcp-sdk venv, run_all_checks --tmp-dir=실디스크, abort 0, 워킹트리 변경 0). mypy strict 121 files 0 errors(mypy 2.1.0, --config-file 명시). 되주입 9건 각각 다른 신호로 실패. 실저장소 실측: handoff §4 가 11이 되지 않고 10 유지, state.json 의 latest_backlog_path/task_count/done_items 실값 전환, workflow-linter 자기 저장소 issue 0.
- Follow-up: handoff §4 는 상한만 생겼고 정렬 기준은 없다(legacy 저장소에서는 builder 가 앞에서 자르므로 오래된 것이 남는다). run_workflow_linter 의 project_root 가 저장소 루트보다 한 단계 위를 가리켜 [tool.workflow-doctor] 가 default 로 떨어지는 것은 별건으로 남겼다.
