---
id: TASK-2026-07-31-main-003
status: done
created_at: 2026-07-31
source_anchor: generic-task-2026-07-31-main-003
source_path: backlog/2026-07-31.md
kind: generic
---

# TASK-2026-07-31-main-003 — 기준 경로가 한 칸 어긋나 있었다 — 린터의 설정과 maturity

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-07-31
- Owner: ykylee
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/common/metadata.py`
  - `workflow-source/workflow_kit/common/__init__.py`
  - `workflow-source/skills/workflow-linter/scripts/run_workflow_linter.py`
  - `workflow-source/tests/check_linter_config_resolution.py`
  - `workflow-source/tests/check_v0_7_15_config_thresholds.py`
  - `workflow-source/releases/Beta-v1.0.0.md`
  - `docs/PROJECT_PROFILE.md`
  - `docs/CODE_INDEX.md`
  - `docs/INSTALLATION_AND_USAGE.md`

- Description: §2.46 이 별건으로 남긴 `run_workflow_linter.py` 의 기준 경로(`parent.parent.parent`)를 workspace root 로 바로잡고, 설정이 조용히 기본값으로 떨어지는 것을 산출물에서 구별 가능하게 한다.
- Completion criteria: (1) `project_root` 가 workspace root 다. (2) 선언한 `excluded_paths` 가 실제 판정까지 도달한다. (3) 물어본 경로/출처/기본값 이유가 `source_context` 에 남는다. (4) `--maturity` 가 실행되지 못하면 통과로 보고하지 않는다. (5) 되주입 각각이 서로 다른 신호로 실패한다.

## 🛠️ Implementation / Content

- Progress: `project_workspace_root` 로 기준 통일 + `load_config_with_provenance`(consulted/path/source/reason) 신설 + `--config-path` / `--maturity-path` 신설 + `maturity_check_not_run`(high) 노출. 검사 1종 신규(`check_linter_config_resolution.py` 9 case) + `check_v0_7_15_config_thresholds.py` 9번째 case 를 문자열 검사 → 동작 검사로 교체.
- Next session starting point: CI 실측 (smoke·mypy-strict·mkdocs)
- Remaining risks: `--maturity` 가 처음으로 실제로 돌면서 드러난 내용 드리프트 2건(matrix 의 `Phase 13` in_progress vs roadmap, `task-modes` test_path 부재)은 이 커밋 범위 밖 — 드러낸 채로 후속에 남긴다.

## ✅ Outcome

- Result: 기준이 한 칸 어긋나 있었고(`docs/PROJECT_PROFILE.md` 에서 `parent.parent.parent` 는 root 가 아니라 root 의 위), 그 값이 두 곳으로 갔다 — `load_config` 는 언제나 기본값이었고(`[tool.workflow-doctor]` 가 v0.7.15 도입 이래 한 번도 적용된 적이 없다), `--maturity` 는 늘 `skipped` 인데 runner 가 `issues_found` 만 반영해 **실행되지 못한 검사가 `status: ok / total_issues: 0`** 으로 보고됐다(v0.11.17 backlog 에 "정합 검증 통과" 로 기록). 둘 다 조용했던 이유는 같다 — 실패하지 않는 loader 가 "적용됨" 과 "떨어짐" 을 같은 모양으로 만들었다. `load_config_with_provenance` 로 물어본 경로/얻은 파일/출처/기본값 이유를 함께 내놓고 산출물의 `source_context` 에 남긴다. 사본을 늘리지 않고 호출을 명시하는 쪽(`--config-path`)을 택했다.
- Verification: 전량 smoke **227/227 PASS**(dev,release,mcp-sdk venv, `run_all_checks --tmp-dir=/var/tmp/saw-smoke`, abort 0). mypy strict **121 files 0 errors**(mypy 2.1.0, `--config-file` 명시 + `Config File:` 줄로 로드 확인). 신규 `check_linter_config_resolution.py` 9/9, 교체한 `check_v0_7_15_config_thresholds.py` 9/9. 되주입 3건 스팟 체크 각각 다른 신호: 기준 경로 되돌림 → 5 case 실패(`project_root=/tmp`, excluded_paths 미도달, maturity skipped), `--config-path` 무시 → `config_source: default(section_missing)`, `issues_found` 만 반영 → "matrix 부재인데 통과로 보고됐다: status=ok". 실저장소 실측: `config_source: pyproject`, `project_root` = 저장소 루트, `--maturity` 가 처음으로 실제 실행.
- Follow-up: `--maturity` 가 실제로 돌면서 드러난 내용 드리프트 2건 — matrix 의 `Phase 13` 이 `in_progress` 인데 roadmap 은 그 단계를 현재로 말하지 않는다(메모리상 Phase 13 은 v0.13.3 에서 완료), `task-modes` 가 stable 인데 `test_path` 가 없다. 둘 다 내용 정정이라 이 커밋 범위 밖으로 두고 드러낸 채 남긴다. `workflow_kit.cli.doctor` 는 아직 provenance 를 안 쓴다(`load_config(args.project_root)` 그대로).
