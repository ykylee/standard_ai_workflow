---
id: TASK-2026-07-31-main-004
status: done
created_at: 2026-07-31
source_anchor: generic-task-2026-07-31-main-004
source_path: backlog/2026-07-31.md
kind: generic
---

# TASK-2026-07-31-main-004 — 검사가 처음 돌자 나온 2건, 하나는 진짜 하나는 위양성

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-07-31
- 담당: ykylee
- 호스트명:
- 호스트 IP:
- 영향 문서:
  - `workflow-source/workflow_kit/common/maturity.py`
  - `workflow-source/workflow_kit/common/linter.py`
  - `workflow-source/workflow_kit/common/__init__.py`
  - `workflow-source/tests/check_maturity_drift_judgment.py`
  - `workflow-source/tests/check_maturity_registry.py`
  - `workflow-source/core/workflow_kit_roadmap.md`
  - `ai-workflow/core/workflow_kit_roadmap.md`
  - `workflow-source/core/phase_13_followup.md`
  - `workflow-source/releases/Beta-v1.0.0.md`
  - `docs/CODE_INDEX.md`
  - `docs/INSTALLATION_AND_USAGE.md`

- 작업 내용: §2.47 이 `--maturity` 를 처음 실제로 돌게 만들면서 나온 2건을 종류별로 가른다 — `task-modes` 는 위양성이라 검사를 고치고, roadmap 은 진짜 드리프트라 문서를 고친다.
- 완료 기준: (1) `kind: "spec"` 어휘 정본이 한 곳이고 린터와 registry 검사가 그것을 읽는다. (2) 명세 항목의 근거가 `spec_path` 실재로 검증된다. (3) roadmap 이 `in_progress` 단계를 `planned` 로 적으면 issue 로 드러난다 — 이름만 적어서는 통과하지 못한다. (4) 실저장소 `--maturity` 가 issue 0 / warning 0. (5) 되주입 각각이 다른 신호로 실패한다.

## 🛠️ Implementation / Content

- 진행 현황: 어휘 정본 `common/maturity.py` 신설(`SKILL_KIND_SPEC` / `TEST_REQUIRED_STAGES` / `is_spec_entry` / `requires_test_path` / `spec_path_of` / `roadmap_planned_contradictions`) + 린터의 spec 분기(`missing_spec_file`) + roadmap 모순 판정(`roadmap_milestone_still_planned`, 숫자 경계 매칭) + registry 검사의 리터럴 사본 3곳 제거 + roadmap/phase_13_followup 내용 정정 + 배포 사본 동기화.
- 다음 세션 시작 포인트: CI 실측 (smoke·mypy-strict·mkdocs)
- 남은 리스크: roadmap 모순 판정은 한국어/영어 표현 3종(`planned` / `진입 대기` / `진입 예정`)에 기댄다. 다른 어휘로 "아직 안 했다" 를 적으면 못 잡는다 — 그 대신 `name` 언급 판정이 남아 있다.

## ✅ Outcome

- 작업 결과: 두 건을 같은 "드리프트" 로 묶을 뻔했으나 종류가 달랐다. (1) `task-modes` 위양성 — matrix 항목에는 실행 표면이 없는 명세(`kind: "spec"`, 근거는 `spec_path`)가 있고 그 규약을 아는 자리가 `check_maturity_registry.py` 하나뿐이라, **kit 이 배포하는 린터는 몰랐다**. 소비자 저장소에서 `--maturity` 를 돌리면 영영 이 위양성을 본다는 뜻이다. 어휘를 정본 한 곳으로 모으고 명세의 근거는 `spec_path` 실재로 확인한다(완화가 아니라 근거의 교체). (2) roadmap 진짜 드리프트 — roadmap 과 phase_13_followup 이 둘 다 2026-07-21 자로 "Phase 13 planned 진입 대기" 인데 v1.0.0 은 2026-07-22 발행(entry gate 6/6 PASS, `-beta` 는 릴리스 노트가 명시한 명명 관례)이고 matrix 는 `in_progress`/`started: 2026-07-21` 다. 두 문서가 릴리스 하루 전에서 멈춰 있었다. 판정도 함께 고쳤다 — 예전엔 milestone `name` 문자열 포함 여부 하나라 그 줄만 넣으면 `planned` 라고 적혀 있어도 통과했다.
- 검증 결과: 전량 smoke **228/228 PASS**(dev,release,mcp-sdk venv, `--tmp-dir=/var/tmp/saw-smoke`, 워킹트리 변경 0). mypy strict **122 files 0 errors**. 신규 `check_maturity_drift_judgment.py` 10/10, `check_maturity_registry` 3/3, `check_standard_single_source` 7/7. 실저장소 `--maturity`: `status: ok`, issue 0 / warning 0. 되주입 5건 각각 다른 신호(spec 분기 제거 시 원래 위양성 문구 그대로 재현 / 모순 판정 제거 시 "이름만 적어도 통과" / roadmap 되돌림 시 실저장소 판정 실패 / 숫자 경계 제거 시 `Phase 1` 이 `Phase 13` 줄에 걸림 / registry 리터럴 복원 시 "어휘 사본이 남아 있다").
- 후속 작업: 중간 smoke 에서 `check_standard_single_source` 가 red 를 냈다 — 정본(`workflow-source/core/`)만 고치고 배포 사본(`ai-workflow/core/`)을 안 옮겼기 때문이다. 검사가 옳았고 사본 동기화로 해소. `phase_13_followup.md` / `maturity_matrix.json` 은 배포 사본이 없어 해당 없음. roadmap 모순 판정의 어휘 3종은 확장 여지가 있다.
