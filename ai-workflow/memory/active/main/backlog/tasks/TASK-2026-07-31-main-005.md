---
id: TASK-2026-07-31-main-005
status: done
created_at: 2026-07-31
source_anchor: generic-task-2026-07-31-main-005
source_path: backlog/2026-07-31.md
kind: generic
---

# TASK-2026-07-31-main-005 — 같은 결함이 CLI 에도 있었다 — doctor 의 기준 경로와 출처

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-07-31
- 담당: ykylee
- 호스트명:
- 호스트 IP:
- 영향 문서:
  - `workflow-source/workflow_kit/cli/doctor.py`
  - `workflow-source/tests/check_doctor_config_provenance.py`
  - `workflow-source/tests/check_v0_7_4_followup.py`
  - `workflow-source/releases/Beta-v1.0.0.md`
  - `docs/PROJECT_PROFILE.md`
  - `docs/CODE_INDEX.md`
  - `docs/INSTALLATION_AND_USAGE.md`

- 작업 내용: §2.47 이 후속으로 남긴 `workflow_kit.cli.doctor` 의 provenance 부재를 닫는다.
- 완료 기준: (1) `--project-root` 기본값이 cwd 다. (2) `--config-path` 로 설정 위치를 명시할 수 있고 선언한 설정이 **평가까지 도달**한다. (3) 출력 3종에 출처가 남고 기존 `--show-config` 5 field 계약은 유지된다. (4) 되주입 각각이 다른 신호로 실패한다.

## 🛠️ Implementation / Content

- 진행 현황: `default_project_root()`(cwd) + `--config-path` + `load_config_with_provenance` 전환 + `config_provenance` 를 `--show-config`(top-level 5 field 유지) / `--json` / pretty footer 3종에 노출. 신규 `check_doctor_config_provenance.py`(6) + `check_v0_7_4_followup` 의 JSON envelope 계약 갱신.
- 다음 세션 시작 포인트: CI 실측 (smoke·mypy-strict·mkdocs)
- 남은 리스크: `--project-root` 는 여전히 state.json 기준과 설정 기준 두 역할을 겸한다. 이 저장소처럼 둘이 갈라진 배치에서는 `--config-path` 를 매번 줘야 한다.

## ✅ Outcome

- 작업 결과: provenance 만의 문제가 아니었다. `DEFAULT_PROJECT_ROOT = Path(__file__).resolve().parent × 5` 는 이 저장소에서 `/home/yklee/repos` = **저장소 루트의 두 단계 위**였고(실측), 설치본에서는 사용자 프로젝트와 무관한 경로다 — 모듈 위치로 workspace 를 추측할 수 있다는 전제 자체가 틀렸다. 그 값이 `load_config` 와 `_read_state_json` 양쪽으로 가서 인자 없는 기본 호출은 설정도 state.json 도 못 찾았다. 기존 doctor smoke 는 전부 `--project-root` 를 명시해 돌아서 이 결함을 못 봤다 — 사용자가 실제로 치는 것은 인자 없는 쪽인데(§2.47 과 같은 자리). 고치자마자 동작이 바뀌었다: `--config-path workflow-source` 로 돌리면 선언한 `partial_rules = { resiliency = [RES-WF-01, RES-WF-02] }` 가 평가 결과에 실제로 반영된다. v0.7.8 이 "display only → actual apply 격상" 이라고 적은 기능은 이 저장소에서 한 번도 apply 된 적이 없었다.
- 검증 결과: 전량 smoke **229/229 PASS**(dev,release,mcp-sdk venv, `--tmp-dir=/var/tmp/saw-smoke`, 워킹트리 변경 0). mypy strict **122 files 0 errors**. 신규 `check_doctor_config_provenance.py` 6/6, doctor 관련 기존 검사 5종(`check_cli_doctor` / `check_v0_7_4_followup` / `check_v0_7_15_config_thresholds` / `check_metadata` / `check_state_aware_baselines`) PASS. 되주입 4건 각각 다른 신호(default 를 모듈 위치로 → `project_root: /home/yklee/repos` / `--config-path` 무시 → 선언 설정이 평가에 미도달 / pretty 출처 줄 제거 → footer 에 출처 없음 / `--show-config` nested → 기존 5 field 계약 파기).
- 후속 작업: `--project-root` 가 state.json 기준과 설정 기준 두 역할을 겸하는 것은 그대로 뒀다 — 분리하려면 별도 결정이 필요하다. §2.47→§2.49 로 같은 모양이 두 도구에서 나왔으니, 경로 기준을 잡는 다른 진입점이 더 있는지는 별건으로 볼 것.
