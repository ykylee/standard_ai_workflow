---
id: TASK-2026-07-31-main-006
status: done
created_at: 2026-07-31
source_anchor: generic-task-2026-07-31-main-006
source_path: backlog/2026-07-31.md
kind: generic
---

# TASK-2026-07-31-main-006 — 세 번째를 찾으러 갔다 — 경로 기준 전수 조사

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-07-31
- 담당: ykylee
- 호스트명:
- 호스트 IP:
- 영향 문서:
  - `workflow-source/workflow_kit/common/paths.py`
  - `workflow-source/tests/check_branch_resolver_agreement.py`
  - `workflow-source/releases/Beta-v1.0.0.md`
  - `docs/CODE_INDEX.md`
  - `docs/INSTALLATION_AND_USAGE.md`

- 작업 내용: §2.47(린터)·§2.49(doctor)가 같은 모양이었으므로 "기준 경로를 잡는 진입점" 을 AST 로 전수 조사하고, 나온 결함을 닫는다.
- 완료 기준: (1) 손으로 센 목록이 아니라 AST 산출물로 판정한다. (2) 저장소 밖으로 착지하는 기준이 0건임을 확인한다. (3) 나온 결함은 재현 → 수정 → 회귀 검사까지 간다. (4) 되주입 각각이 다른 신호로 실패한다.

## 🛠️ Implementation / Content

- 진행 현황: AST sweep 3부류(`Path(__file__)` 유도 309 / 인자 `.parent` 연쇄 175(depth≥2 는 3) / `Path.cwd()` 9). 경로 축에서는 세 번째 없음 확인. branch 축에서 `workflow_branch_dir` / `workflow_archived_branch_dir` 가 v1.0.1 규칙 미적용인 것을 발견 → `branch_for_workspace(project_workspace_root(...))` 로 교정 + 신규 `check_branch_resolver_agreement.py`(4).
- 다음 세션 시작 포인트: CI 실측 (smoke·mypy-strict·mkdocs)
- 남은 리스크: `dashboard_data._repo_root` 는 `workspace_root=None` 일 때 `Path(__file__).parents[3]` 로 떨어진다 — 이 저장소에서는 맞지만 설치본에서는 무의미하다. 선언된 fallback 이라 이번 범위 밖으로 뒀다. 그리고 `_branch_scoped_or_legacy(active_dir, ...)` 는 active dir 만 받아 workspace 를 역산하지 않는 것이 의도라, 여전히 `get_current_branch()` 로 떨어진다.

## ✅ Outcome

- 작업 결과: 경로 축에서는 세 번째가 없었다 — `Path(__file__)` 유도 309건 중 **저장소 밖 착지 0건**(doctor 가 마지막이었다). 배포 패키지 안의 11건 중 `server/*` 4건 + `harness` 1건이 `<repo>/workflow-source/` 배치를 가정하지만 `pyproject.toml` 이 "나머지는 저장소 디렉터리 레이아웃으로 소비한다" 고 적은 **선언된 설계**라 결함이 아니다. **대신 다른 축에서 나왔다 — 경로만 기준이 아니라 branch 도 경로를 고른다.** v1.0.1 이 "workspace 로 파라미터화된 함수는 그 workspace 의 git 을 본다" 를 선언했는데 적용된 곳은 `state_path_for_workspace` 하나뿐이었고, profile 을 받는 `workflow_branch_dir` / `workflow_archived_branch_dir` 는 `get_current_branch()`(모듈 저장소)를 계속 썼다. 같은 workspace 에 대해 state.json 과 handoff/backlog 가 **다른 branch 디렉터리**를 가리켰다. 이 저장소는 모듈 저장소 == workspace 라 안 드러나고 소비자 프로젝트에서만 발현한다. 기존 검사가 못 본 이유는 fixture 를 `get_current_branch()` 로 만들고 결과를 그것과 대조해서다 — 자기 자신과 비교하면 두 해석기가 갈라져도 통과한다.
- 검증 결과: 전량 smoke **230/230 PASS**(dev,release,mcp-sdk venv, `--tmp-dir=/var/tmp/saw-smoke`, 워킹트리 변경 0). mypy strict **122 files 0 errors**. 신규 `check_branch_resolver_agreement.py` 4/4, 경로 관련 기존 검사 9종(paths / branch_scoped_memory / backlog_update / merge_doc_reconcile / self_application / workflow_linter / v0_7_26_branch_detection_fix / project_docs_branch_memory / writer_reader_roundtrip) PASS. 실측 재현: repoB(feature/probe-branch) 의 profile 로 수정 전 `branch_dir=main` vs `state=feature/probe-branch`, 수정 후 셋 다 `feature/probe-branch`. 되주입 2건 각각 다른 신호.
- 후속 작업: `dashboard_data._repo_root` 의 `workspace_root=None` fallback(설치본에서 무의미) + `_branch_scoped_or_legacy` 의 `get_current_branch()` fallback 은 남겼다. 둘 다 선언된 동작이라 바꾸려면 별도 결정이 필요하다. 전수 조사 스크립트는 일회용으로 돌렸고 저장소에 남기지 않았다 — 정례화하려면 별건.
