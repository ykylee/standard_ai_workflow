---
id: TASK-2026-07-31-main-007
status: done
created_at: 2026-07-31
source_anchor: generic-task-2026-07-31-main-007
source_path: backlog/2026-07-31.md
kind: generic
---

# TASK-2026-07-31-main-007 — 모든 panel 의 기준이 자기 근거를 안 내고 있었다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-07-31
- Owner: ykylee
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/common/dashboard_data.py`
  - `workflow-source/tests/check_dashboard_workspace_provenance.py`
  - `workflow-source/tests/check_quality_dashboard_v0_13_0.py`
  - `workflow-source/releases/Beta-v1.0.0.md`
  - `docs/CODE_INDEX.md`
  - `docs/INSTALLATION_AND_USAGE.md`

- Description: §2.50 의 전수 조사가 "선언된 fallback 이라 범위 밖" 으로 남긴 마지막 한 건 — `dashboard_data._repo_root` 의 모듈 위치 fallback 을 닫는다.
- Completion criteria: (1) 미지정이면 cwd 다. (2) snapshot 이 `workspace_root_source` 로 출처를 밝힌다. (3) 보고하는 경로와 panel 이 쓰는 경로가 같다. (4) 되주입 각각이 다른 신호로 실패한다.

## 🛠️ Implementation / Content

- Progress: `resolve_workspace_root(ws) -> (Path, source)` 신설(명시 인자 → cwd 두 갈래), snapshot 에 `workspace_root_source` 추가, `_repo_root` 는 그것을 부르는 얇은 wrapper. 신규 `check_dashboard_workspace_provenance.py`(4) + `check_quality_dashboard_v0_13_0` 의 무인자 호출 2곳을 `REPO_ROOT` 명시로 교정.
- Next session starting point: CI 실측 (smoke·mypy-strict·mkdocs)
- Remaining risks: cwd 가 workspace 가 아니면 8 panel 이 빈 값을 낸다. 이제 그 사실이 `workspace_root_source: cwd` + 그 경로로 드러나지만, "빈 값" 자체를 오류로 올리지는 않는다.

## ✅ Outcome

- Result: `_repo_root(None)` 이 `Path(__file__).resolve().parents[3]` 로 떨어지고 있었다. 이 저장소는 editable install 이라 그 값이 **우연히** 저장소 루트였고, 설치본 배치로 복사해 재보니 `<venv>/lib/python3.13` 이었다 — 실재하는 디렉터리인데 workspace 가 아니다. 그러면 8 panel 이 전부 빈 값을 내고 그 빈 값이 그 경로의 측정 결과처럼 보고된다. **이 모듈이 이미 답을 적어 두고 있었다** — `JUDGMENT_METRICS` 위 주석의 "판정 지표는 값만 내지 않는다, 무엇을 보고 그렇게 판정했는지 함께 낸다". 그런데 모든 panel 의 기준인 workspace root 자신은 근거를 안 냈다. 규칙을 세운 자리와 안 지켜진 자리가 같은 파일 안에 있었다.
- Verification: 전량 smoke **231/231 PASS**(dev,release,mcp-sdk venv, `--tmp-dir=/var/tmp/saw-smoke`, 워킹트리 변경 0). mypy strict **122 files 0 errors**. 신규 검사 4/4(CI env `GITHUB_REF_NAME=main` 재현 포함), dashboard 관련 기존 검사 11종 PASS. 실측: 설치본 배치에서 수정 전 `parents[3] = <venv>/lib/python3.13`(ai-workflow 없음) → 수정 후 cwd. 되주입 3건 각각 다른 신호.
- Follow-up: **되주입 3번이 처음엔 통과했다** — 검사를 저장소 루트에서 돌리면 cwd 와 `parents[3]` 이 우연히 같아 차이가 안 보인다. §2.50 에서 배운 "자기 자신과 비교하는 검사" 를 바로 다음 검사에서 또 썼다. 다른 cwd 에서 재도록 고쳐 잡았다. 그리고 전량 smoke 가 `check_quality_dashboard_v0_13_0` 의 무인자 호출(모듈 위치 추측 의존)을 곧바로 잡았다 — 단독 실행은 저장소 루트라 통과하고 smoke 는 다른 cwd 라 red. 검사가 옳고 의존이 틀렸다.
