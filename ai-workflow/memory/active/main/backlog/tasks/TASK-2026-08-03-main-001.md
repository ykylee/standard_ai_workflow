---
id: TASK-2026-08-03-main-001
status: done
created_at: 2026-08-03
source_anchor: generic-task-2026-08-03-main-001
source_path: backlog/2026-08-03.md
kind: generic
---

# TASK-2026-08-03-main-001 — 생성물인지를 이름으로 가르고 있었다 — 정본은 .gitignore 다

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-08-03
- 담당: ykylee
- 호스트명:
- 호스트 IP:
- 영향 문서:
  - `workflow-source/tools/audit_root_anchors.py`
  - `workflow-source/tests/check_root_anchor_audit.py`
  - `workflow-source/releases/Beta-v1.0.0.md`

- 작업 내용: §2.53 이 남긴 한계. EXCLUDED_PARTS 는 이름 기반이라 'build' 라는 이름의 진짜 소스가 조용히 빠지고, 애초에 .gitignore 가 선언한 것의 약한 사본이었다. git ls-files 로 정본을 쓴다.
- 완료 기준: (1) 생성물 판정을 이름이 아니라 저장소 선언으로 한다. (2) 추적되는 build/ 안의 결함이 잡힌다. (3) fallback 으로 떨어지면 그 사실이 산출물에 있다. (4) 이관으로 현재 대상 집합이 바뀌지 않는다.

## 🛠️ Implementation / Content

- 진행 현황: git ls-files(tracked) + ls-files --others --exclude-standard(untracked-not-ignored) 합집합. --show-toplevel == scan_root 일 때만 사용. source_selection(git/name-fallback) 을 payload 에 추가. case_10 기대값도 이름 목록 → git 으로 이관.
- 다음 세션 시작 포인트: CI 실측
- 남은 리스크: fallback 경로는 여전히 이름 기반이다(git 없는 소비자 환경). 그리고 R1/R2 는 Path 계열 표현만 본다 — os.path.dirname 연쇄 미탐.

## ✅ Outcome

- 작업 결과: 이관 시점 두 방식이 446건으로 완전 일치(git 전용 0 / 이름 전용 0) — 지금 바뀌는 것 없이 이름 충돌 위험만 제거. EXCLUDED_PARTS 는 fallback 전용으로 격하.
- 검증 결과: 전량 smoke 232/232 PASS(격리 venv, 워킹트리 변동 0), check_root_anchor_audit 11/11, mypy strict 2파일 clean. 되주입: 추적되는 build/real_source.py 에 parents[6] 를 심으면 git 모드는 미선언 1건으로 잡고 fallback 모드는 미선언 0건으로 놓친다 — 같은 코드 같은 결함, 틀린 쪽이 더 조용하다.
- 후속 작업:
