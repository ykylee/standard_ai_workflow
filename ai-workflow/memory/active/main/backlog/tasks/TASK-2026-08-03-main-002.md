---
id: TASK-2026-08-03-main-002
status: done
created_at: 2026-08-03
source_anchor: generic-task-2026-08-03-main-002
source_path: backlog/2026-08-03.md
kind: generic
---

# TASK-2026-08-03-main-002 — 슬래시 브랜치에서 깨지던 것들 — 셋이었고 원인이 서로 달랐다

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-08-03
- 담당: ykylee
- 호스트명:
- 호스트 IP:
- 영향 문서:
  - `workflow-source/tests/check_branch_scoped_memory.py`
  - `workflow-source/tests/check_self_application.py`
  - `workflow-source/tests/check_workflow_linter.py`
  - `workflow-source/releases/Beta-v1.0.0.md`

- 작업 내용: handoff 가 '2건' 으로 적어 둔 슬래시 브랜치 결함을 재현하니 셋이었고, 그중 check_self_application 은 슬래시와 무관했다. smoke 가 branches:[**] 로 돌기 때문에 브랜치를 따는 순간 CI 가 red 였다.
- 완료 기준: (1) main/무슬래시/단일슬래시/다중슬래시 네 환경에서 전량 smoke 가 통과한다. (2) 각 결함이 되주입에서 서로 다른 신호로 red 가 된다. (3) 제품 결함과 검사 결함을 구분해 기록한다.

## 🛠️ Implementation / Content

- 진행 현황: 제품(branch_slug 정규화, 아카이버 rglob)은 이미 슬래시를 감당하고 있었고 깨진 건 전부 검사 쪽이었다. fixture 가 제품이 만들지 않는 파일명 생성 / 경로 한 컴포넌트를 다중 컴포넌트 브랜치명과 비교 / 링크 깊이 상수 / 검사가 저장소 대신 브랜치를 물음. 슬래시 브랜치를 끝까지 밟는 case_9 신규.
- 다음 세션 시작 포인트: CI 실측
- 남은 리스크: session_handoff_template.md 의 ../../docs/ 링크가 깊이 의존이다 — 슬래시와 무관하게 branch-scoped layout 전반의 별건(템플릿이 legacy 평면 layout 기준). 이 저장소 handoff 는 손으로 쓴 링크라 현재는 동작한다.

## ✅ Outcome

- 작업 결과: handoff 가 지목한 2건 중 1건(check_self_application)은 오귀속이었고 — 슬래시 없는 새 브랜치에서도 FAIL — 대신 아무도 몰랐던 3번째(check_workflow_linter)가 나왔다. 발견 계기는 슬래시 브랜치 환경으로 전량 smoke 를 한 번 돌린 것 하나다. check_self_application 은 smoke 가 branches:[**] 라 브랜치를 따는 순간 무관하게 red 였던 것이라 슬래시보다 영향이 컸다.
- 검증 결과: main 232/232, feature/slash-probe 232/232(이 저장소 최초), 개별 검사는 release/v1.2/hotfix(다중 슬래시)까지 통과. 되주입 4종 각각 다른 신호: 아카이버 rglob→glob(중첩 미탐) / state.json 제거(자기 적용 없음, main·브랜치 양쪽) / 링크 깊이 상수 복원(main 통과·슬래시 FAIL).
- 후속 작업: 슬래시 브랜치 전량 smoke 를 CI 에 정례화할지는 별건 — 지금은 로컬 env 재현으로만 확인했다.
