---
id: TASK-2026-08-03-main-003
status: done
created_at: 2026-08-03
source_anchor: generic-task-2026-08-03-main-003
source_path: backlog/2026-08-03.md
kind: generic
---

# TASK-2026-08-03-main-003 — 슬래시 브랜치를 밟는 실행이 우연이었다 — smoke 를 2셀로

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-08-03
- 담당: ykylee
- 호스트명:
- 호스트 IP:
- 영향 문서:
  - `.github/workflows/smoke.yml`
  - `workflow-source/tests/check_branch_scoped_memory.py`
  - `workflow-source/releases/Beta-v1.0.0.md`

- 작업 내용: §2.55 는 로컬에서 env 를 손으로 덮어 검증했다. 개발이 거의 main 에서 이뤄지므로 슬래시 브랜치를 밟는 CI 실행은 아무도 보장하지 않는 우연이다. smoke job 을 브랜치 컨텍스트 2셀 matrix 로 만든다.
- 완료 기준: (1) CI 가 슬래시 컨텍스트로도 전량을 돈다. (2) 오버라이드가 안 먹으면 조용히 통과하지 않는다. (3) matrix 셀이 지워지면 검사가 red 가 된다. (4) branch-sensitive 검사 목록을 선언하지 않는다.

## 🛠️ Implementation / Content

- 진행 현황: smoke job 을 branch_context 2셀(native / slash) matrix 로. 오버라이드 키는 CODEX_WORKFLOW_BRANCH(BRANCH_ENV_KEYS 최우선) — GITHUB_REF_NAME 을 덮으면 러너 실제 컨텍스트까지 바뀐다. 빈 값은 export 안 함(빈 문자열은 다음 키로 흘러 native 와 구분 불가). 해석된 브랜치를 step summary 에 찍고 선언과 다르면 ::error:: 로 죽인다. artifact 이름도 셀별로 분리(upload-artifact@v4 는 중복 이름에서 실패).
- 다음 세션 시작 포인트: CI 실측 — 특히 2셀의 step summary 에 찍힌 해석 브랜치가 실제로 갈리는지(러너의 GITHUB_HEAD_REF 등과의 상호작용은 CI 가 처음 잰다)
- 남은 리스크: 오버라이드가 러너에서 안 먹을 가능성은 workflow 자기 검증으로 막았지만, 그 자기 검증 자체가 CI 에서 처음 돈다. 그리고 컴퓨트가 2배다(wall-clock 은 동일).

## ✅ Outcome

- 작업 결과: branch-sensitive 검사 목록을 만들지 않은 것이 핵심 결정이다 — §2.53 에서 없앤 포함 목록과 같은 모양이고, 실제로 §2.55 에서 handoff 가 지목한 2건 중 1건은 틀렸고 3번째는 목록에 없었다. 전량을 두 컨텍스트로 돌린다(병렬이라 wall-clock 동일). case_10 신규: case_9 는 코드가 슬래시를 감당하는지, case_10 은 그 검증이 CI 에서 실제로 도는지 — 다른 층이다.
- 검증 결과: 로컬 전량 2컨텍스트 각각 232/232 PASS(native / CODEX_WORKFLOW_BRANCH=feature/ci-slash-probe). check_branch_scoped_memory 10/10 양쪽. 되주입 2종 각각 다른 신호: 슬래시 셀 제거 → '슬래시가 든 브랜치 셀이 없다' / 오버라이드 자기 검증 제거 → '두 셀이 같은 것을 재면서 2셀 green 이 된다'. smoke.yml YAML 파싱 유효.
- 후속 작업:
