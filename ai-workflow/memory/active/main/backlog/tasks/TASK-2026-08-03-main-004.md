---
id: TASK-2026-08-03-main-004
status: done
created_at: 2026-08-03
source_anchor: generic-task-2026-08-03-main-004
source_path: backlog/2026-08-03.md
kind: generic
---

# TASK-2026-08-03-main-004 — 오래 red 인 스케줄 workflow 2건 — 둘 다 원인이 딴 데 있었다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-03
- Owner: ykylee
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/url_validity.py`
  - `workflow-source/tests/check_url_validity.py`
  - `.github/workflows/consumer-metrics-digest.yml`
  - `workflow-source/releases/Beta-v1.0.0.md`

- Description: okf-validate(V-R10 URL 검증)와 consumer-metrics-digest(issue 게시)가 각각 6주/5주 연속 red. 열어 보니 URL 문제도 게시 문제도 아니었다 — CLI 옵션 등록 삭제와 저장소 라벨 부재.
- Completion criteria: (1) okf-validate 의 실제 호출이 로컬에서 exit 0. (2) 같은 회귀가 되주입에서 잡힌다. (3) consumer-metrics-digest 가 저장소 상태에 암묵 의존하지 않는다.

## 🛠️ Implementation / Content

- Progress: url_validity 에서 --cache(46b6b7a 가 삭제)와 --max-bytes(1da10ef 가 삭제) 복원 — 둘 다 무관한 커밋이 등록 한 줄만 지우고 main() 의 args.* 참조는 남긴 같은 사고다. 검사 3종 추가: 소비자 yml 에서 인자 추출 대조 / --online 파싱+args.cache 실재 / CI 실제 호출을 main() 으로 끝까지(네트워크만 스텁). consumer-metrics-digest 는 사용 직전 gh label create --force 로 라벨 확보.
- Next session starting point: 푸시 후 okf-validate 와 consumer-metrics-digest 를 workflow_dispatch 로 수동 트리거해 실측
- Remaining risks: consumer-metrics-digest 의 라벨 생성은 GITHUB_TOKEN 의 issues:write 에 의존한다 — 없으면 명시적으로 죽는다. traffic API 403(WARN)은 별건으로 남겼다.

## ✅ Outcome

- Result: --online CLI 경로가 7주간 통째로 죽어 있었다(주면 ambiguous option, 안 주면 AttributeError). smoke 232건이 못 본 이유는 '네트워크 의존이라 skip' 이었는데, 네트워크 의존은 호출을 건너뛸 이유이지 인자 계약을 건너뛸 이유가 아니다. 그리고 파싱 검사만으로는 --max-bytes 를 못 잡는다 — 실측으로 확인했다.
- Verification: check_url_validity 17/17, 전량 smoke 232/232, mypy strict 122 files 0 errors. 실제 CI 호출(https://example.com --mode=strict --online --cache --body --ttl 86400 --timeout 10 --max-retries 3)이 네트워크 포함 exit 0. 되주입: --cache 제거 → 계약 case+파싱 case 2건 red / --max-bytes 제거 → 실행 case 만 red(16/17, 파싱 case 2건은 통과).
- Follow-up:
