---
id: TASK-2026-08-04-main-001
status: done
created_at: 2026-08-04
source_anchor: generic-task-2026-08-04-main-001
source_path: backlog/2026-08-04.md
kind: generic
---

# TASK-2026-08-04-main-001 — 검사가 처음 돌자 나온 URL 2건 — 죽은 링크가 아니라 태어난 적 없는 링크

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-08-04
- 담당: ykylee
- 호스트명:
- 호스트 IP:
- 영향 문서:
  - `workflow-source/workflow_kit/frontmatter_urls.py` (신규)
  - `workflow-source/workflow_kit/okf_export.py`
  - `workflow-source/tests/check_frontmatter_url_extraction.py` (신규)
  - `workflow-source/tests/check_okf_export.py`
  - `workflow-source/tests/check_wiki_drift.py`
  - `.github/workflows/okf-validate.yml`
  - `ai-workflow/wiki/concepts/okf-open-knowledge-format.md`
  - `docs/samples/okf-bundle-2026-06-16/concepts/{okf-open-knowledge-format,wiki-source-rule-r9}.md`
  - `workflow-source/releases/Beta-v1.0.0.md`, `docs/CODE_INDEX.md`, `docs/INSTALLATION_AND_USAGE.md`

- 작업 내용: §2.57 이 CLI 를 되살리자 okf-validate 가 처음 실제로 돌아 exit 123 으로 URL 2건을 냈다. 판정 이름은 "링크가 죽었다"였지만 둘 다 존재한 적 없는 URL 이었다 — 추출기 위양성 1 + 생산자 결함 1. 셋(추출기 / 데이터 / 규약 검사)을 함께 고친다.
- 완료 기준: (1) okf-validate 의 실제 호출이 네트워크 포함 exit 0. (2) `resource` bare-URI 규약이 검사로 고정되고 저장소 전수 위반 0. (3) 커밋된 sample bundle 의 `resource` 가 지금 생산자가 만드는 값과 같다.

## 🛠️ Implementation / Content

- 진행 현황: 추출을 워크플로우 안의 grep 한 줄에서 `workflow_kit.frontmatter_urls` 로 옮겼다(frontmatter 블록만, 값 안의 URL 전부, 출처 파일:줄:key 동반, 스캔 0건이면 exit 2). `okf_export._derive_resource` 에 가드 2종 — 공백 있는 값은 URI 가 아니다 / 저장소에 없는 경로는 URL 이 되지 않는다. 데이터는 wiki 의 `external (…)` 값을 bare URL 로 교정하고 bundle 2 page 를 생산자 출력과 일치시켰다. 검사 신규 12 case.
- 다음 세션 시작 포인트: 푸시 후 트리거되는 CI 실측 — 특히 `okf-validate`(6주 red 였다)와 smoke 2셀
- 남은 리스크: 뽑히는 URL 4건 전부가 외부 호스트 의존이다(raw.githubusercontent / github / blog.scottlogic). 그중 하나가 죽으면 이제는 *진짜* stale 로 red 가 된다 — 이번 판정과 구분해서 볼 것.

## ✅ Outcome

- 작업 결과: 결함 3층이 각각 다른 자리에 있었다. (1) 추출기: grep 이 파일 전체를 훑어 산문 예시를 URL 로 만들고(백틱·괄호·마침표 포함), 값을 공백에서 끊어 `a + b` 의 두 번째 URL 을 조용히 버렸다 — ponytail page 의 blog URL 은 이번까지 한 번도 검사된 적이 없다. (2) 생산자: `last_ingested_from` 은 자유 서술인데 통째로 in-repo 경로로 넘겨 `…/blob/main/external` 을 만들었고, 커밋된 5-page 번들에 그런 `resource` 가 2건 있었다(한 건은 추출기가 공백에서 끊어 준 덕에 우연히 통과 중이었다). (3) 규약: `resource` 가 bare URI 여야 한다는 사실이 어디에도 검사로 없었다.
- 검증 결과: 전량 smoke **233/233**(native / `CODEX_WORKFLOW_BRANCH=feature/ci-slash-probe` 양쪽, 격리 venv `dev,release,mcp-sdk`), mypy strict **123 files 0 errors**, actionlint exit 0(shellcheck 있는 환경), `check_frontmatter_url_extraction` 12/12, `check_okf_export` 18/18. CI 호출 재현: 추출 4 URL + `--check` OK + `url_validity --mode=strict --online --cache --body` **exit 0**(네트워크 포함). 되주입 5종이 각각 다른 신호 — 생산자 가드 제거 3건 red / 데이터 되돌림 2건 / frontmatter 경계 제거 3건 / 정규식 되돌림 2건 / 소비자 grep 복귀 1건.
- 후속 작업:
