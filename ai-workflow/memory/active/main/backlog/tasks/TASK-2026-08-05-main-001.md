---
id: TASK-2026-08-05-main-001
status: done
created_at: 2026-08-05
source_anchor: generic-task-2026-08-05-main-001
source_path: backlog/2026-08-05.md
kind: generic
---

# TASK-2026-08-05-main-001 — 자기 harness 를 부분만 적용하고 있었다 — 선언한 slash command 3종 부재 + claude-code 만 skill 미발행

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-05
- Owner: ykylee
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/scripts/bootstrap_lib/harnesses/__init__.py`
  - `workflow-source/scripts/bootstrap_lib/harnesses/renderers.py`
  - `workflow-source/tests/check_self_application.py`
  - `workflow-source/workflow_kit/upgrade_diff.py`
  - `workflow-source/tests/check_harness_skill_frontmatter.py`
  - `workflow-source/tests/check_docs.py`
  - `workflow-source/tests/check_v0_10_1_skill_only_entry_mode.py`
  - `workflow-source/tests/check_v0_10_2_delivery_layer_extension.py`
  - `.claude/commands/workflow-{session-start,backlog-update,doc-sync}.md` (신규)
  - `.claude/skills/standard-ai-workflow/SKILL.md` (신규)

- Description: 세션 시작 시 사용자가 "여기는 스킬이 적용이 안 되어 있다" 고 지적. 확인하니 (A) 루트 `CLAUDE.md` 가 광고하는 slash command 3종이 저장소에 없고, (B) skill 을 발행하는 다른 harness 3종(opencode / grok-build / codewhale)과 달리 claude-code HarnessSpec 만 skill 을 하나도 내보내지 않는다. 자기적용 검사는 green 이었다 — 요구 목록 `REQUIRED_ENTRYPOINTS` 가 HarnessSpec 의 손복사본이라 `entry_files` 만 보고 `extra_files` 를 아예 안 봤다.
- Completion criteria: (1) claude-code HarnessSpec 이 선언하는 파일 전부가 이 저장소에 실재한다. (2) 요구 목록이 HarnessSpec 정본에서 파생된다 — 손복사본 제거. (3) 새 검사가 결함 상태(파일 제거)에서 실제로 red 가 된다. (4) 발행되는 skill 이 기존 `check_harness_skill_frontmatter` 대상에 자동 포함된다.

## 🛠️ Implementation / Content

- Progress: 완료. renderer 에 `render_claude_code_skill` 추가 + HarnessSpec 등록, `.claude/` 4개 파일을 **renderer 로** 생성(손으로 쓰지 않음), 자기적용 검사에 case 2b 신설(요구 목록을 `HARNESS_SPECS` 에서 파생), stamping 이 frontmatter 를 깨던 결함 수정 + 검사 신설.
- Next session starting point: 푸시 후 트리거되는 CI 실측(smoke 2셀 / mypy-strict). 그리고 아래 "남은 리스크" 의 소비자 업그레이드 건.
- Remaining risks: **이미 부트스트랩된 소비자 프로젝트의 opencode / grok-build skill 파일은 깨진 채로 남는다.** 마커 버전이 같으면 `decide_action` 이 `IGNORED` 를 내므로 재실행해도 안 고쳐진다 — kit 버전이 올라가야 갱신된다. 이번 커밋에서 버전을 올리지 않았다.

## ✅ Outcome

- Result: 시작은 "여기는 스킬이 적용이 안 되어 있다" 는 지적 한 줄이었고, 파고 보니 층이 넷이었다.
  (1) **자기적용이 산출물의 부분집합만 가져왔다** — `CLAUDE.md` 는 renderer 가 찍은 그대로 slash command 3종을 광고하는데 `.claude/` 디렉터리 자체가 없었다.
  (2) **검사가 그걸 못 본 이유는 요구 목록이 손복사본이었기 때문** — `REQUIRED_ENTRYPOINTS = ("CLAUDE.md",)` 는 `HarnessSpec.entry_files` 의 사본이라 `extra_files` 를 아예 보지 않았다. 정본에서 파생하도록 바꾸고 case 2b 를 신설했다.
  (3) **skill 을 내보내는 harness 중 claude-code 만 없었다** — opencode / grok-build 는 진작 `.../skills/standard-ai-workflow/SKILL.md` 를 emit 하고 있었다. `render_claude_code_skill` 을 추가했다. (`workflow-source/skills/*/SKILL.md` 13개는 대상이 아니다 — `check_harness_skill_frontmatter` docstring 이 이미 "frontmatter 없는 내부 명세 문서" 라고 선언해 둔 층이다.)
  (4) **가장 큰 것은 딸려 나왔다** — `stamp_marker` 가 버전 마커를 무조건 1행에 붙여서 `---` 를 2행으로 밀어냈다. frontmatter 는 위치 계약이라 그 순간 평범한 산문이 되고, 하네스는 skill 을 아예 못 본다. opencode skill·agent 5종·grok skill 까지 **이미 배포된 8개 블록 전부** 가 같은 결함이었다. 기존 검사는 *렌더러 안의 문자열 리터럴* 만 봤기 때문에 4/4 PASS 를 내고 있었다 — 생성기를 검사하는 것과 산출물을 검사하는 것은 다른 일이다.
  덤으로 같은 부류 3건: `check_self_application` 의 `total = 5` 하드코딩(케이스를 늘리자 "3/5" 로 갈라짐 → 케이스 목록을 정본으로), `check_v0_10_1`/`check_v0_10_2` 의 `len(extra_files) == 3`(계약은 "3종이 있는가" 인데 개수로 대리 판정 → 이름 존재 검사로), `check_docs` 의 harness overlay 노출(프롬프트로 먹는 파일에 산문 계약을 요구 → `HARNESS_SPECS.extra_files` 에서 파생해 제외).
- Verification: 격리 venv(`dev,release,mcp-sdk`) 전량 smoke **233/233 PASS**, mypy strict `--config-file workflow-source/pyproject.toml` **123 files 0 errors**. 되주입 확인: `upgrade_diff.py` 만 stash 하고 새 검사를 돌리자 `test_survives_version_stamping` 이 **8개 블록 전부 FAIL**(opencode skill + agent 5 + grok skill + claude skill) → 검사가 무력하지 않음을 실측. `.claude/` 를 지우고 자기적용 검사를 돌리자 case 2b 가 **부재 4건**을 정확히 지목. 기준선 오염 여부는 HEAD worktree 에서 따로 측정 — 시스템 python 의 실패 5건(mypy 3 / release-pipeline 2 / workflow_kit_cli)은 **HEAD 에서도 동일하게 실패**하는 환경 문제이고 격리 venv 에서는 전부 green 이다.
- **push 직후 실측으로 드러난 후속 결함(같은 뿌리, 커밋 2개째)**: `227219e` 를 push 하자
  하네스가 새 skill/command 를 실제로 로드했고 — 그 자리에서 "확인 못 함" 이 답을 냈다 —
  skill 은 정상 표시됐지만 **slash command 3종의 설명이
  `<!-- standard-ai-workflow-kit: v1.0.0-beta -->` 로 떴다.** command 파일에는
  frontmatter 가 없어서 Claude Code 가 첫 줄을 설명으로 집었고, 그 자리에 버전 마커가
  앉아 있었다. 무조건 1행 prepend 의 **두 번째 피해자**다 — skill 은 frontmatter 를
  가져서 살았고 command 는 안 가져서 죽었다. 3개 command 렌더러에 `description`
  frontmatter 를 넣고, `test_claude_code_surfaces_declare_description` 을 신설했다.
  기존 1-5번 case 는 *있는* frontmatter 만 검사하므로 **없는 것** 은 구조적으로 못
  잡았다 — "무엇이 있어야 하는가" 를 묻는 case 가 따로 필요했다.
- Follow-up:
  - 소비자 업그레이드 경로: 깨진 채 배포된 opencode / grok-build skill 은 kit 버전 상승 없이는 갱신되지 않는다. 릴리스 시 이 건을 breaking-ish 로 다룰지 결정 필요.
  - MCP 는 이번 범위 밖이다 — `workflow-source/mcp_servers/` 에 서버 11개가 있고 `core/mcp_installation_by_harness.md` 에 설치 절차가 있는데 이 저장소에는 `.mcp.json` 이 없다(선택 C안).
