---
id: TASK-2026-08-05-main-002
status: done
created_at: 2026-08-05
source_anchor: generic-task-2026-08-05-main-002
source_path: backlog/2026-08-05.md
kind: generic
---

# TASK-2026-08-05-main-002 — 설치 문서가 존재하지 않는 배송을 선언하고 있었다 — Claude Code MCP

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-05
- Owner: ykylee
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/scripts/bootstrap_lib/mcp.py`
  - `workflow-source/tests/check_bootstrap_mcp_roundtrip.py`
  - `workflow-source/tests/check_self_application.py`
  - `workflow-source/core/mcp_installation_by_harness.md`
  - `.mcp.json` (신규)

- Description: §2.59 가 남겨 둔 C안. `core/mcp_installation_by_harness.md` §4 표는 Claude Code 행(`<root>/.mcp.json`)을 처음부터 싣고 있었는데 **그것을 만드는 렌더러가 없었다** — skill 결함과 같은 모양이다. 렌더러를 추가하고, 이 저장소에 실제로 심고, 검사로 고정한다.
- Completion criteria: (1) `--enable-mcp` 가 claude-code 로 `.mcp.json` 을 emit 한다. (2) 그 파일로 실제 MCP 서버가 뜨고 `tools/call` 이 성공한다(선언이 아니라 왕복). (3) 자기적용 검사가 MCP 층을 묻고, 경로를 손으로 적지 않는다. (4) round-trip 검사가 렌더러 정본과 대조돼 새 하네스를 조용히 빠뜨리지 못한다.

## 🛠️ Implementation / Content

- Progress: 완료.
- Next session starting point: push 후 CI 실측. 그리고 이 세션은 `.mcp.json` 을 로드하지 못한다 — 세션 시작 시점에 없던 파일이라 **다음 세션에서 MCP 도구 13종이 실제로 붙는지 확인** 할 것.
- Remaining risks: grok-build 의 MCP 블록은 `render_grok_build_config_example` 안에 **command/args 를 손으로 적어** 두고 있다(`_mcp_server_command` 를 안 쓴다). transport 기본값이 바뀌면 grok 만 갈라진다 — 사본이다.

## ✅ Outcome

- Result: §4 표의 7개 행 중 **Claude Code 하나만 거짓** 이었다(Grok 은 자기 `config.toml.example` 안에서 emit 중). `render_claude_code_mcp_config` + writer 분기 + dispatch 등록으로 채웠다.
  가장 값진 것은 **transport 실측**이다. 처음엔 "Claude Code 는 정식 MCP 니까 `stdio-sdk` 를 써야 한다" 고 판단했는데, 재 보니 정반대였다 — emit 되는 `command` 는 `python3` 즉 **시스템 python3** 이고, 거기엔 `mcp` SDK 가 없어서 `stdio-sdk` 는 `Connection closed` 로 죽는다. 반면 이름이 초라한 `jsonrpc-bridge`(default)는 공식 MCP 클라이언트와 `initialize`/`tools/list`/`tools/call` 왕복이 전부 정상이다. **두 transport 모두 MCP 를 말하고, 갈리는 건 프로토콜이 아니라 의존성이었다.** 특별대우 없이 default 를 그대로 따른다.
  두 번째로 `STANDARD_AI_WORKFLOW_ROOT` 가 절대 경로였다. `.mcp.json` 은 **커밋되는 파일** 이라 한 사람의 체크아웃 위치가 공유 파일에 박힌다. 같은 env 블록의 `PYTHONPATH` 는 이미 상대(`workflow-source`)였으니 두 값이 서로 어긋나 있었고, 절대 쪽이 틀린 쪽이었다 — `"."` 로 정합을 맞추고 6종 하네스 전부 왕복 재확인했다.
  검사 쪽: round-trip 검사의 docstring 은 "every supported harness" 를 돈다고 적어 두고 실제로는 손으로 유지되는 5종 부분집합만 돌고 있었다. `MCP_CONFIG_RENDERERS` 와 import 시점에 대조해, 렌더러가 있는데 대상에 없으면 즉시 깨뜨린다. 자기적용에는 case 2c 를 넣되 기대 경로를 `.mcp.json` 이라고 적지 않고 **임시 디렉터리에 한 번 emit 시켜 writer 가 고르는 파일명을 배운 뒤** 저장소에 있는지 본다.
- Verification: 저장소의 `.mcp.json` **그 파일을 읽어** 서버를 띄운 실측 — `initialize OK: workflow_read_only_bundle v1.0.0-beta`, `tools/list 13종`, `tools/call latest_backlog isError=False` (실데이터 반환). bootstrap 산출물 round-trip **6종 전부 ok**(codex / opencode / gemini-cli / antigravity / minimax-code / claude-code). 격리 venv 전량 smoke **233/233**, mypy strict **123 files 0 errors**. 되주입: `.mcp.json` 을 치우자 case 2c 가 `writer 가 내놓는 ['.mcp.json'] 중 부재 ['.mcp.json']` 로 정확히 red.
- Follow-up:
  - grok-build 의 하드코딩된 MCP command/args 를 `_mcp_server_command` 로 접기.
  - 남은 하네스(aider / goose / codewhale / pi-dev)는 §4 표에도 없다 — MCP 미지원인지 미구현인지 표에 명시할 것.
