---
id: TASK-2026-08-05-main-003
status: done
created_at: 2026-08-05
source_anchor: generic-task-2026-08-05-main-003
source_path: backlog/2026-08-05.md
kind: generic
---

# TASK-2026-08-05-main-003 — grok 의 MCP 사본을 접자 아무도 파싱해 본 적 없던 산출물이 드러났다

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-08-05
- Owner: ykylee
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/scripts/bootstrap_lib/mcp.py`
  - `workflow-source/scripts/bootstrap_lib/harnesses/renderers.py`
  - `workflow-source/tests/check_mcp_tool_descriptors.py`

- Description: §2.60 이 남긴 후속. `render_grok_build_config_example` 안에 MCP command/args/alias/tool 설명이 손으로 적혀 있었다 — Codex 는 `_mcp_server_command` 정본을 쓰는데 Grok 만 사본이라, transport 기본값이나 entry-point 모듈명이 바뀌면 Grok 만 옛 값을 계속 내보낸다.
- Completion criteria: (1) Grok 과 Codex 가 같은 조립 helper 를 쓴다. (2) Codex 산출물이 바이트 단위로 안 바뀐다. (3) 접은 결과가 검사로 고정된다.

## 🛠️ Implementation / Content

- Progress: 완료. `render_mcp_toml_block` 신설(mcp.py) → Codex·Grok 양쪽에서 사용. 사본은 활성 블록 1 + 주석 처리된 stdio-sdk 변형 1, 총 2개였다.
- Next session starting point: push 후 CI 실측.
- Remaining risks: `scripts/generate_read_only_harness_mcp_examples.py` + `schemas/read_only_harness_mcp_examples.json` 이 같은 entry-point 문자열을 또 들고 있다 — 이번에 보지 않았다. 세 번째 사본일 수 있다.

## ✅ Outcome

- Result: 접는 것 자체는 작았다. **값은 접는 과정에서 나왔다.**
  before/after 를 대조하니 Codex 산출물은 **바이트 동일**, Grok 은 활성 블록 바이트 동일 + 주석 변형만 보강(tool_descriptions 2줄 + stdio-sdk 경고 3줄)이었다 — 의도한 것 외에는 아무것도 안 바뀐 것을 실측으로 확인했다.
  그리고 확인하려고 산출물을 **tomllib 으로 파싱해 보다가** 결함이 나왔다: `tool_descriptions` 의 키가 `workflow_kit.read_only = "…"` 로 따옴표 없이 나가고 있었고, TOML 의 dotted key 규칙에 따라 `{workflow_kit = {read_only = "…"}}` 로 **중첩**된다. 코드는 `MCP_TOOL_NAME` 이라는 키 하나를 쓴다고 말하는데 파일은 다른 구조를 뜻했다. Codex 렌더러에 처음부터 있던 것이고 Grok 이 그대로 베낀 것이라 **양쪽 동일**했다. 접어 뒀기에 한 줄로 둘 다 고쳐졌다 — 사본이었으면 두 번 고치고 한 곳을 잊었을 것이다.
  왜 안 보였나: 기존 `check_mcp_tool_descriptors` case 4 는 **커밋된 예시 파일**만 파싱한다. 생성기가 뱉는 것은 아무도 파싱해 본 적이 없었다. §2.59 의 축("생성기를 검사하는 것과 산출물을 검사하는 것은 다른 일이다")이 MCP 층에서 한 번 더 나왔다.
- Verification: 되주입 — 따옴표만 되돌리자 새 case 가 `실제: ['workflow_kit']` 를 지목하며 2 bridge 모두 FAIL, 복구하니 5/5 PASS. (첫 시도는 `git stash` 로 mcp.py 를 통째로 치웠다가 helper 자체가 사라져 import 가 죽었다 — 그건 결함 상태가 아니라 다른 상태다. 되주입은 *한 줄만* 되돌려야 한다.) MCP round-trip **6종 전부 ok**, 격리 venv 전량 smoke **233/233**, mypy strict **123 files 0 errors**.
- Follow-up:
  - `generate_read_only_harness_mcp_examples.py` / `schemas/read_only_harness_mcp_examples.json` 이 세 번째 사본인지 확인.
