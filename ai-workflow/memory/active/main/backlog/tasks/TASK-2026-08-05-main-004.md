---
id: TASK-2026-08-05-main-004
status: done
created_at: 2026-08-05
source_anchor: generic-task-2026-08-05-main-004
source_path: backlog/2026-08-05.md
kind: generic
---

# TASK-2026-08-05-main-004 — 세 번째 사본은 이미 갈라져 있었다 + 자기 자신과 비교하는 검사를 썼다가 되주입에서 걸렸다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-05
- Owner: ykylee
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/scripts/bootstrap_lib/mcp.py`
  - `workflow-source/scripts/generate_read_only_harness_mcp_examples.py`
  - `workflow-source/schemas/read_only_harness_mcp_examples.json`
  - `workflow-source/tests/check_mcp_tool_descriptors.py`

- Description: §2.61 이 남긴 "세 번째 사본인지 확인" 후속. 확인 결과 사본이 맞았고, **이미 갈라져 있었다**. 사본을 정본으로 접고 갈라진 값을 고친다. (`transport_ready` 승격은 범위 밖 — 승격 조건이 정의돼 있지 않다.)
- Completion criteria: (1) 생성기가 alias / command / args / 최상위 키를 `bootstrap_lib.mcp` 에서 가져온다. (2) 갈라진 값이 고쳐지고 커밋된 산출물이 재생성된다. (3) 기존 5종 렌더러 산출물이 바이트 단위로 안 바뀐다. (4) 재발 방지 검사가 **결함 상태에서 실제로 red** 다.

## 🛠️ Implementation / Content

- Progress: 완료.
- Next session starting point: push 후 CI 실측. 그리고 여전히 **MCP 도구 13종이 실제 세션에 붙는지** 확인이 남아 있다(이 세션은 `.mcp.json` 을 로드하지 못한다).
- Remaining risks: `claude-code` 는 `examples/mcp_config_examples/` 에 독립 예시가 없어 방언 대조 대상에서 빠진다 — 검사가 `[info]` 로 매번 밝힌다. 그리고 `transport_ready` 는 registry 에 상수로 4곳(444/459/473/481) 박혀 있고 승격 조건이 어디에도 없다.

## ✅ Outcome

- Result: **사본은 하나가 아니라 셋이었다** — `SERVER_ALIAS` / `codex_toml_example` 의 command·args / `opencode_jsonc_example` 의 command·args, 거기에 `bridge_entrypoint` 2곳. 한 파일 안에 command/args 사본만 2개였다.
  **그리고 이미 갈라져 있었다**: OpenCode 예시가 최상위 키로 `mcp_servers` 를 가르치는데 실제 `render_opencode_mcp_config` 이 내보내는 키는 `mcp` 다. 그 예시대로 붙여넣으면 OpenCode 는 서버를 못 본다. "사본은 반드시 갈라진다" 가 예측이 아니라 이미 일어난 일이었다.
  방언을 아는 자리를 `MCP_CONFIG_ROOT_KEY` 하나로 두고 5종 렌더러 + 생성기가 함께 읽게 했다. 산출물 대조: 5종 렌더러 **전부 바이트 동일**, 커밋된 예시 JSON 은 **갈라진 한 곳만** 변경(`mcp_servers` → `mcp`).
  **가장 값진 것은 검사를 잘못 짰다가 되주입에서 걸린 것이다.** 처음엔 "표가 렌더러 산출물과 같은가" 로 짰는데, 렌더러도 같은 표를 읽으므로 표를 바꾸면 양쪽이 함께 바뀐다 — `opencode` 를 `mcp_servers` 로 되돌려도 **6/6 PASS** 가 나왔다. 자기 자신과 비교하는 검사는 아무것도 보장하지 않는다. 독립 출처를 찾으니 `examples/mcp_config_examples/opencode-mcp.json` 이 **처음부터 `mcp` 를 쓰고 있었다** — 증인은 내내 있었고 아무도 대조하지 않았을 뿐이다. 검사를 그쪽에 대조하도록 다시 썼다.
- Verification: 되주입(표만 한 줄) — 잘못 짠 판에서는 6/6 PASS, 고쳐 쓴 판에서는 `표는 'mcp_servers' 인데 opencode-mcp.json 는 ['mcp'] 를 쓴다` 로 정확히 FAIL. 대조 못 한 하네스(`claude-code`)는 `[info]` 로 노출하고, 대조 3종 미만이면 실패시킨다(대상 0건은 통과가 아니다). 5종 렌더러 산출물 diff 0, `check_read_only_harness_mcp_examples` PASS, MCP round-trip **6종 전부 ok**, 격리 venv 전량 smoke **233/233**, mypy strict **123 files 0 errors**.
- Follow-up:
  - `transport_ready` 승격 조건 정의 (C안). `read_only_mcp_transport_promotion.md` §4 는 "false 인 동안 manual_review_only 유지" 만 적고 **무엇이 참이면 true 인가를 안 적었다** — 조건 없는 플래그는 영원히 false 다. 그리고 그 false 를 검사가 강제하고 있어(`if checked_in["transport_ready"] is not False: raise`) 옛 상태를 고정한다.
  - `claude-code` 용 독립 설정 예시를 `examples/mcp_config_examples/` 에 추가하면 방언 대조 대상이 5종이 된다.
