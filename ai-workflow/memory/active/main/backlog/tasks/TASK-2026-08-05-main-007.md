---
id: TASK-2026-08-05-main-007
status: done
created_at: 2026-08-05
source_anchor: generic-task-2026-08-05-main-007
source_path: backlog/2026-08-05.md
kind: generic
---

# TASK-2026-08-05-main-007 — 버전 고정 SDK 필드 이름을 작성 시점에 막는다 (A안)

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-08-05
- Owner: ykylee
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/tests/check_mcp_lowlevel_sdk_compat.py`

- Description: §2.64 가 B안(로컬 매트릭스)만 했고 A안(전역 금지)을 남겨 뒀다. 로컬 매트릭스는 "돌렸을 때" 잡으므로, 작성 시점에 막는 층을 더한다.
- Completion criteria: (1) 금지 목록이 손 목록이 아니라 실측 기반이고 썩으면 드러난다. (2) 위양성 0. (3) 되주입으로 production·테스트·목록 썩음 3방향이 각각 잡힌다.

## 🛠️ Implementation / Content

- Progress: 완료. 새 파일을 만들지 않고 `check_mcp_lowlevel_sdk_compat` 에 case 2개를 더했다 — 같은 주제이고, 새 파일이면 파생물 4종(CODE_INDEX / INSTALLATION / 릴리스 노트 누적 / core 사본)이 또 따라와야 한다.
- Next session starting point: push 후 CI 실측. **MCP 도구 13종이 실제 세션에 붙는지** 확인이 아직 남아 있다.
- Remaining risks: `getattr(obj, "isError")` 문자열 접근은 못 잡는다(버전 무관 접근자가 쓰는 형태라 의도적). mcp 를 import 하지 않는 파일이 *남에게서 받은* SDK 객체를 읽으면 못 잡는다 — 범위를 넓히면 위양성이 압도한다.

## ✅ Outcome

- Result: 두 번의 실측이 설계를 바꿨다.
  (1) **금지 목록이 `isError` 하나가 아니었다.** 5개 클래스만 훑어 11쌍으로 시작했는데, `mcp.types` 전체를 introspect 하니 **53쌍**이었다(`read_only_hint`/`readOnlyHint`, `list_changed`/`listChanged`, `input_schema`/`inputSchema` …). 손으로 고른 목록은 처음부터 불완전했고, 완전성 case 가 그것을 잡았다.
  (2) **저장소 전역 금지는 못 쓴다.** 53쌍에는 `task_id`, `created_at`, `max_tokens` 처럼 **우리 자신의 객체에서도 흔한 이름**이 섞여 있어, 전역 스캔은 30건 중 **28건이 위양성**이었다(backlog task 의 `.task_id`, memory index 의 `.created_at`). 위양성 내는 검사는 무시당하고 그러면 진짜 결함도 함께 무시된다(§2.48). 범위를 **AST 로 판정한 "실제 mcp 를 import 하는 파일"** (지연 import·`import_module("mcp…")` 포함) 7개로 좁히니 히트 2건, 위양성 0건.
  목록은 snake 이름만 저장하고 camel 은 `_camel()` 로 파생한다 — 53쌍 전부 규칙이 성립함을 실측했고, 두 목록을 나란히 두면 사본이 되어 갈라진다. 의도적 면제는 파일 목록이 아니라 그 줄의 `# sdk-field-ok: <이유>` 로 하고, 면제 건수를 매번 `[info]` 로 노출한다.
- Verification: 되주입 3방향 — (a) 새 검사에 `result.isError` → `check_mcp_apply_mode_criterion.py:130 .isError` 지목 FAIL, (b) production 에 2.x 이름 `.structured_content` → `read_only_mcp_sdk.py:113` 지목 FAIL, (c) 목록에서 `is_error` 제거 → mcp 2.0.0 에서 `설치된 SDK 에 있는데 없는 필드 1건: ['is_error']` FAIL. 1.27.0 / 2.0.0 양쪽 venv 에서 각각 10/10 PASS(2.0.0 에서는 "rename 53건 전부 선언 + 규칙 정합" 실측, 1.x 에서는 "대조하지 못했다" 를 밝히고 통과). 로컬 SDK 매트릭스 3버전 전부 PASS, 격리 venv 전량 smoke **234/234**, mypy strict **123 files 0 errors**.
- Follow-up:
