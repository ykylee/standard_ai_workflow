---
id: TASK-2026-08-05-main-008
status: done
created_at: 2026-08-05
source_anchor: generic-task-2026-08-05-main-008
source_path: backlog/2026-08-05.md
kind: generic
---

# TASK-2026-08-05-main-008 — transport_ready 를 wire 에서 제거했다 (§6.2 완료)

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-05
- Owner: ykylee
- Host:
- Host IP:
- Affected documents:
  - `workflow_kit/server/{read_only_registry,read_only_jsonrpc,read_only_mcp_sdk}.py`
  - `scripts/{generate_read_only_jsonrpc_fixtures,generate_read_only_harness_mcp_examples,scaffold_harness}.py`
  - `scripts/bootstrap_lib/mcp.py`, `scripts/bootstrap_lib/harnesses/renderers.py`
  - `schemas/read_only_{transport_descriptors,jsonrpc_fixtures,harness_mcp_examples}.json` (재생성)
  - 검사 8종 + 문서 16곳 + `mcp_servers/read_only_bundle.md`
  - `core/read_only_mcp_transport_promotion.md` §6.2 (+ `ai-workflow/core/` 사본)

- Description: §2.63 이 세 축을 정의하고 §6.2 에 제거 계획을 적어 뒀다. 그 계획을 실행한다.
- Completion criteria: (1) wire 산출물과 코드에서 `transport_ready` 가 사라진다. (2) 각 축이 답할 자격 있는 자리에서만 나온다. (3) 옛 필드를 고정하던 검사가 새 축을 고정한다. (4) 실제 서버 기동으로 확인한다.

## 🛠️ Implementation / Content

- Progress: 완료. §6.2 를 "제거 계획" → "제거 완료" 로 갱신.
- Next session starting point: push 후 CI 실측. **MCP 도구 13종이 실제 세션에 붙는지** 확인이 아직 남아 있다(이 세션은 `.mcp.json` 을 로드하지 못한다).
- Remaining risks: 소비자가 `_meta.transport_ready` 를 읽고 있었다면 breaking 이다. 옮길 자리를 §6.2 표에 적어 뒀다(단계→`transport_phase`, 정책→`apply_mode`, 런타임 능력→`sdk_runtime_status()["sdk_available"]`).

## ✅ Outcome

- Result: 4층에서 제거했다.
  (1) **registry ×4 — 제거**. registry 는 어느 transport 가 자기를 서빙할지 모르므로 답할 자격이 없다. `bundle_phase` 는 registry 자신의 사실이라 남긴다.
  (2) **jsonrpc bridge — `transport_phase: "jsonrpc_draft"` 로 통일**. `transport_ready` 와 `bridge_phase` 가 같은 축을 두 이름으로 말하고 있어서 하나로 접었다.
  (3) **sdk server — 런타임 능력을 `sdk_available` 이라는 이름으로**. tool-call `_meta` 의 자기모순(status 는 ready, 모든 호출은 not-ready)은 §2.63 에서 이미 고쳤고 이번에 필드 자체를 뺐다.
  (4) **하네스 예시 / bootstrap 산출물 — `transport_phase` + `apply_mode`**.
  덤으로 `mcp_servers/read_only_bundle.md` 에 붙어 있던 생성 산출물 JSON 사본이 **이미 갈라져 있었다** — `tool_count: 12`(실제 13), `tool_version: v0.4.1-beta`(실제 v1.0.0-beta), `apply_robust_patch` 누락. 아무도 그 블록이 틀렸다는 것을 몰랐다. 사본을 고치지 않고 **없애고** 정본 포인터로 바꿨다.
  ADR-003 의 "`transport_ready=false` 면 manual review only" 정책은 같은 뜻의 `apply_mode` 로 승계됐음을 그 ADR 에 supersede 로 표시했다(기록은 지우지 않는다).
- Verification: 재생성 diff 가 의도한 것만 — `transport_ready` 51곳 제거 + `transport_phase` 추가, 다른 변경 0. **저장소 `.mcp.json` 으로 실제 서버를 띄워 확인**: `tools/list meta = {descriptor_target, tool_count: 13, transport_phase: "jsonrpc_draft"}`, `tools/call meta = {tool, transport_phase}` — `transport_ready` 없음. 옛 필드를 고정하던 검사 8종을 새 축으로 옮겼고, 그중 3종은 옮기다 놓쳐 FAIL 로 드러났다(`bridge_phase` 잔존 1, `transport_ready` 잔존 2) — **검사가 제거를 완주시켰다**. 격리 venv 전량 smoke **234/234**, mypy strict **123 files 0 errors**, 로컬 SDK 매트릭스 3버전 전부 PASS, `check_standard_single_source` 7/7.
- Follow-up:
