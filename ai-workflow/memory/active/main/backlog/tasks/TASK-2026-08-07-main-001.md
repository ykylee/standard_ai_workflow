---
id: TASK-2026-08-07-main-001
status: done
created_at: 2026-08-07
source_anchor: generic-task-2026-08-07-main-001
source_path: backlog/2026-08-07.md
kind: generic
---

# TASK-2026-08-07-main-001 — MCP 도구 13종 세션 로드 검증 — 안 붙음 (mavis 가 글로벌 mcp.json 만 본다)

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-08-07
- 담당: AI Agent (ykylee 승인)
- 호스트명: homelab (darwin)
- 호스트 IP:
- 영향 문서:
  - `workflow-source/core/mcp_installation_by_harness.md` (1차 출처 보강 — §4/§6.5/§8)
  - `ai-workflow/memory/active/main/session_handoff.md` (§5 갱신)
  - `ai-workflow/memory/active/main/sessions/mcp_load_verification_2026-08-07.md` (신규)
  - `ai-workflow/memory/active/main/state.json` (재생성)

- 작업 내용: §2.66 / TASK-2026-08-05-main-002 의 "다음 세션 첫 확인" — `.mcp.json` 이 §2.60 에서 만들어졌고 §2.60 세션에선 시작 시점에 없어서 안 붙었음. 이번 세션(Aug 7 00:05 KST 시작)에는 *파일이 존재*한 상태로 시작. 두 가지를 실측:
  1. **서버 자체 검증** — `.mcp.json` 의 command/args 로 stdio 서버를 띄워 `initialize` / `tools/list` / `tools/call` 왕복. → §2.60 outcome 재확인.
  2. **세션 attach 검증** — mavis 가 *이 세션*에게 13종 MCP tool 을 native tool 로 노출했는지. → §2.59 skill 결함("로드는 됐는데 설명이 마커로 떴다")과 같은 *announce 됐지만* 실체는 다른 함정 패턴 재발 여부.

- 완료 기준: (1) 서버 실측 결과 기록. (2) 세션 attach 실측 결과 기록. (3) attach 가 안 된 경우 root cause 1차 출처로 1-layer 깊이까지. (4) 1차 출처 + handoff + state 갱신.

## 🛠️ Implementation / Content

- 진행 현황: 완료.
- 다음 세션 시작 포인트: 본 task 의 §"후속 후보" 두 항목. 둘 다 mavis 글로벌 config (`/Users/yklee/.minimax/mcp/mcp.json`) 에 standardAiWorkflowReadOnly 를 *등록하는 방향* 으로 갈라지는데, STANDARD_AI_WORKFLOW_ROOT 의 절대 경로 정책 + cwd 전제 부재라는 결정이 남아 있다.
- 남은 리스크: §2.66 의 `transport_ready` 제거 / §2.59 의 `stamp_marker` 와 별개로, **mavis attach 가 안 붙는 것 자체**가 (1) 도구 13종을 못 쓰는 불편 + (2) workflow agent 가 *왜* 못 쓰는지 모르는 사각지대. 본 task 는 (2) 만 닫았다.

## ✅ Outcome

- 작업 결과:
  - **(A) 서버 자체**: `PYTHONPATH=workflow-source STANDARD_AI_WORKFLOW_ROOT=. python3 -m workflow_kit.server.read_only_jsonrpc --stdio-lines` 으로 stdio 서버 기동. `initialize` → `protocolVersion: 2025-03-26`, `serverInfo: workflow_read_only_bundle v1.0.0-beta`, capabilities.tools.listChanged=false. `tools/list` → `_meta: {tool_count: 13, transport_phase: "jsonrpc_draft"}` (transport_ready 없음 — §2.66 wire 제거 정합). `tools/call latest_backlog {}` → `invalid_tool_payload_schema` (allowed_fields = `[backlog_dir_path, work_backlog_index_path]`, anyOf required). **서버는 정상이지만, TASK-2026-08-05-main-002 outcome 의 "isError=False" 는 정상 payload 였음** — 이번엔 빈 payload 의도적 검증. schema 가 *강화*된 게 아니라 *원래* 이 schema 였고 outcome 의 "isError=False" 는 `{"work_backlog_index_path": "..."}` 등을 줬을 것.
  - **(B) 세션 attach**: mavis 가 이 세션에게 노출한 tool 목록에 `mcp__*` prefix 가 *하나도 없음*. 보이는 mavis native tool 13종+ (image_synthesize, gen_videos, batch_text_to_audio, web_search, web_fetch, playwright, computer-use, transcribe_audio, synthesize_speech, batch_text_to_music, image_reverse_search, images_search_and_download, get_voice_list, ...) 은 모두 `/Users/yklee/.minimax/mcp/mcp.json` 의 5개 (matrix / playwright / cu / trash / github) 가 *native tool 로 재노출* 된 것. **`standardAiWorkflowReadOnly` 가 거기 등록돼 있지 않음** → 13종이 attach 되지 않음.
  - **root cause** (3-layer):
    1. mavis 가 MCP config 를 읽는 위치는 **글로벌** `{{DATA_DIR}}/mcp.json` (= `/Users/yklee/.minimax/mcp/mcp.json`) **하나**. workspace 단위 (예: `<project>/.mcp.json`) 자동 로드 *없음* — `{{DATA_DIR}}-<profile>/` 패턴만 있음. → standard_ai_workflow 의 `.mcp.json` (Claude Code 용 §2.60) 은 mavis 가 자동으로 안 읽음.
    2. mavis 공식 user-guide: *"MCP servers live in `{{DATA_DIR}}/mcp.json`. Configure directly ... and validate via MiniMax Code's built-in MCP tooling."* + *"changing config affects new sessions; old sessions keep their existing runtime context until they rotate"*. → **이 세션은 rotate 안 되면 attach 안 됨** (현재 세션은 Aug 7 00:05 KST 시작, .mcp.json 은 Aug 6 09:19 에 존재했으나 글로벌 mcp.json 에 등록 안 됨).
    3. **1차 출처 문서(`mcp_installation_by_harness.md` §6.5) 가 stale** — MiniMax Code 의 글로벌 위치를 `~/.MiniMax/mcp.json` (legacy 빌드) 로 적고 있고, mavis 가 *현재* 읽는 `~/.minimax/mcp/mcp.json` (소문자, `/mcp/` 디렉터리) 는 §4 표/§6.5 어디에도 없음. → 정공법 단락 자체가 잘못된 자리에 있었음.

- 검증 결과:
  - 실측 (A) raw stdio line-by-line — `initialize` / `tools/list` 13종 / `tools/call latest_backlog` schema 거부 = 3/3 실측 (서버 자체 ✅, 빈 payload 거부는 schema 정합 — 옳음).
  - 실측 (B) mavis tool 목록 grep — `mcp__` / `mcp-` / `standardAiWorkflowReadOnly` 0 hit. 보이는 13종 native tool 모두 글로벌 5개에서 옴.
  - 1차 출처: `mcp_installation_by_harness.md` §4 표의 MiniMax Code 행 갱신 + §6.5 재작성 (mavis / legacy 두 위치) + §1.2 끝에 MiniMax Code (mavis) 절대 경로 정책 한 줄 + §8 에 후속 TASK 1건 추가.
  - handoff: §5 의 "첫 번째로 할 일 — MCP 도구 13종이 실제로 붙는지" → **닫힘** 표시 + §2.67 session link + 후속 후보 §2.68 로 이동.
  - state: `generate_workflow_state.py` 재생성 (manual edit X).

- 후속 작업:
  1. **글로벌 mcp.json 등록** — `/Users/yklee/.minimax/mcp/mcp.json` 의 `mcpServers` 에 `standardAiWorkflowReadOnly` 추가. command = `python3`, args = `["-m", "workflow_kit.server.read_only_jsonrpc", "--stdio-lines"]`, env = `{"STANDARD_AI_WORKFLOW_ROOT": "/Users/yklee/repos/standard_ai_workflow", "PYTHONPATH": "/Users/yklee/repos/standard_ai_workflow/workflow-source"}` (모두 *절대 경로* — 글로벌은 cwd 전제 없음). **새 세션에서 검증** (rotate 후 native tool 13종 노출 여부).
  2. **bootstrap 에 mavis 자동 등록** — `bootstrap_workflow_kit.py` 의 `--harness minimax-code` (또는 신규 alias `mavis`) 가 글로벌 mcp.json 까지 emit 하도록. 1차 출처 §6.5 가 두 위치를 다루니 bootstrap 도 두 위치 옵션 필요. §1.2 의 "글로벌 설정은 절대 경로" 정책과 정합.
  3. **handoff §2.66 의 §6.2 / §2.59 의 stamp_marker 와 별개** — mavis 가 글로벌 mcp.json 의 *변경*을 기존 세션에 전파하지 않는다는 사실이, 다른 §2.66 breaking 들과 *같은 부류* (consumer 에게 silent) 인지 별도 사이클로 점검. 본 task 의 scope 아님.
