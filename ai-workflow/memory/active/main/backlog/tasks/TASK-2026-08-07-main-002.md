---
id: TASK-2026-08-07-main-002
status: done
created_at: 2026-08-07
source_anchor: generic-task-2026-08-07-main-002
source_path: backlog/2026-08-07.md
kind: generic
---

# TASK-2026-08-07-main-002 — 멀티 워크스페이스 오케스트레이션 설계 (사용자 · 서버 · 하네스 3계층)

## 📝 Description

- 상태: done
- 비고: 설계 문서화까지. 구현은 별도 task.
- 우선순위: medium
- 요청일: 2026-08-07
- 담당: AI Agent (ykylee 요청)
- 호스트명: homelab (darwin)
- 영향 문서:
  - `workflow-source/core/multi_workspace_orchestration.md` (신규 — 설계 정본)
  - `ai-workflow/memory/active/main/session_handoff.md` (§5 갱신)
  - `ai-workflow/memory/active/main/state.json` (재생성)

- 작업 내용: "사용자 - 서버 - 하네스" 3계층으로 워크스페이스를 나누고, 각 하네스
  워크스페이스에서 에이전트가 워크플로우를 돌리며, 중앙 메인 에이전트가 전체를 취합해
  `main` 을 관리하는 컨셉의 **설계 문서화**. 구현은 본 task 범위 아님.

- 완료 기준: (1) 기존 구현 현황 실측. (2) 컨셉과 기존 기계장치의 중첩/공백 구분.
  (3) 격리 키 결정과 그 근거를 실측으로 뒷받침. (4) 설계 문서 작성.

## 🛠️ Implementation / Content

- 진행 현황: 설계 문서 작성 완료. 구현 미착수.
- 남은 리스크: 본 문서는 *설계* 이며 어떤 코드도 바꾸지 않았다. §7 의 확장 지점
  (`_branch_state_paths` 복수 root) 은 아직 구현/검증되지 않은 제안이다.

## ✅ Outcome

- 작업 결과:
  - **실측 (A) 기존 기계장치**: 컨셉의 하부 구조가 이미 있다.
    - 격리: `active/<branch>/` branch-scoped memory (v1.0.0 결정).
    - 취합: `dashboard_data.py:1267` `_branch_state_paths()` 가 `active/` 를
      `rglob("state.json")` 으로 훑어 합친다. MEMORY_GOVERNANCE.md:218 의 *"집계는
      파일이 아니라 뷰"* 는 문서상 다짐이 아니라 실제 코드였다.
    - 주입점: `paths.py:9` `BRANCH_ENV_KEYS` 5종 환경변수로 slug override 가능.
  - **실측 (B) env override 거동** — `get_current_branch()`:
    `no env -> main` / `feat-a -> feat-a` / `harness/codex -> harness/codex` (슬래시
    허용, 중첩 디렉터리) / `../escape -> main` (traversal 거부).
  - **실측 (C) — 결정적 발견**: `tools/archive_branch_memory.py` 의
    `find_branch_memories()` + `branch_exists()` 를 실제 호출. 임시 트리
    (`active/feat-x/codex`, `active/feat-x/claude`, `active/main`) 로 검증:
    ```
    name='feat-x/claude' -> branch_exists? False
    name='feat-x/codex'  -> branch_exists? False
    name='main'          -> branch_exists? True
    ```
    → `active/<name>/` 의 `<name>` 이 **실재하는 git 브랜치가 아니면 고아로 판정되어
    `archived/<name>/` 로 이동**한다. 즉 하네스 이름 디렉터리(`active/codex/`)나
    하네스 중첩(`active/<branch>/<codex>/`)은 자동 아카이브 도구가 치운다.
    **격리 키는 git 이 검증 가능한 브랜치여야 한다** — 이것이 격리 키 결정의 근거.
  - **결정**: 격리 키 = 작업/브랜치. 하네스는 워크스페이스의 *속성*(metadata).
    사용자 질문("같은 서버에서 여러 하네스 동시")의 답 = 하네스마다 **자기 브랜치 +
    자기 worktree**, 점유는 lease 로 배타.
  - **공백**: `_branch_state_paths(root)` 가 단일 root 만 받는다. 미병합(in-flight)
    워크스페이스를 가로지르는 취합은 문서·구현 모두 부재. cross-repo grep 1건, 그것도
    `adr-016` 의 의도적 out-of-scope 표기.

  - **실측 (D) 서버 식별자** — hostname/IP 를 식별자로 쓸지 검토. 기존 task 템플릿에
    이미 `호스트명` / `호스트 IP` 필드가 있어 전체 메모리(66 파일) 실집계:
    `호스트 IP` 빈칸 87 / `127.0.0.1` 20 / `192.168.0.139` 12,
    `호스트명` 빈칸 35 / `homelab (darwin)` 2.
    → (1) 손으로 채우는 필드는 **안 채워진다**. (2) `127.0.0.1` 은 아무것도 식별 못 함.
    (3) `192.168.0.139` 는 DHCP 사설 대역 — 재사용됨. (4) 기록된 `homelab` 은 이 머신의
    실제 hostname (`iyeong-gyun-ui-MacBookAir.local`) 과 **이미 갈라져 있음**.
    **결정**: `host_id`(registry 발급 안정 ID) = 정본 식별자, hostname/IP = 자동 수집
    진단 표시, `endpoint` = 접속 정보로 3분리. 문서 §4.5.
    참고: `read_only_registry.py:207` 의 `create_environment_record_stub` 도 hostname 을
    *호출자 필수 인자* 로 받아 자동 수집이 아니다 — 같은 함정.

  - **실측 (E) 인벤토리 별도 관리 검토** — 이미 그 자리가 있고 **비어 있다**:
    `state.json.environment_path` = `ai-workflow/memory/active/environments/` 인데
    디렉터리가 **실재하지 않음**. bootstrap 기본값(`bootstrap_lib/__main__.py:315`) 과
    전용 생성 도구(`create_environment_record_stub` MCP) 까지 있는데,
    **그 기록을 읽는 코드가 없다** — `environment_path` 는 `project_docs.py:266` 에서
    읽어 `builder.py:445` 로 state.json 에 옮겨 담기만 한다.
    → §4.5.1 의 빈칸 87건과 **같은 실패 구조** (쓰는 곳은 있는데 읽는 곳이 없음).
    **결정**: registry 는 만들되 (a) 소비자를 같은 사이클에 함께, (b) 자동 등록,
    (c) git 밖, (d) `environments/`(사람용 진단 스냅샷) 와 역할 분리. 문서 §4.5.5~§4.5.6.

  - **실측 (F) 다중 호스트 동시 작업 충돌** — bare remote + 2 클론으로 재현:
    - 서로 다른 브랜치 + `active/<branch>/` → **충돌 0, 깨끗이 병합** (§3 격리 키 결정이
      다중 호스트에서도 유효함 확인).
    - 서로 다른 브랜치 + 공유 `log.md` → **CONFLICT**. `memory/active/README.md` §2 는
      `log.md` 를 "append-only, 충돌 없음" 으로 분류하지만 **그건 한 저장소 전제**다.
    - 같은 브랜치 + 파일 통째 rewrite → **조용한 데이터 소실** (git 이 충돌 미보고,
      TASK-001 이 HEAD 에서 사라짐). 최악 케이스.
    - 같은 브랜치 + 진짜 append(`>>`) → CONFLICT (시끄럽지만 복구 가능).
    - **`.gitattributes` union merge 실측**: 적용 후 `log.md` 양쪽 보존 충돌 0,
      daily backlog index 도 A·B task 모두 생존. **단 `state.json` 에 걸면
      `{"a":3}\n{"a":2}` 로 JSONDecodeError — union 은 구조화 포맷을 깨므로 금지.**
    - **이 저장소에 `.gitattributes` 가 없음** → 위 `log.md` 충돌은 오늘도 재현 가능.
    - 우선순위: (1) `.gitattributes` union (2) whole-file rewrite 금지 규약
      (3) lease 로 브랜치당 1 에이전트 (4) 병합 직렬화. 문서 §5B.

  - **실측 (G) 우선순위별 상세 검토** — §5B.8 의 4개 조치를 각각 실측 (문서 §5C):
    - **P1 union merge — 채택(조건부)**. JSONL 3줄 valid, 저장소 자체 파서
      `parse_backlog` 로 union 병합본 파싱 시 **task 4건 정상 인식 / warning 0**,
      머리말 중복 0. **단 동일 문자열 2줄 append 시 중복 제거되어 1건 소실** →
      완화책은 로그 라인에 `host_id` + 타임스탬프 포함 (실측으로 양쪽 보존 확인).
      → §4.5 `host_id` 의 2번째 용도.
    - **P2 — 진단 정정**. §5B.2 의 "조용한 소실" 은 *손으로 덮어썼을 때*였고,
      실제 도구 `upsert_backlog_entry`(`workflow_writes.py:150`)는 read-modify-write 라
      그렇게 동작하지 않음. 두 호스트 동시 갱신 실측 → union 없이 **CONFLICT(가시적)**,
      union 적용 시 900·901 both 보존. **규약 문구를 "도구를 통해 갱신" 으로 정정,
      새 구현 불필요.**
    - **P3 lease — 구현 방식 변경**. (a) 로컬 `O_EXCL` 10 프로세스 경합 → 정확히 1명 획득.
      (b) git 에 두면 둘 다 획득했다고 믿고 병합 때야 CONFLICT — 배타 제어 불가(확증).
      (c) **TTL 회수에 race** — 순진한 구현은 동시 steal 시 **2명 동시 획득 + 크래시**.
      해법으로 `os.link` 원자 claim 패턴 실측 → 5 프로세스 경합에서 정확히 1명.
      제약: 같은 파일시스템 전제, 진짜 다중 호스트는 서비스/DB 필요.
    - **P4 병합 직렬화 — 강등(구현 불필요)**. git 이 이미 직렬화: 동시 push 시
      `! [rejected] (fetch first)`. pull 후 재시도로 양쪽 보존 후 push 성공 실측.
      애플리케이션 병합 큐는 중복 → §7 에서 제외.

  - **적용 (H) P1·P2 반영 + 신규 플로우 검토**:
    - **P1 적용**: `.gitattributes` 신규 생성 (저장소 최초). `git check-attr` 로 대상 검증 —
      `log.md` / `**/telemetry/*.jsonl` / `active/**/backlog/*.md` = `union`,
      **`state.json` 과 task 파일은 `unspecified`** (의도대로 제외).
      저장소 clone 에서 실제 병합 검증: `log.md` 충돌 → union 으로 양쪽 보존 0 conflict,
      `state.json` 은 의도대로 **CONFLICT 발생**(조용히 깨지지 않음).
    - **P2 적용**: `ai-workflow/memory/active/README.md` §4 에 규약 2건 추가
      ("도구를 통해 갱신", "공유 append 라인에 타임스탬프+host 식별자") + §2 충돌 표면
      표에 *"본 표는 한 저장소 전제"* note 추가 (`log.md` 행 정정).
    - **신규 플로우 실측 — lease 를 대체함**:
      - 5개 클론이 같은 브랜치명 동시 선점 → **정확히 1명 성공** (git ref 생성이 원자적
        = 분산 CAS). §5C.3 의 `os.link` 원자 claim 을 git 이 서버측에서 제공.
      - TOCTOU 안전: 조회가 낡아도 push 에서 `rejected` — 판정은 push 에서만 일어남.
      - 원격 조회 실측: `git ls-remote --heads` (활성 브랜치=진행중 작업),
        `git log origin/<b> --format='%an %ar' -1` (owner/시각),
        `git show origin/<b>:<path>` (**체크아웃 없이 남의 작업계획 read**).
      - 취약점 2건: (a) **TTL 없음** — 죽은 에이전트 브랜치 영구 점유, git 은 마지막
        커밋 시각만 앎 → stale 은 heuristic, **자동 삭제 금지·보고만**.
        (b) **`--force` 통과** — 배타성은 규약이지 강제가 아님 → `--force` 금지 규약 +
        가능하면 서버측 branch protection.
      - **결정**: §4.2/§5C.3 의 파일 lease 를 **브랜치 선점으로 교체**. 새 인프라 0,
        registry 는 `host_id` 매핑 + in-flight 가시성으로 역할 축소. 문서 §5D.

  - **결정 (I) 취약점 2건을 사람 확인 게이트로 닫음 (ykylee 지시)**:
    - **(a) stale 임계 = 마지막 활동 1일(86400s)**. 초과 시 **사용자에게 문의**,
      에이전트가 삭제·선점하지 않는다. 실측 검증 (0h/12h/72h 브랜치 생성):
      `dead idle=72h → STALE`, `fresh 0h` / `half 12h → active`. 판정 스크립트는 §5D.4a.
    - **⚠️ 실측으로 발견한 필수 전제**: **`git fetch --prune` 선행 없으면 오판**.
      다른 호스트가 브랜치를 되살려도 로컬 remote-tracking ref 가 낡으면 여전히
      `idle=72h` 로 보임 (fetch 전 72h → fetch 후 0h 실측). **살아있는 작업을 지우자고
      사용자에게 제안하게 되므로** stale 판정 직전 fetch 필수.
    - 지표는 `%ct`(commit date) 사용 — `%at`(author date)는 rebase/cherry-pick 시 원본
      시각을 유지해 활동성 지표로 부적합.
    - **(b) `--force` 는 사용자 확인 후에만**. 에이전트 자율 실행 금지.
      push `rejected` 는 *뚫어야 할 장애* 가 아니라 **남이 이미 가져갔다는 신호** —
      기본 대응은 다른 작업 선택. 가능하면 서버측 branch protection 으로 이중화.
    - 공통 원칙: **되돌릴 수 없는 작업(남의 브랜치 삭제·덮어쓰기)은 에이전트 단독 결정 ❌**.

  - **정리 (J) 신규 워크플로우 정본화**: 문서가 조사 순서대로 쌓여 최종 결론이 여러 절에
    흩어져 있었음(§4.2 → §5C.3 → §5D 로 결정이 2회 뒤집힘). **§0 "확정 워크플로우
    (정본 요약)" 를 맨 앞에 신설** — 한 줄 요약 / 3계층 매핑 / 핵심 결정 5 / 세션
    워크플로우 다이어그램 / 단계별 필수사항 / 충돌 방지 규약 / 지시 전달 방식 /
    적용 상태 / 열린 질문. 헤더에 **읽는 순서 + 뒤집힌 절 교체 표시 표** 추가.
    §0 이 인용한 절 번호 10개 전부 실재 확인, §0.7 "적용됨" 주장은 `git check-attr` 과
    README grep 으로 재확인.
    `core/README.md` 주요 진입 문서에 1줄 등록 (발견 가능성).
  - **발견 (K) 배포본 core 는 큐레이션된 부분집합**: `workflow_agent_topology.md` 에
    신규 문서 링크를 추가했더니 `check_standard_single_source.py` 의
    `test_distributed_core_matches_canonical` 이 FAIL (2건). 확인 결과
    `ai-workflow/core/` 는 23개 파일만 있는 **부분집합** 이고 `multi_workspace_orchestration.md`
    도 `README.md` 도 없음. 정본에 링크를 넣으면 배포본 사본이 **소비자에게 깨진 링크**가
    된다 → **링크 추가를 되돌림**. 사전 존재 1건(`mcp_installation_by_harness.md`)만 남김.
    교훈: `workflow-source/core/*.md` 를 고칠 때는 배포본 동기화 제약을 먼저 본다.

- 검증 결과:
  - 설계 문서 `workflow-source/core/multi_workspace_orchestration.md` 작성.
  - `check_doc_metadata` / `check_doc_links` 로 신규 문서 검증 (아래 §후속 참조).
  - **별건 발견(본 task 무관)**: `dashboard` 실행 시 `drift_prevention.guard_status:
    fail` 1건 — `test_case_6_maturity_last_updated_freshness`. `maturity_last_updated`
    가 2026-07-22 인데 surface 변경은 2026-08-05 (delta 16일). 기록만 남긴다.

- 후속 작업:
  1. 설계 리뷰 후 구현 착수 여부 결정. 구현 시 1순위는 §7 의 `_branch_state_paths`
     복수 root 확장 + workspace registry.
  2. `maturity_last_updated` stale 갱신 (별건).
