---
id: TASK-2026-08-07-main-003
status: done
created_at: 2026-08-07
source_anchor: generic-task-2026-08-07-main-003
source_path: backlog/2026-08-07.md
kind: generic
---

# TASK-2026-08-07-main-003 — 다중 작업·팀웍 워크플로우 정식 반영 (표준 §10 + §1)

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-08-07
- 담당: AI Agent (ykylee 지시)
- 호스트명: homelab (darwin)
- 영향 문서:
  - `workflow-source/core/global_workflow_standard.md` (정본 — §10 신설 + §1 bullet 2건)
  - `ai-workflow/core/global_workflow_standard.md` (배포본 동기화)
  - `workflow-source/workflow_kit/common/_standard_rules_snapshot.py` (재생성)
  - `CLAUDE.md` / `.claude/skills/standard-ai-workflow/SKILL.md` (규칙 블록 재생성)
  - `workflow-source/core/multi_workspace_orchestration.md` (§0.7 정본 관계 명시)

- 작업 내용: TASK-2026-08-07-main-002 에서 설계한 다중 워크스페이스/팀웍 워크플로우를
  **표준 문서에 정식 반영**. 사용자 결정: (1) 표준 §1 + 신규 §10 전면 반영,
  (2) 단일 에이전트 프로젝트에도 **무조건 적용**.

- 완료 기준: (1) 표준에 규칙 반영. (2) 스냅샷/배포본/진입점 재생성. (3) 신규 bootstrap
  에서 다른 하네스로 전파되는지 실측. (4) 회귀 검사가 baseline 을 넘지 않을 것.

## 🛠️ Implementation / Content

- 진행 현황: 완료. **규칙은 반영됐고 도구 구현은 여전히 미착수**(§7).
- 남은 리스크: §10 은 절차를 규정하지만 이를 자동화하는 도구(registry / seed / stale
  보고)가 없다. 당분간 사람과 에이전트가 절차를 손으로 지켜야 한다.

## ✅ Outcome

- 작업 결과:
  - **표준 §10 "다중 작업과 협업" 신설** — 10.1 작업 단위와 격리 / 10.2 세션 시작(확인
    후 선점) / 10.3 공유 파일 취급 / 10.4 사용자 확인이 필요한 작업.
    구현 세부(명령어·경로)를 넣지 않고 **원칙만** 기술 — 표준은 얇게 유지(§1 마지막 bullet)
    하고 근거는 `multi_workspace_orchestration.md` 로 링크.
  - **§1 공통 원칙에 bullet 2건 추가** (6 → 8):
    원격 동기화 후 겹치지 않는 작업 선택 / 되돌릴 수 없는 작업은 사용자 확인.
    §1 은 `standard_rules.parse_standard` 가 추출해 **12개 하네스 진입점에 자동 주입**되는
    자리라, 이 2건이 모든 소비자 에이전트에게 전달된다.
  - **연쇄 재생성 4곳**: 스냅샷(`--apply`), 배포본 사본(cp), `CLAUDE.md`,
    `.claude/.../SKILL.md`. 진입점 2곳은 정본 렌더러(`render_entrypoint_rules`)로
    생성해 손 транс크립션 오류를 배제.

- 검증 결과:
  - `parse_standard` 재실행 → §1 bullet 8개 정상 추출.
  - `check_standard_single_source.py`: `test_snapshot_matches_standard` PASS,
    `test_generated_entrypoints_carry_rules` PASS, `test_renderers_have_no_rule_literals`
    PASS. FAIL 은 사전 존재 1건(`mcp_installation_by_harness.md`, c63b54e 부터)뿐.
  - `check_self_application.py` 7/8 (사전 존재 `handoff_bloat`).
  - `check_bootstrap` PASS / `check_bootstrap_mcp_roundtrip` PASS.
    `check_bootstrap_interactive_picker` FAIL 은 **stash 로 baseline 대조해 사전 존재** 확인
    (`ModuleNotFoundError: bootstrap_lib.__main__`).
  - **전파 실측**: 빈 git 저장소에 `bootstrap --harness codex --harness gemini-cli` 실행 →
    생성된 `AGENTS.md` **2/2**, `GEMINI.md` **2/2** 신규 협업 규칙 포함. 즉 신규 소비자
    프로젝트는 자동으로 팀웍 규칙을 받는다.
  - 진입점 diff 는 각각 **+2줄** 로 최소(섹션 구조 보존 확인).

- 사고 기록 (반복 방지):
  - `CLAUDE.md` 편집 시 `text.index('## 작업 원칙')` 이 **본문 인용구(L14)** 에 먼저 걸려
    상위 40줄이 삭제됐다. `git checkout` 으로 즉시 복원 후 `re.search(r'^## …$', re.M)`
    **줄 앵커**로 재작업. 교훈: 마크다운 섹션 치환은 반드시 줄 앵커를 쓴다.

- 후속 작업:
  1. §7 구현 착수 — workspace registry → 메모리 seed 도구 → 복수 root 취합.
  2. 사전 존재 red 3종 정리(`handoff_bloat`, `mcp_installation` 사본,
     append-only frontmatter 3건, bootstrap picker import).
