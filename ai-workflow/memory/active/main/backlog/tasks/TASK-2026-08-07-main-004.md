---
id: TASK-2026-08-07-main-004
status: done
created_at: 2026-08-07
source_anchor: generic-task-2026-08-07-main-004
source_path: backlog/2026-08-07.md
kind: generic
---

# TASK-2026-08-07-main-004 — 메모리 seed 도구 구현 (표준 §10.2 자동화 1단계)

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-08-07
- 담당: AI Agent (ykylee 지시)
- 호스트명: homelab (darwin)
- 영향 문서:
  - `workflow-source/tools/seed_workspace_memory.py` (신규)
  - `workflow-source/tests/check_seed_workspace_memory.py` (신규)
  - `workflow-source/core/multi_workspace_orchestration.md` (§0.4 · §0.7 · §7 갱신)

- 작업 내용: 표준 §10.2 의 "브랜치를 만들고 작업 상태 문서를 먼저 생성" 을 자동화.
  새 워크스페이스가 `missing_required_document` 로 시작조차 못 하는 실측 실패를 닫는다.

- **착수 순서 변경 근거**: §7 은 registry 를 1순위로 뒀지만, registry 는 *저장 위치가
  미결*(§0.8 — 다중 호스트면 파일 기반이 성립하지 않음)이라 지금 만들면 틀린 것을
  만들 위험이 있다. seed 도구는 **실측된 실패가 있고 열린 질문이 0** 이라 먼저 착수했다.

- 완료 기준: (1) seed 후 `session-start` 가 `status=ok`. (2) 멱등. (3) 회귀 검사.
  (4) 실제 worktree 에서 end-to-end 실측.

## 🛠️ Implementation / Content

- 진행 현황: 완료.
- 설계 결정:
  - **`state.json` 은 seed 하지 않는다** — 파생 파일이므로
    `generate_workflow_state.py` 가 rebuild 한다 (`memory/active/README.md` §4 정합).
    도구가 안내 메시지로 다음 명령을 출력한다.
  - **멱등** — 이미 있는 파일은 skip, `--force` 로만 덮어쓴다. 진행 중 워크스페이스를
    실수로 초기화하면 남의 작업을 지우는 것과 같기 때문 (표준 §10.4 정합).
  - **task 번호는 브랜치 안에서만 채번** — 다른 호스트가 동시에 만들어도 안 겹친다.
  - 포맷은 손으로 만들지 않고 기존 writer(`upsert_backlog_entry` / `render_task_file`)
    재사용 — 사본이 갈라지는 것을 피한다.

## ✅ Outcome

- 작업 결과:
  - `tools/seed_workspace_memory.py` — `--branch/--axis/--task-title/--out-of-scope`,
    `--dry-run`(기본) / `--apply` / `--force` / `--json`.
  - `tests/check_seed_workspace_memory.py` — 8 assertions.

- 검증 결과:
  - smoke **8/8 PASS**, runner 통합 확인(`--filter=seed`), `-W error::SyntaxWarning` 통과.
  - `--filter=memory` 8종 중 seed 포함 7 PASS. FAIL 은 사전 존재
    `check_appendonly_memory_layout` 3건(2026-08-06 task frontmatter)뿐.
  - **end-to-end (실제 worktree)**: `git worktree add -b e2e-seed-demo` →
    seed 전 `status=error / missing_required_document` **재현** → seed 후
    **`status=ok`**, in_progress 1건 정상 인식. worktree/브랜치 정리 완료.
  - 격리 환경 smoke: `status=ok`, **warnings `[]`** (완전 무경고 복원).

- **구현 중 잡은 결함 3건 (전부 "파일은 생겼는데 복원은 안 되는" 부류)**:
  1. 섹션 라벨을 `주 작업 축` 으로 썼는데 정본은 `현재 주 작업 축` → 필수 섹션 누락 warning.
  2. 기준선 문구에 백틱을 넣어 summary 에 깨진 문자열(`feat-login\`` )이 새어 나옴.
  3. 진행 중 작업을 `- <ID> <제목>: <상태>` 로 썼는데, 파서는 **`현재 \`in_progress\` 작업:`
     라벨 뒤의 목록**을 읽음 → handoff in_progress 가 빈 리스트가 되어 backlog 와 불일치 warning.
  → 셋 다 파일 존재만 보는 검사로는 못 잡는다. 그래서 회귀 검사를 **"seed 후
  session-start 가 ok + warnings 0"** 로 잡았다 (파일 생성 여부 ❌).

- 후속 작업:
  1. 원격 조회/선점 자동화 (표준 §10.2 의 1~3단계 — 현재는 손으로).
  2. workspace registry — **저장 위치 결정이 선행**되어야 한다(§0.8).
  3. 사전 존재 red 정리: append-only frontmatter 3건 등.
