---
id: TASK-2026-08-08-main-002
status: done
created_at: 2026-08-08
source_anchor: generic-task-2026-08-08-main-002
source_path: backlog/2026-08-08.md
kind: generic
---

# TASK-2026-08-08-main-002 — 워크스페이스 선점 도구 (표준 §10.2 4~5단계, 플로우 완결)

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-08-08
- 담당: AI Agent (ykylee 지시)
- 호스트명: homelab (darwin)
- 영향 문서:
  - `workflow-source/tools/claim_workspace.py` (신규)
  - `workflow-source/tests/check_claim_workspace.py` (신규)
  - `workflow-source/core/multi_workspace_orchestration.md` (§0.4 · §0.7 · §5D.5)

- 작업 내용: 표준 §10.2 의 4~5단계(브랜치 생성 + seed + 작업 예정 내역 + 1회 push).
  이로써 §10.2 **전 단계가 도구화**된다 (조회 → 선정 근거 → seed → 선점).

- 완료 기준: (1) 동시 경합에서 1명만 성공. (2) rejected 를 신호로 처리(재시도 ❌).
  (3) `--force` 수단 부재. (4) 진 쪽 로컬 작업 보존. (5) 회귀 검사. (6) end-to-end 실측.

## 🛠️ Implementation / Content

- 진행 현황: 완료.
- 설계 결정:
  - **`--force` 를 제공하지 않는다.** 배타성이 규약일 뿐 강제가 아니라서(§5D.4b 실측)
    도구가 그 수단을 가지면 언젠가 쓰인다. 강제로 밀어야 하면 사람이 직접 한다.
  - **rejected 는 정상 경로**이므로 exit code 0. 진짜 오류(브랜치 생성/commit 실패)만 1.
  - **진 쪽 로컬 브랜치를 되돌리지 않는다** — 조용히 지우면 사용자가 상태를 확인할 수
    없다. 남겨 두고 안내한다.
  - 사전 `ls-remote` 확인은 **힌트일 뿐** — 판정은 push 가 한다(TOCTOU 안전, §5D.2).

## ✅ Outcome

- 작업 결과:
  - `tools/claim_workspace.py` — `--branch/--axis/--task-title/--out-of-scope/--base`,
    `--dry-run`(기본) / `--apply` / `--json`. status: `ok` / `already_claimed` /
    `lost_race` / `error`.
  - `tests/check_claim_workspace.py` — 9 assertions.

- 검증 결과:
  - smoke **9/9 PASS**, runner 통합. seed·survey 검사도 그대로 PASS (3종 전부 green).
  - **end-to-end (bare remote + 4 에이전트 동시 경합)**:
    - `agent3 -> ok`, 나머지 3명 `lost_race` — **정확히 1명만 성공**.
    - 진 3명 모두 선점자(agent3)를 정확히 보고.
    - 원격에 `refs/heads/feat-contested` **1개만** 존재 (덮어쓰기 없음).
    - 승자의 seed 가 원격에 온전 (`현재 주 작업 축: 경합 축 3`).
    - 승자가 곧바로 `session-start` → **`status=ok`, warnings `[]`**.
    - 진 쪽 로컬 브랜치 보존 확인.
    - 진 쪽이 `survey` 재실행 → `feat-contested owner=agent3` 로 현황 갱신 확인.
  - 실제 저장소에서 `--branch mooneye` 선점 시도 → `already_claimed` + owner 표시,
    **git 상태 무변경** 확인.

- **회귀 검사의 음성 대조**: `test_no_force_push_capability` 가 실제로 force 를 잡는지
  확인하려고 도구에 `push --force` 를 일시 주입 → **FAIL 재현**, 원복 후 PASS.
  검사가 공허하게 통과하는 게 아님을 확인했다.
  (초안에서는 안내 문구의 `--force` 문자열까지 잡아 위양성이 났고, git 호출 인자만
  보도록 좁혔다.)

- 후속 작업:
  1. workspace registry — **다중 호스트 경로 매핑만** 남는다. 단일 호스트면 불필요.
  2. 복수 root 취합 (`_branch_state_paths` 가 root 목록을 받도록).
  3. 사전 존재 red 정리: append-only frontmatter 3건, handoff_bloat 등.
