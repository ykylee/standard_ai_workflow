---
id: TASK-2026-08-08-main-005
status: done
created_at: 2026-08-08
source_anchor: generic-task-2026-08-08-main-005
source_path: backlog/2026-08-08.md
kind: generic
---

# TASK-2026-08-08-main-005 — 사전 존재 red 2건 정리 (회귀 정합 회복)

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-08-08
- 담당: AI Agent (ykylee 지시)
- 호스트명: homelab (darwin)
- 영향 문서:
  - `ai-workflow/core/mcp_installation_by_harness.md` (사본 → 정본 동기화)
  - `workflow-source/tests/check_bootstrap_interactive_picker.py` (sys.path 1줄)
  - `ai-workflow/memory/active/main/session_handoff.md` (§6 red 표 갱신)
  - `ai-workflow/memory/active/main/state.json` (recent_done_items)

- 작업 내용: 세션 시작 시 계속 보이던 사전 red 2건의 *실제 원인* 확인 후 닫는다.
  - **red 1**: `check_standard_single_source` — `ai-workflow/core/mcp_installation_by_harness.md`
    사본이 정본(`workflow-source/core/...`)과 diverge. 정본 = 2026-08-07 00:12 갱신
    (mavis 데스크탑 런타임 §1.2.1 + §6.5.2 추가). 사본 = 2026-08-06 09:19 정본. *정본이
    우선* 원칙 (cf. PROJECT_PROFILE.md §5) 으로 cp 동기화.
  - **red 2**: `check_bootstrap_interactive_picker` — `from bootstrap_lib.__main__ import ...`
    `ModuleNotFoundError`. `bootstrap_lib` 은 `workflow-source/scripts/bootstrap_lib/` 에
    있고, in-process test들이 `sys.path.insert(0, SCRIPTS_DIR)` 없이 동작하는 게 원인이었다.
    subprocess 테스트(`cwd=SCRIPTS_DIR`) 와 in-process 테스트의 path 처리가 비대칭.
    표준 패턴(다른 bootstrap 회귀 검사가 `sys.path.insert(0, SOURCE_ROOT)` 만 쓰는 것과
    다른 케이스) — 본 검사는 `bootstrap_lib` 이 `scripts/` 안에 있으므로 *둘 다* 필요.
    1줄 추가.

- 완료 기준: (1) `check_standard_single_source` 7/7 PASS. (2) `check_bootstrap_interactive_picker`
  10/10 PASS. (3) dashboard 3종 + 도구 3종 + 이전 신규 2종 회귀 모두 green 유지.

## 🛠️ Implementation / Content

- 진행 현황: 시작.
- 설계 결정:
  - **red 1 — 단순 cp**: 사본 (`ai-workflow/core/mcp_installation_by_harness.md`) 을
    정본 (`workflow-source/core/mcp_installation_by_harness.md`) 으로 덮어쓴다. §2.68
    의 *mavis 글로벌 mcp.json attach* 는 별도 task (이전 축 §2.68 미완) 로 남긴다.
    cp 만으로 §6.5.2 의 §1.2.1 내용까지 자동 따라옴.
  - **red 2 — 1줄 추가**: 검사 파일 상단 (constants 직후) 에
    `sys.path.insert(0, str(SCRIPTS_DIR))` 1줄. 다른 검사 패턴과 정합.
  - **왜 합리적**: 검사가 의존하는 함수 `prompt_for_harnesses` / `enforce_harness_selection`
    / `HARNESS_SPECS` / `HARNESS_DEFINITIONS` 가 모두 `bootstrap_lib.__main__` /
    `bootstrap_lib.harnesses` 안에 있고, 둘 다 `scripts/` 의 자식. 검사 본문은 이미
    `SCRIPTS_DIR` 상수를 정의하고 subprocess에는 쓰고 있으니, in-process import 만
    누락된 비대칭.

- 비범위:
  - bootstrap_lib 자체 리팩터 (이미 잘 동작).
  - §2.68 mavis attach (별도 task).

## ✅ Outcome

- 작업 결과:
  - **red 1 (`check_standard_single_source`)** — `ai-workflow/core/mcp_installation_by_harness.md`
    사본을 정본(`workflow-source/core/...`)으로 cp 동기화. 정본 = 2026-08-07 00:12
    갱신본 (mavis 데스크탑 §1.2.1 + §6.5.2 추가). 사본 = 2026-08-06 09:19 정본.
    *정본이 우선* 원칙 (PROJECT_PROFILE.md §5, multi_workspace_orchestration.md §6).
  - **red 2 (`check_bootstrap_interactive_picker`)** — 검사 파일 상단 (constants 직후)
    `sys.path.insert(0, str(SCRIPTS_DIR))` 1줄 추가. `bootstrap_lib` 모듈이
    `workflow-source/scripts/bootstrap_lib/` 안에 있어 in-process tests 가
    `scripts/` 디렉터리를 path 에 올려야 동작. subprocess tests (`cwd=SCRIPTS_DIR`,
    `-m bootstrap_lib`) 는 이미 동작 중.

- 검증 결과:
  - **red 1 → 7/7 PASS** (`check_standard_single_source`).
  - **red 2 → 10/10 PASS** (`check_bootstrap_interactive_picker` "Interactive picker
    regression suite passed (10 checks).").
  - **전체 회귀 11/11 green** (`check_standard_single_source` /
    `check_bootstrap_interactive_picker` / dashboard 3종 / 도구 3종 /
    `check_recent_done_items_order` / `check_dashboard_multi_root_aggregator` /
    `check_workspace_registry`).
  - 사전 존재 red 0건 (4건 모두 닫힘).

- 의미 있는 정합 회복:
  - "mavis attach 가 안 붙는다" 는 *전역 mcp.json 등록 미완* + *사본이 정본보다
    오래됨* 두 가지가 겹친 결과였고, 사본 동기화로 *정본이 우선* 원칙이 다시
    적용됐다. attach 자체는 §2.68 별도 축.
  - bootstrap_lib import 오류는 *검사 자체의 결함* 이지 코드 결함이 아니었다 — 검사가
    in-process 와 subprocess 의 path 처리가 비대칭이었던 사소한 회귀.

- 후속 작업:
  1. §2.68 mavis 글로벌 mcp.json — `standardAiWorkflowReadOnly` 13종 attach. 이전 축.
  2. registry → §2.68 mcp attach 시 cross-host 자동 발견 (옵션).
