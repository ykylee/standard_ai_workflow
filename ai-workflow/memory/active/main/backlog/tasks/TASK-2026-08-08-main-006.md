---
id: TASK-2026-08-08-main-006
status: done
created_at: 2026-08-08
source_anchor: generic-task-2026-08-08-main-006
source_path: backlog/2026-08-08.md
kind: generic
---

# TASK-2026-08-08-main-006 — §2.68 mavis 글로벌 mcp.json 표준 register attach (이전 축 미완)

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-08
- Owner: AI Agent (ykylee 지시)
- Host: homelab (darwin)
- 영향 영역:
  - `~/.minimax/mcp/mcp.json` (호스트 외부 자원, **사용자 확인 후 진행**)
  - `~/.minimax/mcp/mcp.json.bak` (백업, 동일 방식)
  - `ai-workflow/memory/active/main/session_handoff.md` (§2.68 닫힘 표시)
  - `ai-workflow/memory/active/main/state.json` (done item)

- Description: 세션 시작 시 *mavis attach 가 안 붙는다* 라고 표현되던 회귀의
  진짜 원인 — `~/.minimax/mcp/mcp.json` 에 `standardAiWorkflowReadOnly` 가 등록돼
  있지 않아서. mavis 데스크탑 런타임은 *글로벌 한 곳* (DATA_DIR = `~/.minimax`)
  만 읽으므로, §6.5.2 의 merge 스니펫을 모든 안전 규칙(절대 경로, atomic write,
  builtin 5종 보존) 을 지키며 merge 한다. 새 세션에서 13종 attach 검증은
  mavis 데스크탑 자체를 재기동해야 하므로 *본 task 의 책임 밖* — 결과 보고만.

- Completion criteria: (1) 자동 backup 파일 생성. (2) 표준 §6.5.2 형식의
  `standardAiWorkflowReadOnly` entry 가 `mcpServers` key 아래에 추가.
  (3) builtin 5종 (`matrix` / `playwright` / `cu` / `trash` / `github`) 무손상.
  (4) command = `python3` (PATH 의 abs), `STANDARD_AI_WORKFLOW_ROOT` /
  `PYTHONPATH` 둘 다 *절대 경로*. (5) atomic write (tmp + os.replace). (6) JSON
  parse 정상. (7) 회귀 11종 + bootstrap 2종 그대로 green.

## 🛠️ Implementation / Content

- Progress: 완료.
- 설계 결정:
  - **사용자 확인 받음**: dry-run 또는 백업+적용 중 후자 선택. 백업 파일 형식
    `mcp.json.bak.<UTC>` (collision 방지).
  - **블록 빌드**: 정본 §6.5.2 형식 그대로. `command`=`python3`, `args` =
    `["-m", "workflow_kit.server.read_only_jsonrpc", "--stdio-lines"]`,
    `env.STANDARD_AI_WORKFLOW_ROOT` = 프로젝트 *절대* root, `env.PYTHONPATH` =
    `<root>/workflow-source` *절대*. `enabled=True`, `configured=True`.
  - **병합**: 기존 mcp.json 의 `mcpServers` key 아래에 추가. 동일 alias 가
    이미 있으면 빌드된 블록으로 *덮어쓰기* (사용자 확정 후 재실행 가능성).
  - **atomic write**: tmp 를 *같은 디렉터리* 에 만들고 `os.replace`. mcp.json
    자체 권한 / 소유권 보존.
  - **비범위**: 새 세션 rotate 후 13종 attach 검증 (mavis 데스크탑 재기동
    필요 → 본 task 의 책임 밖). `bootstrap_workflow_kit.py --harness mavis`
    가 §6.5.2 의 글로벌 mcp.json 까지 emit 하도록 (TASK-2026-08-07-main-001
    후속 2, 별도 task).

- 안전 규칙 (정본 §1.2.1 / §6.5.2 강제):
  - 절대 경로 두 env 모두 박힘.
  - builtin 5종 보존 (`matrix` / `playwright` / `cu` / `trash` / `github`).
  - trailing comma / JSON 문법 충돌 ❌.
  - atomic write.
  - `cwd` 가 데스크탑 런타임 자리인 점 인지 (mavis 가 attach 성공 / 13종 안 보임
    의 *조용한 실패*).

## ✅ Outcome

- Result:
  - **`~/.minimax/mcp/mcp.json`** — `mcpServers` key 아래 `standardAiWorkflowReadOnly`
    entry 1개 추가. 정본 §6.5.2 형식 그대로. **backup 보존**:
    `~/.minimax/mcp/mcp.json.bak.20260808T013427Z` (3,467 bytes, 원본과 동일).
  - **블록**: `command=python3`, `args=["-m", "workflow_kit.server.read_only_jsonrpc",
    "--stdio-lines"]`, `env.STANDARD_AI_WORKFLOW_ROOT=/Users/yklee/repos/standard_ai_workflow`,
    `env.PYTHONPATH=/Users/yklee/repos/standard_ai_workflow/workflow-source`,
    `enabled=true`, `configured=true`.
  - **builtin 5종 무손상**: `matrix` / `playwright` / `cu` / `trash` / `github` 보존.
  - **atomic write**: tmp + os.replace. 권한 보존.

- Verification:
  - **post-write verify**: JSON parse ✅ / 5종 보존 ✅ / 1종 추가 ✅ / 두 env
    모두 `Path(...).resolve()` 검증 ✅.
  - **회귀 안정성**: 11/11 green (red 2 + dashboard 3 + 도구 3 + ORDER +
    multi_root + registry).
  - **모듈 import sanity**: `from workflow_kit.server.read_only_jsonrpc import __name__`
    → cwd 부재(`/tmp`) 시점에서도 정상 (정합 — 서버가 *실제로* 부팅 가능한지
    의미 있는 1차 검증).

- 안전 / 책임 범위:
  - **세션 rotate 후 13종 native tool 노출 검증은 별도** (mavis 데스크탑 자체
    재기동 필요). 사용자가 새 세션에서 `standardAiWorkflowReadOnly` 13종이
    보이지 않으면 §1.2.1 의 cwd 부재 함정 — 절대 경로 두 env 가 박혀 있는지,
    builtin 5종 사이 JSON trailing comma 가 없는지, `manifest.json` 변동이
    없는지 확인.
  - **bootstrap 자동 emit**: `bootstrap_workflow_kit.py --harness mavis`
    가 §6.5.2 의 글로벌 mcp.json 까지 emit 하도록 (TASK-2026-08-07-main-001
    후속 2, 별도 task). 다음 사이클 후보.

- Follow-up:
  1. mavis 데스크탑 새 세션 rotate 후 13종 attach 검증 (수동).
  2. `bootstrap_workflow_kit.py --harness mavis` 가 §6.5.2 의 글로벌 mcp.json
     까지 emit 하도록 (TASK-2026-08-07-main-001 후속 2, 별도 task).
