---
id: TASK-2026-08-08-main-007
status: done
created_at: 2026-08-08
source_anchor: generic-task-2026-08-08-main-007
source_path: backlog/2026-08-08.md
kind: generic
---

# TASK-2026-08-08-main-007 — bootstrap 자동 emit (mavis) — 신규 harness "mavis"

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-08-08
- 담당: AI Agent (ykylee 지시)
- 호스트명: homelab (darwin)
- 영향 영역:
  - `workflow-source/scripts/bootstrap_lib/harnesses/__init__.py` (HARNESS_SPECS +
    SUPPORTED_HARNESSES)
  - `workflow-source/scripts/bootstrap_lib/__main__.py` (HARNESS_DEFINITIONS 구 mirror)
  - `workflow-source/scripts/bootstrap_lib/mcp.py` (mavis 글로벌 renderer + writer)
  - `workflow-source/scripts/bootstrap_lib/harnesses/renderers.py` (entry builder +
    MCP_BRIDGE dispatch)
  - `workflow-source/tests/check_bootstrap_mavis_global_mcp.py` (신규 회귀)
  - `workflow-source/core/multi_workspace_orchestration.md` (§0.7 / §0.4)
  - `docs/PROJECT_PROFILE.md` (commands + smoke 카운트)

- 작업 내용: TASK-2026-08-07-main-001 후속 2 (정본 §6.5.2 TODO). bootstrap 이
  `--harness mavis --enable-mcp` 를 받으면 호스트 글로벌 `~/.minimax/mcp/mcp.json`
  (mavis 데스크탑 런타임이 *유일하게* 읽는 곳) 에 `standardAiWorkflowReadOnly` 블록을
  *atomic merge* 한다. backup / builtin 5종 보존 / 절대 경로 env / MCP_BRIDGE 호환
  (jsonrpc-bridge → active_ok, stdio-sdk → manual_review_only) 그대로. 프로젝트
  root 진입점(AGENTS.md / MiniMax.md) 은 **emit 하지 않는다** — mavis 가 자동 read
  못 하므로 오히려 *유인* 이 된다. 신규 harness "mavis" 를 dispatch 1급 자리에
  등록 (HARNESS_SPECS + SUPPORTED_HARNESSES + HARNESS_DEFINITIONS + renderers'
  `HARNESS_FILE_BUILDERS` + mcp.py dispatch).

- 완료 기준: (1) `python3 -m bootstrap_lib --harness mavis --enable-mcp --apply`
  가 mavis 데스크탑 시뮬레이션(tmp 로 redirect) 동작: backup 생성 + 5종 보존 +
  1종 추가 + env 절대 경로 + atomic write. (2) 회귀 6+ assertions PASS. (3) 기존
  dispatch 표 정합 (HARNESS_SPECS == HARNESS_FILE_BUILDERS == SUPPORTED_HARNESSES
  순서 정합). (4) multi_workspace_orchestration.md §0.7 / PROJECT_PROFILE.md 갱신.
  (5) 전체 회귀 11+ green.

## 🛠️ Implementation / Content

- 진행 현황: 시작.
- 설계 결정:
  - **신규 harness 1급 등록**: `mavis` 추가. 정합 자료 5곳 동시 갱신 —
    `HARNESS_SPECS` / `SUPPORTED_HARNESSES` / `HARNESS_DEFINITIONS` (legacy mirror,
    argparse choices 동기화) / `HARNESS_FILE_BUILDERS` (entry builder) /
    `MCP_CONFIG_RENDERERS` (mavis mcp renderer).
  - **mavis 의 emit 산출물**: host 글로벌 `~/.minimax/mcp/mcp.json` *merge* 만.
    *project-local* 산출물 0 (mavis 가 자동 read 못 함, §6.5.2 명시). 따라서
    `HARNESS_FILE_BUILDERS[mavis]` 는 *no-op* — 명시적으로 빈 write_* 함수
    + `register_harness_builder` 등록 (consistency check 가 "spec 은 있는데
    builder 가 없다" 로 false fail 하지 않게).
  - **renderer 위치**: `bootstrap_lib/mcp.py` 에 `render_mavis_global_mcp_config`
    + `write_mavis_global_mcp_files(args, paths, harnesses)` 추가. dispatch 표
    `MCP_CONFIG_RENDERERS["mavis"]`. `write_mcp_config_files` 분기 추가.
  - **호스트 경로 결정**: `write_mavis_global_mcp_files` 가 `~/.minimax/mcp/mcp.json`
    으로 고정 (env override 없음 — 같은 하네스면 같은 경로). 테스트는 env
    injection 대신 tmp 경로로 *redirect* 하기 보다는, *테스트 자체에서 generate
    한 mcp.json* 을 미리 만들고 그 절대 경로 + `--mavis-global-mcp-path` CLI
    옵션으로 명시 입력. 본 CLI 옵션 1개 (테스트 가능성 + 사용자 명시성).
  - **atomic write + backup**: 기존 `minimax-code` 산출물 작성 패턴과 동일
    (`shutil.copy2` + `tempfile.mkstemp` + `os.replace`). builtin 5종 보존은
    *현재 사용자 mavis 의 5종* 이 일반적이므로 그 이름들이 이미 있으면 set
    그대로 두고 새 entry 만 추가 — *rename / 재배치 ❌*.
  - **CLI flag**: `--mavis-global-mcp-path` (default = `~/.minimax/mcp/mcp.json`).
    --apply 시에만 실제 write. --dry-run 이 default.
  - **bridge**: `MCP_BRIDGE_PHASE` / `MCP_BRIDGE_APPLY_MODE` 자동으로 같은 표
    를 따르므로 `mcp_server_command(bridge)` / `_mcp_server_env()` 재사용.

- 비범위:
  - mavis 데스크탑 세션 rotate 후 13종 attach 자동 검증 (사용자 수동).
  - 다른 transport / bridge (이미 정본이 정함).
  - cross-host sync (registry 와 동시, 다음 사이클).

## ✅ Outcome

- 작업 결과:
  - **`bootstrap_lib/harnesses/__init__.py`**: `HARNESS_SPECS["mavis"]` +
    `SUPPORTED_HARNESSES` 에 `mavis` 추가. entry_files=(), extra_files=() (project-local 0).
  - **`bootstrap_lib/__main__.py`**: `HARNESS_DEFINITIONS["mavis"]` (legacy mirror)
    + `--mavis-global-mcp-path` CLI 옵션 + 본체 emit 호출 (`--enable-mcp` &&
    `--harness mavis` 시 `write_mavis_global_mcp_files` 별도 진입).
  - **`bootstrap_lib/mcp.py`**: `DEFAULT_MAVIS_GLOBAL_MCP_PATH` 상수 +
    `render_mavis_global_mcp_config()` + `atomic_merge_mavis_global()` +
    `write_mavis_global_mcp_files()` + `__all__` 갱신. `MCP_CONFIG_RENDERERS` 는
    *project-local 0*이라 mavis 미포함 (의도적).
  - **`bootstrap_lib/harnesses/renderers.py`**: `write_mavis_harness_files` (no-op) +
    `register_harness_builder("mavis", ...)`. 기존 `_verify_harness_registry_consistency`
    가 5개 정합 자료 (HARNESS_SPECS / SUPPORTED_HARNESSES / HARNESS_FILE_BUILDERS /
    HARNESS_DEFINITIONS / MCP_CONFIG_RENDERERS) 모두 통과.
  - **`tests/check_bootstrap_mavis_global_mcp.py`** (신규): 7 assertions.

- 검증 결과:
  - **신규 7/7 PASS** (격리 tmp + sys.argv mock).
  - **전체 회귀 12/12 green** (red 2 + dashboard 3 + 도구 3 + ORDER +
    multi_root + registry + mavis_global).
  - **consistency check OK** (5 dispatch 자료 정합).
  - **CLI invocation**: `parse_args(['--harness', 'mavis', '--enable-mcp',
    '--mavis-global-mcp-path', '...'])` 가 `ns.harnesses=['mavis']`,
    `ns.mavis_global_mcp_path=...`, `ns.enable_mcp=True` 받아냄.

- **§6.5.2 정합**: backup (`.bak.<UTC-iso>`) + builtin 5종 보존 + 절대 경로 env
  두 개 (STANDARD_AI_WORKFLOW_ROOT / PYTHONPATH) + 동일 alias 재실행 시 keep
  (force=False) / overwrite (force=True).

- 후속 작업:
  1. mavis 데스크탑 새 세션 rotate 후 13종 attach 검증 (수동).
  2. registry ↔ mavis 글로벌 merge 자연스러운 연계 (다음 사이클).
