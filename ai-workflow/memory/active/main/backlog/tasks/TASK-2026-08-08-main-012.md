---
id: TASK-2026-08-08-main-012
status: done
created_at: 2026-08-08
source_anchor: generic-task-2026-08-08-main-012
source_path: backlog/2026-08-08.md
kind: generic
---

# TASK-2026-08-08-main-012 — §2.68 mavis attach 신규 세션 rotate 13종 native tool 노출 수동 검증 (사용자)

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-08
- Owner: 사용자 (ykylee 수동 검증)
- Host: homelab (darwin)
- 영향 영역:
  - `~/.minimax/mcp/mcp.json` (TASK-2026-08-08-main-006 의 결과물)
  - mavis 데스크탑 세션 lifecycle

- Description: TASK-2026-08-08-main-006~011 의 §2.68 cycle 이 *코드* 측면은 모두 닫혔지만,
  mavis 데스크탑의 *attach 동작* 자체는 *새 세션* 부터 반영된다 (mavis 공식: *"changing
  config affects new sessions; old sessions keep their existing runtime context until they
  rotate"*). 본 task 는 그 *rotate + 13종 native tool 노출* 을 사용자가 *수동* 으로 확인.

- Completion criteria:
  1. mavis 데스크탑에서 **새 세션** rotate.
  2. 그 세션 안에서 **13종** `standard_ai_workflow_read_only__*` native tool 이 노출되는지 확인.
     (tool name prefix 는 mavis alias `standardAiWorkflowReadOnly` 가 자동으로 부여.)
  3. 1~2개 tool (예: `latest_backlog`, `check_doc_metadata`) 을 *실제 호출* 해서 정상 응답 확인.
  4. 확인 결과를 본 task 의 *Outcome* 섹션에 한 줄 코멘트로 박기.
  5. 안 보이면 → 1차 출처 (§6.5.2) 의 *cwd 부재 함정* 재확인 (절대 경로 두 env / builtin 5종 보존 /
     JSON 문법 / trailing comma).

## 🛠️ Implementation / Content

- Progress: 코드 자동 검증 완료. 사용자 수동 검증만 남음.

- 자동 검증 (Mavis 가 *본 세션* 에서 실행):
  - `~/.minimax/mcp/mcp.json` 의 `standardAiWorkflowReadOnly` 항목이 §6.5.2 형식과 **100% 정합**:
    - keys: `command` / `args` / `env` / `enabled` / `configured` ✅
    - command: `python3` ✅
    - args: `['-m', 'workflow_kit.server.read_only_jsonrpc', '--stdio-lines']` ✅
    - env 둘 다 **절대 경로**: `STANDARD_AI_WORKFLOW_ROOT=/Users/yklee/repos/standard_ai_workflow`,
      `PYTHONPATH=/Users/yklee/repos/standard_ai_workflow/workflow-source` ✅
    - `enabled` / `configured`: True ✅
  - builtin 5종 (matrix / playwright / cu / trash / github) **보존** ✅
  - `mcp.json.bak.20260808T013427Z` (3.4KB, 2026-08-08 01:34) **보존** ✅
  - 회귀 smoke: `check_registry_mavis_sync` 6/6, `check_registry_endpoint_synth` 6/6,
    `check_registry_entry_env` 6/6 PASS.

- 비범위:
  - mavis 데스크탑 *세션 lifecycle* 자체의 hook — mavis 측 책임.
  - 13종 tool 의 실제 응답 payload 검증 — 다음 task (사용자 수동 호출 후).

## ✅ Outcome

- Result:
  - mavis 글로벌 mcp.json 등록 상태 = **§6.5.2 정합 100%** (위 자동 검증 결과).
  - 사용자 (ykylee) 결정: **갈래2 trust 채택** (2026-08-08 22:06 KST, 본 세션).
    새 세션 rotate 대신 e2e smoke (`check_mavis_attach_e2e.py` ALL PASS) 가 실제 attach
    경로와 100% 동치 subprocess 라는 근거로 갈음. mavis 의 *silent fail* 가능성 (§1.2.1) 은
    subprocess 직접 검증으로 회피됨.
  - 추가 회귀 자동화: TASK-2026-08-08-main-013 (위 e2e smoke 표준화) 동시 close.

- 안전 / 의미:
  - mavis 가 attach 실패해도 *silent fail* (사용자 보고 §1.2.1) 가능. §6.5.2 의 *cwd 부재 함정*
    회피용으로 env 두 개 모두 절대 경로. 안 붙으면 builtin 5종 / JSON 문법 / trailing comma 재확인.
  - 갈래2 채택 = *수동 검증* 슬롯을 *자동 검증* 으로 대체. 비용 ↓, 인적 오류 ↓, 회귀 검출 ↑.

- 후속 작업 (예상):
  1. **collateral fix** (본 task 의 사이드, 완료됨): TASK-2026-08-08-main-009/010/011 의 L14
     `상태: in_progress` 본문 → `done` 정정 (frontmatter `status: done` 과 정합).
     `check_self_application` 의 `test_own_linter_passes_on_own_repo` 회귀 회복.
  2. handoff L12 / state.json `current_baseline` 을 `a3a9442` / `27010a5` / `f97a9b1` 의
     *비정합 3종* → `e4470e5` (실제 HEAD) 로 통일 (본 turn 에 commit).
  3. 다음 축 후보: §0.8 #2 in-flight 워크스페이스 신뢰도 표시 (registry §5A.3 의 *첫 소비자*).
