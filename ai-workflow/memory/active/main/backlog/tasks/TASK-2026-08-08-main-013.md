---
id: TASK-2026-08-08-main-013
status: done
created_at: 2026-08-08
source_anchor: generic-task-2026-08-08-main-013
source_path: backlog/2026-08-08.md
kind: generic
---

# TASK-2026-08-08-main-013 — mavis attach end-to-end 회귀 smoke 자동화

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-08
- Owner: AI Agent (ykylee 지시)
- Host: homelab (darwin)
- 영향 영역:
  - `workflow-source/tests/check_mavis_attach_e2e.py` (신규 회귀 smoke)
  - mavis 글로벌 mcp.json 의 `standardAiWorkflowReadOnly` 항목

- Description: TASK-2026-08-08-main-006 (글로벌 mcp.json 등록) ~ 012 (사용자 수동 검증)
  사이클의 *자동 검증* 을 회귀 smoke 로 박는다. mavis 데스크탑 rotate 없이 mavis mcp.json
  env 그대로 subprocess 로 띄워서 initialize → tools/list → 2 tool call 까지 end-to-end
  verify. 향후 mcp.json 변경 / STANDARD_AI_WORKFLOW_ROOT 이동 / PYTHONPATH 깨짐 / builtin 5종
  보존 실패 / JSON 문법 오류 시 자동 catch.

- Completion criteria:
  1. `check_mavis_attach_e2e.py` 가 §6.5.2 형식 100% 일치 env 로 MCP 서버 기동.
  2. initialize 응답: serverInfo.name=workflow_read_only_bundle, protocolVersion=2025-03-26.
  3. tools/list: 정확히 13종, EXPECTED_TOOL_NAMES 와 set equality.
  4. tools/call latest_backlog: candidates + latest_backlog_path + status=ok 응답.
  5. tools/call check_doc_metadata: checked_files + missing_metadata + status=ok 응답.
  6. 모든 assertion 통과 시 exit 0, 한 건이라도 실패 시 exit 1 + 실패 목록.
  7. stdlib 만 사용 (subprocess + json + os), 외부 deps 0.
  8. 회귀 smoke 실행 시 mavis 데스크탑 사이드 이펙트 0 (별도 subprocess, 글로벌 mcp.json 변경 ❌).

## 🛠️ Implementation / Content

- Progress: 완료. 4 step ALL PASS.

- 설계 결정:
  - **stdlib only**: subprocess + json + os 만 사용. 외부 deps 0. §6.5.2 의
    "mavis attach 가 안 붙는다" 환경에서도 본 smoke 는 *독립* 으로 검증 가능.
  - **MCP_ENV = mavis mcp.json env 100% 일치**: REPO_ROOT + PYTHONPATH 두 env 의
    *절대 경로* 가 §6.5.2 정합이어야 attach 가능. 본 smoke 의 *실패* = mavis mcp.json
    의 *env 변경/누락/상대경로화* 자동 catch.
  - **4 step 순서**: initialize → tools/list → tools/call × 2. 각 step 의 응답
    schema 까지 검증 (단순 status 확인 ❌). tools/call 의 status=ok + 필수 key 존재
    까지 — *실제 응답 내용* 의 의미를 verify.
  - **subprocess teardown**: SIGTERM (5s timeout) → SIGKILL fallback. 글로벌 mcp.json
    변경 ❌, side effect 0.
  - **env 일치 검증**: REPO_ROOT constant 가 §6.5.2 의 mavis mcp.json env 와 100% 일치.
    smoke 가 PASS 면 §6.5.2 형식 정합 1차 검증. FAIL 면 §6.5.2 의 *cwd 부재 함정* 회귀.

- 비범위:
  - mavis 데스크탑의 *세션 lifecycle* hook 자체 — mavis 측 책임.
  - 13종 tool *전부* 호출 검증 — 2종 (latest_backlog / check_doc_metadata) 으로 충분
    (대표 read-only). 나머지 tool 회귀는 *기존* smoke (check_*.py) 가 cover.
  - JSON 문법 (trailing comma / mavis mcp.json syntax) — 본 smoke 의 subprocess 호출이
    *자동* 검증 (server 가 못 뜨면 initialize fail). 별도 lint 불필요.

## ✅ Outcome

- Result:
  - **`workflow-source/tests/check_mavis_attach_e2e.py`** (신규, 8.7KB, stdlib only):
    - 4-step 검증: initialize → tools/list (13종 set equality) → tools/call latest_backlog
      → tools/call check_doc_metadata.
    - 7 assertions: serverInfo.name, protocolVersion, tool count, tool names, content
      존재, 필수 key, status=ok.
    - 실패 시 exit 1 + failures[] 출력. PASS 시 exit 0.

- Verification:
  - **신규 e2e ALL PASS** (`check_mavis_attach_e2e.py`):
    - serverInfo = `{name: workflow_read_only_bundle, version: v1.0.0-beta}`
    - protocolVersion = `2025-03-26` (expected)
    - tools/list = **13종 100% 일치** (EXPECTED_TOOL_NAMES set equality)
    - latest_backlog 응답: candidates[] + latest_backlog_path + status=ok
    - check_doc_metadata 응답: checked_files + missing_metadata + status=ok

- 안전 / 의미:
  - **§2.68 cycle 의 *자동 검증* 닫힘**: 본 task 이전엔 *사용자 수동 rotate* 만 가능했던
    13종 attach 가 *코드* 차원에서 자동 verify 가능. 향후 mcp.json 변경 시 CI 에서
    즉시 catch.
  - **side effect 0**: subprocess 만 띄웠다 내림, 글로벌 mcp.json 변경 ❌, builtin
    5종 보존 ✅ (mcp.json 자체는 *읽기 전용*). 사용자가 mavis 데스크탑 rotate 를 안 해도
    *기동 가능성* 만 verify — 실제 사용자 세션 attach 는 사용자 수동.
  - **mavis 데스크탑 새 세션 rotate 가 여전히 필요**: mavis 공식 spec 에 따라
    *config 변경은 새 세션부터* 반영. 본 smoke 는 그 *바닥* (mcp.json + server 기동) 만
    verify. *top* (mavis 가 그 server 를 실제로 attach) 는 사용자 수동.

- 후속 작업 (예상):
  1. 사용자가 mavis 데스크탑 새 세션 rotate 후 13종 native tool 노출 확인 (수동).
     PASS 면 → §2.68 100% 닫힘. FAIL 면 → §6.5.2 cwd 부재 함정 / builtin 5종 / JSON
     문법 / trailing comma 재확인.
  2. 본 smoke 를 CI 에 자동 포함 (다음 release cycle). §2.68 의 *silent fail*
     회피용 runtime check.
  3. handoff L12 baseline 을 `4e85bab` (직전 commit) → 새 commit hash 로 갱신.
