---
id: TASK-2026-08-08-main-014
status: done
created_at: 2026-08-08
source_anchor: generic-task-2026-08-08-main-014
source_path: backlog/2026-08-08.md
kind: generic
---

# TASK-2026-08-08-main-014 — in-flight 워크스페이스 신뢰도 표시 (표준 §0.8 #2, §5A.3)

## 📝 Description

- 상태: planned
- 우선순위: high
- 요청일: 2026-08-08
- 담당: Mavis (구현) + 사용자 (채택 확인)
- 호스트명: homelab (darwin)
- 영향 영역:
  - `workflow-source/workflow_kit/common/workspace_registry.py` — `confidence()` 추가
  - `workflow-source/workflow_kit/common/dashboard_data.py` — `collect_recent_releases` + Panel 5 render
  - `workflow-source/tests/check_registry_confidence.py` — 신규 smoke
  - `workflow-source/core/multi_workspace_orchestration.md` — §0.7 적용 상태 + §0.8 close

- 작업 내용: §0.8 의 "in-flight 워크스페이스를 취합 뷰에 **어떤 신뢰도로** 표시할 것인가"를
  4-level enum (`fresh` / `recent` / `stale` / `orphan`) 으로 닫는다. registry 가 §5A.3 의
  *첫 소비자* 자리 (§7.1) 인데 비어 있어서 registry 자체의 가치를 평가할 길이 없었다.
  본 task 가 그 *최초 사용 사례* 가 되어 registry 의 의미를 정착시킨다.

- 완료 기준:
  1. `workflow_kit.common.workspace_registry.confidence(entry, *, worktree_branch=None, now=None)` 4-level enum 함수 구현.
  2. `collect_recent_releases()` 가 `confidence` 필드를 timeline item 마다 emit.
     - main state.json → `fresh` (강제 — main 은 merge 후라 신뢰도 최상)
     - registry in-flight → `confidence(entry)` 계산
  3. Panel 5 markdown / html render 가 inline badge `[fresh]` / `[recent]` / `[stale]` / `[orphan]` 표시.
  4. smoke `check_registry_confidence.py` (stdlib only) ALL PASS — 6+ case (4 enum + 2 edge).
  5. `multi_workspace_orchestration.md` §0.7 적용 상태 표 갱신 + §0.8 의 #2 close 표시.
  6. 회고: §0.8 의 #2 close 후 §0.8 잔존 항목 3건 (#1 / #3 / #4) 의 후속 우선순위 1줄.

## 🛠️ Implementation / Content

- 진행 현황: spec 확정, 구현 진행.

- 결정 사항 (사용자 채택, 2026-08-08 22:23 KST):
  - **4-level enum** (정량 점수 ❌, 라벨 ✅ — dashboard 가 분기 결정에 쓰기 쉬움).
    - `fresh`: path.is_dir() ✓ AND last_seen_at < 24h AND worktree_branch == entry.branch
    - `recent`: 위 3개 중 1개 fail (24h~7d)
    - `stale`: last_seen_at > 7d OR branch mismatch
    - `orphan`: path.is_dir() ✗ (worktree 삭제됨) — 자동 unregister 후보 (표시만, §5D.4)
  - **Panel 5 표시** = timeline 줄 끝에 inline badge 1개. 별도 column ❌ (timeline 이 text list 라 layout 깨짐).
  - **계산 위치** = `workspace_registry.py` (이미 host-scoped 정적 메타 보유). dashboard 는 `entry.confidence` 만 읽음.
  - **3-way freshness signal** = `path.is_dir()` (시스템) + `last_seen_at` (registry) + `worktree_branch` (git). 한 신호만으로 결정 ❌.

- 비범위 (이 task 가 닫지 *않는* 것):
  - §0.8 #1 registry 저장 위치 (여러 호스트)
  - §0.8 #3 seed 한 일 vs 실제 한 일 범위 이탈 검출
  - §0.8 #4 `--force` 서버측 branch protection 이중화
  - registry 의 자동 unregister (orphan 표시만, §5D.4 의 *되돌릴 수 없는 작업은 확인 후*)

- 1차 출처:
  - `multi_workspace_orchestration.md` §0.8 (4개 후보)
  - `multi_workspace_orchestration.md` §5A.3 (실측 — 중앙이 in-flight 못 본다)
  - `multi_workspace_orchestration.md` §7.1 (registry)
  - 기존 `is_stale(entry, *, threshold_seconds=DEFAULT_STALE_SECONDS)` (7일 임계) — 본 task 가 분기细化

## ✅ Outcome

- 작업 결과: (구현 후 박기)
  - confidence() 4-level + Panel 5 inline badge + smoke ALL PASS.
  - §0.8 #2 close. §0.8 잔존 3건 (후속 우선순위 1줄).

- 안전 / 의미:
  - 자동 unregister ❌ (orphan 표시만, §5D.4 정합). 사람이 unregister 결정.
  - main worktree 는 *항상* fresh 강제 — main 의 items 는 merge 후라 *사실* 이다.
  - registry in-flight 의 confidence = *추정* 이지 사실이 아님 (dashboard 가 "신뢰도" 라고 부르는 이유).

- 후속 작업 (예상):
  - §0.8 #1 (registry 분산 저장) — multi-host 운영 들어갈 때 자연스러운 후속.
  - §0.8 #3 (범위 이탈 검출) — §0.5 충돌 방지 규약 보강 단계.
  - §0.8 #4 (`--force` 이중화) — 서버측 branch protection 운영 도입 시.
