---
id: TASK-2026-08-08-main-017
status: done
created_at: 2026-08-08
source_anchor: generic-task-2026-08-08-main-017
source_path: backlog/2026-08-08.md
kind: generic
---

# TASK-2026-08-08-main-017 — operational MCP tool 4종 CLI wrapper (dual mode)

## 📝 Description

- 상태: planned
- 우선순위: medium
- 요청일: 2026-08-08
- 담당: Mavis (구현)
- 호스트명: homelab (darwin)
- 영향 영역:
  - `workflow-source/tools/rotate_workflow_logs.py` (신규)
  - `workflow-source/tools/apply_robust_patch.py` (신규)
  - `workflow-source/tools/create_environment_record_stub.py` (신규)
  - `workflow-source/tools/check_quickstart_stale_links.py` (신규)
  - `workflow-source/tests/check_cli_wrappers.py` (신규 smoke)
  - `workflow_kit.server.read_only_tools` — 변경 ❌ (MCP server 그대로 유지)

- 작업 내용: `standardAiWorkflowReadOnly` MCP server 의 13 tool 중 *operational* 4종에
  CLI wrapper 추가. **dual mode** — MCP server (LLM inline 호출) + CLI (cron/script/
  사용자 직접 실행) 둘 다 같은 underlying `*_payload` 함수를 부르는 형태.

- 완료 기준:
  1. 4개 CLI 모두 같은 underlying `*_payload` 함수를 import 해서 호출 (중복 구현 ❌).
  2. 각 CLI: `--dry-run` default / `--apply` 실제 변경 / `--json` machine-readable.
  3. `tools/rotate_workflow_logs.py` — `--handoff-path` (default = `ai-workflow/memory/active/main/session_handoff.md` 자동), `--max-done-items` (default 10).
  4. `tools/apply_robust_patch.py` — `--file-path` + `--patch-file` (또는 stdin) + `--apply` (default dry-run).
  5. `tools/create_environment_record_stub.py` — `--hostname` / `--os-type` 자동 detect (없을 때 `socket.gethostname()` / `platform.system()`), `--output-path`.
  6. `tools/check_quickstart_stale_links.py` — multi `--quickstart-path` + `--project-profile-path` / `--session-handoff-path` / `--work-backlog-index-path` / `--agents-path` (모두 default 자동).
  7. smoke `check_cli_wrappers.py` (stdlib only) ALL PASS — 각 CLI 1 case (CLI 가 underlying_*_payload 와 같은 output 내는지).
  8. doc `multi_workspace_orchestration.md` §0.7 상태표에 *dual mode* ✅ 추가.

## 🛠️ Implementation / Content

- 진행 현황: spec 확정, 구현 진행.

- 결정 사항 (사용자 채택, 2026-08-08 22:49 KST):
  - **Dual mode** — MCP server *변경 없음*. CLI wrapper 만 추가. 4 tool 모두 같은
    underlying 함수 (`*_payload`) 를 부르므로 정합성 자동.
  - **CLI target 4**: `rotate_workflow_logs` / `apply_robust_patch` / `create_environment_record_stub` / `check_quickstart_stale_links`. 9개는 MCP 유지
    (LLM-interpretation 필수).
  - **dry-run default** — 모든 CLI 가 *읽기 우선*, `--apply` 시에만 변경. §5D.4 의
    *되돌릴 수 없는 작업은 확인 후* 원칙 정합.
  - **--json default output** — script/cron 연동에 적합. human-readable 모드는
    *명시적* opt-in (예: `--human`).

- 비범위 (이 task 가 닫지 *않는* 것):
  - 다른 9 tool 의 CLI wrapper (LLM 필수, MCP 유지)
  - MCP server 변경 / 신규 tool 추가
  - 4 CLI 의 운영 자동화 hook 등록 (cron / pre-commit)

- 1차 출처:
  - `workflow_kit.server.read_only_tools` (MCP dispatcher)
  - `workflow_kit.common.read_only_bundle` / `workflow_kit.common.writing_bundle` (underlying)
  - `workflow-source/tools/workspace_registry.py` (CLI 패턴 — argparse / --apply / --json)

## ✅ Outcome

- 작업 결과: (구현 후 박기)
  - 4 CLI + smoke ALL PASS.

- 안전 / 의미:
  - dry-run default + `--apply` 명시 = 운영 실수 방지.
  - CLI 와 MCP 가 같은 underlying 함수 부르므로 *출력 정합* 자동. smoke 가 verify.

- 후속 작업 (예상):
  - TASK-018: release 묶음 (v0.15.20+~v0.15.24+, TASK-001~016) + 이번 dual mode 추가.
  - TASK-019: §0.8 #3 (범위 이탈 검출)
  - TASK-020: §0.8 #4 (`--force` 서버측 branch protection 이중화)
