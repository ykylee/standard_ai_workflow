---
id: TASK-2026-08-08-main-020
status: done
created_at: 2026-08-08
source_anchor: generic-task-2026-08-08-main-020
source_path: backlog/2026-08-08.md
kind: generic
---

# TASK-2026-08-08-main-020 — `[project.scripts]` entry points (CLI 化 A안)

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-08
- Owner: Mavis (구현)
- Host: homelab (darwin)
- 영향 영역:
  - `workflow-source/pyproject.toml` — `tools` packages 추가 + `[project.scripts]` 30+ entry points
  - `workflow-source/tests/check_entry_points.py` (신규) — smoke
  - `workflow-source/releases/Beta-v1.1.0.md` (addendum) — entry points 섹션 추가
  - 신규 release v1.1.1-beta (patch, entry points 만 추가)

- Description: 표준 §0.7 의 *CLI 도구화* A안. 32개 `workflow-source/tools/*.py` 의
  `main()` 함수를 `[project.scripts]` console_script 로 박기. `pip install -e .` 후
  32개 binary 가 자동 생성됨 (`workflow-registry` / `workflow-drift-detect` / ...).
  기존 script 그대로, *진입점만 추가* — 0 리스크. my_harness 가 *즉시* 표준 호출 가능.

- Completion criteria:
  1. `pyproject.toml` `tools` package 추가 (현재 `packages = [bootstrap_lib, workflow_kit]` 에 `tools` 추가).
  2. `[project.scripts]` 30+ entry point 추가 — 각 `command-name = tools.X:main`.
  3. naming convention: `workflow-{name}` (e.g., `workflow-registry`, `workflow-drift-detect`).
  4. `tools` 디렉터리의 `__init__.py` 가 *loud* import 가 가능해야 함 (현재 "script 라 import 안 됨" 주석 → 도구화 시 정정).
  5. smoke `check_entry_points.py` (stdlib only) ALL PASS — entry point 30+ parse + 각 module importable + 각 `main()` callable.
  6. `Beta-v1.1.0.md` addendum (또는 v1.1.1-beta release note) — entry points 섹션 추가.
  7. `pip install -e .` 검증 (선택) — 또는 dry-run.

## 🛠️ Implementation / Content

- Progress: spec 확정, 구현 진행.

- 결정 사항 (사용자 채택, 2026-08-08 23:13 KST):
  - **A안만** — 점진적 도입 1단계. B (dispatcher) 는 후속 release (1 follow-up × N release 분할 정공법).
  - **naming**: `workflow-{name}` (hyphen, lowercase). 32개 → 한눈에 `workflow-*` 인식.
  - **`tools` packages 추가** — 기존 *non-importable* 주석 정정. `__init__.py` 가 *loud* (있어야 import 가능).
  - **sub-agents**: `release_pipeline` 의 12+ sub-command 는 entry point 1개 (sub-commands 는 argparse 가 처리).
  - **dry-run**: `pip install -e .` 가 *optional* — 테스트는 import + main() callable 로 충분.

- 비범위 (이 task 가 닫지 *않는* 것):
  - dispatcher (`wk` 단일 binary) — 후속 release
  - `python -m workflow_kit` 패턴 — A안의 [project.scripts] 로 *충분*
  - tab completion (argcomplete / shtab) — 후속
  - 외부 패키지 publish (PyPI / TestPyPI) — 정책상 ❌ (GitHub Releases 만)

- 1차 출처:
  - `multi_workspace_orchestration.md` §0.7 CLI 도구화 안 (A/B/C/D 옵션)
  - `pyproject.toml` 기존 `[tool.setuptools]` packages
  - 표준 §0.7 retrospective 4 phase 의 *Phase 4: CLI 통합 = dispatcher registry* 의 *선행* 정공법

## ✅ Outcome

- Result: (구현 후 박기)
  - 30+ entry points + smoke ALL PASS + v1.1.1-beta release.

- 안전 / 의미:
  - 기존 script 변경 0 — 0 리스크. 단지 *진입점 추가*.
  - my_harness 가 `subprocess(["workflow-registry", "list"])` 로 표준 호출 가능.
  - 사용자가 *어디서든* `workflow-drift-detect --help` 가능 (PATH 에 install 된 후).

- 후속 작업 (예상):
  - TASK-021: dispatcher (`wk` 단일 binary) — `tools/cli/dispatcher.py` + 각 tool 의 `__main__` 을 dispatcher 호출로 wrapping
  - TASK-022: tab completion (argcomplete)
  - TASK-023: shell completion (zsh shtab)
