---
id: TASK-2026-08-09-main-001
status: done
created_at: 2026-08-09
source_anchor: generic-task-2026-08-09-main-001
source_path: backlog/2026-08-09.md
kind: generic
---

# TASK-2026-08-09-main-001 — memory 문서 정합성 정리 (v1.1.1-beta 세션 종료 잔여)

## 📝 Description

- 상태: done
- 우선순위: medium
- 요청일: 2026-08-09
- 담당: Claude Code
- 호스트명: homelab (darwin)
- 영향 영역:
  - `ai-workflow/memory/active/main/backlog/2026-08-08.md` — status / 링크
  - `ai-workflow/memory/active/main/backlog/tasks/TASK-2026-08-08-main-014~020.md` — 본문 상태 라인
  - `ai-workflow/memory/active/main/state.json` — done_items / recent_done_items
  - `ai-workflow/memory/active/main/session_handoff.md` — stale 서술 4곳

- 작업 내용: 세션 시작 시 baseline 복원 중 발견한 memory 문서 정합성 어긋남을 정리.
  v1.1.1-beta 세션(2026-08-08)이 TASK-014~020 을 실제로는 끝내고 release 까지 냈는데,
  memory 문서 여러 곳이 그 이전 상태로 남아 있었다.

- 완료 기준: `check_self_application` 8/8 PASS (기존 7/8, `handoff_bloat` +
  `file_not_found` ×2 red).

## 🛠️ Implementation / Content

1. **backlog 인덱스** (`2026-08-08.md`)
   - TASK-020 `status: planned` → `done`, notes 를 실제 결과로 갱신
     (29 entry points + venv e2e + v1.1.1-beta 발행).
   - TASK-018/019/020 의 `path:` href 3건이 `TASK-2026-08-018.md` 처럼
     `-08-main` 이 빠져 있었다 → 실제 파일명으로 정정. linter 의 `file_not_found`
     2건이 바로 이것이었다.

2. **task 파일 본문** (TASK-014~020, 7건)
   - frontmatter 는 `status: done` 인데 본문은 `- 상태: planned` 로 남아 있었다.
     기존 done task (006/010/013) 관행에 맞춰 `done` 으로 정정.

3. **state.json**
   - `backlog.done_items` 가 13건뿐인데 `task_count: 20` → TASK-014~020 7건 추가.
   - `session.recent_done_items` 에 TASK-018 만 빠져 있었다 → 추가.
   - **전면 재생성은 하지 않았다.** `generate_workflow_state.py` 재생성본과 diff 해보니
     `done_items` 를 최근 10건으로 자르고 `current_focus` 를 *이미 끝난* TASK-014 로
     잡는다. 현재본이 수동 보강된 정본이라 어긋난 필드만 손으로 맞췄다.

4. **session_handoff.md** (stale 서술 4곳)
   - §4 최근 완료가 TASK-014 까지만 → 최신 10건(020~011)으로 교체. 11건이라
     `RECENT_DONE_ITEMS_CAP`(10) 초과 → `handoff_bloat` 였던 것도 여기서 해소.
   - §5 "§0.8 잔존 3건" → 015/016/018/019 로 전부 close 됨을 반영, 다음 후보 축
     4건(B안 dispatcher / HTTP server / gh api check / title drift v2)으로 교체.
   - §5 `origin/main` = `838b12f` "현재 HEAD" → 세션 전반부 시점임을 명시.
   - §6 "`--force` 서버측 이중화 미적용" → 2-layer 적용 + 남은 한계(hook 미설치
     호스트, 3rd layer 자동 check 부재)로 정정.

## ✅ Outcome

- `check_self_application` **8/8 PASS** (이전 7/8).
- `check_appendonly_memory_layout` PASS.
- `check_handoff_done_cap` **7/7 PASS**.
- 배운 것: `handoff_bloat` 는 *줄 수* 가 아니라 §4 항목 **개수**(cap 10)를 본다.
  §4 를 최신화하면서 오래된 항목을 안 지우면 바로 red 가 된다.
- 후속: state.json 의 수동 보강분과 `generate_workflow_state.py` 재생성본이 갈라져
  있다. 재생성이 정보를 깎는 구조라 지금은 손으로 유지 중 — 생성기 쪽을 맞출지는
  별도 판단 필요.
