---
id: TASK-2026-08-09-main-007
status: done
created_at: 2026-08-09
source_anchor: fix-task-2026-08-09-main-007
source_path: backlog/2026-08-09.md
kind: fix
---

# TASK-2026-08-09-main-007 — 남은 red 4건 close (FULL mypy strict 복구 포함)

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-09
- Owner: Claude Code
- Host: homelab (darwin)
- 영향 영역:
  - `workflow_kit/common/workspace_registry.py` (mypy 24건)
  - `workflow_kit/common/paths.py` (`branch_slug_for` 신규)
  - `tools/survey_remote_workspaces.py` (R3 위반 수정)
  - `schemas/read_only_*.json` 3건 (재생성)
  - `tests/` 8 파일 (tempdir 누수 6 + guard 1 + wiki_url 1)

- Description: TASK-006 에서 "범위 밖"으로 남긴 red 4건을 전부 닫는다.

- Completion criteria: 4건 모두 PASS + 회귀 0.

## 🛠️ Implementation / Content

**왜 남겼었나** — TASK-006 에서 red 를 `[FAIL]` 패턴으로 잘못 세어 "7건"이라 보고했고,
사용자 요청("red 검토")을 그 잘못된 목록으로 좁게 읽었다. 남긴 근거가 내가 만든
숫자였다. 지시받고 다시 열어 전부 처리했다.

**1. `check_source_without_runtime_layer`** — 근본 원인은 `read_only_*.json`
fixture 3건의 `tool_version` 이 `v1.0.0-beta` 로 남은 것. **TASK-006 이 처리한
stamp 6건과 완전히 같은 뿌리였는데 fixture 만 놓쳤다.** 재생성 스크립트로 갱신 →
관련 4개 검사 PASS.

**2. `check_tempdir_leak_guard`** — 두 결함:

- case_7: 정리 없는 `mkdtemp`. 처음 8건을 고치니 3건이 더 드러나 총 11건. 6개
  테스트 파일에 `atexit` 기반 `_tmpdir()` 헬퍼를 넣었다. `TemporaryDirectory`
  컨텍스트가 정석이지만 테스트 함수가 한 덩어리라 전부 재들여쓰기해야 해서,
  정리 보장은 같게 두고 변경면을 줄였다 (assert 가 중간에 터져도 정리된다).
- case_3: 검사가 `"/var/tmp"` 를 **문자열로** 찾는데 macOS 는 `resolve()` 결과가
  `/private/var/tmp` 다. **구현은 정상이고 검사가 플랫폼을 못 넘긴 것** — resolve
  비교로 정정.

**3. `check_wiki_url_validity`** — `multiprocessing` worker 가 test 함수 안의 local
function 이라 spawn(macOS/Windows 기본)에서 `PicklingError`. Linux 는 fork 라 통과 —
**즉 이 두 케이스는 Linux 에서만 도는 검사였다.** worker 2개를 모듈 레벨로 빼고
인자는 값으로 넘겼다. 39/39 PASS.

**4. `check_workflow_kit_cli`** — 뿌리가 깊었다. `release-doctor` 의 mypy gate 가
red 였고 원인은 `workspace_registry.py` 의 **mypy 오류 24건** — 저장소가 표방하는
**FULL mypy strict 가 깨져 있었다**. 유형은 `type-arg` 17 / `attr-defined` 6 /
`no-any-return` 1. 전부 정리해 `workflow_kit/` **128 files clean** 복구.

**부수 — R3 위반 1건** (`check_root_anchor_audit`): `survey_remote_workspaces.survey()`
가 `repo_root` 로 파라미터화됐는데 브랜치는 `get_current_branch()` 로 얻었다. 그
함수는 *이 모듈이 속한* 저장소에 앵커를 박으므로, `--repo-root` 로 다른 저장소를
지목하면 `is_current` / `current_branch` 가 엉뚱한 브랜치를 가리켰다. `paths.py` 에
`branch_slug_for(repo_root)` 를 신설 (호출자가 저장소를 고른다, env override 도 안
본다) 하고 그것을 쓰게 했다. 11/11 PASS.

## ✅ Outcome

- 4건 + R3 1건 전부 close. 회귀 0 (registry 관련 9종 / survey 8 assertions PASS).
- **`workflow_kit/` mypy strict 128 files clean** — Phase 13 **P0-1** (mypy strict
  venv 직접 verify) 의 acceptance 를 실측으로 충족했다. 릴리스 노트에 기록하면 close.
- dashboard `drift_prevention.guard_status`: `fail` → **`pass`** (TASK-006 의 stamp
  정리 효과). handoff §6 의 "별건 1" 이 함께 닫혔다.
- 전체 검사 잔여 red: **환경 3건**(시스템 python3 에 mypy 미설치 — venv 에서는 PASS)
  + **`check_smoke_trend_cross` 1건**(릴리스 사이클 대기).
- 배운 것 2가지:
  - **검사가 플랫폼/환경을 못 넘기면 "통과"가 거짓이 된다.** `/var/tmp` 문자열 비교는
    macOS 에서 늘 red 였고, `PicklingError` 케이스는 Linux 에서만 돌고 있었다.
    둘 다 "검사가 있다"는 사실이 안심을 주는 동안 실제로는 반쪽만 검증됐다.
  - **`tools/` 는 CI mypy 대상이 아니다.** `workflow_kit/` 만 본다. survey 의 잔여
    7건은 그래서 red 로 세지 않는다 — 다만 그 사실 자체가 기록될 가치가 있다.
