---
id: TASK-2026-08-09-main-010
status: done
created_at: 2026-08-09
source_anchor: generic-task-2026-08-09-main-010
source_path: backlog/2026-08-09.md
kind: generic
---

# TASK-2026-08-09-main-010 — Phase 13 P0-2 (telemetry source 다양성) close

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-08-09
- 담당: Claude Code
- 호스트명: homelab (darwin)
- 영향 영역:
  - `workflow-source/core/phase_13_followup.md` §2.2 (AC2) / §3.1 (P0-2)

- 작업 내용: AC2 `source 다양성 ≥ 4` 를 실측으로 수렴시키고, 실제와 다른 문서를 정정한다.

- 완료 기준: by_source 4 source + hit_rate ≥ 0.9.

## 🛠️ Implementation / Content

**문서가 두 군데 틀려 있었다.** 코드를 고치기 전에 그것부터 드러났다.

| 문서의 기술 | 실제 |
| --- | --- |
| "현 1 source: **dispatcher**" | 1 source 는 **`session-start`** (132 calls). `dispatcher` 는 `--command=memory-index-query` 를 부를 때만 쌓이는데 **아무도 부른 적이 없었다.** |
| action item: "3 skill 의 retrieval 호출 **활성화** 필요" | **이미 v0.15.21+ 에서 세 skill 모두 자동 활성.** `_build_memory_index_query_output` 이 셋 다 동일하다 — flag 부재 + workspace `memory_index/` 존재 → 자동. |

즉 남아 있던 것은 wiring 이 아니라 **그 skill 들이 실행된 적이 없다는 사실** 이었다.

확인 방법은 코드 읽기가 아니라 **한 번 돌려보는 것** 이었다:

```
doc-sync 실행       → by_source 에 doc-sync: calls=1 hits=1  (즉시 기록됨)
backlog-update 실행 → backlog-update: calls=1
memory-index-query  → dispatcher: calls=1
```

세 번 다 실제 용도로 돌렸다 (doc-sync 는 오늘 변경된 `release_pipeline.py` 의 영향
문서 조회, backlog-update 는 본 task 등록, memory-index-query 는
`telemetry,memory-index,phase-13` 질의).

## ✅ Outcome

- **AC2 수렴**: by_source **4 source** (`session-start` 132 / `doc-sync` 1 /
  `backlog-update` 1 / `dispatcher` 1) + hit_rate **1.0** / 135 events.
- `phase_13_followup.md` §2.2 에 정정 블록, §3.1 P0-2 를 **닫힘** 으로 갱신.
- smoke: `check_telemetry_cross_v0_15_6` 5/5, `check_memory_index_telemetry` PASS.

### acceptance 자체의 약점 (기록)

"by_source 에 4 source 등장" 은 **각 경로를 한 번씩만 돌려도 충족된다.** 지속적
사용을 재는 지표가 아니다. 오늘 한 일이 정확히 그 한 번씩이었고, 그래서 숫자는
채워졌지만 *실제로 이 skill 들이 워크플로우에서 쓰이는가* 라는 원래 질문은 그대로
남는다. 윈도 기반(최근 N일 안의 source 다양성) 지표로 바꿀지는 별도 판단 — 문서에
후속 후보로 적었다.

### 배운 것

**"활성화가 필요하다" 고 적힌 항목이 이미 활성화돼 있었다.** 문서가 코드보다
느렸고, 그 사이 두 릴리스가 지나갔다. 오늘 반복해서 나온 패턴과 같은 모양이다 —
*기록이 실제와 갈라져 있는데 아무도 대조하지 않는 상태*. 여기서는 검사가 아니라
**한 번 실행해 보는 것** 이 대조 수단이었다.
