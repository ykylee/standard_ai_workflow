---
id: TASK-2026-08-09-main-012
status: done
created_at: 2026-08-09
source_anchor: generic-task-2026-08-09-main-012
source_path: backlog/2026-08-09.md
kind: generic
---

# TASK-2026-08-09-main-012 — Phase 13 P1 묶음 close (3건 모두 문서가 실제와 달랐다)

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-08-09
- Owner: Claude Code
- Host: homelab (darwin)
- 영향 영역:
  - `workflow-source/CHANGELOG.md` (재생성)
  - `tools/release_pipeline.py` (`NON_RELEASE_VERSIONS`)
  - `tests/check_release_pipeline_changelog_gen.py` (7번째 case)
  - `workflow-source/core/phase_13_followup.md` (P1-1/2/3 정정)

- Description: Phase 13 P1 세 항목을 닫는다.

- Completion criteria: 각 항목의 acceptance 충족 또는 이미 충족됐음을 실측으로 확인.

## 🛠️ Implementation / Content

**세 항목이 전부 실제와 달랐다.** P0-2 에 이어 같은 모양이다.

| 항목 | 문서의 기술 | 실제 |
| --- | --- | --- |
| P1-1 | "`release --apply` 의 changelog-gen pre-step **부재**" | **v0.15.21+ 에 이미 있다** (`# 3.5 changelog-gen auto-step`) |
| P1-2 | "automated-repro-scaffold **stable 승격** 필요 (9/11 stable)" | **이미 stable** (v0.11.24). 실제 분포는 13 stable / 1 beta |
| P1-3 | "git-conflict-resolver **alpha → beta**" | **이미 stable** — beta 를 건너뛰고 승격됐다 (v0.11.24). 문서가 두 단계 뒤처졌다 |

**P1-1 — 구현은 있었지만 두 가지가 남아 있었다**

1. **최근 3 release 가 그 경로를 타지 않았다.** v1.1.0 / v1.1.1 / v1.1.2 를
   `cmd_release` 가 아니라 수동 절차(bump → tag → `gh release create`)로 발행해
   CHANGELOG 가 v0.15.21 에서 멈춰 있었다. **오늘 내가 v1.1.2 를 그렇게 냈다** —
   auto-step 이 있는 줄 몰랐다. 재생성으로 3건 반영.

2. **오탐 2건.** `RELEASE_RE` 가 subject 의 `(v3.0)` / `(v3.0.1)` 을 release 로
   인식했다. 둘 다 *워크플로우 문서 체계* 의 Phase 5 버전이고 package release 가
   아니다 (해당 commit 은 `pyproject.toml` 미변경). semver 정렬 탓에
   **`[3.0.1]` 이 최신 release 자리에** 앉아 CHANGELOG 를 읽는 사람에게
   "최신이 3.0.1" 이라고 거짓말하고 있었다.

   **git tag 대조는 쓸 수 없었다** — 0.15.x 대 다수가 tag 없이 릴리스돼 진짜
   release 를 대량으로 지운다 (실측: CHANGELOG 152 vs tag 121). 그래서
   `NON_RELEASE_VERSIONS` **선언된 예외** 로 뒀다 (`ROOT_ANCHOR_LEDGER` 와 같은 정공법,
   각 항목에 이유 필수).

## ✅ Outcome

- CHANGELOG 재생성: `[1.1.2]` / `[1.1.1]` / `[1.1.0]` 이 최신 순으로 정렬, 오탐 0.
  **P1-1 acceptance 충족** (3 release 연속 auto-gen + commit ↔ release note 정합).
- `check_release_pipeline_changelog_gen.py` **7/7** (신규
  `test_non_release_versions_are_not_sections` — 예외 명세에 이유가 있는지 +
  가짜 commit 이 `unreleased` 로 흡수되는지 + 실제 CHANGELOG 에 예외 section 부재).
- P1-2 / P1-3 은 **이미 닫혀 있었다** — `maturity_matrix.json` 실측으로 확인하고
  문서를 정정.
- release 회귀 5종 + maturity 6종 PASS.

**남은 승격 후보 (신규 발견)**

skill 14개 중 **13 stable / 1 beta**. 유일한 beta 는 **`memory-index-query`**
(v0.11.22, Phase 3). P1-2/P1-3 이 가리키던 대상이 아니라 이쪽이 남은 후보다.

**반복되는 패턴**

오늘 P0-2 에 이어 P1 세 항목까지 **문서가 코드/matrix 와 대조된 적이 없다는 사실**
이 드러났다. `phase_13_followup.md` 는 로드맵 문서인데 실행 상태를 따라오지 못했고,
그 사이 여러 릴리스가 지나갔다. 특히 P1-1 은 **내가 오늘 그 기능을 안 쓰고 수동으로
릴리스한** 탓에 갭이 하나 더 벌어졌다 — 문서가 "없다" 고 적어 둔 기능을 찾아보지
않았기 때문이다.
