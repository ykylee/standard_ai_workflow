---
id: TASK-2026-08-10-main-002
status: done
created_at: 2026-08-10
source_anchor: fix-task-2026-08-10-main-002
source_path: backlog/2026-08-10.md
kind: fix
---

# TASK-2026-08-10-main-002 — check_mavis_attach_e2e 호스트 사본 제거

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-08-10
- Owner: Claude Code
- 영향 영역: `workflow-source/tests/check_mavis_attach_e2e.py`

- Description: TASK-001 전량 검사에서 유일 red 로 드러난 사전 존재 결함.
  검사가 mavis 글로벌 mcp.json 항목의 **사본** 을 하드코딩하고 있었고, 사본에는
  darwin 호스트 절대 경로(`/Users/yklee/...`)가 박혀 있어 **darwin 이 아닌 모든
  호스트에서 무조건 red** 였다 (존재하지 않는 경로로 subprocess 기동 →
  JSONDecodeError).

- Completion criteria: 정본(실제 `~/.minimax/mcp/mcp.json`)을 읽어 e2e, 부재 시 graceful
  skip, 유효 항목이면 이 호스트에서도 전량 green 임을 실증.

## 🛠️ Content

- 하드코딩 `REPO_ROOT`/`MCP_ENV`/`MCP_CMD` → `load_mavis_entry()` 로 실제
  mcp.json 의 command/args/env 를 읽는다. **"mcp.json env 그대로" 라는 검사의
  주장이 이제 문자 그대로 사실이 된다** (사본은 반드시 갈라진다 —
  [[convention-single-source]]).
- 파일/항목 부재 = mavis 미설치 호스트 → **graceful skip** (모름 ≠ 실패),
  `--require-mavis` 로 hard fail 강제 (TASK-2026-08-09-main-004 의 gh 부재 +
  `--require-gh` 패턴).

## ✅ Outcome

- 이 호스트(Linux, mavis 미설치): SKIP rc 0 / `--require-mavis` rc 1 확인.
- 로드 경로 실증: fake HOME 에 유효 항목을 넣자 **ALL PASS** (initialize /
  13 tools set equality / tool call 2종) — 이 호스트에서도 파이프라인 전체 green.
  (검증 함정 1건: fake HOME 이 user site-packages 를 지워 pydantic 부재로 죽는
  인공물 — 항목 env 에 `PYTHONUSERBASE` 명시로 실조건 재현.)
- darwin homelab 에서는 실제 항목으로 기존과 동일한 e2e 가 돈다 (이제 mcp.json
  이 바뀌면 검사도 그것을 본다 — drift 검출력이 오히려 올라감).
