---
id: TASK-2026-08-10-main-003
status: done
created_at: 2026-08-10
source_anchor: release-task-2026-08-10-main-003
source_path: backlog/2026-08-10.md
kind: release
---

# TASK-2026-08-10-main-003 — v1.1.4-beta 를 cmd_release 경로로 발행

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-08-10
- 담당: Claude Code
- 영향 영역: version bump / `releases/Beta-v1.1.4.md` / stamp / tag / GitHub Release

- 작업 내용: TASK-001 이 정상화한 릴리스 파이프라인의 **실전 검증** — v1.1.0 부터
  네 번 연속이던 수동 발행 관행을 끝낸다. 미발행 커밋 3건 (cmd_release 사용성
  회복 + mavis e2e 사본 제거 + v1.1.3 memory).

- 완료 기준: `release --apply` 가 pre_check 통과 → tag push → GitHub Release
  생성까지 파이프라인 안에서 완주. `release-verify` 로 실제 조회 확인.

## 🛠️ Implementation / Content

절차 전체가 파이프라인 안에서 진행됐다:

1. `version-bump --apply` — dirty tree 를 **정확히 거부** (amend 흡수 가드 동작)
   → backlog 커밋 후 재실행, post-step `sync_release_hash` 가 미push 커밋에만
   amend (`head_pushed: False` 확인, `3236bca`). **v1.1.2 에서 고친 코드가 처음
   정상 완주.**
2. `note-draft --apply` → skeleton 을 관행 형식(판정/요약/deliverable/smoke 260/260
   /후속)으로 편집.
3. stamp 정합 — bump 로 붉어진 검사 4종(README/CODE_INDEX/INSTALLATION/
   drift_prevention)을 검사로 찾아 갱신, 재실행 green.
4. `dist --apply` — 1.1.4 whl+sdist 빌드, twine check PASSED (업로드는 정책상 없음).
5. `release --dry-run` — **pre_check 5/5 를 skip 플래그 없이 통과** (ci_mypy
   `ci_stale` 는 미push 커밋 탓, advisory). plan 검토 후 push.
6. `release --apply` — tag `v1.1.4-beta` 생성+push → `gh release create` exit 0
   → dashboard snapshot emit → bidir-link audit 노트 append 까지 자동 완주.

## ✅ Outcome

- **수동 발행 관행 종료** — v1.1.0 부터 4연속이던 수동 절차 없이 `cmd_release`
  경로로 발행 완료. tag `v1.1.4-beta`, [GitHub Release](https://github.com/ykylee/standard_ai_workflow/releases/tag/v1.1.4-beta)
  (assets: whl + sdist, 2026-08-10T02:48:53Z). `release-verify` 실제 조회 확인.
- 자동 후처리(최종 수정일 68건 + dashboard + CHANGELOG `[1.1.4]` + 노트 audit)는
  post-release 커밋으로 수습.
- 릴리스 직후 전량 smoke 재실행으로 오염 없음 확인 (v1.0.0 auto-bump 오염 전례
  방지 절차).
