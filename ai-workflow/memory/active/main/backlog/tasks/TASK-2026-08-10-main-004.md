---
id: TASK-2026-08-10-main-004
status: done
created_at: 2026-08-10
source_anchor: generic-task-2026-08-10-main-004
source_path: backlog/2026-08-10.md
kind: generic
---

# TASK-2026-08-10-main-004 — TST-WF-01 측정 재설계 (관행 인식형)

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-08-10
- Owner: Claude Code
- 영향 영역: `workflow_kit/common/contracts/baselines.py` / pyproject partial_rules /
  `extensions/testing-baseline.md` / `stable_guarantee.md` §5.1 / 관련 smoke

- Description: TASK-001 이 선언 예외로 남긴 자리. TST-WF-01 의 측정(`def test_/case_`
  이름 count)이 정당한 case 관행 2종(inline `check()` / `failures.append`)을 못 봐
  만성 red 였다. 측정을 관행 인식형으로 재설계하고, 성립하면
  `partial_rules.testing` 예외를 제거한다. v0.15.18 의 `assert True` dummy 는
  **세지 않는다** (가짜 신호가 측정을 다시 채우면 안 된다).

- Completion criteria: 새 측정이 dummy 없이 전 파일에서 성립 → partial 예외 제거 →
  doctor `testing` 이 hard 로 복귀하고도 non_compliant 아님 → 회귀 smoke 가
  세 관행 + dummy 배제 + 결함 되주입을 고정. 정본 문서(testing-baseline §3.1)와
  측정이 일치.

## 🛠️ Implementation / Content

**설계는 실측이 결정했다.** AST 기반으로 4개 관행(def test_/case_ · assert ·
inline reporter 호출 · 실패 수집 append)을 세되 `assert True` dummy 를 배제하고
분포를 재니: **260개 전 파일이 ≥1 신호, min=1, 5 미만은 7개뿐, dummy 153개 배제**.
→ hard floor 는 "파일당 ≥1 신호"(검증 없는/parse 불가 파일 검출), 파일당 ≥5 는
권장으로 강등해 notes 에 미달 수를 노출.

- `_count_verification_signals()` + `_is_trivial_test_def()` 신설
  (`baselines.py`, mypy strict 128 clean 유지). parse 불가 = 신호 0.
  대상 파일의 SyntaxWarning 은 doctor 출력에 새지 않게 억제.
- notes 형식은 `across N files` 구문 유지 (기존 소비자 regex 정합).
- `partial_rules.testing` 예외 제거 — **TST-WF-01 hard 복귀 + 정직하게 compliant**.
- `check_release_pre_check_gates` case 10 반전: 예외 *부재* 를 고정 (조용한 재도입 검출).
- 정본 문서 동기: `testing-baseline.md` §3.1 Verification 재작성 +
  `stable_guarantee.md` §5.1 v1.1.5 종결 기록.

## ✅ Outcome

- `check_tst_wf01_signals.py` **9/9 PASS** 신설 — 관행 3종 counting + dummy 배제 +
  **되주입 3종**(신호 0 파일 / parse 불가 / 빈 디렉터리 → non_compliant).
- doctor 실측: testing `advisory` (TST-WF-01 hard 로 compliant, 02·03 advisory).
  release pre_check 게이트 정합 유지.
- v0.15.18 이 남긴 dummy wrapper 는 이제 **측정에 안 세일 뿐 파일에는 남아 있다**
  — 제거는 115 파일 churn 이라 별건 (측정이 정직해졌으므로 급하지 않음).
- smoke 260 → **261**, 노트/CODE_INDEX/INSTALLATION 수치 갱신.
