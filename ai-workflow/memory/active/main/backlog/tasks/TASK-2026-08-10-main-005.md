---
id: TASK-2026-08-10-main-005
status: done
created_at: 2026-08-10
source_anchor: fix-task-2026-08-10-main-005
source_path: backlog/2026-08-10.md
kind: fix
---

# TASK-2026-08-10-main-005 — dist 기본값 dry-run 반전 (release 와 정합)

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-08-10
- Owner: Claude Code
- 영향 영역: `tools/release_pipeline.py` dist subparser / `check_release_pre_check_gates`

- Description: TASK-001 이 `release` 의 `--apply default True` 를 반전할 때 같은
  결함이 `dist` subparser 에도 복제돼 있음을 확인하고 보류했던 것. 무인자
  `dist` 가 빌드를 수행하던 것을 dry-run 기본으로 반전.

- Completion criteria: 무인자 `dist` 가 dry-run 으로 진입하고, 회귀 case 가 두 subparser
  의 default 를 함께 고정.

## 🛠️ Content / ✅ Outcome

- `p_dist --apply` default True → False. main() 의 "둘 다 없으면 dry-run" 정규화가
  release 때처럼 이미 있었고 default True 가 그걸 무력화하고 있었다 — **같은
  결함이 두 subparser 에 복제돼 있던 것** (규약은 단일 출처로의 사촌: 결함도
  사본으로 늘어난다). lib wrapper (`cmd_dist(apply=False)`) 는 이미 dry-run 기본.
- `check_release_pre_check_gates` 10 → **12/12** — case 1b (dist default AST) +
  3b (무인자 dist subprocess → dry-run). 기존 dist 소비자는 전부 flag 명시라
  회귀 없음 (phase3 8/8 / lib 9/9 재확인).
- docs/RELEASE.md 기본값 문단에 dist 반영.
