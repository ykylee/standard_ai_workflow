---
id: TASK-2026-08-10-main-008
status: done
created_at: 2026-08-10
source_anchor: generic-task-2026-08-10-main-008
source_path: backlog/2026-08-10.md
kind: generic
---

# TASK-2026-08-10-main-008 — title drift 임계 0.6 실측 보정

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-08-10
- Owner: Claude Code
- 영향 영역: `workflow_kit/common/drift_detection.py`,
  `tests/check_title_drift.py`, `core/multi_workspace_orchestration.md` §7.5

- Description: `TITLE_SIMILARITY_THRESHOLD = 0.6` 은 주석 스스로 "실측으로 고른
  값이 아니라 출발점" 이라고 적힌 미보정 값 (2026-08-09 세션 "검증 못 한 것").
  이 저장소의 backlog git 히스토리에는 같은 TASK-ID 제목의 실제 개정 이력
  (표기 차이 vs 실질 개정)이 쌓여 있으므로, 그것을 캘리브레이션 데이터로 삼아
  임계를 실측으로 확정한다. 조사는 일회용으로 끝내지 않고 검사로 고정한다
  (one-off-audit-is-no-audit).

- Completion criteria: 실측 데이터 기반 임계 확정 (유지든 변경이든 근거 명시),
  캘리브레이션이 재실행 가능한 형태로 저장소에 고정, 정본 문서/주석 동기,
  전량 검사 green.

## 🛠️ Implementation / Content

- **채굴 1차 실패가 첫 수확**: 임의 줄의 TASK-ID 언급을 전부 먹였더니 제목 아닌
  산문("있다", "[x] ~~")이 섞여 분포가 뒤집혔다 (양성 median 0.07 / 음성 median 1.0).
  제목이 *정의되는* 자리(backlog bullet / task H1 / handoff 의 production 소비
  섹션)만 먹이니 정상 분포. production 경로 추출은 쓰레기 제목 0건 — 입력층은
  production 에서 건강하다는 것도 확인.
- **측정 범위 = production 범위**: 구식 ID(`TASK-2026-06-26-010`, 브랜치 세그먼트
  없음)는 `TASK_ID_PATTERN` 이 안 잡는다 = production title drift 대상이 아니다.
  캘리브레이션도 신식 ID 88개로 제한 (무엇으로 실행되는지를 재라).
- 데이터: 현재 트리 326 문서 + git 히스토리 576 버전 → 양성 81쌍 (canonical 14 /
  handoff 67), 음성 프록시 375쌍 (인접 hard + 원거리 far).
- **기각한 대안 (실측)**: 꼬리 괄호 제거 정규화 — 양성 노이즈 26→17 대신 음성
  놓침 115→287. 괄호 부연이 정확히 변별 정보였다.
- 산출물: `scripts/calibrate_title_drift.py` (--date 명시, 기본 dry-run) +
  `schemas/title_drift_calibration.json` (75KB, _meta 에 방법 기록) +
  `tests/check_title_drift_calibration.py` 7 case (되주입 2종 실증: similarity
  위조 / 임계 표류 모두 fail).

## ✅ Outcome

- **임계 0.6 유지 — 이제 실측 근거가 있다**: 정본 양성 노이즈 1/14 (유일 사례 =
  제목 vs 제목+괄호 부연), 음성 검출 373/375, 임계는 음성 p95(0.41)와 정본 양성
  사이에 여유 있게 위치.
- **구조적 한계 실측 고정**: 같은-축 형제 task 쌍 2건 (0.694 / 0.706) 은 어떤
  임계로도 유사도로 못 가른다 — 검사 case 6 이 이 한계를 표본으로 동결 (임계를
  올려 "고치면" 노이즈 상한이 깨져 의식적 재캘리브레이션을 강제).
- handoff 양성 25/67 이 임계 미만 — 계획 문구 vs 완료 제목의 정당한 재표현으로,
  LLM 판정에 넘기는 것이 설계 (threshold 로 줄일 대상이 아님).
- 검사가 임계 == fixture 캘리브레이션 임계를 강제 — 값 변경 시 재캘리브레이션 동반 필수.
- 정본 동기: `drift_detection.py` 주석 / §7.5 / §0.7 상태표 / schemas·scripts README.
