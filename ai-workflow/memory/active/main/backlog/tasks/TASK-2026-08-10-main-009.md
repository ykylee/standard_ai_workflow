---
id: TASK-2026-08-10-main-009
status: done
created_at: 2026-08-10
source_anchor: generic-task-2026-08-10-main-009
source_path: backlog/2026-08-10.md
kind: generic
---

# TASK-2026-08-10-main-009 — registry server 비-loopback bind 실측

## 📝 Description

- 상태: done
- 우선순위: low
- 요청일: 2026-08-10
- 담당: Claude Code
- 영향 영역: `tests/check_registry_server.py`,
  `core/multi_workspace_orchestration.md` §7.4

- 작업 내용: registry HTTP server 는 loopback 왕복만 실측된 상태 (2026-08-09
  세션 "검증 못 한 것"). 이 호스트의 LAN 인터페이스 IP 로 bind + 그 주소로의
  pull 왕복(토큰 포함)을 smoke case 로 추가해 비-loopback 경로를 실측한다.
  진짜 cross-host / 방화벽 / TLS 는 이 호스트에서 불가 — darwin homelab 몫으로
  남김을 문서에 명시한다.

- 완료 기준: 비-loopback bind + pull 왕복 case green (인터페이스 부재 시
  graceful skip), §7.4 미검증 잔여 명시 갱신, 전량 검사 green.

## 🛠️ Implementation / Content

- `check_registry_server.py` 에 case 10 신설: `_lan_ip()` (UDP connect to
  TEST-NET-1 — 패킷 없이 OS 라우팅의 source IP 만 획득) 로 비-loopback IPv4 를
  얻어, 그 주소로 bind 한 서버에 GET + `pull_remote_registry()` + 토큰 왕복.
- LAN IP 부재(오프라인 컨테이너 등)는 graceful skip + `--require-lan` 강제
  (--require-mavis / --require-gh 와 같은 패턴). 합계는 실행된 case 만 센다
  (skip 포함 시 N/N 이 거짓말이 되므로).
- `_Server` 헬퍼에 `bind` 파라미터 additive.

## ✅ Outcome

- **비-loopback 왕복 실측 성공**: 이 호스트 LAN IP(192.168.0.121) bind + GET +
  pull + 토큰 인증 왕복 green. 10/10 PASS.
- §7.4 / §0.7 상태표 갱신 — **여전히 검증 밖**: 진짜 cross-host / 방화벽 /
  reverse proxy / TLS 종단 (두 번째 호스트 필요, darwin homelab 몫으로 명시).
