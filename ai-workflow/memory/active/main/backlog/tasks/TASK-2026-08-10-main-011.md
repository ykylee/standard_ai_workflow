---
id: TASK-2026-08-10-main-011
status: done
created_at: 2026-08-10
source_anchor: feature-task-2026-08-10-main-011
source_path: backlog/2026-08-10.md
kind: feature
---

# TASK-2026-08-10-main-011 — ADR-006 W-1: memory_index write-path advisory 루프

## 📝 Description

- 상태: done
- 우선순위: medium
- 요청일: 2026-08-10
- 담당: Claude Code
- 영향 영역: `workflow_kit/common/state/memory_index.py`,
  `tools/suggest_memory_entries.py` (신규), `tests/` (신규 smoke),
  `workflow_kit/common/tool_dispatch.py`, `pyproject.toml`,
  `docs/architecture/ADR-006-memory-index-retrospective.md`

- 작업 내용: ADR-006 회고의 최우선 후속 W-1. entries 가 2026-07-09 이후 신규
  0건 — 읽기 자동화(v0.15.21)에 대응하는 쓰기 운영 루프가 없다. 세션이 남긴
  완료 작업(handoff §4)을 기존 entry 의 cue 와 대조해 "index 가 모르는 것" 을
  entry 후보로 **advisory 제안** 하는 도구를 만든다. 자동 적재는 하지 않는다
  (도구가 대신 쓰면 거짓이 된다 — 노트 누적 수치 검증 step 3.4 와 같은 원칙).

- 완료 기준: pure 판정 함수 + CLI (`wk suggest-memory-entries`, 무-write) +
  smoke (되주입 포함) + dispatcher/[project.scripts] 등록 정합 + ADR-006 W-1
  구현 표시. 전량 검사 green.

## 🛠️ Implementation / Content

- `suggest_memory_entry_candidates()` (pure, `state/memory_index.py`): handoff
  §4 제목 token 을 entry corpus (`_bm25_text_for_entry` 재사용) 와 대조,
  coverage = |제목∩entry|/|제목| < 0.5 면 후보. 후보에 스키마 모양 skeleton
  (다음 빈 `MEM-<date>-NNN` id 포함) 을 붙인다. 제목 추출은 drift_detection 의
  `extract_section`+`extract_task_titles` 재사용 (규약 단일 출처).
- `tools/suggest_memory_entries.py` CLI + `wk suggest-memory-entries` 등록
  (TOOL_MODULES + [project.scripts], check_wk_dispatcher 10/10 정합).
- `check_memory_entry_suggestions.py` 8 case — tmp workspace fixture (살아있는
  저장소 상태에 안 기댐), 되주입(corpus 에 넣으면 후보가 사라짐), advisory
  무-write, CLI json/error path.

## ✅ Outcome

- **루프 실증 완주**: 첫 실측에서 최근 10 task 전부 후보 (max coverage 0.14 —
  회고 발견의 재확인) → 도구 제안 중 회고 자체를 에이전트가 판단해
  `MEM-2026-08-10-001` 로 적재 (**33일 만의 첫 신규 entry**) → 재실행 시
  covered 0→1, `query [retrospective,write-path]` 가 새 entry 를 돌려줌.
  읽기(자동 활성)에 대응하는 쓰기 운영 루프가 생겼다.
- validate issues 0 / bidir-link 6/6 / memory_index smoke 26/26 / mypy clean.
- ADR-006 W-1 ✅ 구현 표시 + memory_index README 운영 규칙에 사용 시점 명시.
- smoke 262→263 파생 3곳 갱신.
