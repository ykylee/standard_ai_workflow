---
id: TASK-2026-08-10-main-012
status: done
created_at: 2026-08-10
source_anchor: feature-task-2026-08-10-main-012
source_path: backlog/2026-08-10.md
kind: feature
---

# TASK-2026-08-10-main-012 — ADR-006 W-2: memory_index 질의 다양화

## 📝 Description

- 상태: done
- 우선순위: medium
- 요청일: 2026-08-10
- 담당: Claude Code
- 영향 영역: `workflow_kit/common/state/memory_index.py`,
  `workflow_kit/common/schemas/memory_index.py` (additive),
  `skills/{session-start,doc-sync,backlog-update}/scripts/`,
  `workflow_kit/workflow_kit_cli.py`, `tests/` (신규 smoke),
  `docs/architecture/ADR-006-memory-index-retrospective.md`

- 작업 내용: ADR-006 W-2. 3 skill 의 질의가 각자 고정 trio 였고 공통 token
  "workflow" 가 항상 같은 entry 를 집어 33일간 질의 다양성이 1 이었다.
  (1) 현재 컨텍스트(state.json 의 current_axis + 최근 done 제목)에서 질의
  token 을 유도하는 공용 helper — 유도 실패 시 기존 default 로 떨어지되
  **출처를 보고** (silent fallback 금지). (2) telemetry 에 질의 *내용*
  (`query_tokens`) 과 출처(`query_source`)를 additive 로 기록해 다양성을
  측정 가능하게.

- 완료 기준: 공용 helper (단일 출처, 3 skill + dispatcher 공유) + telemetry
  additive 2 필드 (구 라인 하위호환) + smoke (되주입 포함) + ADR-006 W-2
  구현 표시 + 실측 정밀화 (skill 별 고정 trio). 전량 검사 green.

## 🛠️ Implementation / Content

- `derive_context_query_tokens(state_path, base_tokens=…)` (pure, 단일 출처):
  state.json 의 `session.current_axis` + `backlog.done_items[:3]` 을
  `_bm25_tokenize` 로 토큰화 (stopword/숫자 제거, dedupe, cap 8). 유도 실패
  (state 부재/깨짐/유의미 token 0) 는 skill 별 기존 trio 로 떨어지되 출처
  ("context"/"default") 를 함께 반환 — 조용한 fallback 은 출처를 내놓아야 한다.
- telemetry additive 2 필드: `query_tokens` (내용, ≤16) + `query_source`
  ("context"/"default"/"explicit"). 구 라인은 기본값으로 하위호환 (실측 case 6).
- 3 skill (`session-start`/`doc-sync`/`backlog-update`) 의 builder 를 공용
  helper 호출로 교체 + telemetry 3곳/skill 에 신규 필드. dispatcher 는
  "explicit". SKILL.md 3종 동기.
- **부수 발견/수정**: 첫 컨텍스트 질의의 정직한 miss (cue 0) 가 33일간
  hit_rate=1.0 뒤에 숨어 있던 **패널 간 반올림 불일치** (Panel 3 raw 0.99619…
  vs Panel 8 round-4 0.9962, cross-check case 4 실패) 를 즉시 드러냄 →
  round(4) 를 계산 지점(단일 출처, `summarize_telemetry`)으로 이동.

## ✅ Outcome

- **실사 검증**: session-start 실행 → query_tokens `['릴리스','파이프라인',
  '정상화','사이클','완결','2회','실전','adr']`, `query_source: "context"`,
  cue 0 / selected 0 — index 가 최근 한 달을 모른다는 **정보가 있는 miss**
  (W-1 write-path 로 흐를 신호). 33일간 다양성 1 이던 질의가 컨텍스트를
  따라가기 시작.
- `check_context_query_tokens` 8 case 신설 (하위호환 + 되주입 포함).
  telemetry 6/6 · window 8/8 · cross 4/4 · diversity 5/5 · dashboard 4/4 ·
  FULL mypy 128 clean. smoke 263→264 파생 3곳 갱신.
- "변하지 않는 지표는 자기 소비자의 결함도 숨긴다" — 회고 교훈의 즉석 재확인.
