---
id: TASK-2026-08-10-main-014
status: done
created_at: 2026-08-10
source_anchor: feature-task-2026-08-10-main-014
source_path: backlog/2026-08-10.md
kind: feature
---

# TASK-2026-08-10-main-014 — ADR-006 W-4: memory_index 지표 재정의 (3-tuple)

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-08-10
- Owner: Claude Code
- 영향 영역: `workflow_kit/common/schemas/memory_index.py` (additive),
  `workflow_kit/common/state/memory_index.py`, 3 skill + dispatcher telemetry,
  `workflow_kit/common/dashboard_data.py` (Panel 8), `tests/` (신규),
  `docs/architecture/ADR-006-memory-index-retrospective.md`

- Description: ADR-006 W-4. hit_rate 단독 지표는 33일간 1.0 으로 고정돼
  정보가 없었고 소비자 결함(패널 반올림 불일치)까지 숨겼다. 재정의:
  **(질의 다양성 / 30일 신규 entry / 조회된 distinct entry) 3-tuple** 을
  north-star 로, hit_rate 는 보조로 강등. distinct 조회를 측정하려면
  telemetry 에 `selected_ids` 가 필요 (additive). 구 라인은 내용이 없으므로
  각 지표에 `*_measurable` 동반 필드로 "미측정" 을 0 과 구분한다
  (미분류 fallback 은 사실을 날조한다).

- Completion criteria: telemetry `selected_ids` additive + summarize 3-tuple (단일
  출처) + Panel 8 north-star 교체 + smoke (되주입 포함) + 실사 검증 +
  ADR-006 W-4 표시. 전량 검사 green.

## 🛠️ Implementation / Content

- telemetry `selected_ids` additive (성공 경로 4곳: 3 skill + dispatcher) —
  어떤 entry 가 실제로 읽히는지가 처음으로 기록된다.
- `summarize_telemetry` 에 3-tuple (단일 출처): `query_diversity`(distinct
  질의 내용) / `distinct_entries_retrieved`(selected_ids 합집합) + 각
  `*_measurable` 분모. **measurable 판정은 값의 비어있음이 아니라 필드의
  존재 (`model_fields_set`)** — 0건 조회(miss)도 측정이고, 구 라인의 미측정은
  0 으로 위장하지 않는다 (구현 중 실제로 이 함정을 밟고 고침: `selected_ids=[]`
  가 구 라인 기본값과 구분되지 않았다).
- Panel 8: `entries_new_30d` (created_at 30일 경계) + `utilization_3tuple` +
  `phase_15_north_star` 교체. hit_rate 는 은퇴 대신 보조 강등 (W-2 이후 더는
  고정 1.0 아님 + 기존 소비자 하위호환).

## ✅ Outcome

- **첫 실측 (정직한 저점)**: diversity 1/8 · entries_new_30d 1 · distinct
  0/1. "0/1" 은 "측정했고 0건" — 이전의 "0/0 = 미측정" 과 구분된다.
  33일간 1.0 고정이던 north-star 가 이제 움직일 수 있는 지표가 됐다.
- `check_utilization_3tuple` 9 case 신설 (miss-도-측정 case 3 이 되주입 검출
  겸용). 회귀 sweep 9종 전부 green (telemetry/window/cross/panels/diversity/
  context/links/memory_index). FULL mypy 128 clean. smoke 265→266.
- ADR-006 W-4 표시 — **후속 W-1~W-4 전체 완결**, wiki topic 동기.
