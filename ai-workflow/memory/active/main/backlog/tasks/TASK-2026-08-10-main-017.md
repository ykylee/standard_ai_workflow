---
id: TASK-2026-08-10-main-017
status: done
created_at: 2026-08-10
source_anchor: feature-task-2026-08-10-main-017
source_path: backlog/2026-08-10.md
kind: feature
---

# TASK-2026-08-10-main-017 — 브랜치 컨텍스트를 정본화하고 로컬 재현을 관행으로

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-08-10
- 담당: Claude Code
- 영향 영역: `workflow-source/workflow_kit/common/branch_matrix.py` (신설),
  `workflow-source/tests/run_all_checks.py`,
  `workflow-source/tests/check_branch_context_matrix.py` (신설),
  `.github/workflows/smoke.yml`, `CLAUDE.md`

- 작업 내용: TASK-016 의 **근본 대책**. 그 건은 검사 하나를 고쳐 red 를 껐지만,
  *같은 함정이 다시 생기는 것* 은 막지 못했다. 뿌리는 결함이 아니라 **재현 수단의
  비대칭** 이다 — CI 는 전량을 두 브랜치 컨텍스트(`native`/`slash`)로 돌리는데
  로컬 `run_all_checks.py` 는 native 하나만 돈다. 그래서 15연속 red 인 동안 로컬은
  계속 green 이었고 handoff 는 내내 "전량 검사 green" 을 기록했다.

  같은 모양을 저장소가 이미 알고 있었다: `mcp` SDK 매트릭스. 대응도 이미 있다 —
  **정본을 한 곳에 두고 CI 와 로컬이 그것을 읽는다** (`sdk_matrix.py` +
  `--run-local`). 브랜치 축에만 그 대응이 없었다.

- 완료 기준: 컨텍스트 목록의 정본이 하나이고, CI 와 로컬이 **같은 것을** 읽으며,
  복제·누락이 검사로 잡히고, 관행이 CLAUDE.md 에 적힌다.

## 🛠️ Content

- **정본 신설** `workflow_kit/common/branch_matrix.py` — `BRANCH_CONTEXTS`
  (label / workflow_branch / reason). stdlib only (CI prepare job 이 설치 없이
  실행). 오버라이드 키는 `BRANCH_ENV_KEYS[0]` 를 재사용해 그것마저 복제하지 않는다.
  `apply_context(env, ctx)` 가 native 에서 **상속된 오버라이드를 제거** 한다
  (아래 Outcome 의 자기 검출 참조). 빈 문자열 주입과는 다르다 — 빈 값은 다음 env
  키로 흘러가 "native 를 쟀다" 와 "빈 값이라 흘렀다" 가 같은 모양이 된다.
- **로컬 재현** `run_all_checks.py --branch-context=<label|all>`. 미지정은 기존
  동작(호출자 환경 그대로) 유지. 단일 컨텍스트의 `--json` 도 기존 형태 그대로다 —
  smoke.yml 의 요약 스크립트가 `data["total"]`/`data["results"]` 를 직접 읽는다.
  `all` 만 `{"contexts": [...]}` 라는 새 형태를 낸다.
- **CI 가 정본을 읽는다** — `smoke.yml` 에 prepare job 추가, 셀 목록은
  `--github-matrix` 로 주입 (`mcp-sdk-matrix.yml` 과 같은 패턴). yml 에서 브랜치
  문자열을 제거했다.
- **검사 신설** `check_branch_context_matrix.py` 8 case — registry 정합 / env 주입
  계약 / 매트릭스 형태 / yml 이 정본을 소비 / **yml 복제 검출** / runner 선언 /
  **주입 end-to-end** / `all` 이 선언 전부를 순회.
- **관행 기록** — CLAUDE.md 에 SDK 매트릭스 항목과 나란히 "브랜치 매트릭스도 push
  전에 로컬에서 돌린다" 추가.

## ✅ Outcome

- **새 축이 첫 실행에서 결함 2건을 잡았다** — 그것도 *자기 자신의* 결함을. 이 축이
  없었다면 둘 다 다음 push 의 CI red 로 나타났을 것이다:

  1. **`--branch-context=native` 가 native 를 재지 않았다.** `env_override()` 는
     native 에 대해 "주입하지 않는다" 였는데, 부모 env 에 이미
     `CODEX_WORKFLOW_BRANCH` 가 있으면 그 값이 그대로 이긴다. `--branch-context=all`
     의 slash 패스 안에서 native 를 요청하자 slash 결과(13/13)가 나왔다 — **요청한
     것과 다른 축을 재고 통과를 보고하는**, 이 작업이 막으려던 실패 양상 그 자체다.
     `apply_context()` 로 바꿔 native 가 상속 오버라이드를 *제거* 하게 했다.
     잡아낸 것은 `test_runner_actually_injects_context` — 선언이 아니라 end-to-end
     를 재게 한 판단이 값을 했다.
  2. **`check_branch_scoped_memory` case 10 이 yml 인라인 셀을 파싱하고 있었다.**
     matrix 를 `fromJSON` 으로 바꾸자 dict 가 아니라 문자열이 되어 깨졌다 (정당한
     검출). 같은 의도를 정본 기준으로 재작성 — 셀 목록은 `BRANCH_CONTEXTS` 에서
     읽고, yml 에 대해서는 *정본을 소비하는가* + *오버라이드 자기 검증이 있는가* 만
     본다.

- `check_branch_context_matrix` **8/8**, 되주입 2종 실증: runner 의 주입을 제거하면
  end-to-end 케이스가 잡고(slash 가 14/14 를 낸다), yml 에 브랜치 문자열을 복제하면
  복제 검출이 잡는다.
- mypy strict **129 파일 clean** (`branch_matrix.py` 포함).
- 부수: 검사 파일 266→267 로 stamp 4곳 갱신 (CODE_INDEX / INSTALLATION 2곳 /
  Beta-v1.1.6 노트의 "살아있는 지표" 줄 — 노트가 스스로 그렇게 선언한 자리다).
- 이 호스트(darwin)에 남는 사전 존재 실패 1건: `check_branch_scoped_memory` 의
  `/private/var` ↔ `/var` symlink 케이스. 변경 전에도 실패했고 CI(Linux)는 green.
  별건이다.
