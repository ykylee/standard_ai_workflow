---
id: TASK-2026-08-10-main-019
status: done
created_at: 2026-08-10
source_anchor: fix-task-2026-08-10-main-019
source_path: backlog/2026-08-10.md
kind: fix
---

# TASK-2026-08-10-main-019 — 원본 저장소에 `--apply` 하던 검사 4건을 사본으로

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-10
- Owner: Claude Code
- 영향 영역: `workflow-source/tests/_repo_sandbox.py` (신설),
  `check_release_pipeline_version_auto_sync.py`, `check_self_recovering_v0_13_2.py`,
  `check_bidir_link_v0_13_3.py`, `check_release_pipeline_phase3.py`,
  `check_no_repo_write.py`

- Description: TASK-018 이 정숙 마커로 봉합했던 것의 근본 해법. 네 검사가 **원본 저장소**
  에 도구를 `--apply` 로 돌린 뒤 되돌려 왔다:

  | check | 건드리던 것 |
  |---|---|
  | `check_release_pipeline_version_auto_sync` | `pyproject.toml` (version → 99.99.99), `__init__.py` |
  | `check_self_recovering_v0_13_2` | `README.md`, `pyproject.toml`, `__init__.py` (drift 주입) |
  | `check_bidir_link_v0_13_3` | memory_index entry, wiki topic 3건 |
  | `check_release_pipeline_phase3` | `standard_ai_workflow-<ver>/` 실빌드 산출물 |

  되돌리므로 `check_no_repo_write` 의 전후 비교는 통과했다. 그러나 CLAUDE.md 는
  "여러 에이전트가 함께 일할 수 있다" 를 전제한다 — 그 사이 다른 에이전트가 버전을
  읽으면 `99.99.99` 를 본다. 죽으면 되돌아오지도 않는다.

- Completion criteria: 네 검사가 원본을 읽기만 하고, 그 계약이 실행 경로에서 검증되며,
  정숙 구간에서 빠져 병렬로 돈다.

## 🛠️ Content

- **`_repo_sandbox.py` 신설** — `repo_sandbox(repo_root, include_git=False)` 하나.
  파일명이 `check_` 로 시작하지 않아 runner 가 검사로 줍지 않는다. 실측 비용:
  `.git` 제외 0.24s/18MB, 포함 0.52s/41MB.
- 네 검사 모두 `main()` 에서 사본을 만들고 **모듈 전역 경로를 재바인딩** 한 뒤
  기존 케이스를 `_run_cases()` 로 돌린다. 함수마다 경로를 주입하는 편이 깔끔하지만
  그러면 파일 전체를 손대야 하고, "원본을 안 건드린다" 는 계약은 이것으로 지켜진다.
- **계약을 실행 경로에서 잰다** — 각 검사가 끝에서 원본 파일의 바이트를 실행 전과
  비교해 assert 한다. 말로만 두지 않는다.
- `check_bidir_link_v0_13_3` 은 `_SNAPSHOT_ROOTS` 도 사본 기준으로 다시 잡아야 했다.
  모듈 로드 시점의 스냅샷은 원본을 가리키므로, 그대로 두면 `_restore_state()` 가
  사본이 아니라 **원본에 쓴다** (실제로 그 함정을 밟고 고쳤다).
- `check_no_repo_write` 감시 목록에 3건 추가 (이중 방어). 13 감시 대상 전부 변경 0.

## ✅ Outcome

- 네 검사 모두 **단독 실행 + 20ms 폴링에서 "깨끗"**. 정숙 구간 **9 → 3**
  (`check_no_repo_write` / `check_parallel_smoke` / `check_source_without_runtime_layer`
  만 남았고, 앞의 둘은 전역 관찰·runner 호출이라 본질적으로 정숙이 필요하다).
- 전량 **268/268 × 2 컨텍스트 PASS** (native 128.9s / slash 125.9s).
- **성능 이득은 사실상 0이다.** 정숙 구간에서 25초어치가 병렬로 옮겨갔지만 사본 복사
  비용이 그만큼을 먹었다 (118.8s → 128.9s, 측정 변동 포함). 착수 전 예상한 대로이며,
  이 작업의 값어치는 속도가 아니라 **협업 안전**이다. 섞어서 "정리하면 빨라진다" 고
  말하지 않는 편이 정확하다.
- **남은 것**: `check_no_repo_write` 의 계약은 여전히 "실행 **후** 복원되었는가" 다.
  `check_bidir_link_v0_13_3` 은 **이미 감시 목록에 있었는데도** 잡히지 않았다 —
  되돌리기 때문이다. 실행 *중* 감시(폴링)로 강화하면 남은 감시 대상 다수가 같은
  이유로 red 가 될 수 있어 범위가 크다. 별도 task 로 남긴다.
- 방법론 교훈: **전수 폴링은 후보를 좁히는 데만 쓰고 단독 실행으로 확정한다.**
  전수 조사가 6건을 지목했지만 단독 재검증에서 2건(`version_flag`, `session_start`)은
  깨끗했다 — 짧은 check 라 폴링 스레드가 읽는 "현재 이름" 이 전환 순간에 어긋나며
  인접 check 의 소행이 잘못 귀속됐다.
