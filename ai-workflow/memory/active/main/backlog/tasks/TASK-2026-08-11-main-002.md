---
id: TASK-2026-08-11-main-002
status: done
created_at: 2026-08-11
source_anchor: fix-task-2026-08-11-main-002
source_path: backlog/2026-08-11.md
kind: fix
---

# TASK-2026-08-11-main-002 — release amend Guard 2 가 staged 삭제 경로에서 fatal (git add pathspec 불일치)

## 📝 Description

- 상태: done
- 우선순위: medium
- 요청일: 2026-08-11
- 담당: Claude Code
- 영향 영역: `workflow-source/tools/release_pipeline.py` (amend Guard 2, `_git_dirty_paths` 소비처), `workflow-source/tests/check_release_wrapper_args.py` (case 5·6)

- 작업 내용: TASK-001 (mypy strict 검사 8개 제거) 중 발견. `git rm` 으로 **staged
  삭제** 가 있는 상태에서 `check_release_wrapper_args` case 6 이
  `fatal: pathspec '...' did not match any files` (rc=128) 로 red.
  뿌리는 검사보다 production 경로다 — amend Guard 2 (`release_pipeline.py:1030`)
  가 `_git_dirty_paths()` 결과를 `git add -- *paths` 로 넘기는데, **이미 index 에
  staged 된 삭제** (`D ` 상태) 는 worktree 에도 index 에도 없어 pathspec 이 아무것도
  못 잡고 add 전체가 fatal 한다. `--allow-dirty` + staged 삭제 조합의 실전
  release 에서 같은 방식으로 터진다. case 5 (`(REPO_ROOT / p).exists()`) 도 삭제
  경로에서 같은 이유로 red.

- 완료 기준: staged 삭제가 있어도 amend add 경로가 정상 동작 (staged 삭제는 이미
  index 에 있으므로 add 대상에서 제외하거나 동등한 처리) + case 5·6 이 삭제 포함
  dirty 상태에서 green + 되주입 case 로 고정.

## 🛠️ Content

- `_git_dirty_paths(needs_add_only=True)` 신설 — porcelain worktree 열(Y)이
  index 와 다른 entry 만 반환 (`git add` 대상 선별용). staged-only 변경
  (`M `/`D `/`A `, Y=' ') 은 이미 index 에 있어 add 불필요, 특히 staged 삭제는
  pathspec fatal. 기본 동작 (전체 dirty) 은 불변 — bump 의 clean-tree 가드
  (line 859) 는 계속 전체를 본다.
- amend Guard 2: 보고용 `staged_paths` (전체) 와 add 대상 (`add_targets` 선별)
  을 분리. add 대상이 비면 add 를 건너뛰고 amend 로 직행 (staged-only 변경은
  amend 가 그대로 흡수).
- `check_release_wrapper_args` 갱신: case 5 는 삭제 entry 를 존재 검사에서
  제외 (삭제 경로는 정의상 부재가 정상), case 6 은 production 이 실제 add 하는
  집합 (선별) 로 대조, **case 10 신설** — tmp git repo 에 staged 삭제 / unstaged
  수정·삭제 / untracked 를 심고 `_git_toplevel` monkeypatch 로 함정과 해법을
  결정적으로 고정 (full list add 는 rc=128, 선별 add 는 성공, unstaged 삭제는
  선별에 포함).

## ✅ Outcome

- 11/11 PASS (기존 10 + case 10). **되주입 실증**: 필터를 무력화하면 case 10 이
  잡는다 (10/11 FAIL, sel 이 staged 삭제를 포함 + fix_rc=128) → 복원 후 11/11.
- mypy: 변경 전후 동일 89 오류 (tools/ 는 strict 범위 밖, 신규 오류 0).
- release 계열 검사 전량 green. 전량 2-컨텍스트 검증 후 push.
