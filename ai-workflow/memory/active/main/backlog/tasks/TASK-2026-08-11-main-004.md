---
id: TASK-2026-08-11-main-004
status: done
created_at: 2026-08-11
source_anchor: cleanup-task-2026-08-11-main-004
source_path: backlog/2026-08-11.md
kind: cleanup
---

# TASK-2026-08-11-main-004 — check_cache_* 13개를 check_cache.py 1개로 통합

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-08-11
- Owner: Claude Code
- 영향 영역: `workflow-source/tests/check_cache_*.py` (13개 → 1개), 파생 수치 문서 3종

- Description: 2026-08-10 3차 세션 조사(handoff §7) 후보 3. cache 계열 검사가
  모듈당 1파일 (각 65~195줄, 1~4 case) 로 잘게 쪼개져 subprocess 기동만 13번.
  단일 `check_cache.py` 로 병합 — **test 본문은 verbatim 보존** (버전 이력이
  담긴 함수명 포함), 변경은 모듈 로더 보일러플레이트 공용화 (`_load` /
  `_load_wk` 2계보) 와 충돌 상수의 동일-정의 dedupe 뿐.
- 외부 참조 실측: 릴리스 노트(이력)뿐 — 코드/CI/문서의 살아있는 참조 0.

- Completion criteria: 병합본 31/31 PASS (원본 13파일 case 합계와 동일) + 13개 파일 제거
  + 파생 수치 문서 동기 (smoke 260→248) + 전량 검사 green (2 브랜치 컨텍스트).

## 🛠️ Content

- 병합 형태: 섹션 주석으로 원본 13파일 경계 보존 + 단일 main() (원본 파일 순서
  유지). 로더는 2계보로 정리 — `_load(mod)` (bare 등록, 5파일 계보) /
  `_load_wk(mod)` (`workflow_kit.<name>` package 등록, 8파일 계보). 동작이 다른
  두 계보를 하나로 합치지 않았다 (병합이 로드 의미론을 바꾸면 안 된다).
- 충돌 상수 5건 (`_analytics`, `_lfu_config`, `_decay`, `_url_validity`,
  `CacheEntry`) 은 전부 동일 모듈의 byte-identical 로드라 최초 출현 정의로
  dedupe — rename 으로 인한 test 본문 수정 0건. 본문 수정은 로더 call-site
  치환뿐.

## ✅ Outcome

- `check_cache.py` **31/31 PASS** (원본 13파일 case 합계 2+2+1+2+2+2+3+2+4+2+5+2+2
  = 31 과 동일). check 파일 수 260→248 (−13+1).
- 파생 수치 3문서 동기 (260→248): CODE_INDEX / INSTALLATION 2곳 / v1.1.6 노트
  누적 줄.
- 외부 참조 실측 0 (릴리스 노트 이력 언급만 — 보존). 전량 검사 green 은
  commit 후 2 브랜치 컨텍스트로 확인.
