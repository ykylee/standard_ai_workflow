---
id: TASK-2026-08-11-main-005
status: done
created_at: 2026-08-11
source_anchor: fix-task-2026-08-11-main-005
source_path: backlog/2026-08-11.md
kind: fix
---

# TASK-2026-08-11-main-005 — smoke CI red 해소: version_flag test 3 이 다른 검사의 dist 오염에 우연히 의존

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-08-11
- 담당: Claude Code
- 영향 영역: `workflow-source/tests/check_release_pipeline_version_flag.py`

- 작업 내용: CI `smoke` 가 **59f3365 (TASK-2026-08-10-main-019) 부터 red**
  (native·slash 양쪽, f4f7fc6 까지 green — 우리 리팩터링 커밋 이전부터).
  `check_release_pipeline_version_flag` test 3 (`test_version_default_pyproject`)
  이 `out["tag"]` 를 무조건 기대하는데, `dist/` 에 현재 버전 산출물이 없으면
  dry-run 은 `error: no dist files found ...` 를 반환하고 `tag` 키가 없다 →
  KeyError.
- 지금까지 CI 에서 통과한 이유: **`check_release_pipeline_phase3` 가 원본
  `dist/` 에 실빌드 산출물을 만들었고** (TASK-019 가 제거한 바로 그 저장소
  오염), 알파벳 순서상 먼저 돌아 version_flag 가 그 부산물을 우연히 받아먹었다.
  TASK-019 가 빌드를 사본으로 격리하자 CI 의 dist 부재가 드러났다.
  로컬은 실제 릴리스가 남긴 dist (gitignored) 가 있어 계속 green —
  로컬/CI 비대칭의 4번째 사례. "우연한 측정은 선언이 아니다" 패턴의 재현이자,
  **오염 제거는 그 오염에 기대던 소비자를 드러낸다** (만성 red fixture 패턴의
  변형).
- 같은 파일 test 2 는 이미 `has_staging` 분기로 dist 부재를 정당 상태로
  다루고 있다 — test 3 만 무조건 기대였다.

- 완료 기준: dist 유/무 양쪽에서 검사가 결정적으로 green (되주입 2방향 실증)
  + CI smoke green 복귀.

## 🛠️ Content

- test 3 에 test 2 와 같은 분기 도입: `has_dist` (dist/ 에 현재 버전 산출물
  존재) 일 때만 `tag == v{current}-beta` 를 기대하고, 부재 시 error 에 현재
  버전이 들어있는지 검증 (부재 = fresh CI checkout 의 정당 상태).
- 진단 재현: `dist/` 를 치우면 dry-run 이
  `error: no dist files found for version 1.1.6` + `tag` 키 부재 — CI 실패와
  동일. 복원 후 원상.

## ✅ Outcome

- dist 유/무 **양방향 3/3 PASS** 실증 (without-dist 는 CI 시뮬레이션).
- 타임라인 실측: smoke CI green 은 f4f7fc6 까지, **59f3365 부터 4연속 red**
  (native·slash 모두) — 전 세션 마지막 2 push 는 CI 확인 없이 종료됐고
  handoff 는 로컬 green 만 기록했다. push 후 CI 확인까지가 검증이다.
