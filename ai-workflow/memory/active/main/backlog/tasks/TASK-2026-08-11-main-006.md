---
id: TASK-2026-08-11-main-006
status: done
created_at: 2026-08-11
source_anchor: fix-task-2026-08-11-main-006
source_path: backlog/2026-08-11.md
kind: fix
---

# TASK-2026-08-11-main-006 — PERF-WF-04 벤치마크의 저장소 오염 제거 + sandbox 복사 race 내성

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-08-11
- 담당: Claude Code
- 영향 영역: `workflow_kit/common/contracts/baselines.py` (PERF-WF-04), `workflow-source/tests/_repo_sandbox.py`

- 작업 내용: TASK-002 검증 전량 실행에서 `check_bidir_link_v0_13_3` 이
  `shutil.Error: [Errno 2] ... tmp_audit_perf.log` 로 1회 fail (재실행 green —
  병렬 race). 원인 2겹:
  1. **PERF-WF-04 가 살아있는 저장소 루트에** `tmp_audit_perf.log` 를 100회
     append 하고 지운다 — PERF-WF-05 가 v1.0.0 에서 받은 "벤치마크는 temp 에서"
     처방을 **04 만 누락**. doctor/pre_check 를 부르는 검사가 병렬로 돌 때마다
     저장소에 임시 파일이 명멸한다 (건드렸다 되돌리기 오염의 잔존 사례).
  2. `_repo_sandbox.repo_sandbox` 의 `shutil.copytree` 가 스캔 시점에 있던
     파일이 복사 시점에 사라지면 통째로 실패 — 스냅샷 복사는 소멸 파일에
     내성이 있어야 한다.
- **부수 반성**: 이 실패에도 push 가 진행됐다 — 검증 체인을
  `run_all_checks | tail && git push` 로 묶어 **파이프라인 exit 이 tail 의 0**
  이 됐다. 게이트 명령은 파이프에 넣지 않는다 (또는 `set -o pipefail`).
  push 된 커밋 자체는 단독 재실행 green 으로 무결 확인.

- 완료 기준: PERF-WF-04 가 저장소 밖(temp)에서만 쓴다 (경로 포착 테스트로
  고정) + sandbox 복사가 소멸 파일에 내성 (결정적 재현 테스트) + 전량 green.

## 🛠️ Content

- `baselines.py` PERF-WF-04: 벤치마크 파일을 `tempfile.TemporaryDirectory` 로 —
  PERF-WF-05 의 v1.0.0 처방과 동일. cleanup 코드도 함께 제거 (temp 가 회수).
- `_repo_sandbox.py`: `copy_function=_copy_ignore_vanished` (FileNotFoundError
  건너뜀) + `shutil.Error` 를 `_vanished_only` 로 선별 재던짐 — **소멸(ENOENT)
  만 내성, 그 외 오류는 그대로 던진다** (조용한 쪽이 틀린 쪽).
- 테스트 2건: `check_baselines_compliance` 에 append 경로 포착 case (17번째),
  `check_parallel_smoke` 에 소멸/권한 주입 case 9 (`shutil.copy2` wrap 으로
  결정적 재현).

## ✅ Outcome

- 17/17 · 9/9 PASS, baselines mypy strict clean. **되주입 양방향 실증**:
  PERF-WF-04 를 project_root 로 되돌리면 경로 포착이 잡고 (FAIL 1), sandbox 를
  원래 copytree 로 되돌리면 case 9 가 잡는다 (8/9).
- 부수 교훈 기록: 검증 게이트 명령을 파이프에 넣으면 exit 이 tail 로 덮인다 —
  이번 flake 가 push 를 통과한 이유. 이후 체인은 pipefail 또는 단계 분리.
- push 된 35d2b27 은 단독 재실행 green + CI smoke success 로 무결 확인.
- **addendum (전량 검증이 잡은 후속 2건)**: (1) `check_docs` 의 스캔 제외에
  `.claude` 추가 — worktree 격리 에이전트(`.claude/worktrees/`)의 사본을
  문서 스캔이 크롤링해 상대 링크 오탐 (`.codex`/`.opencode` 는 이미 제외돼
  있었고 `.claude` 만 누락). (2) `check_run_all_checks` 의
  `test_run_one_baselines` 기대값 16→17 (본 task 가 추가한 case 반영 —
  정확값 고정 규약 유지). pipefail 이 실제로 push 를 막았다 (직전 교훈의
  첫 실전 검증).
