---
id: TASK-2026-08-11-main-007
status: done
created_at: 2026-08-11
source_anchor: cleanup-task-2026-08-11-main-007
source_path: backlog/2026-08-11.md
kind: cleanup
---

# TASK-2026-08-11-main-007 — release_pipeline.py 안전 부분 분할 (3908 → 3174줄 + 모듈 4개)

## 📝 Description

- 상태: done
- 우선순위: medium
- 요청일: 2026-08-11
- 담당: Claude Code
- 영향 영역: `workflow-source/tools/release_pipeline{,_changelog,_dist,_emit,_frontmatter}.py`

- 작업 내용: 3차 세션 조사 후보 4 ("위험도 높음 — 단독 task"). **분할 전
  가능성 분석을 먼저 수행** — 25개 검사가 이 파일의 소스 텍스트/AST 를 직접
  스캔하므로, 심볼 전수를 SOURCE-BOUND (문자열/AST/monkeypatch 로 이 파일에
  묶임 — 이동 불가) vs ATTR-ONLY (모듈 속성 접근만 — 재수출로 이동 가능) 로
  분류한 지도를 만들고, 그 지도의 안전 그룹만 추출했다.

- 완료 기준: 소스-스캔 검사 포함 관련 검사 전부 green (테스트 무수정) +
  재수출로 기존 속성 표면 완전 보존 + 전량 2-컨텍스트 green.

## 🛠️ Content

- 추출 4모듈 (이동 코드는 verbatim, 각 모듈 `__all__` 에 underscore 이름까지
  명시 — `import *` 는 기본으로 `_` 이름을 건너뛰므로):
  - `release_pipeline_changelog.py` (335줄): commit 수집/분류, `draft_changelog`,
    `_resolve_notes_file`, `find_dist_files` + 전용 정규식 상수.
  - `release_pipeline_dist.py` (163줄): build/twine/업로드 시뮬 6함수.
  - `release_pipeline_frontmatter.py` (178줄): release note frontmatter 파서 5함수.
  - `release_pipeline_emit.py` (187줄): dashboard/self-recovery/bidir-audit 포맷터.
- 배선: `spec_from_file_location` 로 package 없이 로드되므로 **상대 import 불가**
  — `sys.path.insert(0, tools/)` + `from release_pipeline_<x> import *` 4줄.
  신규 모듈은 release_pipeline 을 import 하지 않는다 (순환 0).
- **잔류 결정** (분석 지도 + 구현 중 확인): SOURCE-BOUND 전부 (`cmd_*` 전체,
  `main`, `cmd_validate`, `cmd_release`, `verify_release_note_smoke_count`,
  `_git_toplevel`/`_git_dirty_paths`, `cmd_self_recover`/`_self_recover_step`,
  `DRIFT_LEDGER_RELPATH`, `_append_drift_ledger_entry`) + 순환이 필요해지는
  2건 (`_emit_self_recovery_log`, `_emit_bidir_link_audit_log` — 잔류 심볼
  `read_version` 직접 호출). 시그니처 변경 0.

## ✅ Outcome

- 3908 → **3174줄** (−734). 모듈 4개 신설 (계 863줄).
- 격리 worktree 에서 구현·검증 후 반영: 소스-스캔 검사 포함 **관련 검사
  21종 전부 PASS, 테스트 수정 0**. `release_status.py` 의
  `from release_pipeline import ...` 경로도 재현 확인. 반영 후 본 트리에서
  재수출 표면 spot 검증 (36 이름) + 전량 2-컨텍스트 green.
- 남은 대형 파일 후보 (미착수): `dashboard_data.py` 2488줄,
  `workflow_kit_cli.py` 2095줄 — 같은 "분석 지도 먼저" 절차 권장.
