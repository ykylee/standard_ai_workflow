---
id: TASK-2026-08-11-main-008
status: done
created_at: 2026-08-11
source_anchor: fix-task-2026-08-11-main-008
source_path: backlog/2026-08-11.md
kind: fix
---

# TASK-2026-08-11-main-008 — 원본-무결성 관찰 검사 3건을 정숙 구간으로 (병렬 race 위양성 제거)

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-11
- Owner: Claude Code
- 영향 영역: `check_release_pipeline_version_auto_sync.py`, `check_self_recovering_v0_13_2.py`, `check_bidir_link_v0_13_3.py`

- Description: TASK-007 검증 전량 실행에서
  `check_release_pipeline_version_auto_sync` 가 "원본을 건드렸다:
  pyproject.toml" assert 로 1회 fail. 단독 재실행 green + 저장소 clean —
  병렬 중 어떤 검사의 transient write 와 race 한 위양성. 표적 부분집합 3회
  + 전량 재현 시도에서 **재현 실패** (희귀 race), watcher (pyproject md5
  폴링 + 프로세스 스냅샷) 로도 미포착.
- 처방: 이 3검사는 도구를 사본에서 돌리지만 **원본 무손상을 byte 대조로
  관찰**한다 — 저장소 전역 관찰 검사는 `REQUIRES_QUIET_REPO = True` 를
  선언해야 한다는 기존 규칙 (CLAUDE.md / TASK-018 §2.53) 의 적용 누락이었다.
  정숙 구간에서는 trip = 자기 sandbox 누수로 의미가 결정적이 된다.

- Completion criteria: 마커 3건 선언 + 정숙 분류 인식 (`check_parallel_smoke` green)
  + 3검사 단독 green + 전량 2-컨텍스트 green.

## 🛠️ Content

- 3파일에 `REQUIRES_QUIET_REPO = True` + 사유 docstring (race 실측 날짜와
  본 task ID 명시). 정숙 구간 3→6.
- 미해결로 남긴 것: **원본 pyproject 를 일시적으로 쓰는 병렬 검사의 정체**
  — 1회 관측 후 재현 실패. `check_no_repo_write` 의 "실행 후 복원" 계약
  한계 (§6 기존 리스크) 와 같은 뿌리로 추정. 재발 시 watcher 스크립트
  (`/home/yklee/tmp/watch_pyproject.sh` 패턴) 로 프로세스 스냅샷을 뜰 것.

## ✅ Outcome

- `check_parallel_smoke` 9/9 (정숙 분류 인식), 3검사 단독 green, 전량
  2-컨텍스트 green (push 게이트 통과).
- 정숙 구간 증가 비용: 3검사 합계 수 초 수준 — 위양성 flake 제거와 교환.
