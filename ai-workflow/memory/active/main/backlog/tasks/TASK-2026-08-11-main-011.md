---
id: TASK-2026-08-11-main-011
status: done
created_at: 2026-08-11
source_anchor: cleanup-task-2026-08-11-main-011
source_path: backlog/2026-08-11.md
kind: cleanup
---

# TASK-2026-08-11-main-011 — workflow_kit_cli.py 안전 부분 분할 (2095줄 → 목표 ~500줄 + 모듈 5개)

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-08-11
- Owner: Claude Code
- 영향 영역: `workflow_kit/workflow_kit_cli.py`, 신규 `cli_registry.py` + `cli_commands_{cache,memory,release,okf}.py`

- Description: 대형 파일 3번째, 같은 "분석 지도 먼저" 절차. 이 파일의 구조는
  argparse 가 아니라 **`@register` 데코레이터 레지스트리** (`COMMANDS` dict) —
  핸들러 38개가 함수-레벨 lazy import 위주라 핸들러 간 결합이 사실상 0.
- 분석이 확정한 제약:
  - **레지스트리 선행 분리 필수** (`cli_registry.py`): 신규 모듈이
    `workflow_kit_cli` 를 import 하면 순환 + `check_workflow_kit_cli` 의
    최상위-이름 exec 방식과 충돌해 `COMMANDS` 가 2개로 갈라진다.
  - SOURCE-BOUND 잔류: `cmd_release_create` / `cmd_release_status` (+ 각
    `@register` 리터럴) — 검사 4종이 소스 정규식으로 본문·docstring 을 본다.
  - `wk_main` / `run_workflow_kit_cli` / `__main__` 가드 / 모듈 docstring 잔류
    (`python -m` 계약 + `[project.scripts] wk`).
  - 신규 모듈 import 는 `_register_tool_commands()` 호출보다 **위**
    (`ALREADY_REGISTERED` 순서 계약, dispatcher case 4b).
  - 신규 모듈 위치는 `workflow_kit/` 최상위 (cli/ 하위는 packages 목록 밖 —
    wheel 누락). `workflow_kit/__init__.py` 의 동결 `__all__` 에 추가 금지.

- Completion criteria: mypy strict 전 패키지 0 오류 + `check_workflow_kit_cli` 53 /
  `check_wk_dispatcher` 10 포함 관련 검사 전부 green (테스트 무수정) + 전량
  2-컨텍스트 green.

## 🛠️ Content

- `cli_registry.py` (48줄) 선행 신설 — `COMMANDS`/`register`/`_parse_flag`/
  `_has_flag`/`_print_usage` verbatim 이동. 신규 핸들러 모듈은 registry 만
  import (순환 0, `check_workflow_kit_cli` 의 최상위-이름 재-exec 방식과도
  양립 — COMMANDS 가 캐시된 registry 에 살아서 재실행마다 완전한 dict).
- 핸들러 4모듈 추출 (verbatim): `cli_commands_cache` (619, 13핸들러) /
  `cli_commands_memory` (618, 6핸들러) / `cli_commands_release` (262,
  `_wrap_release_pipeline` + 7핸들러) / `cli_commands_okf` (216, 5핸들러).
- 잔류: `cmd_release_create`·`cmd_release_status` (검사 4종 소스-바인딩),
  dashboard/layer2/federate/score-wiki-trend/consumer-metrics, tool 지연 등록,
  라우터/`wk_main`/`__main__` 가드, 모듈 docstring (arg-surface 정본).
  신규 import 는 `_register_tool_commands()` 보다 위 (ALREADY_REGISTERED 순서).
- 재수출: 명시 from-import + `__all__` (underscore 포함) — mypy
  `no_implicit_reexport` + ruff F401 동시 충족. `workflow_kit/__init__.py`
  동결 `__all__` 은 무변경.

## ✅ Outcome

- 2095 → **583줄** (−1512). 모듈 5개 신설 (계 1763줄).
- 본 트리 재검증: mypy strict **137파일 0 오류** + `check_workflow_kit_cli`
  **53/53** + `check_wk_dispatcher` **10/10** (subprocess↔in-process 레지스트리
  동일성 case 6 포함) + entry points 32종 + release 검사 5종 green. 테스트
  수정 0. 격리 worktree 구현 후 반영, 전량 2-컨텍스트 게이트로 확정.
- 대형 파일 3건 분할 누적: release_pipeline −734 / dashboard_data −962 /
  workflow_kit_cli −1512 = **−3,208줄** (전부 verbatim 이동, 테스트 무수정).
