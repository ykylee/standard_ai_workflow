---
id: TASK-2026-08-11-main-013
status: done
created_at: 2026-08-11
source_anchor: fix-task-2026-08-11-main-013
source_path: backlog/2026-08-11.md
kind: fix
---

# TASK-2026-08-11-main-013 — transient pyproject writer 추적 (감시 체계 보강)

## 📝 Description

- Status: done
- Priority: low
- Request date: 2026-08-11
- Owner: Claude Code
- 영향 영역: `workflow-source/pyproject.toml`, `workflow-source/tests/` (감시 체계), handoff §6 리스크

- Description: 2026-08-11 병렬 전량 실행 중 원본 `pyproject.toml` 이 일시
  변경됐다 되돌아온 현상 (version_auto_sync byte-대조가 1회 포착, 재현 실패 —
  표적 3회 + 전량 2회 + 50ms md5 watcher 전부 미포착). 관찰자 3검사는
  TASK-008 정숙화로 위양성은 차단됐지만 **writer 정체는 미상**. 재발 시 즉시
  특정할 수 있도록 감시 체계를 보강한다: `/home/yklee/tmp/watch_pyproject.sh`
  패턴 (md5 폴링 + 프로세스 스냅샷) 을 저장소 내 도구로 승격하거나, 전량 실행
  래퍼에 opt-in 감시 훅을 추가. `check_no_repo_write` 의 "실행 후 복원" 계약
  한계 (되돌리는 것은 안 건드리는 것이 아니다) 와 같은 뿌리로 추정.

- Completion criteria: 재발 시 writer 프로세스를 특정할 수 있는 감시 수단이 저장소에
  고정되고 (일회용 조사는 조사가 아니다), 사용법이 handoff §6 리스크 항목에
  연결됨. writer 특정 자체는 재발 전까지 완료 조건이 아님.

## 🛠️ Content

- `workflow-source/tools/watch_transient_writer.py` 신설 — 일회용
  `~/tmp/watch_pyproject.sh` 의 승격판. md5 폴링 (기본 0.05s) 에 증거 수집을
  더했다: 이벤트별 **unified diff + 내용 스냅샷** (무엇을 고쳤는지가 writer 를
  가장 강하게 좁힌다) + **ps 전량 스냅샷** (용의자 패턴으로 필터하지 않는다 —
  포함 목록은 사각지대를 못 본다, 필터링은 사후에) + fuser (best-effort).
  소멸/재출현 (rename-swap 형 쓰기) 도 별도 이벤트. SIGKILL 대비 events.jsonl
  즉시 기록 + 종료 시 summary.json. 감시 대상 저장소 **안**에 로그를 두려 하면
  exit 2 거부 (`--allow-repo-log` 로만 해제) — 감시 도구가 저장소를 오염시키면
  관찰 자체가 다른 검사의 위양성이 된다. 기준 경로는 명시 인자 → cwd 기본
  2갈래 + 출처 보고 (audit_root_anchors R2 규칙 준수).
- `workflow-source/tests/check_watch_transient_writer.py` 5 case — transient
  되주입 (변경→복원 2건 + diff 에 marker) / 무변화 0건 + poll>0 (실행 못 한
  검사는 통과가 아니다) / 소멸·재출현 구분 / 저장소-안-로그 거부 계약 /
  조용한 실행 잔여물 0. temp 전용이라 `REQUIRES_QUIET_REPO` 불필요.
- **되주입 실증**: 도구의 변화 판정을 무력화하면 검사가 red (transient case
  실패), 복원하면 전량 green — 검사가 자기가 심은 가짜를 본다.
- 전량 실행이 잡은 후속 2건: `check_tempdir_leak_guard` 가 자동 로그 dir 의
  정리-없는 mkdtemp 를 지적 → **관측 0건 + 위치 미지정이면 자동 삭제** 로 해소
  (상시 감시가 temp 를 쌓지 않게 됨 — case 5 로 고정); smoke 파일 수 파생
  검사 2종 (`release_wrapper_args` case 9 / `smoke_trend_cross`) 이 248→249
  불일치를 지적 → Beta-v1.1.6.md 의 살아있는 누적 지표 갱신.

## ✅ Outcome

- 감시 수단 저장소 고정 완료 (일회용 조사는 조사가 아니다). smoke 248→249,
  mypy strict 신규 2파일 0 오류, ruff clean, 파생 수치 3문서
  (CODE_INDEX / INSTALLATION_AND_USAGE / Beta-v1.1.6 살아있는 지표) +
  CODE_INDEX 진입점 안내 동기, handoff §6 리스크 항목에 사용법 연결.
  writer 특정 자체는 재발 시의 일 (완료 조건 아님 — 계획대로).
