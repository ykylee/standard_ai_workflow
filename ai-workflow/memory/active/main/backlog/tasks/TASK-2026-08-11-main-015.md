---
id: TASK-2026-08-11-main-015
status: done
created_at: 2026-08-11
source_anchor: fix-task-2026-08-11-main-015
source_path: backlog/2026-08-11.md
kind: fix
---

# TASK-2026-08-11-main-015 — 로컬 병렬 전량 검사 TIMEOUT flake 2건 해소

## 📝 Description

- Status: done (2026-08-11)
- Priority: medium
- Request date: 2026-08-11
- Owner: Claude Code
- 영향 영역: `workflow-source/tests/run_all_checks.py` (per-check timeout),
  `check_release_summary_v0_11_15` / `check_release_status_auto_bump_v0_11_16`

- Description: 2026-08-11 전체 검토에서 로컬 `--branch-context=all` 병렬 전량이
  **각 축에서 1건씩 TIMEOUT** (60s 상한). native 축 `check_release_summary_v0_11_15`,
  slash 축 `check_release_status_auto_bump_v0_11_16` — **둘 다 단독 실행은 PASS**
  (28~31s, 상한의 절반). 12코어 `--jobs auto`(=10) 병렬 부하에서 2배로 늘어져
  상한 초과. CI 는 green — 로컬 전용 flake. 60s 근처의 느린 검사들 전반의
  timeout 여유 문제로 보고 처방 (per-check timeout 상향 / solo 실측 기반 여유
  계수 / 느린 검사 그룹 조정 중 택일).

- Completion criteria: 로컬 `--branch-context=all` 병렬 전량 반복 실행에서 TIMEOUT 0 +
  처방이 CI 소요를 악화시키지 않음 + 재발 방지 근거 기록.

## 🛠️ Content

- 위험군 확정 (부하 실측 ≥40s): `wiki_score` 57s / `release_status_v0_11_14` 55s /
  `release_status_auto_bump_v0_11_16` 55s (flaked) / `release_summary_v0_11_15`
  53s (flaked) / `release_pipeline_lib` 44s / `mypy_config_actually_loaded` 43s.
- 처방: **`CHECK_TIMEOUT_S` 파일 안 선언** (REQUIRES_QUIET_REPO 와 같은 패턴 —
  목록은 파일에서 멀어지면 드리프트한다). runner 가 AST 로 읽어 CLI `--timeout`
  과 **max** 로 합침 (선언은 상한을 늘릴 수만 있다 — 행 검출 유지). 위험군
  6파일에 `CHECK_TIMEOUT_S = 150` 선언. 마커 스캔은 lru_cache 로 1회 parse
  (`_scan_markers` 가 quiet/timeout 동시 추출).
- 계약 고정: `check_parallel_smoke` case 10 — 선언(30s)한 3s check 는
  `--timeout=1` 에 생존, 무선언은 TIMEOUT (**양방향**) + decoy (주석/문자열/
  음수) 불인정 + max 의미론 (선언이 CLI 값을 줄이지 못함).
- 선행 결함 수정: `--tests-dir` 가 저장소 밖이면 `relative_to(SOURCE_ROOT)` 가
  ValueError — 표시용 경로라 절대 경로 fallback.
- 규약 문서화: CLAUDE.md 실행 기본값 절에 선언 규약 추가 (solo ~25s+ 검사).
- 부수 기록: 이 flake 는 첫 실행에서 `| tail` 파이프가 exit 1 을 가려 놓칠
  뻔했다 — "게이트 명령은 파이프에 넣지 않는다" (TASK-006 교훈) 재확인.

## ✅ Outcome

- **로컬 `--branch-context=all` 병렬 전량 2회 반복 = 4패스 전부 249/249,
  TIMEOUT 0** (완료 기준 충족, 파이프 없이 exit 코드 개별 확인).
- `check_parallel_smoke` 10/10, `check_run_all_checks` 11/11,
  `check_standard_single_source` 7/7 green.
- CI 소요 영향 0 — 선언은 상한만 늘리고 통과 검사의 실행 시간은 불변.
