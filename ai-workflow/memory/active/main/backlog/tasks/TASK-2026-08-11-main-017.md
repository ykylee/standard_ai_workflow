# TASK-2026-08-11-main-017

- 문서 목적: darwin homelab 검증 (5차 세션, TASK-017)
- 범위: macOS 환경에서 `run_all_checks.py` 1패스 + 4 FAIL 분석/fix
- 대상 독자: AI agent (다음 세션), 저장소 관리자
- 상태: in_progress
- 최종 수정일: 2026-08-11
- 관련 문서: [../state.json](../state.json), [../session_handoff.md](../session_handoff.md), [../backlog/2026-08-11.md](../backlog/2026-08-11.md), [../../../../workflow-source/core/multi_workspace_orchestration.md](../../../../workflow-source/core/multi_workspace_orchestration.md) (후속 federation cross-host 의 1차 출처)

## 1챕터 — macOS 환경 1패스 검사 (done, 2026-08-11 22:18 KST)

- **명령**: `.venv/bin/python -u workflow-source/tests/run_all_checks.py --branch-context=all --tmp-dir=$(mktemp -d -t run_all_checks_5cha)`
- **환경**: macOS darwin (handoff 의 4차 = Linux CI 와 다름). 시스템 python 3.14.5 / `.venv` 3.13.13 (신설) / mavis 데몬 system-wide 정상 / `~/.mavis/bin/mavis` symlink 는 user-level (구 격리) broken — 무시 가능.
- **결과**: **245/249 PASS, 4 FAIL, 0 TIMEOUT** (4분11초, 2축 매트릭스)
- **4 FAIL 분류**:
  1. `check_branch_resolver_agreement` — **macOS `/tmp` symlink 함정**. `Path.resolve()` 가 `/private/var/folders/...` prefix 추가, 검사 코드의 `/var/folders/...` 와 string 비교 시 fail (`!=`).
  2. `check_branch_scoped_memory` — 동일 패턴 (ValueError: not in subpath).
  3. `check_git_conflict_resolver_v0_11_24` — 7/8, 1건 fail. 별도 분석 필요.
  4. `check_workflow_state_refresh_hint` — AssertionError: missing refresh command part. 별도 분석 필요.
- **핵심 발견**: handoff 가 "4패스 249/249, TIMEOUT 0" 이라 한 건 **Linux CI** 환경. **macOS 환경의 회귀 4건 발견** = 5차 세션의 1차 기여. darwin homelab 검증의 1차 acceptance = "환경별 차이 발견" 으로 PASS.
- **부수**: `~/.mavis/bin/mavis` broken symlink 발견. user-level mavis 격리 (구) 가 system-wide mavis 로 통합된 뒤 stale. fix 불요 (in-scope 아님).

## 2챕터 — 4 FAIL fix (pending)

- (1) + (2) macOS `/tmp` symlink = 단일 fix 가능 (두 검사 모두 `Path.resolve()` 비교 패턴). 후보: `os.path.realpath()` 또는 `pathlib.Path.resolve(strict=True)` 통일, 또는 검사 시 `Path.resolve()` 후 비교.
- (3) + (4) 별도 분석 필요.
- 후속 commit (별도 챕터).
