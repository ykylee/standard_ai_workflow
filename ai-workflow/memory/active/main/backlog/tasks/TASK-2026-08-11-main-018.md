---
id: TASK-2026-08-11-main-018
status: done
created_at: 2026-08-11
source_anchor: feature-task-2026-08-11-main-018
source_path: backlog/2026-08-11.md
kind: feature
---

# TASK-2026-08-11-main-018 — state.json 을 생성물로 전환

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-08-11
- 담당: Claude Code
- 영향 영역: `ai-workflow/memory/active/main/state.json`,
  `workflow-source/scripts/generate_workflow_state.py`,
  `workflow-source/workflow_kit/common/state/builder.py` (읽기),
  `workflow-source/tools/session_start.py`, `workflow-source/tools/refresh_state.py` (신설),
  `CLAUDE.md` / `docs/PROJECT_PROFILE.md` (절차 규약), 신설 검사 1건

- 작업 내용: TASK-017 4챕터에서 `state.json` 이 **정본 생성기 출력과 갈라져** 있었다.
  기계 판독 필드는 그때 정합시켰으나 **어느 쪽이 정본인지는 여전히 미선언**이라,
  다음 사람이 같은 자리를 다시 밟는다. 저장소는 이미 코드 주석으로 답을 적어 뒀다 —
  `workflow_writes.py:320` "이 목록은 **파생물**이다. SSOT 는 `backlog/tasks/` 이고
  state.json 의 `recent_done_items` 도 같은 상한으로 잘린다." 그 선언을 문서와
  절차와 검사에 반영한다.

- **드리프트가 쌓인 진짜 원인**: 세션 종료 절차에 생성기를 부르는 단계가 **없다**.
  `refresh-purpose --apply` 는 `purpose_digest` 만 건드리고, 전체 재생성은
  `generate_workflow_state.py` 스크립트뿐인데 아무도 호출하지 않는다. 선언만 하고
  절차를 안 고치면 드리프트는 그대로 쌓인다.

- **범위 확장 (2026-08-11, 소유자 지시)**: 6차 세션 리뷰에서 발견한 `wk` 소비 결함
  2건을 같이 처리한다 —
  1. `wk session-start` 가 backlog **인덱스 문서**를 전제 (`find_latest_backlog_path`
     가 인덱스의 마지막 링크를 최신 backlog 로 판정) — branch-scoped 레이아웃
     (인덱스 없음, `backlog/YYYY-MM-DD.md` daily) 에서 task 상세 파일을 최신
     backlog 로 오판한다.
  2. `wk session-start` 무인자 호출 불가 (필수 경로 3개) — 정본 §11 이 안내하는
     `wk session-start` 를 소비자가 그대로 실행할 수 없다. workspace 자동 탐색
     기본값을 만든다.

- 완료 기준:
  1. `state.json` 이 생성물임이 문서에 **명시**된다 (손 편집 금지 + 재생성 명령).
  2. 세션 종료 절차에 생성기 호출이 들어간다 (`wk refresh-state` 노출).
  3. `state.json` 이 생성기 출력과 갈라지면 **검사가 잡는다** (되주입으로 실증).
  4. **handoff 형식이 생성기 입력**이라는 사실이 문서화된다 — 이게 없으면 handoff 를
     자유롭게 쓰는 순간 state.json 이 조용히 다시 오염된다 (TASK-017 §2 실측).
  5. 확장 결함 2건 해소: `wk session-start` 무인자 동작 + branch-scoped daily
     backlog 정탐.
  6. 전량 2축 green.

## 🛠️ Content

- 진행 현황: 리뷰 3종(하네스 파생본 / 스킬·CLI / MCP) 완료. 범위 확장 —
  `wk session-start` 무인자 자동 탐색 + branch-scoped latest backlog 해석 +
  `wk refresh-state` 노출 + §11 정본 갱신 + drift 검사.
- 진행 중 실측 2건 (별도 결함 후보로 기록):
  - `wk backlog-update --mode update` 가 task 파일을 인자 기준으로 **전체
    재생성**하며 미지정 필드(작업 내용·완료 기준·담당·kind)를 삭제했다 — 원문 복원함.
  - 같은 실행이 handoff `in_progress` 목록에 기존 항목과 표기만 다른 **중복
    bullet** 을 추가했다 (task ID 기준 dedupe 부재).

## ✅ Outcome

- 작업 결과: 완료 기준 6개 전부 충족. (1) 정본 §11.1 에 `wk refresh-state` 행 +
  §11.2 에 생성물 선언·생성기 입력 선언 2 bullet 신설, 배포 사본·스냅샷·CLAUDE.md
  동시 갱신. (2) `wk refresh-state` 신설 (재생성 + `--check` drift 판정, wk 72명령).
  (3) `check_state_json_generated` 6 case — 되주입 양방향 + 자기 적용(case 5) +
  선언↔창구 정합(case 6), REQUIRES_QUIET_REPO. (4) handoff=생성기 입력 문서화.
  (5) `wk session-start` 무인자 자동 탐색 + branch-scoped daily 관측 (부수 수정:
  args 경유 TypeError, self-bootstrap 의 skills/ 경로 → wk). (6) 전량 2축
  **250/250 ×2 green** (venv, 실디스크 tmp). smoke 249→250, 파생 문서 3곳 갱신.
- 후속 작업: `wk backlog-update` update 모드의 파괴적 재생성 + handoff 중복 bullet
  (세션 기록 §3), 리뷰 3종 결함 목록 (세션 기록 §4 — readOnlyHint 허위·goose hook
  깨진 경로·§11.1 손 사본 7곳·SKILL.md 미갱신 등).
