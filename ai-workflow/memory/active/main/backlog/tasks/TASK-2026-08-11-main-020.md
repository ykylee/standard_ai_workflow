---
id: TASK-2026-08-11-main-020
status: in_progress
created_at: 2026-08-11
source_anchor: fix-task-2026-08-11-main-020
source_path: backlog/2026-08-11.md
kind: fix
---

# TASK-2026-08-11-main-020 — 하네스 진입점이 kit 스크립트를 안 가리킨다

## 📝 Description

- 상태: in_progress
- 우선순위: high
- 요청일: 2026-08-11
- 담당: Claude Code
- 영향 영역: `workflow-source/scripts/bootstrap_lib/harnesses/renderers.py` (렌더러 32개),
  `workflow-source/core/global_workflow_standard.md` (정본),
  `workflow-source/workflow_kit/common/standard_rules.py` (추출·렌더),
  `workflow-source/tests/check_standard_single_source.py` (강제),
  bootstrap/renderer 검증 검사 약 12건

- 발견 경로: TASK-018 검토 중. "backlog/handoff 를 에이전트가 쓴다" 는 전제를 확인하다
  **왜** 그렇게 되는지가 나왔다 — kit 의 `skills/backlog-update/SKILL.md` 는
  `run_backlog_update.py` 를 4번 안내하는데, 이 저장소에 설치된 진입점
  `.claude/commands/workflow-backlog-update.md` 는 **0번** 언급한다. 산문 절차만 있다.
  그래서 `workflow_writes.py` 에 코드로 박힌 계약(빈 목록은 빈 bullet `-`, 상한 10,
  index block 모양)이 **한 번도 적용되지 않는다**. 실행되지 않는 코드 안의 계약은
  계약이 아니라 주석이다.

## 🛠️ Content — 전수검사 (2026-08-11)

렌더러 32개 전수:

| 구분 | 수 | 내용 |
|---|---|---|
| ❌ 결함 | **26** | 메모리 문서 갱신을 지시하면서 스크립트를 안 가리킴 |
| ✅ 정상 | 1 | `render_goose_config` — goose 설정 형식이 command 문자열을 **요구해서** 어쩔 수 없이 옳게 됨 |
| — 해당없음 | 6 | worker 페르소나 / 설정 예시 (메모리 문서 미언급) |

- 지시 밀도 상위: `grok_build_skill` 22회 · `codewhale_skill` 18회 ·
  `claude_code_session_start_command` 15회 · `grok_build_agents` 14회 ·
  `minimax_agents` 13회 · `claude_code_agents` 13회.
  **가장 강하게 지시하는 진입점일수록 방법을 안 알려준다.**
- 정상 1개가 설계 의도가 아니라 **형식 강제의 부산물**이라는 점이 핵심이다.
  규약이 사람 문장에만 있으면 12개 하네스 중 11개에서 샌다.

**주입점 분포**

- **이미 `render_entrypoint_rules()` 주입 있음 (9)** — 정본 블록만 늘리면 자동 적용:
  `gemini_cli_agents` / `antigravity_agents` / `minimax_agents` / `codex_agents` /
  `opencode_skill` / `claude_code_agents` / `claude_code_skill` / `aider_conventions` /
  `grok_build_agents`
- **주입점 없음 (17)** — 주입 코드 추가 필요. 1순위(에이전트가 실제로 읽는 진입점):
  `claude_code_session_start_command` / `claude_code_backlog_update_command` /
  `claude_code_doc_sync_command` / `grok_build_skill` / `codewhale_skill` /
  `custom_skill_template` / `pi_dev_agents` / `minimax_orchestrator` / `opencode_agent`.
  2순위(설정 예시·worker 페르소나): 나머지 8개.

## 정정된 진단 — 파생 경로가 아예 없다 (2026-08-11)

첫 처방("진입점이 스크립트를 가리키게 한다")은 **소비자 프로젝트에서 성립하지 않는다.**
배포물을 전수 확인한 결과:

| 층 | 배포 | 실태 |
|---|---|---|
| 규약 `global_workflow_standard.md` | ✅ 진입점에 주입 | 정본·추출·강제 모두 있음 |
| 구현 `workflow_kit` 패키지 (`workflow_writes.py`) | ✅ pip | **계약이 여기 산다** |
| skill 스크립트 (`run_backlog_update.py` 등) | ❌ | 패키지에도 번들에도 없음. `pyproject.toml:155-160` 주석은 "bootstrap 이 복사한다" 고 적었지만 **bootstrap 은 복사하지 않는다** (`export_harness_package` 번들 목록에도 없음) |
| `wk` CLI | ✅ | 68개 명령 중 backlog/session/doc-sync **0개** |

- 따라서 `render_goose_config` 가 emit 하는
  `ai-workflow/skills/backlog-update/scripts/run_backlog_update.py` 는
  **부트스트랩된 프로젝트에 존재하지 않는 경로**다. 유일한 '정상' 렌더러조차 깨져 있었다.
- **이건 하네스 26개의 문서 결함이 아니라, 소비자에게 실행 가능한 경로가 처음부터
  없는 것이다.** 에이전트가 손으로 쓰는 것은 규율 부족이 아니라 **다른 선택지가 없어서**다.

## 아키텍처 결정 (소유자 방침, 2026-08-11)

**정본 하나 + 하네스별 파생본.**

- **정본(규약)** = `core/global_workflow_standard.md`
- **정본(구현)** = `workflow_kit` 패키지
- **노출 창구 하나** = `wk` CLI (`wk session-start` / `wk backlog-update` / `wk doc-sync`)
- **하네스 파생본** = 각 형식으로 **같은 `wk` 명령**을 렌더 (goose=command 문자열,
  claude-code=command md, codex/gemini=AGENTS.md 문단, …)
- **강제** = `check_standard_single_source` 확장 + 신규 검사

skill 스크립트는 CLI 핸들러의 얇은 wrapper 로 남거나 흡수된다 — 배포 안 되는 자산에
로직이 사는 상태가 해소된다.

## 후속 분해

- **TASK-021** — `wk` 에 3개 명령 노출 (skill 로직 → CLI 핸들러). **선행**: 없는 명령을
  가리킬 수는 없다.
- **TASK-022** — 하네스 파생본 통일 (정본 블록 신설 + 전 렌더러 주입 + 검사 강제 +
  goose 깨진 경로 수정 + `pyproject` 주석의 거짓 전제 정리).
- **TASK-018** — state.json 생성물 전환 (021·022 가 서면 자연히 닫힌다).

- 본 task 의 완료 기준: 진단·전수검사·아키텍처 결정이 기록되고 후속 task 로 분해될 것.
