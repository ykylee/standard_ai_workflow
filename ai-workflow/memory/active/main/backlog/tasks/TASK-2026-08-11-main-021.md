---
id: TASK-2026-08-11-main-021
status: done
created_at: 2026-08-11
source_anchor: feature-task-2026-08-11-main-021
source_path: backlog/2026-08-11.md
kind: feature
---

# TASK-2026-08-11-main-021 — `wk` 에 session-start / backlog-update / doc-sync 노출

## 📝 Description

- Status: done (2026-08-11)
- Priority: high
- Request date: 2026-08-11
- Owner: Claude Code
- 영향 영역: `workflow-source/workflow_kit/` (신규 CLI 핸들러),
  `workflow_kit/common/tool_dispatch.py` (TOOL_MODULES 정본),
  `workflow-source/pyproject.toml` (`[project.scripts]` 목록),
  `workflow-source/skills/*/scripts/run_*.py` (wrapper 로 축소),
  `tests/check_wk_dispatcher.py` 외 CLI 검사

- 배경: TASK-020 진단 — skill 스크립트는 pip 패키지에도 bootstrap 번들에도 없어서
  **소비자 프로젝트에 실행 가능한 경로가 없다**. `wk` CLI 는 배포되지만 68개 명령 중
  이 3개 기능이 없다. 그래서 모든 하네스에서 에이전트가 메모리 문서를 손으로 쓰고,
  `workflow_writes.py` 의 계약이 한 번도 적용되지 않는다.

- Description: skill 스크립트의 로직을 CLI 핸들러로 올려 `wk` 한 창구로 노출한다.
  스크립트는 같은 핸들러를 부르는 얇은 wrapper 로 남긴다 (kit 저장소 안에서의
  기존 사용 경로를 깨지 않기 위해).

- 설계 요점:
  - 정본은 `tool_dispatch.py` 의 `TOOL_MODULES` — `[project.scripts]` 목록과 갈라지면
    `check_wk_dispatcher.py` 가 red 를 낸다 (기존 계약, 그대로 따른다).
  - 기존 dispatcher 를 **확장**한다. 새 dispatcher 를 만들지 않는다 — 저장소 교훈
    "진입점이 둘로 갈리면 `--help` 도 갈린다".
  - `--apply` 기본값은 dry-run 쪽으로 (v1.1.2 §2.3 "도구는 판정만 한다" 정합).

- Completion criteria: `wk session-start` / `wk backlog-update` / `wk doc-sync` 가 소비자
  설치본(venv)에서 동작 + skill 스크립트가 같은 핸들러 경유 + dispatcher/CLI 검사 green +
  전량 2축 green.

## 🛠️ Content

**이동 (git mv, verbatim + 경로 부트스트랩 1줄만 수정)**

| 원 위치 | 새 위치 | 줄수 | 노출 |
|---|---|---|---|
| `skills/doc-sync/scripts/run_doc_sync.py` | `tools/doc_sync.py` | 369 | `wk doc-sync` |
| `skills/session-start/scripts/run_session_start.py` | `tools/session_start.py` | 433 | `wk session-start` |
| `skills/backlog-update/scripts/run_backlog_update.py` | `tools/backlog_update.py` | 759 | `wk backlog-update` |

- `parents[4]` → `parents[2]` 한 줄만 바뀐다 (skills/<n>/scripts/ 는 4단, tools/ 는 2단).
- 원 경로에는 **얇은 wrapper** 만 남긴다 — 로직을 두 벌로 만들지 않는다.
- `tool_dispatch.TOOL_MODULES` + `[project.scripts]` 양쪽 등록. 갈라지면
  `check_wk_dispatcher` 가 red (기존 계약 그대로).
- 기존 dispatcher 확장, 새 dispatcher 금지 — "진입점이 둘로 갈리면 `--help` 도 갈린다".
- **`wk` 명령 68 → 71개.**

**이동이 깬 것 (전량 검증이 잡음, 3종)**

사전 분류에서 `read_text` 만 grep 해 **모듈 로드**와 **소스 문자열 스캔**을 놓쳤다.
"내용을 읽는 검사 0건" 은 틀린 보고였고, 전량 2축이 그것을 잡았다:

| 검사 | 원인 | 처방 |
|---|---|---|
| `check_branch_scoped_memory` | 스크립트를 모듈 로드해 `branch_slug` 참조 — wrapper 엔 없다 | 검사가 **구현체**(`tools/backlog_update.py`)를 보게 |
| `check_maturity_registry` | stable 승격 기준이 `skills/.../run_*.py` 안의 `add_argument` 리터럴을 봄 | 구현이 `tools/` 로 갔으면 거기서도 인정 — 기준의 뜻은 "파라미터 받는 CLI 가 있는가" 이지 "특정 파일에 리터럴이 있는가" 가 아니다 |
| `check_migration_group_heading` | TASK-020 문서의 `### 주입점 분포` 가 legacy heading 유출로 검출 | 굵은 글씨로 교체 (검사가 옳고 내 문서가 규약 위반) |

- 교훈: **이동의 참조 지도를 뜰 때 "파일을 읽는가" 만 보면 부족하다.** 모듈 로드
  (`spec_from_file_location`) 와 소스 문자열 스캔이 같은 결합이다.

**검증**: 정숙 저장소 전량 2축 **249/249, FAIL 0, TIMEOUT 0** (EXIT=0, 파이프 없이 수신).
`wk session-start` / `wk backlog-update` / `wk doc-sync` 3개 동작 확인.

## ✅ Outcome

**소비자 프로젝트에 실행 가능한 경로가 생겼다.** 지금까지는 `pip install` 을 해도 이
세 기능을 부를 방법이 없었고(그래서 모든 하네스에서 에이전트가 메모리 문서를 손으로
썼고 `workflow_writes.py` 의 계약이 한 번도 적용되지 않았다), 이제 `wk` 한 창구로
배포된다. TASK-022 가 가리킬 대상이 확보됐다.
