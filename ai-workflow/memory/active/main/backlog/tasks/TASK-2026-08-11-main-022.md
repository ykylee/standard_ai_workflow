---
id: TASK-2026-08-11-main-022
status: done
created_at: 2026-08-11
source_anchor: fix-task-2026-08-11-main-022
source_path: backlog/2026-08-11.md
kind: fix
---

# TASK-2026-08-11-main-022 — 하네스 파생본 통일 (정본 블록 + 전 렌더러 주입)

## 📝 Description

- Status: done (2026-08-12)
- Priority: high
- Request date: 2026-08-11
- Owner: Claude Code
- 선행: TASK-2026-08-11-main-021 (`wk` 노출) — 없는 명령을 가리킬 수는 없다.
- 영향 영역: `workflow-source/core/global_workflow_standard.md` (정본),
  `workflow_kit/common/standard_rules.py` (추출·렌더),
  `scripts/bootstrap_lib/harnesses/renderers.py` (렌더러 32개),
  `tests/check_standard_single_source.py` + bootstrap/renderer 검증 검사 약 12건,
  `workflow-source/pyproject.toml` (주석의 거짓 전제)

- 배경: TASK-020 전수검사 — 렌더러 32개 중 **26개**가 메모리 문서 갱신을 지시하면서
  방법을 안 알려준다. 유일한 '정상' `render_goose_config` 조차 **존재하지 않는 경로**
  (`ai-workflow/skills/.../run_*.py`) 를 가리킨다.

- Description: 손으로 26개를 고치지 않는다 — 같은 드리프트가 다시 난다. 이 저장소가
  이미 쓰는 단일정본 주입 패턴에 얹는다 (`global_workflow_standard.md` §1·§3·§8 →
  `render_entrypoint_rules()` → 진입점 블록 → `check_standard_single_source.py` 강제).

  **두 번째 생성 블록**을 정본에 신설하고 같은 경로로 주입:
  1. **메모리 갱신 경로** — `wk session-start` / `wk backlog-update` / `wk doc-sync`
  2. **파싱 계약** (TASK-018 과 공유) — 빈 목록은 빈 bullet `-` / handoff §4 는
     `TASK-` 시작·10건 이하 / `status` 는 `planned|in_progress|blocked|done`

- 주입점 분포 (TASK-020 실측):
  - 기존 `render_entrypoint_rules()` 보유 **9개** — 블록만 늘리면 자동 적용
  - 미보유 **17개** — 1순위 9 (`claude_code_*_command` 3 / `grok_build_skill` /
    `codewhale_skill` / `custom_skill_template` / `pi_dev_agents` /
    `minimax_orchestrator` / `opencode_agent`), 2순위 8 (설정 예시·worker 페르소나)

- 부수 정리: `render_goose_config` 의 깨진 경로 교체, `pyproject.toml:155-160` 의
  "bootstrap 이 skills 를 복사한다" 는 거짓 전제 정정.

- Completion criteria: 정본 블록 신설 + 렌더 + 1순위 주입점 확보 + 검사가 누락을 잡음(되주입
  실증) + 이 저장소 자기 진입점 재생성(self-application) + 전량 2축 green.

## 🛠️ Content

**정본 §11 신설** (`core/global_workflow_standard.md`)

- §11.1 갱신 명령 표 — `wk session-start` / `wk backlog-update` / `wk doc-sync`
- §11.2 파싱 계약 3줄, **"도구를 쓰지 않고 손으로 쓸 때도 지켜야 한다"** 로 명시.
  도구 경로가 생겨도 에이전트는 계속 손으로 쓴다 — 그때가 사고 지점이다.
- 근거 문장에 실측을 넣었다: handoff 빈 목록에 산문을 써 두었더니 `state.json` 이
  그것을 작업 항목으로 읽었고 **아무 검사도 오류로 보지 않았다** (TASK-017 4챕터).

**추출·렌더·강제** — 정본 한 곳 → 전 하네스

- `StandardRules` 에 `memory_commands` / `parse_contract` 추가, §11 표·bullet 파싱
- `render_entrypoint_rules()` 가 `## 메모리 갱신 경로` 블록을 덧붙임
- 마커 `§1 · §3 · §8` → `§1 · §3 · §8 · §11`, 스냅샷 재생성
- `check_standard_single_source` 확장 — 진입점에 §11 명령·계약이 없으면 누락으로 잡는다

**결과: 결함 26 → 14**

- 주요 진입점 8개(`PRIMARY_ENTRYPOINTS`)는 기존 주입점을 타고 **자동 적용**
- `.claude/commands/*.md` 3개에 실행 경로 직접 주입 — **이번 조사의 출발점**이던 파일들
  (세션 시작 때 읽은 `/workflow-backlog-update` 가 스크립트를 0번 언급하던 그것)
- 자기 적용: 이 저장소 `CLAUDE.md` + `.claude/commands/*` 3개를 자기 렌더러 출력으로 재생성

**커밋 전에 잡힌 결함 (FAIL 10건, 단일 뿌리)**

`StandardRules` 에 필드를 추가하면서 **스냅샷 fallback 경로의 생성자 호출**을 놓쳤다
(`standard_rules.py:225`). 정본 파일이 없는 환경(wheel 설치처럼 `core/` 가 함께
배포되지 않을 때)을 위한 경로라 **이 저장소에서 평범하게 부르면 실행되지 않는다** —
mypy strict 가 아니었으면 배포 환경에서만 터졌을 결함이다. `render_snapshot_module`
에 두 상수 출력 추가 + 스냅샷 재생성 + 생성자 인자 전달로 해소.

- 교훈: **정본·추출기·스냅샷은 한 커밋 안에서 같이 움직인다.** 절반만 옮기면
  "정본이 없는 환경" 이라는, 평소에 안 밟는 경로에서만 깨진다.

**검증**: mypy strict 137파일 0 오류 + 정숙 저장소 전량 2축 **249/249, FAIL 0**.

## ✅ Outcome

정본 한 곳을 늘리고 주입점을 넓히는 방식이라, **앞으로 하네스가 추가돼도 같은 결함이
재발하지 않는다** — 새 하네스의 진입점이 §11 을 안 실으면 `check_standard_single_source`
가 잡는다.

**잔여 (기록)**: 확장한 검사는 `PRIMARY_ENTRYPOINTS` 8개만 강제한다. 남은 렌더러
14개(설정 예시·worker 페르소나, 그리고 검사가 이미 면제 사유를 명시한
`codewhale`/`custom`/`pi-dev`)는 검사 밖이라 드리프트 가능성이 남는다. 후속에서
`EXEMPT_HARNESSES` 처럼 **사유를 남기는 방식**으로 좁히는 것이 정공법이다 —
조용히 빠지는 경로를 두지 않는다.
