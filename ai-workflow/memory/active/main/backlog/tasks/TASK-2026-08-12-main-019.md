---
id: TASK-2026-08-12-main-019
status: done
created_at: 2026-08-12
source_anchor: generic-task-2026-08-12-main-019
source_path: backlog/2026-08-12.md
kind: generic
---

# TASK-2026-08-12-main-019 — CLAUDE.md install 기본값이 macOS homebrew 에서 PEP 668 로 실패

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-08-12
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: CLAUDE.md '프로젝트 실행 기본값' 의 install 명령이 시스템 python3 (homebrew 3.14.5) 에서 externally-managed-environment 로 거부된다. 문서의 기본값이 이 호스트에서 그대로는 안 돈다.
- Completion criteria: CLAUDE.md install 줄이 venv 전제(또는 uv/pipx)를 명시하고, macOS homebrew 환경에서 그대로 복사해 실행 가능

## 🛠️ Implementation / Content

- Progress: 완료 (2026-08-14, 45차).
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: CLAUDE.md 실행 기본값 5개(install/run/quick/isolated/smoke) + SDK·브랜치 매트릭스 예시를 전부 .venv/bin/python3 형태로 교체 — 활성화 없이 복사해 실행 가능. install 은 python3 -m venv .venv 생성을 앞에 붙였다.
- Result: 왜 venv 전제가 문서 첫 줄인가: PEP 668 거부만이 아니라, 시스템 python3 로 검사를 돌리면 dev 의존성 부재로 무더기 오탐이 난다 — 2026-08-14 이 세션에서 homebrew python3 로 전량 2축을 돌려 9건 red(전부 미설치, 코드 결함 0)를 실제로 겪었다.
- Result: 부수 발견: 이 호스트의 기존 .venv 는 uv 계열이라 pip 이 없었다(No module named pip). ensurepip --upgrade 로 채웠고 그 함정도 문서에 한 줄 남겼다.
- Verification: 문서 install 줄을 이 호스트(macOS homebrew)에서 그대로 실행해 실증 — venv 생성 idempotent + pip install dry-run rc=0 ×2, run 명령 실측 rc=0. self-application 8/8 · standard_single_source 9/9
- Follow-up: 없음
