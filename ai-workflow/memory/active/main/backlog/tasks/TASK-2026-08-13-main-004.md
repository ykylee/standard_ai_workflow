---
id: TASK-2026-08-13-main-004
status: in_progress
created_at: 2026-08-13
source_anchor: generic-task-2026-08-13-main-004
source_path: backlog/2026-08-13.md
kind: generic
---

# TASK-2026-08-13-main-004 — CI native 셀 mypy 게이트 flake — cmd_validate mypy 전역 스캔의 병렬 race 판정

## 📝 Description

- Status: in_progress
- Priority: high
- Request date: 2026-08-13
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/tools/release_pipeline.py`
  - `.github/workflows/smoke.yml`
  - `workflow-source/tests/run_all_checks.py`
  - `workflow-source/tests/check_release_pipeline_lib.py`

- Description: 2026-08-13 run 31657948983 native 셀에서 check_mypy_strict_release_gate 가 mypy exit_code 2 로 1회 FAIL, 같은 커밋 slash 셀·재실행은 green (.py 변경 없는 커밋). exit 2 는 mypy blocking/config 오류 코드 — 병렬 구간의 transient write 와 mypy 저장소 스캔 race 추정. 검사에 REQUIRES_QUIET_REPO 선언 또는 mypy 대상 격리(sandbox) 검토. 재발 시 이 task 에 기록 누적
- Completion criteria: [개정] 다음 재발이 mypy 트레이스백을 CI 로그(step summary)에 남긴다 — 그래야 상류 보고/우회 판단이 선다. 'N run 연속 green' 은 원인을 모르던 시점의 기준이라 폐기

## 🛠️ Implementation / Content

- Progress: ⑤ **flake 확정.** 같은 커밋(`b119d68b`) 재실행이 native 셀까지 green — 3차와 동일한
서명(같은 커밋에서 재실행 green)이 재확인됐다. 내 변경이 원인이 아니다.

⑥ **수리: `--show-traceback` 을 게이트 invocation 에 추가.** mypy 는 내부 오류일
때만 트레이스백을 찍으므로 정상 경로 비용이 0 이다(게이트 실측: `ok=True`,
`exit 0`, `stderr_tail=None`). 그물 2건을 `check_mypy_strict_release_gate` 에
붙였다 — case 1b 는 플래그가 invocation 안에 있는지, **case 1c 는 신호 추출이
트레이스백과 실제 예외를 살려서 앞으로 보내는지** 본다. 플래그만 주고 추출이
잘라 버리면 아무것도 얻지 못하기 때문이다(되주입 2종 red 실증).

**다음 재발이 완료 기준을 판정한다** — 이제 트레이스백이 step summary 까지 온다.

① **재발 1건.** run 32692148339, 커밋 `b119d68b`, **native** 셀,
`check_mypy_strict_release_gate_v0_11_12`, `exit_code 2` / `error_count 0` /
`first_error None`. 같은 커밋의 **slash 셀은 green** — 3차와 같은 서명이다.

② **3차의 수리가 절반 작동했다.** `stderr_tail` 이 이제 step summary 에 **바로**
보인다(아티팩트를 내려받지 않고). 그 값은
`note: please use --show-traceback to print a traceback when reporting a bug` 로,
INTERNAL ERROR 서명이 확증됐다.

③ **그런데 트레이스백은 없다 — 우리가 `--show-traceback` 을 안 주기 때문이다.**
`release_pipeline.py` 의 invocation 은 `-m mypy --no-incremental --config-file …
workflow_kit/` 뿐이다. mypy 가 트레이스백을 달라고 *요청하는 문구*를 우리가 로그로
옮겨 놓고, 정작 그 플래그를 준 적이 없다. 완료 기준("다음 재발이 트레이스백을
남긴다")은 그래서 **원리적으로 충족될 수 없었다.** 4차까지 온 이유가 여기다.

④ **내 새 모듈이 원인일 가능성은 배제했다.** 같은 invocation 을 로컬에서 3회 돌려
전부 `exit 0` (199 source files — 이번에 추가된 모듈 2개 포함). 로컬 mypy 도 CI 와
같은 2.1.0(compiled) 이다.

**① 재발 1건.** 2차 기준선(`b5c3909`, run 31708626708) 이후 smoke run **69건** 중 mypy 게이트 실패 **1건** — 2026-08-18T03:10, `426da99`, run 32094513107, `check_mypy_strict_release_gate_v0_11_12`, exit 2. 나머지 실패 10건은 전수 분류 결과 mypy 가 아니었다(8건은 **양 셀 동시** 실패라 flake 서명과 다름, 1건은 `check_consumer_metrics` 의 `gh auth status` 타임아웃, 1건은 이 사건).

**② native 전용이라던 서명이 깨졌다.** 이전 4건은 전부 native 셀이었는데 이번은 **slash 셀**이다. 셀 편향 가설은 표본 4로는 성립하지 않았다.

**③ 원인 계열 확정 — race 가 아니라 mypy INTERNAL ERROR(크래시).** 아티팩트의 `stderr_tail` 원문: `error: INTERNAL ERROR -- Please try using mypy master on GitHub`. 2차의 유력 가설("병렬 구간의 transient 파일 → `can't read file`")은 **반증**됐다. 보강 근거: 그 검사의 `duration_sec` 이 **0.65s** 인데 정상 mypy 는 로컬 실측 **3.4s / 197 files** 다 — 분석 중이 아니라 **시작 단계**에서 죽었다. 로컬 mypy 도 CI 와 같은 2.1.0(compiled)이라 버전 차이도 아니고, 순차 8회 재현 시도는 전부 exit 0.

**④ 4번 터지는 동안 원인을 못 좁힌 진짜 이유(2차 판단의 연장) — 증거가 로그까지 오지 못했다.** 2차가 `stderr_tail` 을 넣은 것은 맞았지만, 그 위의 **절단 두 겹**이 사유를 잘랐다: `smoke.yml` 의 `error_excerpt[:120]` 과 `_error_excerpt(limit=400)`. 실제로 CI 요약에 남은 것은 `... 'error_count': ` 까지였고, 원인은 **아티팩트를 내려받아서야** 보였다. 게다가 mypy 크래시 메시지는 **보일러플레이트로 시작**(안내 문구 + 문서 URL)해서, 앞에서 자르면 신호가 아니라 잡음만 남는다.

**⑤ 수리 3건.** (a) `_mypy_stderr_signal` — 보일러플레이트를 걷어 내 **신호를 앞으로** (전부 걷히면 원문 유지 — 증거 0 은 안 된다). (b) `smoke.yml` 요약 상한 120 → **800**. (c) `_error_excerpt` 상한 400 → **1200**. 검사는 상한 숫자를 복제하지 않고 **smoke.yml 에서 읽어** 그 폭 안에 사유가 들어오는지 잰다 — 복제하면 워크플로가 폭을 줄여도 검사는 계속 green 이다.

**⑥ 완료 기준을 바꾼다.** 기존 "33 run 연속 green" 은 **원인을 모르던 시점**의 기준이다. 원인 계열이 상류 mypy 크래시로 좁혀진 지금, green 누적은 상류 비결정성에 대해 아무것도 증명하지 못한다. 새 기준: **다음 재발이 트레이스백을 CI 로그에 남긴다**(그러면 상류 보고/우회가 가능해진다). 그 전까지는 close 하지 않는다.

**[관찰 1차 — 2026-08-13, 32차 세션 (v1.2.0 발행 후)]**

**판정: 재발 없음 — 그러나 close 불가 (표본 부족).**

**① 수정 후 재발 0건.** `cca074c`(2026-08-13T02:08:33Z) 이후 smoke run **5건**
(cca074c / 364db82 / 573f313 / 7c80619 / 6f70cf5) 전부 green, mypy 게이트 실패 0.

**② 그런데 발생 이력이 이 task 의 기록(1회)보다 많다 — 실제 3회.** 만성 red 구간이
끝난 뒤(2026-08-11T12:00 이후) smoke run 34건을 전수 조사해 실패·재실행 run 의
annotation 을 확인:

| 시각(UTC) | commit | 검사 | run id |
|---|---|---|---|
| 2026-08-11T15:14 | `4929aa5` | `check_mypy_strict_ci_v0_11_11` | 31505959891 |
| 2026-08-12T08:22 | `f7209f4` | `check_mypy_strict_release_gate_v0_11_12` | 31578036121 |
| 2026-08-13T01:31 | `2de04e8` | `check_mypy_strict_release_gate_v0_11_12` (exit_code 2) | 31657948983 |

**3건 전부 native 셀 단독 실패**이고, 셋 다 **transient 임을 실측 확인**:
①·② 커밋을 worktree 로 체크아웃해 지금 mypy 를 돌리면 **clean**
(`191 source files` / `137 source files`, 0 errors). 특히 `f7209f4`(red) →
`e93e3b2`(green) 사이에는 **`.py` 변경이 0** 이다 — 코드 수정 없이 red→green 이므로
진짜 타입 오류가 아니다. 즉 이 flake 는 이미 **두 mypy 게이트 검사에 걸쳐** 나타났다.

**③ 5회 green 은 아직 아무것도 증명하지 못한다 (이 관찰의 핵심).**
수정 전 발생률 = 3/34 = **run 당 8.8%**. 발생률이 **전혀 개선되지 않았어도**
5회 연속 green 이 나올 확률은 **63%** 다. 95% 신뢰로 "개선됐다" 고 말하려면
**약 33 run** 연속 green 이 필요하다 (부족분 28). → 완료 기준을 이 수치로 고정했다.

**④ 대안 가설 하나 기각**: "mypy 캐시(.mypy_cache) 동시 접근 경합" — `cmd_validate`
의 mypy 호출은 `--no-incremental` 이라 캐시를 읽지도 쓰지도 않는다
(`release_pipeline.py:260`). 캐시 경합은 원인이 아니다.

**⑤ 검사 자체는 살아 있다** (무력화 아님): `check_mypy_strict_release_gate_v0_11_12`
는 branch-context 분기 없이 두 셀 모두에서 돌고, `cmd_validate` 로 mypy 를 실제
실행한다 (로컬 실행 PASS 확인). native 셀 단독 실패 3/3 은 표본 3으로는 결론 불가.

- Next session starting point: 게이트 invocation 에 --show-traceback 을 추가한다 — 정상 경로 비용 0(mypy 는 내부 오류일 때만 트레이스백을 찍는다). 그 뒤 재발이 상류 보고 가능한 증거를 남긴다
  `gh run list --workflow smoke.yml` 로 `cca074c` 이후 run 을 세고, 실패·재실행 run 은
  `gh api repos/.../check-runs/<job_id>/annotations` 로 검사 이름을 확인한다
  (로그는 만료돼도 annotation 은 남는다 — 이번 조사에서 로그 0줄, annotation 은 생존).
- Remaining risks: **표본이 33 run 에 닿기 전에 "무재발" 을 근거로 close 하는 것.**
  8.8% 발생률에서 5회 green 은 63% 확률의 일상적 사건이다. 또한 재발 시
  `check_mypy_strict_ci_v0_11_11` 쪽으로도 나올 수 있으므로 두 검사를 함께 본다.

**[관찰 2차 — 2026-08-13, 35차 세션 (PR #24 작업 중)]**

**판정: 재발 — 유력 원인 제거 가설이 반증됐다. 연속 green 카운터 리셋.**

| 시각(UTC) | commit | 검사 | run id | 셀 |
|---|---|---|---|---|
| 2026-08-13T14:08 | `b5c3909` | `check_mypy_strict_ci_v0_11_11` (exit 2, 0 errors) | 31708626708 | native |

**① 1차 관찰이 예측한 대로 나왔다.** 1차는 "재발 시 `check_mypy_strict_ci_v0_11_11`
쪽으로도 나올 수 있으므로 두 검사를 함께 본다" 고 적었고, 정확히 그 검사에서 났다.
같은 내용의 직후 push(`e10ba4c`)와 같은 커밋의 PR 셀은 green — transient 확정.

**② 표본 재집계 (전수).** `cca074c` 이후 완료된 smoke run **28건** 중 mypy 게이트
실패 **1건**. 나머지 실패 run 은 전부 PR #23 계열의 다른 검사였다 (실패 run 11건을
로그로 전수 분류: `check_claim_workspace` / `check_seed_workspace_memory` /
`check_path_resolver` / `check_frontmatter_url_extraction` / `check_branch_context_matrix`
등). → **27 run 연속 green 후 재발**. 완료 기준(33 연속)은 **0 에서 다시 시작**한다.

**③ "유력 원인 제거됨" 은 반증됐다.** 1차는 TASK-001(원본 pyproject 왕복) 이관으로
유력 원인이 제거됐다고 봤다. 수정 후 발생률 1/28 = **3.6%** 는 수정 전 3/34 = 8.8%
와 표본상 구분되지 않는다 (1건으로는 개선 판정 불가). **관찰을 더 쌓는 것은 이제
비용 대비 효과가 낮다** — 33 run 을 채워도 "왜" 는 여전히 모른다.

**④ 진짜 병목은 표본이 아니라 증거였다 (이번 세션의 실질 발견).**
두 mypy 게이트 모두 **실패 사유를 버리고 있었다**:

- `exit 2` 는 mypy 의 **blocking error** 이고 사유는 **stderr** 로 나온다.
- 두 게이트의 오류 집계는 stdout 에서 `.py:` + `error:` 형태만 센다. blocking error
  는 그 필터에 안 걸린다 → 남는 기록이 `exit 2 (0 errors)` **한 줄**.
- CI 로그는 만료되고, **annotation 에는 검사 이름조차 안 실린다** (이번에 실측:
  `Process completed with exit code 1` 뿐). 1차가 적어 둔 관찰 방법("로그가 만료돼도
  annotation 은 남는다")이 이 실패 형태에서는 **성립하지 않는다**.

즉 4번 터지는 동안 원인을 못 좁힌 것은 표본이 부족해서가 아니라 **매 사건이 증거
없이 지나갔기** 때문이다. 수리:

- `cmd_validate` 의 mypy 결과에 `stderr_tail` / `stdout_tail` 추가 — **exit≠0 인데
  셀 오류 0건일 때만** 싣는다 (정본 한 곳, 두 게이트가 함께 혜택).
- `check_mypy_strict_ci_v0_11_11` case 8 의 assertion 메시지에 stderr + stdout 말미 첨부.
- 되주입 실증: blocking error 를 흉내내면 `stderr_tail` 에 사유가 그대로 실린다.

**⑤ 유력 가설 (미확정, 다음 재발이 판정한다)**: 전량은 병렬이라 다른 check 가 스캔
대상 아래에 파일을 잠깐 만들었다 지우면, mypy 가 나열한 파일을 읽는 순간 사라져
blocking error 가 된다 (`can't read file`). exit 2 + 0 errors 의 모양과 정확히
맞는다. 1차가 기각한 "캐시 경합" 과는 다른 기전이다 (두 호출 다 `--no-incremental`
이라 캐시는 여전히 무관).

- 다음 관찰 포인트: **다음 재발의 `stderr_tail`**. 그 한 줄이 ⑤를 확정하거나 기각한다.
  확정되면 처방은 두 mypy 게이트에 `REQUIRES_QUIET_REPO = True` (정숙 구간 이동) 또는
  스캔 대상 sandbox 격리다. 다만 정숙 구간은 이미 벽시계의 36%를 먹고 있으므로
  (TASK-…-guard-003) 이동 비용을 함께 본다.
- 관찰 방법 정정: 실패 run 분류는 annotation 이 아니라 **`gh run view <id> --log-failed`**
  로 한다 (로그 만료 전에). annotation 은 검사 이름을 담지 않는 경우가 있다.

## ✅ Outcome

- Result: 관찰 4차: run 32692148339 native red / slash green / 같은 커밋 재실행 green. 로컬 게이트 invocation 3회 exit 0 (199 files). check_mypy_strict_release_gate case 1b·1c 신설, 되주입 2종 red 실증
- Verification: 관찰 4차: run 32692148339 native red / slash green / 같은 커밋 재실행 green. 로컬 게이트 invocation 3회 exit 0 (199 files). check_mypy_strict_release_gate case 1b·1c 신설, 되주입 2종 red 실증
- Follow-up:
