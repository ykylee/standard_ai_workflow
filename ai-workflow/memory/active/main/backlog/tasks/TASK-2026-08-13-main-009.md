---
id: TASK-2026-08-13-main-009
status: done
created_at: 2026-08-13
source_anchor: generic-task-2026-08-13-main-009
source_path: backlog/2026-08-13.md
kind: generic
---

# TASK-2026-08-13-main-009 — 전량 검사 시간 — 정숙 구간 직렬화가 벽시계의 36%

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-13
- Owner: AI Agent
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/tests/check_no_repo_write.py`
  - `workflow-source/tests/check_wiki_score.py`
  - `workflow-source/tests/run_all_checks.py`

- Description: 브랜치 종료로 아카이브된 TASK-2026-08-13-fix-branch-memory-namespace-guard-003 의 main 이월 (원본: ai-workflow/memory/archived/fix/branch-memory-namespace-guard/backlog/tasks/). 1축 실측 124.5s / CPU 482s / 실효 병렬도 3.9x. 정숙구간 45.0s(직렬, 벽시계의 36%) 중 check_no_repo_write 가 29.5s(65%). 병렬 구간 하한은 check_wiki_score 단일 39.1s. 검사 254개 중 176개는 1초 미만 — 개수가 아니라 두 검사가 시간을 결정한다. 처방 후보: (1) check_no_repo_write 를 저장소 사본 검증으로 (선례: check_source_without_runtime_layer, 같은 문제를 사본으로 이미 해결) (2) check_wiki_score 분할 또는 score 도구 memoize (3) MAX_AUTO_JOBS 8 -> 코어수. 셋 다 하면 축당 124.5s -> ~85s 추정. 주의: TASK-2026-08-13-main-004 의 처방 후보(두 mypy 게이트를 정숙 구간으로 이동)와 상충하므로 함께 판단한다.
- Completion criteria: 축당 벽시계가 **재실측으로** 유의미하게 줄어든다 (측정은 같은 부하 조건에서 3회, 중앙값으로 판정 — 오늘 두 실측이 1.5배 벌어진 것이 부하 탓임을 감안)
- Completion criteria: 줄인 결과가 **검사 범위를 줄여서 얻은 것이 아님**을 보인다 (각 처방 전후로 같은 case 수·같은 판정)
- Completion criteria: TASK-2026-08-13-main-004(mypy 게이트를 정숙 구간으로 이동)와 상충을 함께 판단한 결과를 남긴다

## 🛠️ Implementation / Content

- Progress: `2026-08-14 03:46` 기준 무거운 8개가 시간을 결정한다 — 임계경로 2개부터
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: **무거운 8개 중 4개가 같은 계산 반복이었다.** 검사 범위를 줄이지 않고 산출물을 한 번만 만들어 나눠 본다. 단독 실행 142.2s → 72.2s. 상세는 브랜치 세션 기록
- Result: `check_wiki_score` 58.1→19.4s (점수 도구 6.4s × 동일 인자 9회 → `_score_once()` + deep copy) / 릴리스 계열 3종 (`cmd_release_status` 가 호출마다 mypy 5.1s → 실측 판정 재사용) summary 32.0→21.1 · auto_bump 29.2→18.9 · status 22.9→12.8
- Result: **`--no-incremental` 은 안 건드렸다** — CI yml·`check_mypy_strict_ci_v0_11_11:74`·`check_yaml_surfaces` fallback·v1.0.0 Gate 3 이 같은 명령을 쓰도록 고정한 값이라 로컬만 바꾸면 게이트 동일성이 깨진다. 느린 것이 곧 잘못은 아니다
- Result: 나머지 4개는 낭비가 아니라고 판정 — `branch_context_matrix` 서로 다른 컨텍스트 3 probe / `mypy_config_actually_loaded` config 로딩 실측이 목적 / `no_repo_write` 감시 표본 13종 직렬(정숙 구간 필수) / `release_pipeline_lib` 는 위 수정으로 함께 하락
- Result: TASK-2026-08-13-main-004(mypy 게이트를 정숙 구간으로 이동)와의 상충은 **발생하지 않았다** — 이번 수리는 mypy 실행 위치·플래그를 바꾸지 않고 테스트의 중복 호출만 줄였다
- Verification: 전량 2축 255/255 ×2 green. native 벽시계 중앙값 **195.5s (n=5: 193.1/195.2/195.5/195.8/196.6) → 154.2s (n=3: 150.9/154.2/154.5) = −21%**. 분포가 좁아 부하 편차로 설명되지 않는다. 각 검사 단독 전후 2회씩 측정 + case 목록·판정 diff 동일 확인 (범위 축소 아님)
- Follow-up: 정숙 구간 no_repo_write 39s 는 남았다 — 사본 검증으로 옮기려면 '사본을 접으면 외부 증인이 필요하다' 를 먼저 판단해야 한다
