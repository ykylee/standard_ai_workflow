---
id: TASK-2026-08-13-main-009
status: done
created_at: 2026-08-13
source_anchor: generic-task-2026-08-13-main-009
source_path: backlog/2026-08-13.md
kind: generic
---

# TASK-2026-08-13-main-009 — Codex·Claude Code native plugin release asset 분리 및 release pipeline 연결

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-08-13
- 담당:
- 호스트명:
- 호스트 IP:
- 영향 문서:
  - `plugin/`
  - `workflow-source/workflow_kit/tools/release_pipeline.py`
  - `docs/RELEASE.md`

- 작업 내용: Codex native plugin과 기존 Claude Code plugin을 개별 GitHub Release asset으로 배포하고, 이후 plugin-capable harness를 확장 가능한 registry로 관리한다.
- 완료 기준: `dist --apply`가 두 ZIP을 만들고, Codex ZIP은 marketplace 설치 구조와 manifest validation을 통과하며 release가 두 asset을 포함한다.

## 🛠️ Implementation / Content

- 진행 현황: `2026-08-13` 기준 완료. ID 재발급: 이 task 는 원래 `-008` 로 등록됐으나
  같은 날 `main` 에서 별개 세션이 같은 번호로 TestPyPI 리허설을 등록해 충돌했다.
  병합 시 `-009` 로 재발급했다 (아래 "정합 보완" ①).
- 다음 세션 시작 포인트: PR #23의 GitHub smoke CI 결과를 확인하고, 실패 시 PR 변경과 기존 검사 드리프트를 분리해 처리한다.
- 남은 리스크: `interface` 블록(UI 표면)의 실기 효과는 관측 경로를 못 찾아 미확인이다
  — 스펙 원문 근거로만 싣는다. `skills` / `mcpServers` 는 실측으로 로드를 확인했다.

## ✅ Outcome

- 작업 결과: 공유 payload에 Codex `.codex-plugin/plugin.json` 추가. `PLUGIN_HARNESS_SPECS` registry가 Codex/Claude Code별 ZIP을 분리 생성. Codex ZIP에는 `.agents/plugins/marketplace.json`과 `plugins/standard-ai-workflow/`를 포함하고, Claude ZIP에는 Claude manifest/hooks만 포함. `release-pipeline dist`가 ZIP을 생성하고 `release`가 asset 누락 시 중단 후 GitHub Release attach 목록에 포함.
- 검증 결과: payload 18/18, plugin distribution smoke, Codex plugin validator, actual `dist --apply`, release dist regression 8/8, 대상 ruff 검사 통과.
- **실기 로드 실측** (codex-cli 0.143.0, 격리 `CODEX_HOME`): ZIP →
  `codex plugin marketplace add` → `codex plugin add` → `installed, enabled`.
  `codex debug prompt-input` 의 `<skills_instructions>` 에 스킬 4종이
  `standard-ai-workflow:<slug>` 로 잡히고 `codex mcp list` 에 read-only 번들이
  `enabled` 로 잡힌다. Codex 가 번들한 공식 검증기
  (`plugin-creator/scripts/validate_plugin.py`) 도 통과하며, `interface.displayName`
  을 빼서 되주입하면 그 필드를 지목하며 실패한다 — 검증기가 공허하지 않다.
- 정합 보완 (main 병합 시): ①task ID 충돌 재발급 + 재발 방지 case 7 신설
  (`check_appendonly_memory_layout`, 되주입 실증) ②`RELEASE.md §1` 은 main 의
  TestPyPI 1회 한정 허용을 유지 ③release note 누적 수치 갱신 ④TST-WF-01 이
  `raise AssertionError` 를 신호로 못 보던 사각지대 수리 (`baselines.py`, case 10·11)
  ⑤저자 이메일 하드코딩(`yklee@`, 정본은 `ykylee@`) → packaging metadata 파생 +
  case 18 ⑥`test_codex_skill_metadata` 의 PyYAML fail-open → fail-closed
  ⑦mypy strict 미통과 기록은 오탐이었다 — `.venv` 인터프리터로 재측정 시
  **193 files 0 errors** (system python 사용 시의 알려진 위양성 경로).
- 후속 작업: 신규 하네스의 native manifest와 `PluginHarnessSpec` 등록 후 distribution smoke 확장.
