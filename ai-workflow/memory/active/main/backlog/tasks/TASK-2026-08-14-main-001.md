---
id: TASK-2026-08-14-main-001
status: done
created_at: 2026-08-14
source_anchor: generic-task-2026-08-14-main-001
source_path: backlog/2026-08-14.md
kind: generic
---

# TASK-2026-08-14-main-001 — 브랜치 정리 — fix/archive-history-integrity 종료 + 아카이브, 그리고 자기 적용 검사의 위양성

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-08-14
- 담당: AI Agent
- 호스트명:
- 호스트 IP:
- 영향 문서:
  - 

- 작업 내용: 병합된 브랜치가 원격·active/ 에 남아 있던 것을 닫는다 (36차 세션이 미결로 남긴 자리)
- 완료 기준: 브랜치 task 가 실제 산출물·검증으로 채워진 뒤 done 으로 닫힌다 (빈 완료 기준으로 닫지 않는다)
- 완료 기준: 원격 브랜치 삭제 + `wk archive-branch-memory --apply` 로 참조까지 재작성된다
- 완료 기준: 존재하지 않는 task ID 참조가 저장소에 남지 않는다
- 완료 기준: 전량 2축 green

## 🛠️ Implementation / Content

- 진행 현황: 36차 세션 handoff §5 가 스스로 남긴 미결 항목을 이어받아 닫았다.
- 다음 세션 시작 포인트:
- 남은 리스크: case 7 은 여전히 코드 블록 안의 *형태가 온전한* 링크는 링크로 본다. 현재 corpus 에는 없다

## ✅ Outcome

- 작업 결과: task 마감 — `in_progress` 인 채라 아카이브가 정당하게 차단됐다. 완료 기준·작업 결과·검증 결과가 전부 빈칸이어서 채운 뒤 `done` 으로 닫았다
- 작업 결과: 유령 ID 교정 2건 — handoff §4 와 세션 기록이 `TASK-2026-08-13-fix-…-001` 을 가리켰으나 **존재한 적 없는 ID** 였다 (실재는 `…-08-14-…`). 세션 기록의 `관련 문서` 링크는 죽어 있었다
- 작업 결과: `origin/fix/archive-history-integrity` 삭제 (고유 커밋 0, tip `f798947` 은 main 이력에 잔존) → 아카이브 apply. 참조 재작성이 실물에서 동작 — handoff 링크 2건 + archived state.json 5경로. `open_task_ids` 는 `[]`
- 작업 결과: `active/` 에 남은 브랜치 네임스페이스는 `main` 하나 (빈 `active/fix/` 도 제거)
- 작업 결과: **`check_archive_history_integrity` case 7 의 위양성 수리** — 링크 정규식 자체 사본이 label 을 요구하지 않는 약한 형제라, 링크 문법을 *설명하는* 산문(`](path "제목")`)을 링크로 오인해 아카이브 직후 red. 정본(`workflow_kit.common.markdown.LINK_RE` + `normalize_link_target`)으로 교체하고 사본을 걷었다
- 작업 결과: case 14 신설 — 예시 산문은 안 잡고 **진짜 깨진 링크는 잡는지** 양방향 fixture. case 7 은 살아있는 저장소만 관찰해 '안 잡는' 쪽으로 무력화돼도 조용히 green 이다
- 검증 결과: 전량 검사 2축 255/255 ×2 green (native/slash, 195s+). 되주입 실측: 약한 정규식을 되돌리면 case 14 가 지목하며 FAIL, 정본에서 14/14 PASS
- 후속 작업: TASK-2026-08-13-main-008 TestPyPI 리허설 (blocked — 소유자 토큰)
