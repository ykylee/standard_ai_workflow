---
id: TASK-2026-08-14-main-003
status: done
created_at: 2026-08-14
source_anchor: generic-task-2026-08-14-main-003
source_path: backlog/2026-08-14.md
kind: generic
---

# TASK-2026-08-14-main-003 — 변경 범위 기반 선택 실행 — run_all_checks --changed

## 📝 Description

- 상태: done
- 우선순위: medium
- 요청일: 2026-08-14
- 담당: AI Agent
- 호스트명:
- 호스트 IP:
- 영향 문서:
  - `workflow-source/tests/run_all_checks.py`

- 작업 내용: 각 검사가 자기가 보는 경로를 파일 안에 선언하고, 변경과 무관한 검사를 건너뛴다 (미선언은 항상 실행)
- 완료 기준: 검사가 **파일 안에서** 자기 관찰 범위를 선언한다 (`REQUIRES_QUIET_REPO` / `CHECK_TIMEOUT_S` 와 같은 idiom — 선언과 코드가 같은 파일에 있어야 갈라지지 않는다)
- 완료 기준: **미선언 = 항상 실행**이 기본값이다. 선택은 사각지대를 만들지 않는 방향으로만 작동한다
- 완료 기준: 건너뛴 검사와 그 이유를 **반드시 출력**한다 (조용한 축소는 '전부 돌았다' 로 읽힌다)
- 완료 기준: `--changed` 결과가 전량 결과와 **같은 판정**임을 되주입으로 실증한다 — 결함을 심었을 때 `--changed` 가 그 검사를 선택하는지 본다
- 완료 기준: push 게이트는 여전히 전량 2축이다. 이 옵션은 편집 중 수단이지 게이트를 대체하지 않는다

## 🛠️ Implementation / Content

- 진행 현황: `2026-08-14 04:21` 기준 각 검사가 자기가 보는 경로를 파일 안에 선언하고, 변경과 무관한 검사를 건너뛴다
- 다음 세션 시작 포인트:
- 남은 리스크: 선언이 코드와 갈라지면 **조용히 안 도는 검사**가 생긴다 — 가장 위험한 실패 양식이다. 선언 누락 시 항상 실행(보수적 기본값)으로 그 위험을 구조적으로 막는다

## ✅ Outcome

- 작업 결과: `run_all_checks --changed` 신설 + `WATCHES` 선언 idiom + `check_changed_selection` 9 cases. 상세는 아카이브된 브랜치 세션 기록
- 작업 결과: 계약이 전부 **조용히 안 도는 검사**를 막는 쪽 — 미선언=항상 실행 / 자기 파일 변경시 무조건 실행 / 비리터럴 원소면 미선언 취급 / parse 실패도 실행 / 건너뛴 것은 이름·사유 전부 출력 / 변경 0건이면 "통과가 아니라 잴 것이 없음" / 매 출력에 "게이트가 아니다"
- 작업 결과: 무거운 8개 중 7개 선언. `check_no_repo_write` 는 **일부러 미선언** (감시 표본 13종 → 관찰 범위가 사실상 저장소 전체)
- 검증 결과: 전량 2축 256/256 ×2 green. 되주입 2종(매칭 항상 거짓 → case 3 FAIL / 미선언도 건너뜀 → case 1·5·6 FAIL). 실이득: 메모리 문서 1건 편집 시 실행 250/건너뜀 6, 벽시계 125.9s (전량 154.2s 대비 −18%)
- 후속 작업:
