---
id: TASK-2026-08-14-main-004
status: done
created_at: 2026-08-14
source_anchor: generic-task-2026-08-14-main-004
source_path: backlog/2026-08-14.md
kind: generic
---

# TASK-2026-08-14-main-004 — 2축 전량을 조건부 1축으로 — 브랜치 컨텍스트 민감 경로 판정

## 📝 Description

- Status: done
- Priority: low
- Request date: 2026-08-14
- Owner: AI Agent
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/tests/run_all_checks.py`
  - `workflow-source/workflow_kit/common/branch_matrix.py`

- Description: 브랜치 컨텍스트에 민감한 경로를 안 건드린 변경에서는 slash 축을 생략한다 (게이트 시간 50% 절감)
- Completion criteria: '민감 경로' 목록이 **정본에서 파생**된다 (`branch_matrix.BRANCH_CONTEXTS` 와 브랜치 경로를 조립하는 자리) — 손 목록이면 갈라진다
- Completion criteria: 생략 판정이 났을 때 **무엇을 근거로 생략했는지** 출력한다
- Completion criteria: **CI 는 계속 2축**이다. 이 절감은 로컬 전용이고, 로컬이 CI 보다 약해지는 방향이므로 그 비대칭을 문서에 명시한다
- Completion criteria: 되주입 — slash 축에서만 깨지는 결함을 심고, 판정이 '민감함' 을 옳게 내는지 본다

## 🛠️ Implementation / Content

- Progress: 판단 완료 (2026-08-14, 45차): 하지 않는다.
- Next session starting point:
- Remaining risks: **이것이 15연속 CI red 를 만든 바로 그 비대칭이다** (2026-08-10, 로컬 1축 / CI 2축). 판정이 조금이라도 헐거우면 같은 사고가 재현된다. 절감(축당 ~196s)보다 재발 비용이 크다고 판단되면 **하지 않는 것도 결론**이다 — 그 판단을 task 에 남길 것

## ✅ Outcome

- Result: 결론: 하지 않는다. 리스크 노트가 예고한 그 판단이다 — 절감보다 재발 비용이 크다. 세 가지 근거.
- Result: 근거 1 — 판정이 건전하게 성립하지 않는다: '민감 경로' 의 정본 파생(완료 기준 1)이 불가능하다. 브랜치 경로를 조립하는 소비자가 kit 21개 모듈 + 검사 16개 파일에 퍼져 있고, 결정적으로 15연속 red 의 결함은 kit 코드가 아니라 검사 자신(check_release_pre_check_gates 의 state.json 존재 전제)에 있었다. 건전한 민감 집합은 검사 260개 전부를 포함해야 하고, 그러면 생략이 거의 발동하지 않는다. 발동하도록 좁히면 그 좁힘이 곧 15연속 red 를 만든 헐거움이다.
- Result: 근거 2 — 절감이 task 작성 시점의 절반으로 줄었다: 작성 시점 축당 ~196s 였으나 v1.1.7 병렬화 후 오늘 14회 실측은 축당 102~115s(평균 ~106s). push 당 게이트 1회 규율(CLAUDE.md)에서 절감은 push 당 ~106s 뿐이다. 오판 1회의 비용은 실측 10일(15연속 CI red, 그동안 handoff 는 '전량 green' 을 기록)이었다 — 기대비용이 비대칭이다.
- Result: 근거 3 — 이 저장소의 push 는 거의 항상 민감하다: 세션 종료 규율이 memory 갱신을 커밋에 싣는데, active/<branch>/ 의 메모리 문서가 바로 브랜치 컨텍스트로 모양이 갈리는 데이터다. 오늘 5 push 전부가 memory 를 포함했다 — 생략 가능 push 가 실질 0이다.
- Result: 대안이 이미 있다: 한 축만 볼 때는 --branch-context=slash 명시 지정, 편집 루프는 --filter (CLAUDE.md 게이트 표). 조건부 자동 생략이 더할 것이 없다. CLAUDE.md 게이트 절에 재론 방지 한 줄을 남겼다.
- Verification: 판단 task — 검증은 근거의 실측: 게이트 14회 축당 102~115s · 브랜치 경로 소비자 21+16 파일 · 15연속 red 실측 참조(TASK-2026-08-10-main-016 계보) · 오늘 5 push 전부 memory 포함
- Follow-up: 없음 — 재론 조건: 게이트가 push 당 1회를 넘게 강제되는 구조 변화가 생기거나, 검사 저작 규약이 브랜치 컨텍스트 접근을 선언식으로 강제하게 되면 그때 다시 잰다
