---
id: TASK-2026-08-14-main-005
status: done
created_at: 2026-08-14
source_anchor: generic-task-2026-08-14-main-005
source_path: backlog/2026-08-14.md
kind: generic
---

# TASK-2026-08-14-main-005 — seed 직후 sessions/ 가 비어 layout 검사가 red — '한 벌이면 green' 이 아직 참이 아니다

## 📝 Description

- 상태: done
- 우선순위: medium
- 요청일: 2026-08-14
- 담당: AI Agent
- 호스트명:
- 호스트 IP:
- 영향 문서:
  - `workflow-source/workflow_kit/tools/seed_workspace_memory.py`
  - `workflow-source/tests/check_appendonly_memory_layout.py`

- 작업 내용: 36차가 state.json 까지 붙여 seed 의 끝을 맺었지만, sessions/ 는 빈 디렉터리만 만들어 check_appendonly_memory_layout 이 여전히 red
- 완료 기준: `wk seed-workspace-memory --apply` 직후의 브랜치 네임스페이스가 **전량 검사에서 green** 이다 (지금은 `check_appendonly_memory_layout` 이 `sessions/` 비어 있음으로 red)
- 완료 기준: 결정과 근거를 남긴다 — seed 가 세션 기록 stub 을 쓰는 것이 옳은가, 아니면 layout 검사가 '아직 세션 기록이 없는 갓 만든 네임스페이스' 를 구분해야 하는가. **조용한 예외는 두지 않는다**
- 완료 기준: 되주입 — 갓 seed 한 브랜치를 fixture 로 만들어 판정이 의도대로 나오는지 본다

## 🛠️ Implementation / Content

- 진행 현황: 완료 (2026-08-14, 45차).
- 다음 세션 시작 포인트:
- 남은 리스크: layout 검사를 느슨하게 푸는 방향은 위험하다 — '갓 만든 것' 을 예외로 두면 오래된 절반짜리도 같이 통과한다

## ✅ Outcome

- 작업 결과: 결정: seed 가 세션 기록을 쓴다. layout 검사를 '갓 만든 것' 예외로 푸는 방향은 기각 — 그 예외는 오래된 절반짜리도 같이 통과시키고, task 의 리스크 노트가 이미 경고한 방향이다. 조용한 예외 0.
- 작업 결과: 근거: seed 는 그 네임스페이스의 첫 세션 사건이므로 기록이 stub 이 아니라 진실이다 — sessions/workspace_seed_<date>.md 에 누가·왜·무엇을 만들었는지(브랜치·작업 축·시작 task·범위 밖)를 남긴다.
- 작업 결과: 기록의 링크는 브랜치 디렉터리 내부 상대 경로만 쓴다 — 문서와 대상이 함께 아카이브되므로 이동 후에도 풀린다 (main-006 의 문서-이동 함정을 처음부터 피하는 형태).
- 작업 결과: check_seed_workspace_memory 5 → 6 cases (assertion 7 → 11): sessions/ 가 .gitkeep 제외 1+ 파일 (layout 검사 case 1 과 같은 술어) + 기록이 seed 사건(브랜치·task ID)을 담는다. 멱등은 기존 note() 경로라 재실행이 덮어쓰지 않는다.
- 검증 결과: seed smoke 11 assertions PASS · 되주입 1종 잡힘(기록 쓰기 제거 → test_sessions_not_empty_after_seed FAIL) · appendonly layout 검사 green · mypy strict clean. 완료 기준 1(전량 green)은 layout 검사와 같은 술어로 fixture 실증 — 실브랜치를 새로 seed 하지는 않았다(만들면 도로 아카이브해야 한다).
- 후속 작업: 없음
