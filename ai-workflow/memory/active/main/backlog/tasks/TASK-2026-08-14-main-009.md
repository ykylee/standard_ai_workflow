---
id: TASK-2026-08-14-main-009
status: done
created_at: 2026-08-14
source_anchor: generic-task-2026-08-14-main-009
source_path: backlog/2026-08-14.md
kind: generic
---

# TASK-2026-08-14-main-009 — task SSOT 4단계 — 본문 라벨 영어 전환 (release 경계)

## 📝 Description

- Status: done
- 우선순위: medium
- 요청일: 2026-08-14
- 담당: AI Agent
- 호스트명:
- 호스트 IP:
- 영향 문서:
  - `workflow-source/workflow_kit/common/project_docs.py`
  - `workflow-source/tests/check_task_ssot_source.py`
  - `workflow-source/tests/check_backlog_update_layout.py`
  - `workflow-source/core/backlog_update_skill_spec.md`

- 작업 내용: 3단계가 만든 정본 표를 영어로 바꾼다. 리더는 이미 두 표기를 받는다
- 완료 기준: `TASK_FIELD_LABELS` 를 영어로 바꾼다 (`TASK_FIELD_ALIASES` 는 그대로 둔다 — 옛 문서를 계속 읽어야 한다)
- 완료 기준: **이번 release 이후**에 한다. 소비자의 리더가 두 표기를 아는 버전을 받은 뒤여야 한다
- 완료 기준: `read_only_bundle` 의 라벨 리터럴도 정본 조회로 옮긴다 (MCP 번들의 task 생성 경로)
- 완료 기준: 전환 후에도 옛 표기로 적힌 기존 task 파일이 그대로 읽힌다 (되주입으로 실증)

## 🛠️ Implementation / Content

- Progress: `2026-08-20 14:37` 기준 3단계가 만든 정본 표를 영어로 바꾼다. 리더는 이미 두 표기를 받는다
- Next session starting point: 없음 — 4단계 종료
- Remaining risks: 저장소의 task 파일은 이제 혼합 표기다 — 읽는 쪽은 ALIASES 가 덮지만, 새로 만드는 리더가 ALIASES 를 안 거치고 리터럴로 비교하면 같은 계열의 결함이 다시 난다. case 12·13 과 ssot case 11 이 그 그물이다

## ✅ Outcome

- Result: TASK_FIELD_LABELS 17개를 영어로 뒤집었다. TASK_FIELD_ALIASES 는 그대로 둔다 — 기존 task 파일 160여 개가 한국어 표기이고 거기서 한국어를 빼는 순간 그 문서들의 필드가 조용히 사라진다
- Result: 전환이 레지스트리 밖 리터럴 4곳을 드러냈다: ① BacklogParser 의 잘못된 상태 형식 경고가 '- 상태:' 로만 발화 ② detect_confirmation_fields 가 누락 필드 이름을 한국어 리터럴로 보고 ③ merge_task_file 이 상태 라벨을 리터럴로 써서 render_task_file 과 표기가 갈렸다 ④ **_BODY_STATUS_LINE_RE 가 '^- 상태:' 만 매칭** — 영어 표기 파일에서 본문 상태 줄이 안 지워져 합성한 frontmatter 줄보다 뒤에 남았고, 파서는 뒤에 오는 값을 쓰므로 **본문이 frontmatter 를 이겼다**. 실증: frontmatter done + 본문 Status in_progress → in_progress 로 읽힘. 전환 후 새로 쓰이는 모든 task 의 상태가 state.json 에서 조용히 틀렸을 것이다
- Result: 검사의 그물이 콜론 모양('- 라벨:')만 보고 있어 ②는 case 8·9 를 둘 다 통과했다. case 12(라벨 이름만 든 리터럴) · case 13(경고의 두 표기) · ssot case 11(frontmatter 우선순위의 두 표기) 신설
- Result: 리터럴을 박은 검사 3종을 계약 파생으로 교정: check_task_label_registry 4 case · check_task_multivalue_fields 7 case · check_backlog_update_layout 7곳. 동작은 내내 옳았고 검사가 그 시점 상수를 인코딩하고 있었다. 부수로 check_task_ssot_source 의 총계가 '10/10' 리터럴이라 case 를 늘려도 숫자가 안 따라왔다 — 세어서 내도록 고쳤다
- Result: spec 문서의 draft_entry 예시와 fields_requiring_confirmation 을 새 표기로 갱신하고 배포 사본(ai-workflow/core/)을 동기화했다. 표기 계약을 spec §5 말미에 명시
- Verification: 전량 2축 264/264 x2 green · check_task_label_registry 13/13 · check_task_multivalue_fields 12/12 · check_task_ssot_source 11/11 · check_backlog_update_layout 9/9 · check_standard_single_source 11/11 · 되주입 4종 red 실증 · 이 task 자신을 새 표기 도구로 close 해 혼합 코퍼스를 종단 검증(건드린 줄만 영어, 나머지는 한국어 보존)
- 후속 작업:
