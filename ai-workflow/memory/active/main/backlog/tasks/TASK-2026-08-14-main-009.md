---
id: TASK-2026-08-14-main-009
status: in_progress
created_at: 2026-08-14
source_anchor: generic-task-2026-08-14-main-009
source_path: backlog/2026-08-14.md
kind: generic
---

# TASK-2026-08-14-main-009 — task SSOT 4단계 — 본문 라벨 영어 전환 (release 경계)

## 📝 Description

- 상태: in_progress
- 우선순위: medium
- 요청일: 2026-08-14
- 담당: AI Agent
- 호스트명:
- 호스트 IP:
- 영향 문서:
  - `workflow-source/workflow_kit/common/project_docs.py`
  - `workflow-source/workflow_kit/common/read_only_bundle.py`
  - `workflow-source/workflow_kit/common/workflow_writes.py`
  - `workflow-source/workflow_kit/tools/seed_workspace_memory.py`
  - `workflow-source/tests/check_task_label_registry.py`

- 작업 내용: 3단계가 만든 정본 표를 영어로 바꾼다. 리더는 이미 두 표기를 받는다
- 완료 기준: `TASK_FIELD_LABELS` 를 영어로 바꾼다 (`TASK_FIELD_ALIASES` 는 그대로 둔다 — 옛 문서를 계속 읽어야 한다)
- 완료 기준: **이번 release 이후**에 한다. 소비자의 리더가 두 표기를 아는 버전을 받은 뒤여야 한다
- 완료 기준: `read_only_bundle` 의 라벨 리터럴도 정본 조회로 옮긴다 (MCP 번들의 task 생성 경로)
- 완료 기준: 전환 후에도 옛 표기로 적힌 기존 task 파일이 그대로 읽힌다 (되주입으로 실증)

## 🛠️ Implementation / Content

- 진행 현황: 게이트 밖 준비 완료 (2026-08-14). 완료 기준 4개 중 2개(리터럴 이관 · 되주입 실증) 충족. 남은 것은 TASK_FIELD_LABELS 전환 한 줄 — release 경계.
- 다음 세션 시작 포인트: release(v1.2.1-beta) 발행 후 TASK_FIELD_LABELS 를 영어로 뒤집는다. ALIASES 는 그대로 둔다. case 10 이 안전을 이미 실증해 두었다.
- 남은 리스크: 전환 자체는 여전히 release 경계 뒤다. 쓰는 쪽을 먼저 바꾸면 소비자의 옛 리더(v1.2.0-beta, 한국어만 안다)가 새 문서를 못 읽는다 — ALIASES 를 담은 리더가 배포된 뒤여야 한다.

## ✅ Outcome

- 작업 결과: 정본 표 누락 라벨 3개 보강: request_date(요청일, task 파일 160개에 존재) · completion_date(완료일) · out_of_scope(범위 밖). 표에 없어 정본 writer 조차 리터럴로 쓰고 있었다 — 전환 시 그 줄만 한국어로 남아 섞인 문서가 된다.
- 작업 결과: 리터럴 이관 27곳: read_only_bundle(13, MCP task 생성 경로) · seed_workspace_memory(10) · workflow_writes(3) · backlog_update(1).
- 작업 결과: 3단계가 못 잡은 이유 = case 8 이 backlog_update.py 한 파일만 훑었다. 검사의 범위가 곧 주장의 범위였다. 본문 조립 함수 5개로 확대하되 파일이 아니라 함수 단위로 좁혔다 — 문서 메타 헤더(- 상태:)와 환경 기록(- 호스트명:)은 check_doc_metadata 가 지키는 다른 계약이라 파일 단위로 넓히면 오탐이 난다.
- 작업 결과: 잠재 결함 1건 수리: merge_task_file 이 앵커를 리터럴로 비교했다(startswith('- 작업 결과:'), label == '검증 결과'). 영어 표기 문서에서 항상 거짓이라 검증 결과 주입 분기가 조용히 안 돌았다 — done 판정의 근거가 missing 으로 버려졌다. 실측 전후: 수리 전 영어 표기 missing=['검증 결과'], 수리 후 missing=[]. 한국어 동작은 불변.
- 작업 결과: 검사 8 → 11 cases. case 9 = 역방향(표에 없는 라벨이 본문에 들어가는 것 — 요청일이 샌 구멍) · case 10 = 되주입 실증(표를 영어로 뒤집은 상태에서 한국어 문서가 그대로 읽힌다, 4단계 완료 기준을 전환 전에 실증) · case 11 = 검증 결과 주입 앵커 회귀 가드.
- 검증 결과: check_task_label_registry 11/11 PASS · mutation 3종 전부 잡힘(검사가 실제로 문다) · 산출물 바이트 동일(정본이 아직 한국어라 오늘 동작 변화 0) · SDK 매트릭스 3/3(1.27.0/1.29.0/2.0.0) · 전량 2축 260/260 ×2 green
- 후속 작업:
