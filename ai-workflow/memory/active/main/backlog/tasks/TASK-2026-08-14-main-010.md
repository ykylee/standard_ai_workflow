---
id: TASK-2026-08-14-main-010
status: done
created_at: 2026-08-14
source_anchor: generic-task-2026-08-14-main-010
source_path: backlog/2026-08-14.md
kind: generic
---

# TASK-2026-08-14-main-010 — 검증 결과 주입이 작업 결과 묶음을 갈라 다음 갱신에서 고아 줄을 만든다

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-08-14
- 담당:
- 호스트명:
- 호스트 IP:
- 영향 문서:
  - `workflow-source/workflow_kit/common/workflow_writes.py`
  - `workflow-source/tests/check_task_multivalue_fields.py`

- 작업 내용: merge_task_file 이 검증 결과를 첫 작업 결과 줄 뒤에 넣어 열거 묶음이 갈라진다. 다음 갱신에서 _set_list_field 가 첫 줄만 묶음으로 보고 나머지를 남긴다 — 43차가 닫았다고 본 중복 누적 계열이 다른 경로로 살아 있다.
- 완료 기준: 검증 결과를 열거 묶음 안이 아니라 묶음 끝(또는 Outcome 절 끝)에 넣어 작업 결과 줄이 연속을 유지한다
- 완료 기준: 되주입으로 실증한다 — 결과 3줄 + 검증 1줄을 쓴 뒤 결과를 2줄로 갱신했을 때 고아 줄이 0이다
- 완료 기준: 기존에 이미 갈라진 task 파일(TASK-2026-08-14-main-009 포함)이 갱신 시 고아를 안 만든다

## 🛠️ Implementation / Content

- 진행 현황: 완료 (2026-08-14, 45차).
- 다음 세션 시작 포인트:
- 남은 리스크: 묶음 갈라짐은 조용하다 — 파일은 읽히고 검사도 통과한다. 다음 --result-note 갱신에서야 옛 값이 새 값과 나란히 남는 형태로 나타난다.

## ✅ Outcome

- 작업 결과: 주입 위치 교정: 검증 결과를 작업 결과 묶음 첫 줄 뒤(idx+1)가 아니라 묶음 끝 뒤에 넣는다 — 묶음이 더는 갈라지지 않는다.
- 작업 결과: _heal_validation_split 신설: merge_task_file 진입 시 [작업 결과][검증 결과][작업 결과] 패턴을 감지해 검증 줄을 묶음 끝으로 옮긴다 — 구버전 주입이 이미 갈라 놓은 디스크 파일도 다음 touch 에서 온전해진다.
- 작업 결과: 실물 치유 실증: 저장소에서 갈라진 파일은 main-009 하나였고(전수 스캔, 아카이브 포함 0), 실제 wk backlog-update touch 로 작업 결과 5줄이 연속으로 복원됐다.
- 작업 결과: check_task_multivalue_fields 9 → 12 cases: case 10 = 주입이 묶음 끝 뒤 · case 11 = 갈라진 파일 되주입 후 갱신해도 고아 0 · case 12 = 자기 적용(실물 task 파일에 갈라진 묶음 없음).
- 검증 결과: 12/12 PASS · 되주입 2종 전부 잡힘(치유 제거 → case 11 FAIL, 주입 idx+1 복원 → case 10 FAIL) · task/writer 이웃 검사 green · self-application 8/8
- 후속 작업: 없음 — 43차의 중복 누적 계열(열거를 스칼라로 다룸)의 마지막 알려진 경로였다
