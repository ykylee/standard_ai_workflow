---
id: TASK-2026-08-14-main-017
status: done
created_at: 2026-08-14
source_anchor: generic-task-2026-08-14-main-017
source_path: backlog/2026-08-14.md
kind: generic
---

# TASK-2026-08-14-main-017 — 채널별 재실행 계약 표 — 5개 플러그인 채널의 재설치/업데이트 시 행동을 §7.0 에 고정

## 📝 Description

- 상태: done
- 우선순위: medium
- 요청일: 2026-08-14
- 담당:
- 호스트명:
- 호스트 IP:
- 영향 문서:
  - 

- 작업 내용: gap 2. smart update 는 bootstrap 규율일 뿐 — marketplace update/extension link/pi install 재실행이 각각 무엇을 하는지 실측해 표로.
- 완료 기준:

## 🛠️ Implementation / Content

- 진행 현황: `2026-08-18 10:08` 기준 4채널 실측 + 표 고정 + 누락 방지 검사
- 다음 세션 시작 포인트:
- 남은 리스크:

## ✅ Outcome

- 작업 결과: INSTALLATION §7.0.2 신설 — 채널별 (설치본의 정체 / 설치 재실행 / update / 페이로드가 낡았을 때 복구) 4열 표. 2026-08-18 이 호스트 실측이고 추정은 넣지 않았다
- 작업 결과: 핵심 실측: 계약이 채널마다 다르다. claude-code = 캐시 사본이고 plugin update 가 **버전 문자열만 보고 거절**(uninstall→install 만 복구) · codex = 캐시 사본이지만 plugin add 재실행이 marketplace 루트에서 **다시 복사**해 같은 버전에서도 갱신 · grok-build = 재설치 거부이고 update 는 'already live' 를 출력하면서 **실제로는 갱신 안 함**(원본에 표식 넣어 확정) · pi-dev = settings.json 의 packages[] **경로 참조**라 사본이 없고 갱신 자체가 불필요
- 작업 결과: claude 의 marketplace update 는 **클론만** 최신으로 당기고 설치 캐시는 그대로다 — 실측 중 설치본이 main-002 가 고친 잘린 줄을 그대로 들고 있었고 버전은 양쪽 다 1.2.0 이었다
- 작업 결과: 컨셉 §6: 원칙 B(재실행 안전)는 4채널 전부 성립 — 파괴도 중복도 없었다. 원칙 C(깔린 것은 스스로 말한다)는 **플러그인 채널에서 깨진다**고 명시하고 근거를 남겼다
- 작업 결과: check_installation_usage case 5 신설(4→5) — 재실행 계약 표가 §2.1 매트릭스의 플러그인 채널을 전부 덮는지 **파생으로** 검사. 손 목록 없음. 되주입으로 채널 누락 red 실증
- 검증 결과: 전량 2축 262/262 green (park 없음). grok·pi 는 측정을 위해 임시 설치 후 원상복구 — 둘 다 'No plugins installed' / packages=[] 로 복구 확인. gemini-cli 는 이 호스트에 CLI 가 없어 미실측이며 표에 그렇게 적었다
- 후속 작업: gemini-cli 채널 재실행 계약 미실측 — CLI 가 있는 호스트에서 표의 빈 행을 채운다
- 후속 작업: TASK-2026-08-14-main-018 페이로드 해시 비교 — 이번 실측이 그 필요성의 직접 근거다 (버전 동일·내용 상이가 실제로 관측됐고 채널이 스스로 못 고쳤다)
