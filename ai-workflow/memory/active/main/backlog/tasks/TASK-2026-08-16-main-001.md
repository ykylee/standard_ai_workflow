---
id: TASK-2026-08-16-main-001
status: done
created_at: 2026-08-16
source_anchor: generic-task-2026-08-16-main-001
source_path: backlog/2026-08-16.md
kind: generic
---

# TASK-2026-08-16-main-001 — backlog-update update 모드의 새 daily index 이월 결함 — 두 번째 task 부터 cannot_determine 조용한 스킵

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-08-16
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: 실측 2026-08-16: 날짜가 바뀐 뒤 update 는 (a) 그날 첫 호출이면 create_daily_backlog 로 새 index 를 만들며 통과하지만 (b) 그날 index 가 이미 있고 해당 task 항목이 없으면 cannot_determine 으로 apply 를 스킵한다. 이때 최상위 status 는 ok 라 호출자가 미반영을 놓친다. in_progress task 를 새 날짜 index 로 이월하는 도구 경로도 없다 — 이번엔 형식 보존 손 append 로 우회.
- Completion criteria:

## 🛠️ Implementation / Content

- Progress: `2026-08-17 00:16` 기준 이월 경로 신설 + cannot_determine 의 status 정직화 완료
- Progress: `2026-08-17` **2회째 재현 — 이번엔 게이트를 막았다.** 세션 도중 날짜가 바뀌어 `2026-08-17.md` 가 새로 생겼는데, 아직 `in_progress` 인 main-009 가 그 index 로 이월되지 않았다. `--mode update` 는 `operation_type=cannot_determine` / `task_found=false` / `apply_status=skipped` 를 내면서 **최상위 `status` 는 `ok`** 였다. 결과로 linter 가 `task_status_mismatch` (latest_backlog 에 main-009 없음) 로 warning → `check_self_application` red. **조용히 끝나지 않고 커밋 게이트를 세운다.** 덤으로, 이 결함을 task 파일에 기록하려는 `--mode update` 호출 **자신도 같은 이유로 스킵됐다** (main-001 역시 08-16 index 에만 있다). 이번에도 형식 보존 손 append 로 우회.
- Next session starting point: `backlog_update` 의 update 경로에서 (1) 대상 task 가 오늘 index 에 없으면 **task 파일 SSOT 를 근거로 오늘 index 에 항목을 이월 생성**하고, (2) 그럴 수 없으면 최상위 `status` 를 `ok` 로 두지 않는다 (`cannot_determine` 은 성공이 아니다). 되주입: 어제 index 에만 있는 task 를 오늘 update 해 index 이월과 non-ok status 를 양방향으로 고정.
- Remaining risks: 우회가 손 append 라 날짜 롤오버마다 반복된다. 롤오버가 세션 중간에 일어나면 게이트가 red 로 멈춘다 (2026-08-17 실측).

## ✅ Outcome

- Result: update 경로에서 오늘 index 에 없는 task 를 만나면, task SSOT(backlog/tasks/<id>.md) 존재 여부로 갈린다 — 있으면 carry_over_entry 로 오늘 index 에 이월 항목을 만들고 갱신을 반영, 없으면 cannot_determine. daily index 는 '그날 손댄 목록' 이고 SSOT 는 task 파일이라는 구분을 코드가 처음으로 지킨다
- Result: cannot_determine 의 최상위 status 를 ok → warning 으로. 아무것도 안 썼으면 그렇게 말한다 — 조용한 미반영의 뿌리는 판정이 아니라 이 보고였다
- Result: 병합·append 경로는 손대지 않았다. update_merge 는 matched_task 가 아니라 task_ssot_path.exists() 를 보고 있었고, _upsert_index_block 은 항목이 없으면 이미 append 한다 — 막고 있던 것은 판정 한 줄뿐이었다
- Result: check_backlog_carry_over.py 5 cases 신설 — 이월/본문 보존/상태 보존/SSOT 부재 시 non-ok/같은 날 재갱신은 여전히 update_entry(반대 방향 확대 방지)
- Verification: 전량 2축 262/262 green (143.82s / 138.08s). 되주입(이월 분기 제거 + status 항상 ok)에서 5 cases 중 4건 red 실증
- Follow-up:
