---
id: TASK-2026-08-16-main-001
status: planned
created_at: 2026-08-16
source_anchor: generic-task-2026-08-16-main-001
source_path: backlog/2026-08-16.md
kind: generic
---

# TASK-2026-08-16-main-001 — backlog-update update 모드의 새 daily index 이월 결함 — 두 번째 task 부터 cannot_determine 조용한 스킵

## 📝 Description

- 상태: planned
- 우선순위: medium
- 요청일: 2026-08-16
- 담당:
- 호스트명:
- 호스트 IP:
- 영향 문서:
  - 

- 작업 내용: 실측 2026-08-16: 날짜가 바뀐 뒤 update 는 (a) 그날 첫 호출이면 create_daily_backlog 로 새 index 를 만들며 통과하지만 (b) 그날 index 가 이미 있고 해당 task 항목이 없으면 cannot_determine 으로 apply 를 스킵한다. 이때 최상위 status 는 ok 라 호출자가 미반영을 놓친다. in_progress task 를 새 날짜 index 로 이월하는 도구 경로도 없다 — 이번엔 형식 보존 손 append 로 우회.
- 완료 기준:

## 🛠️ Implementation / Content

- 진행 현황: `2026-08-16 22:42` 기준 실측 2026-08-16: 날짜가 바뀐 뒤 update 는 (a) 그날 첫 호출이면 create_daily_backlog 로 새 index 를 만들며 통과하지만 (b) 그날 index 가 이미 있고 해당 task 항목이 없으면 cannot_determine 으로 apply 를 스킵한다. 이때 최상위 status 는 ok 라 호출자가 미반영을 놓친다. in_progress task 를 새 날짜 index 로 이월하는 도구 경로도 없다 — 이번엔 형식 보존 손 append 로 우회.
- 진행 현황: `2026-08-17` **2회째 재현 — 이번엔 게이트를 막았다.** 세션 도중 날짜가 바뀌어 `2026-08-17.md` 가 새로 생겼는데, 아직 `in_progress` 인 main-009 가 그 index 로 이월되지 않았다. `--mode update` 는 `operation_type=cannot_determine` / `task_found=false` / `apply_status=skipped` 를 내면서 **최상위 `status` 는 `ok`** 였다. 결과로 linter 가 `task_status_mismatch` (latest_backlog 에 main-009 없음) 로 warning → `check_self_application` red. **조용히 끝나지 않고 커밋 게이트를 세운다.** 덤으로, 이 결함을 task 파일에 기록하려는 `--mode update` 호출 **자신도 같은 이유로 스킵됐다** (main-001 역시 08-16 index 에만 있다). 이번에도 형식 보존 손 append 로 우회.
- 다음 세션 시작 포인트: `backlog_update` 의 update 경로에서 (1) 대상 task 가 오늘 index 에 없으면 **task 파일 SSOT 를 근거로 오늘 index 에 항목을 이월 생성**하고, (2) 그럴 수 없으면 최상위 `status` 를 `ok` 로 두지 않는다 (`cannot_determine` 은 성공이 아니다). 되주입: 어제 index 에만 있는 task 를 오늘 update 해 index 이월과 non-ok status 를 양방향으로 고정.
- 남은 리스크: 우회가 손 append 라 날짜 롤오버마다 반복된다. 롤오버가 세션 중간에 일어나면 게이트가 red 로 멈춘다 (2026-08-17 실측).

## ✅ Outcome

- 작업 결과:
- 후속 작업:
