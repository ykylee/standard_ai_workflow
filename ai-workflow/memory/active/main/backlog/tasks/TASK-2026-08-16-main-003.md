---
id: TASK-2026-08-16-main-003
status: done
created_at: 2026-08-16
source_anchor: generic-task-2026-08-16-main-003
source_path: backlog/2026-08-16.md
kind: generic
---

# TASK-2026-08-16-main-003 — check_deprecation_3rd_cycle 의 제외 목록이 죽어 있다 — rel 기준과 제외 항목 기준이 어긋난다

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-08-16
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/tests/check_deprecation_3rd_cycle_v0_15_4.py`

- Description: case 1 의 _scan_python_files_for_deprecation_warns 가 rel = py.relative_to(REPO_ROOT) 로 경로를 만드는데, 제외 항목은 WORKFLOW_SOURCE 기준 상대경로('build' / '.venv' / 'tests')다. 'workflow-source/.venv/...' 는 '.venv' 로 시작하지 않으므로 제외가 한 건도 성립하지 않는다. cases 2·3 도 같은 패턴(line 110/136)이라 제외가 동일하게 죽어 있고, 스캔 regex 가 거기서 아무것도 못 찾아 우연히 green 일 뿐이다. 로컬에 workflow-source/.venv 가 있으면 site-packages 의 서드파티 warnings.warn 16건이 저장소 결함으로 보고된다 (2026-08-16 실측). CI 는 그 디렉터리가 없어 green — 로컬만 red 인 비대칭이다.
- Completion criteria: rel 기준과 제외 항목 기준을 일치시킨다 (WORKFLOW_SOURCE 상대로 통일)
- Completion criteria: cases 1·2·3 모두 동일하게 교정한다 — 우연히 green 인 두 건도 같은 결함이다
- Completion criteria: workflow-source/.venv 와 build/ 를 만든 상태에서 green 임을 되주입으로 실증한다

## 🛠️ Implementation / Content

- Progress: `2026-08-17 00:31` 기준 제외 기준 통일 + 제외 규칙 자체를 회귀로 고정
- Next session starting point:
- Remaining risks: 제외가 되살아나면 그동안 가려져 있던 tests/ 내부의 진짜 emit 이 새로 드러날 수 있다

## ✅ Outcome

- Result: 경로 기준을 WORKFLOW_SOURCE 상대로 통일하고 _iter_source_files 하나로 모았다 — cases 1·2·3 이 각자 제외 목록을 들고 있던 구조 자체를 없앴다
- Result: 판정을 문자열 startswith → 경로 조각(_is_excluded)으로. startswith 는 buildtools/ 를 build 로 오인하고 중첩된 .venv 를 놓친다. EXCLUDED_DIR_NAMES 는 어느 깊이에서든 잡는다
- Result: case 1 의 허용 목록 비교에서 endswith 완화를 제거했다 — 그 완화가 기준 어긋남을 가리고 있었다 (정확 비교로 성립)
- Result: case 4 신설 (3→4): 합성 경로 7종으로 제외 규칙을 직접 판정 + 원 결함을 실행 가능한 단언으로 보존 + 제외 대상이 실제로 스캔에 새는지 관찰. cases 1~3 은 제외가 통째로 죽어도 조용히 green 이었다 — 대상 디렉터리가 평소엔 없기 때문이다
- Verification: 전량 2축 262/262 green. **로컬 workflow-source/.venv 를 비켜 두지 않고** 4/4 PASS — 세션 내내 하던 수작업이 없어졌다. 되주입(옛 REPO_ROOT startswith 복원)에서 case 1·4 red 실증
- Follow-up: 게이트의 로컬 유래 red 는 이제 untracked AGENTS.md(check_docs + check_self_application) 하나만 남았다 — 소유자 판단 대기
