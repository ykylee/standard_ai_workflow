---
id: TASK-2026-08-18-main-003
status: done
created_at: 2026-08-18
source_anchor: generic-task-2026-08-18-main-003
source_path: backlog/2026-08-18.md
kind: generic
---

# TASK-2026-08-18-main-003 — 배포본에서 죽는 workflow-source 경로 참조 — wk 명령 6종 실측

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-18
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/tools/wiki_emit.py`
  - `workflow-source/workflow_kit/tools/install_pre_push_hook.py`
  - `workflow-source/workflow_kit/tools/rotate_workflow_logs.py`
  - `workflow-source/workflow_kit/tools/seed_workspace_memory.py`
  - `workflow-source/workflow_kit/tools/claim_workspace.py`
  - `workflow-source/workflow_kit/tools/score_wiki_trend.py`

- Description: 패키지 코드가 저장소 체크아웃 레이아웃(workflow-source/, scripts/, tools/hooks/)을 런타임에 참조한다. editable 설치라 이 호스트에서는 안 드러났다.
- Completion criteria: 패키지 코드가 자기 모듈을 '파일 경로'로 재실행하지 않는다 — import 또는 python -m 로 부른다
- Completion criteria: wheel 미포함 자산(scripts/ · tools/hooks/ · reverse-engineering/)을 런타임에 참조하지 않거나 부재를 명시적으로 보고한다
- Completion criteria: 비-editable wheel 설치 + 소비자 프로젝트에서 대상 wk 명령이 도는지 검사로 고정 (editable 로는 영원히 green 이다)
- Completion criteria: wk wiki-emit 은 이 저장소에서도 이미 죽어 있다 — 우선 복구

## 🛠️ Implementation / Content

- Progress: `2026-08-18 11:15` 기준 패키지가 체크아웃 레이아웃에 기대지 않게
  - **실측 red 3건**
    - `wk wiki-emit` — `<git루트>/workflow-source/tools/refresh_wiki_memory.py` 를 부른다. 그 파일은 **어디에도 없다** (v1.1.8 에 `workflow_kit/tools/` 로 옮겼고 v1.2.0 에 구경로 shim 을 drop, TASK-2026-08-13-main-005). 배포본이 아니라 **이 저장소에서 지금 죽는다**: `can't open file .../workflow-source/tools/refresh_wiki_memory.py`.
    - `wk rotate-workflow-logs` — 기본 대상이 `memory_active_dir(REPO_ROOT)/main/session_handoff.md`. 배포본에서 `<venv>/lib/python3.14/ai-workflow/...` 를 열려다 `FileNotFoundError`. 브랜치도 `main` 하드코딩이라 branch-scoped 규약과 어긋난다.
    - `wk install-pre-push-hook status` — `_git_root(REPO_ROOT)` 가 소비자 cwd 가 아니라 **모듈 위치**를 묻는다 → git 저장소인 소비자 프로젝트에서 `ERROR: not a git repository`. 게다가 `HOOK_SOURCE` 는 wheel 미포함 자산이다.
  - **같은 패턴, 미실행 확인**: `seed-workspace-memory`(`SOURCE_ROOT/scripts/generate_workflow_state.py`, "생성기 부재" 로 graceful degrade) · `claim-workspace`(`SOURCE_ROOT/workflow_kit/tools/seed_workspace_memory.py` 를 **파일 경로로** 실행 + `PYTHONPATH=SOURCE_ROOT`) · `score-wiki-trend`(같은 방식, 현재는 조용히 `(empty history)`) · `session-start`(archive 분기에서만 — **기본 경로는 정상**, 소비자 프로젝트에서 확인) · `fill-reverse-engineering-artifacts`(`SOURCE_ROOT/reverse-engineering` 템플릿).
  - **무해로 판정**: `SOURCE_ROOT` 를 `sys.path.insert` 에만 쓰는 다수(`doc_sync`·`backlog_update`·`refresh_state` 등)는 없는 경로를 넣는 no-op 이고 설치본에서 import 는 정상 해결된다. 저장소 개발 전용(`release_pipeline*`·`check_packaging`·`release_status`·`migrate_*`)도 대상 밖. MCP `read_only_registry` 의 `script_path=SOURCE_ROOT/mcp_servers/...` 는 **설명용 메타데이터**이지 실행 경로가 아니다 — 실제 서버는 `-m workflow_kit.server.read_only_jsonrpc` 로 뜨고, 소비자 프로젝트에서 `initialize` → `tools/call latest_backlog` 왕복 **정상 확인**.
  - **왜 안 걸렸나**: 전량 검사도 `wk` 도 전부 저장소 체크아웃(=editable)에서 돈다. `check_source_without_runtime_layer` 는 런타임 레이어 부재만 보고 **패키지 설치 레이아웃은 안 본다**. `audit_root_anchors` 는 "어느 workspace 를 보는가"(R2/R3)를 재지 "그 경로가 배포본에 존재하는가"는 안 잰다 — 그래서 0 미선언인데도 위 3건이 살아 있었다.
- Next session starting point: `wk wiki-emit` 부터 (저장소에서 이미 죽어 있다). 그다음 `rotate-workflow-logs` · `install-pre-push-hook`.
- Remaining risks: 검사를 wheel 설치로 세우면 게이트 시간이 는다 — 전량이 아니라 **별도 축**(SDK 매트릭스처럼 push 전 1회)이 맞는지 판단 필요.

## ✅ Outcome

- Result: 정공법 3가지로 갈렸다. ①**자기 모듈은 -m 으로 부른다** — 파일 경로 재실행을 전부 제거하고 규칙을 `common/child_process.py`(module_command / child_env) 한 곳에 뒀다: wiki_emit · score_wiki_trend · session_start(archive) · claim_workspace. seed_workspace_memory 는 subprocess 자체를 없애고 `refresh_workflow_state_cache` 를 직접 호출(그 스크립트가 한 줄 wrapper 였다). ②**런타임 자산은 패키지 안으로** — `tools/hooks/`·`reverse-engineering/`·`templates/prompts/` 를 `workflow_kit/assets/` 로 옮기고 pyproject package-data 에 선언. ③**workspace 는 cwd 에서** — rotate_workflow_logs 의 기본 handoff 를 discover_project_profile_path + workflow_branch_dir 로(브랜치 'main' 하드코딩도 제거), install_pre_push_hook 의 git root 를 _git_root(Path.cwd()) 로. 검사: check_deployed_layout 신설 4 cases(모듈 유도 루트에서 체크아웃 레이아웃 역산 금지 / 자산이 패키지 안 / package-data 선언 / 파일 경로 재실행 금지) + check_pre_push_hook case 8 신설(7→8, 1~7 은 _git_root 를 monkeypatch 해서 '어느 저장소를 고르는가' 를 한 번도 안 쟀다). 되주입 3종 전부 red 실증(첫 시도는 REPO_ROOT 정의까지 지워 import 조차 안 되는 무효 주입이었고, 원 결함 형태로 다시 넣어 확정). **배포본 e2e**: 새 wheel 을 빈 venv 에 깔고 가짜 소비자 프로젝트에서 session-start/refresh-state/rotate-workflow-logs/install-pre-push-hook(status+install --apply)/score-wiki-trend/fill-reverse-engineering-artifacts 전부 정상, 자산 13개가 wheel 에 실린 것 확인. **남긴 것**: wiki 3-step 의 하위 두 단계가 각각 스키마·레이아웃 드리프트로 죽어 있다 — 경로 결함이 아니라 별건이라 TASK-2026-08-18-main-004 로 분리했다.
- Verification: check_deployed_layout 4/4 · check_pre_push_hook 8 case ALL PASS · check_reverse_engineering 19/19 · 되주입 3종 red · 배포본 wheel e2e 6명령 정상 · 전량 2축 263/263 green
- Follow-up: TASK-2026-08-18-main-004 (wiki 3-step 하위 두 단계)
