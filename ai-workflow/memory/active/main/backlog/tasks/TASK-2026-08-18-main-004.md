---
id: TASK-2026-08-18-main-004
status: done
created_at: 2026-08-18
source_anchor: generic-task-2026-08-18-main-004
source_path: backlog/2026-08-18.md
kind: generic
---

# TASK-2026-08-18-main-004 — wiki 3-step 파이프라인의 하위 두 단계가 죽어 있다 — 스키마·레이아웃 드리프트

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-08-18
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `ai-workflow/wiki/sources/.gitkeep`
  - `workflow-source/tests/check_wiki_emit_pipeline.py`
  - `workflow-source/tests/check_v0_7_17_wiki_in_repo_isolation.py`
  - `workflow-source/tests/check_v0_7_23_wiki_cross_link.py`

- Description: wiki-emit 이 살아나자 드러난 하위 결함 2건
- Completion criteria: 1단계 refresh-raw 의 raw mirror write 경로가 제거되고 state.json 이중 writer 가 사라진다
- Completion criteria: 2단계 emit_wiki_l2_body 가 현재 in-repo 레이아웃에서 동작하고 emit 대상이 0이 아니다
- Completion criteria: 3단계가 현재 SSOT 에서 L2 stub 4개를 파생하고 last_touched 를 실제 emit 일자로 쓴다
- Completion criteria: 검사가 dry-run 뿐 아니라 apply 경로를 실측한다

## 🛠️ Implementation / Content

- Progress: `2026-08-19 00:11` 기준 화석 은퇴 + 파생 뷰 재정의 (1단계 제거 / 2단계 수리 / 3단계 재작성)
  - **1단계 `refresh_wiki_memory --refresh-raw`** — `update_state_json` 이 `data["memory"]["last_freeze"]` 를 쓰는데 현재 `state.json` 에 `memory` key 가 없다 (`KeyError: 'memory'`). state.json 스키마가 그 사이 바뀌었다.
  - **2단계 `emit_wiki_l2_body`** — L1 미러를 `RAW_MIRROR/<project>/ai-workflow/wiki/` 아래로 가정하는데 실제로는 `ai-workflow/wiki/` 다 (`ValueError: … is not in the subpath of …/standard-ai-workflow/ai-workflow/wiki`). 레이아웃이 그 사이 바뀌었다.
  - **3단계 `--emit-l2`(stub last_touched) 는 `rc=0` 이지만 정상이 아니다** — 진단 실행 한 번으로 `ai-workflow/wiki/sources/` 4개의 `last_touched` 가 **2026-07-22 → 2026-06-14 로 뒷걸음쳤다**(`--since` 기본값 2026-06-10 을 그대로 찍는다). 그 결과 wiki score 의 `lifecycle` 이 5.0 → 0.0 이 되고 `check_wiki_trend` 가 red 가 된다. 2026-08-18 세션에서 `git checkout` 으로 원복했다. rc=0 이라 **조용히 퇴행시킨다** — 세 단계 중 가장 위험하다.
  - 참고: `path_in_active` 의 docstring 이 "규칙을 복사해 둔 caller 가 layout 변경을 놓치는 것이 `refresh_wiki_memory` red 의 원인이었다" 고 이미 적고 있다 — 같은 자리에서 또 났다.
- Next session starting point: [열림, 소유자 판단] `emit_wiki_l2_body` 의 L1→L2 갭 85개. `.gitkeep` 계약은 L1 wiki page 마다 L2 파생 뷰를 두라고 하지만, 그 근거(외부 vault retrieval)는 v0.7.17 in-repo 전환 때 사라졌다 — in-repo 에서 L1 은 이미 검색 가능하므로 85장은 절삭 사본 ~170KB 증가일 뿐이다. 그래서 `--bootstrap-missing` 을 **기본 off** 로 두고 갭 개수만 보고한다. 켜는 것도, 계약을 'L2 = memory 파생 4종' 으로 좁히는 것도 한 줄이면 된다.
- Remaining risks: `emit_wiki_l2_body` 의 L1→L2 경로는 이제 **동작하지만 이 저장소에 살아 있는 입력이 없다**(후보 0, 갭 85). 그 0 은 이전의 거짓 0(크래시 + 일회성 게이트)과 달리 **측정된** 0 이지만, 계약 자체의 존폐는 미결이다.
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: **wk wiki-emit 이 3-step → 2-step 이 됐고, 파이프라인이 처음으로 끝까지 실행된다.** 검사 263→264. ① **1단계 은퇴** — write 대상 4개가 전부 무너져 있었다. 핵심은 스키마 드리프트가 아니라 **소유권**이다: state.json 은 정본 §11.2 의 생성 산출물이고 생성기는 `wk refresh-state` 하나라, 이 단계는 **두 번째 writer** 였다. 은퇴는 조용한 no-op 이 아니라 **write 0 + 사유 보고**(rc=0)이고, 함수 자체를 지워 `check_refresh_wiki_memory` 가 정적으로 부활을 막는다 — 분기로만 막으면 다음 사람이 함수를 다시 부른다. ② **2단계는 vault 화석 3종이 전부 실행 경로 위에 있었다** — 이중 경로 · `RAW_MIRROR.parts.index("raw")` · **정의된 적 없는 `VAULT_ROOT`**(앞의 둘을 고쳐도 그 다음이 NameError). 고쳐도 할 일이 없던 진짜 이유는 **게이트가 `<needs content>` placeholder 하나**여서 — 한 번 emit 된 page 는 L1 이 아무리 바뀌어도 영원히 대상이 아니었다. 게이트를 **신선도**(placeholder OR L1 이 L2 보다 새로움)로 바꿨다. 본문 전체를 갈아끼우게 되므로 `> Generated:` 표식 없는 page 는 **manual 로 보고 건드리지 않는다**. ③ **3단계 재작성** — 2026-06-14 스냅샷 축자 재생성을 걷어내고 현재 SSOT(state.json / 최신 backlog / handoff / wiki log)에서 파생한다. `last_touched` 는 실제 emit 일자고, 결과 바이트가 같으면 write 하지 않는다(`unchanged`) — 진단 실행이 저장소를 바꾸지 않는다. L1 이 없는 stub 은 채우지 않고 `missing_l1` 로 밝힌다. ④ **날짜 있던 붕괴를 막았다** — L2 4개의 `last_touched` 가 2026-07-22 라 **2026-08-21 에 lifecycle 5.0→0.0, overall 4.71 A→3.88** 이 예약돼 있었고, 그걸 갱신할 유일한 도구가 67일 전으로 되돌리고 있었다(7/22 값은 사람이 커밋 `dcbf2af7` 로 올린 것). 지금은 도구가 유지한다. ⑤ **검사가 apply 를 잰다** — 이전 8 cases 는 전부 dry-run 이라 두 크래시를 구조적으로 못 봤다. `check_refresh_wiki_memory` 11 cases 재작성 + `check_wiki_emit_pipeline` 11 cases 신설, 되주입 6종(last_touched 하드코딩 · status reviewed · state.json writer 부활 · VAULT_ROOT · one-shot 게이트 · 사람 글 보호 제거) 전부 red 실증.
- Verification: check_refresh_wiki_memory 11/11 · check_wiki_emit_pipeline 11/11 · check_v0_7_17_wiki_in_repo_isolation 11/11 · check_v0_7_23_wiki_cross_link 5/5 · wiki 필터 15개 전부 green · check_self_application 8/8. 되주입 6종 red 실증. 개수 표기 3곳 갱신(INSTALLATION_AND_USAGE · CODE_INDEX · Beta-v1.2.0 노트) 263→264.
- Follow-up:
