---
id: TASK-2026-08-18-main-005
status: done
created_at: 2026-08-18
source_anchor: generic-task-2026-08-18-main-005
source_path: backlog/2026-08-18.md
kind: generic
---

# TASK-2026-08-18-main-005 — 드리프트 감지 — 마커가 아니라 페이로드 해시로 비교

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-08-18
- 담당:
- 호스트명:
- 호스트 IP:
- 영향 문서:
  - `workflow-source/workflow_kit/deploy_doctor.py`
  - `workflow-source/core/workflow_deployment_idempotency.md`

- 작업 내용: gap 3 잔여: 버전이 같고 내용만 낡은 상태를 wk doctor 가 못 본다
- 완료 기준: 버전이 같고 내용만 다른 설치본을 drift 로 보고한다
- 완료 기준: drift.limitation 의 '내용 드리프트는 못 본다' 문구가 사실이 아니게 된다
- 완료 기준: report-only 계약 유지 — 아무것도 쓰지 않는다

## 🛠️ Implementation / Content

- 진행 현황: 전제가 먼저 막혔다 — 정본 렌더러 render_agent_plugin() 이 설치본에서 통째로 죽었다(_project_table 이 체크아웃 경로만 봤다). 설치 metadata fallback 을 넣어 풀었다.
- 다음 세션 시작 포인트:
- 남은 리스크:

## ✅ Outcome

- 작업 결과: wk doctor 에 content_drift 절 신설. 지키는 것 넷: ①정본은 **생성기와 같은 함수**(render_agent_plugin) — 기준을 따로 두면 기준이 드리프트한다 ②기대치는 채널별 **파생**(PLUGIN_HARNESS_SPECS.include_prefixes) — codex 는 매니페스트·MCP·skills 만 담아서 payload 전체를 기대하면 정상 설치가 '없음 10건' 으로 보고됐다(실측) ③사본 거주지도 registry(PLUGIN_INSTALL_CACHES); 사본 없는 채널(pi-dev=경로 참조)·미실측(gemini-cli)은 not_applicable 로 밝힌다 ④report-only 유지. 부수: _project_table() 이 설치 metadata 로 떨어지게 해 소비자 호스트에서도 정본 렌더가 된다(mypy strict 로 PackageMetadata 매핑 접근). check_deploy_doctor 9→13 cases, 되주입(내용 비교 제거) 으로 핵심 case red 실증. 문서: 컨셉 §7 gap 3 → 해소, INSTALLATION §7.0.2 꼬리를 '한계' 에서 '복구 열' 로.
- 검증 결과: check_deploy_doctor 13/13 · 되주입 red 실증 · 이 호스트 실측(claude-code 12개·codex 10개 in-sync) · 배포본 wheel 에서 정본 렌더 확인 · 전량 2축 263/263 green
- 후속 작업:
