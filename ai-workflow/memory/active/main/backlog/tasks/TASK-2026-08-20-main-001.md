---
id: TASK-2026-08-20-main-001
status: done
created_at: 2026-08-20
source_anchor: generic-task-2026-08-20-main-001
source_path: backlog/2026-08-20.md
kind: generic
---

# TASK-2026-08-20-main-001 — wiki L2 계약을 memory 파생 4종으로 좁힌다 — L1→L2 경로 은퇴 + 지표 분모 재정의

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-08-20
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/tests/check_v0_7_17_wiki_in_repo_isolation.py`
  - `workflow-source/tests/check_v0_7_23_wiki_cross_link.py`

- Description: L2 계약을 'memory SSOT 파생 4종' 으로 좁힌다 (소유자 결정 2026-08-20)
- Completion criteria: emit_wiki_l2_body 의 L1→L2 경로와 --bootstrap-missing 이 은퇴하고 기계가 파일에서 사라진다
- Completion criteria: wk wiki-emit 이 1-step 이 된다
- Completion criteria: L2 계약(.gitkeep)이 'memory SSOT 파생 4종' 으로 고정된다
- Completion criteria: discoverability·lifecycle 의 분모가 선언된 L2 집합이 되어 stub 사라짐을 잡는다

## 🛠️ Implementation / Content

- Progress: `2026-08-20 08:58` 기준 L2 계약을 'memory SSOT 파생 4종' 으로 좁힌다 (소유자 결정 2026-08-20)
- Next session starting point: [관찰, 신규 후보] **날짜 롤오버 때 열린 task 가 최신 backlog index 에서 조용히 빠진다.** v1.2.1(main-016)이 *갱신하는* task 의 이월은 고쳤지만, 손대지 않은 다른 `in_progress` task 는 새 일자 index 에 안 들어간다 — 그러면 linter 의 `task_status_mismatch` 가 뜨고 커밋 게이트가 막힌다. **2세션 연속**(49차·50차) 손으로 이월해 풀었다. 자동 이월이 맞는지, 아니면 linter 가 '최신 index' 대신 task SSOT 를 봐야 하는지가 설계 질문이다.
- Remaining risks:

## ✅ Outcome

- Result: **L2 의 정의가 좁아졌고, 그 정의가 검사로 고정됐다.** `wk wiki-emit` 이 2-step → **1-step**. ① **계약**: `L2 = wiki 모양이 *아닌* SSOT 를 wiki 검색용으로 압축한 뷰` — 4종뿐이고 늘어나지 않는다. L1 wiki page 는 정의상 제외된다(이미 wiki 모양이고 이미 검색된다). 정본은 `refresh_wiki_memory.L2_STUBS`, 설명은 `.gitkeep`. ② **은퇴 형태는 49차와 같다** — 진입점(`wk emit-wiki-l2-body`)은 남기되 write 0 + 사유 보고(rc=0), **기계는 파일에서 지운다**(`RAW_MIRROR`/`L2_SOURCES`/`_detect_repo_root`/`update_l2_full` 부재를 검사가 정적으로 고정). 옛 인자는 계속 받는다 — 박혀 있던 호출이 argparse 오류로 죽는 것보다 **왜 아무것도 안 했는지 듣는 편**이 낫다. ③ **부수로 지표 결함 2건이 드러났고 둘 다 고쳤다.** (a) **분모가 찾은 파일 수**였다 — stub 3장을 지우면 total=1/searchable=1 이라 **5.0 이 그대로** 나왔다. 즉 *사라짐* 이 지표에 전혀 안 잡혔다(저장소 규칙: 있는가가 아니라 몇 개인가). 분모를 **선언된 집합**으로 바꿨고, 부재는 '측정 대상 아님' 이 아니라 검색 불가/최악 신선도로 센다. (b) **placeholder 판정이 부분 문자열**이었다 — handoff 파생 뷰가 본문에서 `<needs content>` 를 *언급*했다는 이유로 검색 불가로 집계돼 discoverability 가 5.0→3.75 로 떨어졌다(이번 세션 실측). 줄 전체 일치로 앵커링. ④ **목록을 복제하지 않는다** — 지표가 생성기 상수를 import 하고, 검사가 '복제 0' 을 직접 확인한다.
- Verification: check_wiki_emit_pipeline 10/10(재작성) · check_v0_7_17_wiki_in_repo_isolation 11/11 · check_v0_7_23_wiki_cross_link 5/5 · wiki 필터 15개 green · check_self_application 8/8. 되주입 3종 red 실증(분모를 찾은 파일 수로 · placeholder 부분 문자열 · 지표가 목록 복제). 검사 개수는 264 유지(파일 재작성, 증감 없음). wiki score 4.71 A 유지.
- Follow-up:
