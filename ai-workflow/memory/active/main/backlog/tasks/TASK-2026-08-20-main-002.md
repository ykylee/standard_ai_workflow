---
id: TASK-2026-08-20-main-002
status: done
created_at: 2026-08-20
source_anchor: generic-task-2026-08-20-main-002
source_path: backlog/2026-08-20.md
kind: generic
---

# TASK-2026-08-20-main-002 — 날짜 롤오버 때 열린 task 가 mismatch 로 잡힌다 — linter 가 SSOT 대신 하루치 index 를 본다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-20
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/common/schemas/linter.py`
  - `workflow-source/schemas/generated_output_schemas.json`
  - `workflow-source/tests/check_workflow_linter.py`

- Description: linter 의 in_progress 대조 출처를 일자 index 에서 task SSOT 로 옮긴다
- Completion criteria: 날짜가 바뀌어도 손대지 않은 in_progress task 가 mismatch 로 잡히지 않는다
- Completion criteria: handoff 와 task SSOT 가 실제로 어긋나면 여전히 잡힌다 (검사 약화 아님)
- Completion criteria: state.json 이 낡았을 때(refresh-state 누락) 그것도 잡힌다

## 🛠️ Implementation / Content

- Progress: `2026-08-20 09:25` 기준 linter 의 in_progress 대조 출처를 일자 index 에서 task SSOT 로 옮긴다
- Next session starting point: 없음 — 2세션 연속 손으로 풀던 이월 작업이 사라진다.
- Remaining risks:

## ✅ Outcome

- Result: **linter 가 3자 대조의 세 번째 출처를 일자 index 에서 task SSOT 로 옮겼다.** 갈래는 실측으로 닫혔다 — 자동 이월은 오답이었다. `state.json` 의 `in_progress_items` 는 `state/builder._aggregate_from_appendonly_layout` 이 **`backlog/tasks/` 전체**를 집계해 만드는데, linter 는 `parse_backlog(latest_backlog_path)` 로 **하루치 index 하나**를 봤다. 일자 index 의 정의는 그 문서 스스로 '해당 일자의 task' 이므로 **어제 연 task 가 오늘 index 에 없는 것이 정상**이다 — 즉 결함은 이월 누락이 아니라 **출처 선택**이었다. 자동 이월을 택했다면 append-only 이력 문서를 매일 고쳐 쓰고 index 의 정의와도 싸웠을 것이다. ① **판정을 복제하지 않는다** — 새 규칙을 쓰지 않고 **state.json 생성기와 같은 함수**를 부른다. 린터가 생성기와 다른 규칙으로 '불일치' 를 외치는 것이 가장 나쁜 결과다. ② **레이아웃으로 출처를 고르고 결과에 적는다** — v0.14.0+ 는 `backlog/tasks/`, 그 이전은 일자 backlog(그 레이아웃의 SSOT). 폴백을 조용히 하지 않도록 `summary.in_progress_source` `{kind, path}` 를 출력 계약에 추가했다(additive, JSON Schema 재생성). ③ **검사가 약해지지 않았다. 오히려 하나 늘었다** — handoff 드리프트는 그대로 잡히고, **낡은 state.json**(`wk refresh-state` 누락)이 새로 잡힌다. 이전 조합(state ↔ 일자 index)으로는 **볼 수 없던** 상태다. ④ 실측: main-009 이월 항목을 지워 '손대지 않은 날' 을 재현하니 이전에는 red 였던 것이 green.
- Verification: check_workflow_linter 5→9 cases (롤오버 · handoff 드리프트 · 낡은 state.json · 레거시 폴백). 되주입 3종 red 실증(출처를 일자 index 로 되돌림 · handoff 다리 제거 · state.json 다리 제거). check_linter_config_resolution 9/9 · check_v0_7_22_linter_symlink_resolve 3/3 · check_handoff_done_cap · check_v0_7_15_config_thresholds · check_output_json_schema green. check_self_application 8/8. 검사 개수 264 유지.
- Follow-up:
