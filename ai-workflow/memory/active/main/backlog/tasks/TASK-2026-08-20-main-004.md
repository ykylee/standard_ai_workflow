---
id: TASK-2026-08-20-main-004
status: done
created_at: 2026-08-20
source_anchor: generic-task-2026-08-20-main-004
source_path: backlog/2026-08-20.md
kind: generic
---

# TASK-2026-08-20-main-004 — memory_index 3-tuple 관찰 — 저점 고착의 원인은 검색이 아니라 종료 절차 배선

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-08-20
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/core/global_workflow_standard.md`
  - `CLAUDE.md`
  - `AGENTS.md`
  - `workflow-source/tests/check_standard_single_source.py`

- Description: W-4 north-star 실측 + 승격 단계가 에이전트 문서 체인 밖이던 결함 수리
- Completion criteria: 3-tuple 실측치가 기록되고 저점 여부가 8/10 기준선의 조건으로 판정된다
- Completion criteria: suggest-memory-entries 가 에이전트가 읽는 문서(정본 → CLAUDE.md/AGENTS.md)에 실린다
- Completion criteria: 진입점이 정본 표 전체를 싣는지 검사가 대조한다 (대표 1개 단언 폐기)

## 🛠️ Implementation / Content

- Progress: **관찰 결과: 저점 그대로 — 8/10 기준선이 정한 판정 조건에 걸렸다.** ADR-006 회고(2026-08-10)는 "정직한 저점에서 출발했다. 오르는지는 다음 세션들의 실사용이 말해 준다. **항상 저점이면 W-2 의 컨텍스트 유도나 W-1 루프가 안 도는 것**" 이라고 판정 조건을 미리 적어 뒀다. 10일 뒤 실측: `query_diversity` **4 / 285** · `entries_new_30d` **2** · `distinct_entries_retrieved` **1 / 9** (보조 `hit_rate` 0.119). 즉 **285회 질의 동안 조회된 entry 가 1건**이고 9건 중 8건은 한 번도 안 읽혔다.

**원인은 검색이 아니라 배선이었다.** 회고가 종료 절차로 추가한 `wk suggest-memory-entries` 가 **`memory_index/README.md` 에만** 적혀 있었다 — `global_workflow_standard.md`·`CLAUDE.md`·`AGENTS.md` 어디에도 없다(grep 0건). 에이전트가 세션 시작에 읽는 문서 체인 밖이라 **아무도 그 단계를 돌지 않았다.** 실제로 지금 돌리니 **승격 후보 5건**이 대기 중이었다(main-017/019, main-002/004/005).

**수리.** 정본 §8.1 에 4단계로 넣고 §11.1 표에 명령을 추가 → `CLAUDE.md`/`AGENTS.md` 규칙 블록 재생성, 배포 사본 동기화, 스냅샷 재생성. **그리고 이 드리프트가 조용히 지나간 이유도 고쳤다**: `check_standard_single_source` 의 case 3 이 §11 표에서 **대표 1개만** 봤다(`memory_commands[0]`). 그래서 정본에 6번째 명령이 늘어도 진입점에 5개뿐인 채로 green 이었다 — 또 '있는가' 단언이다. 전 항목 대조로 바꾸고, **이 저장소 자신의 진입점**을 재는 case 를 신설했다(기존 case 는 임시 bootstrap 산출물만 봤다).

**새 절차를 이번 세션에 실제로 적용**했다 — 후보 5건을 판단해 1건 승격(`MEM-2026-08-20-001`, '있는가 단언이 개수·정체 결함을 가린다' 패턴). task 5건을 그대로 5 entry 로 만드는 대신 반복 관측된 패턴 하나로 묶었다. `entries_total` 9→10, `entries_new_30d` 2→3.
- Next session starting point: 다음 세션 종료 시 suggest-memory-entries 를 실제로 돌리고(이제 문서에 있다) 30일 뒤 3-tuple 을 다시 잰다.
- Remaining risks:

## ✅ Outcome

- Result: 관찰이 지표가 아니라 **배선 결함**을 찾아냈다. 지표는 정직했고(저점), 저점의 이유는 승격 루프가 에이전트 문서 체인 밖이라 한 번도 안 돈 것이었다. 다음 관찰의 관전 포인트: 종료 절차가 문서에 실린 뒤 entries_new_30d 와 distinct_entries_retrieved 가 움직이는가 — 안 움직이면 이번엔 진짜로 W-2(컨텍스트 유래 질의) 쪽이다.
- Verification: check_standard_single_source 10→11 cases (전 항목 대조 + 자기 적용 신설), 되주입 1종 red 실증. check_memory_index_cross / check_memory_governance_cross green. 3-tuple 재실측: entries_total 9→10, entries_new_30d 2→3.
- Follow-up:
