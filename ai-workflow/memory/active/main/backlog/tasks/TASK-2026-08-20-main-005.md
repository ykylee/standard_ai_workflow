---
id: TASK-2026-08-20-main-005
status: done
created_at: 2026-08-20
source_anchor: generic-task-2026-08-20-main-005
source_path: backlog/2026-08-20.md
kind: generic
---

# TASK-2026-08-20-main-005 — watch_transient_writer 의 고정 sleep 이 병렬 부하에서 깨진다 — 시간이 아니라 관측을 기다린다

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-08-20
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/tests/check_watch_transient_writer.py`

- Description: 게이트 slash 축 1회 red — 폴러 기아로 transient 가 되돌려진 뒤 관측
- Completion criteria: 병렬 부하에서 transient 관측이 시간 가정에 의존하지 않는다

## 🛠️ Implementation / Content

- Progress: 게이트(2026-08-20 slash 축)에서 `check_watch_transient_writer` 의 `test_transient_reinjection_is_captured` 가 **1회 red**. standalone·재실행은 green. **`REQUIRES_QUIET_REPO` 문제가 아니다** — 이 검사는 전부 temp 에서 논다(선언 불필요가 맞다). 문제는 **타이밍 가정**이었다: `SETTLE_S = 0.4` 의 근거가 "폴링 간격 0.02 의 20배 — 누락 여지 없음" 인데, 그건 *폴러가 실제로 20번 스케줄된다* 는 전제다. 전량은 16-way 병렬이라 감시자가 굶으면 0.4초 벽시계 안에 폴이 몇 번 도는지 보장되지 않고, 그 사이 테스트가 파일을 **되돌리면 transient 가 통째로 사라진다** (증상: `주입 marker 가 diff 에 없다`). 수리: 고정 sleep 대신 `_wait_for_events` 로 **관측을 기다린 뒤** 되돌린다 — `events.jsonl` 은 즉시 기록되므로 종료를 기다리지 않고 셀 수 있다. sleep 은 하한으로만 남는다. 검증: 8-way CPU 부하를 걸고 3회 연속 5/5.
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: 이번 세션의 mypy flake 와 **같은 계열**이다 — 둘 다 '부하 없는 상태에서 계산한 여유' 가 병렬에서 무너졌다. 다만 이쪽은 상류가 아니라 우리 검사라 바로 고칠 수 있었다.
- Verification: 8-way CPU 부하 아래 3회 연속 5/5 PASS, 전량 2축 264/264 green.
- Follow-up:
