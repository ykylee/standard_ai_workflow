---
id: TASK-2026-08-20-main-007
status: done
created_at: 2026-08-20
source_anchor: generic-task-2026-08-20-main-007
source_path: backlog/2026-08-20.md
kind: generic
---

# TASK-2026-08-20-main-007 — v1.3.0 릴리스 — 101 커밋 누적분 발행 + breaking 표기 판단 기준 문서화

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-08-20
- 담당:
- 호스트명:
- 호스트 IP:
- 영향 문서:
  - `workflow-source/tests/check_deploy_doctor.py`
  - `workflow-source/CHANGELOG.md`

- 작업 내용: 소유자 결정 minor. 외부 spec 버전 != 우리 API 버전 기준을 RELEASE.md 에 고정
- 완료 기준: RELEASE.md 에 `!` 표기 판단 기준(외부 spec 버전 vs 우리 API)이 적힌다
- 완료 기준: 버전이 1.3.0 으로 박히고 release-pipeline validate 가 통과한다
- 완료 기준: dry-run 검토 후 GitHub Release 가 발행된다

## 🛠️ Implementation / Content

- 진행 현황: `2026-08-20 12:33` 기준 소유자 결정 minor. 외부 spec 버전 != 우리 API 버전 기준을 RELEASE.md 에 고정
- 다음 세션 시작 포인트: [TASK-2026-08-14-main-009] 라벨 영어 전환이 이 릴리스 경계로 풀렸다 — 다음 세션의 첫 후보.
- 남은 리스크:

## ✅ Outcome

- 작업 결과: **v1.3.0 발행 완료** — https://github.com/ykylee/standard_ai_workflow/releases/tag/v1.3.0 (tag `v1.3.0`, asset 4종: wheel 814KB · sdist 695KB · claude-code plugin · codex plugin). ① **판단 기준 정본화** — `docs/RELEASE.md` §1.5 신설. `!` 는 "무언가 깨진다" 는 표시일 뿐 **무엇이** 깨지는지 말하지 않는다; 우리가 SemVer 로 보장하는 것은 이 패키지의 공개 API 이므로 4문항(공개 시그니처 / 진입점 소멸 / 소비자가 못 읽게 되나 / 외부 spec 버전)으로 등급을 본다 — **외부 spec 버전이 오른 것만으로는 major 가 아니다**. v1.3.0 의 `feat(okf)!` 를 적용 사례로 근거 4가지와 함께 박았다. ② **노트 재작성** — 자동 skeleton(102 커밋 나열)을 버리고 축 4개 + 도구 결함 수리로 재구성. §0 판정 주제는 '검사가 정작 재야 할 자리를 재고 있는가' 다. ③ **태그 표기 정정** — dry-run 이 만드는 태그는 `v1.3.0` 인데 노트는 `-beta` 를 달고 있었다. §2.2 가 v1.2.1 부터 접미사를 뗐고 도구도 그렇게 만든다 — 직전 `v1.2.0-beta` 가 옛 표기의 마지막이 됐다. ④ **버전 범프 파생물 13종** 재생성 (self-recover · doc-headers · read-only 스키마 3종 · output samples 24파일 · README/CODE_INDEX/INSTALLATION 스탬프 · 검사 상수 2개). ⑤ **부수 결함**: `check_deploy_doctor` fixture 가 설치 버전을 `"1.2.0"` 리터럴로 박고 있었다 — 그 case 의 전제가 "설치 버전 == 정본 버전, 내용만 낡음" 이므로 `__version__` 파생으로 바꿨다. 리터럴이면 릴리스마다 red 가 되고 그때 고치는 것은 계약이 아니라 그 시점 상수다.
- 검증 결과: 전량 2축 264/264 green (발행 직전) · mypy strict 197파일 0 errors · plugin_payload in_sync · twine check PASSED · gh release create rc=0. 발행 후 확인: draft=False, asset 4종 첨부.
- 후속 작업:
