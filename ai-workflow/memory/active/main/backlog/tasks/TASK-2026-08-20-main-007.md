---
id: TASK-2026-08-20-main-007
status: in_progress
created_at: 2026-08-20
source_anchor: generic-task-2026-08-20-main-007
source_path: backlog/2026-08-20.md
kind: generic
---

# TASK-2026-08-20-main-007 — v1.3.0 릴리스 — 101 커밋 누적분 발행 + breaking 표기 판단 기준 문서화

## 📝 Description

- 상태: in_progress
- 우선순위: high
- 요청일: 2026-08-20
- 담당:
- 호스트명:
- 호스트 IP:
- 영향 문서:
  - `workflow-source/releases/Beta-v1.3.0.md`
  - `workflow-source/workflow_kit/__init__.py`

- 작업 내용: 소유자 결정 minor. 외부 spec 버전 != 우리 API 버전 기준을 RELEASE.md 에 고정
- 완료 기준: RELEASE.md 에 `!` 표기 판단 기준(외부 spec 버전 vs 우리 API)이 적힌다
- 완료 기준: 버전이 1.3.0 으로 박히고 release-pipeline validate 가 통과한다
- 완료 기준: dry-run 검토 후 GitHub Release 가 발행된다

## 🛠️ Implementation / Content

- 진행 현황: `2026-08-20 11:50` 기준 소유자 결정 minor. 외부 spec 버전 != 우리 API 버전 기준을 RELEASE.md 에 고정
- 다음 세션 시작 포인트: 커밋·push 후 release-pipeline dist/release 를 dry-run 으로 검토하고, 소유자 확인 뒤 --apply 로 GitHub Release 발행.
- 남은 리스크:

## ✅ Outcome

- 작업 결과: **v1.3.0 릴리스 준비 완료.** ① **판단 기준을 정본화** — `docs/RELEASE.md` §1.5 신설: `!` 는 "무언가 깨진다" 는 표시일 뿐 **무엇이** 깨지는지 말하지 않는다. 우리가 SemVer 로 보장하는 것은 **이 패키지의 공개 API** 이므로, 등급은 (공개 시그니처 / 진입점 소멸 / 소비자가 못 읽게 되는가 / 외부 spec 버전)의 4문항으로 본다 — **외부 spec 버전이 오른 것만으로는 major 가 아니다**. v1.3.0 의 `feat(okf)!` 를 적용 사례로 근거 4가지와 함께 박았다. ② 버전 1.2.0 → **1.3.0** (pyproject + `workflow_kit.__version__` + 플러그인 매니페스트 5종 동기화). ③ 릴리스 노트 작성 — 자동 skeleton(102 커밋 나열)을 버리고 **축 4개 + 도구 결함 수리**로 재구성했다. 노트의 §0 릴리스 판정은 이 사이클의 공통 주제를 '검사가 정작 재야 할 자리를 재고 있는가' 로 잡았다 — 같은 모양의 결함이 다섯 자리에서 반복됐고 그 반복 자체가 내용이다. ④ `release-pipeline validate`: mypy 197파일 0 errors, plugin_payload in_sync, git 은 커밋 전이라 dirty.
- 검증 결과: release-pipeline validate: mypy ok(197 files, 0 errors) · plugin_payload in_sync(drift 0) · check_v0_7_24_release_notes_template 5/5 · release 필터 8개 green.
- 후속 작업:
