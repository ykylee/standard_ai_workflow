---
id: TASK-2026-08-20-main-008
status: done
created_at: 2026-08-20
source_anchor: generic-task-2026-08-20-main-008
source_path: backlog/2026-08-20.md
kind: generic
---

# TASK-2026-08-20-main-008 — session-end 가 bootstrap 채널에 없다 — 두 채널의 스킬 집합이 갈라져 있었다

## 📝 Description

- 상태: done
- 우선순위: high
- 요청일: 2026-08-20
- 담당:
- 호스트명:
- 호스트 IP:
- 영향 문서:
  - `.claude/skills/standard-ai-workflow/SKILL.md`
  - `workflow-source/tests/check_agent_plugin_payload.py`

- 작업 내용: 생성기에 session-end 명령 추가 + 진입 스킬 본문 정합 + 채널 파리티 검사
- 완료 기준: bootstrap 이 workflow-session-end 슬래시 명령을 emit 한다
- 완료 기준: 진입 스킬 본문이 4종을 가리킨다 (description 의 약속과 일치)
- 완료 기준: 두 채널의 스킬 집합이 갈라지면 검사가 red 를 낸다

## 🛠️ Implementation / Content

- 진행 현황: `2026-08-20 12:52` 기준 생성기에 session-end 명령 추가 + 진입 스킬 본문 정합 + 채널 파리티 검사
- 다음 세션 시작 포인트: [열림] 플러그인 스킬 4종 미로드 규명 — 1.3.0 재설치 후 `claude plugin details` 로 인벤토리 실측.
- 남은 리스크: **③ 플러그인 스킬 4종 미로드는 별건으로 남는다.** 플러그인은 설치·활성화돼 있고(user scope 1.2.0, `enabledPlugins` true) `skills/*/SKILL.md` 4개가 실재하는데 세션에 하나도 안 뜬다 — session-end 만이 아니라 **전부**다. 로컬 `.claude/` 가 3개를 덮고 있어 session-end 만 빠진 것처럼 보였다. 설치본이 1.2.0(오늘 1.3.0 발행 전)이고 Claude Code 의 플러그인 스킬 로딩 규약(매니페스트 `skills` 선언 필요 여부 · 재시작 필요 여부)이 걸린 문제라 저장소 안에서는 확정 불가. 재설치 후 실측이 필요하다. 참고: `wk doctor` 의 `content_drift` 는 **페이로드 파일**을 대조하지 그 파일이 **표면에 뜨는지**는 안 본다 — 존재를 적용으로 세지 않는다는 규칙이 여기에도 걸린다.

## ✅ Outcome

- 작업 결과: **`/workflow-session-end` 가 생겼고 이 세션에서 바로 잡혔다** (명령 파일을 쓰자 Claude Code 가 즉시 로드). ① **생성기 수리** — `render_claude_code_session_end_command()` 신설 + `render_claude_code_entrypoints()` 가 4종을 emit. 격리 bootstrap 실행으로 `.claude/commands/` 4개 산출 확인. ② **광고와 배선 정합** — 진입 스킬 본문에 §4 세션 종료 절을 넣고 호출 목록을 4종으로. `description` 은 처음부터 세션 종료를 약속하고 있었는데 본문이 3개만 가리켜, 모델이 있지도 않은 명령을 찾는 상태였다. ③ **파리티 검사 2종 신설** — (a) `PLUGIN_SKILLS` 의 slug 집합 ↔ 생성기 emit ↔ 이 저장소 `.claude/commands/` 를 **집합으로** 대조(개수만 세면 이름이 어긋난 채 통과한다) (b) 진입 스킬이 모든 명령을 가리키는지. 되주입 3종 red 실증(로컬 명령 제거 · 생성기 emit 제거 · 진입 스킬 언급 제거). **되주입 B 가 처음엔 안 걸렸다** — §4 절에 같은 명령이 또 적혀 있어서였고, 전부 지워 다시 확인했다.
- 검증 결과: check_agent_plugin_payload 20→22 cases · 되주입 3종 red 실증 · harness 필터 6개 green · check_standard_single_source 11/11 · check_entry_points green · 격리 bootstrap 산출물 4종 확인.
- 후속 작업:
