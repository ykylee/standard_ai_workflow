---
id: TASK-2026-08-20-main-009
status: done
created_at: 2026-08-20
source_anchor: generic-task-2026-08-20-main-009
source_path: backlog/2026-08-20.md
kind: generic
---

# TASK-2026-08-20-main-009 — 플러그인 스킬 4종이 인벤토리엔 있고 세션엔 없다 — in_sync 를 쓸 수 있음으로 읽던 자리

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-08-20
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/deploy_doctor.py`
  - `docs/INSTALLATION_AND_USAGE.md`
  - `workflow-source/tests/check_deploy_doctor.py`
  - `workflow-source/core/workflow_deployment_idempotency.md`

- Description: 가설 기각 + 탐침의 미측정 선언 + 문서화 → **새 세션 대조 실험으로 원인 규명** + `runtime_load` 탐침 신설
- Completion criteria: content_drift 가 '노출 미측정' 을 선언한다 (모름을 통과로 세지 않는다) — ✅
- Completion criteria: INSTALLATION 이 in_sync 의 한계와 확인 방법을 적는다 — ✅
- Completion criteria: 미로드 원인 규명 — 새 세션 실측 — ✅ (52차, 아래 대조 실험)
- Completion criteria: 규명된 조건을 **잰다** — declared_unmeasured 에 남기지 않는다 — ✅ (`runtime_load` 절)

## 🛠️ Implementation / Content

- Progress: 원인 확정: 호스트 프로세스(pid 6397, 2026-08-16 22:53 KST)가 플러그인 설치(2026-08-18 09:57 KST)보다 35시간 먼저 시작했다. 플러그인은 프로세스 시작 때 로드되고 /clear 는 프로세스를 재시작하지 않는다. 대조 실험 2축으로 이름 충돌 가설을 기각: 저장소 cwd 와 중립 cwd 양쪽에서 claude -p (새 프로세스)는 스킬 4종을 전부 노출했다. ~/.claude/skills/ 는 존재조차 않아 skills-dir 충돌도 성립 안 함.

**확정된 사실**: `claude plugin details` 는 `Skills (4) backlog-update, doc-sync, session-end, session-start` 로 **다 센다**. 그런데 세션에서 호출하면 `Unknown skill: standard-ai-workflow:doc-sync` — **인벤토리는 세션 가용성의 증거가 아니다**. `.in_use` 마커는 오늘 갱신돼 있어(Claude Code 가 플러그인을 적극적으로 쓰고 있다) "무시되고 있다" 도 아니다.

**대조 실험 (52차, 대조군이 성립했다)** — 축은 프로세스이지 cwd 가 아니었다:

| 축 | cwd | 플러그인 스킬 4종 |
|---|---|---|
| `claude -p` (새 프로세스) | 저장소 | **뜬다** |
| `claude -p` (새 프로세스) | 중립 디렉터리 | **뜬다** |
| 실행 중 대화형 세션 | 저장소 | **없음** |

**이름 충돌 가설 기각**: 저장소 cwd 의 새 프로세스도 4종을 다 노출했다. `~/.claude/skills/` 는 존재하지 않아 skills-dir 충돌도 성립하지 않고, `claude plugin list` 도 플러그인 1개만 센다.

**확정된 원인 — 시간이다**: 이 대화를 띄운 프로세스는 pid 6397 / 시작 **2026-08-16 22:53:10 KST** (cwd = 저장소, 부모 사슬로 확인), 플러그인 설치는 **2026-08-18 09:57:53 KST** — 설치가 **35시간 늦다**. 플러그인은 프로세스 시작 때 로드된다. 방증: 실행 중 프로세스의 바이너리는 `2.1.233`, 디스크의 `claude --version` 은 `2.1.234`.

**51차가 기각을 잘못한 이유 — 잰 단위**: 설치 시각을 *대화(session)* 시작 시각(2026-08-18T21:38Z)과 대조해 "설치가 21시간 앞선다" 고 봤다. 그런데 `/clear` 는 대화만 새로 열 뿐 프로세스를 재시작하지 않는다. **세션은 프로세스의 대리값이 아니다.**
- Next session starting point: 없음 — **53차 세션이 재시작 뒤 실제 호출로 확증했다.** 새 호스트(pid 77951)에서 `/standard-ai-workflow:session-start` 가 플러그인 채널 (`~/.claude/plugins/cache/.../1.2.0/skills/session-start`)에서 정상 로드됐다. 같은 시점의 `runtime_load` 는 claude-code 를 `낡은 호스트 0개 / 최신 1개` 로 읽어 **탐침의 판정과 실제 노출이 일치한다**. 남은 것은 이 저장소가 v1.3.0 을 냈는데 설치본이 1.2.0 이라는 것 — 별개 사안(플러그인 갱신)이다.
- Remaining risks: 프로세스 식별이 ps comm 이름에 의존한다 — 런처가 node 등으로 뜨는 환경에서는 이 절이 호스트를 못 센다(declared_unmeasured 로 선언).

## ✅ Outcome

- Result: 51차의 가설 ④ 기각은 잰 단위가 틀렸다 — 설치 시각을 대화(session) 시작 시각과 대조했는데 로드 단위는 프로세스다. 이 조건은 잴 수 있으므로 declared_unmeasured 에서 한 칸을 측정으로 옮겼다: wk doctor 에 runtime_load 절 신설(설치 시각 vs 실행 중 호스트 시작 시각). 실 호스트에서 pid 6397 을 그대로 잡았고 codex 낡은 호스트 3개도 함께 잡혔다.
- Verification: **실 호스트 대조 (53차)** — `runtime_load` 가 claude-code 낡은 호스트 0개(설치 뒤 시작), codex 낡은 호스트 3개(pid 6191·97626·30424)를 잡았다. 같은 세션에서 플러그인 스킬이 실제로 로드됐으므로 **판정과 노출이 일치하는 것을 한 번 실측했다**.
- Verification: check_deploy_doctor 17→20 cases, 되주입 3종 red 실증(낡음 판정 제거 / 설치 시각 출처 은폐 / lstart 사용). check_standard_single_source 11/11, check_installation_usage 6/6, check_self_application 8/8, mypy green.
- Follow-up:
