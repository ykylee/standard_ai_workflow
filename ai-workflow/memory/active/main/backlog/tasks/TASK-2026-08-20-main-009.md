---
id: TASK-2026-08-20-main-009
status: in_progress
created_at: 2026-08-20
source_anchor: generic-task-2026-08-20-main-009
source_path: backlog/2026-08-20.md
kind: generic
---

# TASK-2026-08-20-main-009 — 플러그인 스킬 4종이 인벤토리엔 있고 세션엔 없다 — in_sync 를 쓸 수 있음으로 읽던 자리

## 📝 Description

- 상태: in_progress
- 우선순위: medium
- 요청일: 2026-08-20
- 담당:
- 호스트명:
- 호스트 IP:
- 영향 문서:
  - `workflow-source/workflow_kit/deploy_doctor.py`
  - `docs/INSTALLATION_AND_USAGE.md`
  - `workflow-source/tests/check_deploy_doctor.py`

- 작업 내용: 가설 4개 기각 + 탐침의 미측정 선언 + 문서화 (원인 규명은 새 세션 필요)
- 완료 기준: content_drift 가 '노출 미측정' 을 선언한다 (모름을 통과로 세지 않는다)
- 완료 기준: INSTALLATION 이 in_sync 의 한계와 확인 방법을 적는다
- 완료 기준: [미완] 미로드 원인 규명 — 새 세션 실측 필요

## 🛠️ Implementation / Content

- 진행 현황: 세션에서 실측으로 좁혔다. **기각된 가설 4개**: ① 스킬 파일 부재 — `skills/*/SKILL.md` 4개 실재 확인 ② 플러그인 비활성 — `enabledPlugins: {"standard-ai-workflow@standard-ai-workflow": true}` ③ 매니페스트 파손 — 설치본과 저장소의 `marketplace.json` 이 버전만 다르고 구조 동일 ④ **세션 시작 후 설치** — 설치 `2026-08-18T00:57Z`, 세션 시작 `2026-08-18T21:38Z` 로 설치가 **21시간 앞선다**.

**확정된 사실**: `claude plugin details` 는 `Skills (4) backlog-update, doc-sync, session-end, session-start` 로 **다 센다**. 그런데 세션에서 호출하면 `Unknown skill: standard-ai-workflow:doc-sync` — **인벤토리는 세션 가용성의 증거가 아니다**. `.in_use` 마커는 오늘 갱신돼 있어(Claude Code 가 플러그인을 적극적으로 쓰고 있다) "무시되고 있다" 도 아니다.

**남은 유력 가설(미확정)**: 이 저장소는 두 채널을 **같은 머신에 동시에** 깐다 — 로컬 `.claude/skills/standard-ai-workflow/`(name: `standard-ai-workflow`)와 **같은 이름의 플러그인**. 자기 충돌이 구조적으로 성립한다. 확인은 새 세션이 필요해 이 세션에서는 불가.

**대조군 없음**: 설치된 플러그인이 이것 하나뿐이라 "플러그인 스킬 로딩 자체가 이 환경에서 되는가" 를 가를 수 없다.
- 다음 세션 시작 포인트: 새 세션에서 (a) 1.3.0 재설치 후 `claude plugin details` + 실제 호출 (b) 로컬 `.claude/skills/standard-ai-workflow/` 를 잠시 치우고 플러그인 스킬이 뜨는지 — 이름 충돌 가설의 직접 검증.
- 남은 리스크: 원인은 아직 모른다. 유력 가설(로컬 스킬과 플러그인의 이름 충돌)은 새 세션에서만 검증 가능하다. 그 전까지 이 저장소에서는 로컬 `.claude/` 채널이 4종을 다 덮으므로 실사용 손실은 없다.

## ✅ Outcome

- 작업 결과: **측정 못 하는 것을 통과로 세지 않게 고쳤다.** `wk doctor` 의 `content_drift` 가 재는 것은 *파일이 같은가* 이지 *하네스가 노출하는가* 가 아닌데, 그 한계가 어디에도 안 적혀 있어 `in_sync` 가 "쓸 수 있음" 으로 읽혔다. 실측이 정확히 그 간극을 보여 줬다 — in-sync + 인벤토리 4종인데 **세션 로드 0종**. ① `content_drift` 에 `declared_unmeasured` 신설(main-019 의 `installable` 과 같은 원칙) ② INSTALLATION §7.0.1 에 "`in_sync` 는 '쓸 수 있음' 이 아니다" 절 추가 — 스킬이 안 보이면 파일 비교로 끝내지 말고 **새 세션에서 실제 호출**로 확인하라고 적었다 ③ 선언이 조용히 사라지지 않게 검사 case 신설(되주입 red 실증).
- 검증 결과: check_deploy_doctor 16→17 cases, 되주입 1종 red 실증. check_installation_usage / check_quickstart_stale_links green.
- 후속 작업:
