---
id: TASK-2026-08-20-main-010
status: done
created_at: 2026-08-20
source_anchor: generic-task-2026-08-20-main-010
source_path: backlog/2026-08-20.md
kind: generic
---

# TASK-2026-08-20-main-010 — §7.0.2 의 미측정 셀 — 버전이 실제로 다를 때 claude-code 재실행 계약

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-08-20
- Owner: AI Agent
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/tests/check_deploy_doctor.py`

- Description: 재실행 계약 표는 전부 같은 버전(1.2.0=1.2.0)에서 실측됐다. v1.3.0 발행으로 처음 버전 상이 상태가 생겼다 — 그 셀을 실측한다
- Completion criteria: marketplace 클론이 고정된 상태에서 plugin update 가 무엇을 하는지 실측한다
- Completion criteria: marketplace update → plugin update 2단계 뒤 설치 캐시가 실제로 1.3.0 이 되는지 실측한다
- Completion criteria: §7.0.2 표에 '버전 상이' 를 추정 없이 실측 행으로 남긴다

## 🛠️ Implementation / Content

- Progress: 실측 완료 + 실측이 드러낸 탐침 결함 1건 수리
- Next session starting point: 이 호스트의 claude-code 는 재시작 전까지 1.3.0 이 노출되지 않는다 — runtime_load 가 pid 77951 을 낡음으로 잡고 있다. codex 사본은 여전히 1.2.0 drift 상태
- Remaining risks:

## ✅ Outcome

- Result: §7.0.2 미측정 셀 실측. ① 맨 이름 update 는 rc=1 'Plugin not found' — install 은 맨 이름을 받는데 update 는 <plugin>@<marketplace> 를 요구한다 ② marketplace update 선행 불필요 — plugin update 가 클론을 스스로 당긴다(HEAD c55631f4→deb74b82) ③ 당겨오는 것은 태그가 아니라 브랜치 팁이다 — 설치된 1.3.0 의 gitCommitSha 는 v1.3.0 태그(1f33a958)가 아니라 main 팁이었다 ④ 옛 버전 디렉터리는 남는다 ⑤ CLI 자신이 'Restart to apply changes' 라고 말한다(main-009 의 벤더 방증)
- Result: 실측이 탐침 결함을 드러냈다 — 갱신에 성공한 직후 발견이 6→8건으로 늘었다. content_drift 와 runtime_load 가 installed_plugins.json 의 installPath 선언을 안 읽고 glob 매치를 전부 동등한 설치로 셌다. 아무도 안 읽는 1.2.0 잔재가 드리프트로 보고되고(installed_version 도 옛 버전을 말했다) runtime_load 는 글자까지 같은 발견을 사본 수만큼 복제했다. MEM-2026-08-20-001 의 '있는가가 아니라 어느 것인가' 가 세 번째로 같은 모양으로 나타난 자리다
- Result: 수리: _declared_install_roots + _resolve_install_roots 신설. 선언이 있으면 그것으로 가르고, 없는 채널은 glob 전부를 설치로 보되 폴백임을 active_source 에 남긴다. 옛 사본은 지우지도 숨기지도 않고 superseded 로 남긴다. 교정 후 발견 8→6, claude-code v1.3.0 은 in-sync 로 정확히 보고된다
- Result: 도달 불가능한 분기 1개 제거: 비교 쪽 root.resolve() 는 probe 가 home 을 이미 resolve 하므로 밟을 수 없었고 되주입해도 red 가 안 났다. 정규화는 선언 쪽에서 끝낸다
- Verification: check_deploy_doctor 20→23 cases · 되주입 3종 red 실증(active 미적용 / 잔재 순회 / 폴백 은폐) · 4종째는 물지 않아 그 분기를 제거 · mypy clean(workflow-source 기준) · check_installation_usage 6/6
- Follow-up:
