---
id: TASK-2026-08-24-main-001
status: done
created_at: 2026-08-24
source_anchor: generic-task-2026-08-24-main-001
source_path: backlog/2026-08-24.md
kind: generic
---

# TASK-2026-08-24-main-001 — watch_transient_writer flake — 이벤트 1건이 '내 주입 완결본' 을 뜻하지 않는다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-24
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/tests/check_watch_transient_writer.py`

- Description: 2026-08-22 전량 native 축에서 test_transient_reinjection_is_captured 가 1회 red('주입 marker 가 diff 에 없다'). 단독으로는 재현 안 되고 부하에서만 깨진다. 원인 규명: 테스트가 Path.write_text 로 쓰는데 그것은 truncate 후 write 라 원자적이지 않고, 감시자는 그 사이 상태를 정직하게 관측한다. 그래서 changed[0] 가 내 완결 주입이 아니라 '빈/부분 파일' 일 수 있다. 실측 재현: 큰 파일로 창을 넓히니 changed 22건 중 12건이 완결 아닌 크기(0 · 1800046)를 관측했다. 도구 결함이 아니라 테스트 가정의 결함이다 — main-005 가 '시간이 아니라 관측을 기다린다' 로 고쳤지만, 그 관측 신호(이벤트 개수)가 무엇을 뜻하는지는 여전히 가정이었다.
- Completion criteria: 테스트가 원자적으로 써서 부하와 무관하게 결정적이고, 비원자적 쓰기가 추가 이벤트를 낳는다는 사실이 별도 case 로 선언되며, 되주입 red 를 실증한다

## 🛠️ Implementation / Content

- Progress: `2026-08-24 09:13` 기준 close
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: 도구는 내내 옳았다. 테스트가 Path.write_text(truncate 후 write)로 쓰는데 그것은 비원자적이라, 감시자가 그 사이의 빈/부분 파일을 첫 변경으로 잡을 수 있었다 — changed[0] 가 내 완결 주입이라는 가정이 틀렸다. 실측: 1.8MB 로 창을 넓히니 changed 22건 중 12건이 완결 아닌 크기(0 · 1800046)를 관측. 수리 (1) transient case 를 _atomic_write(임시 파일 + os.replace)로 — 되주입(비원자 + 넓은 창)에서 3회 중 2회 red 로 부담을 실증. (2) '중간 상태는 보고된다' 를 우연이 아니라 계약으로 못박는 case 신설 — 다음 사람이 그 관측을 잡음으로 오해해 도구를 뭉개는 것을 막는다(되주입: 빈 파일 무시 → red). **첫 판의 그 case 자체가 5회 중 4회 red 인 flake 였다** — 폴러가 truncate 창을 운으로 잡길 기대한 설계였고, 고치려던 병을 검사가 다시 앓았다. 중간 상태를 폴링 간격보다 확실히 긴 시간 실재하게(2단계 쓰기 + fsync + 관측 대기) 바꿔 결정적으로 만들었다. 6회 연속 6/6.
- Verification: check_watch_transient_writer 5→6 cases, 6회 연속 6/6 PASS. 되주입 3종 red 실증(비원자 쓰기 · 도구가 빈 파일 무시 · 첫 판 flake 확인)
- Follow-up:
