---
id: TASK-2026-08-24-main-003
status: done
created_at: 2026-08-24
source_anchor: generic-task-2026-08-24-main-003
source_path: backlog/2026-08-24.md
kind: generic
---

# TASK-2026-08-24-main-003 — bootstrap 이 v0.14.0 이전 daily 템플릿을 낸다 — 새 프로젝트가 첫날부터 혼재한다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-24
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/templates/daily_backlog_template.md`

- Description: daily_backlog_template.md 이 한국어 표기 + 임베드 task 레이아웃(v0.14.0 이전)이다. bootstrap 한 새 프로젝트는 그 파일을 받고, 도구는 그 아래에 append-only 인덱스를 덧붙인다 — 한 파일에 두 형식이 겹치고 유령 task(TASK-001)가 남는다. 표기 혼재는 증상이고 레이아웃 불일치가 원인이다. 이 저장소 자신의 daily 파일(# Backlog Index —)과 대조하면 차이가 그대로 보인다.
- Completion criteria: 새로 bootstrap 한 프로젝트의 daily 파일이 이 저장소의 현행 형식과 같은 구조·표기를 갖고, 그것을 대조하는 그물이 생긴다

## 🛠️ Implementation / Content

- Progress: `2026-08-24 09:45` 기준 close
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: 템플릿을 고치는 대신 **사본을 없앴다**. render_daily_backlog 이 templates/daily_backlog_template.md 를 읽던 것을 그만두고, 도구와 같은 정본 작성기(workflow_writes.render_daily_backlog_header + daily_index_entry_lines)로 조립한다 — 결함의 원인이 사본이었으므로 사본을 지우는 것이 수리다. _daily_index_entry_lines 를 공개화해 두 채널이 같은 조립기를 쓴다. 함께 고친 것 둘: ① 씨앗 task 파일이 아예 없어서 인덱스가 빈 곳을 가리켰다 — render_initial_task_file 신설(본문은 task_label 파생). ② 기본 ID 'TASK-001' 이 TASK_ID_PATTERN 과 안 맞아 kit 자신의 파서가 못 읽었다 — 날짜와 slug 에서 파생하되 사용자 지정은 존중한다. 배포 템플릿은 소비자 참고 자료라 남기되 정본 작성기 출력으로 갈고, 검사가 일치를 강제한다. 실측: 새로 bootstrap 한 프로젝트가 이 저장소와 같은 구조·표기를 받고, 씨앗 task 가 planned_items 로 파싱되며(main-014 가 뚫은 자리), 이어서 wk backlog-update 로 쓴 두 번째 task 와 표기가 완전히 일치한다.
- Verification: check_daily_backlog_template 신설 4 cases, 검사 265→266. 되주입 3종 red 실증(씨앗 ID 파생 제거 · 템플릿 옛 형식 회귀 · 렌더러가 다시 템플릿 읽기). 날짜 줄은 매일 바뀌므로 비교에서 제외했다
- Follow-up:
