---
id: TASK-2026-08-24-main-006
status: done
created_at: 2026-08-24
source_anchor: generic-task-2026-08-24-main-006
source_path: backlog/2026-08-24.md
kind: generic
---

# TASK-2026-08-24-main-006 — 세션 시작이 부재 파일을 스스로 채운다 — 부재는 생성, 낡음은 보고

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-24
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/tools/ensure_entrypoints.py`

- Description: 사용자 요청: 워크플로우 시작 시 진입점/필요 파일이 없으면 버전에 맞게 생성하고, 있으면 버전 체크 후 갱신. 현재 wk session-start 는 필수 문서 부재 시 missing_required_document 로 중단하고 recovery_hint 로 legacy shim 경로(scripts/bootstrap_workflow_kit.py)를 안내한다. 필요한 조각은 이미 다 있다 — HARNESS_SPECS 가 필요 파일을 선언하고, bootstrap_lib 렌더러가 현재 kit 버전으로 찍으며, decide_action 이 CREATE/UPDATED/IGNORED/PRESERVED/FORKED 를 판정한다. 소유자 결정(2026-08-24): 부재는 자동 생성(self-bootstrap 이 이미 약속한 동작이고 되돌리기 쉽다), 낡음은 보고만(선언 안 한 커스터마이즈가 조용히 날아가지 않게).
- Completion criteria: wk ensure-entrypoints 가 부재/낡음/포크/최신을 분류하고 create-only 모드로 부재만 채우며, session-start 가 그것을 거쳐 중단 대신 복구하고, 되주입 red 를 실증한다

## 🛠️ Implementation / Content

- Progress: `2026-08-24 13:48` 기준 close
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: wk ensure-entrypoints 신설 + session-start 배선. 경계는 writes._resolve_write 한 곳에서 강제한다 — 쓰기 판정이 다섯 곳에 복제돼 있어 인자로 흘리면 한 곳만 빠뜨려도 조용히 덮는다. Action 에 UPDATE_AVAILABLE 추가(updated 면 '덮었다' 가 거짓, ignored 면 '최신이다' 가 거짓이라 자기 이름이 필요했다). 매니페스트 버킷이 main-013 덕에 열거형 파생이라 새 분류가 자동으로 실렸다. 프로젝트 정체를 모르면(PROJECT_PROFILE.md 부재) 아무것도 만들지 않고 needs_bootstrap 을 낸다 — 이름을 지어내면 그 거짓이 이후 모든 산출물에 실린다. 실측이 설계를 두 번 고쳤다: ① 처음엔 복구를 session_start 의 실패 경로에만 달았는데, 하네스 진입점이 없어도 session-start 는 상태 문서만 읽어 status ok 로 끝나 복구가 아예 안 돌았다 — 성공 경로로 옮겼다. ② ensure_entrypoints 가 내부 bootstrap 을 부를 때 PYTHONPATH 를 저장소 루트로 줘 import 가 실패했다(수동 시험은 PYTHONPATH 가 export 돼 있어 가려졌고 검사가 잡았다).
- Verification: check_ensure_entrypoints 신설 4 cases (검사 267→268), 되주입 3종 red 실증(경계 붕괴 · 실패 경로 회귀 · 정체 지어내기). 격리 fixture 로 부재 복구·손수정 보존·마커 보존 확인. 전량 2축 268/268
- Follow-up: bootstrap 은 대상의 git 브랜치만 보고 env 오버라이드를 의도적으로 무시한다(sandbox caller 근거) — fixture 가 그것을 상속하지 않도록 검사에서 끊었다. 제품 결함 아님
