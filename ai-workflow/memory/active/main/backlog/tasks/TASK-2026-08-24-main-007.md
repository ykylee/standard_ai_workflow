---
id: TASK-2026-08-24-main-007
status: done
created_at: 2026-08-24
source_anchor: generic-task-2026-08-24-main-007
source_path: backlog/2026-08-24.md
kind: generic
---

# TASK-2026-08-24-main-007 — mypy 캐시 격리 — no-incremental 은 읽기만 끄고 디렉터리는 만든다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-24
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/tools/release_pipeline.py`

- Description: 관찰 4차의 트레이스백이 create_metastore 를 지목했다. 실측으로 전제 확증: --no-incremental 은 캐시 읽기만 끄고 .mypy_cache 디렉터리는 그대로 만든다. 그래서 병렬 구간의 mypy 호출 6곳이 같은 cwd 의 같은 디렉터리를 두고 경합한다(이 저장소에는 지금 ./ 와 workflow-source/ 두 층에 .mypy_cache 가 있다). --cache-dir= (빈 값)은 아무것도 만들지 않고 rc=0 이며, 실제 대상에서 판정이 동일하다(199 source files 양쪽). --no-incremental 과 함께면 캐시는 읽히지 않으므로 만들지 않는 것이 엄격히 낫다.
- Completion criteria: mypy 를 부르는 모든 자리가 캐시를 격리하고, 전량 실행이 .mypy_cache 를 만들지 않으며, 격리 누락을 검출하는 그물이 생긴다

## 🛠️ Implementation / Content

- Progress: `2026-08-24 15:02` 기준 close
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: mypy 호출 6곳 전부에 --cache-dir= (빈 값) 을 넣었다. 전제 실측: --no-incremental 은 캐시 읽기만 끄고 .mypy_cache 를 그대로 만든다 / --cache-dir= 는 아무것도 만들지 않고 rc=0 / 실제 대상 판정 동일(199 source files 양쪽). 읽지 않는 캐시를 만들 이유가 없으므로 만들지 않는 편이 엄격히 낫다. 그물은 check_mypy_strict_release_gate case 1e — mypy 호출을 정규식으로 전수 조사해 격리 누락을 잡는다(한 자리만 고치면 나머지가 계속 경합하므로 전수여야 한다). 첫 판이 .venv 안의 mypy 자기 테스트를 물어 red 였고, 저장소 소유 코드로 한정했다 — 그물이 남의 코드를 물면 위양성이 된다.
- Verification: 판정 최종: 전량 2축 268/268 green 이고 .mypy_cache 가 0개 생성됐다(실행 전 삭제 후 확인). 경합면 자체가 사라졌다. 되주입: 한 자리만 격리를 빼면 case 1e 가 그 파일을 지목한다. mypy -v 로 Config File 을 파싱하는 검사도 그대로 통과
- Follow-up: 다음 재발 여부가 이 가설의 최종 판정이다 — 경합면이 없어졌으므로 create_metastore 크래시가 멈춰야 한다. main-004 는 그 관찰로 열어 둔다
