---
id: TASK-2026-08-24-main-009
status: done
created_at: 2026-08-24
source_anchor: generic-task-2026-08-24-main-009
source_path: backlog/2026-08-24.md
kind: generic
---

# TASK-2026-08-24-main-009 — doctor 환경 탐침이 자기 인터프리터를 잰다 — uv tool venv 의 pip 부재는 결함이 아니다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-24
- Owner: AI Agent
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/deploy_doctor.py`
  - `workflow-source/tests/check_deploy_doctor.py`

- Description: wk doctor 의 'venv 에 pip 이 없다' finding 이 오탐: 탐침이 pip 을 자기 인터프리터(wk 의 uv tool venv, ~/.local/share/uv/tools/standard-ai-workflow)에서 import 하는데, uv tool venv 는 설계상 pip 이 없고 uv-receipt.toml 로 자신을 선언한다. 처방(ensurepip)은 이미 pip 이 있는 저장소 .venv 를 향해 헛돈다(실측: Requirement already satisfied). 53차 규칙 '탐침은 잰 단위가 맞아야 한다'의 재현. 수리: 선언(uv-receipt.toml)을 읽어 by-design 부재는 finding 에서 제외하되 payload 에 판정 근거를 남기고, finding 을 낼 때는 잰 인터프리터를 명시한다
- Completion criteria: uv tool venv(uv-receipt.toml 선언)에서 pip 부재가 finding 으로 잡히지 않고, payload 에 by-design 판정과 잰 인터프리터가 남는다. 선언 없는 venv 의 pip 부재는 여전히 finding 이고 잰 인터프리터 경로를 명시한다. 판정은 순수 함수로 추출해 check 가 양방향(receipt 유/무)으로 실측한다

## 🛠️ Implementation / Content

- Progress: 발견 경위: wk doctor 실측에서 'venv 에 pip 이 없다' finding 이 나왔는데 처방(ensurepip)을 따라가 보니 저장소 .venv 에는 pip 26.0.1 이 이미 있었다(Requirement already satisfied). 추적: 탐침은 pip 을 자기 인터프리터에서 import 하고, wk 는 uv tool install 로 깔려 그 인터프리터가 ~/.local/share/uv/tools/standard-ai-workflow — uv tool venv 는 설계상 pip 이 없고 루트의 uv-receipt.toml 로 자신을 선언한다. 53차 '탐침은 잰 단위가 맞아야 한다' + '하네스가 이미 말하고 있는 것을 추측하지 않는다'의 재현. 수리: 판정을 순수 함수 _pip_absence_verdict 로 추출 — uv-receipt.toml 선언이 있으면 by_design_uv_tool 로 finding 억제(안 낸 이유는 payload pip_absence 키에 남긴다 — 조용한 통과 금지), 선언이 없으면 여전히 결함이되 finding 에 잰 인터프리터 경로를 명시(처방이 엉뚱한 venv 로 가지 않게)
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: check_deploy_doctor 24→25 cases. 실환경 확증: 오탐을 냈던 그 인터프리터에서 pip_absence=by_design_uv_tool·finding 0
- Verification: check_deploy_doctor 25/25 · 되주입(선언 읽기 제거)으로 red 실증 후 git restore --worktree 원복(스테이징 보존 — 58차 교훈 적용) · wk 인터프리터 실측으로 오탐 해소 확인 · mypy strict deploy_doctor.py clean
- Follow-up:
