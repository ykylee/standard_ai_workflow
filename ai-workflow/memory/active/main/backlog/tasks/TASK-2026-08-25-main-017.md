---
id: TASK-2026-08-25-main-017
status: in_progress
created_at: 2026-08-25
source_anchor: generic-task-2026-08-25-main-017
source_path: backlog/2026-08-25.md
kind: generic
wbs: M-007/WBS-7.1
---

# TASK-2026-08-25-main-017 — MCP emit command 가 항상 python3 — PATH 에 python3 이 없는 Windows 에서 emit 설정으로 서버를 spawn 할 수 없다

## 📝 Description

- Status: in_progress
- Priority: high
- Request date: 2026-08-25
- Owner: AI Agent
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/bootstrap_lib/mcp.py`
  - `workflow-source/tests/check_bootstrap_mcp_roundtrip.py`
  - `workflow-source/tests/check_mcp_emit_launcher_sweep.py`

- Description: bootstrap 이 emit 하는 MCP 설정의 command 가 python3 로 고정 (workflow_kit/bootstrap_lib/mcp.py:87 docstring, :98). 2026-08-25 실측 Windows 머신(opencode 채널) 에는 PATH 에 python3 이 없어, emit 된 mcp.opencode.json 으로 opencode 가 서버를 spawn 하면 실패한다. 서버 코드 자체는 정상(실측: initialize, tools/list 왕복) — 해석기 선택 문제.
- Completion criteria: consumer 플랫폼에서 emit 된 MCP command 가 실재하는 실행 파일로 해석된다 (플랫폼별 해석기, 예: sys.executable 파생 또는 명시적 override).
- Completion criteria: Windows 실측 경로(bootstrap + MCP spawn 왕복) 가 emit 설정 그대로 통과한다.

## 🛠️ Implementation / Content

- Progress: `2026-09-07 14:04` 기준 77차: 62차 수리의 온전성을 실측으로 재확인하고, 그 수리를 지키는 판정 공백을 닫았다. 완료 기준 2(Windows 실측)는 이 호스트에서 불가하여 그대로 남는다.
- Next session starting point: Windows 호스트(Oh My Pi)에서 bootstrap emit → 하네스 spawn 왕복 실측 후 close
- Remaining risks: 판정은 emit 산출물의 command **이름**만 잰다. 그 이름이 Windows PATH 에 실제로 존재하는지, spawn 이 성공하는지는 이 축으로 못 본다 — 그것이 완료 기준 2 가 남아 있는 이유다. python.org 설치본이 아닌 배포(예: Microsoft Store 판, py launcher 전용)에서 'python' 이 없을 가능성도 실측 전까지는 열려 있다.

## ✅ Outcome

- Result: [62차 수리는 온전하다 — 실측] emit 지점 전수를 훑었다. MCP command 를 조립하는 자리는 전부 정본 mcp_server_command 를 지난다(mcp.py 의 6개 렌더러 + plugin_payload). python3 리터럴 전수 grep 에서 emit 경로에 남은 사본 0건 — deploy_doctor 의 것은 '플러그인 채널은 리터럴로 잰다' 는 의도된 것이고, score_wiki/baselines 의 것은 서브프로세스 호출이라 emit 이 아니다. **고칠 코드가 남아 있지 않다.**
- Result: [판정 공백이 있었다] 기존 check_bootstrap_mcp_roundtrip.unit_checks 는 정본 함수를 **직접** 부를 뿐이고, 그 docstring 이 스스로 'smoke 는 이 호스트의 산출물만 밟는다' 고 적어 두었다. 그래서 새 렌더러가 command 를 손으로 적어 정본을 우회해도 red 가 되지 않는다 — 이 저장소가 Grok 렌더러에서 이미 겪은 사본 분기와 같은 모양이다(2026-08-05).
- Result: [판정 신설] check_mcp_emit_launcher_sweep.py (3 case): 함수가 아니라 **산출물**을 잰다. 렌더러 6개(레지스트리 5 + 레지스트리 밖 mavis)를 돌려 emit 된 설정에서 command 를 꺼내고, python_launcher 를 win32/posix 로 강제해 관례와 대조한다. sys.platform 전역 패치 대신 정본 해석기 하나만 갈아 끼운다(전역 패치는 이 호스트의 shutil.which 까지 민다 — check_deploy_doctor 와 같은 방식).
- Result: [커버리지도 함께 잰다 — 모름을 통과로 세지 않는다] 추출이 0건을 내면 검사는 조용히 통과하고 그 0건은 위반 0건과 구분되지 않는다. 그래서 렌더러마다 command 를 최소 1개 찾았는지 확인한다. **실제로 이 함정을 밟았다**: opencode 는 command 가 문자열이 아니라 **리스트**라, 문자열 전용 추출이 0건을 반환했고 처음엔 그것이 통과로 보였다. 추출을 두 형태 모두 받도록 고쳤다. 더해서 모듈의 render_*_mcp_config 전수가 훑는 집합과 같은지 대조해, 등록을 빠뜨린 새 렌더러가 검사 밖에 남지 않게 했다.
- Result: [남은 것은 여전히 완료 기준 2 뿐] Windows 호스트에서 bootstrap emit → 하네스 spawn 왕복 실측. **이 호스트(macOS)에서는 원리적으로 불가하다.** 다만 win32 분기가 emit 산출물 수준에서 관례를 지킨다는 것은 이제 판정이 상시 보증한다 — 실측이 재는 것은 그 다음 칸(실재하는 실행 파일로 spawn 되는가)이다.
- Verification: check_mcp_emit_launcher_sweep 3/3 PASS · 되주입 3종(정본 우회 사본 / payload posix 고정 제거 / 렌더러 미등록) 전부 red 후 원복 green
- Follow-up:
