---
id: TASK-2026-08-25-main-018
status: done
created_at: 2026-08-25
source_anchor: generic-task-2026-08-25-main-018
source_path: backlog/2026-08-25.md
kind: generic
wbs: exempt
wbs_exempt_reason: 로드맵 밖 작업 — emit 환경 결함(PYTHONPATH). ADR-027 로드맵 층 WBS 대상 아님
---

# TASK-2026-08-25-main-018 — emit PYTHONPATH 가 source-checkout 모드에서만 실재 — 순수 신규 프로젝트에서 실재하지 않는 디렉터리를 가리킨다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-25
- Owner: AI Agent
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/bootstrap_lib/mcp.py`

- Description: emit 된 MCP 설정의 PYTHONPATH: workflow-source 는 source-checkout 실행 시에만 emit (workflow_kit/bootstrap_lib/mcp.py:130-131). 순수 신규 프로젝트는 킷이 ai-workflow/ 에 있어 emit 된 env 가 실재하지 않는 디렉터리를 가리킨다. checkout 대상(이 저장소 등)에서는 정상.
- Completion criteria: emit 된 PYTHONPATH 가 source-checkout / 순수 신규 프로젝트 두 모드 모두에서 킷이 실재하는 디렉터리를 가리키거나(또는 생략되거나), 두 모드에서 MCP spawn 왕복이 통과한다.
- Completion criteria: source-checkout 모드에서 기존 동작이 회귀하지 않는다 (check_bootstrap_mcp_roundtrip 통과).

## 🛠️ Implementation / Content

- Progress: `2026-08-25 14:23` 기준 emit PYTHONPATH 를 target 레이아웃 기준으로 교정
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: _mcp_server_env 의 조건을 'bootstrap 자신이 checkout 에서 돌았나'(SOURCE_ROOT)에서 'target 프로젝트에 workflow-source/ 가 실재하나'로 교정 — 잰 단위가 틀렸던 사례('탐침은 잰 단위가 맞아야 한다'의 emit 판). 신규 프로젝트는 PYTHONPATH 미emit(서버는 site-packages 해석), vendored target 은 기존 동작 유지. check_bootstrap_mcp_roundtrip 에 unit case(env-by-layout) + 스모크 형식 게이트(emit 된 PYTHONPATH 실재 검증) 신설.
- Verification: 62차(macOS) 실측: fresh target emit 에 PYTHONPATH 없음 + vendored target 은 workflow-source 유지 (bootstrap 실행 재현 2종). 되주입(무조건 emit) red 2중 실증 — unit case AssertionError + 스모크 형식 게이트 rc=1. check_bootstrap_mcp_roundtrip·check_bootstrap 등 이웃 검사 green, mypy clean, check_self_application 8/8.
- Follow-up:
