---
id: TASK-2026-08-25-main-020
status: in_progress
created_at: 2026-08-25
source_anchor: generic-task-2026-08-25-main-020
source_path: backlog/2026-08-25.md
kind: generic
wbs: exempt
wbs_exempt_reason: 로드맵 밖 작업 — cross-host 플랫폼 형식 결함(state.json). ADR-027 로드맵 층 WBS 대상 아님
---

# TASK-2026-08-25-main-020 — state generator 가 Windows 호스트에서 백슬래시 경로를 쓴다 — safe_relpath 에 POSIX 정규화가 없다

## 📝 Description

- Status: in_progress
- Priority: high
- Request date: 2026-08-25
- Owner: AI Agent
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/common/paths.py`
  - `workflow-source/tests/check_state_json_generated.py`

- Description: safe_relpath(workflow_kit/common/paths.py:49) 는 os.path.relpath 결과(와 절대 경로 폴백 str())를 그대로 돌려주며, state builder 가 source_of_truth/next_documents/backlog/repository_assessment 경로를 그 값으로 적는다. Windows 호스트에서 wk 실행 시 state.json 경로 값이 백슬래시 표기가 돼 POSIX 소비자(cross-host federation, MacBook) 에서 단일 파일명으로 해석된다. 형식 게이트 부재로 조용히 통과(check_state_json_generated 는 same-platform drift 비교). 61차 실측: safe_relpath('docs/PROJECT_PROFILE.md', '.') → 'docs\\PROJECT_PROFILE.md'. 수리(진행): safe_relpath 두 분기 as_posix() + check_state_json_generated case_7(단위 2분기 + 산출물 형식 대조) + 되주입 red 실증 완료.
- Completion criteria: safe_relpath 의 두 분기(상대/절대 폴백) 모두 POSIX 표기(as_posix)를 반환하고, Windows/POSIX 양 플랫폼에서 state.json 경로 값에 백슬래시가 없다.
- Completion criteria: state.json 형식 게이트(case_7) 신설 — 재생성 산출물의 경로 값을 검사해 백슬래시가 있으면 red, 되주입(정규화 제거)으로 red 실증.
- Completion criteria: 이 저장소 state.json 을 재생성(POSIX)하고 check_state_json_generated green.

## 🛠️ Implementation / Content

- Progress: safe_relpath 두 분기 as_posix() 수정 완료. check_state_json_generated case_7 신설 후 7 cases green (재등록 전, 분기 전 코드 기준). 되주입 red 실증: 상대 분기 'docs\\PROJECT_PROFILE.md' AssertionError. state.json 재생성 후 재검증 필요 (재베이스 후).
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result:
- Follow-up:
