---
id: TASK-2026-08-28-main-012
status: done
created_at: 2026-08-28
source_anchor: generic-task-2026-08-28-main-012
source_path: backlog/2026-08-28.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-08-28-main-012 — wk release-bump 가 자기 설치 디렉터리의 pyproject 를 찾는다 — uv tool 실행 시 FileNotFoundError

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-28
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: '자기 위치 오인' 결함족의 또 다른 사본 (main-003·023 과 동형). wk release-bump 가 uv tool venv 의 site-packages/pyproject.toml 을 열려다 즉시 실패 — cwd 기반 프로젝트 탐색으로 교정 필요. 2026-08-28 실측, 저장소 venv 의 release_pipeline 모듈 직접 실행으로 우회.
- Completion criteria: 설치본 실행이 traceback 대신 두 경로(설치본 위치·cwd 체크아웃)를 명시한 exit 2 로 끝난다 + 체크아웃 안에서의 동작은 불변
- Completion criteria: 회귀 검사 신설 + 되주입(설치본 판정 제거) red 실증 + uv tool 설치본 실측

## 🛠️ Implementation / Content

- Progress: `2026-08-28 23:27` 기준 설치본 명시 거부 완료
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: release_pipeline_lib._require_source_checkout() 이 로드 **전에** 판정한다 (module-level 상수가 load time 에 계산되므로 판정도 그 앞이어야 한다). 설치본 위치·cwd 에서 찾은 체크아웃·거기서 돌릴 명령 3줄을 찍고 exit 2. cwd 체크아웃의 코드는 로드하지 않는다 — 62차 main-019 가 defect 로 세운 foreign_path 와 같은 모양이 되기 때문(소유자 결정). release_pipeline 을 쓰는 8개 wrapper 전부가 이 관문을 지나고, cmd_lfu_decay_persist(소비자 명령)는 이 경로를 안 타므로 영향 없다. dispatcher 는 예외 이름 접두 없이 메시지를 그대로 찍는다.
- Verification: 체크아웃 내 동작 불변 확인(release-bump dry-run 1.7.0→1.7.1), 설치본 레이아웃 2종(cwd=저장소/저장소 밖) 실측, 전량 2축 green
- Follow-up:
