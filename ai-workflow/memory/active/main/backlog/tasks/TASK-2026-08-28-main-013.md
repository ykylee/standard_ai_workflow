---
id: TASK-2026-08-28-main-013
status: done
created_at: 2026-08-28
source_anchor: generic-task-2026-08-28-main-013
source_path: backlog/2026-08-28.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-08-28-main-013 — '자기 위치 오인' 결함족 전수 마감 — wk 진입점 기본값 4건 교정 + 새 사본을 막는 정적 검사

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-28
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: 사본 단위 추격을 끝내고 구조로 닫는다
- Completion criteria: 4개 사본이 cwd 기반 해석 + path_source 선언으로 교정되고 각 도구의 기존 스모크가 green
- Completion criteria: 정적 검사 신설 — 되주입(사본 하나를 원복)으로 red 실증

## 🛠️ Implementation / Content

- Progress: `2026-08-28 23:28` 기준 사본 7건 교정 + 정적 검사로 구조적 마감
- Next session starting point: check_seed_workspace_memory fixture 를 현실적 workspace 로 고쳤다(이전 green 이 모듈 저장소 profile 차용에 기대고 있었다). 같은 부류의 fixture 가 다른 검사에도 있는지는 미확인.
- Remaining risks:

## ✅ Outcome

- Result: 정본 신설: paths.resolve_workspace_root() — cwd profile → git toplevel → cwd 순으로 해석하고 근거(path_source)를 함께 돌려준다. 교정 7건(수동 조사 4 + 정적 검사가 추가 발견 3): seed-workspace-memory(메모리에 쓴다 + state 생성기가 모듈 저장소 profile 을 빌려 썼다 + registry 에 박는 절대경로) · migrate-memory-to-branch-scoped(git mv 의 cwd 포함) · claim-workspace · survey-remote-workspaces · consumer-metrics(설치본에서 site-packages 에 history 를 쌓았다) · detect-scope-drift(모듈 위치 + 'main' 브랜치 하드코딩 + git show/log 기준 저장소) · migrate-active-to-appendonly. 정적 case 8 신설 — TOOL_MODULES 전 진입점의 add_argument 기본값이 __file__ 파생이면 red. **수동 grep 4건 대비 정적 검사가 3건을 더 찾았다** — 사본 추격을 그만두는 것이 이 case 의 목적이다.
- Verification: check_self_location_resolution 8/8, 되주입 red 실증(사본 1건 원복 → case 8 red), 영향 도구 스모크 10종 green, mypy strict 0 errors
- Follow-up:
