---
id: TASK-2026-08-30-main-002
status: done
created_at: 2026-08-30
source_anchor: generic-task-2026-08-30-main-002
source_path: backlog/2026-08-30.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-08-30-main-002 — doctor 의 grok-build 설치본 탐지가 디렉터리 이름 가정에 기대 실패 — 정본은 registry.json

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-30
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/deploy_doctor.py`
  - `workflow-source/tests/check_deploy_doctor.py`

- Description: PLUGIN_INSTALL_CACHES 의 glob '.grok/installed-plugins/*standard-ai-workflow*' 가 아무것도 매치하지 않는다. grok 1.0.5 는 설치 디렉터리를 'plugin-<hash>' 로 짓고 이름→경로 매핑을 registry.json 에 적는다 (claude-code 의 installed_plugins.json 과 같은 모양). 그 결과 사본이 실재하는데도 content_drift 가 grok 을 통째로 건너뛴다.
- Completion criteria: 이 호스트의 grok 설치본(plugin-da9172c3, v1.7.0)이 content_drift 에 in-sync 로 나온다
- Completion criteria: 탐지는 디렉터리 이름이 아니라 registry.json 의 path 선언을 읽는다 — 이름 규칙은 하네스 것이지 우리 것이 아니다
- Completion criteria: registry.json 부재·손상 시 통과가 아니라 미측정으로 남는다 (모름 ≠ 안전)

## 🛠️ Implementation / Content

- Progress: claude-code 전용 특수분기(if harness == 'claude-code')였던 선언 읽기를 INSTALL_PATH_DECLARATIONS 표로 일반화하고 grok 리더를 추가했다. 선언된 경로는 glob 이 못 찾아도 후보에 합치되, raw/resolve 중복은 실경로로 접는다 — 접지 않으면 같은 사본이 두 번 세어져 발견이 복제된다(이 파일이 이미 한 번 고쳤던 실패라 검사 2종이 즉시 red 로 잡았다).
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: grok-build v1.7.0 [in-sync] 대조 19개 — 이전에는 content_drift 에 줄 자체가 없었다
- Result: 부수 효과: runtime_load 도 grok 을 재게 됐다 (설치 시각이 사본에서 나오므로). 실측으로 '설치보다 먼저 시작한 grok 프로세스 pid 61110' 을 잡았다
- Result: 수리 직후 실제 드리프트를 즉시 적발 — 35a7a859 가 추가한 mcp_config.json 이 설치본에 없었다. 재설치로 해소
- Verification: check_deploy_doctor 29/29 (신규 2 cases), 되주입 2종 red 실증 (선언 리더 제거 / 사본 0 침묵)
- Follow-up:
