---
id: TASK-2026-08-30-main-004
status: done
created_at: 2026-08-30
source_anchor: generic-task-2026-08-30-main-004
source_path: backlog/2026-08-30.md
kind: generic
wbs: M-007/WBS-7.4
---

# TASK-2026-08-30-main-004 — pi 패키지 매니페스트가 버전 정합 밖에 있다 — plugin/package.json 이 1.2.0 에 고착

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-30
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `plugin/package.json`
  - `workflow-source/workflow_kit/tools/release_pipeline.py`
  - `workflow-source/tests/check_agent_plugin_payload.py`

- Description: plugin/package.json 은 render_agent_plugin() payload(19개)에 없는 손 체크인 파일이라 어떤 생성기도 안 건드린다. release bump 대상도 아니고 검사도 version 을 단언하지 않아, kit 1.7.0 인데 5개 minor 동안 1.2.0 으로 남았다. npm/git 배포 순간 갤러리에 1.2.0 으로 올라간다.
- Completion criteria: plugin/package.json 의 version 이 kit 버전과 같고, 검사가 그 일치를 단언한다 (되주입 red 실증)
- Completion criteria: release bump 가 이 파일을 함께 올린다 — 다음 발행에서 사람이 기억하지 않아도 된다
- Completion criteria: 같은 자리에 다른 미갱신 체크인 파일이 있는지 전수로 본다 (.pi-pkg 포함) — 하나만 고치면 결함족이 남는다

## 🛠️ Implementation / Content

- Progress: 예외(_PI_STATIC_BASENAMES)는 의도된 결정이었다 — pi.dev 자산은 렌더 대상이 아니라 손으로 유지하고 check_pi_dev_adapter 가 따로 본다고 문서화돼 있다. 그래서 결정을 뒤집지 않고 결함만 좁혔다: 구조는 손 유지가 맞지만 version 은 kit 을 따라야 하는데 아무도 안 보고 있었다. release_pipeline.write_pi_package_version 신설(파일·필드 부재 시 loud 실패 — 갱신 못 한 것을 성공으로 보고하지 않는다)을 write_workflow_kit_version 에 연결하고, 검사에 kit 버전 일치 단언을 넣었다.
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: 검사가 실제 드리프트를 즉시 적발 (1.2.0 != 1.7.0) → 동기화 함수로 1.7.0 정렬. 손으로 쓰지 않았다
- Result: 결함족 전수: 예외 파일은 package.json 과 .pi-pkg/mcp-settings-snippet.json 둘뿐이고 후자는 버전 문자열이 없다 — 남은 사본 0
- Result: pi 노출 실측 기록을 검사 docstring 에 남겼다 (RPC get_commands 로 skill 4/4, 이름 충돌 0)
- Verification: check_agent_plugin_payload 23/23, 되주입 2종 red 실증 (버전 되돌림 → 검사 red / 대상 파일 부재 → 동기화 loud 실패)
- Follow-up:
