---
id: TASK-2026-08-31-main-003
status: done
created_at: 2026-08-31
source_anchor: generic-task-2026-08-31-main-003
source_path: backlog/2026-08-31.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-08-31-main-003 — backlog-update update 가 열거 필드를 교체해 이전 세션 기록을 지운다 — 손실에 경고가 없다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-31
- Owner: AI Agent
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/common/workflow_writes.py`
  - `workflow-source/workflow_kit/tools/backlog_update.py`

- Description: 실측 2026-08-31: --mode update 로 --affected-document 1건 + --done-criteria 1건을 넘겼더니 파일에 있던 영향 문서 1건과 완료 기준 2건이 전부 사라졌다. merge_task_file 의 _set_list_field 가 '묶음 단위 교체' 라 넘긴 필드를 통째로 갈아치운다. merge_missing 은 라벨 줄이 부재할 때만 경고하고 값이 버려질 때는 침묵한다. 부수 결함: --progress-note 와 --next-step 이 action=append 가 아니라 여러 번 넘기면 argparse 단계에서 마지막 하나만 남는다 — v1.2.2 가 done_criteria/result_note/risks/follow_up/affected_document 에 대해 바로 이 부류를 고쳤는데 이 둘이 빠졌다. merge_task_file 자신이 TASK-2026-08-11-main-023 의 데이터 손실을 고치려 만들어졌고 docstring 이 '파생 writer 는 원문을 보존해야 한다' 고 적고 있는데, 보존이 '안 넘긴 필드' 에만 적용되고 '넘긴 필드' 에는 적용되지 않는다. 완료 기준·영향 문서는 누적 사실이라 교체 의미가 안 맞는 반면 Progress·Status 는 현재값이라 교체가 맞다 — 한 정책을 성격이 다른 두 부류에 함께 쓰고 있는 것이 뿌리다.
- Completion criteria: 누적 성격 필드(완료 기준·영향 문서)의 update 가 기존 값을 보존한다 — 또는 교체를 유지하되 버려지는 값을 loud 하게 경고한다 (소유자 결정 필요)
- Completion criteria: --progress-note·--next-step 을 여러 번 넘겼을 때 조용히 버려지지 않는다
- Completion criteria: 되주입 red 실증 — 손실이 재발하면 검사가 잡는다

## 🛠️ Implementation / Content

- Progress: `2026-08-31 00:30` 기준 완료
- Next session starting point: 소유자 결정: 누적 필드의 병합 정책(보존 / 교체+경고 / 명시 플래그)
- Remaining risks: 교체 의미를 보존으로 바꾸면 '완료 기준을 줄이는' 정당한 갱신 경로가 사라진다 — 명시적 --replace 플래그 등 별도 창구가 필요할 수 있다

## ✅ Outcome

- Result: 누적 필드(affected_documents·done_criteria·result·risks·follow_up)의 update 기본을 병합으로 바꿨다. 정책은 CLI 층(backlog_update)에 두고 merge_task_file 은 저수준 블록 setter 로 남겨 시그니처를 안 깼다. workflow_writes 에 read_task_list_field·read_task_affected_documents·merge_preserving_order 추가(가산). 교체는 --replace-field 로 명시하며 버린 값을 경고에 싣는다. --progress-note·--next-step 은 action=append 로 받아 2회 이상이면 거부(exit 1) — 구분자를 도구가 지어내지 않는다.
- Verification: check_task_multivalue_fields 17/17 (신규 5 cases, 기존 case 4·7 은 --replace-field 계약으로 재작성) · 되주입 3종 red 실증 · mypy clean · 실호스트 실측으로 손실 재현 후 병합 확인
- Follow-up:
