---
id: TASK-2026-09-01-main-005
status: done
created_at: 2026-09-01
source_anchor: generic-task-2026-09-01-main-005
source_path: backlog/2026-09-01.md
kind: generic
wbs: M-007/WBS-7.4
---

# TASK-2026-09-01-main-005 — 발행 게이트가 CI 워크플로 9개 중 1개만 보고 그마저 advisory — v1.8.0 이 smoke red 위에서 나갔다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-01
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/tools/release_pipeline.py`
  - `workflow-source/tests/check_release_ci_gate.py`
  - `docs/RELEASE.md`

- Description: 필수 CI 워크플로 목록을 선언하고 HEAD sha 로 전수 조회해 기본 차단으로 바꾼다
- Completion criteria: 필수 워크플로가 HEAD sha 에서 전부 green 이 아니면 release --apply 가 태그 생성 전에 멈춘다
- Completion criteria: v1.8.0 발행 커밋(6c495e61)의 실제 CI 결과를 먹이면 차단 판정이 나온다
- Completion criteria: 못 읽은 경우(gh 부재·run 없음·진행 중)도 통과가 아니라 차단이다

## 🛠️ Implementation / Content

- Progress: 메모만 덧붙인다
- Next session starting point: 전량 2축 green 확인 후 close
- Remaining risks:

## ✅ Outcome

- Result: REQUIRED_CI_WORKFLOWS(smoke·mypy-strict·os-matrix·mcp-sdk-matrix) 선언 + verify_required_ci() 신설. 질의는 gh run list --commit <HEAD sha> 로 대상을 직접 지정한다 (이전의 --workflow 1개 + --limit 1 + --branch 없음 을 전부 걷음). cmd_release 의 step 1.5 에서 **기본 차단** — apply 경로에서 태그 생성 전에 멈춘다. escape hatch 는 --skip-ci-verify 명시이고 결과에 남는다. dry-run 은 보고만 한다(smoke_count_check 와 같은 관례). 목록 기준은 '소비자에게 나가는 산출물의 정확성을 재는 축' — mkdocs·actionlint 를 넣으면 막을 이유 없는 것이 막혀 사람이 escape hatch 를 습관적으로 쓰게 되고 그러면 게이트가 다시 없어진다.
- Verification: 전량 2축 278/278 ×2 PASS (RUNNER_EXIT=0, 좁은 선언 0) · check_release_ci_gate 8/8 · 반사실 실측: v1.8.0 발행 커밋 6c495e61 의 실제 CI 결과(smoke=failure, 나머지 3종 success)를 그대로 먹이면 ok=False, blocking=['smoke'] — 새 게이트는 그 발행을 막았을 것이다 · 배선 실증: verify_required_ci 를 not-ok 로 주입하고 cmd_release(apply=True) 호출 시 error 반환 + _tag_create 호출 0회(태그 생성 전에 멈춘다) · missing/pending/fetch_error 도 전부 차단(모름 ≠ 안전) 4 case 로 고정
- Follow-up:
