---
id: TASK-2026-09-02-main-002
status: done
created_at: 2026-09-02
source_anchor: generic-task-2026-09-02-main-002
source_path: backlog/2026-09-02.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-09-02-main-002 — session-start 의 상태 불일치 경고가 handoff 를 하루치 backlog 와 비교해 영구 오탐이다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-02
- Owner: AI Agent
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/common/reconcile.py`
  - `workflow-source/workflow_kit/tools/session_start.py`
  - `workflow-source/skills/merge-doc-reconcile/scripts/run_merge_doc_reconcile.py`
  - `workflow-source/tests/check_state_reconcile.py`
  - `workflow-source/examples/output_samples/`
  - `docs/CODE_INDEX.md`
  - `docs/INSTALLATION_AND_USAGE.md`
  - `workflow-source/releases/Beta-v1.9.0.md`

- Description: compare_state_lists 가 handoff 의 열린 작업 전체를 오늘자 daily backlog 하나와 비교한다. append-only 레이아웃에서 in_progress task 는 등록된 날짜의 파일에 있으므로, 날이 바뀌는 순간부터 매 세션 시작마다 거짓 경고가 난다. 실측(2026-09-02): SSOT(backlog/tasks/ 전수 365개) in_progress = TASK-2026-08-25-main-017 로 handoff 와 정확히 일치하는데, 비교 분모인 backlog/2026-09-02.md 는 [] 라 불일치 경고. 결함은 세 겹 — (1) 분모가 SSOT 가 아니라 하루치, (2) ID 정규화 없는 문자열 집합 비교(handoff 는 'TASK-.. 제목', corpus 는 'TASK-..'), (3) 어느 항목이 다른지 안 말한다(그것을 만드는 explain_state_conflicts 가 같은 모듈에 있는데 session-start 는 안 쓴다). 같은 분모 오판이 merge-doc-reconcile 에도 있고, compare_state_lists 를 덮는 검사는 0 이다.
- Completion criteria: session-start 의 in_progress/blocked 비교 분모가 backlog/tasks/ 전수(SSOT)다 — 어제 등록된 in_progress task 가 있어도 경고가 나지 않는다
- Completion criteria: 비교가 task ID 를 키로 한다 — handoff 의 'ID 제목' 과 corpus 의 'ID' 가 같은 항목으로 인식된다
- Completion criteria: 경고가 어느 항목이 어느 쪽에만 있는지 실어서 사람이 손으로 재대조하지 않는다
- Completion criteria: merge-doc-reconcile 도 같은 분모를 쓴다
- Completion criteria: 되주입 red 를 포함한 검사가 신설된다 — 진짜 불일치(handoff 에만 있거나 corpus 에만 있는 항목)는 여전히 red 다

## 🛠️ Implementation / Content

- Progress: `2026-09-03 00:04` 기준 compare_state_lists 의 분모·키·설명 세 겹을 교정하고 검사로 닫았다.
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: 분모: 비교 기준을 task corpus 전수로 옮겼다 (collect_task_corpus_status 신설 — state.json 생성기가 쓰는 것과 같은 집계). corpus 부재는 None 으로 돌려 legacy 프로젝트가 뒤집히지 않게 했다.
- Result: 분모의 뿌리는 profile 이 아니라 방금 고른 backlog 문서 자신(.parent)이다 — 첫 구현이 profile 파생 경로를 써서 seed/claim 스모크가 자기 fixture 대신 이 저장소의 task 와 대조했다 (게이트가 잡았다).
- Result: 키: task ID 로 비교한다. handoff 의 'ID 제목' 과 corpus 의 'ID' 가 같은 항목이다. 선두 inline backtick 도 걷는다 (normalize_backticked 는 문자열 전체가 감싸였을 때만 벗긴다).
- Result: 설명: 경고가 어느 항목이 어느 쪽에만 있는지 짚는다. 같은 모듈의 explain_state_conflicts 는 호출자가 merge-doc-reconcile 하나뿐인 사실상 사문이었고, 이제 둘이 같은 코어를 쓴다.
- Result: merge-doc-reconcile 도 같은 분모로 교정. output_samples 4종의 낡은 문안을 실제 출력으로 맞췄다 (샘플은 절대경로를 걷어낸 수작업 정제본이라 전량 재생성하지 않았다).
- Verification: 전량 2축 280/280 ×2 PASS (RUNNER_EXIT=0, native 134.2s / slash 131.4s, 좁은 선언 0) · mypy strict 0 errors (203 files) · check_state_reconcile 7 cases 신설 PASS · 되주입 red 2방향 실측 (handoff 에만 있는 유령 항목 / handoff 가 corpus 의 열린 작업을 통째로 빠뜨림) · 실증: 이 저장소 session-start 경고 1건 → 0건
- Follow-up: compare_state_lists 를 덮는 검사가 이번까지 0 이었다 — 경고 경로 전반에 같은 공백이 있는지 훑을 후보
