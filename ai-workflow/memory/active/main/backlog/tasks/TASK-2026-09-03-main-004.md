---
id: TASK-2026-09-03-main-004
status: done
created_at: 2026-09-03
source_anchor: generic-task-2026-09-03-main-004
source_path: backlog/2026-09-03.md
kind: generic
wbs: M-007/WBS-7.4
---

# TASK-2026-09-03-main-004 — README 버전 줄 꼬리 3개 리터럴이 자동 수리의 사각 — 발행마다 손이 간다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-03
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/tools/release_pipeline.py`
  - `workflow-source/tests/check_readme_cross_v0_15_12.py`
  - `README.md`
  - `workflow-source/workflow_kit/common/readme_version.py`

- Description: README.md 헤더 '- 버전:' 줄은 self-recover 가 접두부만 고치고, 같은 줄 끝의 'package: standard-ai-workflow X.Y.Z / runtime __version__ = X.Y.Z / latest tag **vX.Y.Z**' 세 리터럴은 손으로 고쳐 왔다 (71·72·73·74차 4회). 게다가 뒤 둘은 어떤 검사도 안 본다 — package 만 check_readme_cross case_3 이 본다.
- Completion criteria: self-recover 가 '- 버전:' 줄의 네 리터럴을 모두 pyproject 파생으로 고친다
- Completion criteria: runtime __version__ / latest tag 를 대조하는 검사 case 가 생기고 되주입 red 로 실증된다
- Completion criteria: 전량 2축 PASS + mypy strict 0

## 🛠️ Implementation / Content

- Progress: `2026-09-03 09:45` 기준 README 헤더 '- 버전:' 줄의 네 자리 중 self-recover 는 맨 앞 하나만 고쳤고, 나머지 셋 중 둘은 어떤 검사도 보지 않았다.
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: 패턴 정본 workflow_kit/common/readme_version.py 신설 — 헤더 '줄' 하나만 범위로 잡는다. 아래 changelog 의 같은 모양 문자열(v0.9.0 항목의 runtime __version__ = v0.9.1-beta)은 그 릴리스 시점의 사실이라 문서 전체 치환은 역사를 덮어쓴다.
- Result: _fix_readme_header_version 이 네 자리를 모두 고친다. 이미 정합이면 파일을 건드리지 않는다(changed 비면 write 생략).
- Result: 판정 두 곳이 같은 정본을 읽는다 — check_drift_prevention case 4(자동 수리의 트리거)와 check_readme_cross case 3(재 verify leg). 패턴을 복제하면 수리와 판정이 갈라진다.
- Result: 되주입 실증: runtime/latest tag 를 1.9.0 으로 되돌리면 옛 판정은 4/4 PASS(=아무것도 못 봤다), 새 판정은 case 3 FAIL 로 두 자리를 이름까지 찍어 보고. 이어서 self-recover 호출이 literals=['runtime','latest_tag'] 를 고쳐 README 가 바이트 단위로 원복됐다.
- Result: meta-watch 가 좁은 선언을 잡아 WATCHES 를 workflow-source/workflow_kit/* 로 넓혔다 — kit import 하나가 패키지 __init__ 을 끌어와 선언 밖 접근 41건이 났다.
- Verification: 전량 2축 280/280 ×2 PASS (RUNNER_EXIT=0, native 136.5s / slash 130.4s, 좁은 선언 0) · mypy strict 0 errors (204 files) · 되주입 red/green 왕복 실증
- Follow-up:
