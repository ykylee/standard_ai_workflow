---
id: TASK-2026-09-07-main-001
status: done
created_at: 2026-09-07
source_anchor: generic-task-2026-09-07-main-001
source_path: backlog/2026-09-07.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-09-07-main-001 — handoff 경로에 legacy fallback 이 없다 — 평평한 workspace 의 기준선이 조용히 사라진다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-07
- Owner: AI Agent
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/common/paths.py`
  - `workflow-source/workflow_kit/common/state/cache.py`

- Description: 디렉터리(backlog/tasks/sessions)는 전부 _branch_scoped_dir 로 branch-scoped→legacy fallback 하는데, session_handoff.md 만 fallback 없이 branch_dir 하위로 인라인 조립된다. 인라인이 kit 전역 8곳이고 관례가 2종(branch_dir/ vs memory_active/)으로 갈려 있다. 실측: examples/acme_delivery_platform 에 session_handoff.md 가 존재하는데도 refresh_workflow_state_cache 가 못 찾아 state.json 의 current_baseline/current_axis/recent_done_items 가 통째로 null/[] 로 떨어진다 (경로를 손으로 넘기면 전부 복원). 경고 없이 조용히 비어서, 미마이그레이션 소비자 저장소는 이 손실을 알 수 없다.
- Completion criteria: handoff 경로 정본 helper 가 paths.py 에 생기고 branch-scoped→legacy fallback 이 _branch_scoped_dir 와 같은 규약으로 동작한다
- Completion criteria: 인라인 조립 자리가 정본을 경유하고, 새 인라인을 막는 정적 판정이 있다
- Completion criteria: 평평한 workspace 의 handoff 유래 필드가 되주입 실증으로 복원된다 (red→green 양방향)

## 🛠️ Implementation / Content

- Progress: 정본 workflow_handoff_path() 를 paths.py 에 신설 — fallback 규칙을 복제하지 않고 기존 _branch_scoped_dir 를 그대로 탄다(파일 leaf 도 exists() 하나로 같은 규칙). 파일명 리터럴도 HANDOFF_FILENAME 하나로 모았다(seed_workspace_memory 의 사본 상수는 별칭으로 강등). 읽는 쪽 8곳을 정본 경유로 바꿨다: cache.py 2자리 · session_start · backlog_update · rotate_workflow_logs · suggest_memory_entries(+ 하드코딩 main 폴백) · detect_scope_drift · check_quickstart_stale_links. 뒤 셋은 workspace+branch 를 아는 caller 라 이미 있던 정본 path_in_active() 로 보냈다 — 새 함수를 만들지 않았다. 쓰는 쪽(seed_workspace_memory / bootstrap_lib)과 선언된 legacy 세트 API(ingest.py)는 branch-scoped 유지.
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: 예제 workspace 실측: 경로를 손으로 넘기지 않아도 current_baseline / current_axis / recent_done_items 와 source_of_truth.session_handoff_path 가 전부 복원됐다. 되주입(정본을 인라인 조립으로 되돌림) → 새 검사 case 2 FAIL + case 1 이 최상위 key 3개 어긋남으로 동반 red.
- Verification: 전량 2축 281/281 x2 PASS (native/slash, FAIL 0, 좁은 선언 0) · mypy strict 0 (204 files) · check_self_application 8/8
- Follow-up:
