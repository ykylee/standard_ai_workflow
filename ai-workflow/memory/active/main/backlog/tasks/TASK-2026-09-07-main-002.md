---
id: TASK-2026-09-07-main-002
status: done
created_at: 2026-09-07
source_anchor: generic-task-2026-09-07-main-002
source_path: backlog/2026-09-07.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-09-07-main-002 — 거짓말하는 cast 가 문자열을 목록으로 속여 environment_constraints 를 한 글자씩 쪼갠다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-07
- Owner: AI Agent
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/common/state/builder.py`
  - `workflow-source/workflow_kit/common/project_docs.py`

- Description: builder.py:406 의 cast(list[str], handoff.get('constraints') or []) 가 실제 타입을 속인다 — HandoffParser 의 constraints 는 get_value 반환이라 str|None 이다. 그 결과 state.json 의 session.environment_constraints 가 문자열을 iterate 한 한 글자 목록이 된다 (실측: 'VPN 미연결 상태에서는 staging API 및 운영 콘솔 접근 불가' → ['V','P','N','미','연',...] 28개). cast 가 mypy strict 를 통과시켜 타입 검사가 못 본다. main 저장소는 handoff 에 '주요 제약' 줄이 없어 항상 [] 라 이 결함이 안 보였고, 그 줄을 가진 유일한 코퍼스가 examples/acme_delivery_platform 인데 그 산출물을 보는 검사가 없었다.
- Completion criteria: constraints 가 파서 경계에서 목록으로 정규화되고, builder 의 거짓 cast 가 사라진다
- Completion criteria: 문자 단위 분해가 되주입으로 red 를 내는 판정이 있다
- Completion criteria: 같은 모양의 다른 거짓 cast 가 남아 있지 않음을 확인한다

## 🛠️ Implementation / Content

- Progress: normalize.py 에 normalize_constraint_values() 신설 — 문자열은 한 항목, 목록은 펼침, None/플레이스홀더는 버림. builder.py 의 cast(list[str], ...) 와 그 변수를 삭제하고 정본 호출로 대체했다. session_start.py 는 원래 [handoff, profile] 로 감싸 맞는 답을 내고 있었는데 변환이 두 곳에 있던 것 자체가 결함이라 같은 정본으로 모았다. is_meaningful_text 도 정본과 같은 파일(normalize.py)로 옮겼다 — 술어를 복제하면 두 자리가 갈라진다. workflow_state.py 의 re-export 는 builder 경유를 끊고 정본에서 직접 가져오게 고쳤다(mypy no_implicit_reexport 가 잡았다).
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: state.json 과 session-start 가 같은 입력에 같은 답을 낸다: ['VPN 미연결 상태에서는 staging API 및 운영 콘솔 접근 불가']. 되주입(문자열 iterate 복원) → case 4 가 '문자열 하나가 28개 항목이 됐다' 로 FAIL, case 1 도 동반 red.
- Verification: 전량 2축 281/281 x2 PASS · mypy strict 0 (204 files)
- Follow-up:
