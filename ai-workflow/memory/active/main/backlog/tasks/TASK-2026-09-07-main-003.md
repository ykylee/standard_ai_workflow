---
id: TASK-2026-09-07-main-003
status: done
created_at: 2026-09-07
source_anchor: generic-task-2026-09-07-main-003
source_path: backlog/2026-09-07.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-09-07-main-003 — 체크인된 예제 state.json 을 생성기 출력과 대조하는 검사가 없다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-07
- Owner: AI Agent
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/tests/check_generate_workflow_state.py`
  - `workflow-source/examples/acme_delivery_platform/state.json`

- Description: workflow-source/examples/acme_delivery_platform/state.json 은 체크인된 생성 산출물인데 어느 검사도 생성기 출력과 대조하지 않는다. check_generate_workflow_state 와 check_workflow_state_generator 둘 다 tmpdir 로 새로 생성해 느슨한 속성(schema_version, generated_at 보존, 상대경로)만 본다 — 체크인본은 읽지도 않는다. 실측: refresh-state --check 로 재니 drift=true, 어긋난 최상위 key 3개(source_of_truth/session/next_documents), exit 1. 이 공백이 main-001(handoff fallback 부재)과 main-002(거짓 cast)를 살려 둔 자리다.
- Completion criteria: 체크인된 예제 state.json 이 현재 생성기 출력과 일치하고, 갈라지면 red 를 내는 판정이 있다
- Completion criteria: 판정이 날짜(generated_at)만 다른 것을 drift 로 오판하지 않는다
- Completion criteria: 예제 산출물이 재생성되어 main-001·002 수리가 반영된다

## 🛠️ Implementation / Content

- Progress: check_example_state_artifact.py 신설 (5 cases): 1) 체크인본 vs 생성기 재생성 대조(generated_at 제외 — 포함하면 매일 red) 2) 평평한 layout 의 handoff legacy fallback 3) handoff 경로 인라인 조립 금지(정적, 허용목록은 만드는 쪽 4곳뿐) 4) 제약 문자열 문자단위 분해 금지(단위 + 실제 산출물 양쪽) 5) 산출물에 저장소 브랜치 이름이 박히지 않을 것. 예제 산출물을 재생성해 main-001/002 수리를 반영했다 — 수리 전에 재생성했으면 handoff 유래 필드가 통째로 비어 결함이 샘플에 굳었을 자리다.
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: case 5 는 2축 게이트가 찾아낸 것이다: 예제에 sessions/ 가 없어 branch-scoped 로 떨어졌고, 그 branch 가 예제가 아니라 저장소를 체크아웃한 브랜치라 native 에서는 main/sessions, slash 에서는 feature/ci-slash-probe/sessions 가 됐다 — 어떤 값으로 커밋해도 다른 축에서 red 인 구조. 예제에 sessions/ 를 실재시켜 legacy 로 떨어뜨렸고 sessions_dir 은 원래 체크인 값 'sessions' 로 돌아왔다(원래 값이 맞았고 내 재생성이 누출을 넣었다). 되주입 4종 전부 의도한 case 에서 red: 산출물 흔들기·fallback 제거·인라인 추가·거짓 cast 복원.
- Verification: 전량 2축 281/281 x2 PASS (native/slash) · 새 검사 단독 5/5 PASS · 되주입 4/4 red 실증
- Follow-up:
