---
id: TASK-2026-09-07-main-005
status: done
created_at: 2026-09-07
source_anchor: generic-task-2026-09-07-main-005
source_path: backlog/2026-09-07.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-09-07-main-005 — INSTALLATION §7.0.2 grok 복구 열이 registry id 를 주는 것처럼 읽힌다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-07
- Owner: AI Agent
- Host:
- Host IP:
- Affected documents:
  - `docs/INSTALLATION_AND_USAGE.md`

- Description: 표의 왼쪽 열이 설치 경로의 <id> 를 plugin-<hash> 로 못박아 둔 상태에서 복구 열이 'uninstall → install' 로만 적혀 있어, uninstall 에도 그 id 를 주는 것으로 읽힌다. 실제로는 플러그인 이름을 받는다. 75차(2026-09-04) 소비 채널 재적용 중 실측으로 걸렸고 '다음 사이클에 표를 고칠 후보' 로 남아 있었다.
- Completion criteria: 복구 열이 인자 형태를 명시하고, id 를 주면 안 된다는 것이 표 안에서 읽힌다
- Completion criteria: 왜 헷갈리는지(=plugin list 가 id 를 먼저 찍는다)를 실측과 함께 남긴다

## 🛠️ Implementation / Content

- Progress: 복구 열을 'plugin uninstall <플러그인 이름> → plugin install' 로 바꾸고 'plugin-<hash> id 를 주면 안 된다 — 왼쪽 열의 <id> 와 다른 값이다' 를 표 안에 넣었다. 읽는 법 6번을 신설해 왜 헷갈리는지를 남겼다: 함정은 grok plugin list 의 출력 형식 자체다 — 'plugin-da9172c3: standard-ai-workflow' 처럼 id 를 먼저 찍어서, CLI 도움말의 'as shown by grok plugin list' 라는 안내가 오히려 id 를 가리키는 것처럼 읽힌다. 명령에 주는 값은 콜론 뒤다. 저장소 전체에 같은 지시의 사본은 없다(grep 확인).
- Next session starting point:
- Remaining risks: CLI 인자 의미론은 정적 검사로 고정할 수 없다 — 이 자리는 실측 기록으로만 지켜진다. grok CLI 가 인자 해석을 바꾸면 표가 다시 낡는다.

## ✅ Outcome

- Result: 2026-09-07 이 호스트 실측으로 재확인. 파괴적 경로(실제 uninstall)는 돌리지 않고 같은 해석기의 읽기 전용 명령으로 쟀다: grok plugin details plugin-da9172c3 → 'Error: Plugin "plugin-da9172c3" not found.' rc 1 / grok plugin details standard-ai-workflow → rc 0 (출력 머리가 그 id 다). uninstall --help 도 '<NAME> Plugin name (as shown by grok plugin list)' 로 적고 있다.
- Verification: check_installation_usage 7/7 PASS · 전량 2축 게이트
- Follow-up:
