---
id: TASK-2026-09-07-main-006
status: done
created_at: 2026-09-07
source_anchor: generic-task-2026-09-07-main-006
source_path: backlog/2026-09-07.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-09-07-main-006 — task ID 채번이 로컬만 봐 같은 브랜치의 다른 호스트와 겹친다 — 코드가 거짓 보증을 적고 있다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-07
- Owner: AI Agent
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/tools/backlog_update.py`
  - `workflow-source/workflow_kit/tools/seed_workspace_memory.py`

- Description: 채번기 둘(backlog_update.suggest_next_task_id · seed_workspace_memory.next_task_id)이 모두 로컬만 읽는다. 그런데 후자의 docstring 은 '번호는 브랜치 안에서만 매긴다 — 브랜치별로 격리돼 있으므로 다른 호스트가 동시에 만들어도 겹치지 않는다' 고 **보증**한다. 브랜치 격리는 브랜치가 다를 때만 성립하고, 두 호스트가 같은 브랜치(main)에 있으면 아무것도 막지 않는다. 75차(2026-09-04) 실측: 두 호스트가 각각 main-002 를 매겼고 push 거절로만 알았다(원격 96f5f386 이 먼저). 그때 내 task 를 main-004 로 비키며 코드 주석·문서의 ID 참조 6곳을 함께 고쳤다. 원격 추적 ref 로 원격 task 목록을 네트워크 없이 읽을 수 있음을 실측했다(git ls-tree origin/main).
- Completion criteria: 채번이 로컬과 원격 추적 ref 를 함께 보고, 원격에만 있는 ID 를 비켜 간다
- Completion criteria: ref 를 못 읽었으면 그 사실을 경고로 말한다 — 모름을 유일성 보증으로 세지 않는다
- Completion criteria: 거짓 보증을 적은 docstring 을 사실에 맞추고, 되주입으로 red 를 실증하는 판정이 있다

## 🛠️ Implementation / Content

- Progress: ID 형식은 건드리지 않았다 — 형식 변경은 TASK_ID_PATTERN·파서·과거 ID 참조를 전부 흔든다. 대신 common/git.py 에 remote_known_task_ids() 정본 신설: 원격 추적 ref(refs/remotes/<remote>/<branch>)를 git ls-tree 로 읽어 원격이 아는 task ID 를 돌려준다. **네트워크를 타지 않는다** — 채번마다 fetch 하면 오프라인에서 멈추고 실패를 조용히 통과로 바꾸기 쉽다. 반환은 RemoteTaskIds(ids, consulted, ref, reason) 로, 못 봤을 때 빈 목록을 '원격에 없다' 로 읽지 못하게 했다(모름 != 안전). 저장소 루트와 pathspec 계산도 정본이 맡는다 — 호출자가 상대경로를 만들게 하면 절대경로를 넘기기 쉽고 그때 ls-tree 는 조용히 빈 목록을 낸다(=막으려던 실패 모드). 채번기 둘 다 reserved_ids 로 그것을 받고, backlog_update 는 ref 를 못 읽었으면 '유일성을 로컬 안에서만 확인했다' 를 warnings 로 낸다.
- Next session starting point:
- Remaining risks: 원격 추적 ref 는 마지막 fetch 시점에 멈춰 있다 — fetch 없이는 유일성이 로컬 안에서만 성립하고, 도구는 그 사실을 경고로만 말한다. 완전한 방지는 채번 시 fetch 나 ID 형식에 호스트 축을 넣는 것인데 둘 다 비용이 크다고 판단했다.

## ✅ Outcome

- Result: 거짓 보증 제거: seed_workspace_memory.next_task_id 의 '다른 호스트가 동시에 만들어도 겹치지 않는다' 와 MEMORY_GOVERNANCE.md §212 의 '동시 생성해도 겹치지 않고' 를 사실에 맞췄다(§124 는 '두 브랜치가' 로 한정돼 있어 참이었다). 실측: 로컬만 → main-007, 원격이 007 을 알면 → main-008. 되주입 3종 전부 의도한 case 에서 red(reserved_ids 무시 · 못 봤는데 consulted=True · 보증 문구 복원). 되주입 3에서 판정이 내 docstring 의 역사 인용까지 물어(75차 README changelog 와 같은 모양) 인용을 풀어썼다.
- Verification: 새 검사 5/5 PASS · 되주입 3/3 red 실증 · mypy strict 0 (204 files) · 전량 2축 게이트
- Follow-up:
