---
id: TASK-2026-09-07-main-007
status: done
created_at: 2026-09-07
source_anchor: generic-task-2026-09-07-main-007
source_path: backlog/2026-09-07.md
kind: generic
wbs: M-007/WBS-7.1
---

# TASK-2026-09-07-main-007 — 설치본에서 모듈 앵커가 증발해 브랜치 해석이 갈라진다 — 소비자 배포처의 모순 뿌리

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-07
- Owner: AI Agent
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/tools/backlog_update.py`
  - `workflow-source/workflow_kit/common/paths.py`
  - `workflow-source/tests/check_branch_resolver_agreement.py`

- Description: get_current_branch() 는 Path(__file__).parents[3] 에 앵커를 박는다. 소스 배치에서는 그것이 저장소 루트라 맞는 답을 내지만, 설치본(uv tool·플러그인 캐시)에서는 .../lib/python3.13 이라 git 저장소가 아니고 **조용히 'main' 으로 떨어진다** — 오류가 아니라 그럴듯한 오답이다. 실측(feature/xyz 소비자 + 설치본 wk): get_current_branch()=main / branch_for_workspace=feature/xyz, path_in_active→active/main/x / workflow_state_path→active/feature/xyz/state.json. wk backlog-update 는 ID 를 TASK-<date>-main-001 로 매기면서 파일은 active/feature/xyz/ 에 쓴다 — ID slug 와 네임스페이스가 어긋난다. 두 브랜치가 모두 main-001 을 매겨 병합 시 충돌하며, slug 가 존재하는 이유가 소비자에서 통째로 무효다. 이 저장소는 곧 모듈의 저장소라 282개 검사 전부 틀린 해석기가 우연히 맞는 조건에서 돈다.
- Completion criteria: 채번 slug 가 workspace 계열로 해석돼 ID 와 네임스페이스가 일치한다
- Completion criteria: workspace 를 아는 자리에서 모듈 앵커로 조용히 떨어지지 않는다 — 떨어지면 그 사실이 드러난다
- Completion criteria: 판정이 **설치본 배치**(모듈 앵커가 저장소가 아닌 곳)에서도 돌고 채번 slug 를 대상에 넣는다

## 🛠️ Implementation / Content

- Progress: ① paths.py 에 resolve_branch_for_workspace() + BranchResolution(slug, source, detail) 신설 — 답과 함께 **출처**를 돌려준다(env/workspace_git/module_repo/default). 조용히 떨어지는 것이 뿌리라 출처가 핵심이다. get_current_branch 와 branch_for_workspace 가 같은 정본(_resolve_module_branch)을 지난다. ② backlog_update 채번이 workspace 계열을 쓰고, 출처가 workspace 가 아니면 경고한다. ③ refresh_wiki_memory._active_path 가 path_in_active 에 branch 를 명시 — 그러면 ledger 예외를 실제로 밟는 소비자 호출이 남지 않는다. ④ check_branch_resolver_agreement 에 계약 2개 추가: 채번 slug == 네임스페이스, **설치본 배치**(패키지를 저장소 아닌 곳으로 실제 복사; symlink 은 resolve() 가 되짚어 흉내가 안 된다)에서도 workspace 기준 유지. ⑤ audit_root_anchors 의 R3 어휘가 get_current_branch 하나뿐이라 새 fallback 을 못 봤다 — _resolve_module_branch 를 추가해 그물을 넓히고 원장 잔재를 정리했다.
- Next session starting point:
- Remaining risks: get_current_branch() 자체는 그대로 모듈 앵커를 본다(sandbox caller 를 위한 의도된 동작). 새 호출자가 workspace 를 알면서 그것을 쓰면 같은 결함이 재발할 수 있어 audit R3 + resolver 검사로만 막힌다.

## ✅ Outcome

- Result: 설치본 배치 + feature/xyz 소비자 실측. 수리 전: ID=TASK-...-main-001 인데 파일은 active/feature/xyz/ (두 브랜치 모두 main-001 → 병합 충돌). 수리 후: ID=TASK-...-feature-xyz-001, 파일 active/feature/xyz/ — 일치. 같은 배치에서 get_current_branch()=main 은 그대로이고 resolve_branch_for_workspace=feature/xyz|workspace_git 이다. 되주입 2종 모두 red: 채번을 모듈 앵커로 되돌리면 case 4 가, workspace 해석을 되돌리면 case 5 만 잡는다(소스 배치에서는 case 4 가 통과한다 — 설치본 축이 없으면 못 본다는 증거).
- Verification: 전량 2축 282/282 x2 PASS (native/slash, FAIL 0, 좁은 선언 0) · resolver 검사 7/7 · audit_root_anchors 미선언 0 · mypy strict 0 (204 files) · check_self_application 8/8 · 되주입 2/2 red
- Follow-up:
