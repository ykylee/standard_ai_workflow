---
id: TASK-2026-09-07-main-010
status: done
created_at: 2026-09-07
source_anchor: generic-task-2026-09-07-main-010
source_path: backlog/2026-09-07.md
kind: generic
wbs: M-007/WBS-7.1
---

# TASK-2026-09-07-main-010 — graph_insights health 가 완료한 일이 늘수록 내려간다 — 100% 발화하는 scope creep 경고와 항등식을 재는 fixture

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-07
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/common/purpose_graph.py`
  - `workflow-source/workflow_kit/common/purpose_context.py`
  - `workflow-source/tests/check_graph_insights_skill_integration_v0_11_2.py`

- Description: session-start/backlog-update 가 매번 health_score 0 / tier poor 와 scope creep 경고 10건을 낸다. 오탐 여부를 실측했더니 오탐이 아니라 지표 자체의 결함 3개였다.
- Completion criteria: 완료 항목 수가 늘어도 health_score 가 내려가지 않는다 (단조성 되주입 red)
- Completion criteria: 동일 접두사 'scope creep 의심:' 을 서로 반대 술어가 쓰지 않는다
- Completion criteria: coverage 100 fixture 가 goal 문자열을 deliverable 에 복사하지 않는다

## 🛠️ Implementation / Content

- Progress: `2026-09-07 13:29` 기준 결함 3건 + 파생 1건을 수리하고 판정으로 닫았다. 소유자 결정은 '수리 + 주장 좁히기'.
- Next session starting point: 소유자 결정: (A) 단조성·접두사 충돌·fixture 3건만 수리하고 health tier 주장은 좁힌다 (B) 지표 은퇴 (C) 매칭 근거 재설계
- Remaining risks: 지표를 고칠지 은퇴시킬지는 설계 판단이라 소유자 결정 전까지 코드를 건드리지 않았다. 토크나이저 개선은 측정 2 가 이미 기각한다 — 임계값만 옮긴다.
- Remaining risks: 지표는 여전히 이 저장소에서 0 을 낸다. 그것은 이제 clamp 자국이 아니라 **정직한 읽기**다 — coverage 0 + 전량 미분류. 어휘 겹침으로 goal↔deliverable 을 잇는 근거 자체를 바꾸려면 ADR 이 필요하고(예: task 의 WBS·로드맵 링크를 쓰거나 goal 을 task 에 명시 선언), 이번 범위 밖으로 남겼다.

## ✅ Outcome

- Result: [측정 1 — 점수가 일을 벌하고 있다] compute_health_score = 100 - uncovered*15 - scope_creep*10 + min(surprising*5, 25). goal 매칭이 0인 상태를 고정하고 완료 항목 수만 늘리면 raw score 가 items=0 일 때 40(fair) → items=3 일 때 25 → items=10 일 때 -35(0 으로 clamp, poor). 즉 **일을 끝낼수록 점수가 내려간다.** 지금 값 0 은 '나쁘다'가 아니라 **-35 가 바닥에 눌린 것**이다(breakdown 실측: base 100 / uncovered_penalty 60 / scope_creep_penalty 100 / surprising_bonus 25). 벌점은 항목 수에 비례해 무한히 커지는데 보너스는 25 에서 막혀 있고, 표준이 recently-done 을 10건으로 상한하므로 활발한 저장소는 영구히 바닥에 고정된다.
- Result: [측정 2 — 매칭은 구조적으로 성립하지 않는다] Goals 는 지향 문장('여러 프로젝트에서 공통으로 사용할 수 있는 표준 AI 협업 워크플로우를 제공'), recent_done_items 는 결함 수리 제목('설치본에서 모듈 앵커가 증발해 브랜치 해석이 갈라진다')이라 어휘가 겹칠 이유가 없다. 4개 goal 전부 overlap 공집합. 한국어 조사 때문이라는 가설을 세워 조사 제거(에서/를/의/와 등) 후 재측정했으나 **여전히 공집합** — 조사 문제가 아니다. 부분문자열 포함까지 완화해도 회수되는 것은 'ai' 와 '처럼' 둘뿐이다. 토크나이저를 개선해도 이 자리는 안 열린다.
- Result: [측정 3 — 100% 발화하는 경고는 신호가 아니다] goal 매칭이 구조적으로 0이므로 recent_done 10건이 전부 surprising 이 되고, 그중 scope_excluded 에도 안 걸리는 10건 전부가 'scope creep 의심' 으로 뜬다. 입력의 100% 에서 발화하는 경고는 정보량이 0이다.
- Result: [측정 4 — 같은 접두사를 반대 술어가 쓴다] purpose_context.check_scope_creep 은 '제외 영역에 매칭되면' creep 이라 한다(맞는 의미). purpose_graph.find_surprising_deliverables 는 '제외 영역에 매칭 안 되면' creep 이라 한다. 두 술어가 정반대인데 경고 문자열은 둘 다 'scope creep 의심:' 으로 시작하고, 출력에서는 top-level scope_creep_warnings 와 graph_insights.scope_creep_warnings 라는 **같은 이름의 두 필드**로 나간다. 이번 backlog-update 실측에서 앞은 [] 이고 뒤는 10건이었다 — 읽는 쪽이 어느 규칙이 울렸는지 구분할 수 없다.
- Result: [측정 5 — 좋은 점수를 내는 유일한 자리가 항등식을 잰다] check_graph_insights_skill_integration_v0_11_2.py case 1 이 health 100/excellent 를 주장하는데, fixture 의 Goals 가 '표준 워크플로우'/'skill 분리' 이고 recent_done_items 가 'v0.1.0 (aaaaaaa): 표준 워크플로우 release'/'v0.2.0 (bbbbbbb): skill 분리' 다 — **goal 문자열을 deliverable 문자열에 그대로 복사**했다. 실제 제목은 goal 의 재진술이 아니므로 이 case 는 항등함수를 잰다. 게다가 fixture 는 낡은 'vX.Y.Z (hash): summary' 형식을 쓴다 — 표준이 'TASK-' 로 바꾼 뒤라 살아있는 생성기는 그 형식을 더는 내지 않고, 실제 10건은 전부 version='unknown' 으로 떨어진다(_RECENT_DONE_PATTERN 불일치). 모듈 docstring 은 아직 'v0.9.4+ 형식' 을 가정한다고 적어 둔 상태다.
- Result: [측정 6 — 아무도 이것을 소비하지 않는다] skills/ 와 harnesses/ 전체에 graph_insights·health_score·scope creep 참조 0건. state.json 에도 저장되지 않는다(과거 12개 리비전 전수 확인, graph_insights 키 부재). 즉 session-start/backlog-update JSON 에만 실려 에이전트가 읽을 뿐이고, 이번 세션이 그것을 '오탐 조사' 후보로 올린 것이 실제 소비의 전부다.
- Result: [수리 1 — 산식] compute_health_score 를 개수 기반에서 **비율** 기반으로 바꿨다: coverage 70*covered/total_goals + classification 30*(1-미분류/전체). SurprisingResult 에 total_items(분모) 를 정본이 함께 돌려주게 했다 — 호출자가 len(surprising) 로 비율을 내면 분모가 분자와 같아져 늘 1.0 이 된다. 실측: 규모 1~30배에서 점수 불변, 분류된 일을 추가해도 절대 안 내려감, 최악=0/최선=100 이며 clamp 에 닿는 입력 없음.
- Result: [수리 2 — 접두사] UNCLASSIFIED_WARNING_PREFIX='미분류 산출물:' 정본 상수 신설로 purpose_context 의 'scope creep 의심:' 과 갈랐다. **필드 이름 scope_creep_warnings 는 건드리지 않았다** — 공개 schema 라 G3 SemVer 보증상 개명은 deprecation 사이클이 필요하다. 대신 두 schema docstring 에 어느 규칙이 그 필드를 채우는지와 '문구로 가른다' 를 적었다.
- Result: [수리 3 — fixture] goal 문자열을 deliverable 에 복사해 두던 13자리를 현재 TASK- 형식 + 복사 아닌 문장으로 교체(두 검사 파일). 옛 fixture 는 coverage 100% 를 주장하면서 total_items 를 0 으로 두어 '산출물이 없는데 그것으로 goal 이 덮였다' 는 실제 파이프라인에서 성립 불가능한 상태였다 — 일관되게 고쳤다.
- Result: [수리 4 — 파서] _TASK_DONE_PATTERN 을 정본 project_docs.TASK_ID_PATTERN 에서 **파생**해 신설(복제하면 갈라진다). 살아 있는 10건이 전부 version='unknown' 이던 것이 해소됐고, 모든 항목이 공유해 순수 잡음이던 task/2026/09/main/008 토큰이 키워드에서 빠졌다. legacy vX.Y.Z (hash) 형식은 계속 받는다.
- Result: [수리 5 — 주장 좁히기] 이 지표가 재는 것은 어휘 겹침이지 건강도가 아니라는 것, 낮은 값은 '어휘가 겹치지 않는다' 로 읽는다는 것, 토크나이저 개선으로는 안 열린다는 실측 근거(조사 제거·부분문자열 완화 후에도 회수 2개)를 module docstring 과 두 schema docstring 에 정본으로 적었다. 어떤 skill·harness 도 이것을 소비하지 않으므로 자동 판정의 근거로 쓰지 않는다는 것도 명시.
- Result: [판정] check_graph_insights_health_monotonic.py 신설 (4 case): 단조성(비율 고정 시 규모 불변 / 분류된 일 추가가 점수를 안 내림 / clamp 자국 없음) · 두 접두사 분리 · fixture 가 goal 복사가 아님 · TASK- 인식과 ID 토큰 누출 0. 되주입 4종(옛 산식 복원 / 접두사 통일 / fixture 항등식 복원 / TASK- 인식 제거) 전부 의도한 case 에서 red.
- Result: [별건 — 재생성 전에 원인부터] 게이트가 check_state_json_generated 를 'state.json 이 생성기 출력과 갈라졌다 → wk refresh-state 로 재생성하라' 로 red 냈으나, 체크인본과 재생성본을 평탄화 대조하니 **차이 나는 키 0개**였다. payload 실측은 drift=false / drifted_keys=[] / **roadmap_drift=true** — 도구는 두 산출물 중 어느 쪽이 갈라져도 rc=1 인데 case_5 의 실패 문구가 **무조건 state.json 을 지목**했다. 시키는 대로 재생성했으면 멀쩡한 산출물을 다시 쓰며 진짜 원인(roadmap)을 지나쳤을 자리다. 문구가 payload 를 읽고 범인을 말하도록 고쳤다.
- Result: [파생 산출물] roadmap_state.json 재생성(task 2건 추가분) · generated_output_schemas.json 재생성(대조 결과 차이는 내가 고친 두 docstring description **뿐**임을 먼저 확인) · smoke count 282→283 (CODE_INDEX·INSTALLATION). **발행된 Beta 노트를 고치라는 요구는 0건** — main-004 수리가 이번에도 물었다.
- Verification: run_all_checks --changed 257/257 PASS (FAIL 0, runner exit 0) · meta-watch 좁은 선언 0 · mypy strict 204 files 0 errors · check_self_application 8/8 · 되주입 4종 전부 red 후 원복 green
- Follow-up: graph_insights.scope_creep_warnings 필드 개명은 deprecation 사이클 후보다 — 이름은 scope creep 인데 담기는 것은 미분류다.
