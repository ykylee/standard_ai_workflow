---
id: TASK-2026-09-07-main-011
status: done
created_at: 2026-09-07
source_anchor: release-task-2026-09-07-main-011
source_path: backlog/2026-09-07.md
kind: release
wbs: M-007/WBS-7.4
---

# TASK-2026-09-07-main-011 — v1.9.4 발행 + 이 호스트 소비자 채널 재적용

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-07
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `docs/INSTALLATION_AND_USAGE.md`

- Description: 지표 수리 두 자리를 소비자에게 보낸다. 등급은 소유자 결정으로 도구 파생값(patch)을 택했다.
- Completion criteria: 태그 v1.9.4 가 발행 커밋을 가리키고 GitHub Release 에 asset 4종이 붙는다
- Completion criteria: 발행본 wheel 이 격리 환경에서 수리를 담고 있음을 실측한다
- Completion criteria: 이 호스트 소비자 채널이 전부 1.9.4 in-sync

## 🛠️ Implementation / Content

- Progress: `2026-09-07 15:10` 기준 지표 수리 두 자리를 소비자에게 보낸다. 등급은 소유자 결정으로 도구 파생값(patch)을 택했다.
- Next session starting point:
- Remaining risks: codex 채널의 plugin add 는 marketplace 를 갈기 전에 부르면 옛 버전을 다시 깐다 — 실제로 한 번 그랬고 출력의 'Installed plugin root: .../1.9.3' 이 그것을 말했다. 순서가 remove -> marketplace 교체 -> add 여야 한다.

## ✅ Outcome

- Result: [발행] 태그 v1.9.4 -> 51ab7cb2, draft=false, prerelease=false, target=main, published 2026-09-07T06:05:40Z. asset 4종 실측: whl 914785 · tar.gz 787584 · claude-code ZIP 16715 · codex ZIP 15309, 전부 uploaded.
- Result: [등급] 도구 파생 patch(미발행 5커밋이 fix/docs/test/chore/release, breaking 0 / feat 0)와 RELEASE.md 1.5절 적용이 갈렸다 — 공개 API 추가 2종(UNCLASSIFIED_WARNING_PREFIX · SurprisingResult.total_items)을 73차 선례처럼 보면 minor 다. 소유자가 도구 파생값을 택했다. 두 추가는 수리를 가능하게 한 보조 장치이지 소비자용 기능이 아니라는 해석이고, '손으로 등급을 덮지 않는다' 는 저장소 규칙과도 맞는다.
- Result: [발행본 실측이 핵심이다] 발행 직전 wk session-start 가 여전히 옛 문구를 냈다 — 수리 실패로 오독할 뻔했으나 원인은 전역 wk 가 발행본(수리 이전) 1.9.3 을 돌고 있어서였다. wk doctor 의 '버전은 같은데 내용이 다르다 (.py 3개 어긋남)' 줄이 그것을 스스로 말했다. 발행 후 GitHub Release 의 wheel 을 격리 venv(PYTHONPATH 없음, 모듈 경로가 site-packages 임을 assert)에 깔아 확인: __version__ 1.9.4 · 새 접두사 · total_items 필드 · 단조성 [50,50,50,50].
- Result: [준비 게이트 12건은 전부 버전 bump 파생] self-recover 가 README 리터럴 4종을 manual_required=0 으로 처리 · 생성기 3종 재실행(재생성 전에 대조해 차이가 버전 문자열뿐임을 확인: 1·8·1건) · 샘플 24개 tool_version 키만 치환 · 문서 리터럴 8줄. docs/RELEASE.md 175행은 역사 인용이라 일부러 남겼다 — 일괄 치환했으면 과거 기록을 거짓으로 만든다.
- Result: [smoke 왕복은 이번에도 없었다] 검사 파일이 282->284 로 늘었는데 발행된 노트를 고치라는 요구 0건. 갱신 대상은 현재 갯수를 주장하는 CODE_INDEX·INSTALLATION 둘뿐이었다 — main-004 수리가 두 사이클 연속 물었다. doc-headers-update step 은 껐다(안 건드린 문서 77개를 오늘로 스탬프하는데 전량 green 이라 어떤 검사도 요구하지 않는다).
- Result: [채널 재적용 — 문서와 실제가 갈린 자리를 찾았다] 전역 wk 는 발행본 wheel 로 uv tool install --force(버전 박힌 URL 계열이라 upgrade 가 무의미) · claude-code 는 plugin update <plugin>@<marketplace> 로 1.9.3->1.9.4 · codex 는 소스 경로에 버전이 박혀 있어 1.9.4 ZIP 을 새 디렉터리에 풀고 marketplace 를 갈아끼웠다 · grok 은 uninstall <플러그인 이름> -> marketplace update -> install --trust. content_drift 3채널 전부 in-sync(대조 12·10·19), 진입점 낡음 0.
- Verification: 전량 2축 284/284 x2 PASS (FAIL 0, RUNNER_EXIT=0, 좁은 선언 0) · mypy strict 204 files 0 · required_ci 필수 4종 success, blocking=[] · smoke_count_check expected 284 found [284,284] · 격리 venv 실측(단조성 [50,50,50,50]) · content_drift 3채널 in-sync
- Follow-up: INSTALLATION 7.0.2 의 codex 복구 열이 'marketplace remove/add' 로 적혀 있으나 실제 명령은 'codex plugin marketplace remove/add' 다 — 문서 갱신 필요
