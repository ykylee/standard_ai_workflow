---
id: TASK-2026-09-07-main-012
status: done
created_at: 2026-09-07
source_anchor: generic-task-2026-09-07-main-012
source_path: backlog/2026-09-07.md
kind: generic
wbs: M-007/WBS-7.4
---

# TASK-2026-09-07-main-012 — INSTALLATION 7.0.2 가 존재하지 않는 맨 marketplace 명령을 적고 있었다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-07
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `docs/INSTALLATION_AND_USAGE.md`

- Description: v1.9.4 채널 재적용 중 문서대로 부른 명령이 조용히 실패하고 옛 버전을 다시 깔았다. 표와 각주의 명령 표기를 실측으로 교정했다.
- Completion criteria: 세 CLI 의 정규 경로와 갱신 동사가 문서에 실측으로 적힌다
- Completion criteria: 맨 형태가 왜 위험한지(특히 claude 의 rc=0)가 기록된다

## 🛠️ Implementation / Content

- Progress: `2026-09-07 16:24` 기준 v1.9.4 채널 재적용 중 문서대로 부른 명령이 조용히 실패하고 옛 버전을 다시 깔았다. 표와 각주의 명령 표기를 실측으로 교정했다.
- Next session starting point:
- Remaining risks: 이 자리는 정적 판정으로 못 닫는다 — CLI 인자 의미론이라 검사가 고정할 수 없고, 벤더가 명령 트리를 바꾸면 표가 다시 낡는다. 6번 각주(grok id vs 이름)와 같은 부류로, 실측 기록으로만 지켜진다.

## ✅ Outcome

- Result: [증상] v1.9.4 재적용 중 문서의 'marketplace remove -> marketplace add -> plugin add' 를 그대로 따랐더니 앞 둘이 usage 오류로 죽었고, 그 상태에서 plugin add 가 옛 marketplace 를 보고 1.9.3 을 다시 깔았다. 출력이 'Installed plugin root: .../standard-ai-workflow/1.9.3' 이었다 — 명령은 성공했고 버전만 틀렸다. 성공 출력이라 눈으로 안 보면 지나친다.
- Result: [뿌리] 맨 'marketplace ...' 라는 최상위 명령은 세 CLI 어디에도 없다. 전부 'plugin' 아래에 있고 갱신 동사도 갈린다 — claude=update / codex=upgrade / grok=update. 같은 문서 282행은 이미 'codex plugin marketplace add' 로 올바르게 적고 있었다. 즉 문서 안에서 사본이 갈린 것이지 처음부터 틀린 게 아니었다.
- Result: [claude 는 rc 로 못 가른다 — 이번 실측의 핵심] codex/grok 은 맨 형태에서 rc 2 로 죽는데, claude 는 rc 0 을 낸다. 명령으로 실행돼서가 아니라 'marketplace list' 가 프롬프트로 먹혀 모델이 산문으로 답했기 때문이다. 이 채널에서는 rc=0 이 명령 동작의 근거가 못 된다 — 출력이 CLI 의 것인지 모델의 것인지 눈으로 갈라야 한다. 처음 잰 rc 표를 그대로 믿었으면 'claude 는 맨 형태도 된다' 고 문서에 적을 뻔했다.
- Result: [측정 자체를 한 번 틀렸다] 첫 시도에서 셸 함수 없이 $c 로 반복 실행해 rc 를 쟀는데, 여러 단어 문자열이 통째로 명령명으로 해석돼 전부 rc=127/0 이 나왔다. 그 값으로 결론을 냈으면 전부 오답이었다. probe() { "$@"; } 로 고쳐 다시 쟀다.
- Result: [교정] 표의 codex 행(update 열 + 복구 열) · 각주 2 · 각주 4 · 각주 5 의 명령을 전부 CLI 접두사까지 적었다. 각주 7 신설 — 세 CLI 정규 경로/동사 표, 조용한 실패가 옛 버전을 되깔는 경로, claude 의 rc=0 함정. '읽는 법 여섯 가지'를 일곱으로 갱신. deploy_doctor.py 는 이미 올바른 형태를 쓰고 있어 손대지 않았다.
- Verification: run_all_checks --changed 80/80 PASS, FAIL 0, 좁은 선언 0. 명령 형태는 읽기 전용 --help 와 list 로만 실측(파괴적 경로 미실행).
- Follow-up:
