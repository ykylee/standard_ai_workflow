---
id: TASK-2026-09-18-main-001
status: done
created_at: 2026-09-18
source_anchor: generic-task-2026-09-18-main-001
source_path: backlog/2026-09-18.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-09-18-main-001 — backlog-update 가 --progress-note 미지정 시 이전 Progress 를 task_brief 로 덮는다 — 새 타임스탬프가 붙어 갱신처럼 보인다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-18
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/tools/backlog_update.py`

- Description: update 모드에서 progress 는 scalar_updates 에 **무조건** 들어가고(backlog_update.py:741), --progress-note 가 없으면 :673-675 가 task_brief 로 지어내 현재 시각을 찍는다. 다른 스칼라(priority/owner/host_name/host_ip/next_step/validation)는 전부 'if args.X:' 로 보존되는데 progress 만 예외다. 그래서 상태만 바꾸려는 호출이 이전 세션의 진행 기록을 설명문으로 덮고, 새 타임스탬프 때문에 갱신된 것처럼 보인다 (2026-09-18 78차 실측 — TASK-2026-08-25-main-017 의 77차 Progress 가 그렇게 소실됐다).
- Completion criteria: update 에서 --task-brief 를 생략할 수 있다 — 생략 시 기존 진행 현황과 작업 내용이 보존된다.
- Completion criteria: brief 를 준 호출의 동작은 불변 — 기존 계약(update 의 brief = 진행 한 줄)을 깨지 않는다.
- Completion criteria: create 는 여전히 필수이고, 공백 문자열은 부재와 구분해 거부한다.
- Completion criteria: 판정이 그 규칙을 지킨다 — 되주입 시 red.

## 🛠️ Implementation / Content

- Progress: 2026-09-18 78차: 소유자가 C안(생략 허용)을 택해 구현·검증 완료.
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: [측정] 78차가 실제로 데이터를 잃었다 — 상태만 바꾸려고 이전 Description 을 --task-brief 로 넘겼더니 TASK-2026-08-25-main-017 의 77차 Progress 가 그 설명문으로 덮였고, 현재 시각이 찍혀 갱신처럼 보였다.
- Result: [측정] B 안을 시제품으로 구현해 실측했다: 미지정→보존 / 명시→반영 / Description 보존 3축 통과, 신설 case 통과. 그러나 기존 case 1건이 red — 계약을 깨는 변경임이 드러나 **원복**했다 (9/9 PASS 복귀). 저장소에 남은 변경 0.
- Result: [철회] 같은 조사에서 먼저 보고했던 'backlog-update --apply 가 roadmap_state.json 을 낡은 상태로 재생성한다' 는 **사실이 아니다**. mtime 실측: --apply 는 그 파일을 쓰지 않는다. 오늘 날짜가 찍힌 낡은 내용은 세션 이전부터 워킹 트리에 있던 더티 상태였다. generate_roadmap_state() 가 이름과 달리 디스크에 쓴다는 것만 참이다.
- Result: [결정] 소유자가 C안 채택 — 계약 변경(B) 대신 **additive**: update 에서 --task-brief 를 선택 인자로. 기존 호출은 전부 brief 를 넘기므로 동작 불변이고, 생략이 가능해지면서 '의미 없는 문장을 억지로 넘겨 진행 기록을 덮는' 함정 자체가 사라진다.
- Result: [구현] backlog_update.py 6곳: argparse required 해제 + help 에 모드별 이중 의미 명시 / 최상단 guard 를 '부재 vs 공백' 으로 분리 / 모드 확정 후 create 전용 필수 guard 신설(auto→create 포함) / progress 합성은 brief 가 있을 때만 / update 병합은 쓸 값이 있을 때만 progress 를 넣는다 / 빈 '작업 내용' 채우기도 brief 가 있을 때만.
- Result: [판정] check_backlog_update_layout 에 case 10 test_update_task_brief_is_optional 신설 — 생략 update 의 진행·작업내용·상태 / create 거부 / 공백 거부 4축. rc 비검사 헬퍼 _run_raw 를 더했다(거부 경로는 assert rc==0 을 통과할 수 없다).
- Verification: check_backlog_update_layout 10/10 PASS · check_backlog_update PASS · 되주입 3종(무조건 덮기 / create guard 무력화 / 부재도 거부) 전부 의도한 case 에서 red 후 원복 green · --changed 게이트 red 3건은 전부 메모리 산출물(state/roadmap 재생성·handoff 후보)이고 코드 결함 0
- Follow-up:
