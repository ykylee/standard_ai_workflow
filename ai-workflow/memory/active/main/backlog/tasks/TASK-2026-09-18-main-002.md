---
id: TASK-2026-09-18-main-002
status: done
created_at: 2026-09-18
source_anchor: generic-task-2026-09-18-main-002
source_path: backlog/2026-09-18.md
kind: generic
wbs: M-007/WBS-7.1
---

# TASK-2026-09-18-main-002 — 이 호스트(plex) 채널 v1.9.4 재적용 — claude-code 가 v1.1.8-beta 로 5주 낡아 있었다

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-09-18
- Owner: AI Agent
- Host: plex
- Host IP: 192.168.0.121
- Affected documents:
  - `docs/INSTALLATION_AND_USAGE.md`

- Description: 로컬 main 이 158+1 커밋 뒤처져 있었고, claude-code 플러그인 설치본은 2026-08-13 의 v1.1.8-beta 였다 (페이로드 다름 7 / 없음 4 — 스킬 4종 SKILL.md 전부 + hooks.json + rules.md, agents/openai.yaml 4개는 부재). 동기화 후 .venv 재설치 + 설치 가능한 채널 4개를 전부 v1.9.4 로 맞췄다
- Completion criteria: wk doctor content_drift 에서 설치 가능한 전 채널이 v1.9.4 [in-sync]
- Completion criteria: 저장소 워킹 트리가 설치 과정에서 오염되지 않는다
- Completion criteria: 개발 환경 smoke (check_self_application) green

## 🛠️ Implementation / Content

- Progress: `2026-09-18 00:19` 기준 로컬 main 이 158+1 커밋 뒤처져 있었고, claude-code 플러그인 설치본은 2026-08-13 의 v1.1.8-beta 였다 (페이로드 다름 7 / 없음 4 — 스킬 4종 SKILL.md 전부 + hooks.json + rules.md, agents/openai.yaml 4개는 부재). 동기화 후 .venv 재설치 + 설치 가능한 채널 4개를 전부 v1.9.4 로 맞췄다
- Next session starting point: claude CLI 재시작 — runtime_load 가 이 세션(pid 6722, 23:54:14 시작)을 설치(00:13:49)보다 먼저 시작한 낡은 호스트로 잡는다. /clear 로는 부족하다
- Remaining risks: pi-dev 채널은 pi 실행 파일 부재로 미설치 — 소유자 판단으로 무시 (채널 갱신이 아니라 CLI 자체를 새로 까는 일)
- Remaining risks: codex marketplace 는 버전 경로에 고정된다 — 다음 버전에는 marketplace remove → 새 경로 add 가 필요하다 (§7.0.2 읽는 법 5)
- Remaining risks: content_drift 의 in-sync 는 파일 일치일 뿐 하네스가 실제로 스킬을 노출한다는 증거가 아니다 — 마지막 한 칸은 실제 호출로만 재진다

## ✅ Outcome

- Result: claude-code: v1.1.8-beta → 1.9.4. 버전이 달라 §7.0.2 의 '다른 버전' 경로(claude plugin update <plugin>@<marketplace>)가 그대로 먹혔다. 대조 12 in-sync. 옛 v1.1.8-beta 디렉터리는 남지만 installPath 선언이 새것을 가리켜 발견으로 세지 않는다
- Result: codex: 신규 설치. 저장소에 codex payload 가 없어 GitHub Release v1.9.4 의 ZIP 을 ~/.local/share/standard-ai-workflow/ 에 풀어 marketplace 로 등록했다 — 이 경로가 사라지면 채널이 깨지므로 세션 scratchpad 를 쓰지 않았다. 대조 10 in-sync
- Result: grok-build: 신규 설치 (grok plugin install ./plugin --trust). 대조 19 in-sync. 경로 id 는 plugin-ea25f5a7, 명령에 주는 값은 standard-ai-workflow
- Result: antigravity: 신규 설치 (agy plugin install ./plugin). validate/install 모두 skills 4 · mcpServers 1 — hooks 는 payload 에 없다(문서 선언과 일치). 대조 19 in-sync
- Result: .venv 재설치: python 3.13.7 / kit 1.9.4 editable / mcp 1.27.0 · mypy 2.1.0 · jsonschema 4.26.0. wk PATH symlink 유효
- Verification: check_self_application.py 8/8 PASS · wk doctor content_drift 4채널 전부 v1.9.4 [in-sync] · git status --porcelain 비어 있음
- Follow-up:
