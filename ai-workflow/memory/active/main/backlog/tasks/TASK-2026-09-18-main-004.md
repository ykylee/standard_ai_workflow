---
id: TASK-2026-09-18-main-004
status: done
created_at: 2026-09-18
source_anchor: generic-task-2026-09-18-main-004
source_path: backlog/2026-09-18.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-09-18-main-004 — backlog-update --apply 가 roadmap_state.json 을 안 따라간다 — 파생물 갱신을 쓰는 층에 흡수

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-18
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/tools/backlog_update.py`
  - `workflow-source/tests/check_roadmap_wiring.py`
  - `workflow-source/core/roadmap_milestone_wbs_spec.md`

- Description: task frontmatter(wbs·status)는 roadmap_state.json 의 SSOT 인데, 그것을 쓰는 backlog-update --apply 가 state.json 만 재생성하고 roadmap_state.json 은 두고 갔다. 뒤처짐은 push 게이트의 check_roadmap_state_generated 가 red 를 낼 때까지 안 보이고, 2026-09-17·2026-09-18 이틀에 두 번 수동 wk refresh-state 로 풀었다.
- Completion criteria: backlog-update --apply 직후 roadmap_state.json 이 재생성 결과와 일치한다 (state_matches_regeneration=True)
- Completion criteria: 그 write 가 written_paths 에 보인다 — 조용한 쓰기를 만들지 않는다
- Completion criteria: --apply 없는 draft 는 여전히 아무것도 쓰지 않고, roadmap 부재 프로젝트는 파일이 안 생긴다 (additive)
- Completion criteria: 재생성 호출을 되주입으로 제거하면 검사가 red 다

## 🛠️ Implementation / Content

- Progress: 재현 → 수리 → 검사 신설 → 되주입 실증 → 게이트 전량 2축까지 완료.
- Next session starting point: push 후 CI 4종(smoke / mcp-sdk-matrix / mypy-strict / os-matrix) 확인
- Remaining risks: 이 수리가 덮는 것은 backlog-update 경로뿐이다. roadmap/ 마일스톤 파일의 선언 status 를 손으로 고치는 경로는 여전히 세션 종료의 wk refresh-state 가 유일한 창구다 — 그쪽은 사람이 여는 파일이라 같은 '쓰는 층' 논리가 안 붙는다.

## ✅ Outcome

- Result: [결함을 먼저 재현했다] check_roadmap_gates 의 fixture 로 roadmap + 브랜치 메모리를 세우고 roadmap_state.json 을 생성한 뒤 backlog-update --apply --wbs 를 돌렸다. written_paths 4개(daily backlog / task SSOT / handoff / state.json)에 roadmap_state.json 이 없고, state_matches_regeneration=False — 경고도 없이 뒤처진다.
- Result: [뿌리는 비대칭이었다] 같은 자리(backlog_update.py)가 state.json 은 이미 --apply 에서 재생성하고 있었다(v1.0.1/v1.0.2). roadmap_state.json 만 그 규약 밖에 있었고, 유일한 갱신 창구가 wk refresh-state 였다 — 즉 '쓴 사람이 나중에 따로 기억해야' 했다.
- Result: [수리] --apply 경로에서 state.json 재생성 직후 generate_roadmap_state(workspace_root) 를 같이 부른다. roadmap 부재는 None 반환 = 해당 없음(additive). write 는 written_paths 에 싣고, 신규 생성이면 created_paths / 기존이면 updated_paths 로 갈라 적는다 — 조용한 쓰기를 만들지 않는다(v1.0.2 규칙).
- Result: [판정은 산출물을 잰다] check_roadmap_wiring 에 case 신설(5→6). 소스 문자열이 아니라 fixture 에서 CLI 를 돌린 뒤 state_matches_regeneration 과 written_paths 를 본다. 부수 축 2개를 같이 고정: --apply 없는 draft 는 여전히 파일을 안 만들고(dry-run 오염 금지), roadmap 부재 프로젝트에도 안 만든다.
- Result: [정본 갱신] roadmap_milestone_wbs_spec §7.1 이 '재생성은 wk refresh-state' 만 적고 있었다 — 그 선언이 backlog-update 를 규약 밖에 둔 근거였다. SSOT 가 task frontmatter 이기도 하다는 사실과 '쓰는 층이 같이 갱신한다' 를 명시하고 §9 배선 표도 맞췄다.
- Result: [자기 적용] 이 task 자신을 wk backlog-update --apply 로 등록하자 roadmap_state.json 이 같은 호출에서 갱신됐고, 직후 wk refresh-state --check 가 drift=False / roadmap_drift=False 였다. 수리 전이라면 여기서 roadmap_drift=True 다.
- Verification: 되주입(재생성 호출 제거) → check_roadmap_wiring red, 두 사유 모두 지목('roadmap_state 가 뒤처졌다' + 'written_paths 에 안 보인다'). 사본 백업으로 원복 후 6/6 green · 커밋 전 --changed 266/266 green + check_self_application 8/8 · push 게이트 전량 2축(native/slash) 284/284 ×2 green
- Follow-up:
