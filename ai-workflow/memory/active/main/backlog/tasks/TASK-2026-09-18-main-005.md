---
id: TASK-2026-09-18-main-005
status: done
created_at: 2026-09-18
source_anchor: generic-task-2026-09-18-main-005
source_path: backlog/2026-09-18.md
kind: generic
wbs: M-007/WBS-7.3
---

# TASK-2026-09-18-main-005 — runtime_load 재측정 + '노출 한 칸' 첫 실측 — 그리고 캐시 사본은 읽히지 않는다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-18
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `docs/INSTALLATION_AND_USAGE.md`

- Description: 79차 ⑦ 잔여(이 세션 프로세스가 설치보다 먼저 떠서 runtime_load 가 낡은 호스트 1 로 센다)를 CLI 재시작 뒤 재측정한다. 겸해서 §7.0.1 이 '실제 호출로만 재진다' 고 남겨 둔 노출 한 칸을, 이 세션이 그 실제 호출이므로 잰다.
- Completion criteria: runtime_load 가 이 호스트의 claude-code 를 낡은 호스트 0 으로 센다
- Completion criteria: 스킬 노출을 실제 호출로 재고, 저장소 cwd 와 중립 cwd 를 갈라서 잰다

## 🛠️ Implementation / Content

- Progress: 재측정 완료. 부수로 캐시/소스 갈림이 드러나 되주입까지 갔다.
- Next session starting point:
- Remaining risks: 이 판정은 **소스 유형에 달렸다**. directory 소스(개발 호스트)에서만 저장소가 서빙된다 — github 소스로 설치한 소비자는 캐시 사본이 곧 설치본일 가능성이 높고, 그쪽은 이 호스트에서 잴 수 없다. §7.0.2 표의 claude-code 행을 통째로 뒤집지 말 것.

## ✅ Outcome

- Result: [79차 잔여는 재시작으로 풀렸다 — 실측] 이 세션 프로세스는 01:21:06 시작, 설치 선언은 00:13:49(installed_plugins.json lastUpdated). runtime_load: claude-code 최신 호스트 1 / 낡은 호스트 0. codex·grok-build·antigravity 는 0/0 인데 탐침이 스스로 '통과가 아니라 해당 없음' 이라고 말한다 — 돌고 있는 프로세스가 없을 뿐이다.
- Result: [노출 한 칸을 처음 쟀다] 2026-08-20 에는 in-sync + 인벤토리 4종인데 세션 로드 0종이었다(Unknown skill). 지금 이 호스트는 그 반대다: 스킬 4종(session-start / session-end / backlog-update / doc-sync)이 전부 노출되고 실제 호출이 성공한다. 저장소 cwd 뿐 아니라 중립 cwd(/home/yklee)에서 띄운 새 프로세스에서도 4종 전부 보인다 — 즉 프로젝트 오버레이가 아니라 user 스코프 설치가 노출하고 있다.
- Result: [예상 밖 — 읽히는 것은 캐시 사본이 아니다] 실제 호출이 보고한 skill base directory 가 캐시(~/.claude/plugins/cache/.../1.9.4/)가 아니라 **저장소 워킹 트리**(<repo>/plugin/skills/...)였다. 중립 cwd 에서도 같다. 원인은 마켓플레이스 소스 유형이다 — known_marketplaces.json 에서 standard-ai-workflow 는 source=directory, installLocation=<repo> 다. 캐시 사본은 별도 inode 의 실사본으로 00:13:49 에 만들어져 있지만 서빙되지 않는다.
- Result: [되주입으로 확정했다] 캐시 사본 doc-sync/SKILL.md 에 마커(MARKER-CACHE-7F3A2B)를 넣고 중립 cwd 에서 새 프로세스를 띄워 스킬 지시문에 그 문자열이 있는지 물었다 — NO. 사본 백업으로 원복 후 저장소 사본과 byte 동일 확인. 파일 비교만으로는 두 사본이 지금 동일이라 원리적으로 못 가르는 자리였다.
- Result: [이것이 여는 축] wk doctor 의 content_drift 는 캐시 사본을 정본 페이로드와 대조한다. directory 소스에서는 그것이 **로드되는 사본이 아니다** — 재는 대상과 읽히는 대상이 다르다. 게다가 이 저장소에서는 서빙 경로가 정본 생성기의 입력과 같은 디렉터리라, 그쪽을 재면 이번엔 자기 자신과의 비교가 된다(통과로 셀 수 없는 종류의 green). 수리는 TASK-2026-09-18-main-006 으로 분리한다.
- Verification: runtime_load 실측(프로세스 01:21:06 > 설치 00:13:49, 낡은 호스트 0) · 노출 실측 2회(저장소 cwd 세션 = 이 세션의 /standard-ai-workflow:session-start 호출 성공 · 중립 cwd claude -p 에서 스킬 4종 열거) · 캐시 마커 되주입 NO + 원복 byte 동일
- Follow-up: TASK-2026-09-18-main-006 — doctor 가 마켓플레이스 소스 유형을 읽어 '무엇이 서빙되는가' 를 가르게 한다
