---
id: TASK-2026-09-18-main-006
status: done
created_at: 2026-09-18
source_anchor: generic-task-2026-09-18-main-006
source_path: backlog/2026-09-18.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-09-18-main-006 — doctor 의 content_drift 가 서빙되지 않는 사본을 재고 있다 — 마켓플레이스 소스 유형을 읽어야 한다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-18
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/workflow_kit/deploy_doctor.py`
  - `workflow-source/tests/check_deploy_doctor.py`
  - `docs/INSTALLATION_AND_USAGE.md`

- Description: claude-code 채널에서 마켓플레이스가 directory 소스면 하네스는 캐시가 아니라 소스 디렉터리를 읽는다(TASK-2026-09-18-main-005 되주입 실증). content_drift 는 캐시만 대조하므로 이 경우 재는 대상과 읽히는 대상이 다르다.
- Completion criteria: doctor 가 ~/.claude/plugins/known_marketplaces.json 의 source 유형과 installLocation 을 읽어 보고에 싣는다 (codex 의 _codex_marketplace_sources 와 같은 부류 — 경유지를 선언에서 읽는다)
- Completion criteria: directory 소스일 때 캐시 사본을 served=False 로 표시하고, 서빙되지 않는 사본의 드리프트를 발견으로 내지 않는다 — 아무도 안 읽는 사본으로 거짓 발견을 만들지 않는다 (superseded 선례와 같은 규율)
- Completion criteria: directory 소스일 때 **서빙 경로를 실제로 대조한다** — 캐시 대신 그쪽이 in_sync 판정의 대상이 된다
- Completion criteria: fixture 되주입: directory / github 두 소스 유형에서 판정이 갈리고, 서빙 경로를 오염시키면 red 가 난다

## 🛠️ Implementation / Content

- Progress: 구현 + 검사 3종 + 되주입 2종 완료.
- Next session starting point: _codex_marketplace_sources 를 본떠 _claude_marketplace_sources 를 만들고 content_drift 의 active 판정에 소스 유형 축을 더한다
- Remaining risks: github 소스 설치는 이 호스트에서 재현할 수 없다 — 그 셀은 실측 없이 추정으로 채우지 말 것. §7.0.2 표에 소스 유형 축이 없다는 것 자체가 결함이다.
- Remaining risks: github 소스 셀은 여전히 실측이 아니다 — 이 호스트에 원격 마켓플레이스 설치가 없어 재현할 수 없었다. 소비자가 그 경로로 설치하면 캐시가 읽힌다는 전제가 틀릴 여지가 남아 있고, 그 사실은 declared_unmeasured 에 적혀 있다.

## ✅ Outcome

- Result: [완료 기준 3번의 전제가 틀렸다 — 착수 전 정정] 원래 '서빙 경로가 정본 생성기의 입력과 같은 디렉터리라 자기 자신과의 비교가 된다' 고 적었다. 실측하니 아니다: _canonical_payload 는 render_agent_plugin() 을 부르고 그것은 load_standard_rules 로 **core/global_workflow_standard.md 에서 렌더**한다 — plugin/ 을 읽지 않는다. plugin/ 은 같은 생성기의 산출물이지 입력이 아니다. 따라서 서빙 경로(<repo>/plugin)를 정본과 대조하는 것은 동어반복이 아니라 **커밋된 plugin/ 이 낡았는지를 재는 실제 대조**다. 기준을 '서빙 경로를 실제로 대조한다' 로 바꿨다.
- Result: [경유지를 선언에서 읽는다] _claude_marketplace_sources 신설 — installed_plugins.json 의 <plugin>@<market> 키에서 마켓 이름을 얻고 known_marketplaces.json 에서 source 유형과 installLocation 을 읽는다. codex 의 _codex_marketplace_sources 와 같은 부류지만 한 칸 더 간다: 이 채널에서는 **소스 유형이 무엇이 읽히는지를 바꾼다**.
- Result: [서빙 경로를 실제로 대조한다] directory 소스면 <소스>/.claude-plugin/marketplace.json 의 plugins[].source 로 서빙 루트를 정하고(plugin/ 을 관례로 가정하지 않는다) 그 경로를 정본과 대조해 caches 에 넣는다. 이것을 안 재면 그 호스트에서는 아무것도 잰 것이 없다 — 캐시는 안 읽히고 소스는 안 재니까.
- Result: [읽히지 않는 사본은 발견을 내지 않는다] 캐시 레코드에 served 축을 붙이고, out_of_sync 에서 served=False 를 뺀다. 대신 unserved 목록에 남긴다 — superseded 와 같은 규율이다(아무도 안 읽는 사본으로 거짓 발견을 만들지 않되, 침묵으로 지우지도 않는다). 그리고 '캐시가 아니라 <경로> 가 읽힌다' 를 발견으로 말한다 — 오류가 아니라 상태이지만, 모르면 캐시 in-sync 를 노출의 증거로 읽는다.
- Result: [리터럴 codex 를 걷었다] 보고 렌더러가 marketplace 행을 무조건 'codex marketplace …' 로 찍고 있었다. 목록이 채널 혼합이 되면서 claude 행이 codex 로 찍히는 자리였다 — 채널을 행에서 읽게 고쳤고, 검사의 _codex_source_row 도 위치가 아니라 harness 로 좁혔다.
- Result: [원격 소스는 가정으로 남긴다] github 소스는 캐시가 읽히는 것으로 보되, 그것을 되주입으로 확인한 적이 없다는 사실을 declared_unmeasured 에 적는다. directory 소스만 실증됐다 — 재지 못한 것을 확인으로 적지 않는다.
- Verification: check_deploy_doctor 39/39 (36→39, 신설 3종) · 되주입 2종 전부 red 후 원복 green: ①소스 유형 축 제거 → 'served=False 로 안 적었다' + '아무도 안 읽는 사본으로 발견을 만들었다' ②서빙 경로 대조 제거 → '아예 대조하지 않았다' + '읽히는 사본이 오염됐는데 발견이 없다'. 오염을 캐시/서빙 어느 쪽에 넣느냐로 판정이 갈리는 것까지 case 로 고정 · --changed 252/252 green · check_self_application 8/8 · mypy strict 통과
- Follow-up:
