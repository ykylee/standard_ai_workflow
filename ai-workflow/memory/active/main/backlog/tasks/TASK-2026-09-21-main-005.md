---
id: TASK-2026-09-21-main-005
status: done
created_at: 2026-09-21
source_anchor: generic-task-2026-09-21-main-005
source_path: backlog/2026-09-21.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-09-21-main-005 — 최소 Python 문법 호환을 재는 축이 없다 — 로컬 3.13 / 선언 하한 3.10 이라 CI 에서만 터지는 문법을 쓸 수 있다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-21
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/pyproject.toml`
  - `workflow-source/tests/`

- Description: main-004 에서 중첩 f-string(PEP 701, 3.12+)을 쓸 뻔했다. 로컬은 3.13 이라 통과하고 CI(3.10/3.11)에서만 SyntaxError 가 났을 자리였다. 손으로 ast.parse(feature_version) 를 돌려 잡았지만 상시 판정이 없다. 저장소가 같은 모양('로컬 green + CI red')을 SDK 매트릭스·브랜치 매트릭스 두 축에서 이미 겪고 로컬 재현 수단을 만들어 둔 전례가 있다 — 세 번째 축이다.
- Completion criteria: 기대 하한을 CI 매트릭스가 아니라 pyproject 의 requires-python 선언에서 파생한다 — 두 곳에 적으면 갈라진다
- Completion criteria: 소스 전수를 그 하한으로 ast.parse 한다 (현재 551개, 실측 3.10/3.11/3.12 전부 파싱 실패 0 — 예방 축이다)
- Completion criteria: 되주입: 3.12+ 전용 문법을 심으면 red, 원복하면 green

## 🛠️ Implementation / Content

- Progress: 실물 하한 해석기로 전수 컴파일 + 부분 측정 fallback. 게이트 통과.
- Next session starting point:
- Remaining risks: 덤으로 잠재 결함 1건 발견: check_v0_7_26_sync_release_hash.py 7행·93행의 invalid escape sequence (SyntaxWarning, 백슬래시+백틱). 파싱은 되지만 Python 이 장기적으로 에러로 올린다고 예고한 것이다 — 같은 검사에서 같이 볼지 판단 필요
- Remaining risks: 게이트가 내 결함 2건을 잡았다: (a) check_deployed_layout — main() 이 Path(__file__).parents[3] 로 체크아웃 레이아웃을 역산했다. 이 저장소가 여섯 번 고친 결함족이고 정본 resolve_workspace_root() 가 있는데 참조하지 않았다. (b) meta-watch 좁은 선언 38건 — probe 가 examples/ 와 mcp_servers/*/scripts/ 까지 훑는데 WATCHES 는 4개 glob 뿐이었다. 전역 선언으로 교정.

## ✅ Outcome

- Result: 착수 전 실측: requires-python='>=3.10' (선언 하한). CI 워크플로는 3.10/3.11/3.12 를 섞어 쓴다. 로컬 .venv 는 3.13.7. 소스 551개를 3.10/3.11/3.12 로 각각 파싱해 실패 0 확인 — 지금 깨진 것은 없고 다음 슬립을 막는 축이다.
- Result: 실측: 이 호스트에 3.10/3.11 없음, 3.12/3.13 만 있음. uv 0.11.26 이 있어 'uv python install 3.10' 이 62ms 로 끝났고(3.10.20), 실물 3.10 은 중첩 f-string 을 SyntaxError 로 거부한다(3.13 은 정상 문법으로 통과). 설계 = SDK 매트릭스 전례대로 실물 해석기 + '판정은 긍정 증거로'.
- Result: 전제가 뒤집혔다: ast.parse(feature_version=) 은 이 축을 만들게 한 PEP 701(중첩 f-string, 3.12 도입)을 못 잡는다 — 파서가 버전 게이트를 명시적으로 건 구문만 거부하는데 PEP 701 은 토크나이저 층 변경이라 안 걸린다. 실측 7종 중 거부한 것은 type alias / type parameter / except* 3종뿐.
- Result: 설계: workflow_kit/common/python_floor.py 가 requires-python 선언에서 하한을 파생하고(CI 매트릭스 아님) uv 로 얻은 실물 3.10.20 으로 553개를 메모리 compile() 한다. py_compile 을 안 쓰는 이유는 __pycache__ 가 소스 옆에 생겨 저장소를 오염시키기 때문(신규 0 확인). 판정은 sdk_matrix 규율대로 긍정 증거.
- Result: CI 제약을 정직하게 처리: CI 에는 uv 도 python3.10 도 없어 그대로면 CI 만 red 다. 해석기를 못 구하면 부분 측정(feature_version)으로 내려가되 [unmeasured] 두 줄로 못 잰 부류(PEP 701)와 전수로 올리는 방법을 출력한다. 이 한계가 사실인지도 되주입으로 확인했다 — 해석기 없는 상태에서 except* 는 잡고 PEP 701 은 못 잡는다.
- Result: check_python_floor_syntax.py 3 case. case 3 은 'feature_version 이 이 축을 대신하지 못한다'는 사실 자체를 동결한다 — 없으면 다음 사람이 '해석기는 느리니 feature_version 으로 바꾸자' 고 하고 그러면 동기가 된 결함을 다시 못 잡는다.
- Verification: 되주입 5종 전부 red→복원 byte-identical green: ①PEP 701 심기(case 2 red인데 case 3 은 통과 — feature_version 이었으면 못 잡았을 증명) ②requires-python 제거(3건 전부 red) ③해석기 부재('통과가 아니라 미측정') ④해석기 부재+except*(부분 측정이 잡는다) ⑤해석기 부재+PEP 701(부분 측정은 못 잡는다 — 한계가 사실임을 실증). push 게이트 전량 2축 287/287 x2 green.
- Follow-up: CI 를 전수로 올리려면 smoke 워크플로에 uv 또는 python3.10 setup 단계가 필요하다. 현재 CI 커버리지는 mypy-strict 가 3.10 을 밟는 우연한 것뿐이고 그것은 선언이 아니다 — 소유자 판단 대기.
