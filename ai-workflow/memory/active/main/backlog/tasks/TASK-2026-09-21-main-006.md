---
id: TASK-2026-09-21-main-006
status: done
created_at: 2026-09-21
source_anchor: generic-task-2026-09-21-main-006
source_path: backlog/2026-09-21.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-09-21-main-006 — 검사를 CI 인터프리터로도 돌리는 축이 없다 — 로컬 3.13 / CI smoke 3.11 이라 검사 자신의 판정이 갈린다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-21
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/tests/run_all_checks.py`
  - `workflow-source/workflow_kit/common/`
  - `.github/workflows/smoke.yml`

- Description: 81차에 실측으로 드러났다: check_python_floor_syntax case 3 이 로컬 3.13 에서 green, CI 3.11 에서 red 였다(9915e4ad). 판정이 실행 인터프리터에 달렸는데 로컬에서 그 축을 밟을 수단이 없었다. 저장소는 같은 모양을 SDK 매트릭스와 브랜치 매트릭스 두 축에서 이미 해결했다 — 세 번째다.
- Completion criteria: 정본을 정한다 — 이 축의 기대값은 'CI 가 검사를 실제로 도는 인터프리터'이므로 smoke.yml 에서 파생한다. main-005 의 하한(requires-python)과는 다른 질문이므로 두 정본을 섞지 않는다
- Completion criteria: 로컬에서 그 인터프리터로 검사를 돌리는 수단을 만든다 (sdk_matrix --run-local · branch_matrix 와 같은 자리)
- Completion criteria: 되주입: 호스트 의존 판정을 심으면 두 인터프리터 중 한쪽에서 red 가 되는 것을 실증

## 🛠️ Implementation / Content

- Progress: 완료. 정본 = workflow_kit/common/interpreter_matrix.py 의 GATE_INTERPRETERS, CI yml 이 거기서 읽는다 (branch_matrix/sdk_matrix 와 같은 방향)
- Next session starting point:
- Remaining risks: (b) 를 고르면 선언 누락이 곧 사각지대다 — meta-watch 가 WATCHES 에 대해 하는 일(선언의 좁음을 실측으로 잡기)을 이 축에도 해야 한다. 선언만 두고 메타 판정을 안 두면 '검사는 깨지지 않고 무력화된다' 를 그대로 반복한다.
- Remaining risks: 현재의 유일한 수단은 push 전에 uv python find 3.11 로 손수 돌리는 것이고 사람의 기억에 의존한다 — 81차에 내가 잊어서 CI red 를 냈다.
- Remaining risks: 되주입이 내 결함 3건을 잡았다 — (1) case 4 가 파일 어디든 matrix.python 문자열이 있으면 통과해 정작 setup-python 배선을 끊어도 green 이었다(배선 자체를 보도록 교체) (2) WATCHES 가 pyproject.toml 누락(meta-watch 실측) (3) 부모 stdout 버퍼링으로 셀 헤더가 뒤집혀 출력됨
- Remaining risks: 되주입 harness 자체가 한 번 거짓말했다 — 주입 두 건의 소스 **길이가 같아** .pyc 가 유효 판정돼 재사용됐고, case 2 가 case 1 의 증상을 냈다. PYTHONDONTWRITEBYTECODE + pyc 삭제로 고쳤다. 주입이 실제로 적용됐는지 확인하라는 규율 그대로다

## ✅ Outcome

- Result: 착수 전 실측 1 — 기전: run_all_checks.py 가 검사를 sys.executable 로 띄운다(573행). 즉 러너를 다른 인터프리터로 돌리면 축이 선다. 별도 배선이 필요 없다.
- Result: 착수 전 실측 2 — 비용: uv 로 3.11 venv + requirements 전체 설치가 0.648s 였다(캐시 상태). 즉 비용의 본체는 환경 구성이 아니라 게이트 벽시계다. 전량을 2축으로 더 돌리면 약 6.5분 -> 13분이 된다. CLAUDE.md 가 게이트 벽시계를 명시적으로 관리하므로 전량 반복은 기각 후보다.
- Result: 설계 후보 3안: (a) 전량을 CI 인터프리터로 한 번 더 — 벽시계 2배 (b) 인터프리터 민감 검사만 선언하고 그 부분집합만 — WATCHES/ENFORCES 와 같은 선언 문화, 다만 선언 누락을 재는 메타 판정이 같이 필요하다 (c) 하이브리드: 평시 (b), 발행 전 (a). 실측으로 고를 것.
- Result: 실측이 3안을 갈랐다 — (b) 선언 부분집합 기각. 전량을 두 해석기로 대조하니 287/287 x2 green 인데 **출력이 갈린 검사가 8건**이었고, 민감한 쪽은 '저장소 소스를 파싱하는 검사' 라 미리 목록화할 수 없다. 저장소가 브랜치 축에서 같은 모양의 포함 목록을 이미 두 번 기각했다
- Result: 더 고약한 것을 찾았다 — **3.13 커버리지가 선언이 아니라 우연이었다.** CI 는 3.11 하나로만 돌았고 3.13 은 개발자 .venv 에 깔린 것이다. .venv 를 3.11 로 다시 만들면 커버리지가 조용히 사라지고 아무 검사도 실패하지 않는다
- Result: 축이 **지금 열려 있었다**: 3.12+ 에서만 나는 SyntaxWarning(invalid escape sequence)을 5개 검사가 3.13 에서만 보고 3.11(CI)은 완전히 침묵했다. 출처는 81차가 리스크로만 적어 둔 check_v0_7_26_sync_release_hash.py 7/93행 — r-string 으로 수리했다. 방향이 81차(로컬 green/CI red)와 반대다
- Result: 비용 배치: CI 는 브랜치 2 x 해석기 2 = **4셀**(병렬이라 벽시계 불변), **로컬 게이트는 늘리지 않았다**. 로컬은 재현 수단 interpreter_matrix --run-local (venv 는 .venv-interpreter-matrix/<버전> 에 캐시)
- Result: push 후 CI 실측 — 워크플로 6종 전부 success, smoke 는 선언대로 4셀 (native/py3.11 · native/py3.13 · slash/py3.11 · slash/py3.13, run 35562623777). setup-python 없는 prepare job 도 실전에서 섰다
- Verification: push 후 CI 실측 — 워크플로 6종 전부 success, smoke 는 선언대로 4셀 (native/py3.11 · native/py3.13 · slash/py3.11 · slash/py3.13, run 35562623777). setup-python 없는 prepare job 도 실전에서 섰다
- Follow-up: 해석기별로 갈리는 **경고**는 아직 게이트 신호가 아니다 (exit 0). SyntaxWarning 류를 red 로 올릴지는 별도 판단 — 올리면 3.12+ 셀만 red 가 되는 부류가 생긴다
- Follow-up: mcp-sdk-matrix.yml 은 축 면제다 (AXIS_EXEMPT_WORKFLOWS, 이유 선언됨). 그 부분집합은 smoke 4셀이 덮는다
