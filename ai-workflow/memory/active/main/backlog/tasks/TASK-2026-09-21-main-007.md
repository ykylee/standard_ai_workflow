---
id: TASK-2026-09-21-main-007
status: done
created_at: 2026-09-21
source_anchor: generic-task-2026-09-21-main-007
source_path: backlog/2026-09-21.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-09-21-main-007 — 해석기별로 갈리는 경고가 게이트 신호가 아니다 — SyntaxWarning 류는 exit 0 이라 4셀 전부 green 이었다

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-09-21
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `workflow-source/tests/run_all_checks.py`
  - `workflow-source/workflow_kit/common/interpreter_matrix.py`
  - `workflow-source/core/test_impact_tiering_spec.md`

- Description: 82차가 해석기 축을 세웠지만 그 축이 보는 것은 종료 코드뿐이다. 3.12+ 에서만 나던 SyntaxWarning 을 사람이 출력을 눈으로 대조해서 찾았다 — 다음에는 못 찾는다
- Completion criteria: 현황을 전수로 잰다 — 선언된 해석기별로 검사 288개가 내는 경고를 부류·건수로 수집한다. 82차의 '지금 0' 은 내가 방금 고친 1건 기준이고 전수 측정이 아니다
- Completion criteria: 판정 대상을 선언으로 정한다. 모든 warning 이 아니다 — 전역 금지는 위양성 밭이 된다(2026-08-05 실측: 30건 중 28건). 어느 부류를 강제할지와 그 근거를 정본에 적는다
- Completion criteria: 설계 2안을 실측으로 고른다: (A) 경고 자체를 red — 단순하지만 서드파티 경고를 걸러야 하고 해석기 비대칭 red 가 생긴다 / (B) 해석기 간 **출력 차이** 를 red — 축의 존재 이유에 정확히 대응하지만 잡음 정규화가 선결이고 CI 셀이 서로 다른 job 이라 비교 지점이 없다
- Completion criteria: 되주입: 한 해석기에서만 나는 경고를 심으면 red 가 되고, 잡음(tmp 경로·타이밍)을 바꿔도 green 임을 같이 실증한다 — 위양성 없음까지 재야 한다

## 🛠️ Implementation / Content

- Progress: 완료. 정본 = workflow_kit/common/check_warnings.py, 판정은 run_all_checks 가 하고 계약은 check_warning_gate 가 고정한다
- Next session starting point: 먼저 전수 수집부터. run_all_checks 의 JSON 에 이미 error_excerpt/last_line 이 있지만 경고는 전량 stderr 에 흩어져 있다 — 수집 지점을 정하는 것이 1번 작업이다
- Remaining risks: 잡음 정규화가 본체다. 82차 실측에서 갈린 8건 중 3건은 잡음이었다 — tmp 디렉터리 경로 2건(check_cache · check_wiki_url_validity), stderr 끼어듦 1건(check_mcp_server_sdk_compat 은 'SDK not installed' 가 의도된 case 출력이라 액면가로 읽으면 오진한다). 정규화 못 하면 위양성이 나고, 위양성 내는 체크는 무시당한다
- Remaining risks: (B) 를 고르면 비교 지점이 없다 — CI 4셀은 서로 다른 job 이라 한 셀 안에서 다른 셀 출력을 못 본다. 아티팩트를 모아 비교하는 job 을 새로 두거나, 판정을 로컬 --run-local 에만 두고 CI 는 (A) 로 가는 하이브리드가 후보다
- Remaining risks: 이 축을 열면 '3.12+ 셀만 red' 라는 부류가 생긴다. 그 자체는 정보지만, 수리가 불가능한 서드파티 경고가 끼면 만성 red 가 되고 만성 red 는 fixture 가 된다(2026-08-10)
- Remaining risks: 되주입이 내 검사의 약한 case 2개를 잡았다 — case 1 은 표본에 'Warning:' 토큰 든 산문이 없어 **위치 요구를 지워도 통과**했고, case 3/5 는 표본이 전부 .venv 라 site-packages 분기를 한 번도 안 밟았다. 둘 다 표본을 보강한 뒤에야 주입이 red 가 됐다
- Remaining risks: 게이트가 내 결함 2건을 잡았다 — call_deprecated 타입 주석 누락(mypy strict red), smoke 수 288->289 미반영

## ✅ Outcome

- Result: 수집 지점은 새로 만들 것이 없었다 — run_one 이 이미 stdout+stderr 를 전량 받아 두고(output = out + err) 요약 뒤 버리고 있었다. 1번 작업은 배선이 아니라 '버리지 않기' 였다
- Result: 전수 census(288검사 x 2해석기)가 판정 경계를 정했다: 저장소 안 6 / 6, 서드파티 0 / 2. **갈린 2건은 전부 서드파티**였고 원인도 해석기가 아니라 두 venv 의 의존성 해석 차이였다 (pydantic_settings)
- Result: 그래서 (B) 해석기 간 출력 차이는 기각 — 오늘 당장 위양성 2건이고 실제로 다른 버전이라 정규화로도 안 없어진다. (A) 전역도 기각 — 8건 중 6건이 의도된 deprecation 호출이라 즉시 만성 red 다. **채택: (A) 를 출처(위치)로 좁힌다**
- Result: 그러자 '비교 지점이 없다'는 리스크가 **사라졌다**. 각 셀이 독립 판정해도 3.12+ 에서만 나는 경고는 그 셀이 red 가 된다 — 비교 인프라 없이 같은 신호를 얻는다
- Result: <unknown> 은 게이트 대상으로 뒀다. 82차 결함의 위치가 정확히 <unknown>:93 (문자열 compile 산물)이라, 서드파티로 밀면 이 축을 만든 결함이 그대로 빠져나간다
- Result: 저장소 안 6건은 전부 81차가 만든 deprecation 경로를 의도적으로 부른 것이었다. call_deprecated 로 감싸 **삼키되 경고가 났는지 단언**하게 했다 — 면제 registry 없이 베이스라인이 0 이 됐고, deprecation 이 사라지면 그쪽이 red 가 되어 계약이 늘었다
- Verification: py3.11 셀만 '서드파티 2건 (보고만)' 을 찍고 red 가 되지 않는 것까지 실측 확인 — 위양성 없음이 실물에서 성립한다
- Follow-up: call_deprecated 같은 **간접 호출 래퍼**를 새로 만들면 호출 그래프에서 파생하는 판정에 가르쳐야 한다 (_CALL_INDIRECTIONS). 안 가르치면 그 파생에 기대던 면제가 조용히 풀린다 — 실제로 그렇게 됐고 항등함수 가드가 잡았다
- Follow-up: 서드파티 경고는 관찰 지표다. 건수 추이가 늘면 의존성 축(sdk_matrix)에서 볼 일이지 이 게이트의 일이 아니다
