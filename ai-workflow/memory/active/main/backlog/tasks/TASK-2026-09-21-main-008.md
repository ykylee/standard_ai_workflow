---
id: TASK-2026-09-21-main-008
status: done
created_at: 2026-09-21
source_anchor: generic-task-2026-09-21-main-008
source_path: backlog/2026-09-21.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-09-21-main-008 — 경고 게이트가 두 구멍으로 눈이 먼다 — 부모 프로세스 경고 미수집(kit 44/196) + 판정이 __pycache__ 에 달려 1차 red → 2차 green

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-21
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: 82차 main-007 이 '저장소 코드가 낸 Python 경고 = 게이트 red' 를 세웠으나, 수집 지점이 per-check 서브프로세스 출력 하나뿐이라 두 부류가 구조적으로 안 보인다. 착수 전 실측(2026-09-21, plex/linux)이 둘 다 실증했다. ① runner 부모가 transitively import 하는 workflow_kit 모듈 44/196 의 경고는 배너보다 먼저 stderr 로 찍히면서도 warning_verdict 가 per-check 출력만 보므로 EXIT=0 이다 (branch_matrix.py 에 invalid escape 주입 → 화면에 보이는데 green). ② 경고는 컴파일 시점 신호라 __pycache__ 가 따뜻하면 아예 안 난다 — 부모 밖 모듈 dashboard_data.py 로 소스 동일한 채 1차 exit 1(게이트가 잡음) / 2차 exit 0 을 실측했다. 고친 것 없이 재실행만으로 green 이 되는 게이트다. 원래 잔여 질문('SyntaxWarning 류를 red 로 올릴까')은 실측이 뒤집었다 — 저장소 출처 SyntaxWarning 은 이미 선언상 red 이고, 문제는 정책이 아니라 그 선언이 볼 수 없는 두 자리다.
- Completion criteria: 부모 프로세스가 낸 저장소 출처 경고가 게이트 red 다 (되주입으로 실증)
- Completion criteria: 같은 소스에 대해 cold/warm __pycache__ 판정이 동일하다 (1차=2차)
- Completion criteria: 82차 census(저장소 안 6 / 서드파티 0~2)를 cold cache 로 재측정해 baseline 이 진짜 0 인지 확인
- Completion criteria: 되주입 전 case red → 복원 byte-identical green
- Completion criteria: push 게이트 전량 4셀 green + push 후 CI 확인

## 🛠️ Implementation / Content

- Progress: 완료 — 구멍 둘 다 닫았고, 게이트 290/290 ×4셀 green
- Next session starting point:
- Remaining risks: 부모 경고를 무조건 red 로 하면 서드파티 import 경고(이 저장소 밖)가 섞인다 — 출처 분류를 부모 출력에도 같은 규율로 적용해야 한다
- Remaining risks: PYTHONDONTWRITEBYTECODE 로 캐시를 끄면 전량 벽시계가 늘 수 있다 — 실측으로 비용을 재고 결정한다

## ✅ Outcome

- Result: 82차 잔여였던 'SyntaxWarning 류를 red 로 올릴까' 는 착수 실측이 뒤집었다: 저장소 출처 SyntaxWarning 은 이미 선언상 red 였고, 문제는 정책이 아니라 그 선언이 볼 수 없는 두 자리였다.
- Result: 구멍 ① 부모 프로세스 경고 미수집 — runner 부모가 workflow_kit 모듈 44/196 을 transitive import 하는데 warning_verdict 는 per-check 서브프로세스 출력만 훑었다. branch_matrix.py 주입 실측에서 경고가 배너보다 먼저 화면에 찍히는데 EXIT=0. 수리는 runner 의 PARENT_WARNINGS — 같은 출처 규율에 태운다.
- Result: 구멍 ② 판정이 __pycache__ 에 달려 있었다 — 경고는 컴파일 시점 신호라 .pyc 가 유효하면 아예 안 난다. dashboard_data.py 주입 후 소스를 그대로 둔 채 1차 exit 1 / 2차 exit 0. 고친 것 없이 재실행만으로 green 이 되는 게이트였다. 수리는 check_source_compile_warnings 신설 — 저장소 소스 전수(566개)를 메모리 compile() 로 훑는다. compile() 은 __pycache__ 를 보지 않으므로 캐시 무관이 원리적이고, '누가 무엇을 import 했는가' 와도 무관해 ①의 사각지대까지 덮는다.
- Result: 범위는 손 목록이 아니라 파생이다 — python_floor.iter_sources 를 추출해 하한 호환 판정과 열거를 공유하고, case 2 가 git 추적 *.py 전수(565)와 매 게이트마다 대조해 좁아지면 red. repo-root rglob == git 추적 이 실측으로 정확히 일치했다.
- Result: 되주입 7종 전부 red → 복원 byte-identical green (R1 긍정증거 / R2 범위축소 / R3 판정폐기 / R4 실패삼킴 / R5 저장소경고 / R5b 부모귀속 / R6 부모배선제거→구멍A 재현). 그 되주입이 내 결함 3건을 잡았다: case 5 가 주변 환경 PYTHONDONTWRITEBYTECODE 에 판정이 달렸고, case 6 은 저장소 컴파일 실패가 0 이라 발화 불가능했으며(수집을 지워도 green — 기전까지 재도록 교체), R6 는 harness 자신의 pyc 금지가 구멍을 가려 실제 게이트 환경으로 재현해야 했다.
- Result: push 게이트가 내 결함 하나를 더 잡았다 — case 4·5 가 경고 범주를 SyntaxWarning 으로 하드코딩해 3.11 셀에서만 red 였다. invalid escape 는 3.12+ 에서 SyntaxWarning, 3.11 이하에서 DeprecationWarning 이다. 판정이 실행 해석기에 달리는 바로 그 결함족이라, 82차가 만든 축이 자기 자신을 잡았다. 범주 비의존(메시지 기준)으로 교체.
- Result: 부수 강화: 82차가 '3.11(CI)은 완전히 침묵' 이라 기록한 자리가 닫혔다 — 스윕은 simplefilter('always') 를 쓰므로 3.11 에서 기본 무시되는 DeprecationWarning 형태의 invalid escape 도 잡는다 (실측 확인).
- Verification: 게이트 290/290 × 4셀 green (native/slash × py3.13/py3.11) · 되주입 7/7 red→복원 byte-identical green · check_self_application 8/8 · 검사 289→290
- Follow-up: python_floor 의 범위가 workflow-source/ 라 그 밖의 git 추적 소스 8개(main.py · MCP 서버 스크립트 6종 · scripts/audit_mkdocs_links.py)를 못 잰다. 오늘 실측으로 3.10 컴파일 실패 0 · 경고 0 이라 결함은 없지만 범위가 선언 의도보다 좁다 — 별도 task
