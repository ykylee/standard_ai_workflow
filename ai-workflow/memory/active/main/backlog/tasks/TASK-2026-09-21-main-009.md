---
id: TASK-2026-09-21-main-009
status: done
created_at: 2026-09-21
source_anchor: generic-task-2026-09-21-main-009
source_path: backlog/2026-09-21.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-09-21-main-009 — 하한 호환 축의 범위가 workflow-source/ 로 좁다 — git 추적 소스 8개가 그 밖에 있다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-21
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: main-008 이 컴파일 경고 스윕의 범위를 git 추적 *.py 전수(565)로 파생시키면서 드러났다. python_floor 의 probe/main 과 check_python_floor_syntax 는 source_root = workflow-source 로 잡는데, 저장소에는 그 밖에 git 추적 .py 가 8개 있다 — main.py · ai-workflow/mcp_servers/*/scripts/run_*.py 6종 · scripts/audit_mkdocs_links.py. 그중 MCP 서버 스크립트는 실제로 배포·서빙되는 것이다. 오늘 실측으로 결함은 0 이다(실물 3.10 컴파일 실패 0 · 3.13 경고 0)이므로 급하지 않다. 다만 범위가 '저장소가 컴파일하는 소스 전부' 라는 선언 의도보다 조용히 좁고, 이 저장소가 포함 목록으로 이미 세 번 겪은 모양이다. 덤으로 check_python_floor_syntax 의 _partial_sweep 은 열거를 또 한 벌 손으로 갖고 있고 .venv* 제외가 빠져 있다 — iter_sources 로 접을 수 있다.
- Completion criteria: 하한 축의 범위가 git 추적 *.py 전수와 일치하거나, 좁은 이유가 선언으로 남는다
- Completion criteria: _partial_sweep 의 사본 열거를 iter_sources 로 접는다
- Completion criteria: 넓힌 뒤 실물 하한 해석기로 전수 컴파일 green

## 🛠️ Implementation / Content

- Progress: 완료 — 범위를 저장소 루트 전수로 넓히고, 넓히면서 생긴 선언 출처 함정까지 동결했다
- Next session starting point:
- Remaining risks: 넓히면 consumer 프로젝트(workflow-source 하위가 없는 저장소)의 경로 해석이 달라질 수 있다 — main() 의 fallback 과 같이 봐야 한다

## ✅ Outcome

- Result: 범위를 넓혔다: 컴파일 대상 루트가 workflow-source/ → 저장소 루트다. 실물 3.10 해석기로 557 → 566개 전수 컴파일, 실패 0. 밖에 있던 8개는 main.py · ai-workflow/mcp_servers/*/scripts/run_*.py 6종 · scripts/audit_mkdocs_links.py 이고 그중 MCP 서버 스크립트는 실제로 배포·서빙된다 — 결함이 0 이었던 것은 운이지 측정이 아니었다.
- Result: 넓히면서 함정이 하나 생겼고 그것이 이 task 의 실질이다: 저장소 루트의 pyproject.toml 은 배포되지 않는 placeholder scaffold 이고 requires-python = '>=3.13' 을 선언한다(그 파일 머리말이 정본인 의도된 불일치). 루트로 넓히면서 선언까지 루트로 옮기면 하한이 3.10 → 3.13 으로 올라가 이 축이 아무것도 재지 않게 된다 — 개발 해석기가 이미 3.13 이라 전부 통과한다. python_floor.resolve_scope 가 컴파일 루트와 선언 파일을 한 곳에서 정하고, packaged_declaration 이 배포 패키지 선언의 단일 출처다.
- Result: case 2건 신설: case 4 는 범위가 git 추적 *.py 전수(566)와 일치하는지 매 게이트마다 대조하고(좁아지는 쪽만 red — 추적 밖은 범위가 넓은 것이라 실패 모드가 아니다), case 5 는 선언 출처가 배포 패키지의 것인지 동결한다.
- Result: 사본 열거를 접었다: _partial_sweep(CI 의 부분 측정 경로)이 SOURCE_ROOT.rglob 사본을 갖고 있었고 .venv* 제외가 빠져 있었다 — iter_sources 하나로 통일해 전수 측정과 부분 측정이 같은 집합을 재게 했다. check_source_compile_warnings 도 resolve_scope 를 써서 두 축이 '컴파일 대상 루트' 를 각자 정하지 않는다.
- Result: 되주입 3종 전부 자기 case 를 red 로 만들고 복원 byte-identical green. 그 되주입이 case 5 의 탈출구를 잡았다 — 처음 설계는 '선택된 선언 == 루트 pyproject 면 소비 프로젝트' 여서, 정작 선언이 루트로 뒤바뀐 주입이 [해당 없음]으로 통과하고 case 3 이 대신 터졌다. harness 의 판정도 같이 고쳤다: case 이름은 PASS 에도 찍히므로 '마커가 출력에 있다' 가 아니라 '그 case 의 FAIL 줄이 있다' 로 대조해야 한다.
- Verification: check_python_floor_syntax 3 → 5 cases · 실물 3.10 으로 566개 전수 컴파일 실패 0 · 되주입 3/3 red→복원 green · --changed 271/271 · 게이트 290/290 × 4셀 green
- Follow-up:
