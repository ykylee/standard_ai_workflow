---
id: TASK-2026-09-22-main-001
status: done
created_at: 2026-09-22
source_anchor: generic-task-2026-09-22-main-001
source_path: backlog/2026-09-22.md
kind: generic
wbs: M-007/WBS-7.3
---

# TASK-2026-09-22-main-001 — consumer-metrics-digest 가 8월부터 만성 red — 주간 cron 이 issue post 단계에서 죽는다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-22
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: 2026-09-22 릴리스 준비 중 발견. 이 워크플로는 REQUIRED_CI_WORKFLOWS 가 아니라 발행을 막지 않지만, 8/10 이후 8회 중 7회 failure 이고 유일한 success 는 8/20 하나다 (8/24 · 8/31 · 9/07 · 9/14 · 9/21 전부 failure). 실패 지점은 'Post to existing issue (or create new)' 한 단계이고 앞 단계(digest markdown 생성)와 뒤 단계(artifact 업로드)는 success 다. 확인한 것: job 에 permissions.issues=write 가 선언돼 있고, 대상 issue #22 와 consumer-metrics-feed 라벨이 둘 다 존재한다. 로그에 보이는 403 두 건은 gh api traffic/views · traffic/clones 인데 그것은 WARN 처리라 실패 원인이 아니다. gh run view --log 로 본문을 못 받아(빈 출력) 실제 stderr 를 아직 못 봤다 — 웹 UI 나 job logs API 로 그 단계 출력을 먼저 확보해야 한다.
- Completion criteria: 실패 단계의 실제 stderr 를 확보해 원인을 특정한다 (추측으로 고치지 않는다)
- Completion criteria: 수리 후 workflow_dispatch 로 1회 success 실증
- Completion criteria: 만성 red 를 아무도 안 보던 이유를 함께 본다 — 주간 cron 실패가 어디에도 신호로 안 뜬다

## 🛠️ Implementation / Content

- Progress: 수리 + dispatch 실증 완료 — 3개월 만에 첫 실제 게시
- Next session starting point:
- Remaining risks: 만성 red 는 fixture 가 된다 — 고치면 이 상태를 기대던 것이 있는지 확인
- Remaining risks: --header 는 마커를 붙이려는 의도였다. 그냥 지우면 그 의도가 사라진다 — 본문에 마커를 넣을지 마커를 포기할지는 설계 선택이다

## ✅ Outcome

- Result: 원인은 권한이 아니라 **존재하지 않는 플래그**다. job logs API 로 받은 실패 단계 출력: 'Posting digest as comment to issue #22 / unknown flag: --header' + gh issue comment 의 usage. .github/workflows/consumer-metrics-digest.yml:145-148 이 gh issue comment 에 --header 'consumer-digest:GH-DIGEST:yes' 를 준다 — 그 플래그는 gh api 의 것이고 issue comment 에는 없다. git log -L 로 보면 **파일이 만들어질 때부터**(841329ff, v0.8.15) 있었다. 즉 comment 경로는 한 번도 동작한 적이 없다.
- Result: 등록 때의 '8회 중 7회' 는 틀린 요약이었다. event 로 갈라 보면 **schedule 14건 / success 0건** (2026-06-22 ~ 2026-09-21) — 주간 cron 은 3개월간 한 번도 성공한 적이 없다. success 2건은 둘 다 workflow_dispatch 였고, 그마저 경로가 다르다: 2026-08-03 은 #22 가 없어 **create 경로**를 탔고(그 실행이 #22 를 만들었다), 2026-08-20 은 dry_run=true 라 'Post to existing issue' 단계를 **통째로 skip** 했다 — 목적을 수행하지 않고 green 이었다.
- Result: 두 결함이 시간순으로 겹쳐 있었다. 파일 주석이 기록한 옛 결함(라벨 부재로 gh issue create 실패, 2026-07-06 이후 5주 red)은 gh label create --force 로 고쳐졌고, 그 수리가 create 경로를 살렸다. 그런데 그 성공이 **#22 를 만들었고**, 그 뒤 모든 실행은 한 번도 동작한 적 없는 comment 경로로 라우팅됐다. **결함 1 의 수리가 결함 2 를 노출한 것**이고, 겉으로는 '계속 red' 라 같은 문제로 보였다.
- Result: 로그 확보 수단도 기록해 둔다: gh run view <id> --log 와 --log-failed 는 이 저장소에서 **빈 출력**이다. gh api repos/<owner>/<repo>/actions/jobs/<job_id>/logs 는 정상적으로 받아진다 (456줄).
- Result: 수리: gh issue comment 의 --header 를 걷고, 마커 의도는 본문 끝 HTML 주석(<!-- consumer-digest:GH-DIGEST:yes -->)으로 옮겼다. 렌더링에는 안 보이고 본문 검색으로는 잡히므로 '기계가 digest 코멘트를 식별한다' 는 원래 의도가 살아 있다. 소유자 선택 = 2번(마커를 본문에 넣기).
- Result: 실증(run 35685625923, workflow_dispatch): 전체 success 이고 **'Post to existing issue' 단계가 skipped 가 아니라 success** 다. 그리고 종료 코드만 보지 않았다 — issue #22 코멘트 수가 **0 → 1**, 작성자 github-actions[bot], 본문 477자, 마커 1건, 헤더/표 정상. 이것이 schedule 14건 전부 red 이후 **3개월 만의 첫 실제 게시**다.
- Result: dispatch 검증의 함정을 하나 배웠다: dry_run 입력이 true 면 게시 단계가 if 로 skip 돼 **아무것도 안 하고 green** 이 된다 — 2026-08-20 의 'success' 가 정확히 그것이었다. 그래서 완료 기준을 '종료 코드' 가 아니라 '그 단계가 success 인가 + 코멘트가 실제로 늘었는가' 로 잡았다.
- Verification: workflow_dispatch run 35685625923 success · Post 단계 success(skipped 아님) · #22 코멘트 0→1 · 마커 본문 검색 1건 · --changed 104/104 green
- Follow-up: 이 워크플로의 실패가 3개월간 아무 데도 신호로 안 떴다 — 필수 CI 가 아닌 cron 워크플로의 만성 red 를 어디서 볼지는 별도 판단
