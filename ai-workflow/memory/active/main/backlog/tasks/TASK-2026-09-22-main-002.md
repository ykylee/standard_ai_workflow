---
id: TASK-2026-09-22-main-002
status: done
created_at: 2026-09-22
source_anchor: generic-task-2026-09-22-main-002
source_path: backlog/2026-09-22.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-09-22-main-002 — doc-headers-update --apply 가 내용이 안 바뀐 문서 100개의 스탬프를 올린다 — 거짓 '오늘 수정됨'

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-22
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: v1.10.0 발행 준비(2026-09-22) 중 실측. release 절차가 권하는 doc-headers-update --apply 를 돌리자 변경 파일이 39 → 139 가 됐다. 늘어난 100개는 **내용이 한 줄도 안 바뀐 문서의 '- 최종 수정일' 만** 오늘로 올린 것이다 (ai-workflow/core 23 · 그 밖 77). 되돌리고 나서 check_doc_stamp_rule 6/6 · drift_prevention 7/7 · code_index · release_md · installation_usage · readme_cross 전부 green 임을 실측했다 — 즉 그 bump 는 **필요하지 않았다.** 스탬프 규칙은 '스탬프 >= 마지막 내용 변경일' 이라 blanket bump 는 항상 규칙을 만족시키지만, 문서 100개가 '오늘 수정됨' 이라고 거짓 주장하게 된다. 되돌린 뒤 39 파일인데 이는 v1.9.3·v1.9.4 발행 준비와 같은 규모다 (각 39). 도구의 --help 는 '일괄 갱신' 이라 설계된 동작으로 보이나, 그 설계가 문서 메타데이터를 신뢰할 수 없게 만든다.
- Completion criteria: doc-headers-update 가 내용이 바뀐 문서만 스탬프를 올린다 (git 파생으로 판정)
- Completion criteria: 혹은 blanket bump 가 의도라면 그 근거를 정본에 선언하고, 그때 '최종 수정일' 의 뜻을 다시 정의한다
- Completion criteria: 발행 준비 절차(RELEASE.md §2.3)가 어느 쪽인지 말한다

## 🛠️ Implementation / Content

- Progress: 완료 — 판정 정본을 kit 으로 승격하고 게이트를 걸었다
- Next session starting point:
- Remaining risks: v1.9.2 발행 준비는 144 파일이었다 — 과거에 이미 blanket bump 가 실려 나간 적이 있어, 일부 문서의 현재 스탬프가 이미 거짓일 수 있다

## ✅ Outcome

- Result: 처음 등록 때보다 범위가 넓다: blanket bump 는 doc-headers-update 를 사람이 부를 때만이 아니라 **release --apply 의 post-step 에서도** 일어난다. v1.10.0 발행 준비에서 100건을 되돌리고 커밋했는데, 발행 직후 워킹 트리에 **똑같은 100건이 다시** 나타났다 (전부 1줄 추가/1줄 삭제이고 전부 '최종 수정일' 줄). 즉 커밋 전에 되돌리는 것으로는 못 막는다 — 다음 커밋에 실린다.
- Result: 두 번 다 되돌린 뒤 doc_stamp_rule 6/6 · drift_prevention 7/7 · release_md 7/7 · code_index 5/5 · changelog_gen 7/7 · smoke_count_claims 3/3 green 이라, 이 bump 는 어떤 검사도 요구하지 않는다.
- Result: 원인은 **읽는 쪽만 알고 쓰는 쪽이 모르던 규약**이다. 스탬프 판정('스탬프 >= 마지막 내용 변경일 − 유예')은 tests/_doc_stamp.py 에 있어 검사 5종만 알았고, 쓰는 쪽(release_pipeline.cmd_doc_headers_update)은 헤더가 있는 문서를 전부 오늘로 올렸다.
- Result: 수리: 판정 정본을 workflow_kit/common/doc_stamp.py 로 승격하고 tests/_doc_stamp.py 는 재수출만 한다 — 사본을 만들지 않았다. cmd_doc_headers_update 는 _stamp_needs_bump 로 **뒤처진 문서만** 올리고, 건너뛴 수를 skipped_current 로 내보낸다(조용히 안 하면 '안 돌았다' 와 구분이 안 된다). 판정 불가(git 밖·미추적)는 갱신 쪽으로 떨어뜨린다 — 쓰는 쪽의 모름은 안전한 쪽으로.
- Result: 승격이 meta-watch 에 걸렸다: import workflow_kit 이 검사 5종의 입력 표면을 **kit 전체 43파일**로 넓혔다. 판정 모듈이 stdlib-only 라 파일 경로로 직접 읽게 해 **1파일**로 되돌리고 그 한 줄을 WATCHES 에 선언했다. 같은 파일을 읽으므로 release_pipeline 이 쓰는 판정과 byte 단위로 같다.
- Result: 양방향 실측: 안 바뀐 문서 92개는 건너뛰고(updated 0), 실제로 고친 1개만 올린다. 되주입 3종(게이트 항상 True=옛 blanket / 항상 False / 판정 정본 비교 무력화) 전부 자기 이유로 red → 복원 byte-identical green.
- Result: 덤: 그 검사의 요약 줄이 '{7 - failures}/7' 로 하드코딩돼 case 를 8개로 늘려도 7/7 을 찍었다 — 새 case 가 돌았는지 요약만 보고는 알 수 없었다. 실제 실행 수에서 파생하도록 고쳤다.
- Result: RELEASE.md §2.3 에 규칙과 근거를 적었다 (완료 기준 3). 등록 때 리스크로 적은 '과거에 이미 blanket bump 가 실려 나가 일부 문서의 현재 스탬프가 이미 거짓일 수 있다' 는 그대로 남는다 — 소급 교정은 하지 않았다.
- Verification: check_drift_prevention_helpers 7 → 8 cases · 되주입 3/3 red→복원 green · doc-headers-update dry-run scanned 93 / updated 0 / skipped_current 93 · --changed 269/269 green
- Follow-up: 과거 발행(v1.9.2 는 144 파일)에서 이미 올라간 스탬프는 소급 교정하지 않았다 — 현재 스탬프가 실제 변경일보다 앞선 문서가 남아 있다
