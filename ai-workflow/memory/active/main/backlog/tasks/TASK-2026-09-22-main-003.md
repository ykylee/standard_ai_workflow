---
id: TASK-2026-09-22-main-003
status: done
created_at: 2026-09-22
source_anchor: generic-task-2026-09-22-main-003
source_path: backlog/2026-09-22.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-09-22-main-003 — runtime_load 의 mtime 폴백이 병합 복사 채널에서 낡은 설치 시각을 낸다 — 낡은 호스트를 최신으로 오판한다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-22
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: v1.10.0 채널 재적용(2026-09-22) 중 실측. runtime_load 는 선언 기록이 없는 채널(codex · grok-build · antigravity)에서 '설치 사본 mtime' 을 설치 시각 폴백으로 쓴다. 그런데 antigravity 의 agy plugin install 은 디렉터리를 갈아엎지 않는 **병합 복사** 라(§7.0.2 표에 그렇게 적혀 있다) 최상위 디렉터리 mtime 이 안 움직인다. 실측: 재설치 직후 내부 파일은 2026-09-22 03:01 인데 디렉터리 mtime 은 2026-09-18 00:16 그대로였고, doctor 의 runtime_load 도 설치 시각을 2026-09-18 로 보고했다 (codex · grok-build 는 03:01 로 갱신됨 — 이 둘은 디렉터리를 새로 만든다). 결과: 09-18~09-22 사이에 시작한 호스트 프로세스를 **'최신 호스트' 로 센다.** 그 프로세스는 09-22 내용을 본 적이 없다. 즉 이 절의 판정이 채널의 설치 방식에 따라 조용히 틀린다.
- Completion criteria: 설치 시각 폴백이 병합 복사에서도 실제 갱신 시각을 낸다 (예: 하위 파일 mtime 의 최대값)
- Completion criteria: 혹은 그 채널을 미측정으로 내린다 — 틀린 값보다 모른다가 낫다
- Completion criteria: 되주입으로 '낡은 호스트를 최신으로 세는' case 를 고정한다

## 🛠️ Implementation / Content

- Progress: 완료 — 폴백을 트리 최신 mtime 으로, 되주입 2종으로 case 고정
- Next session starting point:
- Remaining risks: mtime 최대값도 폴백이다 — 선언 기록이 있는 채널(installed_plugins.json)과 판정 근거가 갈리므로 provenance 를 출력에 남겨야 한다

## ✅ Outcome

- Result: 수리: deploy_doctor._install_epoch 의 폴백을 root.stat().st_mtime → _tree_newest_mtime(root) 로 바꿨다. 병합 복사는 최상위 디렉터리 mtime 을 안 건드리므로 트리 전체의 최댓값을 봐야 실제 갱신 시각이 나온다. 실제 설치본(antigravity)에서 판정이 09-18 00:16 → 09-22 03:01 로 교정됐다 (39개 훑음).
- Result: 폴백이라는 사실은 감추지 않았다 — 출처 문자열이 '설치 사본 **트리**의 최신 mtime (N개 훑음 — 선언 기록이 없는 채널의 폴백)' 이라 선언 기록(installed_plugins.json)을 읽는 채널과 근거가 다르다는 것이 결과에 남는다. 등록 때 리스크로 적어 둔 그대로다.
- Result: 검사 39 → 40 cases. 신규 case 는 디렉터리는 낡고 내부 파일만 새로운 배치에 그 **사이에** 시작한 호스트를 놓는다 — 디렉터리만 보면 '최신', 트리를 보면 '낡음' 이라 두 답이 갈리는 지점이다. 되주입 2종(폴백을 옛 형태로 되돌림 / 트리 순회를 최상위 하나로 좁힘) 전부 자기 이유로 red → 복원 byte-identical green.
- Result: 되주입이 내 결함을 하나 잡았다: 처음 쓴 단언이 install_time_epoch 라는 **없는 키**를 봐서 발화할 수 없었다 (실제 키는 installed_at). R1/R2 에서 그 줄이 한 번도 안 터지는 것을 보고 알았고, 고친 뒤 두 단언이 모두 발화한다. '전부 red' 로만 셌으면 못 봤을 자리다.
- Verification: check_deploy_doctor 40/40 · 되주입 2종 red→복원 green · 실 설치본에서 09-18 → 09-22 교정 확인 · --changed 258/258 green
- Follow-up:
