---
id: TASK-2026-09-22-main-004
status: done
created_at: 2026-09-22
source_anchor: generic-task-2026-09-22-main-004
source_path: backlog/2026-09-22.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-09-22-main-004 — 스탬프 판정이 자기 이름과 어긋난다 — '마지막 내용 변경' 이 실은 '마지막 커밋' 이고, 그래서 소급 교정이 성립하지 않는다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-22
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: main-002 의 follow-up(과거 blanket bump 로 올라간 스탬프 소급 교정)을 착수하니 전제가 깨졌다. 실측: 스탬프가 있는 git 추적 md 467개 중 **97개**가 실제 내용 변경보다 2일 이상 앞선 스탬프를 단다 (30일+ 58건 · 8~30일 23건 · 2~7일 16건, 최대 **126일**). 최악 표본들은 전부 stamp=2026-09-04 로 v1.9.2 발행(144파일)의 blanket bump 자국이다. 그런데 그냥 되돌리면 check_doc_stamp_rule 이 전부 red 가 된다 — 그 판정의 기준선이 'git log -1' 즉 **마지막 커밋일**이고, 이 문서들의 마지막 커밋이 바로 그 blanket bump 이기 때문이다. workflow_task_modes.md 로 확인: 마지막 커밋 c5634267 이 바꾼 줄은 '- 최종 수정일' 하나뿐이다. 즉 last_content_change_date 는 이름과 docstring('마지막 **내용** 변경일')이 말하는 것을 측정하지 않는다 — 스탬프만 바꾼 커밋도 내용 변경으로 센다. 판정을 먼저 고쳐야 소급 교정이 성립한다.
- Completion criteria: last_content_change_date 가 스탬프만 바꾼 커밋을 내용 변경으로 세지 않는다
- Completion criteria: 되주입으로 그 구분이 살아 있는지 고정한다 (스탬프만 바뀐 커밋 / 내용도 바뀐 커밋)
- Completion criteria: 그 위에서 97건의 스탬프를 실제 내용 변경일로 소급 교정하고 전량 green

## 🛠️ Implementation / Content

- Progress: 완료 — 판정 두 갈래 수리 + 97건 소급 교정 + 사본 13개 재동기
- Next session starting point:
- Remaining risks: 판정을 좁히면 기준선이 과거로 내려가 지금 green 인 문서가 red 가 될 수 있다 — 교정과 같은 커밋에 실어야 한다
- Remaining risks: 커밋 순회 비용. 대부분 1~2 커밋이면 끝나지만 상한과 폴백이 필요하다

## ✅ Outcome

- Result: 판정 수리 ①커밋 분기: last_content_change_date 가 스탬프 줄만 바꾼 커밋을 건너뛴다. 상한 20 은 추측이 아니라 전수 실측이다 — 스탬프 있는 md 467개 중 건너뛸 것 없는 문서 370개, 나머지 최대 깊이 **15** · 평균 2.2 · 상한 소진 **0건**. (처음 주석에 '최대 3' 이라고 썼다가 실측 15 를 보고 교체했다.)
- Result: 판정 수리 ②워킹트리 분기: **게이트가 잡아준 것이다.** 미커밋 변경이 있으면 유예 0 으로 '오늘' 을 요구하는데, 그 분기가 *무엇을* 고치는 중인지 안 봐서 **스탬프를 교정하는 행위 자체가 위반**이 됐다 (check_document_index 가 그 이유로 red). 스탬프 줄만 바뀐 워킹트리 변경은 이력 판정으로 흘려보낸다.
- Result: 소급 교정: 97건 (최대 −126일), 그대로 370건. 변경 범위를 파이썬으로 전수 확인 — 스탬프 줄만 바뀐 파일이 **정확히 97개**. 교정 대상은 전부 doc-headers-update 관리 범위(docs/** · workflow-source/core · 배포 사본) 안이고, blanket bump 가 실제로 건드린 자리와 일치한다.
- Result: 배포 사본 13개가 정본과 갈렸다 — 두 트리를 독립적으로 고쳐서다. doc-headers-update --apply 로 정본→사본 재동기(스탬프 추가 갱신 0). 이것도 게이트(check_standard_single_source)가 잡았다.
- Result: 위반 수를 옛/새 판정으로 대조했다: 양쪽 다 **284건**으로 동일 — 이번 변경이 만든 위반은 0 이다. 그 284 는 게이트도 doc-headers-update 도 범위에 두지 않는 문서들(.omo/ · QUICKSTART.md 등)의 기존 상태다.
- Result: 검사 6 → 9 cases. case 9 는 **반대 방향 확인**이다 — 스탬프 전용 판정이 더러운 트리의 '유예 0' 규율을 무력화하지 않았는지. 되주입 R3 에서 그 규율을 풀자 case 4 와 case 9 가 같이 터지는 것으로 확인했다. 되주입 3종 전부 자기 이유로 red → 복원 byte-identical green.
- Result: 덤: check_doc_stamp_rule 의 요약이 '6/6' 하드코딩이라 case 를 9개로 늘려도 6/6 을 찍었다. 같은 세션에 check_drift_prevention_helpers 에서 같은 자리를 이미 고쳤으니 **두 번째**다 — 둘 다 등록된 case 수에서 파생하도록 바꿨다.
- Verification: check_doc_stamp_rule 6 → 9 cases · 되주입 3/3 red→복원 green · 소급 97건(스탬프 줄만 바뀐 파일 정확히 97) · 위반 수 옛 284 == 새 284 · 게이트 290/290 × 4셀 green
- Follow-up: 게이트도 doc-headers-update 도 범위에 안 두는 문서 284건의 스탬프가 뒤처져 있다 (.omo/ · QUICKSTART.md 등) — 범위를 넓힐지는 별도 판단
