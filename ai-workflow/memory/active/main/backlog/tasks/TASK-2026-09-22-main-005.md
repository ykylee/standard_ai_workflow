---
id: TASK-2026-09-22-main-005
status: in_progress
created_at: 2026-09-22
source_anchor: generic-task-2026-09-22-main-005
source_path: backlog/2026-09-22.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-09-22-main-005 — CI smoke red — check_release_pipeline_version_flag 의 dist TOCTOU 경합, main-004 가 창을 9초로 벌렸다

## 📝 Description

- Status: in_progress
- Priority: high
- Request date: 2026-09-22
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: v1.10.0 소급 교정(main-004) push 후 CI smoke 가 2연속 red. 같은 검사 check_release_pipeline_version_flag::test_version_default_pyproject 이고 **셀은 매번 다르다**(1차 slash/3.11, 2차 native/3.13 + slash/3.13) — 경합이다. 그 검사는 subprocess 실행 **전에** workflow-source/dist 존재를 재서 기대 분기를 고르는데, 병렬로 도는 다른 검사(check_release_pipeline_phase3 · _lib · wrapper_args)가 그 사이에 dist 를 만들면 분기가 틀어진다. 실패 메시지 'expected 1.10.0 in error, got None' 이 정확히 그 모양이다(dist 가 생겨 error 없이 성공). git 이력에 a9c408b8 'version_flag test 3 의 dist 오염 우연 의존 해소 — smoke CI red 복구'(2026-08-11)가 있어 **같은 자리 재발**이다. 그때는 우연 의존만 걷고 TOCTOU 는 남겼다. 내 main-004 변경이 방아쇠다: cmd_doc_headers_update 가 문서마다 git 을 부르게 되면서 doc-headers-update 가 **6.2s**, 그것을 auto-step 으로 부르는 release --dry-run 이 **9.6s** 가 됐다(실측). 경합 창이 ~0s 에서 ~9s 로 벌어져 드물던 flake 가 상시가 됐다. 로컬이 green 인 이유는 별개다 — 내 dist/ 에 1.10.0 산출물이 있어 has_dist=True 분기를 탄다.
- Completion criteria: 검사가 공유 가변 상태(dist 존재)에 기대 분기를 고르지 않는다
- Completion criteria: main-004 가 더한 비용을 줄인다 — 문서당 git 호출 수를 낮춘다
- Completion criteria: CI smoke 4셀 green (한 번이 아니라 연속 2회로 확인)

## 🛠️ Implementation / Content

- Progress: `2026-09-22 13:24` 기준 v1.10.0 소급 교정(main-004) push 후 CI smoke 가 2연속 red. 같은 검사 check_release_pipeline_version_flag::test_version_default_pyproject 이고 **셀은 매번 다르다**(1차 slash/3.11, 2차 native/3.13 + slash/3.13) — 경합이다. 그 검사는 subprocess 실행 **전에** workflow-source/dist 존재를 재서 기대 분기를 고르는데, 병렬로 도는 다른 검사(check_release_pipeline_phase3 · _lib · wrapper_args)가 그 사이에 dist 를 만들면 분기가 틀어진다. 실패 메시지 'expected 1.10.0 in error, got None' 이 정확히 그 모양이다(dist 가 생겨 error 없이 성공). git 이력에 a9c408b8 'version_flag test 3 의 dist 오염 우연 의존 해소 — smoke CI red 복구'(2026-08-11)가 있어 **같은 자리 재발**이다. 그때는 우연 의존만 걷고 TOCTOU 는 남겼다. 내 main-004 변경이 방아쇠다: cmd_doc_headers_update 가 문서마다 git 을 부르게 되면서 doc-headers-update 가 **6.2s**, 그것을 auto-step 으로 부르는 release --dry-run 이 **9.6s** 가 됐다(실측). 경합 창이 ~0s 에서 ~9s 로 벌어져 드물던 flake 가 상시가 됐다. 로컬이 green 인 이유는 별개다 — 내 dist/ 에 1.10.0 산출물이 있어 has_dist=True 분기를 탄다.
- Next session starting point:
- Remaining risks: 로컬은 dist 가 있어 이 경로를 안 밟는다 — 로컬 green 을 근거로 쓰면 안 된다. dist 를 치우고 재현할 것

## ✅ Outcome

- Result:
- Follow-up:
