---
id: TASK-2026-09-22-main-006
status: done
created_at: 2026-09-22
source_anchor: generic-task-2026-09-22-main-006
source_path: backlog/2026-09-22.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-09-22-main-006 — 뒤처진 스탬프 소급 교정 — doc-headers-update 범위를 손 목록에서 파생으로

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-22
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: main-004 의 follow-up. 스탬프가 실제 내용 변경보다 **뒤처진** 문서를 교정한다. 착수 실측이 앞서 보고한 284 를 정정했다 — 그것은 교정 중 더러운 트리 때문에 부풀려진 값이고 standing 은 199 다. 소유자 선택 = '도구 범위를 먼저 넓힌다'.
- Completion criteria: 범위가 손 목록이 아니라 파생이다
- Completion criteria: 동결 문서(기록·발행 노트·픽스처·무시 경로·배포 사본)를 건드리지 않는다
- Completion criteria: 전수 감시가 어딘가에 남는다

## 🛠️ Implementation / Content

- Progress: `2026-09-22 15:36` 기준 main-004 의 follow-up. 스탬프가 실제 내용 변경보다 **뒤처진** 문서를 교정한다. 착수 실측이 앞서 보고한 284 를 정정했다 — 그것은 교정 중 더러운 트리 때문에 부풀려진 값이고 standing 은 199 다. 소유자 선택 = '도구 범위를 먼저 넓힌다'.
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: 범위: 손 목록(README + docs/** + core/*, 93개) → git 추적 전수 − 동결(210개). 동결 판정 정본을 workflow_kit/common/doc_layers.py 로 승격하고 check_smoke_count_claims 도 그것을 읽는다(사본 0).
- Result: 동결 신호 2 → 5. 기존 둘(기록 계층 · '- 상태:' 헤더 부재)로는 부족했다 — releases/prototype-v* 와 tests/devhub_temp_source/ 가 헤더를 달고 있어 살아있는 문서로 샜다. 추가: 발행 노트 디렉터리 · 테스트 트리 · .gitignore 무시 경로(check-ignore --no-index). 전부 기존 상수/선언에서 파생.
- Result: 무시 경로 신호는 실패가 가르쳐 줬다 — ai-workflow/mcp_servers/ 는 .gitignore:50 이 무시하는데 그 안에 추적 파일이 남아 있어, 교정이 건드리자 check_release_wrapper_args case 6 이 'paths are ignored' 로 red 였다. 8건 복원 + 신호 추가.
- Result: 배포 사본(ai-workflow/core/*)은 스캔에서 제외. 정본과 byte-identical 이어야 하고 동기는 _sync_distributed_core_mirror 가 맡는데, 따로 올리면 매 실행 updated:12 로 **영원히 수렴하지 않았다**.
- Result: 도구로 교정하지 않았다. doc-headers-update 는 '오늘' 을 쓰는데, 내용이 2026-05-01 에 바뀐 문서에 오늘을 박으면 이 축이 없애려던 것과 같은 거짓이다. 범위 확장은 두고 소급 값만 **실제 내용 변경일**로 넣었다 — 116건, 최대 +142일. 동결 236건 미변경.
- Result: 범위 확장이 release --dry-run 을 10s 예산 밖으로 밀어 phase2 가 터졌다(main-005 와 같은 모양). 예산 10s 검사가 여럿이라 timeout 올리기는 손 목록이 되므로 역할을 나눴다 — 파이프라인 auto-step 은 워킹 트리 변경분만(scope 'dirty'), 전수 감시는 게이트가 한 번(check_doc_stamp_rule case 10).
- Result: 게이트 1차는 green 이 아니었다: 셀은 290/290 인데 runner 가 EXIT=1 이었고 원인은 meta-watch 였다(case 10 이 1412건을 만지는데 선언이 좁았다). **요약 수치와 종료 코드가 다른 것을 말하고 있었다.** 선언을 전역으로 교체해 해소.
- Result: 등록 사고 하나: 이 task 자체가 처음에 등록되지 않았다. `wk backlog-update --mode update` 가 대상 부재를 **warning + exit 0** 으로 알려서 쉘  폴백이 안 걸렸고, 커밋 메시지만 main-006 을 가리킨 채 task 파일이 없었다. 도구의 '경고지만 성공' 종료 코드를 조건에 쓰면 안 된다.
- Verification: doc-headers-update 210 스캔 / updated 0 수렴 · check_doc_stamp_rule 9 → 10 cases · 되주입(스탬프 2020-01-01) red→복원 green · 게이트 290/290 × 4셀 EXIT=0 · meta-watch 지적 0 · check_release_wrapper_args 11/11 · check_standard_single_source 11/11
- Follow-up:
