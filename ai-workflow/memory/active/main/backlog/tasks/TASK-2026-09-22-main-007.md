---
id: TASK-2026-09-22-main-007
status: done
created_at: 2026-09-22
source_anchor: generic-task-2026-09-22-main-007
source_path: backlog/2026-09-22.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-09-22-main-007 — CI 의 Python 하한 측정을 부분 → 전수로 — 소유자 보류 번복

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-22
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: 2026-09-21 소유자 지시로 보류했던 건을 2026-09-22 소유자가 진행 지시해 번복한다. 현재 CI 셀에는 uv 도 python3.10 도 없어 check_python_floor_syntax 의 case 2 가 feature_version 부분 측정으로 떨어지고, 그 수단은 PEP 701(중첩 f-string) 부류를 원리적으로 못 본다 — 이 축을 만들게 한 바로 그 결함이다. 검사가 그 사실을 [unmeasured] 두 줄로 출력하고 있다. 하한은 3.10 이고 게이트 해석기는 3.11/3.13 이라 셀에 3.10 이 없다. 핵심 제약: smoke.yml 에 '3.10' 리터럴을 적으면 requires-python 선언의 사본이 생긴다 — branch_matrix/interpreter_matrix 가 registry→yml 로 세운 규율을 깨는 것이라, 하한도 prepare job 이 선언에서 파생해 주입해야 한다.
- Completion criteria: CI 셀에서 case 2 가 부분 측정이 아니라 실물 하한 해석기 전수 측정으로 돈다
- Completion criteria: smoke.yml 에 하한 버전 리터럴이 없다 (prepare 가 선언에서 파생)
- Completion criteria: 리터럴 복제를 검사가 막는다 (interpreter/branch matrix 와 같은 규율)
- Completion criteria: push 후 CI 4셀에서 [unmeasured] 가 사라진 것을 실측

## 🛠️ Implementation / Content

- Progress: 완료 — 로컬 게이트 green, CI 실측은 push 후 확인
- Next session starting point:
- Remaining risks: setup-python 을 두 번 쓰면 셀의 '선언한 해석기로 도는가' 단언이 깨질 수 있다 — 순서(하한 먼저, 매트릭스 나중)가 중요하다

## ✅ Outcome

- Result: 2026-09-21 소유자 보류를 2026-09-22 소유자가 번복해 진행했다. CI 셀에 하한 해석기(3.10)를 깔아 check_python_floor_syntax case 2 가 부분 측정(feature_version)이 아니라 실물 전수 측정으로 돌게 했다. 부분 측정은 PEP 701(중첩 f-string)을 원리적으로 못 보는데, 그것이 이 축을 만들게 한 결함이다.
- Result: yml 에 3.10 리터럴을 적지 않았다 — 그러면 requires-python 의 사본이 생겨 branch_matrix/interpreter_matrix 가 registry→yml 로 세운 규율을 깬다. python_floor 에 --declared-floor 를 신설해 prepare job 이 선언에서 파생해 주입한다.
- Result: 순서가 계약이다. setup-python 은 PATH 앞에 붙으므로 **나중에 깐 것이 python3 을 차지한다** — 하한을 매트릭스보다 먼저 깔아야 셀이 선언한 해석기로 돌고(--assert-running 이 단언) python3.10 은 이름으로 찾을 수 있다.
- Result: 되주입이 구멍을 잡았다: 처음엔 리터럴 복제만 막고 있어서 **smoke.yml 에서 하한 setup 단계를 통째로 지워도 9/9 PASS** 였다. 그러면 CI 가 조용히 부분 측정으로 떨어지고 게이트는 계속 green 이다. check_python_floor_syntax case 6 을 신설해 세 조건(prepare 출력 · 셀 바인딩 · 순서)을 각각 잡게 했고, 되주입 3종이 각자 자기 조건으로 red 임을 확인했다. check_interpreter_matrix case 4 는 '선언 파생 바인딩 둘' 을 허용하도록 넓혔다 — 리터럴을 막는 것이 요지이지 매트릭스 하나만 허용하는 것이 아니다.
- Verification: check_python_floor_syntax 5 → 6 cases · check_interpreter_matrix 9/9 · 되주입 3/3 자기 조건으로 red→복원 PASS · 게이트 290/290 × 4셀 EXIT=0
- Follow-up: push 후 CI 4셀 로그에서 [unmeasured] 가 사라지고 실물 3.10 전수 측정이 뜨는지 실측
