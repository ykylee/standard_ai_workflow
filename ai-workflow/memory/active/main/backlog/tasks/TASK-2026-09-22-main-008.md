---
id: TASK-2026-09-22-main-008
status: done
created_at: 2026-09-22
source_anchor: generic-task-2026-09-22-main-008
source_path: backlog/2026-09-22.md
kind: generic
wbs: M-007/WBS-7.3
---

# TASK-2026-09-22-main-008 — 전량 검사 시간 단축 — 저장소 write 감시를 러너에 흡수 (재실행 76.7s 제거)

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-22
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: 소유자 지시: 전량/CI 시간이 길다. 착수 전 실측(검사 290개, 1축): 벽시계 221s · CPU 914s · **정숙(직렬) 구간 103s = 벽시계의 47%**. 개수는 원인이 아니다 — 174개(60%)가 1초 미만이고 합쳐 53s(6%)뿐이다. 비용은 **다시 도는 것**에 몰려 있다: check_no_repo_write 76.7s(표본 16개 직렬 재실행, 벽시계의 35%) + branch_context_matrix 44.4s + run_all_checks 27.8s = 재실행 계열 166s(18%). 소유자 선택 = A안(감시를 러너에 흡수). 설계 전제 2개를 먼저 실측했다: ① 러너 동시성 중앙 9 / 최대 11(12코어) → 후보 집합이 쓸모 있다 ② git status --porcelain 12.7ms → 0.5s 폴링이 221s 동안 5.6s(2.5%). 즉 76.7s 를 5.6s 로 바꾸는 거래다.
- Completion criteria: 러너가 전후 스냅샷 + 실행 중 폴링으로 저장소 write 를 직접 관찰한다
- Completion criteria: 감시 범위가 표본 16개가 아니라 검사 전수다
- Completion criteria: 위반 시 그 시각 in-flight 검사를 후보로 지목한다 (귀속 약화를 기록에 명시)
- Completion criteria: check_no_repo_write 는 재실행을 그만두고 기전을 되주입으로 검증한다
- Completion criteria: KNOWN_TRANSIENT_TOUCHERS 원장을 잃지 않는다
- Completion criteria: 벽시계 절감을 실측으로 보인다 (221s 기준)

## 🛠️ Implementation / Content

- Progress: 완료 — 벽시계 221s → 138s (−38%), 감시 범위 16 → 290
- Next session starting point:
- Remaining risks: 귀속이 '이 검사' → '이 창의 9개 중 하나' 로 약해진다 — 커버리지 16→290 과 맞바꾸는 것이고 명시해야 한다
- Remaining risks: 폴링은 타이밍 의존이라 음성은 증명이 아니다 (현재 검사도 같은 한계를 명시하고 있다)

## ✅ Outcome

- Result: 착수 전 실측이 방향을 정했다: 검사 290개 중 174개(60%)가 1초 미만이고 합쳐 53s(6%) — **개수는 원인이 아니다**. 비용은 재실행에 몰려 있었다(166s, 18%). 설계 전제 둘도 먼저 쟀다: 러너 동시성 중앙 9/최대 11(12코어)이라 후보 귀속이 쓸모 있고, git status --porcelain 12.7ms 라 0.5s 폴링이 5.6s(2.5%)다.
- Result: 수리: workflow_kit/common/repo_write_watch.py 신설. 러너가 실행 전후 스냅샷 + 실행 중 폴링으로 직접 관찰하고, 변경이 보이면 그 시각 in-flight 검사를 후보로 단다. lingering(끝까지 남음)과 미지의 transient 는 EXIT=1, 원장(KNOWN_TRANSIENT_PATHS)에 있는 접촉만 보고. 못 재면 미측정이고 그것도 red 다.
- Result: 효과: 벽시계 **221s → 138s (−38%)** · check_no_repo_write **76.7s → 3.0s** · 정숙(직렬) 구간 12개 103s → 11개 ~26s · 감시 범위 표본 16 → **전수 290**. 더 빨라지면서 커버리지가 18배 늘었다 — 재실행을 없앤 게 아니라 비용을 위반이 났을 때로 옮겼다.
- Result: 전수 감시가 켜지자마자 실물을 하나 잡았다: check_warning_gate 가 case 6 을 위해 실 저장소 tests/ 에 probe 파일을 만들었다 지운다(러너가 거기서 discover 하므로 불가피). **예전 표본 16개에 없어 아무도 못 보던 접촉**이고, 원장에 이유와 함께 등록했다.
- Result: 내가 계약을 조용히 약화시킨 것을 발견해 되돌렸다. 옛 검사는 '미지의 transient 는 FAIL, 알려진 것만 원장' 이었는데 첫 구현은 둘 다 보고만 했다. 되돌려 놓으면 git status 가 오히려 깨끗해 보여 더 위험한 것이 이 축의 요지다. **기능을 옮길 때 옮기기 전 계약이 그대로 왔는지 확인해야 한다 — 빠지는 쪽은 조용하다.**
- Result: 게이트가 내 결함 3건을 잡았다: ①ENFORCES 가 스펙에 없는 요구를 가리킴(선언 추가) ②check_parallel_smoke 가 검사 이름을 카나리로 하드코딩해 정당한 재배치에 red — 계약을 '선언과 배치의 일치' 로 파생화 ③새 검사가 workflow_kit 패키지를 import 해 표면이 42파일로 번짐 — stdlib-only 모듈이라 파일 경로 직접 로드로 1파일 유지.
- Result: 부수로 main-006 의 case 10(전수 스탬프 감시)이 바로 다음 작업에서 내 누락을 잡았고, doc-headers-update 가 **정확히 1건만** 올렸다 — 같은 세션에 고친 '뒤처진 것만' 규칙이 실전에서 동작했다. 예전 blanket 이었으면 문서 200여 개를 건드렸을 자리다.
- Verification: 게이트 290/290 × 4셀 EXIT=0 · 벽시계 138s(이전 221s) · check_no_repo_write 5/5 · 되주입으로 lingering/transient 양축 실증 · 원장이 실전 전량에서 '알려진 접촉' 으로 분류
- Follow-up: 남은 재실행 계열: check_branch_context_matrix 44.4s · check_run_all_checks 27.8s (합 72s). 같은 방식으로 볼 수 있으나 별도 판단
