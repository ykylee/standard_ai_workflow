---
id: TASK-2026-09-23-main-001
status: done
created_at: 2026-09-23
source_anchor: generic-task-2026-09-23-main-001
source_path: backlog/2026-09-23.md
kind: generic
wbs: M-007/WBS-7.3
---

# TASK-2026-09-23-main-001 — memory_index 승격 — 후보 판정식이 잡음이라 도구 순위가 아니라 판단으로 고른다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-23
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: 소유자 지시로 '후보 10건' 승격에 착수했는데 **착수 실측이 전제를 뒤집었다.** suggest-memory-entries 의 coverage 는 '제목 토큰 ∩ entry 토큰 / 제목 토큰' 순수 어휘 겹침이고, 이는 81차(main-001)가 graph_insights 에서 구조적 0 으로 진단해 선언 사슬로 갈아치운 그 방식이다. 여기서는 구조적 0 은 아니지만(평균 0.22 · 최대 0.40) **임계 0.5 를 영영 못 넘어** 매 세션 '10건 중 0건' 상수를 낸다. 더 결정적인 것은 그 겹침의 내용이다 — 최고 쌍(0.40)의 공유 토큰이 · 이고 나머지도 ·· 로 **전부 기능어/일반어**다. 81차의 / 동음이의와 같은 잡음이다. 입력 쌍도 어긋나 있다: entry 는 영어 추상화 + 영어 kebab cue, task 제목은 한국어 산문이라 cue_anchors 기여가 0.062 다. 즉 후보 10건은 '덮이지 않은 것' 이 아니라 사실상 **최근 task 10개**다.
- Completion criteria: 도구 순위가 아니라 재사용 가치로 골라 승격하고, 고른 이유를 남긴다
- Completion criteria: 판정식이 잡음이라는 실측을 기록해 다음 세션이 같은 조사를 반복하지 않게 한다
- Completion criteria: entry 가 스키마 정합이고 related_ids 로 기존 entry 와 이어진다

## 🛠️ Implementation / Content

- Progress: 완료 — 7건 승격, 판정식 결함은 실측으로 기록
- Next session starting point:
- Remaining risks: 기계적으로 10건을 다 넣으면 index 가 부풀고 query 정밀도가 떨어진다 — 승격은 개수가 아니라 재사용 가치다

## ✅ Outcome

- Result: 도구 순위가 아니라 재사용 가치로 골라 7건을 승격했다(10건 기계 승격 아님). 이번 사이클이 반복해서 가르친 것들이다: 요약 수치는 종료 코드가 아니다 / 통과와 의도 달성은 다른 질문이다 / 쓰는 쪽이 못 읽는 규약은 강제되지 않는다 / 로컬 green 과 CI green 은 다른 경로를 밟을 수 있다 / 최적화는 개수가 아니라 분포다 / 기능을 옮기면 계약이 조용히 빠진다 / 어휘 겹침 지표는 아무것도 재지 않고 상수를 낸다.
- Result: entry 15 → 22. 스키마는 기존 entry 와 필드 집합 동일함을 대조했고, 7건을 서로와 기존 entry(MEM-2026-08-20-003/004)에 related_ids 로 이었다 — 고립 entry 는 expansion 이 안 붙어 검색에서 죽는다. 검색 실측: '종료,코드,요약,green' → 001 1순위 · '로컬,ci,dist,경로' → 004 1순위 · '스탬프,규약,쓰는,쪽' → 003 1순위. 즉 넣은 것이 실제로 걸린다.
- Result: state.json 이 새 entry 를 안 따라와 게이트가 red 였다(memory_entries / memory_entries_count 드리프트). wk refresh-state 로 해소 — memory_index 를 손으로 늘리면 생성물도 같이 갱신해야 한다.
- Verification: check_memory_index 26/26 · memory_index_cross 4/4 · telemetry 6/6 · bidir_link 6/6 · dashboard 계열 전부 PASS · 검색 실측 3질의 모두 의도한 entry 1순위 · --changed 100/100 green
- Follow-up: suggest-memory-entries 의 coverage 판정식을 고칠지는 별도 판단 — 지금은 '최근 task 목록' 이상의 정보를 주지 않는다
