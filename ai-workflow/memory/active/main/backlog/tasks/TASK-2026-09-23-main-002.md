---
id: TASK-2026-09-23-main-002
status: done
created_at: 2026-09-23
source_anchor: generic-task-2026-09-23-main-002
source_path: backlog/2026-09-23.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-09-23-main-002 — check_release_wrapper_args case 6 이 git 인덱스 락을 잡아 병렬에서 flake

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-23
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: CI smoke 의 slash/py3.13 셀 하나가 red 였고 다음 커밋은 green 이었다 — 경합이다. 실패는 check_release_wrapper_args case 6 이고 stderr 가 'rc=128 fatal: Unable to create .../index.lock' 이다. 그 case 는 실 저장소의 더티 경로를 모아 git add --dry-run 을 돌리는데, --dry-run 이어도 git 은 **인덱스 락**을 잡는다. 병렬 구간에서 다른 검사가 동시에 인덱스를 건드리면 128 로 죽는다. 이 검사는 이미 '살아있는 저장소 상태' 를 입력으로 쓰는 부류라(2026-09-22 에 .gitignore 무시 경로 문제로도 red 였다) 같은 자리가 반복된다. 락을 안 잡는 수단(git ls-files / check-ignore 로 pathspec 검증)으로 바꾸거나 정숙 구간으로 옮기는 두 갈래가 있다.
- Completion criteria: 그 case 가 인덱스 락을 잡지 않거나 정숙 구간에서 돈다
- Completion criteria: 경합을 재현해 red 를 실증한 뒤 수리로 green

## 🛠️ Implementation / Content

- Progress: `2026-09-23 00:22` 기준 CI smoke 의 slash/py3.13 셀 하나가 red 였고 다음 커밋은 green 이었다 — 경합이다. 실패는 check_release_wrapper_args case 6 이고 stderr 가 'rc=128 fatal: Unable to create .../index.lock' 이다. 그 case 는 실 저장소의 더티 경로를 모아 git add --dry-run 을 돌리는데, --dry-run 이어도 git 은 **인덱스 락**을 잡는다. 병렬 구간에서 다른 검사가 동시에 인덱스를 건드리면 128 로 죽는다. 이 검사는 이미 '살아있는 저장소 상태' 를 입력으로 쓰는 부류라(2026-09-22 에 .gitignore 무시 경로 문제로도 red 였다) 같은 자리가 반복된다. 락을 안 잡는 수단(git ls-files / check-ignore 로 pathspec 검증)으로 바꾸거나 정숙 구간으로 옮기는 두 갈래가 있다.
- Next session starting point:
- Remaining risks: 정숙 구간으로 옮기면 직렬 구간이 늘어 main-008 의 절감을 일부 되돌린다 — 락 회피가 우선

## ✅ Outcome

- Result: 완료. case 6 의 git add --dry-run 을 git ls-files --error-unmatch --cached --others --exclude-standard 로 바꿨다 — 같은 pathspec 해석기를 쓰면서 index 를 읽기만 한다. 락을 인위로 쥔 tmp repo 에서 실측하면 add 는 rc=128(CI 가 낸 바로 그 오류), ls-files 는 rc=0 이다. 하위 디렉터리에서는 ls-files 도 rc=1 이라 6b 의 버그 재현 방향이 살아 있다. 대체 수단이 원래 결함을 여전히 볼 수 있는지는 case 6c 를 새로 두어 증인으로 세웠다 — 락 경합이 없는 tmp repo 에서 추적/미추적이 섞인 대상에 대해 ls-files 와 git add --dry-run 이 두 cwd 방향 모두 같은 판정을 내는지 잰다. 이게 갈리면 case 6 이 production 의 git add 를 더 이상 대리하지 못한다는 뜻이다. 부수로 이 검사의 total 이 상수 11 이라 12 PASS 에 '11/11' 을 찍고 있던 것을 발화 수 파생으로 고쳤고(생략 분기도 check() 로 통과시켜 세게 했다), 같은 결함족 전수 조사를 main-004 로 등록했다 — 상수 total 20건 중 현재 선언과 실제가 갈린 것이 4건이다.
- Verification: check_release_wrapper_args 12/12 (선언 파생) · 락 보유 tmp repo 실측으로 add rc=128 / ls-files rc=0 대조 · case 6c 가 두 cwd 방향에서 두 수단의 판정 일치를 고정 · CI red 재현 출처는 run 35802958781 slash/py3.11 셀의 smoke-summary.md
- Follow-up:
