---
id: TASK-2026-09-23-main-003
status: done
created_at: 2026-09-23
source_anchor: generic-task-2026-09-23-main-003
source_path: backlog/2026-09-23.md
kind: generic
wbs: M-007/WBS-7.3
---

# TASK-2026-09-23-main-003 — memory_index 승격 후보 판정을 어휘 겹침에서 선언(source_paths)으로

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-23
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: 85차가 승격을 하면서 이 판정식이 잡음임을 실측했고(평균 0.22 최대 0.40, 최고 쌍의 공유 토큰이 '자기'·'판정이' 같은 기능어, cue_anchors 기여 0.062) follow-up 으로 남긴 것을 소유자가 진행 지시했다. 81차가 graph_insights 에서 같은 결함족을 고친 방법이 선언 사슬이었고, 여기에도 선언이 **이미 있다** — entry 의 source_paths 가 task 파일을 인용한다. 실측: entry 22건 중 7건이 task 를 인용하고(2026-08-10 이후 생성분 전부), 인용 없는 15건은 초기 07-09 분 8건과 85차가 손으로 만든 7건이다. 즉 규약은 있는데 판정이 그것을 안 읽고 어휘를 읽고 있었다. 승격이 곧 '이 task 의 교훈을 담았다' 는 선언이므로, coverage 를 '어떤 entry 가 이 task 를 인용하는가' 로 바꾸면 정확해지고, 덤으로 85차가 만든 7건의 인용 누락도 드러난다.
- Completion criteria: coverage 가 entry 의 task 인용 선언에서 파생한다 (어휘 겹침 아님)
- Completion criteria: 선언을 못 읽는 경우는 통과가 아니라 미측정으로 나온다
- Completion criteria: 85차가 만든 7건에 출처 task 인용을 채운다
- Completion criteria: 되주입으로 판정이 살아 있음을 실증한다

## 🛠️ Implementation / Content

- Progress: `2026-09-23 00:22` 기준 85차가 승격을 하면서 이 판정식이 잡음임을 실측했고(평균 0.22 최대 0.40, 최고 쌍의 공유 토큰이 '자기'·'판정이' 같은 기능어, cue_anchors 기여 0.062) follow-up 으로 남긴 것을 소유자가 진행 지시했다. 81차가 graph_insights 에서 같은 결함족을 고친 방법이 선언 사슬이었고, 여기에도 선언이 **이미 있다** — entry 의 source_paths 가 task 파일을 인용한다. 실측: entry 22건 중 7건이 task 를 인용하고(2026-08-10 이후 생성분 전부), 인용 없는 15건은 초기 07-09 분 8건과 85차가 손으로 만든 7건이다. 즉 규약은 있는데 판정이 그것을 안 읽고 어휘를 읽고 있었다. 승격이 곧 '이 task 의 교훈을 담았다' 는 선언이므로, coverage 를 '어떤 entry 가 이 task 를 인용하는가' 로 바꾸면 정확해지고, 덤으로 85차가 만든 7건의 인용 누락도 드러난다.
- Next session starting point:
- Remaining risks: 초기 entry 8건은 task 인용이 없다 — 소급으로 달 수 없는 것은 미측정으로 남기고 그 사실을 출력한다

## ✅ Outcome

- Result: 완료. 판정을 어휘 겹침에서 선언(entry.source_paths 의 task 인용)으로 바꿨다. 지표가 covered=0/candidates=10 상수에서 covered=7 / candidates=3 / unmeasured=8 로 바뀌었다 — 인용 없는 entry 는 '안 덮음' 이 아니라 entries_without_task_citation 으로 따로 나온다. 어휘 겹침은 판정에서 빼고 related_ids 힌트로만 남겼고, skeleton 의 source_paths 에 출처 task 경로를 미리 채워 다음 승격부터 규약이 자동으로 지켜지게 했다. 85차가 손으로 만든 7건에 인용을 채웠고, 초기 8건은 소급 불가라 미측정으로 남는다. 되주입 5종이 각각 다른 지문을 남겼다 — 인용 판정 제거 → covered=0/unmeasured=8, 인용 파싱 제거 → covered=0/unmeasured=22, 옛 어휘식 복원 → covered=0 (옛 식이 아무것도 못 잡는다는 증명), 인용 파싱 상수화 → case 1·6·7·10·11 red, 미측정 집계 상수화 → case 11 만 단독 red. 복원 해시가 원본과 일치했다. 검사에 case 2건을 더했다 — 어휘 겹침으로는 통과할 수 없는 case(어휘0+인용=덮임, 어휘만=후보 유지) 와 미측정 집계 case. 부수로 이 검사의 total 이 상수 9 라 case 11개에 '9/9 PASS' 를 찍고 있던 것을 발화 수 파생으로 고쳤다.
- Result: check_memory_entry_suggestions 11/11 · check_memory_index 26/26 · memory_index_cross 4/4 · memory_index_telemetry 6/6 · check_convention_single_source 5/5 (정본 정규식 미참조로 한 번 red 였던 것을 해소) · 되주입 5종이 각각 다른 지문 + 복원 해시 원본 일치 · 실측 지표 covered=7 candidates=3 unmeasured=8 · --changed 게이트는 state.json drift 1건만 남았고 refresh-state 로 해소 · push 직전 4셀 전량은 커밋 전에 돌린다
- Verification: check_memory_entry_suggestions 11/11 · check_memory_index 26/26 · memory_index_cross 4/4 · memory_index_telemetry 6/6 · check_convention_single_source 5/5 (정본 정규식 미참조로 한 번 red 였던 것을 해소) · 되주입 5종이 각각 다른 지문 + 복원 해시 원본 일치 · 실측 지표 covered=7 candidates=3 unmeasured=8 · --changed 게이트는 state.json drift 1건만 남았고 refresh-state 로 해소 · push 직전 4셀 전량은 커밋 전에 돌린다
- Follow-up:
