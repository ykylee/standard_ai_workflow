---
id: TASK-2026-09-23-main-004
status: done
created_at: 2026-09-23
source_anchor: generic-task-2026-09-23-main-004
source_path: backlog/2026-09-23.md
kind: generic
wbs: M-007/WBS-7.4
---

# TASK-2026-09-23-main-004 — 검사 요약의 총 개수가 상수라 case 증감을 못 본다 (전수 20건 중 4건 현재 불일치)

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-23
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: 검사 요약 줄의 총 개수가 상수라, case 를 늘려도 옛 숫자를 찍는다. 2026-09-23 에 두 번 실물로 만났다 — check_memory_entry_suggestions 는 case 11개에 '9/9 PASS', check_release_wrapper_args 는 12개에 '11/11 PASS'. 둘 다 전체 green 이라 아무도 못 봤다. 전수 조사: 'total = <정수>' 를 쓰는 검사 20개 중 선언과 실제 PASS 줄 수가 **현재 갈린 것이 4개**다 (위 둘 + check_telemetry_window 8/9 + check_wk_dispatcher 10/11). 나머지는 지금 우연히 맞을 뿐 같은 구조다. 이건 '검사는 깨지지 않고 무력화된다' 의 계수 판이다 — case 를 지워도 요약이 옛 개수를 유지하면 무력화가 안 보인다. 20개 중 11개는 check() 헬퍼가 있어 발화 수 파생이 기계적이고, 9개는 형태가 제각각이라 개별로 본다.
- Completion criteria: 상수 total 20개가 전부 발화 수 파생으로 바뀐다
- Completion criteria: 선언과 실제 발화 수가 갈리면 게이트가 red 가 되는 검사를 둔다
- Completion criteria: 되주입 — case 를 하나 지우면 요약 숫자가 따라 줄어든다

## 🛠️ Implementation / Content

- Progress: `2026-09-23 00:50` 기준 검사 요약 줄의 총 개수가 상수라, case 를 늘려도 옛 숫자를 찍는다. 2026-09-23 에 두 번 실물로 만났다 — check_memory_entry_suggestions 는 case 11개에 '9/9 PASS', check_release_wrapper_args 는 12개에 '11/11 PASS'. 둘 다 전체 green 이라 아무도 못 봤다. 전수 조사: 'total = <정수>' 를 쓰는 검사 20개 중 선언과 실제 PASS 줄 수가 **현재 갈린 것이 4개**다 (위 둘 + check_telemetry_window 8/9 + check_wk_dispatcher 10/11). 나머지는 지금 우연히 맞을 뿐 같은 구조다. 이건 '검사는 깨지지 않고 무력화된다' 의 계수 판이다 — case 를 지워도 요약이 옛 개수를 유지하면 무력화가 안 보인다. 20개 중 11개는 check() 헬퍼가 있어 발화 수 파생이 기계적이고, 9개는 형태가 제각각이라 개별로 본다.
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: 완료. 판정을 러너에 뒀다 (workflow_kit/common/case_count.py + run_all_checks.case_count_verdict). 러너는 이미 각 검사의 stdout+stderr 를 전량 받아 두므로 재실행 비용이 0이고, 경고 축·저장소 write 축과 같은 자리다. 선언(N/M PASS 또는 All N tests passed.)과 발화한 case 줄 수가 갈리면 exit 1 이다. 범위는 실측으로 두 번 넓혔다 — 처음에는 줄 맨 앞 case 줄만 받아 290개 중 28개만 읽혔고(못 읽은 표본은 전부 자기 case 를 들여쓰는 검사였으며 선언과 들여쓴 수가 정확히 같았다), 넓힌 뒤 남은 위양성 1건(check_agent_plugin_payload — 콜론 없이 PASS 만 찍는 case 하나, 이미 파생이었다)까지 받아 0으로 만들었다. 전량에서 드러난 위양성 하나 더는 check_cli_per_strategy 로, production CLI(url_validity)가 찍는 'PASS <url>' 이 case 줄 흐름에 섞여 2 vs 4 로 보였다 — production 표기는 두고 검사 쪽에서 stdout 을 삼켰다. 상수 total 은 15건 전부 파생으로 바꿨고(total = 0 누산기 5건은 대상이 아니다) 그 과정에서 check_telemetry_window 8→9, check_wk_dispatcher 10→11 로 값이 정정됐다. check_case_count 를 새로 두어 기구 자체를 8 case 로 고정했고 case 8 은 저장소에 상수 total 이 남아 있지 않은지 전수로 훑는다. 요구 check-summary-must-count-what-ran 을 spec 정본에 등록했다. 되주입 7종이 각각 다른 지문을 남겼다 — 맨앞만 세기→case3, 콜론 필수→4, All N 제거→5, 첫 요약 사용→7, diverged 상수화→2·5, 미측정을 측정으로→6, 상수 total 재도입→8. 복원 해시가 원본과 일치했다.
- Verification: --branch-context=all 582/582 (291 × native/slash) rc=0, 불일치 0 · 축이 실제로 잡는 것을 배선 전에 실측 (telemetry_window 8/9 · wk_dispatcher 10/11 로 rc=1) · 되주입 7종 각각 다른 지문 + 복원 해시 일치 · check_case_count 8/8 · check_requirement_declarations 4/4
- Follow-up: 미측정 320/582 이 남는다. 306건은 개수 선언 자체가 없어(그냥 'X check passed') 대조할 대상이 없고, 14건은 선언은 있는데 PASS:/FAIL: 형태의 case 줄이 없다. 전자는 계약 밖이라 red 가 아니지만, 그만큼 이 축의 사각지대다 — 선언을 갖게 만들지 말지는 따로 판단할 것.
