---
id: TASK-2026-09-23-main-005
status: planned
created_at: 2026-09-23
source_anchor: generic-task-2026-09-23-main-005
source_path: backlog/2026-09-23.md
kind: generic
wbs: M-007/WBS-7.3
---

# TASK-2026-09-23-main-005 — memory_index 검색이 매칭된 entry 자신을 안 돌려준다 (seed 가 확장분에 ID순으로 밀림) + 한국어 질의는 기본 경로에서 0

## 📝 Description

- Status: planned
- Priority: high
- Request date: 2026-09-23
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: 승격 4건을 넣고 '실제로 걸리는지' 를 재다가 검색 경로의 결함 2건을 실측했다. ①**seed 가 자기 확장분에 밀려난다.** query_memory_index_for_dispatcher 는 cue 로 seed 를 찾고 related_ids 로 확장한 뒤 `sorted(seed_and_linked)[:top_k]` 로 자른다. ID 사전순 = 날짜순이고 이 저장소의 링크 관례는 최신→기존(26/26 단방향)이라, 확장분이 항상 seed 보다 오래됐고 **매칭된 entry 자신이 top_k 밖으로 밀린다.** 실측: entry 의 정확한 cue 로 질의해도 그 entry 가 상위 3에 한 번도 안 나온다 — `constant-metric-is-a-defect`(007 의 cue) → 08-20-001·08-20-003·09-23-001, `declaration-already-exists`(008) → 08-20-001·003·004, `dry-run-still-locks`(009) → 08-20-001·003·09-23-002. 3/3 에서 seed 포함=False. 즉 cue_hits=1 을 보고하면서 정작 그 1건을 안 돌려준다. ②**한국어 질의는 기본 경로에서 0 이다.** cue_anchors 가 전부 영문 kebab 이고 `use_bm25_fallback` 기본값이 False 라, wk memory-index-query 로 한국어 토큰을 주면 selected_count=0 이 나온다. BM25 를 켜면 4건 전부 걸린다(락/대체→009 1순위, 소급/스탬프→011 2순위, 선언/인용→008 2순위, 위양성/범위→010 3순위). 85차 handoff 가 '종료,코드,요약,green → 001 1순위' 로 기록한 질의도 지금 기본 경로에서는 0 이다 — 그때 무엇으로 쟀는지 확인이 필요하다.
- Completion criteria: entry 의 정확한 cue 로 질의하면 그 entry 가 결과에 반드시 포함된다 (seed 우선, 확장분은 그 뒤)
- Completion criteria: 선택 순서가 ID 사전순이 아니라 관련도/역할 순이다
- Completion criteria: 한국어 질의의 기본 경로 동작을 결정한다 — BM25 기본 활성이든, 미지원을 명시하든 '조용히 0' 은 아니게
- Completion criteria: 되주입 — seed 를 밀어내는 정렬을 되돌리면 red

## 🛠️ Implementation / Content

- Progress: `2026-09-23 03:06` 기준 승격 4건을 넣고 '실제로 걸리는지' 를 재다가 검색 경로의 결함 2건을 실측했다. ①**seed 가 자기 확장분에 밀려난다.** query_memory_index_for_dispatcher 는 cue 로 seed 를 찾고 related_ids 로 확장한 뒤 `sorted(seed_and_linked)[:top_k]` 로 자른다. ID 사전순 = 날짜순이고 이 저장소의 링크 관례는 최신→기존(26/26 단방향)이라, 확장분이 항상 seed 보다 오래됐고 **매칭된 entry 자신이 top_k 밖으로 밀린다.** 실측: entry 의 정확한 cue 로 질의해도 그 entry 가 상위 3에 한 번도 안 나온다 — `constant-metric-is-a-defect`(007 의 cue) → 08-20-001·08-20-003·09-23-001, `declaration-already-exists`(008) → 08-20-001·003·004, `dry-run-still-locks`(009) → 08-20-001·003·09-23-002. 3/3 에서 seed 포함=False. 즉 cue_hits=1 을 보고하면서 정작 그 1건을 안 돌려준다. ②**한국어 질의는 기본 경로에서 0 이다.** cue_anchors 가 전부 영문 kebab 이고 `use_bm25_fallback` 기본값이 False 라, wk memory-index-query 로 한국어 토큰을 주면 selected_count=0 이 나온다. BM25 를 켜면 4건 전부 걸린다(락/대체→009 1순위, 소급/스탬프→011 2순위, 선언/인용→008 2순위, 위양성/범위→010 3순위). 85차 handoff 가 '종료,코드,요약,green → 001 1순위' 로 기록한 질의도 지금 기본 경로에서는 0 이다 — 그때 무엇으로 쟀는지 확인이 필요하다.
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result:
- Follow-up:
