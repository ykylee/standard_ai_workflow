---
id: TASK-2026-09-23-main-006
status: done
created_at: 2026-09-23
source_anchor: generic-task-2026-09-23-main-006
source_path: backlog/2026-09-23.md
kind: generic
wbs: M-007/WBS-7.3
---

# TASK-2026-09-23-main-006 — case 수 대조 축의 사각지대 — 개수 선언이 없는 검사를 실측으로 가른다 (main-004 follow-up)

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-09-23
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: main-004 가 만든 case 수 대조 축이 292개 중 절반 조금 넘게만 읽는다. 마지막 게이트 실측(2셀 합계 320건 미측정): 개수 선언이 아예 없는 것 306, 선언은 있는데 PASS:/FAIL: 형태의 case 줄이 없는 것 14. 선언이 없는 쪽은 계약 밖이라 red 가 아니지만, 그만큼 이 축이 못 보는 자리이고 미측정은 통과가 아니다. 다만 '전부 선언을 달게 한다' 가 답인지는 아직 모른다 — case 가 여럿인데 안 세는 검사와, 애초에 단일 판정이라 셀 것이 없는 검사가 섞여 있을 것이다. 먼저 미측정 집합을 재현 가능하게 뽑고(일회용 조사는 조사가 아니다) 그 안에서 case 줄을 실제로 찍는 것이 몇 개인지 실측한 뒤, 고칠 대상과 계약 밖으로 남길 대상을 가른다.
- Completion criteria: 미측정 집합을 재현 가능한 수단으로 뽑는다 (일회용 스크립트 아님)
- Completion criteria: 그 안에서 case 줄을 찍는 검사 수를 실측하고, 고칠 대상과 계약 밖을 가른다
- Completion criteria: 고치기로 한 것은 발화 수 파생으로 바꾸고 축이 읽는지 확인한다
- Completion criteria: 남긴 것은 '왜 계약 밖인가' 를 근거와 함께 기록한다

## 🛠️ Implementation / Content

- Progress: `2026-09-23 04:38` 기준 main-004 가 만든 case 수 대조 축이 292개 중 절반 조금 넘게만 읽는다. 마지막 게이트 실측(2셀 합계 320건 미측정): 개수 선언이 아예 없는 것 306, 선언은 있는데 PASS:/FAIL: 형태의 case 줄이 없는 것 14. 선언이 없는 쪽은 계약 밖이라 red 가 아니지만, 그만큼 이 축이 못 보는 자리이고 미측정은 통과가 아니다. 다만 '전부 선언을 달게 한다' 가 답인지는 아직 모른다 — case 가 여럿인데 안 세는 검사와, 애초에 단일 판정이라 셀 것이 없는 검사가 섞여 있을 것이다. 먼저 미측정 집합을 재현 가능하게 뽑고(일회용 조사는 조사가 아니다) 그 안에서 case 줄을 실제로 찍는 것이 몇 개인지 실측한 뒤, 고칠 대상과 계약 밖으로 남길 대상을 가른다.
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: 완료. 실측이 전제를 뒤집었다 — 36개 검사를 고칠 일이 아니라 축의 정규식이 좁았다. '개수 선언이 없다' 로 분류된 153건 중 36건은 이미 선언하고 있었고 장식(=== )이 앞에 붙거나 PASS: 가 비율보다 먼저 오는 모양이었다. main-004 에서 두 번 겪은 자리를 세 번째로 밟은 것이다. 조사를 재현 가능하게 먼저 만들었다 — 러너에 --case-count-dump 를 달아 검사별 (선언, 발화, 측정 여부, 사유)를 JSON 으로 낸다. 확장 5종: 장식 접두(=== N/M PASS ===) · 어순(=== PASS: N/M ===) · 비율 아닌 합(7 pass, 0 fail) · 산문 속 개수((9 assertions)) · 줄 중간 판정어(case 3 (…): PASS). 그리고 흐름이 둘일 때는 명시적 롤업([PASS] name)이 이기게 했다 — check_mypy_config_actually_loaded 이 하위 단언 6줄과 롤업 7줄을 같이 찍어 더하면 13, 하위만 세면 6 이었다. 측정 132 → 188 (292 중). 확장이 진짜 결함 1건을 드러냈다: check_claim_workspace 가 case 10개를 돌리면서 요약은 (9 assertions) 였고 docstring 도 9개만 나열했다 — 나중에 추가된 case 를 아무도 세지 않았다. len(RAN) 파생으로 바꾸고 문서도 10개로 정정했다. 계약 밖으로 남긴 것 3건(consumer_metrics · watch_transient_writer · appendonly_memory_layout)은 개수를 선언하지만 case 줄을 아예 안 찍어 대조할 대상이 없다. **여기서 확장을 멈췄다** — 남은 22건 중 Result: 계열 12건은 흐름이 둘이다(하위 case 23줄 vs 테스트 함수 8). 받으면 위양성 12건이 생기고, 위양성 내는 검사는 무시당한다. 되주입 5종이 각각 다른 지문(장식/합/산문/줄중간/롤업)이고 복원 해시 일치. 중간에 주입 하나가 닫는 괄호를 주석에 넣어 SyntaxError 를 만들었고 그것이 'red 없음' 으로 보였다 — 이 세션 두 번째다.
- Verification: check_case_count 14/14 (확장 5종 + 롤업 우선을 case 9·10·11 로 고정) · 되주입 5종 각각 다른 지문 + 복원 해시 일치 · check_claim_workspace 10/10 (선언 파생) · 게이트 --branch-context=all 584/584 rc=0, 불일치 0 · 미측정 320 → 208 (2셀), 측정 132 → 188 (1셀) · 분류 근거는 --case-count-dump 로 재현 가능
- Follow-up: 잔여 미측정 104건(1셀): ①선언 없음 101 — 그중 case 줄을 찍는 22건이 실질 대상이고, Result: 계열 12건은 흐름이 둘이라 먼저 '무엇이 case 인가' 를 그 검사들이 스스로 말하게 해야 한다([PASS] 롤업을 달면 지금 규칙으로 바로 읽힌다). v0_7_2x 계열 10건은 `Result: N/M PASS` 가 자기 줄에 있고 흐름이 하나라 접두만 받으면 되지만, 같은 정규식이 12건에 위양성을 내므로 둘을 같이 풀어야 한다. ②case 줄 없는 선언 3건은 개수만 있고 나열이 없어 대조 대상이 없다.
