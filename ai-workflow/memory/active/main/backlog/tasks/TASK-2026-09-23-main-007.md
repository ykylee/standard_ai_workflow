---
id: TASK-2026-09-23-main-007
status: done
created_at: 2026-09-23
source_anchor: generic-task-2026-09-23-main-007
source_path: backlog/2026-09-23.md
kind: generic
wbs: M-007/WBS-7.3
---

# TASK-2026-09-23-main-007 — case 수 대조 축 잔여 22건 — 흐름이 둘인 검사가 무엇이 case 인지 스스로 말하게 (main-006 follow-up)

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-09-23
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: main-006 이 축의 범위를 132 → 188 로 넓히고 멈춘 자리다. 남은 미측정 104건(1셀) 중 실질 대상은 'case 줄은 찍는데 선언이 없는' 22건이고, 둘로 갈린다. ①v0_7_2x 계열 10건은 `Result: N/M PASS` 가 자기 줄에 있고 흐름이 하나라 접두만 받으면 된다. ②`=== Result: N/M PASS ===` 계열 12건은 같은 출력에 흐름이 둘이다 — 하위 case(`case 3 (…): PASS`)가 23줄인데 선언 8은 테스트 함수 수다. 접두만 받으면 ②에 위양성 12건이 생기므로 main-006 은 확장을 멈췄다. 푸는 방법은 ②가 '무엇이 case 인가' 를 스스로 말하게 하는 것이다 — 명시적 롤업(`[PASS] name`)을 달면 지금 규칙(롤업 우선)으로 바로 읽히고, 그 다음 접두 확장이 ①②에 모두 안전해진다. 먼저 22건의 흐름 수를 실측으로 확인하고(추측으로 나누지 않는다) 그 위에서 작업한다.
- Completion criteria: 22건의 흐름 수를 실측으로 가른다 (추측 아님)
- Completion criteria: 흐름이 둘인 검사는 명시적 롤업을 달아 무엇이 case 인지 스스로 말한다
- Completion criteria: 그 뒤 Result: 접두를 받아도 위양성 0 임을 전수로 확인한다
- Completion criteria: 되주입 — 롤업을 지우거나 접두를 되돌리면 red

## 🛠️ Implementation / Content

- Progress: `2026-09-23 05:08` 기준 main-006 이 축의 범위를 132 → 188 로 넓히고 멈춘 자리다. 남은 미측정 104건(1셀) 중 실질 대상은 'case 줄은 찍는데 선언이 없는' 22건이고, 둘로 갈린다. ①v0_7_2x 계열 10건은 `Result: N/M PASS` 가 자기 줄에 있고 흐름이 하나라 접두만 받으면 된다. ②`=== Result: N/M PASS ===` 계열 12건은 같은 출력에 흐름이 둘이다 — 하위 case(`case 3 (…): PASS`)가 23줄인데 선언 8은 테스트 함수 수다. 접두만 받으면 ②에 위양성 12건이 생기므로 main-006 은 확장을 멈췄다. 푸는 방법은 ②가 '무엇이 case 인가' 를 스스로 말하게 하는 것이다 — 명시적 롤업(`[PASS] name`)을 달면 지금 규칙(롤업 우선)으로 바로 읽히고, 그 다음 접두 확장이 ①②에 모두 안전해진다. 먼저 22건의 흐름 수를 실측으로 확인하고(추측으로 나누지 않는다) 그 위에서 작업한다.
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: 완료. 22건을 실측으로 갈랐다 — 11 단일 흐름 / 10 이중 흐름 / 1 기타. 그리고 이번에도 파일 편집이 아니라 규칙 문제였다: 이중 흐름 9건은 **이미 롤업을 찍고 있었고** 단지 [PASS] 가 아니라 '✓ name PASS' 모양이었다. 9개 파일을 고칠 게 아니라 롤업 표식에 ✓/✗ 를 더하면 되는 일이었고, 그 위에서 Result: 접두와 (all N checks) 를 받아도 위양성이 0 이다. main-006 이 '위양성 12건이 난다' 며 멈춘 자리의 실제 원인이 이것이었다 — 멈춘 판단은 옳았고, 원인은 검사들이 제각각인 게 아니라 롤업 표식을 하나만 알고 있던 것이었다. 진짜 결함 1건: check_v0_7_26_sync_release_hash 가 선언 5 / 증거 줄 4 였다. test_real_repo_hash_sync 는 실 저장소에 v0.7.25 entry 가 없으면 SKIP 만 찍고 True 를 돌려줘 **미측정이 통과로 세어지면서 요약에도 안 보였다**. _SKIPS 카운터를 두어 'Result: 5/5 PASS (1 skipped)' 로 찍게 하고 case 마다 롤업 한 줄을 내게 했으며, 축도 SKIP 을 발화한 case 로 센다(선언에 세어지면서 롤업에서 빠지면 그 자체가 불일치다). 측정 188 → 212 / 292, 미측정 208 → 160(2셀). **실질 잔여 0** — 선언 없는 76건 중 case 줄을 찍는 것이 하나도 없다(전부 단일 판정 검사라 계약 밖). 개수만 선언하고 나열이 없는 4건(appendonly_memory_layout · bootstrap_interactive_picker · consumer_metrics · watch_transient_writer)은 대조 대상이 없어 남는다.
- Verification: check_case_count 19/19 (12·13·14 신설) · 되주입 4종 각각 다른 지문 (Result 접두 → 12·13·14 / checks 형태 → 12 / ✓✗ 롤업 제거 → 12·13·14 / SKIP 제외 → 14 단독) + 복원 해시 일치 · check_v0_7_26_sync_release_hash 5/5 (1 skipped, 선언=발화=5) · 게이트 --branch-context=all 584/584 rc=0 불일치 0 · 잔여 분류는 --case-count-dump 로 재현 가능
- Follow-up: 개수만 선언하고 case 나열이 없는 4건이 남는다 — 대조할 대상이 없어 이 축이 못 본다. 고치려면 그 검사들이 case 마다 롤업 한 줄을 내야 하는데, 그것이 값어치가 있는지는 각 검사가 무엇을 보장하는지 보고 판단할 것. 선언 없는 76건은 단일 판정이라 계약 밖이다.
