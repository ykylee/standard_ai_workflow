---
id: TASK-2026-09-23-main-008
status: done
created_at: 2026-09-23
source_anchor: generic-task-2026-09-23-main-008
source_path: backlog/2026-09-23.md
kind: generic
wbs: M-007/WBS-7.4
---

# TASK-2026-09-23-main-008 — wk release-bump 가 --version 을 무시하고 patch 자동 증가 + 직전 커밋을 amend

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-23
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: v1.11.0 발행 준비에서 `wk release-bump --version 1.11.0 --apply` 가 두 가지를 했다. ①**요청한 버전을 무시하고** patch 자동 증가를 했다 — pyproject 가 1.10.0 에서 1.10.1 이 됐다. `--version` 을 받는 자리가 없거나 무시된다(`release-bump --help` 도 도움말 대신 dry-run 을 실행한다 — 별개 결함). ②그 직후 post-step 이 `git commit --amend` 로 **직전 커밋(내가 방금 만든 릴리스 노트 커밋)을 덮어** 버전 파일 3개를 흡수했다. push 전이라 `reset --soft` 로 되돌리고 pyproject/`__init__`/plugin package.json 에 1.11.0 을 손으로 박은 뒤 `plugin_payload --apply` 로 동기화해 진행했다. 더티 트리 가드 자체는 정상 동작했다(1차 시도를 막았다) — 문제는 가드를 통과한 뒤다. 결과적으로 발행은 정상이지만, 다음 사람이 같은 명령을 믿고 쓰면 조용히 틀린 버전이 박힌다.
- Completion criteria: --version <X.Y.Z> 를 주면 그 버전이 박힌다 (되주입 — 다른 버전을 주면 그 값이 나온다)
- Completion criteria: --help 가 도움말을 내고 부수효과가 없다
- Completion criteria: amend 대상이 '내가 만든 커밋' 인지 확인하거나, 흡수 대신 새 커밋을 만든다

## 🛠️ Implementation / Content

- Progress: `2026-09-23 06:56` 기준 v1.11.0 발행 준비에서 `wk release-bump --version 1.11.0 --apply` 가 두 가지를 했다. ①**요청한 버전을 무시하고** patch 자동 증가를 했다 — pyproject 가 1.10.0 에서 1.10.1 이 됐다. `--version` 을 받는 자리가 없거나 무시된다(`release-bump --help` 도 도움말 대신 dry-run 을 실행한다 — 별개 결함). ②그 직후 post-step 이 `git commit --amend` 로 **직전 커밋(내가 방금 만든 릴리스 노트 커밋)을 덮어** 버전 파일 3개를 흡수했다. push 전이라 `reset --soft` 로 되돌리고 pyproject/`__init__`/plugin package.json 에 1.11.0 을 손으로 박은 뒤 `plugin_payload --apply` 로 동기화해 진행했다. 더티 트리 가드 자체는 정상 동작했다(1차 시도를 막았다) — 문제는 가드를 통과한 뒤다. 결과적으로 발행은 정상이지만, 다음 사람이 같은 명령을 믿고 쓰면 조용히 틀린 버전이 박힌다.
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: 완료. 등록할 때는 release-bump 하나의 결함인 줄 알았는데 범위가 훨씬 넓었다. --version 이 무시된 진짜 원인은 그 커맨드가 아니라, _parse_flag/_has_flag 로 손수 파싱하는 커맨드 37개가 **전부 모르는 인자를 조용히 버리는 것**이었다 (그 함수들은 찾는 것만 보고 나머지를 무시한다). register 데코레이터에 가드를 넣어 rc=2 로 거절하고 **받는 목록을 함께** 낸다 — 무엇이 틀렸는지만 알려주고 무엇이 맞는지 안 알려주면 사용자는 다시 추측한다. 허용집합은 손 목록이 아니라 소스에서 파생한다(known_flags_for): 자기 docstring 의 --flag 표기 ∪ 자기 본문의 리터럴 ∪ 같은 모듈 _ 헬퍼의 리터럴. 호출한 헬퍼만으로 좁히지 않았다 — 함수를 인자로 받는 래퍼 하나에 호출그래프 파생이 조용히 풀리는 것을 이미 겪었기 때문이고, 모듈 단위로 느슨하게 잡아도 실측 허용집합 중앙값이 4개라 충분히 좁다. --help 는 부수효과 없이 도움말만 낸다. argparse 기반 39개는 자기 파서가 이미 거절하므로 가드를 켜지 않았다 — 켜면 argparse 의 풍부한 --help 를 빈약한 docstring 으로 덮는다. 다만 '이미 거절한다' 를 가정으로 두지 않고 case 3 이 전수로 실측한다. 되주입 6종: 거절 제거 → case 1·2, 받는 목록 숨김 → 2, docstring 파생 제거 → 6, 본문 파생 제거 → 3, --help 무력화 → 5. 그 과정에서 case 6 이 **발화 불가능**이었던 것을 잡았다 — 실물 커맨드는 네 플래그가 docstring·본문 양쪽에 있어 docstring 기여를 지워도 green 이었다. 한쪽에만 플래그를 둔 탐침으로 분리해 두 출처가 각각 발화한다. 요구 cli-must-reject-unknown-flags 를 spec 정본에 등록했다.
- Verification: check_cli_unknown_flags 8/8 (손 파싱 거절 + 받는 목록 + argparse 계열 전수 실측 + 위양성 없음 + --help 무부수효과 + 파생 두 출처 각각) · 되주입 6종 각각 다른 지문 + 복원 해시 일치 · 실측: `wk release-bump --version 1.11.0 --apply` 가 rc=2 로 거절되고 pyproject 불변 · `--to=1.11.0` 은 정상 동작 · argparse 계열 33개 rc=2 확인 · 게이트 --branch-context=all
- Follow-up:
