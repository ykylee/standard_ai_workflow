---
id: TASK-2026-09-23-main-009
status: done
created_at: 2026-09-23
source_anchor: generic-task-2026-09-23-main-009
source_path: backlog/2026-09-23.md
kind: generic
wbs: M-007/WBS-7.3
---

# TASK-2026-09-23-main-009 — 스탬프 유예가 로컬 게이트에서 축을 가린다 — 문서를 고친 사이클은 CI 에서만 발화

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-09-23
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: 86차 마지막 push(f4504818)에서 CI smoke 4셀이 전부 red 였다. 원인은 CLAUDE.md 의 `- 최종 수정일:` 이 뒤처진 것인데, **로컬 게이트는 같은 트리에서 green 이었다.** 스탬프 판정(check_doc_stamp_rule case 10 / doc_stamp.last_content_change_date)에는 '워킹 트리에 미커밋 변경이 있으면 유예' 가 있다 — 편집 중에 스탬프를 강요하지 않으려는 의도이고 그 자체는 타당하다. 그런데 그 유예 때문에 **방금 고친 문서의 스탬프 위반은 커밋되기 전까지 로컬에서 구조적으로 안 보인다.** push 직전 게이트는 정의상 커밋 전에 도는 경우가 많으므로, 문서를 고친 사이클에서는 이 축이 사실상 CI 에서만 발화한다. 이 저장소는 '로컬에 축이 없어 15연속 CI red' 를 이미 겪었는데, 이번은 축이 없는 게 아니라 **유예가 축을 가린** 경우다. 후보: ①게이트에 '커밋되면 어떻게 되는가' 를 재는 모드(스테이징 내용 기준 판정) ②push 직전 게이트에서만 유예를 끄는 축 ③doc-headers-update 를 커밋 전 훅으로. 어느 쪽이든 '유예가 가린 것' 을 사람이 기억하는 데 의존하지 않게 하는 것이 요점이다.
- Completion criteria: 문서를 고치고 커밋 전에 게이트를 돌려도 스탬프 위반이 드러난다
- Completion criteria: 편집 중 강요는 여전히 없다 (유예의 원래 의도 보존)
- Completion criteria: 되주입 — 스탬프를 뒤처지게 두면 커밋 전 게이트가 red

## 🛠️ Implementation / Content

- Progress: 진단 정정 + pre-push 게이트 구현, 커밋 전 검사 green
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: 진단 정정: 등록 당시 '미커밋 유예가 축을 가렸다' 는 틀렸다 — doc_stamp 는 미커밋 변경에 유예 0·오늘 기준이라 커밋 전 게이트에서도 red 다(이 세션에서 실제로 2건 잡힘). 86차 CI 로그 대조: f4504818 의 red 는 CLAUDE.md 스탬프(09-21 < 09-22)이고 그 파일은 그 커밋에서 고쳐졌다 — 게이트를 돈 뒤 편집해 게이트 없이 push 한 것. 로컬 green 은 다른 sha 의 green 이었다. 소유자 결정(pre-push 차단)으로: gate_evidence.check_push/main(pre-push CLI)·missing_contexts(발행 게이트와 공유 — release_pipeline.verify_gate_evidence 도 이것을 쓴다) 신설, .githooks/pre-push 가 ref 마다 tip sha 의 기록(컨텍스트 전부)을 요구·삭제 push 통과·태그 벗김·못 읽는 sha 막음, 이 저장소 core.hooksPath=.githooks 설정. hooksPath 가 .git/hooks 를 끄므로 <common dir>/hooks/pre-push(install_pre_push_hook 의 force 차단)를 같은 인자·입력으로 이어 부른다. CLAUDE.md 게이트 절에 규칙·켜는 법·우회(--no-verify) 명시
- Verification: check_pre_push_gate 2/2 (판정 7종 + hook 실행 exit code + 기존 hook 연결). 되주입 3종(컨텍스트 판정 제거·못 읽는 sha 통과·연결 제거) 각각 red 후 복원. 실 저장소 hook: HEAD(기록 있음) 통과 · HEAD~2(기록 없음) 차단 · 삭제 통과. --changed 270/270
- Follow-up: 기존 force 차단 hook(pre-push-no-force.sh)이 git 이 넘기지 않는 --force 인자를 찾아 사실상 발화하지 않는 것으로 보인다 — 별도 task 칩으로 분리. 다른 클론·호스트는 git config core.hooksPath .githooks 를 한 번 켜야 한다
