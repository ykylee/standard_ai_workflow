---
id: TASK-2026-09-23-main-013
status: done
created_at: 2026-09-23
source_anchor: generic-task-2026-09-23-main-013
source_path: backlog/2026-09-23.md
kind: generic
wbs: M-007/WBS-7.2
---

# TASK-2026-09-23-main-013 — doctor 가 플러그인 MCP 가 실제로 띄우는 해석기의 kit 버전을 재지 않는다

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-23
- Owner: yklee
- Host: macOS
- Host IP:
- Affected documents:
  - `docs/planning/workflow-assessment-2026-09.md`

- Description: 플러그인 .mcp.json 은 python3 -m workflow_kit… 만 부르므로 저장소 밖에서는 주변 python3 에 깔린 kit 이 뜬다. 87차 실측: 시스템 homebrew python 의 kit 이 옛 worktree 를 가리키는 editable 1.2.0 이었는데 wk doctor content_drift 는 플러그인 사본만 대조해 in-sync 로 보였다. 평가 §3.2.
- Completion criteria: doctor 에 '플러그인 MCP command 가 해석되는 해석기에서 import 되는 workflow_kit 의 버전·경로' 절 추가; 설치본과 버전이 다르면 발견; 되주입(옛 버전 설치 / editable 경로 / 미설치)이 각각 발화

## 🛠️ Implementation / Content

- Progress: mcp_interpreter 절 구현 + 되주입 실증, 커밋 전 검사 green
- Next session starting point:
- Remaining risks: MCP 호스트 환경은 doctor 셸의 PATH 로 근사 — GUI 로 뜬 하네스가 다른 PATH 를 쓰면 해석 결과가 다를 수 있다(measurement_note 로 명시)

## ✅ Outcome

- Result: deploy_doctor 에 mcp_interpreter 절 신설(8절째). command 를 정본 payload .mcp.json 에서 파생 → PATH 해석 → 그 해석기를 프로젝트 cwd·PYTHONPATH 제거로 띄워 workflow_kit 출처·배포본 메타데이터를 받고, 사본 버전(사본 옆 pyproject → 같은 자리 배포본 → 모름)을 활성·읽힘 설치본 버전과 대조. 판정: in_sync / version_mismatch / not_importable / version_unknown / probe_failed(발견) · command_not_found(preflight 몫, 중복 안 함) · no_plugin_installed(측정값은 measured_verdict 로 보존). placement=foreign_path(프로젝트 밖·해석기 설치 루트 밖, editable url 포함)는 버전과 별개로 발견. 구현 중 실측 오판 2건 수리: 개발 모드 PYTHONPATH 가 자식에 새어 저장소 소스로 보고 / homebrew 는 site-packages 가 bin 부모 밑에 없어 정상 wheel 을 foreign 으로 — 설치 루트를 자식의 sys.prefix·site.getsitepackages 로. 이 호스트 실측: /opt/homebrew/bin/python3 → site-packages wheel 1.11.0, 설치본 1.11.0 일치(87차의 editable 1.2.0 은 이미 정리됨). 문서: INSTALLATION 8절 + 설명, workflow_deployment_idempotency 8절
- Verification: check_deploy_doctor 43/43 (신규 3 case: 되주입 3종 각각 발화 + 정상 무발화 + 모름=발견 + 설치본 없음 + command 부재 + probe()/텍스트 소비 지점; payload 파생; 실 자식 PYTHONPATH 무시). 탐침 되주입 3종(PYTHONPATH 누수·foreign 판정 제거·버전 비교 제거) 각각 red 확인 후 복원. run_all_checks --changed 271/271
- Follow-up: check_deploy_doctor 의 claude_directory_source 2 case 가 기본 TMPDIR(/var→/private/var) 직접 실행에서만 red — HEAD 에서도 재현, 별도 task 칩으로 분리
