---
id: TASK-2026-09-25-main-002
status: done
created_at: 2026-09-25
source_anchor: generic-task-2026-09-25-main-002
source_path: backlog/2026-09-25.md
kind: generic
wbs: exempt
wbs_exempt_reason: 기존 안전장치 결함 수리 — 마일스톤 밖 유지보수
---

# TASK-2026-09-25-main-002 — force push 차단 hook 이 실제 git push --force 를 막지 못한다

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-09-25
- Owner:
- Host:
- Host IP:
- Affected documents:
  - 

- Description: pre-push-no-force.sh 가 인자에서 --force 를 찾지만 git 은 hook 에 <remote> <url> 두 인자와 stdin ref 줄만 넘긴다 — 실제 force push 에 무력. 검사 case 6 은 스크립트에 --force 를 직접 넘겨 인터페이스를 재지 않는다 (main-009 중 발견)
- Completion criteria:

## 🛠️ Implementation / Content

- Progress: 재현 → 판정 재작성 → 실제 push 검사 + 되주입, 커밋 전 검사 green
- Next session starting point:
- Remaining risks:

## ✅ Outcome

- Result: 재현: 임시 저장소+bare remote 에 hook 설치 후 non-fast-forward git push --force 가 '(forced update)' 로 통과 — git 은 hook 에 <remote> <url> 두 인자와 stdin ref 줄만 넘기고 push 옵션은 안 넘긴다. pre-push-no-force.sh 재작성: stdin ref 마다 remote sha≠0 이고 local sha≠0 인데 merge-base --is-ancestor 실패면 거부, 원격 sha 를 로컬이 모르면(cat-file 실패) 거부(모름≠통과), 새 ref·삭제·ff 인 --force 는 통과, 우회 안내는 --no-verify. check_pre_push_hook case 6 교체: bare remote 에 실제 git push 7 시나리오(new_ref·ff·force_nonff 거부+원격 불변·no_verify·force_ff·unknown_remote 거부·delete) + 준비 단계 성공 단언 + 조상 검사를 뺀 변형 hook 으로 같은 시나리오 되주입(결과가 갈려야 green). push 에 -c core.hooksPath 고정(사용자 전역 hooksPath 무력화 방지). 문서: multi_workspace_orchestration §Layer 2 거부 대상·인터페이스·hooksPath 연결
- Verification: check_pre_push_hook 8/8. 외부 되주입: hook 을 HEAD(옛 인자 스캔)로 되돌리면 case 6 red 4건. e2e: core.hooksPath=.githooks + 설치 hook + 게이트 기록 있는 커밋의 force non-ff → .githooks 게이트 통과 후 연결된 force 차단이 거부, 원격 불변. --changed 272/272
- Follow-up: 삭제 push 는 여전히 통과 — 원격 브랜치 삭제도 되돌리기 어려운 작업이지만 이 hook 의 범위(force) 밖으로 두었다. 막을지는 소유자 판단
