# Session — 2026-07-22 / v1.0.0 close-out + 지표 재정의

- 문서 목적: 특정 세션의 단기 메모리 (영구 보존은 wiki/topics/ 와 함께).
- 날짜: 2026-07-22
- 주제: generic (release close-out + post-release hardening)
- 상태: stable

## 📋 Session Summary

v1.0.0-beta 발행 이후의 보강 사이클. Panel 1 의 두 지표를 재정의하고, 그 과정에서
`state.json` 이 2026-07-21 이후 갱신되지 않고 있던 silent failing 을 찾아 고쳤다.

## 🛠️ Detail

**시작 상태**: `main = 43079c0`, 워킹트리 clean, 릴리스 발행 완료, smoke 203/203.
진행 중 작업 없음 — 직전 메모리의 "미완 3건"(session-start 자동 아카이브 wiring /
governance·README layout 반영 / branch-scoped smoke)은 확인 결과 모두 착지돼 있었다.

**1. north-star / maturity_stale 재정의** (`dbc4ec1`, 노트 §2.19)

- `maturity_stale = (last_updated != 오늘)` 은 매일 스탬프하지 않는 한 영구 red.
  → maturity surface(`skills/` · `mcp_servers/` · `harnesses/` · matrix)를 마지막으로
  바꾼 commit > 선언 이면 stale 로 재정의. 근거가 없으면 `source: unknown` 으로
  stale 로 단정하지 않는다.
- north-star 는 원장 `ai-workflow/memory/release/drift_ledger.jsonl` 에서만.
  drift 가 없던 cycle 도 기록한다 (분모). 원장이 비면 `0` 이 아니라 `미측정`.
- 재정의 직후 진짜 drift 1건 검출 — `43079c0` 이 surface 를 바꾸고 선언은 안 올렸다.

**2. state.json 이 갱신되지 않고 있었다** (`ce81fa7`, 노트 §2.20)

"v1.0.0 사이클이 메모리에 미기록" 을 파고들자 사람이 아니라 코드 문제였다.
v1.0.0 branch-scoped 전환에서 hint 는 `active/<branch>/state.json` 으로 옮기고
writer 는 `active/state.json` 에 남아, refresh 가 아무도 읽지 않는 파일을 만들면서
`"refreshed"` 를 보고하고 있었다. 함께 `backlog-update` 가 `--apply` 없이도 write
하던 dry-run 오염도 드러났다 — **스킬을 직접 돌려보다** 발견했다.

**3. 세 겹의 구조적 방어** (`d285077` → `0116661`, 노트 §2.21~§2.25)

위 두 건과 §2.19 를 나란히 놓자 **하나의 결함**으로 드러났다 — *같은 사실이 두 곳에
있는데 둘을 이어주는 기계적 장치가 없다.* 부품별 테스트는 전부 green 인데 조립하면
안 맞는 상태였다. 그래서 세 겹을 세웠다:

| 겹 | 검사 | 무엇을 잡나 |
|---|---|---|
| §2.22 | `check_writer_reader_roundtrip.py` (8 pair) | 조립이 맞는가 |
| §2.23 | `check_metric_source_contract.py` (5 case) | 지표가 근거를 말하는가 |
| §2.24 | `check_convention_single_source.py` (4 case) | 애초에 사본이 생기지 못하게 |

각 겹을 세울 때마다 **기존 결함이 하나씩 더 나왔다** — §2.21 backlog-update 가
governance layout 을 못 만들던 것, §2.23 도입 중 Panel 6 north-star 도 git 실패를
삼키고 0 을 pass 로 보고하던 것, §2.24 도입 즉시 사본 7건(4 skill 이 `purpose_digest`
없이 동작 / release validate 게이트 조용히 통과 / sync_release_hash 가 안 읽히는
파일에 기록).

**4. 같은 부류 전수 조사** (§2.25) — "하나 고쳤다고 그 부류가 끝난 게 아니다" 를
절차로 옮겼다. AST 로 문자열 상수 빈도를 내니 `ai-workflow`(28) > `memory`(21) >
`active`(19) ≫ `state.json`(12) 로, 앞서 고친 것이 **더 넓은 문제의 한 조각**이었다.
`memory_dir_for_workspace` / `memory_active_dir` 을 정본으로 만들고 14개 모듈의
조립을 교체했다.

## ✅ Outcome

- 신규 smoke 5종 (`check_north_star_metric` / `check_state_cache_branch_scoped_write` /
  `check_backlog_update_layout` / `check_writer_reader_roundtrip` /
  `check_metric_source_contract` / `check_convention_single_source`).
  **전부 결함을 되돌려 주입해 실제로 FAIL 하는 것까지 확인**했다.
- 전량 smoke 203 → **209/209 PASS**, mypy strict 117 files 0 errors, 저장소 오염 0.
- 세션 커밋 7건: `dbc4ec1` `ce81fa7` `3f017a2` `d285077` `02161a3` `d5be282`
  `fe6fcfa` `0116661`. 릴리스 노트 §2.19~§2.25 로 기록.
- 각 방어의 **한계를 과장하지 않고 명시**했다 (§2.23 은 근거 이름을 그럴듯하게 지으면
  통과, §2.24 는 판정이 파일 단위).

## ⏭️ Next Actions

- [x] `backlog-update` skill layout 정합 — `d285077` 로 close (§2.21).
- [ ] **다음 릴리스에서 `drift_ledger.jsonl` 첫 entry 확인** — north-star 가
      `미측정` → 실측으로 전환되는 시점. 지금 0 은 "재보지 않았다"는 뜻이다.
- [ ] 릴리스 노트가 §2.25 까지 자랐다. 다음 릴리스 진입 시 **§2.15~§2.25 를
      별도 노트로 분리할지** 판단할 것 (tag 는 `2e6ae44` 에 고정, main 의 노트만 자란다).
- [ ] 파일명 상수 계열(`work_backlog.md` 10파일 등)은 다섯 번째 규약 후보로 보류.

## ⚠️ Risks & Blockers

- 릴리스 직후에는 tag 존재 여부로 갈리는 코드 경로가 있어 **릴리스 전 green 이
  릴리스 후 green 을 보장하지 않는다**. 발행 후 전량 smoke + `git status` 재확인 필수.
