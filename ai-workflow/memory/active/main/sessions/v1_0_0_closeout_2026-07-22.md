# Session — 2026-07-22 / v1.0.0 close-out + 지표 재정의

- 문서 목적: 특정 세션의 단기 메모리 (영구 보존은 wiki/topics/ 와 함께).
- 날짜: 2026-07-22
- 주제: generic (release close-out + post-release hardening)
- 상태: stable

## 📋 Session Summary

v1.0.0-beta 발행 이후의 보강 사이클. Panel 1 의 두 지표를 재정의하고, 그 과정에서
`state.json` 이 2026-07-21 이후 갱신되지 않고 있던 silent failing 을 찾아 고쳤다.

## 🛠️ Detail

**시작 상태**: `main = 43079c0`, 워킹트리 clean, 릴리스 발행 완료, smoke 203/203.
진행 중 작업 없음 — 직전 메모리의 "미완 3건"(session-start 자동 아카이브 wiring /
governance·README layout 반영 / branch-scoped smoke)은 확인 결과 모두 착지돼 있었다.

**1. north-star / maturity_stale 재정의** (`dbc4ec1`, 노트 §2.19)

- `maturity_stale = (last_updated != 오늘)` 은 매일 스탬프하지 않는 한 영구 red.
  → maturity surface(`skills/` · `mcp_servers/` · `harnesses/` · matrix)를 마지막으로
  바꾼 commit > 선언 이면 stale 로 재정의. 근거가 없으면 `source: unknown` 으로
  stale 로 단정하지 않는다.
- north-star 는 원장 `ai-workflow/memory/release/drift_ledger.jsonl` 에서만.
  drift 가 없던 cycle 도 기록한다 (분모). 원장이 비면 `0` 이 아니라 `미측정`.
- 재정의 직후 진짜 drift 1건 검출 — `43079c0` 이 surface 를 바꾸고 선언은 안 올렸다.

**2. state.json 이 갱신되지 않고 있었다** (`ce81fa7`, 노트 §2.20)

"v1.0.0 사이클이 메모리에 미기록" 을 파고들자 사람이 아니라 코드 문제였다.
v1.0.0 branch-scoped 전환에서 hint 는 `active/<branch>/state.json` 으로 옮기고
writer 는 `active/state.json` 에 남아, refresh 가 아무도 읽지 않는 파일을 만들면서
`"refreshed"` 를 보고하고 있었다. 함께 `backlog-update` 가 `--apply` 없이도 write
하던 dry-run 오염도 드러났다 — **스킬을 직접 돌려보다** 발견했다.

## ✅ Outcome

- 신규 smoke 2종 (`check_north_star_metric` 6 case, `check_state_cache_branch_scoped_write`
  4 case). 둘 다 **버그 코드를 되돌려 주입해 실제로 FAIL 하는 것까지 확인**.
- 전량 smoke 203 → **205/205 PASS**, mypy strict 117 files 0 errors, 저장소 오염 0.
- 본 세션의 메모리(daily index / task SSOT / 본 파일)를 layout 에 맞춰 복원.

## ⏭️ Next Actions

- [ ] `backlog-update` skill 의 출력 layout 을 v0.14.0+ append-only 로 정합
      (TASK-2026-07-22-002 §후속 작업 참조) — stable 선언과 실제가 어긋난 상태.
- [ ] 다음 릴리스에서 `drift_ledger.jsonl` 첫 entry 가 정상 기록되는지 확인
      (north-star 가 `미측정` → 실측으로 전환되는 시점).

## ⚠️ Risks & Blockers

- 릴리스 직후에는 tag 존재 여부로 갈리는 코드 경로가 있어 **릴리스 전 green 이
  릴리스 후 green 을 보장하지 않는다**. 발행 후 전량 smoke + `git status` 재확인 필수.
