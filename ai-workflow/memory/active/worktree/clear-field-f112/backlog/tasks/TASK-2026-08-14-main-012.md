---
id: TASK-2026-08-14-main-012
status: done
created_at: 2026-08-14
source_anchor: generic-task-2026-08-14-main-012
source_path: backlog/2026-08-14.md
kind: generic
---

# TASK-2026-08-14-main-012 — pi.dev plugin 호환성 보강 — 마켓플레이스 정식 등록

## 📝 Description

- 상태: done
- 우선순위: medium
- 요청일: 2026-08-14
- 담당: AI Agent
- 호스트명:
- 호스트 IP:
- 영향 문서:
  - `plugin/package.json`
  - `plugin/skills/session-start/SKILL.md`
  - `plugin/skills/backlog-update/SKILL.md`
  - `plugin/skills/doc-sync/SKILL.md`
  - `plugin/skills/session-end/SKILL.md`
  - `workflow-source/tests/check_agent_plugin_payload.py`
  - `workflow-source/workflow_kit/plugin_payload.py`
  - `plugin/.pi-pkg/mcp-settings-snippet.json`
  - `docs/PROJECT_PROFILE.md`

- 작업 내용: pi.dev 갤러리에 정식 등록하기 위한 plugin 자산(package.json + pi manifest + 영문 description + MCP 가이드) 보강
- 완료 기준: plugin/package.json 에 pi manifest + pi-package keyword 가 들어간다
- 완료 기준: 4개 SKILL.md description 에 영문 표기가 추가된다 (한글 유지)
- 완료 기준: pi 용 MCP 등록 스니펫이 동봉된다
- 완료 기준: check_pi_plugin_payload 회귀가 신설된다
- 완료 기준: 전량 검사 2축 green (push 게이트)

## 🛠️ Implementation / Content

- 진행 현황: 코드 자족 완료. 인접 검사 5종 green. **task ID 리네임**: 본 작업은 시작 시 main-011 로 등록했으나, 병렬 세션이 main 브랜치에 같은 ID 로 다른 스코프(CHECK_TIMEOUT_S 미선언 3건)를 커밋함 — 충돌 방지를 위해 main-012 로 리네임.
- 다음 세션 시작 포인트: git commit + push + pi.dev 갤러리 PR 제출 (`pi.dev/packages` 등록, `video`/`image` 자산은 후속) — 본 작업은 코드 자족 완료, 외부 등록만 남음.
- 남은 리스크: ① pi 시스템 프롬프트는 영어라 skill description 영문화 매칭 강화는 했지만, **`plugins: ['!extensions/legacy.ts']` 필터 등 pi.dev 추가 manifest 필드** 가 갤러리 측에서 요구될 수 있다 (실기 검증 미완, 갤러리 PR 시 spec 확정 필요). ② case 19 의 pi-spec 검증은 pi docs 를 기준으로 작성했으나 `pi install ./plugin` 실기 미실행 (워크트리 환경에 pi CLI 부재) — main 브랜치 머지 후 사용자 환경에서 smoke 권장.
- 정합 보강: `diff_repo_plugin_files` 에 pi 정적 자산 제외 로직 추가 (case 1 의 byte 대조가 pi 분배 자산에서 위양성 안 내도록). 기존 케이스 18종 회귀 0.

## ✅ Outcome

- 작업 결과: pi 분배 채널(11번째) 신설 — `plugin/package.json` (pi-package keyword + `pi.skills: ["./skills"]`) · 4개 SKILL.md description 이 [KO]+[EN] 이중언어로 통일 · `plugin/.pi-pkg/mcp-settings-snippet.json` 동봉 (read-only alias 만) · `tests/check_agent_plugin_payload.py` case 19 (pi.dev 어댑터 검증) · `plugin_payload.py` 다중행 description 블록 스칼라 처리 + pi 정적 자산 byte-diff 제외.
- 작업 결과: 정본 writer (`plugin_payload.py::PLUGIN_SKILLS`) 가 새 description 을 단일 소스로 만들고, `write_agent_plugin()` 이 SKILL.md 를 재생성해 `case 1 (byte-exact)` 가 그대로 통과한다. 손 사본이 갈라질 자리가 없다.
- 작업 결과: pi 의 MCP 는 settings.json 이 표준 경로라 패키지 안에 snippet 으로 동봉 — `pi install` 후 사용자가 `~/.pi/agent/settings.json` 에 붙여 넣는 형태. ADR-003 (read-only opt-in) 준수.
- 검증 결과: `check_agent_plugin_payload` 19/19 PASS · `check_plugin_distribution` PASS · `check_harness_skill_frontmatter` 6/6 PASS · `check_docs` 72 file PASS · `workflow-linter` 0 issue · `check_self_application` 8/8 PASS. 전량 2축의 환경 의존 미설치 항목(mypy/jsonschema/mcp SDK/build) 은 사전 결함이며 본 변경과 무관(stash 검증).
- 후속 작업: pi.dev 갤러리 PR (workflow-source/core/workflow_kit_roadmap.md 의 분배 채널 목록에 11번째 슬롯 추가) · main 브랜치 머지 후 사용자 환경에서 `pi install ./plugin` smoke.
