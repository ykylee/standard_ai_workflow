---
id: TASK-2026-08-14-main-012
status: done
created_at: 2026-08-14
source_anchor: generic-task-2026-08-14-main-012
source_path: backlog/2026-08-14.md
kind: generic
---

# TASK-2026-08-14-main-012 — pi.dev plugin 호환성 보강 — 마켓플레이스 정식 등록

## 📝 Description

- Status: done
- Priority: medium
- Request date: 2026-08-14
- Owner: AI Agent
- Host:
- Host IP:
- Affected documents:
  - `plugin/package.json`
  - `plugin/skills/session-start/SKILL.md`
  - `plugin/skills/backlog-update/SKILL.md`
  - `plugin/skills/doc-sync/SKILL.md`
  - `plugin/skills/session-end/SKILL.md`
  - `workflow-source/tests/check_agent_plugin_payload.py`
  - `workflow-source/workflow_kit/plugin_payload.py`
  - `plugin/.pi-pkg/mcp-settings-snippet.json`
  - `docs/PROJECT_PROFILE.md`

- Description: pi.dev 갤러리에 정식 등록하기 위한 plugin 자산(package.json + pi manifest + 영문 description + MCP 가이드) 보강
- Completion criteria: plugin/package.json 에 pi manifest + pi-package keyword 가 들어간다
- Completion criteria: 4개 SKILL.md description 에 영문 표기가 추가된다 (한글 유지)
- Completion criteria: pi 용 MCP 등록 스니펫이 동봉된다
- Completion criteria: check_pi_plugin_payload 회귀가 신설된다
- Completion criteria: 전량 검사 2축 green (push 게이트)

## 🛠️ Implementation / Content

- Progress: 코드 자족 완료. 인접 검사 5종 green. **task ID 리네임**: 본 작업은 시작 시 main-011 로 등록했으나, 병렬 세션이 main 브랜치에 같은 ID 로 다른 스코프(CHECK_TIMEOUT_S 미선언 3건)를 커밋함 — 충돌 방지를 위해 main-012 로 리네임.
- Next session starting point: pi.dev 갤러리 PR 제출 (`pi.dev/packages` 등록, `video`/`image` 자산은 후속) — 본 작업은 코드 자족 완료, 외부 등록만 남음.
- Remaining risks: ① **pi v0.84.2 는 MCP 미지원** — `plugin/.pi-pkg/mcp-settings-snippet.json` 은 Claude Code 등 외부 클라이언트 또는 향후 pi 버전용. pi 사용자에겐 4종 skill 이 1차 가치. ② pi 시스템 프롬프트는 영어라 skill description 영문화 매칭 강화는 했지만, **pi.dev 추가 manifest 필드** 가 갤러리 측에서 요구될 수 있다 (실기 검증 완료 — 갤러리 PR 시 spec 확정 필요).
- 정합 보강: `diff_repo_plugin_files` 에 pi 정적 자산 제외 로직 추가 (case 1 의 byte 대조가 pi 분배 자산에서 위양성 안 내도록). 기존 케이스 18종 회귀 0.

## ✅ Outcome

- Result: pi 분배 채널(11번째) 신설 — `plugin/package.json` (pi-package keyword + `pi.skills: ["./skills"]`) · 4개 SKILL.md description 이 [KO]+[EN] 이중언어로 통일 · `plugin/.pi-pkg/mcp-settings-snippet.json` 동봉 (read-only alias 만) · `tests/check_agent_plugin_payload.py` case 19 (pi.dev 어댑터 검증) · `plugin_payload.py` 다중행 description 블록 스칼라 처리 + pi 정적 자산 byte-diff 제외.
- Result: 정본 writer (`plugin_payload.py::PLUGIN_SKILLS`) 가 새 description 을 단일 소스로 만들고, `write_agent_plugin()` 이 SKILL.md 를 재생성해 `case 1 (byte-exact)` 가 그대로 통과한다. 손 사본이 갈라질 자리가 없다.
- 작업 결과 (실기 검증 추가): `pi install ./plugin` 로컬 설치 + 4종 skill (`/skill:session-start` · `/skill:backlog-update` · `/skill:doc-sync` · `/skill:session-end`) 전부 로드 및 실행 정상. PR 후속 사용자 요청으로 MCP 등록 시도 — **중요 발견**: pi v0.84.2 는 MCP 서버를 기본 지원하지 않음 (\"No MCP\" 설계 철학, https://mariozechner.at/posts/2025-11-02-what-if-you-dont-need-mcp/). settings.json 의 mcpServers 항목은 pi 가 무시한다. 동봉된 `plugin/.pi-pkg/mcp-settings-snippet.json` 은 **Claude Code 등 MCP 호환 클라이언트** 또는 **향후 pi 버전**을 위한 문서다 (코드 재실행으로 직접 JSON-RPC 통신은 정상 확인). pi 사용자 입장에서의 1차 가치는 4종 skill.
- Verification: `check_agent_plugin_payload` 19/19 PASS · `check_plugin_distribution` PASS · `check_harness_skill_frontmatter` 6/6 PASS · `check_docs` 72 file PASS · `workflow-linter` 0 issue · `check_self_application` 8/8 PASS · `pi -p "/skill:..."` 4/4 정상 실행 · MCP 서버 JSON-RPC 직접 호출 정상 (initialize 응답 + tools/list 11 도구).
- Follow-up: pi.dev 갤러리 PR (workflow-source/core/workflow_kit_roadmap.md 의 분배 채널 목록에 11번째 슬롯 추가) · main 브랜치 머지 후 사용자 환경에서 `pi install git:...@<tag>` 정식 install + smoke.
