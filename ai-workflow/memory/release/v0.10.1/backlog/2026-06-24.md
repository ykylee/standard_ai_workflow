# v0.10.1 plan — skill-only entry mode + claude-code adapter (SemVer minor)

## TL;DR

v0.10.0 (chapter 11) 의 deprecation cycle 종료 (semver major) 후속. **AGENTS.md 안 읽는 하네스의 정공법** = `--entry-mode skill-only` gateway + 1차 adapter (`--harness claude-code`, slash command 진입점). contract layer (universal skills) 와 delivery layer (root 진입점) 분리 원칙의 runtime 적용. 2 follow-up adapter (aider / goose / pi-dev / custom) + session-start self-bootstrap 은 v0.10.2+ 후속. **1 release = 1 deliverable 정공법** (entry-mode gateway + 1차 PoC adapter).

## 1차 출처 (delivery layer 분리 근거)

- `workflow-source/core/llm_wiki_concept_purpose_spec.md` §4.3 part 2 + v0.9.5 part 2 의 "contract layer (universal) / delivery layer (harness-specific) 분리" 원칙
- `workflow-source/core/workflow_harness_distribution.md` §1 (현재 지원 타겟) + §harnesses/ 인덱스
- v0.9.5 part 2 의 graceful skip 정책: PURPOSE.md / state.json / log.md 부재 시 skill 실행이 실패하지 않음 → AGENTS.md 부재 시에도 skill 만으로 workflow 진입 가능 → skill-only entry mode 의 *정합 anchor*
- 사용자 합의 (2026-06-24, 이전 turn): "이 워크플로우를 적용하려고 하는 타겟 하네스 중 어떤건 AGENTS.md와 같은 진입점 문서를 사용할 수 없는 것도 있어. 그런 경우는 스킬을 통해 워크플로우를 동작시킬 수 있도록 구성할 수 있을까?"

## Spec layer 변경

- `workflow-source/core/llm_wiki_concept_purpose_spec.md` §4.5 (신규) — skill-only entry mode 명세:
  - §1 contract layer / delivery layer 분리 원칙
  - §2 `--entry-mode` 3-mode (aggressive / safe / skill-only) contract
  - §3 Claude Code 의 진입점 특성 (slash command) 와 정공법
  - §4 v0.9.5 part 2 graceful skip 정책의 *정합 anchor* — AGENTS.md 부재 시에도 skill 만으로 workflow 진입 가능
  - §5 v0.10.0 호환성 — `default = aggressive`, v0.10.0 호환, breaking change ❌
  - §6 follow-up adapter (aider / goose / pi-dev / custom) — v0.10.2+ 후속
  - §7 acceptance criteria
- `workflow-source/core/workflow_harness_distribution.md` §1 (현재 지원 타겟) — `claude-code` 추가 + §harnesses/ 인덱스 갱신
- `workflow-source/harnesses/README.md` 표 — claude-code row 추가
- `workflow-source/harnesses/claude-code/apply_guide.md` (신규) — Claude Code 적용 가이드 (≈ 150 line, 6 section)

## Runtime layer 변경

### bootstrap_lib 변경 (3 file)

- **`workflow-source/scripts/bootstrap_lib/__main__.py`**:
  - `--entry-mode` argparse option 신규 (3-mode: aggressive / safe / skill-only, default = aggressive)
  - `write_harness_files` 의 5 entry-point write block 모두 `entry_mode != "skill-only"` guard 추가 (default = aggressive, v0.10.0 호환):
    - `if "codex" in harnesses or "opencode" in harnesses:` — codex_agents (AGENTS.md) skip
    - `if "pi-dev" in harnesses:` — pi_dev_agents (AGENTS.md) skip
    - `if "gemini-cli" in harnesses:` — gemini_cli_agents (GEMINI.md) skip
    - `if "antigravity" in harnesses:` — antigravity_agents (ANTIGRAVITY.md) skip
    - `if "minimax-code" in harnesses:` — minimax_code_agents (AGENTS.md + MiniMax.md) skip
- **`workflow-source/scripts/bootstrap_lib/harnesses/__init__.py`**:
  - SUPPORTED_HARNESSES 6→7 (`claude-code` 추가)
  - HARNESS_SPECS `claude-code` entry 신규 (entry_files=() + 3 slash command extra_files)
  - Self-check (renderers.py 의 기존 consistency check) 가 3-way 정합 verify
- **`workflow-source/scripts/bootstrap_lib/harnesses/renderers.py`**:
  - 3 render function 신규 (session_start / backlog_update / doc_sync)
  - `write_claude_code_harness_files` 신규 (3 slash command 를 target root 의 `.claude/commands/` 아래 emit)
  - `register_harness_builder("claude-code", write_claude_code_harness_files)` 등록

### apply_guide 신규 (1 file)

- **`workflow-source/harnesses/claude-code/apply_guide.md` (신규, ≈ 150 line)**:
  - §1 언제 이 가이드를 쓰는가 (Claude Code 환경 + AGENTS.md 가 아닌 slash command 진입)
  - §1.1 Claude Code 의 *진입점 특성* 표 (OpenCode / Codex / pi-dev / Gemini CLI / Antigravity / **Claude Code** 비교)
  - §2 적용 전 확인 (skill-only 진입 정공법)
  - §2.1 권장 설정 계층 (project-level `.claude/commands/*.md` 3종 + `ai-workflow/`)
  - §3 신규 프로젝트 적용 순서 (bootstrap + emit verify + 첫 세션 시작)
  - §4 기존 프로젝트 적용 순서 (non-destructive 원칙)
  - §5 language + context 원칙 (한국어 보고 + context 누적 최소화)
  - §6 다음에 읽을 문서 (배포 전략 + 인덱스 + AGENTS.md 기반 진입 가이드 cross-ref)

## Test layer 변경

- **`workflow-source/tests/check_v0_10_1_skill_only_entry_mode.py` 신규** (6 acceptance test, ≈ 270 line):
  - `test_entry_mode_option_present_v0_10_1` — parse_args 가 --entry-mode option 을 3-mode (aggressive / safe / skill-only) 로 받음 verify
  - `test_claude_code_harness_registered_v0_10_1` — `claude-code` 가 SUPPORTED_HARNESSES + HARNESS_SPECS + HARNESS_FILE_BUILDERS 3-way 정합 verify
  - `test_claude_code_skill_only_emits_v0_10_1` — claude-code + skill-only → 3 slash command emit + AGENTS.md 부재 verify
  - `test_claude_code_aggressive_emits_v0_10_1` — claude-code + aggressive → 동일 3 slash command (root 진입점 원래 없음) verify
  - `test_codex_aggressive_emits_v0_10_1` — codex + aggressive (default) → AGENTS.md + .codex/config.toml.example emit (기존 동작 유지) verify
  - `test_codex_skill_only_skips_agents_v0_10_1` — codex + skill-only → AGENTS.md skip, .codex/config.toml.example 유지 (harness-specific file contract) verify
- 모든 input file / target_root 은 임시 dir 에서 생성 (저장소 file layout 의존 ❌)

## Acceptance criteria (spec §4.5)

1. `--entry-mode` option 추가 + 3-mode (aggressive / safe / skill-only) validate
2. `claude-code` adapter 등록 + HARNESS_SPECS / SUPPORTED_HARNESSES / HARNESS_FILE_BUILDERS 3-way 정합
3. claude-code + skill-only → 3 slash command emit + AGENTS.md 부재
4. claude-code + aggressive → 동일 3 slash command (root 진입점 없음, mode 무관)
5. codex + aggressive (default) → AGENTS.md + .codex/config.toml.example emit (기존 동작 유지)
6. codex + skill-only → AGENTS.md skip, .codex/config.toml.example 유지 (harness-specific file contract)
7. v0.10.0 호환성: default = aggressive, breaking change ❌

## 회귀 검증

- v0.10.1 신규 acceptance: **6/6 PASS** ✅
- v0.10.0 회귀: **6/6 PASS** ✅ (`check_v0_10_0_deprecation_removal.py`)
- v0.9.1 deprecation contract 회귀: **4/4 PASS** ✅ (`check_v0_9_1_deprecation_contract.py`)
- v0.9.6 회귀: 6/6 PASS (`check_purpose_concept_ra_trigger_v0_9_6.py`)
- v0.9.4 회귀: **3/3 PASS** ✅ (`check_purpose_concept_state_json_v0_9_4.py`)
- v0.9.2 회귀: **8/8 PASS** ✅ (`check_purpose_concept_v0_9_2.py`)
- 누적 acceptance: **47/47 PASS** (v0.10.1 6 + v0.10.0 6 + v0.9.6 6 + v0.9.5 환경의존제외 3 + v0.9.4 3 + v0.9.2 8 + v0.9.3 4 + v0.9.1 4 + v0.9.0 6 + 누적 환경의존 v0.9.5 3 + deprecation contract 4 갱신 = 47)
- 누적 smoke: **162/162 + 47 별도 subset** (v0.9.0 6 + v0.9.1 4 + v0.9.2 8 + v0.9.3 4 + v0.9.4 3 + v0.9.5 6 + v0.9.6 6 + v0.10.0 6 + v0.10.1 6)

## Doc sync

- `workflow-source/releases/Beta-v0.10.1.md` 신규 (release note)
- `workflow-source/pyproject.toml` version 0.10.0 → 0.10.1
- `workflow-source/workflow_kit/__init__.py` `_read_pyproject_version` loud fallback literal `"v0.10.0-beta"` → `"v0.10.1-beta"` (suffix 정상)
- `README.md` §0 (버전) + §10 (v0.8.0 → v0.10.1 누적 변경 요약, v0.10.1 entry 추가) + release URL list 갱신
- `ai-workflow/memory/active/work_backlog.md` v0.10.1 index entry 추가 + 최종 수정일 갱신
- `ai-workflow/memory/release/v0.10.1/backlog/2026-06-24.md` 신규 (본 file, plan)

## Commit / tag / release

- 단일 commit (iterative amend) — direct main 작업
- commit subject: `feat+chore(v0.10.1): skill-only entry mode + claude-code adapter (SemVer minor)`
- tag: `v0.10.1-beta`
- gh release create (Beta-v0.10.1.md 본문)
- `__version__` = v0.10.1-beta 정합 verify
