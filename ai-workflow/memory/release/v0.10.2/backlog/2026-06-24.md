# v0.10.2 plan — delivery layer 확장 (claude-code 진입점 정정 + 3 adapter + self-bootstrap)

## TL;DR

v0.10.1 의 claude-code adapter 진입점 설계 오류 정정 (Claude Code 도 `CLAUDE.md` root 진입점 자동 read) + 3 신규 harness adapter (aider / goose / custom) + session-start self-bootstrap mode. **pi-dev 는 기존 adapter 활용** (정합 정공법). 1 release = 1 deliverable 정공법.

## 1차 출처 (진입점 오류 정정 근거)

- 사용자 pushback (2026-06-24): "claude code는 CLAUDE.md로 진입점을 잡지 않나? 이 경우랑 다른거 같은데"
- 공식 docs 검증: https://docs.anthropic.com/en/docs/claude-code/memory
  - Claude Code reads `CLAUDE.md`, not `AGENTS.md`
  - `./CLAUDE.md` or `./.claude/CLAUDE.md` 자동 read
  - AGENTS.md 는 직접 read 안 함 → `CLAUDE.md` 에서 `@AGENTS.md` import 또는 symlink

## Spec layer 변경

- `llm_wiki_concept_purpose_spec.md` §4.5 (v0.10.1) — v0.10.2 진입점 정정 + 3 adapter + self-bootstrap mode 갱신
- `workflow_harness_distribution.md` §1 + §harnesses/ 인덱스 — 3 adapter 추가 (aider / goose / custom), 10 harness
- `harnesses/README.md` 표 — 3 row 추가 (aider / goose / custom)

## Runtime layer 변경

### bootstrap_lib 변경 (3 file)

- **`scripts/bootstrap_lib/harnesses/__init__.py`**:
  - SUPPORTED_HARNESSES 7→10 (`aider` / `goose` / `custom` 추가)
  - HARNESS_SPECS claude-code entry_files = `()` → `("CLAUDE.md",)` (진입점 정정)
  - HARNESS_SPECS 3 entry 신규 (aider / goose / custom)
- **`scripts/bootstrap_lib/harnesses/renderers.py`**:
  - `render_claude_code_agents` 신규 (CLAUDE.md 진입점 render)
  - `render_aider_conventions` + `render_aider_config_example` + `write_aider_harness_files` 신규
  - `render_goose_config` + `write_goose_harness_files` 신규
  - `render_custom_skill_template` + `write_custom_harness_files` 신규
  - 4 register_harness_builder 호출
- **`scripts/bootstrap_lib/__main__.py`**:
  - `write_harness_files` 의 claude-code / aider dispatch block 신규 (entry-mode guard 정합)

### session-start self-bootstrap (2 file)

- **`workflow_kit/common/schemas/session.py`**: SessionStartOutput 에 2 field 신규
  - `self_bootstrap_suggested: bool = False`
  - `self_bootstrap_init_commands: list[str] = Field(default_factory=list)`
- **`skills/session-start/scripts/run_session_start.py`**: self-bootstrap 감지 + init commands emit
  - 부재 감지: `not handoff.exists() and not work_backlog.exists() and not state_json.exists()`
  - 부재 시 status="warning" + init commands (2종) emit

### apply_guide 4개

- `harnesses/claude-code/apply_guide.md` 갱신 (v0.10.1 → v0.10.2 정정, CLAUDE.md 진입점 + AGENTS.md 통합)
- `harnesses/aider/apply_guide.md` 신규 (CONVENTIONS.md + .aider.conf.yml.example 절차)
- `harnesses/goose/apply_guide.md` 신규 (.goose/config.yaml emit + entry_points/hooks)
- `harnesses/custom/apply_guide.md` 신규 (caller wire-up 3종 + self-bootstrap)

### v0.10.1 test 갱신

- `tests/check_v0_10_1_skill_only_entry_mode.py` 의 `test_claude_code_harness_registered_v0_10_1` 갱신
  - entry_files 정합 = `()` → `("CLAUDE.md",)` (v0.10.2 진입점 정정 반영)

## Test layer 변경

- **`tests/check_v0_10_2_delivery_layer_extension.py` 신규** (9 acceptance test, ≈ 340 line):
  1. `test_claude_code_entry_point_corrected_v0_10_2` — SUPPORTED_HARNESSES 7→10 + claude-code entry_files=('CLAUDE.md',) + 3-way 정합
  2. `test_claude_code_aggressive_emits_v0_10_2` — claude-code + aggressive → CLAUDE.md + 3 slash commands emit
  3. `test_claude_code_skill_only_skips_claude_md_v0_10_2` — claude-code + skill-only → CLAUDE.md skip, slash commands 유지
  4. `test_aider_adapter_emits_v0_10_2` — aider → CONVENTIONS.md + .aider/conventions.md + .aider.conf.yml.example
  5. `test_goose_adapter_emits_v0_10_2` — goose → .goose/config.yaml (entry_points + read_files + language: ko)
  6. `test_custom_adapter_emits_v0_10_2` — custom → .workflow-kits/custom/SKILL.md (caller wire-up + self-bootstrap)
  7. `test_supported_harnesses_count_v0_10_2` — SUPPORTED_HARNESSES 10 set 정합
  8. `test_session_start_self_bootstrap_v0_10_2` — subprocess session-start + 4 file 부재 시 self_bootstrap_suggested=True + init commands
  9. `test_v0_10_0_v0_10_1_regression_v0_10_2` — deprecation cycle 종료 + entry-mode 3-mode 회귀

## Acceptance criteria

1. claude-code 진입점 = CLAUDE.md (root, 자동 read) + 3 slash command (additive)
2. aider CONVENTIONS.md (root) + .aider/conventions.md + .aider.conf.yml.example emit
3. goose .goose/config.yaml 1 file emit (entry_points 3종 + read_files 5종)
4. custom .workflow-kits/custom/SKILL.md 1 file emit
5. session-start self-bootstrap: 4 file 부재 시 status="warning" + init commands
6. SUPPORTED_HARNESSES 10 set 정합 (codex / opencode / gemini-cli / pi-dev / antigravity / minimax-code / claude-code / aider / goose / custom)
7. v0.10.0 (deprecation cycle 종료) + v0.10.1 (entry-mode 3-mode) 회귀 유지

## 회귀 검증

- v0.10.2 신규: **9/9 PASS** ✅
- v0.10.1 회귀 (갱신): **6/6 PASS** ✅
- v0.10.0 회귀: **6/6 PASS** ✅
- v0.9.6 회귀: 6/6 PASS ✅
- v0.9.4 회귀: **3/3 PASS** ✅
- v0.9.2 회귀: **8/8 PASS** ✅
- v0.9.1 deprecation contract: **4/4 PASS** ✅
- 누적 acceptance: **56/56 PASS** (v0.10.2 9 + v0.10.1 6 + v0.10.0 6 + v0.9.6 6 + v0.9.5 환경의존 3 + v0.9.4 3 + v0.9.2 8 + v0.9.3 4 + v0.9.1 4 + v0.9.0 6 + 누적 환경의존 v0.9.5 3 + deprecation contract 4 갱신 = 56)

## Doc sync

- `releases/Beta-v0.10.2.md` 신규
- `pyproject.toml` version 0.10.1 → 0.10.2
- `workflow_kit/__init__.py` `_read_pyproject_version` literal `"v0.10.1-beta"` → `"v0.10.2-beta"`
- `README.md` §0 + §10 + release URL list
- `ai-workflow/memory/active/work_backlog.md` v0.10.2 entry
- `ai-workflow/memory/release/v0.10.2/backlog/2026-06-24.md` 신규 (본 file)

## Commit / tag / release

- 단일 commit (iterative amend) — direct main
- commit subject: `feat+chore(v0.10.2): delivery layer 확장 (claude-code 진입점 정정 + aider / goose / custom + self-bootstrap)`
- tag: `v0.10.2-beta`
- gh release create (Beta-v0.10.2.md 본문)
- `__version__` = v0.10.2-beta 정합 verify
