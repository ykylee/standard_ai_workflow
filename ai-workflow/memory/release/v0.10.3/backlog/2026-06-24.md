# v0.10.3 plan — Wiki file deletion cascade cleanup (R-A follow-up cycle 2)

## TL;DR

v0.9.2 cycle 1 (외부 reference concept 흡수) 의 후속. `wiki-event-sync.py` 의 3-method matching 을 `workflow_kit.common.wiki_cascade` 로 흡수 (delete 방향). file deletion cascade cleanup 정공법: source file 삭제 시 wiki page cascade-delete 대상 식별 + destructive subcommand 정공법 (apply=False default dry-run).

## 1차 출처

- v0.9.2 chapter 6 spec — 외부 reference concept 흡수 정공법 1차 적용 (Karpathy + llm_wiki 의 wiki 운영 R-1~R9 cycle)
- v0.9.2 spec §5 follow-up (line 134-135): "wiki 운영 R-A (Purpose Refresh) trigger 가 wiki-event-sync 의 release event 와 hook — follow-up (R-A)"
- v0.9.6 part 3 — wiki-event-sync R-A trigger (release event hook)
- v0.9.6 release note line 113-114: "v0.10.3 follow-up — external reference 흡수 cycle 2: file deletion cascade cleanup (3-method matching)"
- `~/wiki/skills/wiki-event-sync/scripts/wiki-event-sync.py` — 3-method matching SSOT (edit 방향, line 82-128)

## Spec layer 변경

- `core/llm_wiki_concept_purpose_spec.md` §4.4 (R-A trigger) — R-A follow-up cycle 2 (file deletion cascade cleanup) 7 detail 명시
- `core/llm_wiki_concept_purpose_spec.md` §6 cross-reference — `workflow_kit/common/wiki_cascade.py` + `cmd_cascade_delete` + test file cross-ref
- `core/llm_wiki_concept_purpose_spec.md` §5 follow-up checklist — R-A cycle 2 ✅ (이미 v0.10.0 spec 에서 언급, v0.10.3 에서 closed)

## Runtime layer 변경

- **`workflow_kit/common/wiki_cascade.py` 신규** (≈ 230 line):
  - `file_to_stem(path: str) -> str` — kebab-case + lower SSOT
  - `find_cascade_targets(deleted_path, wiki_root, project)` — 3-method matching (basename + stem + project-relative)
  - `emit_cascade_plan(deleted_paths, wiki_root, project)` — 다중 deleted_paths 의 JSON plan
  - `apply_cascade(targets, apply=False)` — destructive subcommand 정공법 (apply=False default dry-run)
  - `render_cascade_plan_text(plan)` — human-readable text render (advisory)
  - 2 dataclass: `CascadeTarget` / `CascadeResult`
- **`workflow_kit/workflow_kit_cli.py` `cmd_cascade_delete` 등록** (subcommand 32):
  - `--deleted-paths=PATH` (repeatable, ≥1 required)
  - `--wiki-root=PATH` (required)
  - `--project=SLUG` (optional)
  - `--apply` (default dry-run, destructive subcommand 정공법 memory #5)
  - `--json` (optional)

## Test layer 변경

- **`tests/check_wiki_cascade_cleanup_v0_10_3.py` 신규** (7 acceptance test, ≈ 250 line):
  1. `test_file_to_stem_v0_10_3` — SSOT (basename / nested path / lowercase / 중복 dash / 비-alphanumeric 5 case)
  2. `test_find_cascade_targets_3_methods_v0_10_3` — 3-method matching (basename + stem + project-relative) 3 case verify
  3. `test_cascade_graceful_missing_v0_10_3` — 부재 / wiki_root 부재 / deleted_path empty 시 graceful skip + advisory warning
  4. `test_emit_cascade_plan_v0_10_3` — 다중 deleted_paths 의 plan emit (deleted_count + total_targets + plans + warnings)
  5. `test_apply_cascade_destructive_pattern_v0_10_3` — apply=False (default) dry-run, apply=True 실제 delete + 재감지
  6. `test_render_cascade_plan_text_v0_10_3` — human-readable text render (advisory 정합)
  7. `test_cascade_delete_cli_registered_v0_10_3` — CLI subcommand 등록 + dry-run subprocess verify

## Bug 발견 + fix (acceptance test 작성 중)

- `Path(deleted_path).name` 가 이미 `.md` 확장자 포함 → `f"{p.name}.md"` 는 `AGENTS.md.md` (이중 .md). fix: `wiki_root / p.name`
- macOS case-insensitive filesystem 에서 `AGENTS.md` 와 `agents.md` 가 같은 file → dedup 필요. fix: `Path.samefile()` 기반 dedup
- v0.9.2 L3 raw mirror `raw/projects/<project>/` prefix 매칭 누락 → fix: project-relative prefix 후보에 추가

## Acceptance criteria (spec §4.4 R-A cycle 2)

1. `file_to_stem` SSOT — kebab-case + lower 변환 (5 case)
2. `find_cascade_targets` 3-method matching (basename + stem + project-relative) — 매치 시 CascadeTarget emit
3. `find_cascade_targets` graceful skip — 부재 / wiki_root 부재 시 advisory warning + empty targets
4. `emit_cascade_plan` — 다중 deleted_paths 의 JSON plan (deleted_count + total_targets + plans + warnings)
5. `apply_cascade` — apply=False (default) dry-run + apply=True 실제 delete + executed/skipped list (destructive subcommand 정공법 memory #5)
6. `render_cascade_plan_text` — human-readable text render (advisory 정합)
7. CLI subcommand `cascade-delete` 등록 + dry-run subprocess verify (file 보존)

## 회귀 검증

- v0.10.3 신규: **7/7 PASS** ✅
- v0.10.2 회귀: **9/9 PASS** ✅
- v0.10.1 회귀: **6/6 PASS** ✅
- v0.10.0 회귀: **6/6 PASS** ✅
- v0.9.6 회귀: 6/6 PASS ✅
- v0.9.4 회귀: **3/3 PASS** ✅
- v0.9.2 회귀: **8/8 PASS** ✅
- v0.9.1 deprecation contract: **4/4 PASS** ✅
- 누적 acceptance: **63/63 PASS** (v0.10.3 7 + v0.10.2 9 + v0.10.1 6 + v0.10.0 6 + v0.9.6 6 + v0.9.5 환경의존 3 + v0.9.4 3 + v0.9.2 8 + v0.9.3 4 + v0.9.1 4 + v0.9.0 6 + 누적 환경의존 v0.9.5 3 + deprecation contract 4 갱신 = 63)

## Doc sync

- `releases/Beta-v0.10.3.md` 신규
- `pyproject.toml` version 0.10.2 → 0.10.3
- `workflow_kit/__init__.py` `_read_pyproject_version` literal `"v0.10.2-beta"` → `"v0.10.3-beta"`
- `README.md` §0 + §10 + release URL list
- `ai-workflow/memory/active/work_backlog.md` v0.10.3 entry
- `ai-workflow/memory/release/v0.10.3/backlog/2026-06-24.md` 신규 (본 file)

## Commit / tag / release

- 단일 commit (iterative amend) — direct main
- commit subject: `feat+chore(v0.10.3): wiki file deletion cascade cleanup (R-A follow-up cycle 2)`
- tag: `v0.10.3-beta`
- gh release create (Beta-v0.10.3.md 본문)
- `__version__` = v0.10.3-beta 정합 verify
