# v0.11.11 plan — mypy strict CI 통합 (GH Actions mypy-strict workflow)

## TL;DR

v0.11.10 release note 의 "다음" §1 follow-up. **mypy strict 도달 (v0.11.10) 의 CI 통합** — `.github/workflows/mypy-strict.yml` 신규 + `mypy 2.1.0` pin 정합 + acceptance test 1 신규. **PR merge 전 strict regression 차단** + 누적 strict clean 35 file 유지 verify.

**Scope (4 deliverables)**:
1. **TASK-V1111-001**: `.github/workflows/mypy-strict.yml` 신규 (1 CI workflow: push/PR to main)
2. **TASK-V1111-002**: `workflow-source/tests/check_mypy_strict_ci_v0_11_11.py` 신규 (acceptance test 4 case)
3. **TASK-V1111-003**: `workflow_kit` dev extra 의 mypy pin `>=1.0` → `==2.1.0` (CI + local 정합)
4. **TASK-V1111-004**: version bump 0.11.10 → 0.11.11 (pyproject + `__init__.py` loud fallback)

**SemVer bump**: patch (v0.11.10 → v0.11.11, breaking change ❌)
**PyPI 배포**: no (GitHub Releases only)

## Runtime layer 변경

### TASK-V1111-001: `.github/workflows/mypy-strict.yml` 신규

**Design 결정**:
- Triggers: `push` to main + `pull_request` to main (smoke 와 동일한 main-only 정책)
- Runner: `ubuntu-latest`
- Python: `3.10` (workflow_kit `python_version` 정합)
- mypy: `2.1.0` (v0.11.10 release note 의 strict 기준 정합)
- Mypy invocation: `mypy --no-incremental workflow-source/workflow_kit/` (**cwd = REPO_ROOT**, 절대경로). sub-package 의 `workflow_kit/pyproject.toml` (`strict=false`) 와 parent 의 `workflow-source/pyproject.toml` (`strict=true`) 의 merge 회피.
  - **Critical lesson**: `cd workflow-source && mypy workflow_kit/` (subdir 상대경로) 는 mypy 가 sub-package config 를 발견 → parent 의 `strict=true` cascade 가 풀려 46 errors 발생 (CI 1차 run fail).
- Step: `actions/checkout@v4` + `actions/setup-python@v5` + `pip install mypy==2.1.0` + mypy 실행

**기존 workflow 정합**:
- `smoke.yml` 은 `check_*.py` subprocess 실행 (느림, mypy wrapper 경유)
- `mypy-strict.yml` 은 direct `mypy` 실행 (빠름, dedicated)
- 둘 다 CI 에서 동작 (smoke = 회귀 verify / mypy-strict = strict maintain)

### TASK-V1111-002: acceptance test 1 신규 (`check_mypy_strict_ci_v0_11_11.py`)

**8 case verify**:
1. **case 1**: `.github/workflows/mypy-strict.yml` file 존재
2. **case 2**: workflow YAML valid parse + 필수 field (name / on.push / on.pull_request / jobs.<job>.steps)
3. **case 3**: workflow 의 mypy invocation = `mypy --no-incremental workflow-source/workflow_kit/` (regex match) + python 3.10 + mypy 2.1.0 pin
4. **case 4**: dev extra mypy pin `mypy==2.1.0` 정합 verify
5. **case 5**: loud fallback literal = `v0.11.11-beta` (regex parse)
6. **case 6**: cumulative strict clean 35 file 유지 verify (`workflow_kit/__init__.py` docstring 의 count)
7. **case 7**: pyproject `[project] version` = `0.11.11` verify
8. **case 8**: CI 의 mypy invocation 실제 실행 (REPO_ROOT cwd, full path) → 0 errors, exit 0 verify (subprocess)

### TASK-V1111-003: dev extra mypy pin 정합

**현재**: `workflow-source/workflow_kit/pyproject.toml` 의 dev extra:
```toml
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
    "ruff>=0.1",
    "mypy>=1.0",  # ← 너무 느슨
]
```

**변경 후**:
```toml
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
    "ruff>=0.1",
    "mypy==2.1.0",  # v0.11.10 strict 기준 정합
]
```

**근거**: v0.11.10 release note 가 mypy 2.1.0 strict 기준. CI 가 `mypy==2.1.0` install + local dev 가 `mypy>=1.0` install 이면 strict 결과 drift 가능. CI/local 정합 = pin 통일.

### TASK-V1111-004: version bump

- `workflow-source/pyproject.toml` `[project] version`: `"0.11.10"` → `"0.11.11"`
- `workflow-source/workflow_kit/__init__.py` loud fallback: `"v0.11.10-beta"` → `"v0.11.11-beta"`
- `__version__` 3-tier fallback (pyproject → importlib.metadata → loud fallback) 자동 derive
- cumulative strict clean = **35 file 유지** (v0.11.10 도달 후 신규 file/strict cleanup 0, 회귀 영향 ❌)

## Acceptance criteria

1. `.github/workflows/mypy-strict.yml` 신규 file 존재
2. workflow YAML valid + trigger (push to main + PR to main) 정합
3. workflow mypy invocation = `mypy --no-incremental workflow-source/workflow_kit/` (REPO_ROOT cwd, 절대경로)
4. dev extra mypy pin = `==2.1.0` (CI + local 정합)
5. `__version__` = `v0.11.11-beta` (3-tier fallback derive)
6. cumulative strict clean 35 file 유지 (mypy workflow_kit/ 0 errors)
7. pyproject `[project] version` = `0.11.11` verify
8. CI mypy invocation 실제 실행 (REPO_ROOT cwd, full path) → 0 errors, exit 0 verify
9. 회귀 acceptance test 90/90 PASS 유지 (v0.11.10 의 1 + 누적 회귀)

## 다음 (v0.11.12+)

1. **v0.11.12** — `cmd_release --full-auto` 의 mypy strict pre-check 통합 (release pipeline 자체에 mypy 0 errors gate 추가).
2. **v0.11.13+** — `workflow_kit/<new_module>.py` 작성 시 mypy strict 자동 통과 verify (CI 가 1차 방어선).
3. **v1.0.0** — full mypy strict milestone release (SemVer major 정렬, `__version__` = `v1.0.0`).
