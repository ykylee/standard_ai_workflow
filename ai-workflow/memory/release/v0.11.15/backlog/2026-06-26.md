# v0.11.15 plan — release summary 1-line (jq-friendly verdict)

## TL;DR

v0.11.14 release note 의 "다음" §1 follow-up. **`cmd_release` + `cmd_release_status` 의 JSON output 에 `summary` 1-line field 추가** (jq-friendly grep/pipe). `cmd_release --json | jq -r .summary` / `cmd_release_status --json | jq -r .summary` 로 1-line 정합 verify 가능. read-only, breaking change 없음.

**Scope (3 deliverables)**:
1. **TASK-V11115-001**: `_summarize_release_status(result)` helper (release_status.py) — 5-field format `ci_mypy / local_mypy / ready / next / unreleased`
2. **TASK-V11115-002**: `_attach_release_summary(results)` helper (release_pipeline.py) — 5-field format `ci_mypy / local_mypy / ready / next / error` + 모든 `cmd_release` return point wrap (11 site)
3. **TASK-V11115-003**: acceptance test `check_release_summary_v0_11_15.py` (8 case, jq-friendly 1-line verify)
4. **TASK-V11115-004**: version bump 0.11.14 → 0.11.15

**SemVer bump**: patch (v0.11.14 → v0.11.15, breaking change ❌)
**PyPI 배포**: no (GitHub Releases only)

## Runtime layer 변경

### TASK-V11115-001: `_summarize_release_status` helper (release_status.py)

`cmd_release_status` 결과 dict → 1-line summary string:
```
ci_mypy=<verdict>, local_mypy=<ok|FAIL>, ready=<true|false>, next=<X.Y.Z>, unreleased=<count>
```

5-field stable order, no space in value, grep / pipe / jq friendly. Example:
```
ci_mypy=ci_sanity, local_mypy=ok, ready=false, next=0.11.15, unreleased=0
```

### TASK-V11115-002: `_attach_release_summary` helper (release_pipeline.py)

`cmd_release` 결과 dict → `summary` field 추가. 모든 return point (11 site) 에서 호출:
- `validate failed; abort release` early return
- `--strict-cross-verify` hard fail (2 site)
- `no dist files found` early return
- `notes_resolution` error
- `release note not found` early return
- `--full-auto` bump fail
- `git push tag` fail
- `gh auth not authenticated`
- `gh release create failed`
- final `return results` (success)

Format: `ci_mypy=<verdict>, local_mypy=<ok|FAIL|skipped>, ready=<true|false>, next=<X.Y.Z|->, error=<msg|ok>`

5-field stable order, no space in value, jq-friendly. `--skip-validate` / `--skip-mypy` 시 `local_mypy=skipped` (Layer 2 not checked) + `ci_mypy=no_local_verify` (CI sanity + local skipped) 정합.

### TASK-V11115-003: acceptance test (8 case)

| Case | Verify |
|---|---|
| 1 | `_summarize_release_status` helper + 5-field format (ci_mypy / local_mypy / ready / next / unreleased) |
| 2 | `cmd_release_status` 가 `summary` field 반환 + 1-line (no newline) + 5-field |
| 3 | `cmd_release` 의 `_attach_release_summary` 11 호출 >= 9 return point |
| 4 | `cmd_release_create --skip-validate --json` summary verify (`local_mypy=skipped` + `ci_mypy=no_local_verify` + `ready=false` + `error=`) |
| 5 | `cmd_release_status` dispatcher text + JSON mode 둘 다 summary 포함 + rc=0 |
| 6 | summary jq-friendly: 5-field dict parse + space-free value |
| 7 | `cmd_release` full validate summary 5-field (unreleased 없음) + `local_mypy=ok` + `ci_mypy=sanity` |
| 8 | `_attach_release_summary` dict mutate + ci_stale verdict reflection |

### TASK-V11115-004: version bump

- `workflow-source/pyproject.toml` `[project] version`: `"0.11.14"` → `"0.11.15"`
- `workflow-source/workflow_kit/__init__.py` loud fallback: `"v0.11.14-beta"` → `"v0.11.15-beta"`
- cumulative strict clean = **36 file 유지** (read-only summary 추가 만, 신규 file / strict cleanup 0)

## Acceptance criteria

1. `_summarize_release_status` helper + 5-field format
2. `_attach_release_summary` helper + 모든 cmd_release return point wrap
3. `cmd_release --json | jq -r .summary` 1-line 정합
4. `cmd_release_status --json | jq -r .summary` 1-line 정합
5. `--skip-validate` / `--skip-mypy` 시 `local_mypy=skipped` + `ci_mypy=no_local_verify`
6. cumulative strict clean 36 file 유지
7. 회귀 acceptance test 116/116 PASS 유지 (v0.11.14 의 8 + v0.11.13 의 8 + v0.11.12 의 8 + 누적 92)

## 다음 (v0.11.16+ / v1.0.0)

1. **v0.11.16** — `cmd_release_status` 의 `ready_to_release` 자동 bump (`--auto-bump` flag, current_version == last_release_tag 일 때 next_version 자동 적용 + version-bump)
2. **v0.11.17** — `summary` field 의 `--format=jsonl` / `--format=kv` 변형 (jq-friendly 외 human-readable)
3. **v0.11.18+** — v1.0.0 spec layer 갱신 (full mypy strict milestone release)
4. **v1.0.0** — full mypy strict milestone release (SemVer major 정렬, `__version__` = `v1.0.0`)
