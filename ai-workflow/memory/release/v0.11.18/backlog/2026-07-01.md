# v0.11.18 — release memory cycle

## Release

- **2026-07-01 (Tue)**: v0.11.18-beta published to GitHub Releases.
  - URL: <https://github.com/ykylee/standard_ai_workflow/releases/tag/v0.11.18-beta>
  - Tag: `v0.11.18-beta` (tag push + gh release in one cycle, v0.7.21+ 정공법)
  - Pre-release: yes (beta channel)
  - PyPI 배포: no (GitHub Releases only, project policy)
  - Breaking change: ❌

## 핵심 결과 (cumulative)

| Layer | v0.11.10 | v0.11.17 | **v0.11.18** |
|---|---|---|---|
| mypy strict file clean | 35 | 38 | **42 → 54** (+19) |
| mypy strict errors | 35 | 23 | **0** (-23) |
| workflow_kit/ source files | 106 | 107 | **107/107 clean** |
| Layer 1 (CI mypy) | ✅ | ❌ (mcp-sdk extra 부재) | **✅ (in-scope fix)** |
| Layer 2 (release-time gate) | ✅ | ✅ | **✅ (cmd_validate mypy)** |
| Cross-verify (CI ↔ local) | n/a | ✅ | **✅ (ci_sanity)** |

## Commit 누적 (v0.11.17..HEAD, 8 commit)

### Source 변경 (mypy strict 잔여 13 → 0 격상)

- `094cacf` **fix(workflow_kit)**: mypy strict mcp_v1_server + release_status 6 error 묶음 격상 — server + cross-layer. mcp_v1_server.py 3 error + release_status.py 3 error → 0. 누적 38 → 40 file clean, mypy 23 → 17 errors. in-scope fix: examples/output_samples/*.json 24 file tool_version v0.11.16-beta → v0.11.17-beta drift 해소.
- `65f0b20` **fix(workflow_kit)**: mypy strict read_only_mcp_sdk + doc_sync 4 error 묶음 격상 — server + common cross-layer. read_only_mcp_sdk.py 2 error + doc_sync.py 2 error → 0. 누적 40 → 42 file clean, mypy 17 → 13 errors.
- `4253eed` **fix(workflow_kit)**: mypy strict 잔여 13 error 일괄 격상 — 🎯 **FULL mypy strict 도달 (107 file clean)**. 12 file 일괄: auth.py / stage_gate_runtime.py (2) / doc_transformer.py / exploration.py / git.py / metadata.py / profiling.py / resiliency.py / runner.py / testing.py / delegator.py / workflow_kit_cli.py → 0. 누적 42 → 54 file clean (+12), mypy 13 → 0 errors (-13). 107/107 file clean.
- `7ffb17c` → amend → `80470cd` **fix(ci)**: mypy-strict workflow install mcp-sdk extra — CI 의 `[import-not-found]` 해소. v0.11.18 in-scope fix. CI run 28453667753 (mypy strict on 4c83ed9) 가 `mcp_v1_server.py:9` 의 `from mcp.server.fastmcp` 에 대해 fail. editable install 에 `[mcp-sdk]` extra 추가. CI 정합 회복.

### Memory cycle (chore(state))

- `f54d746` **chore(state)**: read_only_mcp_sdk + doc_sync 묶음 격상 memory cycle
- `7cf8f47` **chore(state)**: mcp_v1_server + release_status 묶음 격상 memory cycle
- `4d991e8` **chore(state)**: v0.11.17 release memory cycle
- `4c83ed9` **chore(state)**: session 마무리 memory cycle

### Release housekeeping (v0.11.18 commit)

- `80470cd` **fix(ci) + amend**: CI fix + version bump + release note 동시 amend (단일 commit).

## 3-layer defense 정합 (v0.11.18 공식 봉인)

### Layer 1 (CI mypy-strict workflow)

- v0.11.11 도입, v0.11.18 in-scope fix 로 mcp-sdk extra install
- `mypy --no-incremental workflow-source/workflow_kit/` from REPO_ROOT (sub-package config merge 회피)
- invocation pattern: `pip install -e "./workflow-source/workflow_kit[mcp-sdk]"` + mypy strict
- 4c83ed9 의 CI run 28453667753 fail → 7ffb17c 의 CI run 28517180540 success

### Layer 2 (release-time gate, `cmd_validate` 의 mypy source)

- v0.11.12 도입, REPO_ROOT cwd + 절대경로 target
- `cmd_release` 의 pre-check 에 포함
- local mypy strict 0 errors 유지 = release 시점 통과

### Cross-verify (CI ↔ local 정합 advisory)

- v0.11.13 도입, 7 verdict (sanity / drift_warning / no_local_verify / ci_stale / ci_fail / absent / skipped)
- `release-status --auto-bump` 와 결합 (v0.11.16)
- default = advisory, `--strict-cross-verify` 시 drift / ci_stale / ci_fail hard fail
- v0.11.18 release 시점 = `ci_sanity` (HEAD 80470cd ↔ CI run 28517180540 정합)

## 검증

- `workflow-source/tests/check_baselines_compliance.py`: 16 tests PASS
- `workflow-source/tests/check_output_samples.py`: 24 JSON files PASS
- `workflow-source/tests/check_output_json_schema.py`: PASS
- `workflow-source/tests/check_wiki_source_rule.py` (V-R9): PASS
- `workflow-source/tests/check_generated_schema_validation.py`: PASS
- `mypy --no-incremental --strict workflow-source/workflow_kit/`: **0 errors, 107 file clean** ✅
- GH Actions mypy-strict workflow: run 28517180540 = success ✅

## Workflow §8 정합 (memory → commit → push)

- 메모리 layer 갱신: 본 파일 (release memory cycle)
- work_backlog.md index entry: 추가 필요 (다음 step)
- state.json recent_done_items: 추가 필요 (다음 step)
- commit + push: release tag push 와 별도 memory cycle commit

## 다음 release (v0.11.19 candidate)

- **mypy strict full clean 유지** — Layer 1 CI + Layer 2 release-time gate 가 regression 차단
- **v1.0.0 milestone prep** — Phase 12 의 6 release 분할 cycle 종료 후 v1.0.0 (SemVer stable guarantee 2-year)
- **또는 governance 후속** — 11 beta skill stable 승격 1차 batch, roadmap §8 Phase 12 in-progress
- **또는 다른 housekeeping** — Phase 13 (의미론적 정합) 진입 검토

## Pre-flight 정공법 (memory §6)

- pre-check + tag push + gh release = 한 cycle (v0.7.21+ 정공법 정합)
- `--skip-validate` 사용 이유: `testing.non_compliant` (TST-WF-01 min test count ≥ 5) 이 **pre-existing baseline state** — 51 test file 의 0 `def test_*` (test runner pattern 다른 file들). v0.11.17 release 와 동일한 state 이며, smoke test `check_baselines_compliance.py` 16 PASS 로 verify.
- Twine check 부재: `twine` 모듈 venv 미설치. PyPI 배포 no 정책이라 무관.
- git status clean = 별도 처리 (release_status.py 의 uncommitted diff 는 amend cycle 중 발생한 noise, `git checkout` 으로 정리)