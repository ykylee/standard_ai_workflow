# v0.11.7 plan — mypy strict 단계적 격상 19-20단계 (workflow_kit_cli + doc_sync)

## TL;DR

v0.11.6 release note 의 "다음" §1 follow-up. **mypy strict 누적 격상 27 → 29 file** (workflow_kit_cli 4 + doc_sync 2 = 6 errors 해소).

**Scope (3 deliverables)**:
1. **TASK-V1117-001**: `workflow_kit/workflow_kit_cli.py` strict clean (4 errors → 0)
2. **TASK-V1117-002**: `workflow_kit/common/doc_sync.py` strict clean (2 errors → 0)
3. **TASK-V1117-003**: 누적 strict clean **27 → 29 file** 갱신

**SemVer bump**: patch (v0.11.6 → v0.11.7, breaking change ❌)
**PyPI 배포**: no

## Runtime layer 변경

### TASK-V1117-001: workflow_kit_cli.py 4 errors → 0

**Error 분석**: `cmd_ingest_purpose` 의 `--apply` 분기에서 `build_workflow_state_payload(workspace_root=workspace_root)` 호출 시 4개 keyword-only arg (`project_profile_path`, `session_handoff_path`, `work_backlog_index_path`, `generated_at`) 누락.

**Fix 정공법**: `build_workflow_state_payload` 호출 제거 + `purpose_context._read_state_digest_and_rev` 직접 호출. dispatcher context 에서 path 3개 + `generated_at` 미보유 → lightweight 헬퍼 사용이 정합.

### TASK-V1117-002: doc_sync.py 2 errors → 0

**Fix 정공법**: 중간 변수 `operations_path_raw` / `document_home_raw` + `isinstance(..., str) or ... is None` guard + `cast("str | None", ...)` narrow.

### TASK-V1117-003: 누적 strict clean 27 → 29 file 갱신

- `workflow_kit/__init__.py` docstring cumulative count 갱신

## Acceptance criteria

1. `mypy workflow_kit/workflow_kit_cli.py` → **0 strict error** (v0.11.6 4 → 0)
2. `mypy workflow_kit/common/doc_sync.py` → **0 strict error** (v0.11.6 2 → 0)
3. 누적 strict clean file count **27 → 29 file**
4. 회귀 acceptance test 86/86 PASS 유지

## 회귀 검증

- v0.11.7 신규: 6 errors 해소
- 누적 acceptance: **87/87 PASS**
- 누적 mypy strict clean: **27 → 29 file**
- 전체 workflow_kit/ strict error: **12 → 6**

## 다음 (v0.11.8+ / v1.0.0)

1. **v0.11.8** — 잔존 6 file 단계적 해소.
2. **v1.0.0** — full mypy strict 도달.
