# v0.11.8 plan — mypy strict 단계적 격상 21-22단계 (read_only_mcp_sdk + workflow_writes)

## TL;DR

v0.11.7 release note 의 "다음" §1 follow-up. **mypy strict 누적 격상 29 → 31 file** (server/read_only_mcp_sdk 1 + common/workflow_writes 1 = 2 errors 해소).

**Scope (3 deliverables)**:
1. **TASK-V1118-001**: `workflow_kit/server/read_only_mcp_sdk.py` strict clean (1 error → 0)
2. **TASK-V1118-002**: `workflow_kit/common/workflow_writes.py` strict clean (1 error → 0)
3. **TASK-V1118-003**: 누적 strict clean **29 → 31 file** 갱신

**SemVer bump**: patch (v0.11.7 → v0.11.8, breaking change ❌)
**PyPI 배포**: no

## Runtime layer 변경

### TASK-V1118-001: read_only_mcp_sdk.py 1 error → 0

**Error 분석**:
```
read_only_mcp_sdk.py:116: "object" has no attribute "__iter__"
  (for descriptor in descriptors["tools"])
```

**Fix 정공법**:
- `tools_list = cast("list[object]", descriptors.get("tools", [])) if isinstance(descriptors.get("tools"), list) else []`
- 또는 descriptors type 명시적 narrow

### TASK-V1118-002: workflow_writes.py 1 error → 0

**Error 분석**:
```
workflow_writes.py:204: Need type annotation for "current_lists"  [var-annotated]
```

**Fix 정공법**:
- `current_lists: dict[str, list[str]] = {...}` 명시적 annotation (value type 명시)

### TASK-V1118-003: 누적 strict clean 29 → 31 file 갱신

- `workflow_kit/__init__.py` docstring cumulative count 갱신

## Acceptance criteria

1. `mypy workflow_kit/server/read_only_mcp_sdk.py` → **0 strict error** (v0.11.7 1 → 0)
2. `mypy workflow_kit/common/workflow_writes.py` → **0 strict error** (v0.11.7 1 → 0)
3. 누적 strict clean file count **29 → 31 file**
4. 회귀 acceptance test 87/87 PASS 유지

## 다음 (v0.11.9+ / v1.0.0)

1. **v0.11.9** — 잔존 4 file (common/testing, common/runner, common/project_docs, common/profiling).
2. **v0.11.10+** — full mypy strict 도달.
3. **v1.0.0** — full mypy strict 도달.
