# v0.13.1 (telemetry wiring commit, 2026-07-09) — **SemVer patch**

**Phase 13 Operational Intelligence v1.0 의 AC2 (memory_index 활용도) close-out release**.

v0.13.0 (Quality Dashboard) 의 Panel 3 가 `retrieval_hit_rate: 0.0` placeholder (`pending_phase_13_ac2_telemetry`) 로 출시된 후속. 본 release 는 placeholder → 실측값 전환 + telemetry sidecar 인프라 + 3 skill + dispatcher 자동 wiring.

## 1. 핵심 변경 (3 deliverable)

### 1.1 Telemetry sidecar 인프라 (신규)

- **신규 schema** (`workflow_kit/common/schemas/memory_index.py`): `MemoryIndexTelemetryEvent` (1 retrieval call 의 1 row: timestamp / source / workspace_root / query_tokens_count / selected_count / cue_hits / bm25_hits / expansion_hits / top_k / max_depth / use_bm25_fallback / error) + `MemoryIndexTelemetrySummary` (read-time 집계: total_calls / total_hits / hit_rate / by_source / first_event_at / last_event_at / events_parsed / events_skipped / source_version).
- **신규 helper** (`workflow_kit/common/state/memory_index.py`): `TELEMETRY_SUBDIR = "telemetry"` + `TELEMETRY_FILE = "events.jsonl"` constant + `telemetry_path(workspace_root)` + `telemetry_dir(workspace_root)` + **`append_telemetry_event(workspace_root, event)`** (in-process `threading.Lock` + `open("a")` + `flush` 정공법, OSError 시 silent stderr warn) + **`summarize_telemetry(workspace_root)`** (JSONL parse + malformed skip + `error=True` 인 event 는 hit 카운트 제외) + `read_telemetry_events(workspace_root)` (raw list).
- **정공법**: `tools/.consumer_metrics_history.jsonl` (v0.7.58) 와 동일 — JSONL append-only + single pass aggregate. v0.11.22 memory_index 의 `ai-workflow/memory/active/memory_index/` SSOT 옆에 sibling `telemetry/` subdir.

### 1.2 3 skill + dispatcher 자동 wiring (신규 wiring)

- **session-start / doc-sync / backlog-update** (`skills/*/scripts/run_*.py` 의 `_build_memory_index_query_output`): retrieval 성공 시 success-path telemetry emit + workspace_root 외부 dir / 예외 path 는 `error=True` flag 로 emit (negative example 보존).
- **dispatcher** (`workflow_kit/workflow_kit_cli.py:cmd_memory_index_query`): 같은 패턴 + try/except 외부에 emit.
- **4 source × 4 path** = 12 emit point. caller 영향 0 (output schema 변경 ❌, sidecar 만).

### 1.3 Dashboard Panel 3 placeholder → 실측값

- `workflow_kit/common/dashboard_data.py:collect_memory_index_utilization` 가 `summarize_telemetry` 호출 — `retrieval_hit_rate` = telemetry hit rate, `retrieval_hit_rate_source` = `"memory_index_telemetry_v0_13_1"` (placeholder 교체).
- `_render_html_panel_3` 가 telemetry `total_calls` / `total_hits` / `by_source` 표시 추가.
- `_aggregate_memory_index` 의 placeholder 도 함께 교체 (defense in depth).
- `_empty_memory_index_utilization` 도 신규 `telemetry.{total_calls, total_hits, by_source, ...}` field 추가 (graceful empty).

## 2. 신규 subcommand + smoke test

- **`memory-index-telemetry`** (subcommand 36, read-only): `python3 -m workflow_kit.cli --command memory-index-telemetry --workspace-root=. [--json|--show-events]`. §6.3 MUST-NOT-delegate 정합.
- **`tests/check_memory_index_telemetry.py`** (신규 smoke, 6/6 PASS):
  1. `test_emit_one_event_returns_summary_with_one_call` — 1 event → total_calls=1, hit=False (selected_count=0)
  2. `test_two_hits_one_miss_hit_rate_two_thirds` — 3 event (1 hit + 1 miss + 1 error) → total=3, hits=1, hit_rate=1/3
  3. `test_by_source_breakdown_distinguishes_skills` — session-start vs doc-sync 분해
  4. `test_malformed_jsonl_line_is_skipped` — malformed line 2개 skip + valid 1개 parse
  5. `test_workspace_root_absent_returns_empty_summary` — graceful zero
  6. `test_concurrent_append_preserves_all_lines` — 2 thread × 5 event = 10 line 보존 (in-process lock)

## 3. 회귀 + housekeeping

- **drift prevention 6/6 PASS** (test_case_1 pyproject loud fallback sync + test_case_4 README header sync fix 동반)
- **mypy strict 0 new error** (8 pre-existing in state/memory_index.py + state/builder.py 정공법 유지)
- **samples housekeeping**: `examples/output_samples/*.json` 24 file `tool_version` v0.11.20-beta → **v0.13.1-beta** (v0.11.20 정공법 정합)
- **schemas regen**: `schemas/generated_output_schemas.json` (gen-schema 23 family 120,079 bytes) + `schemas/output_sample_contracts.json` (24 family)
- **dashboard HTML regen**: `docs/dashboard/index.html` + `ai-workflow/dashboard/snapshot.md` — Panel 3 의 `retrieval_hit_rate: 1.0` + `telemetry: {total_calls=1, by_source={dispatcher: 1}}` 표시
- **pyproject.toml**: `0.13.0` → **`0.13.1`** (auto-bump 정공법)
- **__init__.py loud fallback**: v0.13.0-beta → **v0.13.1-beta** (drift prevention test_case_1 정합)
- **README.md header**: v0.13.0-beta → **v0.13.1-beta** + Phase 13 v0.13.1 follow-up 1줄 추가 (drift prevention test_case_4 정합)
- **maturity_matrix.json in_progress_highlights**: v0.13.0 + v0.13.1 2줄 추가
- **memory_index/telemetry/.gitkeep**: sibling subdir 실재성 보장
- **memory_index/README.md**: layout 다이어그램 + telemetry 설명 추가

## 4. 검증 결과

- 신규 smoke **6/6 PASS** (check_memory_index_telemetry)
- memory_index smoke **25/25 PASS** (regression)
- 3 skill smoke (session-start / doc-sync / backlog-update) **3/3 PASS**
- Quality Dashboard smoke **10/10 PASS** (v0.13.0 회귀)
- drift prevention **6/6 PASS** (v0.11.23 회귀)
- output samples / generated schema validation / output JSON schema generation **3/3 PASS**
- memory freeze lint **PASS** (3 archive, all required files present)
- mypy strict **0 new error** (8 pre-existing in unrelated files 정공법 유지)

**누적 smoke: 211+ PASS** (v0.13.0 200+ + 신규 6 + 회귀 5+)

## 5. 산출물

### 신규 (3 file)

- `workflow-source/tests/check_memory_index_telemetry.py` (220 line, 6 case smoke)
- `ai-workflow/memory/active/memory_index/telemetry/.gitkeep` (sibling subdir placeholder)
- `ai-workflow/memory/release/v0.13.1/backlog/2026-07-09.md` (본 release note)

### 수정 (12 file)

1. `workflow-source/workflow_kit/common/schemas/memory_index.py` — `MemoryIndexTelemetryEvent` + `MemoryIndexTelemetrySummary` 2 schema 추가
2. `workflow-source/workflow_kit/common/state/memory_index.py` — `TELEMETRY_SUBDIR` + `TELEMETRY_FILE` constant + `telemetry_path` + `telemetry_dir` + `append_telemetry_event` + `summarize_telemetry` + `read_telemetry_events` 6 helper 추가
3. `workflow-source/skills/session-start/scripts/run_session_start.py` — `_build_memory_index_query_output` 에 telemetry emit 3 point (외부 dir / success / exception)
4. `workflow-source/skills/doc-sync/scripts/run_doc_sync.py` — 동일 패턴
5. `workflow-source/skills/backlog-update/scripts/run_backlog_update.py` — 동일 패턴
6. `workflow-source/workflow_kit/workflow_kit_cli.py` — `cmd_memory_index_query` 에 telemetry emit + **`cmd_memory_index_telemetry` subcommand 36 신규 등록**
7. `workflow-source/workflow_kit/common/dashboard_data.py` — `collect_memory_index_utilization` post-hook (`_attach_telemetry_summary`) + `_render_html_panel_3` telemetry field 표시
8. `workflow-source/pyproject.toml` — `0.13.0` → `0.13.1`
9. `workflow-source/workflow_kit/__init__.py` — loud fallback v0.13.0-beta → v0.13.1-beta + cumulative mypy count v0.13.1 entry
10. `README.md` — header v0.13.0-beta → v0.13.1-beta + Phase 13 v0.13.1 follow-up 1줄
11. `workflow-source/core/maturity_matrix.json` — in_progress_highlights v0.13.0 + v0.13.1 2줄
12. `ai-workflow/memory/active/memory_index/README.md` — layout 다이어그램 + telemetry 설명

### Housekeeping (3 file)

13. `examples/output_samples/*.json` — 24 file `tool_version` v0.11.20-beta → v0.13.1-beta
14. `schemas/generated_output_schemas.json` — gen-schema regenerate (23 family, 120,079 bytes)
15. `schemas/output_sample_contracts.json` — sample-contracts-regen (24 family)
16. `docs/dashboard/index.html` — dashboard HTML regenerate
17. `ai-workflow/dashboard/snapshot.md` — markdown snapshot regenerate

## 6. AC2 close-out 검증

- ✅ 3 skill opt-in retrieval 호출이 `ai-workflow/memory/active/memory_index/telemetry/events.jsonl` 에 자동 emit
- ✅ dashboard panel 3 의 `retrieval_hit_rate` placeholder (0.0) → 실측값 (1.0 = 1/1 hit)
- ✅ baseline (1 release = 1+ retrieval call) 측정 가능: `cmd_memory_index_telemetry --json` 으로 verify
- ✅ `phase-13-definition-north-star.md` 의 AC2 status 🟡 → ✅
- ✅ `north-star = silent_failing_cycles_count` 와는 *직접* 무관 (sub-metric 으로 AC1 과 별도)

## 7. 후속 (Phase 13 follow-up, v0.13.2+)

- **AC3 self-recovering** (drift auto-fix, manual 0) — v0.13.2+ 후속
- **AC4+ self-documenting** (wiki-memory 양방향 link 자동 갱신) — 별도 sub-milestone
- **ADR-006 retrospective** (full review) — 2026-07-16 scheduled, 별도 cycle
- **MCP beta 1st batch stable 승격** (v0.11.26) — 별도 priority track

## 8. release URL

- tag: `v0.13.1-beta` (push 예정)
- gh release create: 예정
- breaking change: ❌
- PyPI 배포: no