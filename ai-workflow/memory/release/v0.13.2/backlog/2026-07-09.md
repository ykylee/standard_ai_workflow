# v0.13.2 (self-recover wiring commit, 2026-07-09) — **SemVer patch**

**Phase 13 Operational Intelligence v1.0 의 AC3 (self-recovering) close-out release**.

v0.13.0 (Quality Dashboard) + v0.13.1 (telemetry sidecar) 의 후속. drift prevention 의
6 case 가 검출한 drift 를 **자동 fix** + **manual_required 분리** + **release note
self-recovery log 자동 emit** 의 1-cycle orchestrator.

## 1. 핵심 변경 (3 deliverable)

### 1.1 Self-recover orchestrator (신규)

- **신규 dispatcher subcommand 37** `self-recover` (`workflow-source/tools/release_pipeline.py`)
  ARGS: `--apply` (default False) / `--dry-run` (default True) / `--json`.
- **detect** (`_run_drift_prevention_smoke`) — `check_drift_prevention_v0_11_23.py` 를
  subprocess 로 inline 실행. stdout 에서 `PASS:` / `FAIL:` line regex parse + summary
  line. `dashboard_data.py:run_drift_prevention_guard_inline` 과 동일 정공법.
- **classify** (`_classify_drift_failures`) — `_SELF_RECOVER_CASE_MAP` dict 기반
  auto_fixable / manual_required 2-bucket 분리. 미등록 case 는 manual_required (fail-safe).
- **fix** — auto_fixable case 각각 매핑된 fix 함수 호출:
  - `test_case_1_pyproject_loud_fallback_sync` → `_fix_loud_fallback` (regex 교체 + atomic_write_text)
  - `test_case_4_readme_header_version_sync` → `_fix_readme_header_version` (regex 교체 + atomic_write_text)
  - `test_case_5_harness_supported_ssot_alignment` → `_fix_maturity_matrix_drift` (cmd_maturity_matrix_sync wrapper)
  - `test_case_6_maturity_last_updated_freshness` → `_fix_maturity_matrix_drift` (last_updated today patch)
  - `test_case_2_maturity_matrix_phase_status` / `test_case_3_skill_stage_matches_promotion_set` →
    **manual_required** (human judgment 필요 — release note 의 closed_phases /
    promoted_skills frontmatter 가 제공되어야 fix 가능)
- **re-check** — 동일 smoke 재실행 → 6/6 PASS 검증.
- **emit** (`_emit_recovery_summary`) — `{mode, recovered, manual_required, re_check, summary}`
  dict. `cmd_release` 가 release note body 에 `## Self-recovery log` 자동 append 의 source.

### 1.2 cmd_release 자동 wiring (신규)

- **validate step 후** `cmd_self_recover(_attr_ns(apply=True))` 자동 호출. §3.0 doc-headers-update
  / sync-maturity-matrix step 의 *선행* 으로 self-recover 가 자동 fix 후 doc-header 가
  그 위에 다시 sync (defense in depth).
- **manual_required > 0** 이면 `results["self_recover"]` 포함 후 **early return** (drift
  fix 우선 — 사람의 명시 intervention 필요). error 메시지에 manual_required list 포함.
- **`--skip-self-recover`** escape hatch (manual override).
- **`_attr_ns.normalize`** 에 `skip_self_recover` 추가.

### 1.3 Self-recovery log release note emit (신규)

- `_format_self_recovery_log(sr_result)` — dict → markdown 문자열. drift 가 없거나 (`recovered=[]` + `manual_required=[]`) 면 빈 문자열.
- `_emit_self_recovery_log(args, recovery_log)` — release note 끝에 `## Self-recovery log` 섹션 자동 append. **idempotent** (이미 섹션 존재 시 skip).
- release note 가 없거나 (backfill 시나리오) recovery_log empty 면 no-op.
- release pipeline step 6.5 (dashboard emit 다음) 에서 호출.

## 2. 신규 smoke test

- **`tests/check_self_recovering_v0_13_2.py`** (8 case, 8/8 PASS):
  1. `test_no_drift_no_fix_needed_clean_state` — 깨끗한 상태 dry-run → recovered=[], pass
  2. `test_loud_fallback_drift_auto_fixed_via_apply` — pyproject bump → loud_fallback drift → --apply fix
  3. `test_readme_header_drift_auto_fixed_via_apply` — pyproject + loud fix → README header drift → --apply fix
  4. `test_dry_run_does_not_modify_files` — drift 주입 + dry-run → file unchanged
  5. `test_classify_separates_auto_and_manual` — _classify_drift_failures 의 2-bucket 분리 정확
  6. `test_re_check_pass_after_apply_with_recovered` — apply 후 re_check 6/6 pass
  7. `test_format_self_recovery_log_returns_markdown` — format helper 정확
  8. `test_emit_self_recovery_log_appends_to_release_note` — emit helper 정확

## 3. 회귀 + housekeeping

- **drift prevention 6/6 PASS** (clean state + drift-fix 후 양쪽)
- **mypy strict 0 new error** — 22 errors (v0.13.1 baseline) → 21 errors (v0.13.2,
  workflow_kit_cli.py 의 MemoryIndexTelemetryEvent explicit import 로 1 error 해소)
- **sys.path fix** — `tools/release_pipeline.py` 의 standalone 실행 시
  `workflow-source` 를 sys.path 에 강제 주입. atomic_write_text / atomic_write_json
  가 실제로 import 가능해짐. v0.11.22 ~ v0.13.1 동안의 latent bug fix (release
  pipeline 이 standalone 으로 실행될 때 atomic_write 가 None 으로 fall back 되어
  silent degrade 가능했던 결함).
- **README_PATH fix** — `tools/release_pipeline.py` 의 README_PATH 가
  `REPO_ROOT / "README.md"` (`workflow-source/README.md`, 작음) → `REPO_ROOT.parent /
  "README.md"` (실제 root README, 47KB) 로 정정. case_4 README header fix 가 정상 동작.
- **samples housekeeping**: `examples/output_samples/*.json` 24 file `tool_version` v0.13.1-beta → **v0.13.2-beta**
- **schemas regen**: `schemas/generated_output_schemas.json` (23 family 120,079 bytes) + `schemas/output_sample_contracts.json`
- **dashboard regen**: `docs/dashboard/index.html` + `ai-workflow/dashboard/snapshot.md`
- **pyproject.toml**: 0.13.1 → **0.13.2**
- **__init__.py loud fallback**: v0.13.1-beta → **v0.13.2-beta**
- **README.md header**: v0.13.1-beta → v0.13.2-beta + Phase 13 v0.13.2 follow-up 1줄
- **maturity_matrix.json in_progress_highlights**: v0.13.2 1줄 추가

## 4. 검증 결과

- 신규 smoke **8/8 PASS** (check_self_recovering_v0_13_2)
- 3 skill smoke (session-start / doc-sync / backlog-update) **3/3 PASS**
- memory_index smoke **25/25 PASS**
- memory_index telemetry smoke **6/6 PASS**
- Quality Dashboard smoke **10/10 PASS**
- drift prevention smoke **6/6 PASS**
- output samples + schema validation **3/3 PASS**
- mypy strict **0 new error** (21 pre-existing in unrelated files)

**누적 smoke: 219+ PASS** (v0.13.1 211+ + 신규 8 + 회귀 0 net new)

## 5. 산출물

### 신규 (2 file)

- `workflow-source/tests/check_self_recovering_v0_13_2.py` (260 line, 8 case smoke)
- `ai-workflow/memory/release/v0.13.2/backlog/2026-07-09.md` (release note)

### 수정 (8 file)

1. `workflow-source/tools/release_pipeline.py` — `_SELF_RECOVER_CASE_MAP` + `_classify_drift_failures` + `_fix_loud_fallback` + `_fix_readme_header_version` + `_fix_maturity_matrix_drift` + `_run_drift_prevention_smoke` + `_emit_recovery_summary` + `cmd_self_recover` + `_format_self_recovery_log` + `_emit_self_recovery_log` 10 helper + `cmd_release` 의 self-recover step + argparse `self-recover` subcommand 37 + argparse `--skip-self-recover` flag + sys.path fix + README_PATH fix
2. `workflow-source/workflow_kit/workflow_kit_cli.py` — MemoryIndexTelemetryEvent import 경로 schemas layer 로 정정 (explicit export)
3. `workflow-source/pyproject.toml` — 0.13.1 → 0.13.2
4. `workflow-source/workflow_kit/__init__.py` — loud fallback v0.13.1-beta → v0.13.2-beta
5. `README.md` — header v0.13.1-beta → v0.13.2-beta + Phase 13 v0.13.2 follow-up 1줄
6. `workflow-source/core/maturity_matrix.json` — in_progress_highlights v0.13.2 1줄
7. `workflow-source/examples/output_samples/*.json` — 24 file tool_version v0.13.1-beta → v0.13.2-beta
8. `workflow-source/schemas/generated_output_schemas.json` + `output_sample_contracts.json` — regen

### Housekeeping (2 file)

9. `docs/dashboard/index.html` + `ai-workflow/dashboard/snapshot.md` — regenerate

## 6. AC3 close-out 검증

- ✅ drift 검출 → 자동 fix 매핑 (5 case: case_1 / 4 / 5 / 6 + design 한정 manual_required 2 case)
- ✅ 1 release cycle 내 fix cycle closed (validate → self-recover → dashboard emit → release note log append)
- ✅ self-recovery log 가 release note 본문에 자동 emit (`## Self-recovery log` 섹션, idempotent)
- ✅ `phase-13-definition-north-star.md` AC3 status 🟡 → ✅
- ✅ `silent_failing_cycles_count` north-star 와 정합 (drift 가 self-recover 되면 silent fail 0)

## 7. 후속 (Phase 13 v0.13.3+)

- **AC4+ self-documenting** (wiki-memory 양방향 link 자동 갱신) — 별도 sub-milestone
- **ADR-006 retrospective** (full review) — 2026-07-16 scheduled
- **MCP beta 1st batch stable 승격** (v0.11.26) — 별도 priority track
- **drift-prevention-91-cycle-classification §5 hook 후보 5 종** — 별도 cycle

## 8. release URL

- tag: `v0.13.2-beta` (push 예정)
- gh release create: 예정
- breaking change: ❌
- PyPI 배포: no