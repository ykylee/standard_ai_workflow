# v0.13.3 (bidir-link wiring commit, 2026-07-09) — **SemVer patch**

**Phase 13 Operational Intelligence v1.0 의 AC4+ (self-documenting) close-out release**.

AC1 (drift zero) + AC2 (telemetry) + AC3 (self-recover) + **AC4+ (bidir link)** 모두 ✅.
Phase 13 의 마지막 acceptance close.

## 1. 핵심 변경 (3 deliverable)

### 1.1 bidir_link.py 신규 (R-A + R-C 통합)

- **`workflow_kit/common/state/bidir_link.py`** (260 line) — wiki ↔ memory 양방향 link 자동화.
- **3 helper + 3 dataclass**:
  - `normalize_memory_path_to_wiki_relative(path, workspace_root)` — in-repo 절대 path → wiki relative 변환. `ai-workflow/wiki/topics/foo.md` → `topics/foo.md`.
  - `audit_bidirectional_links(workspace_root) -> BidirLinkAudit` — wiki pages × memory entries 의 cross-reference 검증 (R-C). `total_wiki_pages` / `total_memory_entries` / `symmetric_links` / `asymmetric: list[BidirLinkAsymmetry]` / `is_symmetric` emit.
  - `sync_memory_to_wiki(workspace_root, dry_run=True) -> BidirSyncResult` — memory entry.mentioned_in 의 wiki page path 순회 → 각 wiki page 의 frontmatter `related_pages` 에 memory entry file 의 in-repo relative path 추가 (R-A). idempotent — 이미 있으면 skip.
- **`BidirLinkAudit`** + **`BidirSyncResult`** + **`BidirLinkAsymmetry`** — dataclass. Pydantic 아닌 dataclass (mypy strict 정합).
- **`Frontmatter.parse` 재사용** — `workflow_kit.okf_export.Frontmatter.parse` 로 wiki frontmatter parse (yaml dep 회피).
- **`_emit_yaml_frontmatter`** — 신규 yaml subset emitter (parse 와 round-trip).

### 1.2 cmd_bidir_link dispatcher subcommand 38 (R-A + R-C 1-cycle)

- **`workflow-source/tools/release_pipeline.py:cmd_bidir_link`** (신규).
- **detect → sync → re-audit** 1-cycle orchestrator:
  - pre-audit (drift 검출)
  - sync (auto-apply if --apply)
  - post-audit (정합 확인)
- ARGS: `--workspace-root`, `--apply`, `--json`.
- default = audit only (read-only). `--apply` 명시 시 sync.
- §6.3 MUST-NOT-delegate 정합.

### 1.3 cmd_release 자동 wiring + release note log emit

- **`cmd_release` step 2.8** — `cmd_bidir_link` (default audit) 자동 호출. `--skip-bidir-link` escape hatch. asymmetric > 0 이면 advisory 만 (release 차단 ❌).
- **`_format_bidir_link_audit`** + **`_emit_bidir_link_audit_log`** — release note 본문 끝에 `## Bidirectional link audit` 섹션 자동 append (idempotent marker). v0.13.2 self-recovery log 와 동일 정공법.
- **`tools/release_pipeline.py`** dispatcher 등록 (`bidir-link` argparse subcommand).

## 2. 신규 smoke test

- **`tests/check_bidir_link_v0_13_3.py`** (6 case, 6/6 PASS):
  1. `test_audit_returns_correct_shape` — spec 형식 검증
  2. `test_path_normalization_round_trip` — `ai-workflow/wiki/...` ↔ `topics/...` 양방향
  3. `test_sync_dry_run_does_not_modify` — dry-run 시 file unchanged
  4. `test_sync_apply_updates_wiki_frontmatter` — drift 주입 + --apply → wiki 갱신 + symmetric
  5. `test_audit_is_symmetric_after_sync` — sync 후 re-audit = symmetric
  6. `test_format_bidir_link_audit_returns_markdown` — release note body format 검증

## 3. 회귀 + housekeeping

- **drift prevention 6/6 PASS** (clean state)
- **mypy strict 0 new error** — 21 errors (v0.13.2 baseline) 유지. bidir_link.py 신규 추가되지만 in-place fix (3 mypy hint 처리).
- **samples housekeeping**: `examples/output_samples/*.json` 24 file `tool_version` v0.13.2-beta → **v0.13.3-beta**
- **schemas regen**: `schemas/generated_output_schemas.json` (23 family 120,079 bytes) + `schemas/output_sample_contracts.json`
- **dashboard regen**: `docs/dashboard/index.html` + `ai-workflow/dashboard/snapshot.md`
- **pyproject.toml**: 0.13.2 → **0.13.3**
- **__init__.py loud fallback**: v0.13.2-beta → **v0.13.3-beta**
- **README.md header**: v0.13.2-beta → v0.13.3-beta + Phase 13 v0.13.3 follow-up 1줄
- **maturity_matrix.json in_progress_highlights**: v0.13.3 1줄 추가

## 4. 검증 결과

- 신규 smoke **6/6 PASS** (check_bidir_link_v0_13_3)
- bidir-link smoke **13 case** (6 new + 7 regressions)
- 3 skill smoke (session-start / doc-sync / backlog-update) **3/3 PASS**
- memory_index smoke **25/25 PASS**
- memory_index telemetry smoke **6/6 PASS**
- self_recovering smoke **8/8 PASS**
- Quality Dashboard smoke **10/10 PASS**
- drift prevention smoke **6/6 PASS**
- output samples + schema validation **3/3 PASS**
- mypy strict **0 new error** (21 pre-existing in unrelated files)

**누적 smoke: 225+ PASS** (v0.13.2 219+ + 신규 6 + 회귀 0 net new)

## 5. 산출물

### 신규 (3 file)

- `workflow_kit/common/state/bidir_link.py` (260 line)
- `workflow-source/tests/check_bidir_link_v0_13_3.py` (220 line, 6 case smoke)
- `ai-workflow/memory/release/v0.13.3/backlog/2026-07-09.md` (release note)

### 수정 (8 file)

1. `workflow-source/tools/release_pipeline.py` — `cmd_bidir_link` dispatcher subcommand 38 + argparse 등록 + `_format_bidir_link_audit` + `_emit_bidir_link_audit_log` + `cmd_release` step 2.8 + `--skip-bidir-link` flag + `_attr_ns.normalize` 갱신
2. `workflow-source/pyproject.toml` — 0.13.2 → 0.13.3
3. `workflow-source/workflow_kit/__init__.py` — loud fallback v0.13.2-beta → v0.13.3-beta
4. `README.md` — header v0.13.2-beta → v0.13.3-beta + Phase 13 v0.13.3 follow-up 1줄
5. `workflow-source/core/maturity_matrix.json` — in_progress_highlights v0.13.3 1줄
6. `workflow-source/examples/output_samples/*.json` — 24 file tool_version v0.13.2-beta → v0.13.3-beta
7. `workflow-source/schemas/generated_output_schemas.json` + `output_sample_contracts.json` — regen
8. `docs/dashboard/index.html` + `ai-workflow/dashboard/snapshot.md` — regenerate

## 6. AC4+ close-out 검증

- ✅ memory entry.mentioned_in 변경 → wiki related_pages 자동 갱신 (R-A)
- ✅ 비대칭 link 0 건 (R-C audit smoke) — 실제 audit 시 `is_symmetric=true`, `asymmetric_count=0`
- ✅ wiki cross-link 신뢰 가능 (외부 consumer 의 parse 안정 — yaml subset round-trip)
- ✅ `phase-13-definition-north-star.md` AC4+ status 🟡 → ✅
- ✅ `silent_failing_cycles_count` north-star 와 정합 (drift / self-recover / bidir 모두 self-* 자동화로 silent fail 0)

## 7. 후속 (Phase 13 follow-up, v0.13.4+)

- **R-B** Wiki → Memory reverse lookup (wiki related_pages 변경 시 memory entry 생성 제안) — 별도 sub-milestone
- **ADR-006 retrospective** (full review) — 2026-07-16 scheduled
- **MCP beta 1st batch stable 승격** (v0.11.26) — 별도 priority track
- **drift-prevention-91-cycle-classification §5 hook 후보 5 종** — 별도 cycle
- **Phase 13 close-out release** (AC1+AC2+AC3+AC4+ 모두 ✅ 시 close) — 본 release 가 그 조건 충족. v0.13.4 또는 v0.14.0 으로 Phase 13 close 정식 release 가능.

## 8. release URL

- tag: `v0.13.3-beta` (push 예정)
- gh release create: 예정
- breaking change: ❌
- PyPI 배포: no