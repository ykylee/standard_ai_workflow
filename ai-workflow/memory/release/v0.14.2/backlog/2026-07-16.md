# v0.14.2 (MCP 2nd batch stable — apply_robust_patch, 2026-07-16) — **SemVer patch**

**v0.14.1 의 follow-up.** Phase 14 의 MCP beta 4종 stable 승격 로드맵의 2nd batch 정합.

## 1. 핵심 변경 (3 deliverable)

### 1.1 Pydantic schema 신규 (`ApplyRobustPatchOutput`)

`workflow_kit/common/schemas/apply_robust_patch.py` (skill_beta_criteria §3.1 1st condition):

- `BaseOutput` 상속: status / tool_version / warnings 3 field 자동
- 추가 field 7 종:
  - `file_path: str` (absolute path)
  - `message: str` (summary)
  - `patches_applied: int` (success count)
  - `patches_failed: int` (failure count)
  - `dry_run: bool` (preview mode)
  - `applied_blocks: list[AppliedPatchBlock]` (per-block detail)
  - `error / error_code / source_context: optional` (ErrorOutput envelope)
- `AppliedPatchBlock` nested: `block_index / matched / fuzzy_score / preview`
- `APPLY_ROBUST_PATCH_ERROR_CODES`: 4 종 tuple (skill_beta_criteria §3.1 3rd condition)
  - `missing_required_argument`
  - `file_not_found`
  - `malformed_patch_block`
  - `apply_robust_patch_runtime_error`

### 1.2 writing_bundle.apply_robust_patch_payload refactor

`workflow_kit/common/writing_bundle.py`:
- **file_not_found**: `resolve_existing_path` raise 시 → status=error + error_code='file_not_found'
- **malformed_patch_block**: SEARCH/REPLACE delimiter 부재 → status=error + error_code='malformed_patch_block'
- **apply_robust_patch_runtime_error**: `apply_robust_patch` 예외 → status=error + error_code='apply_robust_patch_runtime_error'
- **success path**: status=ok + patches_applied=1 + error_code=None

ErrorOutput envelope (error / error_code / source_context 3 field) 정합 emit.

### 1.3 Smoke 5 case (`tests/check_apply_robust_patch.py`)

본 MCP 는 **쓰기 작업**. smoke test 시 `tempfile.TemporaryDirectory` 로 격리하여 side-effect 차단:

1. `case_1_help`: `--help` 정상 동작 + description 포함
2. `case_2_valid_patch`: 정상 SEARCH/REPLACE 적용 + 실제 file 의 SEARCH 가 REPLACE 로 교체 검증 (쓰기 작업 정합성)
3. `case_3_file_not_found`: 부재 file_path → error_code='file_not_found'
4. `case_4_malformed_patch`: SEARCH/REPLACE delimiter 부재 → error_code='malformed_patch_block'
5. `case_5_pydantic_validate`: 4 error code 정의 정합 + ok/error sample dict validate

## 2. maturity_matrix 갱신

| MCP | stage (전 → 후) | promotion_target | stable_acceptance_status |
|---|---|---|---|
| `apply_robust_patch` | beta → **stable** | v0.14.2 | **6/6 True** |
| (이전 1st batch) | stable | v0.14.1 | 6/6 True (이전 release) |

1st batch + 2nd batch 모두 stable. **Beta MCP 4종 모두 stable 승격** (workflow_log_rotator 는 stale 제거).

## 3. MCP.md 갱신

- status: prototype → **stable**
- 최종 수정일: 2026-06-09 → **2026-07-16**
- 관련 문서: Pydantic schema file 명시

## 4. 누적 smoke (모두 PASS)

| smoke | 결과 |
|---|---|
| drift prevention 6/6 | ✅ |
| memory_lint 4/4 | ✅ |
| memory_freeze_lint | ✅ |
| appendonly_memory_layout 6/6 + WARN 1 | ✅ |
| git_history_summarizer 5/5 | ✅ |
| smart_context_reader 5/5 | ✅ |
| **apply_robust_patch 5/5** (신규) | ✅ |

## 5. Beta MCP 4종 stable 승격 종합 (v0.13.1 → v0.14.2)

| MCP | batch | stable release |
|---|---|---|
| git_history_summarizer | 1st | v0.14.1 |
| smart_context_reader | 1st | v0.14.1 |
| workflow_log_rotator | (stale) | removed v0.14.1 |
| apply_robust_patch | 2nd | **v0.14.2** |

## 6. Out of scope

- `cmd_refresh_maturity` dispatcher subcommand (helper `refresh_maturity_last_updated` 존재, subcommand 만 추가)
- HTML renderer (`_render_panel_html_1`) 의 stale hint 추가
- `cmd_release` post-emit 의 maturity auto-refresh

Refs:
- [`../../../../workflow-source/MEMORY_GOVERNANCE.md`](../../../../workflow-source/MEMORY_GOVERNANCE.md)
- [`../../../../workflow-source/core/skill_beta_criteria.md`](../../../../workflow-source/core/skill_beta_criteria.md) §3.1
- [`../../../../workflow-source/releases/Beta-v0.14.2.md`](../../../../workflow-source/releases/Beta-v0.14.2.md) (정식 release note)

— generated 2026-07-16