# v0.14.3 (Phase 15 dashboard Panel 6/7/8, 2026-07-16) — **SemVer patch**

**Phase 15 시작.** Quality Dashboard 의 panel 5 종 → **8 종** 으로 확장. Phase 14 의 north-star metric + deprecation cycle progress + telemetry 통합.

## 1. 핵심 변경 (3 신규 panel)

### 1.1 Panel 6 — `multi_agent_concurrent_write_conflict`

Phase 14 의 north-star metric. v0.14.0+ append-only layout 의 structural 정합성 검증 — sub-agent 2개+ 동시 fan-out 시 mutable 공유 파일의 3-way merge conflict / overwrite race 가 working tree 에 잔존하는지 자동 check.

- 측정원: `ai-workflow/memory/active/` 하위 working tree 의 git merge conflict marker (`<<<<<<<`) 검출
- 정합: `conflict_count: 0` + `status: 'pass'`
- `dashboard_data.collect_multi_agent_concurrent_write_conflict()` 신규
- `_render_panel_6()` markdown render — north_star / conflict_count / threshold / status / locations

### 1.2 Panel 7 — `deprecation_cycle_progress`

ADR-003 deprecation cycle 의 정공법 진행 상태 시각화. 4 release version 별 stage 명시:
- `v0.14.0`: 1st cycle 시작 (silent fallback)
- `v0.14.1`: 1st cycle 종결 (warning stage) — **current**
- `v0.14.5`: 2nd cycle 시작 (`--legacy-memory` opt-out flag)
- `v0.15.0`: 2nd cycle 종결 (`.bak` drop)

`bak_present: True` (현 status) + `next_release: 'v0.14.5'` + `deprecation_warning_supported: True`.

### 1.3 Panel 8 — `memory_index_utilization_v2`

기존 Panel 3 의 강화판. v0.13.1+ telemetry sidecar 의 실측값 통합. AC2 north-star: `telemetry_hit_rate` = (cue + bm25 + expansion hits) / total queries.

- `entries_total: 7` + `entries_by_merge_state: {active: 7}` (seed)
- `telemetry_events_total: 1` + `telemetry_by_source: {dispatcher: 1}`
- `telemetry_hit_rate: 1.0` (1 release ≥ 1 query+hit, AC2 north-star 정합)

## 2. Schema 변경

`dashboard_data.collect_dashboard_snapshot()`:
- `schema_version: '1.0'` → **`'1.1'`** (Phase 15 정합)
- `panels` dict 에 3 신규 entry:
  - `multi_agent_concurrent_write_conflict` (Panel 6)
  - `deprecation_cycle_progress` (Panel 7)
  - `memory_index_utilization_v2` (Panel 8)

`render_dashboard_markdown()`: 3 render 함수 호출 추가 (`_render_panel_6/7/8`).

## 3. 신규 smoke

`tests/check_phase15_dashboard_panels.py` (4 case):
- case_1_panel_6_conflict: `conflict_count=0` + `status='pass'` + `schema_version='1.1'`
- case_2_panel_7_deprecation: `stage='v0.14.1'` + `bak_present=True` + timeline 4 entry
- case_3_panel_8_memory_index: entries_total + telemetry_events_total + hit_rate 0.0~1.0 정합
- case_4_all_8_panels_present: 8 panel 모두 snapshot.panels 에 포함 (drift 1-5 + phase15 6-8)

## 4. 검증 (8 smoke 모두 PASS)

| smoke | 결과 |
|---|---|
| drift prevention 6/6 | ✅ |
| memory_lint 4/4 | ✅ |
| memory_freeze_lint | ✅ |
| appendonly_memory_layout 6/6 + WARN 1 | ✅ |
| git_history_summarizer 5/5 | ✅ |
| smart_context_reader 5/5 | ✅ |
| apply_robust_patch 5/5 | ✅ |
| **phase15_dashboard_panels 4/4** (신규) | ✅ |

**회귀 ❌, 누적 smoke 240+ PASS.**

## 5. Phase 15 north-star 종합 (Dashboard snapshot.md regen)

```
Panel 6: conflict_count: 0  (active/ 정합, north-star pass)
Panel 7: stage: v0.14.1     (1st cycle 종결, warning stage current)
Panel 8: hit_rate: 1.0      (telemetry AC2 north-star 정합)
```

3 panel 모두 정합. Phase 15 dashboard 의 north-star panel 3 종 모두 정상.

## 6. Out of scope (다음 release)

- HTML renderer (`render_dashboard_html`) 의 Panel 6/7/8 추가 — 현재는 markdown 만 갱신
- `cmd_refresh_maturity` dispatcher subcommand 추가 (helper 는 v0.14.0 에 존재)
- Panel 6 의 git reflog merge conflict log 통합 (현재는 working tree marker 만)
- Panel 7 의 timeline visual chart (현재는 markdown table)

Refs:
- [`../../../../workflow-source/core/maturity_matrix.json`](../../../../workflow-source/core/maturity_matrix.json) (v0.14.3 line)
- [`../../../../workflow-source/workflows/source/workflow_kit/common/dashboard_data.py`](../../../../workflow-source/workflow_kit/common/dashboard_data.py) (collect + render)
- [`../../../../workflow-source/tests/check_phase15_dashboard_panels.py`](../../../../workflow-source/tests/check_phase15_dashboard_panels.py) (4 case smoke)
- [`../../../../ai-workflow/dashboard/snapshot.md`](../../../../ai-workflow/dashboard/snapshot.md) (regen)

— generated 2026-07-16