# v0.14.5 (2nd deprecation cycle 시작 — `--legacy-memory` flag, 2026-07-16) — **SemVer patch**

**Phase 14 ADR-003 의 2nd cycle 진입.** release note 의 deprecation timeline:
- v0.14.0: 1st cycle 시작 (silent fallback) ✅
- v0.14.1: 1st cycle 종결 (warning stage) ✅
- **v0.14.5: 2nd cycle 시작 (`--legacy-memory` opt-out flag)** ← 본 release
- v0.15.0: 2nd cycle 종결 (`.bak` drop 예정)

## 1. 핵심 변경 (3 deliverable)

### 1.1 `cache.py:refresh_workflow_state_cache` 신규 kwarg

```python
def refresh_workflow_state_cache(
    *,
    project_profile_path: Path,
    ...
    legacy_memory: bool | None = None,  # v0.14.5+ 2nd cycle
):
    legacy_memory_effective = legacy_memory if legacy_memory is not None else True
    if legacy_memory_effective:
        # 1st cycle silent fallback (backward compat)
        resolved_session_handoff_path = (
            (session_handoff_path or (branch_dir / "session_handoff.md")).resolve()
            if (session_handoff_path or (branch_dir / "session_handoff.md")).exists()
            else None
        )
        resolved_work_backlog_index_path = (
            (work_backlog_index_path or (memory_dir / "work_backlog.md")).resolve()
            if (work_backlog_index_path or (memory_dir / "work_backlog.md")).exists()
            else None
        )
    else:
        # 2nd cycle strict opt-out — caller 가 명시한 legacy path 만 resolve.
        resolved_session_handoff_path = session_handoff_path.resolve() if session_handoff_path else None
        resolved_work_backlog_index_path = work_backlog_index_path.resolve() if work_backlog_index_path else None
```

**3 시나리오**:
- `legacy_memory=None` (default) → True (1st cycle backward compat). 기존 caller 변경 ❌.
- `legacy_memory=True` (explicit opt-in) → 1st cycle 동작 유지.
- `legacy_memory=False` (explicit opt-out) → 2nd cycle strict. silent fallback 무효.

### 1.2 deprecation_warnings 2 cycle 동시 emit

```python
if legacy_bak.exists():
    if legacy_memory_effective:
        deprecation_cycle = "1st cycle 종결 (warning stage)"
        # 1st cycle warning (v0.14.1)
        deprecation_warnings.append(
            "[DEPRECATION WARNING v0.14.1] ... v0.14.5 부터 --legacy-memory opt-out flag 필요 ..."
        )
    else:
        deprecation_cycle = "2nd cycle 진행 (strict opt-out)"
        # 2nd cycle ALERT (v0.14.5)
        deprecation_warnings.append(
            "[DEPRECATION ALERT v0.14.5] ... 2nd cycle 진행 — silent fallback 무효. v0.15.0 에서 .bak 완전 drop."
        )
```

return dict 에 `deprecation_cycle` + `legacy_memory` 2 field 추가 (Panel 7 dashboard 동적 stage 표시용).

### 1.3 `generate_workflow_state.py` CLI flag 추가

```python
parser.add_argument(
    "--legacy-memory", dest="legacy_memory", action="store_true", default=None,
    help="v0.14.5+ 2nd cycle: enable legacy fallback (work_backlog.md.bak). default True.",
)
parser.add_argument(
    "--no-legacy-memory", dest="legacy_memory", action="store_false",
    help="v0.14.5+ 2nd cycle: strict opt-out (legacy fallback OFF).",
)
```

argparse BooleanOptionalAction pattern — `--legacy-memory` / `--no-legacy-memory` 둘 다 지원. default None → cache.py 의 `legacy_memory if legacy_memory is not None else True` 가 True 로 처리.

## 2. maturity_matrix 정합

신규 top-level field:
```json
{
  "deprecation_cycle_stage": "v0.14.5",
  ...
}
```

v0.14.5 release 시 Panel 7 의 `collect_deprecation_cycle_progress` 가 `maturity_matrix.deprecation_cycle_stage` 를 read → `declared_stage` field + `stage` field 자동 결정. **다음 v0.15.0 release 시 `v0.15.0` 으로 update 만 하면 Panel 7 가 자동 표시**.

## 3. Panel 7 (Dashboard) 동적 stage 표시

`collect_deprecation_cycle_progress`:
- `bak_present=True` + `legacy_present=False` → `stage` = `declared_stage` (default 'v0.14.1', v0.14.5+ 'v0.14.5')
- 둘 다 부재 → `stage: v0.15.0` (complete)
- `legacy_present=True` + `bak_present=False` → `stage: v0.14.0` (migration 미실행)

Dashboard snapshot.md Panel 7 (현 status):
```
- stage: `v0.14.5`
- declared_stage: `v0.14.5`
- bak_present: `True`
- legacy_present: `False`
- next_release: `v0.15.0`
```

## 4. Smoke 4 case 정합

`tests/check_deprecation_cycle_v0_14_5.py` (4 case):
- `case_1_legacy_memory_with_bak`: `--legacy-memory` 명시 + .bak 존재 → refresh 성공 + 1st cycle warning
- `case_2_no_legacy_memory_with_bak`: `--legacy-memory` 미명시 (default) + .bak 존재 → refresh 성공 (default True 동작)
- `case_3_no_legacy_path`: .bak 부재 시 정상 refresh
- `case_4_no_legacy_memory_explicit`: `--no-legacy-memory` 명시 시 → strict opt-out

**4/4 PASS.**

## 5. 검증 (9 smoke 모두 PASS, 회귀 ❌)

| smoke | 결과 |
|---|---|
| drift prevention 6/6 | ✅ |
| memory_lint 4/4 | ✅ |
| memory_freeze_lint | ✅ |
| appendonly_memory_layout 6/6 + WARN 1 | ✅ |
| git_history_summarizer 5/5 | ✅ |
| smart_context_reader 5/5 | ✅ |
| apply_robust_patch 5/5 | ✅ |
| phase15_dashboard_panels 4/4 | ✅ (Panel 7 declared_stage 동적 정합) |
| **deprecation_cycle_v0_14_5 4/4** (신규) | ✅ |

## 6. Deprecation timeline 진행 상태

| 버전 | 상태 | 현재 |
|---|---|---|
| v0.14.0 | 1st cycle 시작 (silent fallback) | ✅ closed |
| v0.14.1 | 1st cycle 종결 (warning stage) | ✅ closed |
| **v0.14.5** | **2nd cycle 시작 (--legacy-memory opt-out)** | ✅ **done** |
| v0.15.0 | 2nd cycle 종결 (`.bak` drop) | ⏳ next quarter |

## 7. Out of scope (v0.15.0)

- `legacy_memory` kwarg 강제 `False` default → silent fallback 완전 disable.
- `work_backlog.md.bak` 완전 drop.
- maturity_matrix `deprecation_cycle_stage: 'v0.15.0'` 갱신.
- Panel 7 `stage: v0.15.0` (complete) 자동 표시.

Refs:
- [`../../../../workflow-source/core/maturity_matrix.json`](../../../../workflow-source/core/maturity_matrix.json) (`deprecation_cycle_stage` top-level field)
- [`../../../../workflow-source/core/skill_beta_criteria.md`](../../../../workflow-source/core/skill_beta_criteria.md)
- [`../../../../workflow-source/MEMORY_GOVERNANCE.md`](../../../../workflow-source/MEMORY_GOVERNANCE.md) §4 (Freeze 규칙)
- [`../../../../workflow-source/workflow_kit/common/state/cache.py`](../../../../workflow-source/workflow_kit/common/state/cache.py) (`legacy_memory` kwarg)
- [`../../../../workflow-source/scripts/generate_workflow_state.py`](../../../../workflow-source/scripts/generate_workflow_state.py) (`--legacy-memory` / `--no-legacy-memory` flag)

— generated 2026-07-16