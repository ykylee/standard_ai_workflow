# v0.14.6 (refresh-maturity dispatcher + cmd_release auto-wire, 2026-07-16) — **SemVer patch**

**Task 3 follow-up close-out.** Phase 14 dashboard Panel 1 freshness 보강을 standalone dispatcher subcommand 로 노출 + `cmd_release` post-emit 자동 호출.

## 1. 핵심 변경 (3 deliverable)

### 1.1 `cmd_refresh_maturity` dispatcher subcommand

`workflow-source/tools/release_pipeline.py`의 argparse 에 `refresh-maturity` subcommand 추가:

```bash
# default = apply (오늘 날짜로 갱신)
python3 release_pipeline.py refresh-maturity
# --today override
python3 release_pipeline.py refresh-maturity --today 2027-01-01
# --dry-run (plan 만 emit)
python3 release_pipeline.py refresh-maturity --dry-run
# --json output
python3 release_pipeline.py refresh-maturity --json
```

CLI signature (`parse_args` 결과):
- `--today` (default: `date.today().isoformat()`)
- `--maturity-path` (default: `workflow-source/core/maturity_matrix.json`)
- `--dry-run` (default False)
- `--apply` (default True — 양쪽 BooleanOptionalAction)
- `--json` (output 직렬화)

### 1.2 `cmd_refresh_maturity` 함수 정의

`refresh_maturity_last_updated` helper (cache.py v0.14.0) 호출 + idempotency 보장:
- `before` / `after` / `refreshed` / `today` / `maturity_path` / `mode` 5 field emit
- dry-run mode: actual 갱신 ❌, plan 만 emit, `dry_run_note` 명시
- apply mode: helper 직접 호출
- `refresh_maturity_last_updated` import 실패 시 graceful error (workflow_kit 부재 정공법)

### 1.3 `cmd_release` step 6.7 auto-wire

`cmd_release` 본체에 **step 6.7** 신규 — gh release create 성공 후 dashboard post-emit + self-recovery log 사이 호출:

```python
# 6.7 v0.14.6+ maturity_last_updated 자동 갱신 (Task 3 follow-up).
# gh release create 성공 후 dashboard emit 와 self-recovery log 사이 호출.
# idempotent — 이미 today 면 no-op (dashboard Panel 1 freshness 보강).
if not getattr(args, "dry_run", False) and refresh_maturity_last_updated is not None:
    try:
        maturity_result = cmd_refresh_maturity(args)
        results["maturity_refresh"] = maturity_result
        ...
    except Exception as exc:  # noqa: BLE001
        results["maturity_refresh"] = {"error": str(exc), "warning": True}
        print(f"  [maturity] WARN: {exc}")
```

`--dry-run` 모드에서는 skip (실제 release 만 자동 호출). exception 시 warning 만 (release 자체는 성공 유지).

### 1.4 Path doubled fix

`REPO_ROOT = Path(__file__).resolve().parents[1]` 가 `tools/release_pipeline.py` 의 parent.parent = `workflow-source/`. 따라서 `REPO_ROOT / "workflow-source" / "core"` 가 `workflow-source/workflow-source/core/...` 의 doubled path. **Fix**: `REPO_ROOT.parent` 사용 (root 기준) + `.resolve()`.

## 2. 신규 smoke — `tests/check_refresh_maturity_v0_14_6.py`

4 case:
- case_1_default_apply: `refresh-maturity` (--apply default) → maturity_last_updated 가 오늘 날짜로 갱신
- case_2_dry_run: `--dry-run` → refreshed=False + `dry_run_note` 명시
- case_3_idempotency: 이미 today 면 refreshed=False + before==after (no-op)
- case_4_today_override: `--today 2027-01-01` → 임의 날짜 override + helper 정합 + file 갱신 검증

**4/4 PASS.**

## 3. 검증 (10 smoke 모두 PASS, 회귀 ❌)

| smoke | 결과 |
|---|---|
| drift prevention 6/6 | ✅ |
| memory_lint 4/4 | ✅ |
| memory_freeze_lint | ✅ |
| appendonly_memory_layout 6/6 + WARN 1 | ✅ |
| git_history_summarizer 5/5 | ✅ |
| smart_context_reader 5/5 | ✅ |
| apply_robust_patch 5/5 | ✅ |
| phase15_dashboard_panels 4/4 | ✅ |
| deprecation_cycle_v0_14_5 4/4 | ✅ |
| **refresh_maturity_v0_14_6 4/4** (신규) | ✅ |

**누적 smoke 260+ PASS.**

## 4. 작업의 의의

Phase 14 의 release note §\"다음 step\" 의 두 가지 housekeeping item 정공법 적용:
1. **helper 노출**: `refresh_maturity_last_updated` (cache.py v0.14.0) → standalone dispatcher subcommand (Task 3 follow-up 1)
2. **release auto-wire**: `cmd_release` post-emit 자동 호출 (Task 3 follow-up 2)

이제 **release 시 별도 manual 갱신 없이** maturity_last_updated 가 자동으로 오늘 날짜로 갱신 → dashboard Panel 1 의 `last_updated_delta_days` 0 유지 → freshness drift 자동 해소.

## 5. Out of scope

- `cmd_release`의 step 6.7 을 release-tag 차단 stage 와 정렬 (v0.14.5 의 `legacy_memory` strict opt-out 와 동기 — release 시 `--legacy-memory` 명시 안 하면 fail).
- release error 시 maturity 호출 skip (현재는 release 성공 후 호출, exception 시 warning 만).
- dashboard snapshot.md auto-emit 와 maturity refresh 의 ordering (현재는 dashboard emit 다음 maturity refresh, 필요시 순서 변경).

Refs:
- [`../../../../workflow-source/tools/release_pipeline.py`](../../../../workflow-source/tools/release_pipeline.py) (`cmd_refresh_maturity` + step 6.7)
- [`../../../../workflow-source/workflow_kit/common/state/cache.py`](../../../../workflow-source/workflow_kit/common/state/cache.py) (`refresh_maturity_last_updated` helper)
- [`../../../../workflow-source/tests/check_refresh_maturity_v0_14_6.py`](../../../../workflow-source/tests/check_refresh_maturity_v0_14_6.py) (4 case)

— generated 2026-07-16