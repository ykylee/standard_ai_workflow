# v0.14.7 (Phase 15 follow-up — HTML renderer Panel 6/7/8 + git reflog, 2026-07-16) — **SemVer patch**

**Phase 15 dashboard 의 8 panel HTML renderer 완전 정합 + Panel 6 의 git reflog 통합.**

## 1. 핵심 변경 (3 deliverable)

### 1.1 Panel 6 — git reflog 통합 (`collect_multi_agent_concurrent_write_conflict`)

기존: working tree 의 `<<<<<<<` marker scan 만. v0.14.7+ 에서 **git history 까지 확장**:

```python
# subprocess `git log --all --merges --pretty=format:'%H %s'` + CONFLICT keyword filter
proc = _subprocess.run(
    ["git", "log", "--all", "--merges", "--pretty=format:%H %s"],
    cwd=str(root), capture_output=True, text=True, timeout=10, check=False,
)
if proc.returncode == 0:
    git_log_conflict_count = sum(
        1 for line in proc.stdout.splitlines()
        if "CONFLICT" in line.upper()
    )
```

`collect_multi_agent_concurrent_write_conflict` 의 output field 추가:
- `working_tree_conflict_count: int` (기존 marker scan)
- `git_log_conflict_count: int` (신규 git log 통합)
- `conflict_count: int` = working + git_log (combined)
- 기존 `conflict_locations` + `status` + `threshold` 유지

Timeout 10s, `git` 부재 / network error 시 graceful fallback (0 반환). 

### 1.2 `_render_html_panel_6/7/8` 신규

**Panel 6 HTML** — multi-agent concurrent write conflict:
- north_star / conflict_count / status / threshold
- breakdown: `working_tree={N} · git_log={M}` (v0.14.7+ 신규)
- conflict_locations: `<ul>` of `<code>` (max 10)

**Panel 7 HTML** — deprecation cycle progress:
- stage / declared_stage / bak_present / legacy_present / deprecation_warning_supported / next_release
- timeline: `<table>` with `<th>Version</th><th>Stage</th>` + `← **current**` marker

**Panel 8 HTML** — memory index + telemetry v2:
- phase_15_north_star / entries_total
- telemetry_events_total / queries / hits / hit_rate
- by_merge_state / by_source 2 table

### 1.3 `render_dashboard_html` panels 순회 확장

```python
html.append(_render_html_panel_5(panels.get("recent_releases", {})))
# Phase 15 (v0.14.7+) — Panel 6/7/8 HTML render
html.append(_render_html_panel_6(panels.get("multi_agent_concurrent_write_conflict", {})))
html.append(_render_html_panel_7(panels.get("deprecation_cycle_progress", {})))
html.append(_render_html_panel_8(panels.get("memory_index_utilization_v2", {})))
html.append("  </main>")
```

`ai-workflow/dashboard/snapshot.html` regen (9884 bytes).

## 2. dashboard HTML snapshot.md (v0.14.7+ 정합)

```
Panel 6 — Multi-Agent Concurrent Write Conflict
- north_star: multi_agent_concurrent_write_conflict_count
- conflict_count: 0
- status: pass
- breakdown: working_tree=0 · git_log=0

Panel 7 — Deprecation Cycle Progress
- stage: v0.14.5
- declared_stage: v0.14.5
- bak_present: True
- next_release: v0.15.0
- timeline: v0.14.0 → v0.14.1 → v0.14.5 ←current → v0.15.0

Panel 8 — Memory Index + Telemetry Utilization v2
- phase_15_north_star: telemetry_hit_rate (1 release ≥ 1 query + hit)
- entries_total: 7
- telemetry_hit_rate: 1.0
- entries by merge_state: {active: 7}
- telemetry by source: {dispatcher: 1}
```

## 3. Smoke 정합

기존 `tests/check_phase15_dashboard_panels.py` 의 `case_1_panel_6_conflict` 에 v0.14.7+ 검증 추가:
- `working_tree_conflict_count: non-negative int`
- `git_log_conflict_count: non-negative int`

4/4 PASS 유지 (회귀 ❌).

## 4. 검증 (10 smoke 모두 PASS, 회귀 ❌)

| smoke | 결과 |
|---|---|
| drift prevention 6/6 | ✅ |
| memory_lint 4/4 | ✅ |
| memory_freeze_lint | ✅ |
| appendonly_memory_layout 6/6 + WARN 1 | ✅ |
| git_history_summarizer 5/5 | ✅ |
| smart_context_reader 5/5 | ✅ |
| apply_robust_patch 5/5 | ✅ |
| **phase15_dashboard_panels 4/4** (Panel 6 git reflog 정합) | ✅ |
| deprecation_cycle_v0_14_5 4/4 | ✅ |
| refresh_maturity_v0_14_6 4/4 | ✅ |

## 5. Out of scope (v0.15.0 또는 별도)

- `git reflog --grep='CONFLICT'` 직접 grep 통합 (현재는 log --merges + keyword filter). 더 정밀한 conflict 이력.
- Chart.js Panel 6/7/8 chart 추가 (현재는 static table/list 만).
- HTML Panel 6 CSS color coding (status='pass'/'fail' 별 색상).
- `dashboard_data.collect_multi_agent_concurrent_write_conflict` 의 `git_log` subprocess output caching (현재 매번 subprocess 호출).

Refs:
- [`../../../../workflow-source/workflow_kit/common/dashboard_data.py`](../../../../workflow-source/workflow_kit/common/dashboard_data.py) (HTML render + git reflog)
- [`../../../../workflow-source/tests/check_phase15_dashboard_panels.py`](../../../../workflow-source/tests/check_phase15_dashboard_panels.py) (4 case 정합)
- [`../../../../ai-workflow/dashboard/snapshot.html`](../../../../ai-workflow/dashboard/snapshot.html) (regen)

— generated 2026-07-16