# v0.15.0 (**⚠️ breaking change**, 2nd deprecation cycle 종결 — `work_backlog.md.bak` 완전 drop, 2026-07-16) — **SemVer minor**

**Phase 14 ADR-003 deprecation cycle 의 최종 cycle (v0.15.0).** 본 release 는 **breaking change**: `.bak` drop + silent fallback 완전 disable.

## 1. 핵심 변경 (1 deliverable — breaking)

### 1.1 `work_backlog.md.bak` 완전 drop

`git rm ai-workflow/memory/active/work_backlog.md.bak` — `.bak` 의 1st/2nd cycle silent fallback 완전 disable (breaking).

**Pre-v0.15.0 동작**:
- `cache.py:refresh_workflow_state_cache` 가 branch_dir 하위 `session_handoff.md` 또는 memory_dir 하위 `work_backlog.md` 자동 resolve (silent)
- legacy caller 의 handoff/index data 가 그대로 `payload` 에 포함되어 state.json 에 emit

**v0.15.0+ 동작**:
- `.bak` 부재 → `legacy_bak.exists()` False → deprecation_warnings emit 안 됨
- legacy path 자동 resolve branch 가 active directory 의 `.bak` 부재로 silent fallback 안 함
- explicit path 또는 `legacy_memory=True` / `--legacy-memory` 명시 시에만 legacy fallback 동작

### 1.2 maturity_matrix 정합

`deprecation_cycle_stage: 'v0.14.5'` → **`'v0.15.0'`** 갱신 → Panel 7 dashboard 의 timeline `current` marker 가 v0.15.0 으로 자동 이동 (declared_stage 기반 동적 정합).

## 2. Panel 7 dashboard 자동 정합

`collect_deprecation_cycle_progress` 의 file state 결정:
- `.bak` 부재 + `work_backlog.md` 부재 → `stage: 'v0.15.0'` (complete)
- `next_release: '(complete)'` 자동 표시

`snapshot.md` Panel 7 (regen):
```
- stage: v0.15.0
- declared_stage: v0.15.0
- bak_present: False
- legacy_present: False
- next_release: (complete)

### Timeline

| Version | Stage | |
|---|---|---|
| v0.14.0 | 1st cycle 시작 (silent fallback) | |
| v0.14.1 | 1st cycle 종결 (warning stage) | |
| v0.14.5 | 2nd cycle 시작 (--legacy-memory opt-out flag) | |
| v0.15.0 | 2nd cycle 종결 (.bak drop) | ← **current** |
```

## 3. cache.py docstring + warning logic 정합

`cache.py` 의 `deprecation_warnings` emit block 의 docstring 갱신:
- pre-v0.15.0: 1st/2nd cycle 통합 warning logic (`legacy_bak.exists()` 기반 emit)
- **v0.15.0: `.bak` drop 으로 `legacy_bak.exists()` False → 자동 no-op. silent fallback 자동 disable.**

코드 자체 변경 ❌ — docstring 만 갱신. behavior 자동 정합.

## 4. Breaking change 상세

**Affected callers** (silent fallback 사용하던 모든 entry point):
- `cache.py:refresh_workflow_state_cache` — branch_dir 자동 include 무효
- `state/builder.py:build_workflow_state_payload` — caller 가 명시한 path 만 사용
- `scripts/generate_workflow_state.py` — `--legacy-memory` flag 미명시 시 legacy fallback OFF
- `tools/release_pipeline.py:refresh-maturity` — 영향 없음 (maturity 만 다룸)

**Migration 가이드** (affected caller 의 경우):
1. `legacy_memory=True` 또는 `--legacy-memory` flag 명시 (legacy fallback 의도 표시)
2. 또는 explicit `session_handoff_path` / `work_backlog_index_path` 명시 (1st cycle 정공법)
3. 또는 `.bak` 미존재 정공법 (v0.15.0+ 권장) — 신규 layout 사용 권장

## 5. 검증 (10 smoke 모두 PASS, 회귀 ❌)

| smoke | 결과 |
|---|---|
| drift prevention 6/6 | ✅ |
| memory_lint 4/4 | ✅ |
| memory_freeze_lint | ✅ |
| **appendonly_memory_layout 6/6 + WARN 0** (`.bak` drop 후 WARNINGS 빈 list) | ✅ |
| git_history_summarizer 5/5 | ✅ |
| smart_context_reader 5/5 | ✅ |
| apply_robust_patch 5/5 | ✅ |
| **phase15_dashboard_panels 4/4** (Panel 7 stage=v0.15.0 자동 정합) | ✅ |
| deprecation_cycle_v0_14_5 4/4 | ✅ |
| refresh_maturity_v0_14_6 4/4 | ✅ |

**누적 smoke 260+ PASS.** Panel 7 snapshot regen 정합 (stage=v0.15.0, current marker auto).

## 6. ADR-003 deprecation cycle timeline — close-out

| 버전 | 상태 | 결과 |
|---|---|---|
| v0.14.0 | 1st cycle 시작 (silent fallback) | ✅ closed (`8c53c4a`) |
| v0.14.1 | 1st cycle 종결 (warning stage) | ✅ closed (`3afb9ef`) |
| v0.14.5 | 2nd cycle 시작 (--legacy-memory opt-out) | ✅ closed (`49ac3f7`) |
| **v0.15.0** | **2nd cycle 종결 (.bak drop)** | ✅ **complete** |
| v0.15.0+ | **ADR-003 cycle complete** | ⏳ next major 변경 시 cycle 3 시작 |

## 7. Out of scope (다음 release 또는 cycle 3 시작 시)

- `workflow-source/build/**` 의 legacy code 갱신 — auto-generated 라 영향 ❌
- `release_pipelines.py` 의 step 6.7 을 release-tag 차단 + legacy 정합 (v0.14.5 의 `--legacy-memory` 와 정합한 release policy)
- 별도 작업: root scaffold 실체화 (deepagents / fastmcp / langchain-openai 통합) — 별도 track
- Phase 16 (dashboard 8 panel + scheduler): Phase 15 close 후 별도 phase

Refs:
- [`../../../../workflow-source/MEMORY_GOVERNANCE.md`](../../../../workflow-source/MEMORY_GOVERNANCE.md) §3 (template 정합)
- [`../../../../workflow-source/core/maturity_matrix.json`](../../../../workflow-source/core/maturity_matrix.json) (`deprecation_cycle_stage: v0.15.0`)
- [`../../../../workflow-source/workflow_kit/common/state/cache.py`](../../../../workflow-source/workflow_kit/common/state/cache.py) (`legacy_bak` 분기 docstring 갱신)
- [`../../../../ai-workflow/dashboard/snapshot.md`](../../../../ai-workflow/dashboard/snapshot.md) (Panel 7 stage=v0.15.0 regen)
- [`../../../../ai-workflow/dashboard/snapshot.html`](../../../../ai-workflow/dashboard/snapshot.html) (Panel 7 정합 emit)

— generated 2026-07-16