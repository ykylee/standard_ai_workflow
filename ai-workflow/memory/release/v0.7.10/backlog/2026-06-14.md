# 2026-06-14 작업 백로그 (release/v0.7.10)

- 문서 목적: 2026-06-14 세션에서 release/v0.7.10 (main HEAD `67d4a37`) 의 release_pipeline Phase 2 (release / verify / rollback) 작업을 기록한다.
- 범위: TASK-V0710-001 (cmd_release) / TASK-V0710-002 (cmd_verify) / TASK-V0710-003 (cmd_rollback)
- 대상 독자: AI 에이전트, 저장소 maintainer
- 상태: done
- 최종 수정일: 2026-06-14
- 관련 문서: [`../../active/work_backlog.md`](../../active/work_backlog.md), [`../../active/state.json`](../../active/state.json)

## TASK-V0710-001 release (gh release create 통합)

- 상태: done
- 우선순위: high
- 영향: `tools/release_pipeline.py` (430 → 656 line) — `cmd_release` (130 line)
  - 사전 점검: `--skip-validate` 미지정 시 validate 4 source 자동 호출. 1+ fail 시 release 중단
  - dist 파일 glob: PEP 440 normalize (0.7.10 → `standard_ai_workflow-0.7.10*` glob)
  - tag + notes_file + dist glob → gh command build (dry-run 시 print, apply 시 실행)
  - gh auth status 자동 검증 (--apply 시). 미인증 시 graceful fail
  - repo remote 자동 추출 (`git remote get-url origin`)

## TASK-V0710-002 verify (gh release view, read-only)

- 상태: done
- 우선순위: high
- 영향: `cmd_verify` (50 line) — `gh release view`
  - `--json`: tag / name / url / isPrerelease / createdAt / assets / asset_count
  - `--dry-run`: gh_command 만 print. `--apply` 시 gh release view 호출. release 부재 시 graceful fail

## TASK-V0710-003 rollback (destructive)

- 상태: done
- 우선순위: high
- 영향: `cmd_rollback` (80 line) — 3 commands (local tag delete / remote tag delete / gh release delete --yes)
  - `--dry-run`: commands list print. `--apply` 시 순차 실행, 1+ fail 시 중단
  - destructive 작업의 `--dry-run` 명시 (Phase 1 의 `--apply` 정공법과 동일)
- 누적 test: +8 (총 127+)
- 비고: PyPI/TestPyPI 업로드 ❌ (memory #5 release 채널 정책)

### 완료 기준

- [x] Phase 2 3 subcommand 본 구현 (release / verify / rollback)
- [x] 8 신규 test PASS
- [x] 회귀 0 (8 check / 78 test PASS via run_all_checks)
- [x] GH release v0.7.10-beta 발행

### Reference

- `workflow-source/releases/Beta-v0.7.10.md`
- `tests/check_release_pipeline_phase2.py` (8 test)
