# v0.7.21 Backlog (2026-06-15)

본 release 의 TASK 정의. v0.7.18 의 release coordination observability 의 **design 결함** fix — `_check_remote_tag` 가 *release 시점* 에 *이미 push 된* tag 를 catch 하는 *의도와 다른* fail. 본 release 에서 `--allow-existing-tag` flag + `cmd_release` 의 **tag push 자동화** 로 coupling fix.

## TASK-V0721-001: cmd_release --allow-existing-tag flag + tag push 자동화

- **상태**: ✅ done
- **1차 출처**: v0.7.18~v0.7.20 의 auto-bump chain (3 release 의 tag push + pre-check fail cycle). 본 fix 의 *trigger*.
- **commit**: `eb7a79d`
- **범위**:
  - argparse `--allow-existing-tag` flag (caller 명시적 opt-in, default False)
  - `cmd_release` 의 step 3.5: pre-check fail 시 `--allow-existing-tag` 면 skip + 그대로 진행
  - `cmd_release` 의 step 3.6: `git push origin refs/tags/<tag>` 자동 실행 후 `gh release create --verify-tag` (coupling)
  - pre-check + tag push + gh release 가 **한 cycle**
  - dry-run 시 tag_pre_check 결과 + warning (v0.7.18 유지)
- **완료 기준**: 8/8 smoke test PASS (7 기존 + 1 신규)
- **Reference**:
  - `workflow-source/tools/release_pipeline.py` (~1100 line, v0.7.21 본 release)
  - `workflow-source/tests/check_release_pipeline_release_coordination.py` (~200 line, 8 test, 본 release)

---

## 누적 정합

- v0.7.18 의 3-step release coordination observability → **v0.7.21 의 3-step coupling fix**:
  1. pre-check (race catch) — v0.7.18 유지
  2. --allow-existing-tag (caller opt-in) — v0.7.21 신규
  3. tag push 자동화 (coupling fix) — v0.7.21 신규
- v0.7.18~v0.7.20 의 auto-bump chain 의 *3 release 의 tag* 가 remote 에 *고아* 로 존재 (의도된 pre-check fail 의 결과)
- v0.7.21 의 single clean release 가 본 fix 의 *의미*

## v0.7.22+ follow-up

- **3 legacy tag cleanup** (v0.7.21 발견) — v0.7.18/v0.7.19/v0.7.20 의 tag 가 remote 에 *고아* 로 존재. *re-push* 또는 *delete* 결정.
- **ci-publish subcommand** (Phase 5) — GH Actions / pre-push hook
- **Wiki 운영 cross-link** — `emit_wiki_l2_body.py` + `refresh_wiki_memory.py` 1-command 통합
- **`cmd_release --notes-template`** — GH release notes custom template
- **linter 의 symlink-resolve fix** (v0.7.16 발견) — `.resolve()` → `.absolute()`
- **legacy L2 → in-repo migration** (v0.7.17 발견) — `tools/migrate_vault_l2_to_inrepo.py`

## Reference

- [Beta-v0.7.21.md](../../../../workflow-source/releases/Beta-v0.7.21.md) (본 release)
- [Beta-v0.7.20.md](../../../../workflow-source/releases/Beta-v0.7.20.md) (직전) — auto-bump chain
- [Beta-v0.7.18.md](../../../../workflow-source/releases/Beta-v0.7.18.md) — release coordination observability
- [Beta-v0.7.16.md](../../../../workflow-source/releases/Beta-v0.7.16.md) — race 발견의 1차 출처
- [tests/check_release_pipeline_release_coordination.py](../../../../workflow-source/tests/check_release_pipeline_release_coordination.py) (~200 line, 8 test, 본 release)
- [state.json](../state.json) (recent_done_items 에 v0.7.21 추가, 30 entry)
