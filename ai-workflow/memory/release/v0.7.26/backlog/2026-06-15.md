# v0.7.26 Backlog (2026-06-15)

본 release 의 TASK 정의. check_workflow_linter 의 branch detection 정확도 + v0.7.25 의 infinite fix(state) loop 해결.

## TASK-V0726-001: F-7 branch detection fix (detached HEAD → short SHA)

- **상태**: in-flight
- **1차 출처**: v0.7.22 release note (linter symlink-resolve, 본 release 의 collateral 의 1차 출처) + v0.7.18 release note (destructive subcommand 정공법, 본 release 의 *graceful fail* 의 1차 출처)
- **commit**: TBD
- **범위**:
  - `workflow_kit/common/paths.py:get_current_branch()` (~15 line 추가) — detached HEAD (branch == "HEAD") 시 `git rev-parse --short=7 HEAD` 로 short SHA 반환
  - `tests/check_v0_7_26_branch_detection_fix.py` (5 smoke, 5/5 PASS): detached HEAD / normal / env override / unsafe env / git fail
- **정합성 검증**:
  - 5/5 smoke test PASS
  - 기존 `check_paths.py` 회귀 0
  - `BRANCH_NAME` module-level 의 main / worktree 정확

## TASK-V0726-002: F-7+ automated hash sync (infinite fix(state) loop 회피)

- **상태**: in-flight
- **1차 출처**: v0.7.25 release note (infinite fix(state) loop, 본 release 의 *automated hash sync* 의 1차 동기화 trigger) + v0.7.21 release note (--allow-existing-tag, 본 release 의 hash sync 의 *본 release 의 본 release 의 hash* 정공법) + v0.7.12 release note (4-priority REPO_ROOT, 본 release 의 REPO_ROOT auto-detect 의 1차 출처)
- **commit**: TBD
- **범위**:
  - `tools/sync_release_hash.py` (~180 line, 4-priority REPO_ROOT + 1 subcommand + 3 helper): `get_latest_commit_hash` (2-step git rev-parse) + `update_state_json` (TBD 또는 7-char hex 매치) + `update_backlog` (TBD 또는 7-char hex 매치)
  - `tests/check_v0_7_26_sync_release_hash.py` (5 smoke, 5/5 PASS): state.json / backlog / idempotent / dry-run / real repo
- **정합성 검증**:
  - 5/5 smoke test PASS
  - real repo 의 v0.7.25 entry (96a919d → 00e7ca8) sync verify
  - TBD placeholder 도 sync 대상
