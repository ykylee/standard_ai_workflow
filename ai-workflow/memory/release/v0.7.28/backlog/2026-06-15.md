# v0.7.28 Backlog (2026-06-15)

본 release 의 TASK 정의. F-7 (v0.7.26) 의 detached HEAD → short SHA fix 의 *closure* — age-based auto-archive.

## TASK-V0726-004: Detached HEAD memory dir age-based auto-archive

- **상태**: in-flight
- **1차 출처**: v0.7.17 release note (in-repo redirect, memory dir SSOT 의 1차 출처) + v0.7.26 release note (F-7 branch detection, 본 release 의 *cleanup target* 의 1차 출처) + v0.7.18 release note (destructive subcommand 정공법, 본 release 의 *file move* 의 정공법) + v0.7.25 release note (F-6 closure, 본 release 의 *SHA256 idempotency* 의 1차 출처)
- **commit**: 7bb6259
- **범위**:
  - `tools/archive_stale_memory.py` (~250 line): REPO_ROOT 4-priority + 3 subcommand (list / archive / cleanup) + 5 helper (is_short_sha_dir / get_dir_age_days / build_archive_path / check_already_archived / sha256_of_dir)
  - `tests/check_v0_7_28_archive_stale_memory.py` (5 smoke, 5/5 PASS): short SHA detect / age filter / already-archived skip / dry-run no-move / apply archives
- **정합성 검증**:
  - 5/5 smoke test PASS
  - 2-축 검증: 길이 (7자) + charset (`[a-f0-9]`) regex `^[a-f0-9]{7}$`
  - idempotency: path-based (`check_already_archived`) + content-based (`sha256_of_dir`) 의 *2-level* 검증
  - date-based archive path: `archive/<YYYY-MM-DD>/<sha>/` (time + sha 의 2-axis)
