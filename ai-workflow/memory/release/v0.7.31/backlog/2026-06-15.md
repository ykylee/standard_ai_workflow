# v0.7.31 Backlog (2026-06-15)

본 release 의 2 TASK 정의. v0.7.30 의 cron 통합의 *2 follow-up* — post-mortem 분석용 metrics log + cron install idempotency.

## TASK-V0729-001: Run-time metrics log

- **상태**: in-flight
- **1차 출처**: v0.7.30 release note (TASK-V0728-001 cron integration, 본 release 의 *cron integration 의 follow-up* 의 1차 출처) + v0.7.25 release note (F-6 closure, 본 release 의 *SHA256 idempotency* 의 1차 출처)
- **commit**: 6732f48
- **범위**:
  - `append_metrics_log` / `read_metrics_log` / `cmd_show_metrics` (3 helper, ~80 line)
  - `apply_metrics_log` 자동 호출 (`apply` mode 의 result 직전)
  - `--show-metrics` argparse flag
- **정합성 검증**:
  - 5/5 smoke test PASS (TASK-V0729-001 5)
  - entry format: `2026-06-15T13:00:00Z\tolder_than=30\tarchived=2\tskipped=1\terror=0\n`
  - error_count = sum(1 for s in skipped if "fail" in s.get("reason", ""))

## TASK-V0730-001: Install-cron idempotency

- **상태**: in-flight
- **1차 출처**: v0.7.18 release note (destructive subcommand 정공법, 본 release 의 *install-cron 의 idempotency skip* 의 정공법) + v0.7.21 release note (--allow-existing-tag, 본 release 의 *--force-install* flag 의 *caller opt-in* 정신의 1차 출처)
- **commit**: 6732f48
- **범위**:
  - `cmd_install_cron` 의 *idempotency check* (`mavis cron info <agent> <cronName>` + stdout `cronName` 매치) + `idempotency` field 추가
  - `--force-install` argparse flag
  - `check_already_archived` file catch (v0.7.28 의 *dst 가 file* 시나리오 보강)
- **정합성 검증**:
  - 5/5 smoke test PASS (TASK-V0730-001 5)
  - existing → skipped-existing (1 call only, no create)
  - missing → created (2 calls, info + create)
  - --force-install → forced (1 call, skip info check)
