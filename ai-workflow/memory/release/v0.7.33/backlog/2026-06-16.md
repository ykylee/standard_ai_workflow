# v0.7.33 Backlog (2026-06-16)

본 release 의 2 TASK 정의. v0.7.32 의 log rotation + metrics aggregation 의 *2 follow-up* — atomic crash safety + yearly period.

## TASK-V0733-001: Atomic rotation (3-step crash safety)

- **상태**: in-flight
- **1차 출처**: v0.7.32 release note (TASK-V0731-001 log rotation, 본 release 의 *2 follow-up* 의 1차 출처) + v0.7.18 release note (destructive subcommand 정공법, 본 release 의 *3-step atomic rotation* 의 *3-checklist* 의 1차 출처) + v0.7.25 release note (F-6 closure, 본 release 의 *idempotency 의 crash safety* 의 1차 출처)
- **commit**: TBD
- **범위**:
  - `rotate_log_if_needed` 의 3-step implementation (temp file + os.replace + truncate + cleanup-on-fail) (~50 line)
  - `os.replace` (POSIX/NTFS atomic rename) + `f.truncate(0)` (r+b mode)
- **crash safety 정합**:
  - step 1 mid-crash → *temp file* 만 partial. *main log* (full) + *archive* (없음) — main full 보존
  - step 2 mid-crash → `os.replace` atomic — *archive* (full) + *main log* (full) — *둘 다* 보존
  - step 3 mid-crash → *archive* (full) + *main log* (full) — *둘 다* 보존 (main log 의 *중복* 가능, *post-recovery* 의 *dedup* 필요)
- **정합성 검증**: 5/5 smoke test PASS, 5-run stable (50/50)

## TASK-V0734-001: Yearly aggregation

- **상태**: in-flight
- **1차 출처**: v0.7.32 release note (TASK-V0732-001 metrics aggregation, 본 release 의 *2 follow-up* 의 1차 출처)
- **commit**: TBD
- **범위**:
  - `aggregate_metrics` 의 *yearly* 분기 (`ts.strftime("%Y")`) + `cmd_aggregate_metrics` 의 *yearly* validation (~5 line)
- **정합성 검증**:
  - 5/5 smoke test PASS, 5-run stable
  - cross-year boundary (2025-12 + 2026-01) → 2 buckets
  - 4-축 정합: daily|weekly|monthly|yearly (format 일관성 `%Y` prefix)
