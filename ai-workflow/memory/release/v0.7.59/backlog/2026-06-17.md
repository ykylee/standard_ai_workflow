# v0.7.59 Backlog (2026-06-17)

본 release 의 1 TASK 정의. v0.7.58 의 consumer-metrics tool 의 *dispatcher in-process* 정합
— v0.7.55+ 의 release-doctor / v0.7.56+ 의 score-wiki-trend 와 동일 정공법으로 통일.

## TASK-V0759-001: cmd_consumer_metrics in-process refactor (subprocess → import_module)

- **상태**: in-flight
- **1차 출처**:
  - v0.7.58 release note (cmd_consumer_metrics 의 subprocess delegation 본 release 의 정합) §in-flight 발견 + fix §bug 2
  - v0.7.56 release note (score-wiki-trend 의 in-process refactor — 본 TASK 의 *정공법* 의 1차 출처)
  - v0.7.55 release note (release-doctor in-process — 본 TASK 의 *sys.path insert* 의 1차 출처)
  - workflow_kit_cli.py:436-478 (cmd_score_wiki_trend 의 *in-process wrapper template* 의 1차 출처)
- **commit**: TBD
- **범위**:
  - `workflow_kit/workflow_kit_cli.py:1037-1077` `cmd_consumer_metrics` 의 subprocess delegation → in-process wrapper 로 교체
  - *정공법 4종 일치*:
    1. `from pathlib import Path` 로 `workflow_source_dir` 산출
    2. `sys.path.insert(0, str(workflow_source_dir))` (idempotent)
    3. `importlib.import_module("tools.consumer_metrics")`
    4. `sys.argv = ["consumer_metrics", *argv]` patch + `try/finally` 로 restore
  - `except SystemExit` → `e.code if isinstance(e.code, int) else 2` 변환
  - `except Exception` → stderr + rc=2
  - `--repo` / `--days` / `--json` argparse forwarding 은 consumer_metrics.main() 의 자체 argparse 가 처리 (현재 이미 그 구조)
- **호환성**:
  - 외부 invocation: `python -m workflow_kit.workflow_kit_cli --command=consumer-metrics --days=14` (변동 없음)
  - exit code 0/1/2 동일
  - JSON / text output 동일
- **제거**:
  - `cmd_consumer_metrics` 의 `import subprocess as _sp` 라인
  - `from pathlib import Path as _P` 중복 (이미 위에서 import)
  - `metrics_path.exists()` 체크 (in-process 라 import 시점에 fail)
  - `_sp.run(...)` + `sys.stdout.write(proc.stdout)` 등 subprocess plumbing
- **정합성 검증**:
  - `check_workflow_kit_cli.py`: `test_consumer_metrics_registered_v0_7_58` / `test_consumer_metrics_invalid_days_returns_2_v0_7_58` / `test_consumer_metrics_default_argv_v0_7_58` 모두 PASS 유지
  - `check_consumer_metrics.py`: 6/6 PASS 유지
  - 신규 smoke 1~2개 (in-process 임을 검증):
    - `test_cmd_consumer_metrics_no_subprocess_v0_7_59`: dispatcher argv 에 `consumer-metrics --days=7` 호출 시 rc=0/1 (gh auth 의존) + consumer_metrics 의 import 가 in-process 임을 확인 (psutil optional, 또는 `sys.modules` 에서 `tools.consumer_metrics` 존재 확인)
    - `test_cmd_consumer_metrics_argv_forwarded_v0_7_59`: dispatcher 의 argv 가 consumer_metrics.main() 의 argparse 까지 도달 (default repo = ykylee/standard_ai_workflow)
  - cumulative 5 module test 98 PASS (변동 없음)
  - cumulative dispatcher test 41 → 42~43 PASS (+1~2 신규)
- **follow-up (v0.7.60+)**:
  - v0.7.60 E. 5 module audit 4차 (path_resolver / phishing_keywords 정합)
  - v0.7.60 C. dispatcher 28+ (cache-lru-decay / cache-lfu-decay-persist / cache-merge-csv)
  - v0.7.61 F. mkdocs `--strict` 진짜 활성화
  - v0.7.62 B + D. consumer-metrics trend snapshot + weekly digest
  - v0.8.0 J. stable API freeze + mypy strict + PyPI + generated schema SSOT
