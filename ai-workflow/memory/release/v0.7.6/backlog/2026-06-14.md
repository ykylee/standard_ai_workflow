# 2026-06-14 작업 백로그 (release/v0.7.6)

- 문서 목적: 2026-06-14 세션에서 release/v0.7.6 (main HEAD `b9ede19`) 의 run_all_checks 통합 runner + pyproject metadata 작업을 기록한다.
- 범위: TASK-V076-001 (run_all_checks 통합) / TASK-V076-002 (workflow_kit.metadata)
- 대상 독자: AI 에이전트, 저장소 maintainer
- 상태: done
- 최종 수정일: 2026-06-14
- 관련 문서: [`../../active/work_backlog.md`](../../active/work_backlog.md), [`../../active/state.json`](../../active/state.json)

## TASK-V076-001 run_all_checks 통합 runner

- 상태: done
- 우선순위: high
- 영향: `tests/run_all_checks.py` (230 line) — argparse 5 flag (`--tests-dir` / `--filter` / `--fail-fast` / `--timeout` / `--json`)
  - `discover_checks` glob + substring OR match
  - `run_one` subprocess + timeout + stdout/stderr 캡처 + duration 측정
  - `aggregate` → `RunSummary` (total / passed / failed / total_passed_tests / total_failed_tests / total_duration_sec)
- 누적 test: +10 (총 95+)
- 비고: Python 3.14 dataclass + `KW_ONLY` 의 importlib 명시 등록 fix 1회 적용 (memory #3 dry-run → fix → apply 패턴)

## TASK-V076-002 workflow_kit.common.metadata (pyproject loader)

- 상태: done
- 우선순위: high
- 영향: `workflow_kit/common/metadata.py` (162 line) — `DoctorConfig` dataclass (5 field)
  - `partial_rules: dict[str, list[str]]` — baseline 별 hard constraint rule
  - `opt_in: dict[str, list[str]]` — baseline 별 opt-in rule
  - `thresholds: dict[str, float]` — dim 별 alert (score_alert 0.3 / memory_alert_mb 100)
  - `excluded_paths: list[str]` — lint skip glob
  - `fail_on: str` — CI threshold (compliant | advisory | non_compliant)
- `load_config()`: 운영 안정성 (default fallback, 절대 실패 안 함)
- 누적 test: +10 (총 95+)
- 비고: v0.7.7 의 doctor integration 의 1차 source

### 완료 기준

- [x] 77+ check_*.py 통합 runner
- [x] pyproject.toml `[tool.workflow-doctor]` loader
- [x] 20 신규 test PASS (run_all_checks 10 + metadata 10)
- [x] GH release v0.7.6-beta 발행

### Reference

- `workflow-source/releases/Beta-v0.7.6.md`
- `tests/check_run_all_checks.py` (10 test)
- `tests/check_metadata.py` (10 test)
