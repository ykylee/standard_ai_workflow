# v0.7.60 Backlog (2026-06-17)

본 release 의 2 TASK 정의. v0.7.58 의 follow-up 2건 동시 해결: (1) 5 module audit 4차
(path_resolver + phishing_keywords 정합) + (2) dispatcher 27 → 28
(cache-lfu-decay-persist).

## TASK-V0760-001: 5 module audit 4차 (path_resolver + phishing_keywords 정합)

- **상태**: done
- **1차 출처**:
  - v0.7.58 release note (다음 항목 §4) — 5 module audit 4차 의 1차 출처
  - v0.7.56 release note (audit 3차 = okf_import strict mode lint rule coverage +7) — 본 TASK 의 *audit 정공법* 의 1차 출처
  - workflow_kit/path_resolver.py (ADR-008 in-repo path → canonical GitHub URL + ADR-018 commit/ref pinned) — 본 TASK 의 *정합 대상 module 1* 의 1차 출처
  - workflow_kit/phishing_keywords.py (V-R11 phishing keyword list, ADR-023 PhishTank + OpenPhish federation) — 본 TASK 의 *정합 대상 module 2* 의 1차 출처
- **commit**: TBD
- **범위**:
  - 신규 `tests/check_path_resolver.py` (12 test):
    1. `test_path_safe_accepts_v0_7_60` — `_is_path_safe` 가 valid in-repo path accept
    2. `test_path_safe_rejects_unsafe_v0_7_60` — `../`, `/`, embedded `..`, empty reject
    3. `test_normalize_origin_ssh_to_https_v0_7_60` — `git@github.com:foo/bar.git` → HTTPS form
    4. `test_normalize_origin_https_passthrough_v0_7_60` — `.git` suffix 제거, empty / invalid → None
    5. `test_detect_origin_ci_env_v0_7_60` — `GITHUB_SERVER_URL` + `GITHUB_REPOSITORY` 우선
    6. `test_detect_origin_git_config_v0_7_60` — `git config --get remote.origin.url` fallback
    7. `test_detect_branch_local_fallback_v0_7_60` — symbolic-ref fail 시 `git branch --show-current`
    8. `test_detect_branch_fallback_main_v0_7_60` — git 없으면 "main" deepest fallback
    9. `test_resolve_url_passthrough_v0_7_60` — http(s):// prefix 는 그대로 반환
    10. `test_resolve_in_repo_path_v0_7_60` — canonical GitHub blob URL build (default branch 동적 산출)
    11. `test_resolve_pinned_commit_v0_7_60` — commit SHA immutable URL
    12. `test_resolve_pinned_ref_v0_7_60` — ref (branch/tag) URL + invalid SHA → None
  - 신규 `tests/check_phishing_keywords.py` (8 test):
    1. `test_bundled_keywords_v0_7_60` — V-R11 8 baseline keywords (lowercase, non-empty)
    2. `test_load_phishing_keywords_no_args_v0_7_60` — bundled + custom iterable
    3. `test_load_external_feed_jsonl_v0_7_60` — `_load_external_feed` case-preserve, no dedup
    4. `test_load_phishing_keywords_with_external_dedup_v0_7_60` — caller-side dedup
    5. `test_feed_status_no_path_v0_7_60` — `None` → bundled-only diagnostic
    6. `test_feed_status_with_file_v0_7_60` — existing JSONL file → size + count
    7. `test_fetch_phishtank_empty_on_error_v0_7_60` — invalid API key → offline-safe `[]`
    8. `test_fetch_openphish_empty_on_error_v0_7_60` — network live or fail → `[]` OR valid URL list
- **정합성 검증**:
  - 12/12 path_resolver PASS
  - 8/8 phishing_keywords PASS
  - cumulative 5 module test 98 → **119 PASS** (+21, audit 4차 = +20 + dispatcher 신규 = +1)
- **follow-up (v0.7.61+)**:
  - audit 5차: `cache_lfu_decay` + `phishing_federation` (1차 출처: v0.7.45 ADR-023 follow-up)
  - audit 5차: `okf_export` 의 loose mode 가 strict 대비 어떤 error 를 warn 으로 downgrade 하는지 (현재 strict 만 7 test)

## TASK-V0760-002: dispatcher 27 → 28 (cache-lfu-decay-persist in-process)

- **상태**: done
- **1차 출처**:
  - v0.7.58 release note (다음 항목 §5) — dispatcher 28+ (cache-lru-decay / cache-lfu-decay-persist / cache-merge-csv) 의 1차 출처
  - v0.7.55 release note (release-doctor in-process 정공법)
  - v0.7.56 release note (score-wiki-trend in-process 정공법 + release_pipeline_lib wrapper)
  - v0.7.59 release note (consumer-metrics in-process 정공법)
  - workflow_kit/cache_lfu_decay_persist.py (v0.7.49+ per-URL LFU decay score persistence)
  - tools/release_pipeline_lib.py (v0.7.55+ in-process wrapper hub, 현재 8 wrapper)
- **commit**: TBD
- **범위**:
  - `tools/release_pipeline_lib.py` 의 신규 wrapper `cmd_lfu_decay_persist(url, score, scores_path, apply)`:
    - `apply=False` (default) → dry-run, write 안 함, JSON `{mode: dry-run, url, score, scores_path, scores_count, current_count}`
    - `apply=True` → `load_decay_scores` → `existing[url] = score` → `save_decay_scores` → JSON `{mode: applied, ...}`
  - `workflow_kit/workflow_kit_cli.py` 의 subcommand 28 `cache-lfu-decay-persist`:
    - 인자: `--url=URL` (required), `--score=FLOAT` (required), `--scores-path=PATH` (default `cache/lfu_decay_scores.json`), `--apply`, `--json`
    - in-process pattern (sys.path insert + importlib + sys.argv patch)
    - exit code: 0 (success), 2 (usage error)
  - score file format: `{"version": 1, "saved_at": ..., "scores": {url: float, ...}}` (wrapped, v0.7.49+ `save_decay_scores` 가 wrap)
- **호환성**:
  - `python -m workflow_kit.workflow_kit_cli --command=cache-lfu-decay-persist --url=URL --score=FLOAT` (default dry-run, no write)
  - `python -m workflow_kit.workflow_kit_cli --command=cache-lfu-decay-persist --url=URL --score=FLOAT --apply` (write)
- **제거**:
  - 없음 (신규 subcommand, 기존 영향 없음)
- **정합성 검증**:
  - `check_workflow_kit_cli.py`: 4/4 신규 PASS
    - `test_cache_lfu_decay_persist_registered_v0_7_60` — subcommand 28 등록 확인
    - `test_cache_lfu_decay_persist_missing_args_v0_7_60` — `--url` / `--score` 부재, `--score=not-a-number` → rc=2
    - `test_cache_lfu_decay_persist_dry_run_v0_7_60` — dry-run 시 file unchanged (wrapped format)
    - `test_cache_lfu_decay_persist_apply_v0_7_60` — `--apply` 시 file 의 `data["scores"][url]` 에 score 기록
  - cumulative dispatcher test 43 → **47 PASS** (+4)
- **follow-up (v0.7.61+)**:
  - dispatcher 29 = `cache-lru-decay` (LRU-only cache 의 age-aware eviction 점수)
  - dispatcher 30 = `cache-merge-csv` (multi-CSV merge tool)
  - 둘 다 v0.7.58 의 follow-up §5 (dispatcher 28+) 의 잔여분

## 다음 (v0.7.61 / v0.7.62 / v0.8.0)

1. **v0.7.61 F**: mkdocs `--strict` 진짜 활성화 (wiki mirror 또는 multirepo) — v0.7.53 follow-up
2. **v0.7.62 B + D**: consumer-metrics trend snapshot + weekly digest 자동화
3. v0.7.61+ follow-up: dispatcher 29 (`cache-lru-decay`) + dispatcher 30 (`cache-merge-csv`)
4. v0.8.0 J. stable API freeze + mypy strict + PyPI + generated schema SSOT
   (workflow-source/core/v0_8_0_stable_api_spec.md)
