# v0.7.62 Backlog (2026-06-17)

본 release 의 1 TASK 정의. v0.7.58 의 follow-up B + D (consumer-metrics trend
snapshot + weekly digest) 동시 해결 — v0.7.58 의 *수집* 만 가능했던 consumer_metrics
를 *추세* + *digest* 까지 확장.

## TASK-V0762-001: consumer-metrics trend snapshot + weekly digest (B + D 통합)

- **상태**: done
- **1차 출처**:
  - v0.7.58 release note (다음 항목 §3) — B. trend snapshot + D. weekly digest 의 1차 출처
  - v0.7.1+ score_wiki_trend.py 의 jsonl history pattern (record_current / load_history / ascii_chart) — 본 TASK 의 *trend 정공법* 의 1차 출처
  - v0.7.49+ lfu_decay_persist.py 의 wrapped file format (`{version, saved_at, scores: {...}}`) — 본 TASK 의 *history file format* 의 1차 출처
  - v0.7.59 release note (consumer-metrics in-process dispatcher) — 본 TASK 의 *argv forwarding* 의 1차 출처
- **commit**: TBD
- **범위**:
  - **신규 function 5종** (`tools/consumer_metrics.py`):
    1. `record_snapshot(metrics, history_path=HISTORY_PATH)` — 현재 snapshot 을 jsonl 에 append (per-line 1 snapshot). history schema: `{snapshot_at, repo, days, views_total, views_uniques, clones_total, feedback_total, feedback_open, releases_recent_count}`
    2. `load_history(history_path=HISTORY_PATH)` — jsonl load, missing file → `[]` (offline-safe), invalid line → skip
    3. `ascii_trend(records, dim="views_total")` — ASCII bar chart (x=YYYY-MM-DD, y=value, auto-scaled). score_wiki_trend 의 bar chart 정신 정합
    4. `format_digest(metrics)` — Slack-style text (`*bold*` mrkdwn). `• views_total: 100 (50 unique)` 형식
    5. `format_digest_markdown(metrics)` — GH issue comment markdown. `## heading` + `| table |` + `### recent releases` + `### open feedback issues` 형식
  - **신규 flag 4종** (`tools/consumer_metrics.py main()`):
    1. `--record` — 현재 snapshot 을 history 에 append
    2. `--show-trend[=dim]` — history 의 trend chart 출력 (`dim` default: `views_total`, valid: TREND_DIMS 5종)
    3. `--history-path=PATH` — history jsonl path (default: `tools/.consumer_metrics_history.jsonl`)
    4. `--digest` / `--digest-markdown` — weekly Slack / GH markdown digest
  - **TREND_DIMS constant** — `["views_total", "views_uniques", "clones_total", "feedback_total", "feedback_open"]` 5 dim
  - **HISTORY_PATH constant** — `tools/.consumer_metrics_history.jsonl` (gitignored, v0.7.62+ 신규)
  - **Dispatcher 호환**: v0.7.59+ 의 `cmd_consumer_metrics` in-process wrapper 가 argv forwarding — `--record` / `--show-trend` / `--history-path` / `--digest` / `--digest-markdown` 모두 별도 dispatcher subcommand 추가 없이 동작
- **호환성**:
  - 기존 invocation (--repo / --days / --json) 동일하게 동작
  - history file (`.consumer_metrics_history.jsonl`) 은 *local only*, no remote effect
  - cron / GitHub Actions weekly schedule 는 v0.7.62+ follow-up (digest 자체는 완결)
- **제거**:
  - 없음 (신규 기능 추가, 기존 영향 없음)
- **정합성 검증**:
  - `tests/check_consumer_metrics.py`: 6/6 → **9/9** PASS (+3 신규)
    - `test_consumer_metrics_record_snapshot_v0_7_62` — record → load roundtrip (2 records)
    - `test_consumer_metrics_load_history_empty_v0_7_62` — missing file → `[]`
    - `test_consumer_metrics_ascii_trend_v0_7_62` — TREND_DIMS 5종 모두 chart render, unknown dim / empty records
    - `test_consumer_metrics_format_digest_v0_7_62` — Slack-style text contain
    - `test_consumer_metrics_format_digest_markdown_v0_7_62` — GH issue markdown heading + table + sub-sections
  - dispatcher argv forwarding: `python -m workflow_kit.workflow_kit_cli --command=consumer-metrics --show-trend --history-path=...` rc=0 (offline-friendly message)
  - cumulative 5 module test 119 → **122 PASS** (+3)
  - dispatcher 47 → **47 PASS** (변동 없음)

## 다음 (v0.8.0)

1. v0.8.0 J. stable API freeze + mypy strict + PyPI + generated schema SSOT
   (workflow-source/core/v0_8_0_stable_api_spec.md)
2. v0.7.62 follow-up: GitHub Actions weekly cron — `consumer-metrics --digest-markdown` → GH issue comment 자동화
3. v0.7.62 follow-up: dispatcher 29 (`cache-lru-decay`) + dispatcher 30 (`cache-merge-csv`) — v0.7.58 의 3 subcommand 잔여분
