# 2026-06-14 작업 백로그 (release/v0.7.7)

- 문서 목적: 2026-06-14 세션에서 release/v0.7.7 (main HEAD `3300e73`) 의 workflow_kit.cli.doctor × load_config integration 작업을 기록한다.
- 범위: TASK-V077-001 (doctor integration)
- 대상 독자: AI 에이전트, 저장소 maintainer
- 상태: done
- 최종 수정일: 2026-06-14
- 관련 문서: [`../../active/work_backlog.md`](../../active/work_backlog.md), [`../../active/state.json`](../../active/state.json)

## TASK-V077-001 workflow_kit.cli.doctor × load_config integration

- 상태: done
- 우선순위: high
- 영향: `workflow_kit/cli/doctor.py` (172 → 224 line)
  - `load_config(project_root)` 호출 — pyproject.toml `[tool.workflow-doctor]` section 자동 load
  - `should_fail(status, config)` integration — `--exit-on-fail` 의 enum 기반 exit code
  - `--show-config` flag: load 된 config `to_dict()` JSON 출력
  - `--json` output 구조 변경: `{config: ..., results: ...}`
  - `render_pretty` footer: `Config: fail_on=...` + `thresholds:` + `excluded_paths:` 표시
- 누적 test: +8 (총 103+)
- 비고: v0.7.7 본 integration 은 *display + fail_on* 만. state-aware actual apply 는 v0.7.8 follow-up (breaking change 회피)

### 완료 기준

- [x] doctor × metadata integration
- [x] 8 신규 test PASS
- [x] 회귀 0 (5 check / 54 test PASS via run_all_checks)
- [x] GH release v0.7.7-beta 발행

### Reference

- `workflow-source/releases/Beta-v0.7.7.md`
- `tests/check_cli_doctor.py` (8 test)
