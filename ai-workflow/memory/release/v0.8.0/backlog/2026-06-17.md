# v0.8.0 Backlog (2026-06-17)

본 release 의 1 TASK 정의. v0.7.59+ spec 의 *수평 약속 격상* — stable API freeze +
mypy strict 격상 + generated JSON Schema SSOT. 0.7.59~0.7.62 의 follow-up (A~F) 모두
done 인 시점의 *마지막 단계*.

## TASK-V0800-001: stable API freeze + mypy strict + generated JSON Schema SSOT (J)

- **상태**: done
- **1차 출처**:
  - workflow-source/core/v0_8_0_stable_api_spec.md (v0.7.59+ draft, 본 release 에서 freeze)
  - v0.7.59+ release note (다음 항목 §J) — stable API + mypy strict + PyPI + generated schema 의 1차 출처
  - v0.7.5+ release_pipeline.py 의 version-bump 정공법 ([project] version SSOT)
  - v0.7.1+ output_contracts.output_json_schema_bundle (Phase 9 Pydantic refactor)
  - workflow_kit.__init__.py (v0.7.5+ 의 __version__ literal — 본 release 에서 pyproject.toml SSOT 로 변경)
  - pyproject.toml [tool.mypy] (v0.7.5+ 의 strict=false — 본 release 에서 strict=true 격상, 단계적)
- **commit**: TBD
- **범위**:
  - **`pyproject.toml [tool.mypy] strict = true`** (단계적 격상, v0.7.59+ spec §5.3 정공법):
    - base strict = true (workflow_kit/__init__.py 의 public surface 만 strict clean)
    - workflow_kit/<module>.py 별 단계적 strict 격상 (v0.8.x~0.9.0 follow-up)
    - v0.8.0 의 *honest scope*: `__init__.py` 만 mypy strict clean
  - **`workflow_kit/__init__.py` 의 `__all__` 명시** (v0.7.59+ spec §3.1):
    - 27 entries: top-level public submodules (verified by import)
    - internal `common.*`, `server.*`, `contract_v1.*`, `cli.*`, `harness.*` 는 *not in `__all__`*
    - SemVer 2-year guarantee (v0.8.0 → 2.0.0 까지 breaking change 없음)
  - **`__version__` 단일 출처** (v0.7.59+ spec §4.3):
    - pyproject.toml [project] version = SSOT
    - `__init__.py` 가 runtime 에 pyproject.toml 직접 parse (3.11+ tomllib / 3.10 tomli)
    - suffix 통일: `-beta` literal (workflow_release_spec.md §4 정공법)
    - fallback chain: pyproject.toml > distribution metadata > "0.8.0" literal (loud)
  - **`tools/release_pipeline.py` 의 `gen-schema` subcommand** (v0.7.59+ spec §6.1):
    - `cmd_gen_schema(args)` 신규 — runtime contract (workflow_kit.common.output_contracts) → JSON Schema (draft-07) dump
    - `--output=PATH`: 출력 file (default: `schemas/generated_output_schemas.json`)
    - `--check`: byte-identical 검증 (CI gate)
    - `--dry-run`: write 안 함
    - `--family=NAME`: 단일 family 만 dump
    - `--json`: JSON output
    - 21 Pydantic model family → 85743 bytes JSON Schema bundle
- **호환성**:
  - PyPI/TestPyPI 업로드: **no** (memory #5 release 채널 정책 — GitHub Releases only, 사용자 요청)
  - `pyproject.toml [project]` section 의 `version` literal 변경 가능 — `version-bump` subcommand 가 자동
  - `__init__.py` 의 `__version__` 자동 = `f"v{[project] version}-beta"`
  - 기존 dispatcher surface (28 subcommand) 변동 없음
- **제거**:
  - `workflow_kit/__init__.py` 의 `__version__ = "v0.7.58-beta"` literal (SSOT 로 교체)
- **정합성 검증 (smoke 9종)**:
  1. `mypy --strict workflow_kit/__init__.py` → "Success: no issues found"
  2. `python -c "import workflow_kit; assert workflow_kit.__version__ == 'v0.7.59-beta'"` → OK
  3. `len(workflow_kit.__all__) == 27` (top-level public modules)
  4. `python tools/release_pipeline.py gen-schema --output=schemas/generated_output_schemas.json` → "ok: True", 85743 bytes
  5. `python tools/release_pipeline.py gen-schema --check` → "check_status: identical"
  6. `python -c "import workflow_kit.url_validity"` (public module, in __all__) → OK
  7. 회귀: 5 module test ≥ 98 PASS (cumulative: 122 PASS, 변동 없음)
  8. 회귀: dispatcher test ≥ 47 PASS (cumulative: 47 PASS, 변동 없음)
  9. `Beta-v0.8.0.md` Stable API frozen 섹션 + Breaking changes 섹션 존재

## 다음 (v0.8.x / v0.9.0)

1. **v0.8.1+** 단계적 mypy strict 격상 — `workflow_kit/url_validity.py` 부터 module 별
   annotation 보강, v0.8.x 의 작은 release 로 분할 (40 file / 231 error 의 *단계적* 해소).
2. **v0.8.1+** read-only MCP manifest 의 `outputSchema` 가 generated schema 와 byte-identical
   임을 *assertion test* 강제 (현재는 *manual* check).
3. **v0.8.1+** GitHub Actions weekly cron — `consumer-metrics --digest-markdown --days=7` →
   GH issue comment 자동 post (v0.7.62 follow-up).
4. **v0.8.1+** dispatcher 29 (`cache-lru-decay`) + dispatcher 30 (`cache-merge-csv`) —
   v0.7.58 의 3 subcommand 잔여분.
5. **v0.9.0** full mypy strict (모든 module strict clean) — 단계적 격상의 마지막 단계.
6. v0.8.0+ follow-up: `docs/architecture/`, `docs/planning/` 의 본문 작성 후 mkdocs nav
   에서 exclude 해제.
