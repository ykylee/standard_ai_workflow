# v0.8.1 Backlog (2026-06-17)

본 release 의 1 TASK 정의. v0.8.0 spec §5.3 의 mypy strict 단계적 격상 — 첫 단계
(workflow_kit/url_validity.py) 의 strict clean. 25 mypy error → 0. 누적 다른 module
들은 v0.8.2+ follow-up.

## TASK-V0801-001: mypy strict 단계적 격상 1단계 (workflow_kit/url_validity.py)

- **상태**: done
- **1차 출처**:
  - workflow-source/core/v0_8_0_stable_api_spec.md §5.3 단계적 격상 정공법
  - workflow_kit/url_validity.py (V-R10, ADR-010/012/013/017/019/020/021) — 25 mypy error
  - workflow_kit.url_validity 의 14 PASS test (v0.7.53+ 의 check_url / cache subsystem)
- **commit**: TBD
- **범위**:
  - **`Severity = Literal["error", "warn", "info"]`** 확장 (기존: `["error", "warn"]`).
    11 error fix — INFO level 의 url issue 가 real-world (advisory) 라 *silent* 변환.
  - **`EvictionStrategy = Literal["lru", "lfu", "mixed"]`** 신규 type alias.
    *real bug fix* — type alias 가 정의되지 않은 채 *reference* 됨. runtime NameError 안
    났던 이유는 *dynamic typing Python* + `lru`/`lfu`/`mixed` 가 단순 string default
    였기 때문 (mypy strict 만 detect).
  - **`CacheEntry.timestamp: float` field 추가** — *real bug fix*. 기존 constructor 가
    `timestamp=float(data["timestamp"])` 으로 field 세팅했으나, `CacheEntry` class 에
    `timestamp` field 가 *선언되지 않음*. mypy strict 가 detect. 836/1039/1048 line 의
    모든 constructor site 가 이 field 의 *missing* 에 의존 중이었음.
  - **`tuple[dict[str, Any], ...]`** generic 명시 (기존 `tuple[dict, ...]`).
  - **`cache_stats` return type**: `dict[str, int]` → `dict[str, int | float]` (last_eviction_timestamp: float)
  - **`cache_stats_per_strategy[_with_hit_rate]` return type**: `dict[str, dict[str, int | float]]` 통일
  - **`_CacheLock._fd: IO[Any] | None`** 명시
  - **`_CacheLock.__enter__` / `__exit__` annotation**: `__enter__() -> "_CacheLock"`,
    `__exit__(*args: Any) -> Literal[False]` (False 명시 — 예외 swallow 안 함)
  - **`subprocess_run: Any = None`** parameter type 명시
  - **`max_diff_lines: int = 1000`** parameter 추가 (line 673 다음, *real bug* — body 의
    line 752 에서 reference 하지만 signature 에 없음)
  - **`check_url_semantic_range_diff` 의 `severity` typed as Severity** 명시
  - **`_evict_key` return type `tuple[int, float]`** 명시
  - **`cache_prune` return type `dict[str, dict[str, int | bool]]`**, missing `return result` 추가
  - **`main` missing `return 1 if failed else 0` 추가**
- **호환성**:
  - `CacheEntry` 에 field 1개 추가는 *frozen dataclass* 의 *backward-compatible* (default value 0.0 으로
    *silent initialize*, but no existing code path 가 default 에 의존 — 모두 *required* positional/named)
  - `Severity` 의 `'info'` 추가는 *new valid value* — 기존 user 가 `'info'` 사용 시에도 호환
  - `EvictionStrategy` 신규 type alias 는 *runtime behavior* 동일 (string literal 그대로)
  - 다른 module 영향 없음
- **제거**:
  - 없음
- **정합성 검증 (smoke 4종)**:
  1. `mypy --strict workflow_kit/url_validity.py` → "Success: no issues found in 1 source file"
  2. `mypy --strict workflow_kit/__init__.py` → "Success: no issues found" (v0.8.0+ 유지)
  3. `tests/check_url_validity.py`: 14/14 PASS (변동 없음, 회귀 0)
  4. 회귀: 5 module 71/71 + dispatcher 47/47 + consumer_metrics 9/9 (cumulative: 127 PASS)
  5. cumulative mypy error: 25 → 0 in url_validity.py
- **follow-up (v0.8.2+)**:
  - v0.8.2: `workflow_kit/okf_import.py` strict clean (9 error: type-narrow, dict entry, e outside except)
  - v0.8.3: `workflow_kit/okf_export.py` strict clean (2 error: e outside except)
  - v0.8.4: `workflow_kit/phishing_federation.py` + `phishing_federation_v4.py` (4+4 error)
  - v0.8.5: `workflow_kit/cache_lfu_decay.py` + `cache_lfu_decay_persist.py` strict clean
  - v0.8.6: `workflow_kit/url_validity.py` 외 cli module (`workflow_kit_cli.py` 48 error, 가장 큼)
  - v0.8.7: `workflow_kit/common/state/builder.py` 35 error
  - v0.8.8: `workflow_kit/common/contracts/baselines.py` 27 error
  - v0.8.x: small 1-2 file strict clean per release
  - v0.9.0: full mypy strict
