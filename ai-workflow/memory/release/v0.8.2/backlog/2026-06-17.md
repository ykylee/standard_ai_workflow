# v0.8.2 Backlog (2026-06-17)

본 release 의 1 TASK 정의. v0.8.0 spec §5.3 mypy strict 단계적 격상 2단계
(workflow_kit/okf_import.py) 의 strict clean. 9 mypy error → 0.

## TASK-V0802-001: mypy strict 단계적 격상 2단계 (workflow_kit/okf_import.py)

- **상태**: done
- **1차 출처**:
  - workflow-source/core/v0_8_0_stable_api_spec.md §5.3 단계적 격상 정공법
  - workflow_kit/okf_import.py (OKF v0.1 consumer, ADR-007/011/019) — 9 mypy error
  - workflow_kit.okf_import 의 25 PASS test (v0.7.53+ 의 OKF bundle ingest)
- **commit**: TBD
- **범위**:
  - **opening docstring 추가 (line 1)**: 기존 line 1-2 가 blank, line 3 부터 code
    — python 이 *implicit* docstring 인식 못해서 syntax error (`Leading zeros in decimal
    integer literals`). *real bug fix*.
  - **`→` U+2192 제거 (line 504-505)**: `OKF → wiki import` 의 docstring 안의 unicode
    arrow 가 mypy 2.1 의 *false positive* syntax error (`Invalid character '→'`).
    fix: `→` → `to` (textual). *cosmetic*.
  - **`Severity`-like return type cast 3종**: `cli_mode: str | None` (line 111),
    `declared: str` (line 119, 130) — return type `Literal['strict', 'loose']` 와 mismatch.
    fix: `cast(Mode, ...)` 명시.
  - **`m` variable name 충돌 fix (line 125)**: 116 의 `m = _parse_simple_yaml(...)` 가
    `dict[str, object]`, 125 의 `m = _FRONTMATTER_RE.match(text)` 가 `Match[str] | None` —
    *real bug* (variable name reuse, type inference 깨짐). fix: 125 의 `m` → `m_match`.
  - **lambda → def function (line 372)**: `norm = lambda s: re.sub(...)` 가 untyped.
    fix: `def norm(s: str) -> str: return ...` 함수.
  - **`subprocess_run: Any = None` 명시 (line 618)**: type-narrowing 후 373 의
    untyped function call fix 의 일환.
  - **`e` → `err` loop variable rename (line 883-884)**: try/except scope 의 `e` 와
    *deleted variable* 충돌. fix: loop variable 를 `err` 로 rename.
  - **return type `dict[str, int | str | bool]` (line 447)**: cleanup_staging 의
    return type 이 `dict[str, int]` 였지만 `staging_dir: str`, `dry_run: bool` 가
    포함되어야 함. fix: `int | str | bool` 로 변경.
  - **`Any` import 추가 (line 51)**: typing import 누락.
- **호환성**:
  - *모든 변경이 behavior 보존* — `cast` 는 type-narrowing 의 explicit 버전, lambda → def 는 same behavior, variable rename 은 same behavior
  - `cleanup_staging` 의 return type 변경 (int → int | str | bool) — consumer 가 type-check 시 영향. 기존 consumer 가 `dict["removed"]` 의 `int` 를 기대하면 *runtime* 에는 영향 없지만 *static check* 에서 차이 발생. `removed: int` → 변경 없음, *추가* `staging_dir: str` + `dry_run: bool` 가 *type widening*.
- **제거**:
  - file line 1-2 의 *implicit* docstring (syntax 깨진 form) → *explicit* docstring (line 1 `"""` + line 2 blank)
  - `→` U+2192 (mypy 2.1 false positive 회피)
  - `# type: ignore[return-value]` 2개 (line 119, 130) — `cast` 로 교체
- **정합성 검증 (smoke 4종)**:
  1. `mypy --strict workflow_kit/okf_import.py` → 9 → **0** error
  2. `mypy --strict workflow_kit/__init__.py` → 0 error (v0.8.0+ 유지)
  3. `mypy --strict workflow_kit/url_validity.py` → 0 error (v0.8.1+ 유지)
  4. `tests/check_okf_import.py`: 25/25 PASS (회귀 0)
  5. 회귀 5 module 71/71 + dispatcher 47/47 + consumer_metrics 9/9 (cumulative: 127 PASS)
- **follow-up (v0.8.3+)**:
  - v0.8.3: `workflow_kit/okf_export.py` strict clean (2 error: `e` deleted variable)
  - v0.8.4: `workflow_kit/phishing_federation.py` + `phishing_federation_v4.py` (4+4 error)
  - v0.8.5: `workflow_kit/cache_lfu_decay.py` + `cache_lfu_decay_persist.py` strict clean
  - v0.8.6: `workflow_kit/workflow_kit_cli.py` (48 error, 가장 큼)
  - v0.8.7: `workflow_kit/common/state/builder.py` 35 error
  - v0.8.8: `workflow_kit/common/contracts/baselines.py` 27 error
  - v0.9.0: full mypy strict
