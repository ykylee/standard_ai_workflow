# v0.8.4 Backlog (2026-06-17)

본 release 의 1 TASK 정의. v0.8.0 spec §5.3 mypy strict 단계적 격상 4단계
(workflow_kit/phishing_federation.py + phishing_federation_v4.py) 의 strict clean.
8 mypy error → 0 (각 file 4 error).

## TASK-V0804-001: mypy strict 단계적 격상 4단계 (phishing_federation + phishing_federation_v4)

- **상태**: done
- **1차 출처**:
  - workflow-source/core/v0_8_0_stable_api_spec.md §5.3 단계적 격상 정공법
  - workflow_kit/phishing_federation.py (V-R11 phishing URL federation, ADR-023) — 4 mypy error
  - workflow_kit/phishing_federation_v4.py (V-R11 v4 implementation, ADR-023 follow-up) — 4 mypy error
  - PEP 589 TypedDict (Python 3.8+) — inner dict 의 structured type 정공법
- **commit**: TBD
- **범위**:
  - **`_UrlRecord` TypedDict 신규 정의** (양쪽 file): `confidence: float` + `sources: list[str]`
    *real type safety* — inner dict 의 *different-type-per-key* 구조 명시.
  - **`url_data: dict[str, dict[str, object]]` → `dict[str, _UrlRecord]` 명시** (양쪽):
    기존 추론이 *object* 였기 때문에 *narrowing* 실패 (4 error) → TypedDict 으로 *narrow*.
  - **`cast(list[str], [])` empty list 명시** (양쪽): TypedDict strict mode 의 empty list
    의 `list[Never]` 추론 회피.
  - **`Callable, TypedDict, cast` import 추가** (양쪽)
- **호환성**:
  - TypedDict 은 PEP 589 (Python 3.8+) — 본 project 의 Python 3.10+ 와 정합.
  - behavior 보존 — `dict["confidence"]`, `dict["sources"]` access pattern 동일.
  - *real bug fix*: 기존 `dict[str, object]` 추론은 *runtime* 에 잘못된 access 가 가능했음
    (예: `data["confidence"].append(...)` — runtime TypeError). TypedDict 로 *static* 차단.
- **제거**:
  - `dict[str, object]` 추론의 *implicit* unsafe access
- **정합성 검증 (smoke 4종)**:
  1. `mypy --strict workflow_kit/phishing_federation.py` → 4 → **0** error
  2. `mypy --strict workflow_kit/phishing_federation_v4.py` → 4 → **0** error
  3. `mypy --strict` (6 file): 7 error in phishing_keywords.py (v0.8.5 follow-up)
  4. 회귀 5 module 71/71 + dispatcher 47/47 + consumer_metrics 9/9 (cumulative: 127 PASS)
- **follow-up (v0.8.5+)**:
  - v0.8.5: `workflow_kit/phishing_keywords.py` strict clean (7 error: 5 no-untyped-def + 2 no-return)
  - v0.8.5: `workflow_kit/cache_lfu_decay.py` + `cache_lfu_decay_persist.py` strict clean
  - v0.8.6: `workflow_kit/workflow_kit_cli.py` (48 error, 가장 큼)
  - v0.8.7: `workflow_kit/common/state/builder.py` 35 error
  - v0.8.8: `workflow_kit/common/contracts/baselines.py` 27 error
  - v0.9.0: full mypy strict
