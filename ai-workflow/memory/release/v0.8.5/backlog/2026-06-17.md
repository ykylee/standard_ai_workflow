# v0.8.5 Backlog (2026-06-17)

본 release 의 1 TASK 정의. v0.8.0 spec §5.3 mypy strict 단계적 격상 5단계
(workflow_kit/phishing_keywords.py + cache_lfu_decay.py + cache_lfu_decay_persist.py) 의 strict clean.
11 mypy error → 0.

## TASK-V0805-001: mypy strict 단계적 격상 5단계 (phishing_keywords + cache_lfu_decay*)

- **상태**: done
- **1차 출처**:
  - workflow-source/core/v0_8_0_stable_api_spec.md §5.3 단계적 격상 정공법
  - workflow_kit/phishing_keywords.py (V-R11 phishing keyword + PhishTank/OpenPhish API, ADR-017/023) — 7 mypy error
  - workflow_kit/cache_lfu_decay.py (LFU decay integration, ADR-021) — 4 mypy error
  - workflow_kit/cache_lfu_decay_persist.py (LFU decay persistence, ADR-021 follow-up) — 0 error (clean)
- **commit**: TBD
- **범위**:
  - **`phishing_keywords.py` (7 error)**:
    - `from typing import Any, Callable, Iterable` 추가 (Any + Callable)
    - 4 function 의 `requests_get=None` → `requests_get: Callable[..., Any] | None = None` 명시
      (fetch_phishtank_feed, fetch_openphish_feed, fetch_federated_phishing_urls 의 pt/op)
    - 2 nested `def requests_get(url, **kwargs)` 의 parameter + return type annotation
  - **`cache_lfu_decay.py` (4 error)**:
    - `from workflow_kit.lfu_config import LFUConfig` import 추가
    - 3 function 의 `config` (no type) → `config: LFUConfig` 명시 (save_cache_with_decay,
      select_eviction_candidates_with_decay, save_cache_lfu_decay_full)
    - 1 function 의 `entries` (no type) → `entries: dict[str, Any]` 명시 (forward ref via str quote)
    - 1 return type 명시: `dict[str, Any]` → `dict[str, dict[str, Any]]` (save_cache_lfu_decay_full)
- **호환성**:
  - `cache_lfu_decay_persist.py` 는 이미 strict clean (v0.7.49+ 의 `dict[str, ...]` typing
    잘 정립됨)
  - 모든 function signature 가 *narrowing* 만 — behavior 보존
- **제거**:
  - 없음
- **정합성 검증 (smoke 4종)**:
  1. `mypy --strict` (3 file): 11 → **0** error
  2. `mypy --strict` (9 file cumulative): "Success: no issues found in 9 source files"
  3. 회귀 5 module 71/71 + dispatcher 47/47 + consumer_metrics 9/9 (cumulative: 127 PASS)
- **follow-up (v0.8.6+)**:
  - v0.8.6: `workflow_kit/workflow_kit_cli.py` (48 error, 가장 큼 — module 별 분할)
  - v0.8.7: `workflow_kit/common/state/builder.py` 35 error
  - v0.8.8: `workflow_kit/common/contracts/baselines.py` 27 error
  - v0.9.0: full mypy strict
