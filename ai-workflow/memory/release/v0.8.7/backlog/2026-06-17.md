# v0.8.7 Backlog (2026-06-17)

본 release 의 1 TASK 정의. v0.8.0 spec §5.3 mypy strict 단계적 격상 7단계
(workflow_kit/v_r13_commit_diff.py + cache_analytics.py + cache_analytics_trend_chart.py) 의 strict clean.
13 mypy error → 0.

## TASK-V0807-001: mypy strict 단계적 격상 7단계 (v_r13_commit_diff + cache_analytics + cache_analytics_trend_chart)

- **상태**: done
- **1차 출처**:
  - workflow-source/core/v0_8_0_stable_api_spec.md §5.3 단계적 격상 정공법
  - workflow_kit/v_r13_commit_diff.py (V-R13 layer 2 commit-level diff, ADR-019) — 11 mypy error
  - workflow_kit/cache_analytics.py (cache analytics summary, ADR-021) — 1 mypy error
  - workflow_kit/cache_analytics_trend_chart.py (cache trend ASCII chart, v0.7.50+) — 1 mypy error
- **commit**: TBD
- **범위**:
  - **`v_r13_commit_diff.py` (11 error)**:
    - `from typing import Any, Callable, cast` import 추가
    - 5 function 의 `requests_get=None` → `requests_get: Callable[..., Any] | None = None`
      (check_url_semantic_commit_diff_github/bitbucket/dispatch + check_url_semantic_with_commit_diff + run_layer2_pipeline)
    - 2 nested `def requests_get(url, **kwargs)` 의 `**kwargs: Any` + return type `Any`
    - 2 `cast(list[dict[str, Any]], data["commits"])` / `data["values"]` return cast
  - **`cache_analytics.py` (1 error)**:
    - `int(per_strategy[s]["size"])` cast (line 91) — return type `dict[str, int]` 정합
  - **`cache_analytics_trend_chart.py` (1 error)**:
    - `from typing import Any, Literal` import 추가
    - `snapshots: list[dict]` → `list[dict[str, Any]]` 명시 (line 18)
- **호환성**:
  - 모든 변경이 *narrowing* + annotation — behavior 보존
  - `int(...)` cast 는 runtime 시 *truncate* 효과 — 기존 `per_strategy[s]["size"]` 가 항상 int 였음
- **제거**:
  - 없음
- **정합성 검증 (smoke 4종)**:
  1. `mypy --strict` (3 file): 13 → **0** error
  2. `mypy --strict` (cumulative, 26 file): "Success: no issues found in N file" + 8 stray error (v0.8.8+ follow-up)
  3. 회귀 5 module 71/71 + dispatcher 47/47 + consumer_metrics 9/9 (cumulative: 127 PASS)
- **follow-up (v0.8.8+)**:
  - v0.8.8: 4 file strict clean: `upgrade_diff.py` (1) + `bitbucket_v2.py` (4) + `lfu_integration.py` (1) + `cache_size_compare.py` (2) = 8 error
  - v0.8.9: `common/state/builder.py` 35 error
  - v0.8.10: `common/contracts/baselines.py` 27 error
  - v0.9.0: full mypy strict
