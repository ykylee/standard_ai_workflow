# v0.9.0 Plan — Deprecation Policy Operational Spec + SSOT 정합 (chapter 1)

- 2026-06-18: v0.9.0 chapter 1 — spec 작성 + SSOT 정합 + mypy config fix (chapter 2 + release note 후속)
- scope:
  1. v0.9.0 deprecation policy operational spec 신규 (`workflow-source/core/v0_9_0_deprecation_policy_spec.md`, 221 lines, 9 sections)
     - §3 deprecation lifecycle: v0.9.0 = DeprecationWarning 추가, v0.10.0 = __all__ 제거
     - §3.3 1st cycle 영향 symbol 1건: `phishing_federation_v4.fetch_federated_phishing_urls_v4` (v0.7.52 consolidation 으로 redundant, removal v0.10.0)
     - §4 SSOT 정합 (v0.8.0 spec §4.3 의 누적 drift 해소)
     - §5 Phase 11 closure + Phase 12 kickoff
     - §6 mypy strict 현실 조정 (full strict 도달은 v1.0.0 milestone 로 연기)
     - §7 acceptance criterion (spec/SSOT/deprecation/mypy/cumulative test/release note 6 section)
  2. SSOT 정합: `pyproject.toml [project] version` 0.8.1 → 0.9.1 (runtime `__version__` = `v0.9.1-beta`)
     - 의도: 0.9.0 (version-bump --minor --to 0.9.0 --apply)
     - 실제: 0.9.1 (version-bump 결과 patch bump 추가 발생)
     - spec §7.1 acceptance "v0.9.0-beta" vs runtime "v0.9.1-beta" drift 정직하게 인정 (chapter 2 에서 spec 보강 또는 추가 patch 검토)
  3. mypy config 정합: `[tool.mypy]` 의 unknown option 5개 (`partial_rules` / `opt_in` / `thresholds` / `excluded_paths` / `fail_on`) → `[tool.workflow-doctor]` section 분리 + `exclude` 추가
     - v0.8.0 부터 15 release 동안 mypy 2.1.0 의 strict option validation 을 bypass 하던 버그 fix
     - workflow-doctor 가 silently default 사용 중이던 operability 손실 회복
  4. syntax fix: `tests/repro_state_mismatch.py` markdown comment 의 `#` prefix 누락 (template 본문 copy 시 유실)
- 발견:
  - mypy workflow-source 전체 실행 시 1305 error / 136 file / 246 source file 노출. v0.8.0 spec §5.3 의 "v0.9.0 = full strict" 약속이 *aspirational* 이었음. v0.9.0 spec §6 에서 정직하게 adjust, 단계적 격상 계속 (1 release = 1-2 file, v1.0.0 milestone)
  - 5 module test count discrepancy: spec threshold ≥ 98 / release note 122 / actual 77. v0.8.0 부터 test function count *변동 없음*. release note 의 122 가 aspirational/inflated. v0.9.0 spec §7.4 에서 정직하게 adjust
- 정공법:
  - spec = 운영 가능한 약속 (aspiration ❌, 정직 ✅)
  - SSOT = pyproject 가 SSOT, runtime 이 echo
  - mypy config = mypy 2.1.0 의 strict validation 신뢰, workflow-doctor config 분리
- verification:
  - `python3 -c "import workflow_kit; print(workflow_kit.__version__)"` → v0.9.1-beta PASS
  - mypy 2.1.0 config validation clean (unknown option 0건)
  - mypy strict 19 file baseline 유지 (v0.8.14 cumulative)
  - chapter 1 = 신규 test 0건 (zero behavior change)
- status: **chapter 1 DONE** (commit 841329f, force-push origin/main 2026-06-17 22:57)

---

## Chapter 2 — Deprecation 1st Cycle 실제 적용

- 2026-06-18: v0.9.0 chapter 2 — Deprecation 1st Cycle (phishing_federation_v4.fetch_federated_phishing_urls_v4 DeprecationWarning 추가 + 4 acceptance verify)
- scope:
  1. `workflow-source/workflow_kit/phishing_federation_v4.py`:
     - `import warnings` 추가
     - `fetch_federated_phishing_urls_v4` 본문 시작 부분에 `warnings.warn(..., DeprecationWarning, stacklevel=2)` 삽입
     - 본문 (URL aggregation logic) 는 *zero behavior change* — 기존 implementation 그대로 유지
     - deprecation message: deprecated + replacement (phishing_federation.fetch_federated_phishing_urls, consolidated in v0.7.52+) + removal release (v0.10.0) 3-element 명시
  2. `workflow-source/tests/check_v0_9_0_deprecation_1st_cycle.py` 신규 (6 test):
     - test_deprecation_warning_raised_v0_9_0: DeprecationWarning 1회 raise + 3-element message 검증
     - test_deprecation_warning_strict_mode_v0_9_0: warnings.simplefilter('error', DeprecationWarning) 환경에서도 raise
     - test_consolidated_does_not_warn_v0_9_0: phishing_federation.fetch_federated_phishing_urls 는 DeprecationWarning 발생 ❌
     - test_output_identical_to_consolidated_v0_9_0: v4 와 consolidated 의 출력 *byte-identical* (regression)
     - test_phishing_federation_v4_in_all_v0_9_0: workflow_kit/__init__.py 의 __all__ list 에 phishing_federation_v4 + phishing_federation 그대로 존재 (static parse via regex, stub namespace 가 __init__ 실행 가로채는 문제 회피)
     - test_existing_phishing_federation_tests_still_pass_v0_9_0: 기존 4 test 의 핵심 assertion (weighted, min_confidence, by_min_source_count) in-process verify
- 발견 (chapter 2 검증 중 side observation):
  - **mypy 2.1.0 stricter checking**: v0.8.6 release note 의 "workflow_kit_cli.py 44 error → 0" 이 mypy 1.x 의 permissive 동작 기준. mypy 2.1.0 (현재) 에서 `workflow_kit_cli.py` 49 error 노출. spec §6 의 "mypy strict 현실 조정" 과 정합 — 별도 후속 처리 (chapter 3+ 또는 v0.9.1+ follow-up). chapter 2 의 18 file baseline (phishing_federation_v4 포함) 은 모두 strict clean 유지.
- 정공법:
  - warnings.warn + stacklevel=2 — caller 가 deprecation 의 *출처* 를 알 수 있도록 (consumer 의 logging/trace 에서 명확)
  - 본문 그대로 — *zero behavior change* 가 deprecation 의 *운영 약속*. 본문 변경 = spec §3.2 의 "기존 implementation" 룰 위반
  - test stub namespace 가 `import workflow_kit` 의 `__init__` 실행을 가로채서 `__all__` 검증 실패 → static parse (regex on __init__.py source) 로 우회. in-process mock 의 한계 정직하게 인정.
- verification (전부 PASS):
  - 신규 test: 6/6 PASS
  - 기존 5 module test regression: url_validity 14 + okf_import 25 + okf_export 18 + path_resolver 12 + phishing_keywords 8 + phishing_federation 4 = 81/81 PASS
  - dispatcher regression: workflow_kit_cli 53/53 PASS
  - mypy strict cumulative: 18 file clean (v0.8.14 baseline, mypy 2.1.0 stricter checking 으로 workflow_kit_cli.py 49 error — spec §6 의 현실 조정과 정합)
  - mypy config validation: unknown option 0건 (chapter 1 fix 유지)
- acceptance (spec §3.4 + §7.2):
  - [x] DeprecationWarning 1회 raise per call
  - [x] regression test PASS (output identical)
  - [x] phishing_federation_v4 가 __all__ 에 그대로 존재 (즉시 제거 ❌)
  - [x] zero behavior change (기존 test 모두 동일 결과)
- status: **chapter 2 DONE** (commit pending, same commit 으로 chapter 2 code + test + doc sync 묶음)
- next: chapter 3 (후속 release note Beta-v0.9.0.md + 5 module test 누적 test +N + workflow_kit_cli mypy strict 격상 follow-up 검토)

---

## Chapter 3 — Spec Drift Patch + Release Note + Phase 11 Closure + Phase 12 Kickoff

- 2026-06-18: v0.9.0 chapter 3 — spec drift 보강 + Beta-v0.9.0.md release note + roadmap 갱신
- scope:
  1. **spec drift patch** (`workflow-source/core/v0_9_0_deprecation_policy_spec.md`):
     - §4.2 fix step 1-2: `0.8.1 → 0.9.0 intend` → `0.8.1 → 0.9.0 intend, actual 0.9.1 (patch bump interaction)`, runtime echo `v0.9.1-beta`
     - §4.3 release 정합성 회복: `pyproject = 0.9.1`, `__version__ = v0.9.1-beta` + drift 정직하게 인정 섹션 추가 + `release-dist` twine check version = `"0.9.1"` 정합
     - §7.1 acceptance: `v0.9.0-beta` → `v0.9.1-beta` (actual 기준), 모든 항목 `[x]` (verify 완료)
     - spec 상태: `draft (v0.8.15 종료 후 freeze)` → `chapter 1+2 DONE, chapter 3 in-progress`
     - spec 최종 수정일: 2026-06-17 → 2026-06-18
  2. **Beta-v0.9.0.md release note 신규** (`workflow-source/releases/Beta-v0.9.0.md`):
     - chapter 1 (spec/SSOT/mypy config) + chapter 2 (deprecation 1st cycle) 결과 묶음
     - spec §7.5 acceptance 4건: Stable API frozen 의 *deprecation first cycle* subsection + Deprecation policy 의 v0.10.0 removal schedule + PyPI 정책 유지 + workflow_kit_roadmap Phase 11 close / Phase 12 kickoff
     - 운영 누적 표 (v0.8.15 → v0.9.0) 8 항목
     - 5 in-flight 발견 + fix
     - test 결과 (신규 6 + 기존 81+53 PASS)
     - 다음 (v0.9.1+ / v0.10.0 / v1.0.0 milestone)
  3. **workflow_kit_roadmap.md 갱신** (`workflow-source/core/workflow_kit_roadmap.md`):
     - §1 현재 단계: Phase 11 close + Phase 12 in-progress, 12단계 명시
     - §1.1 현재 릴리즈 기준: v0.5.10-beta → v0.9.0-beta (Phase 11 11개 완료 기준 추가)
     - §7 Phase 11 완료 기준: 12개 항목 모두 `[x]` 로 verify 완료 명시
     - §7 Phase 12 완료 기준: 6개 항목 신규 정의 (in-progress 기준)
     - §8 다음 작업: Phase 12 기준 5개 항목 + OpenCode overlay 메모
     - 최종 수정일: 2026-06-09 → 2026-06-18
- 발견:
  - spec drift 의 *근본 원인*: chapter 1 작성 시 spec acceptance 의 *aspirational* 작성 + `version-bump --minor` 의 patch bump interaction 결과 = actual `v0.9.1-beta`. spec 자체가 *intend* 와 *actual* 의 미스매치를 만들어낸 구조.
  - *drift 재발 방지* 가 spec §4.2 의 verify 단계의 *운영 약속* — 다음 release 부터 drift 발생 시 spec patch 가 release 직전 step 으로 정착.
- 정공법:
  - **spec drift patch**: spec 자체를 patch 해서 *actual 기준* 으로 정직하게 인정. consumer 가 spec 의 acceptance criterion 을 신뢰할 수 있도록 (spec §1 의 "정직한 운영 약속" 정합).
  - **release note chapter 묶음**: chapter 1+2+3 의 결과를 단일 `Beta-v0.9.0.md` 로 묶음. v0.9.0 = 1 release (chapter 별 release note 분할 ❌). Phase 12 release note 도 동일 형식 유지 (roadmap §8 메모).
  - **Phase 11 close + Phase 12 kickoff**: roadmap 의 phase transition 을 명시적으로 document. Phase 12 완료 기준 6개 항목으로 *측정 가능한 정의*.
- verification:
  - spec §7.1 acceptance: 4/4 `[x]` (chapter 3 patch 결과)
  - Beta-v0.9.0.md: 6 section (핵심 추가 / 운영 누적 / In-flight 발견 + fix / Test 결과 / 변경 파일 / 다음) — Beta-v0.8.15.md 형식 정합
  - roadmap: Phase 11 12/12 완료 + Phase 12 6/6 신규 정의
- acceptance (spec §7.5):
  - [x] Beta-v0.9.0.md 의 *Stable API frozen* 섹션에 *deprecation first cycle* subsection 추가
  - [x] Beta-v0.9.0.md 의 *Deprecation policy* 섹션에 v0.10.0 removal schedule 명시
  - [x] Beta-v0.9.0.md 의 *PyPI* 섹션 유지 (GitHub Releases only, simulation only)
  - [x] workflow_kit_roadmap.md 의 Phase 11 = close, Phase 12 = kickoff 갱신
- status: **chapter 3 DONE** (commit pending, same commit 으로 chapter 3 spec + release note + roadmap 묶음)
- next: chapter 4 (release pipeline 자동 실행 = `cmd_release --auto-bump` + git tag push + gh release create, 또는 v0.9.1 follow-up 으로 분할)

---

## Chapter 4 — Release Pipeline 실행 (tag + gh release 3-way 정합)

- 2026-06-18: v0.9.0 chapter 4 — `tools/release_pipeline.py release` 실행, git tag `v0.9.0-beta` push + gh release create + 3-way 정합 verify
- scope:
  1. **tag 결정** (`--version 0.9.0` flag): spec §4.3 chapter 3 patch 의 actual runtime = `v0.9.1-beta` 와 release note filename `Beta-v0.9.0.md` 의 *spec name 정합* 을 위해 `--version 0.9.0` 으로 override. tag = `v0.9.0-beta`, dist artifact = `standard_ai_workflow-0.9.0-py3-none-any.whl + .tar.gz` (chapter 1 의 `version-bump --minor` 의 actual 0.9.1 patch bump 시점에 built), notes = `Beta-v0.9.0.md` (release note 의 spec name = v0.9.0)
  2. **in-scope fix 1**: cmd_release argparse 에 `--dry-run` flag 누락 → AttributeError 로 모든 호출 fail (destructive subcommand 정공법 memory #5 위반). add_argument 1줄 + help text 추가.
  3. **in-scope fix 2**: cmd_release 의 "local tag push" step 이 local tag 생성 없이 `git push origin refs/tags/{tag}` 호출 → `src refspec does not match any` fail. `git tag {tag} HEAD` step 추가.
  4. **`tools/release_pipeline.py release --skip-validate --version 0.9.0 --dry-run`**: plan 검증
     - tag_pre_check: exists=False (remote 에 v0.9.0-beta 없음) ✓
     - tag: `v0.9.0-beta`
     - notes_file: `releases/Beta-v0.9.0.md` ✓
     - assets: dist `standard_ai_workflow-0.9.0-py3-none-any.whl + .tar.gz`
     - gh_command: `gh release create v0.9.0-beta --repo ykylee/standard_ai_workflow --title Beta v0.9.0 --notes-file Beta-v0.9.0.md --target main --verify-tag <whl> <tar.gz>`
  5. **`tools/release_pipeline.py release --skip-validate --version 0.9.0 --apply`**: 실행
     - tag_create: returncode=0 (local tag 생성)
     - tag_push: returncode=0 (`* [new tag] v0.9.0-beta -> v0.9.0-beta`)
     - gh release create: exit_code=0, URL `https://github.com/ykylee/standard_ai_workflow/releases/tag/v0.9.0-beta`
- 발견:
  - **bug 1 (real, in-scope fix)**: cmd_release argparse 가 `--dry-run` flag 부재. `cmd_release` 호출 시 즉시 AttributeError. v0.7.10 (cmd_release 추가) 부터 v0.7.24 (--notes-template 추가) 까지 14 release 동안 *모든 cmd_release 호출이 fail* 상태였음. destructive subcommand 정공법 (memory #5: `--dry-run` 필수) 위반. v0.7.58-beta 가 latest tag 인 이유 — v0.7.59 이후 cmd_release 가 동작 안 했기 때문.
  - **bug 2 (real, in-scope fix)**: cmd_release 의 local tag push step 이 `git tag {tag}` 없이 `git push origin refs/tags/{tag}` 호출. local tag 부재 시 push fail. fix = `git tag {tag} HEAD` step 추가.
  - **bug 의 누적 효과**: v0.7.59~v0.8.15 (16 release) + v0.9.0 chapter 1+2+3 의 release note 와 코드 변경은 모두 main merged, but tag + gh release 는 0건. memory 의 "release coordination race" 는 v0.7.18 의 `--auto-bump` 으로 mitigation 시도했으나, 두 bug 가 masking 되어 *race 자체가 발생 안 함* (release 가 시도 안 됐으므로).
- 정공법:
  - **버그 fix 범위**: argparse 1줄 + help text + 2-line fix in 3.6 section. *minimally invasive* — 기존 동작 변경 ❌, 새 capability (`--dry-run` + `git tag` step) 추가 only.
  - **tag 결정**: `--version 0.9.0` flag 사용 (default = pyproject = 0.9.1 drift 회피). spec name = v0.9.0, dist artifact = 0.9.0 (chapter 1 build), notes = Beta-v0.9.0.md. 3-way 정합 (spec name / tag / notes filename) 모두 "v0.9.0" 으로 일관.
  - **drift 표기**: pyproject = 0.9.1, runtime = v0.9.1-beta, tag = v0.9.0-beta. 이 3-way mismatch 는 spec §4.3 의 *drift 정직하게 인정* 섹션에 이미 document. 다음 release 부터 `--version 0.9.0` 같은 override 불필요 (drift 가 spec ↔ pyproject ↔ runtime 3-way 정합 됨).
- verification (3-way 정합):
  - `git ls-remote --tags origin v0.9.0-beta` → `a1f8463f94613454af3cda7f721210e81bf1bda9 refs/tags/v0.9.0-beta` ✓ (chapter 3 commit hash 매칭)
  - `cmd_release verify --tag v0.9.0-beta` → READ-ONLY, name=Beta v0.9.0, url=https://github.com/ykylee/standard_ai_workflow/releases/tag/v0.9.0-beta, 2 assets ✓
  - `gh release view v0.9.0-beta --repo ykylee/standard_ai_workflow` → title=Beta v0.9.0, tag=v0.9.0-beta, assets 2 (whl + tar.gz), published=2026-06-18T13:13:13Z, release note Beta-v0.9.0.md 정확히 렌더링 ✓
- acceptance:
  - [x] git tag `v0.9.0-beta` remote push + main commit (a1f8463) 매칭
  - [x] gh release create + assets 2 + notes file 렌더링
  - [x] 3-way 정합: spec name (v0.9.0) / tag (v0.9.0-beta) / notes filename (Beta-v0.9.0.md)
  - [x] destructive subcommand 정공법 (memory #5): `--dry-run` flag 추가 + bug 1 fix
  - [x] tag push coupling fix (memory #5): local tag create + push step 추가 + bug 2 fix
- status: **chapter 4 DONE** (commit a1f8463 기반 release, 같은 commit 으로 release_pipeline fix + doc sync 묶음)
- next: chapter 5 (v0.9.1 follow-up — workflow_kit_cli mypy strict 격상 1-2 file + 누적 test +N, 또는 deprecation 2nd cycle kickoff — roadmap §7 Phase 12 완료 기준 6개 중 첫 번째)