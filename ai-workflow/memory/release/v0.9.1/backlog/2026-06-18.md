# v0.9.1 Plan — mypy strict workflow_kit_cli 격상 + release pipeline automation + deprecation contract test

- 2026-06-18: v0.9.1 3 deliverables (Phase 12 task 1차)
- scope:
  1. **mypy strict workflow_kit_cli.py 격상** (chapter 5 task #1)
     - 49 → 0 error (register decorator Callable 명시 + cast 활용)
     - 누적 strict clean file: 18 → **19 file** (workflow_kit_cli.py 추가)
     - spec §6 의 "full strict 도달 v1.0.0 milestone" 의 진척: 19 file (1 release = 1 file 단계적 격상 정공법)
  2. **release pipeline automation** (chapter 5 task #2)
     - `cmd_release --full-auto` flag 신규: pre-check conflict 시 자동으로 --auto-bump 동작 후 새 tag 로 release 진행
     - 1-cycle close: operator intervention 없이 tag push + gh release create 완료
     - in-scope fix: dispatch logic 의 `rollback` subcommand 누락 fix (`unknown command: rollback` 였던 진짜 bug)
     - in-scope fix: phase 2 test 의 pre-existing 2건 fail fix (test_release_dry_run_no_dist 의 error message acceptance 확장 + test_rollback_dry_run 의 dispatch fix)
  3. **deprecation policy contract test** (chapter 5 task #3)
     - `tests/check_v0_9_1_deprecation_contract.py` 신규 (4 test)
     - `workflow_kit.__all__` 의 모든 public symbol 이 *deprecation-free* 또는 *deprecation-marked* 인지 contract verify
     - DEPRECATION_MARKED_CALLABLES whitelist + 자동 verify
     - chapter 2 의 1st cycle verify 와 별개의 *meta-contract*: 전체 public surface 자동 검사
- 발견 (chapter 5 in-scope fix):
  - **bug A (real)**: `cmd_release` 의 dispatch logic (line 1725-1742) 에 `elif args.command == "rollback": result = cmd_rollback(args)` branch 누락. argparse 에 `rollback` 정의됐지만 dispatch 에서 "unknown command: rollback" error. v0.7.10 부터 (rollback 정의 시점) 의 14 release 동안 모든 rollback 호출 fail. fix = dispatch 1줄 추가.
  - **bug B (real)**: `test_release_dry_run_no_dist` 가 "no dist files found" error 만 accept 했지만, v0.9.0 chapter 1 의 pyproject bump (0.8.1 → 0.9.1) 으로 0.9.1 dist artifact 존재 → 다음 단계인 "release note not found" error 발생. test 가 fail 한 채로 누적. fix = "dist 부재 OR notes 부재" 둘 다 accept.
  - **bug C (premature auto-bump)**: `--full-auto --apply` 테스트 중 `--apply default=True` 의 위험성 노출. v0.9.0-beta 가 이미 push 된 상태에서 --full-auto --apply 실행 시 자동 bump → 새 tag push → 새 release 생성. rollback 으로 즉시 복구 (git tag -d + git push --delete + gh release delete). 본 release 의 *교훈*: destructive subcommand 의 `--apply default=True` 는 memory #5 의 "destructive subcommand 정공법" 위반 — 후속 release 에서 fix 권장 (다만 breaking change 회피 위해 본 release 에서는 skip).
- 정공법:
  - **mypy strict 격상 정공법**: cascade fix 우선 시도. register decorator 의 outer return type 1줄 추가로 25+ error 가 cascade fix (untyped decorator → typed decorator). 이후 개별 cast(Literal[...]) / cast(dict[str, Any]) / cast(int) 등 *consumer-side* annotation 으로 나머지 fix.
  - **release automation 정공법**: pre-check conflict 시 *operator 가 flag 조합을 외울 필요 없이* `--full-auto` 만으로 cycle close. graceful degradation: conflict 가 다시 발생하면 (이미 bump 했는데 여전히 exist) 자동으로 `--allow-existing-tag` 로 fallback.
  - **contract test 정공법**: whitelist + importlib + warnings.catch_warnings 의 *meta-test* — 개별 test 가 아닌 *전체 public surface* 의 contract 자동 검증. deprecation 2nd cycle 부터 whitelist 에 entry 추가만 하면 됨.
- verification:
  - mypy strict workflow_kit_cli.py: 49 → 0 error
  - cumulative 19/19 strict clean file (workflow_kit_cli.py 신규 추가)
  - `cmd_release --full-auto --dry-run`: plan 정상 verify
  - `cmd_release rollback --tag=X --dry-run`: 3 commands print 정상 (이전엔 dispatch fail)
  - 신규 contract test 4/4 PASS
  - 회귀: check_v0_9_0_deprecation_1st_cycle 6/6, check_phishing_federation 4/4, check_workflow_kit_cli 53/53, check_release_pipeline_release_coordination 7/7, check_release_pipeline_phase2 8/8 (fix 후)
- acceptance (roadmap §7 Phase 12 완료 기준 6개 중 3개):
  - [x] mypy strict cumulative 18 → 19 file (1 release = 1 file 단계적 격상)
  - [x] release pipeline automation (`--full-auto` 1-cycle close)
  - [x] deprecation policy contract test (전체 public surface 자동 검증)
  - [ ] deprecation 2nd cycle 영향 symbol 식별 (1st cycle 운영 검증 결과 기반, 후속)
  - [ ] release pipeline 의 full auto (`--apply` 만으로 cycle close, 후속)
  - [ ] consumer signal 2nd wave (v0.7.62 follow-up 2차, 후속)
- status: **DONE** (commit pending, 같은 commit 으로 chapter 5 code + test + doc sync 묶음)
- next: v0.9.2 follow-up — deprecation 2nd cycle 영향 symbol 식별 + 적용