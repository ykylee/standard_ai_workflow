# 2026-04-22 작업 백로그

- 문서 목적: 2026-04-22에 수행한 작업의 계획, 진행 현황, 완료 내역을 기록한다.
- 범위: 2026-04-22 작업 이력
- 대상 독자: 프로젝트 참여자, 문서 작성자, 개발자, 운영자
- 상태: draft
- 최종 수정일: 2026-04-22
- 관련 문서: `../work_backlog.md`, `../core/project_status_assessment.md`, `../core/workflow_kit_roadmap.md`, `../core/output_schema_guide.md`, `../core/existing_project_onboarding_contract.md`

## TASK-001 출력 계약 정렬과 샘플 검증 기준 고정

- 상태: completed
- 우선순위: high
- 요청일: 2026-04-22
- 완료일: 2026-04-22
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `core/output_schema_guide.md`
- `schemas/output_sample_contracts.json`
- `workflow_kit/common/output_contracts.py`
- `examples/output_samples/README.md`
- `tests/check_output_samples.py`
- 작업 내용:
- output schema guide, 정적 계약 파일, 런타임 계약 맵, 대표 JSON 샘플, smoke test 가 같은 필드 기준을 보도록 정렬한다.
- 진행 현황:
- `2026-04-22` 기준 로드맵과 상태 진단 문서를 읽고 출력 계약 정렬을 최우선 TODO 로 분류했다.
- `2026-04-22` 기준 `status`, `tool_version`, `warnings`, `source_context`, `error_code` 중심으로 공통 계약 축을 다시 확인했다.
- `2026-04-22` 기준 `check_doc_metadata`, `check_doc_links`, `check_quickstart_stale_links`, `create_backlog_entry`, `suggest_impacted_docs` 를 런타임 계약 맵과 정적 schema 검증 대상에 포함했다.
- `2026-04-22` 기준 `tests/check_output_samples.py` 가 README 목록과 실제 JSON 파일 집합 일치 여부까지 검사하도록 보강했고 관련 smoke 를 통과했다.
- 완료 기준:
- 대표 샘플과 실제 프로토타입 출력이 같은 계약을 공유하고, 테스트가 이를 한 번에 검증한다.
- 작업 결과:
- 계약 맵, 정적 schema, 샘플 README, 출력 가이드, smoke test 가 같은 샘플 집합을 바라보도록 1차 정렬을 반영했다.
- 다음 세션 시작 포인트:
- 실패 케이스 샘플을 더 늘리거나 JSON Schema 승격이 필요해질 때 `workflow_kit/common/output_contracts.py` 와 `schemas/output_sample_contracts.json` 을 함께 확장한다.
- 남은 리스크:
- 문서 계약과 런타임 계약이 어긋나면 runner 와 sample 검증이 동시에 흔들릴 수 있다.
- 후속 작업:
- 실패 케이스 샘플과 top-level error wrapping 규칙을 runner 기준으로 다시 정리한다.

## TASK-002 기존 프로젝트 온보딩 runner 계약과 하네스 소비 경로 정리

- 상태: completed
- 우선순위: high
- 요청일: 2026-04-22
- 완료일: 2026-04-22
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `core/existing_project_onboarding_contract.md`
- `scripts/run_existing_project_onboarding.py`
- `tests/check_existing_project_onboarding.py`
- `scripts/README.md`
- `harnesses/codex/README.md`
- `harnesses/opencode/README.md`
- 작업 내용:
- onboarding runner 의 단계별 입출력 계약과 생략/경고/실패 조건을 코드와 문서에서 맞추고, Codex/OpenCode 하네스가 어떤 순서로 결과를 소비할지 짧은 가이드로 연결한다.
- 진행 현황:
- `2026-04-22` 기준 현재 흐름을 `repository_assessment -> latest_backlog -> session-start -> validation-plan -> code-index-update -> onboarding_summary` 로 정리했다.
- `2026-04-22` 기준 하네스 소비 우선순위는 `status -> recommended_next_steps -> warnings -> orchestration_plan` 순으로 읽는 계약이 문서화돼 있음을 확인했다.
- `2026-04-22` 기준 onboarding smoke test 의 실패 경로 기대값이 runner 의 `resolve()` 처리와 어긋나 있음을 확인했고, 테스트를 resolved path 기준으로 수정해 smoke 를 통과시켰다.
- `2026-04-22` 기준 onboarding runner 가 latest backlog 를 찾지 못해도 계속 진행하도록 명령 조립을 조건부로 바꾸고, success `source_context` 에 `repository_assessment_path`, `latest_backlog_path` 를 포함하도록 보강했다.
- `2026-04-22` 기준 Codex/OpenCode 하네스 문서와 scripts 안내 문서에 onboarding 결과 소비 순서와 error 우선 읽기 원칙을 추가했다.
- 완료 기준:
- onboarding runner 의 최종 JSON 구조와 하위 step 소비 방식이 문서, 코드, 테스트에서 일치한다.
- 작업 결과:
- runner 실패 경로 smoke test 의 경로 정합성 오류를 수정했고, latest backlog 미발견 fallback 과 하네스 소비 순서 문서화까지 반영했다.
- `repository_assessment.summary` 와 `onboarding_summary.inferred_commands` 가 실제로 채워진 onboarding success sample 을 추가해 하네스 소비 예시를 보강했다.
- 계약 문서와 Codex/OpenCode 하네스 README 에 assessment 포함 샘플 참조를 연결했다.
- 다음 세션 시작 포인트:
- 필요하면 onboarding summary 예시 샘플을 `examples/output_samples/` 에 추가해 하네스 소비 예시를 더 선명하게 만든다.
- 남은 리스크:
- 하네스 가이드가 runner 출력 구조와 어긋나면 실제 도입 첫 세션에서 사용자 노출 요약이 흔들릴 수 있다.
- 후속 작업:
- 필요하면 onboarding summary 예시 샘플을 `examples/output_samples/` 에 추가한다.

## TASK-003 orchestration 공통 로직의 package 추출 범위 정리

- 상태: completed
- 우선순위: medium
- 요청일: 2026-04-22
- 완료일: 2026-04-22
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `workflow_kit/README.md`
- `workflow_kit/common/runner.py`
- `workflow_kit/common/errors.py`
- `scripts/run_demo_workflow.py`
- `scripts/run_existing_project_onboarding.py`
- 작업 내용:
- runner step 실패 감싸기, 상위 warnings 집계, orchestration metadata 조립 같은 중복 로직을 `workflow_kit/common` 으로 더 끌어올릴 범위를 정리한다.
- 진행 현황:
- `2026-04-22` 기준 `workflow_kit/common` 이 파서, reconcile, planning, runner helper, error payload, output contracts 까지 포함하는 초기 루트임을 확인했다.
- `2026-04-22` 기준 다음 확장 후보로 result payload builder, runner 단계 조립, 읽기 전용 MCP server 엔트리포인트가 명시돼 있음을 확인했다.
- `2026-04-22` 기준 `latest_backlog` 단계 조립, optional path flag 생성, top-level step failure wrapping 을 `workflow_kit/common/runner.py` helper 로 추출했다.
- `2026-04-22` 기준 `run_demo_workflow.py` 와 `run_existing_project_onboarding.py` 가 새 공통 helper 를 사용하도록 정리했고 관련 smoke 를 통과했다.
- `2026-04-22` 기준 runner 공통 success payload builder 를 추가해 상위 `status/tool_version/warnings/orchestration_plan/source_context` 조립 중복을 줄였다.
- 완료 기준:
- runner 스크립트가 공통 helper 를 사용하도록 재구성할 후보 범위와 이동 순서가 명확해진다.
- 작업 결과:
- runner 2종 사이에 중복되던 latest backlog 단계 조립, step error wrapping, top-level success payload 조립을 공통 helper 로 이동했다.
- 다음 세션 시작 포인트:
- 읽기 전용 MCP server 1호 범위를 정할 때 현재 `workflow_kit/common/runner.py` helper 중 server 측 재사용 대상과 runner 전용 대상을 구분한다.
- 남은 리스크:
- 추출 범위를 넓게 잡으면 오히려 계약 안정화 전에 구조만 복잡해질 수 있다.
- 후속 작업:
- 읽기 전용 MCP server 1호 범위에 포함할 도구와 제외할 도구를 별도 메모로 정리한다.

## TASK-004 실제 적용 검증 준비와 smoke CI 가시성 정리

- 상태: completed
- 우선순위: medium
- 요청일: 2026-04-22
- 완료일: 2026-04-22
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `tests/README.md`
- `.github/workflows/smoke.yml`
- `core/workflow_kit_roadmap.md`
- 작업 내용:
- 실제 외부 저장소 시범 적용 대상을 선정하기 전에 smoke test 실패 분류와 실행 기준을 더 읽기 쉽게 정리하고, 파일럿 적용 기록 형식을 준비한다.
- 진행 현황:
- `2026-04-22` 기준 tests 문서에서 원샷 실행 명령과 대표 smoke 범위를 확인했다.
- `2026-04-22` 기준 로드맵에서 실제 적용 검증은 4단계 진입 조건으로 분류돼 있음을 확인했다.
- `2026-04-22` 기준 GitHub Actions smoke workflow 에 실패한 `tests/check_*.py` 목록과 로컬 재실행 명령을 Step Summary 에 남기도록 보강했다.
- `2026-04-22` 기준 `tests/README.md` 에 변경 유형별 권장 실행 순서와 실패 분류 가이드를 추가했다.
- `2026-04-22` 기준 실제 저장소 파일럿 적용 기록용 `templates/pilot_adoption_record_template.md` 초안을 추가했다.
- `2026-04-22` 기준 파일럿 후보 저장소 선정 기준과 적용 전후 비교 항목을 `templates/pilot_candidate_checklist.md` 로 분리했다.
- 완료 기준:
- 테스트 실패 시 원인을 빠르게 분류할 수 있고, 첫 파일럿 적용 기록을 남길 최소 포맷이 준비된다.
- 작업 결과:
- smoke CI 실패 요약 가시성과 파일럿 적용 기록 포맷, 파일럿 후보 선정 체크리스트의 1차 초안을 반영했다.
- `existing` 모드 기준의 파일럿 적용 기록 예시를 `examples/pilot_adoption_existing_repo_example.md` 로 추가해 템플릿 사용 예시를 보강했다.
- `existing` 모드 기준의 파일럿 실행 계획 예시를 `examples/pilot_execution_plan_existing_repo.md` 로 추가해 후보 선정 이후의 실제 진행 순서를 문서화했다.
- 파일럿 후보를 서비스 운영형 저장소와 리서치 운영형 저장소로 비교해 1순위 선정 논리를 적은 `examples/pilot_candidate_shortlist_example.md` 를 추가했다.
- 1순위 후보 체크리스트를 실제로 채운 예시인 `examples/pilot_candidate_existing_service_repo_example.md` 를 추가해 후보 구체화 단계를 보강했다.
- 실제 로컬 저장소 `open_git_client` 를 첫 파일럿 1순위 후보로 평가한 문서 `examples/pilot_candidate_open_git_client_assessment.md` 를 추가했다.
- 실제 로컬 저장소 `open_git_client` 에 bootstrap, onboarding runner, quick test 까지 적용한 기록 예시 `examples/pilot_adoption_open_git_client_example.md` 를 추가했다.
- `validation-plan` 이 docs-only 입력에서도 프로젝트 프로파일의 빠른 테스트 명령을 기본 회귀 확인으로 제안하도록 정책을 보강했다.
- docs-primary 정책을 보여주는 대표 샘플 `examples/output_samples/validation_plan.docs_primary.acme_delivery_platform.json` 을 추가했다.
- `tool_version` 단일 출처가 이미 코드에서는 `workflow_kit.__version__` 로 정리돼 있음을 문서와 schema 안내에도 반영했다.
- 읽기 전용 MCP 1차 묶음을 위한 draft registry, entrypoint, smoke test 와 안내 문서 `mcp/read_only_bundle.md` 를 추가했다.
- 읽기 전용 MCP 5종의 input schema 를 `workflow_kit/server/read_only_registry.py` manifest 와 `workflow_kit/server/read_only_entrypoint.py` payload 검증에 연결했다.
- `workflow_kit/server/read_only_tools.py` direct-call adapter 와 `workflow_kit/common/read_only_bundle.py` 공통 callable layer 를 추가해 read-only bundle 이 subprocess adapter 에만 머물지 않도록 정리했다.
- `workflow_kit/common/output_contracts.py`, `schemas/output_sample_contracts.json`, `tests/check_output_samples.py` 를 함께 확장해 read-only MCP 5종, `session_start`, `backlog_update`, `create_backlog_entry`, `doc_sync`, `merge_doc_reconcile`, `code_index_update`, `validation_plan`, `demo_workflow`, `existing_project_onboarding` 의 nested output shape 를 공통 계약으로 관리하도록 정리했다.
- `scripts/generate_output_json_schema.py`, `schemas/generated_output_schemas.json`, `tests/check_output_json_schema.py`, `tests/check_generated_schema_validation.py` 를 추가해 runtime 계약에서 generated JSON Schema draft 를 생성하고, sample JSON 을 실제 Draft 2020-12 validator 로 검증하는 흐름을 연결했다.
- `tests/check_backlog_update.py`, `tests/check_create_backlog_entry.py` 를 추가해 backlog 계열 prototype 도 공통 output contract smoke 아래 두었다.
- `mcp/create-backlog-entry/scripts/run_create_backlog_entry.py` 에 repo root bootstrap 을 보강해 단독 실행 시 `workflow_kit` import 가 깨지지 않도록 정리했다.
- `examples/output_samples/existing_project_onboarding.acme_delivery_platform.json` 을 현재 runner 의 `source_context` 출력 형태에 맞춰 갱신했다.
- `2026-04-23` 기준 `core/output_schema_guide.md`, `schemas/README.md`, `mcp/read_only_bundle.md`, `tests/README.md` 를 실제 구현 상태에 맞춰 다시 정렬했다.
- `2026-04-23` 기준 output schema guide 의 MCP 섹션에서 `suggest_impacted_docs` 와 `check_quickstart_stale_links` 설명 순서가 뒤바뀐 문제를 수정했다.
- `2026-04-23` 기준 generated JSON Schema 와 sample validation smoke 가 이미 동작 중이라는 점을 schema/read-only bundle 문서에도 반영했다.
- 다음 세션 시작 포인트:
- generated JSON Schema 를 MCP manifest 또는 외부 소비 지점에서 재사용할지, 아니면 `backlog_update`/runner 계열 nested object 를 더 깊게 세분화할지 우선순위를 정한다.
- 남은 리스크:
- generated JSON Schema 는 주요 nested shape 까지는 반영하지만, 아직 모든 family 의 깊은 nested object 와 enum 제약까지 완전히 고정한 것은 아니다.
- 후속 작업:
- `doc_sync`, `merge_doc_reconcile`, `repository_assessment.summary`, `source_context` 계열에서 아직 느슨한 nested object 를 추가로 세분화하고, 필요 시 generated schema 기반 외부 validator 실행을 CI summary 와 연결한다.

## 세션 인계 메모

- 현재 저장소 상태:
- `standard_ai_workflow` 는 read-only MCP bundle 의 schema-validated direct-call adapter, 공통 callable layer, 공통 output contract validator, generated JSON Schema draft, sample JSON schema validation smoke 까지 반영된 상태다.
- 이번 세션 핵심 완료 항목:
- read-only MCP bundle 의 input/output schema manifest 노출과 direct-call adapter 정리
- `workflow_kit/common/read_only_bundle.py` 공통 callable layer 도입으로 CLI prototype 과 server path 로직 공유
- `workflow_kit/common/output_contracts.py`, `schemas/output_sample_contracts.json`, `schemas/generated_output_schemas.json` 정렬
- `scripts/generate_output_json_schema.py`, `tests/check_output_json_schema.py`, `tests/check_generated_schema_validation.py` 추가
- `tests/check_backlog_update.py`, `tests/check_create_backlog_entry.py` 추가 및 backlog 계열 output contract smoke 편입
- 이번 세션 검증 완료:
- `python3 tests/check_backlog_update.py`
- `python3 tests/check_create_backlog_entry.py`
- `python3 tests/check_validation_plan.py`
- `python3 tests/check_code_index_update.py`
- `python3 tests/check_demo_workflow.py`
- `python3 tests/check_existing_project_onboarding.py`
- `python3 tests/check_read_only_mcp_server.py`
- `python3 tests/check_output_json_schema.py`
- `python3 tests/check_generated_schema_validation.py`
- `python3 tests/check_output_samples.py`
- `python3 tests/check_docs.py`
- 다음 세션 우선 시작 순서:
- generated JSON Schema 를 read-only bundle manifest 또는 다른 소비 지점에 직접 연결할지 검토
- `doc_sync`, `merge_doc_reconcile`, runner `source_context`, `repository_assessment.summary` 의 deeper nested shape 를 계속 세분화
- 필요 시 `tests/README.md` 와 `.github/workflows/smoke.yml` 에 generated schema validation smoke 강조 또는 편입 순서를 보강
- 별도 저장소 참고 메모:
- `/home/yklee/repos/open_git_client` 는 `Add AI workflow pilot bootstrap docs` 커밋이 로컬 `codeserver` 브랜치에만 있고 아직 push 하지 않았다.
