# 2026-04-23 작업 백로그

- 문서 목적: 2026-04-23에 수행한 작업의 계획, 진행 현황, 완료 내역을 기록한다.
- 범위: 2026-04-23 작업 이력
- 대상 독자: 프로젝트 참여자, 문서 작성자, 개발자, 운영자
- 상태: draft
- 최종 수정일: 2026-04-23
- 관련 문서: `../work_backlog.md`, `../backlog/2026-04-22.md`, `../schemas/README.md`, `../mcp/read_only_bundle.md`, `../workflow_kit/README.md`

## TASK-001 generated JSON Schema 의 read-only manifest 소비 경로 연결

- 상태: completed
- 우선순위: high
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `workflow_kit/server/read_only_registry.py`
- `tests/check_read_only_mcp_server.py`
- `mcp/read_only_bundle.md`
- `schemas/README.md`
- `workflow_kit/README.md`
- `tests/README.md`
- 작업 내용:
- runtime output contract 에서 생성한 tool 별 JSON Schema draft 를 read-only MCP bundle manifest 에 직접 노출하고, smoke test 와 문서를 함께 정렬한다.
- 진행 현황:
- `2026-04-23` 기준 read-only manifest 가 required keys 와 field_shapes 만 노출하고, generated JSON Schema 는 별도 파일/테스트에만 머물러 있음을 확인했다.
- `2026-04-23` 기준 `workflow_kit.common.output_contracts.output_json_schema_for_family` 를 manifest 의 `output_schema.json_schema` 단일 출처로 연결했다.
- `2026-04-23` 기준 read-only manifest smoke 가 모든 bundle tool 의 `output_schema.json_schema` 를 runtime 생성 결과와 비교하도록 보강했다.
- `2026-04-23` 기준 schema/read-only bundle/tests/package 문서를 manifest 연결 상태에 맞춰 정리했다.
- 완료 기준:
- `--list-tools` manifest 에 tool 별 Draft 2020-12 JSON Schema 가 포함되고, smoke test 가 runtime contract 생성 결과와 같음을 확인한다.
- 작업 결과:
- read-only MCP bundle manifest 가 tool 별 generated JSON Schema draft 를 직접 노출하게 됐고, 테스트가 해당 schema 의 단일 출처 정합성을 확인한다.
- 검증 완료:
- `python3 tests/check_read_only_mcp_server.py`
- `python3 tests/check_output_json_schema.py`
- `python3 tests/check_generated_schema_validation.py`
- `python3 tests/check_output_samples.py`
- `python3 tests/check_docs.py`
- `for t in tests/check_*.py; do python3 "$t" || exit 1; done`
- 다음 세션 시작 포인트:
- manifest 바깥의 외부 소비 지점에서 generated schema bundle 을 어떻게 읽을지 결정한다.
- 남은 리스크:
- JSON Schema 는 여전히 주요 nested shape 중심이며, 모든 enum 과 깊은 nested object 를 완전히 고정한 것은 아니다.

## TASK-002 doc_sync / merge_doc_reconcile source_context shape 세분화

- 상태: completed
- 우선순위: high
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `workflow_kit/common/output_contracts.py`
- `schemas/output_sample_contracts.json`
- `schemas/generated_output_schemas.json`
- 작업 내용:
- `doc_sync`, `merge_doc_reconcile` 성공 payload 의 느슨한 `source_context` object 를 실제 runner 출력 기준으로 세분화한다.
- 진행 현황:
- `2026-04-23` 기준 두 runner 의 성공 `source_context` 가 각각 `project_profile_path`, 변경 파일 목록, 요약 필드를 안정적으로 반환함을 확인했다.
- `2026-04-23` 기준 runtime output contract 와 정적 sample contract 에 해당 required keys 와 타입을 반영했다.
- `2026-04-23` 기준 runtime 계약에서 `schemas/generated_output_schemas.json` 를 다시 생성했다.
- 완료 기준:
- output sample contract 와 generated JSON Schema 가 세분화된 source_context shape 를 반영하고, sample validation smoke 가 통과한다.
- 작업 결과:
- `doc_sync.source_context` 는 `project_profile_path`, `changed_files`, nullable `change_summary` 를 검증한다.
- `merge_doc_reconcile.source_context` 는 `project_profile_path`, `merge_result_summary`, `changed_files` 를 검증한다.
- 검증 완료:
- `python3 tests/check_output_samples.py`
- `python3 tests/check_output_json_schema.py`
- `python3 tests/check_generated_schema_validation.py`
- `for t in tests/check_*.py; do python3 "$t" || exit 1; done`
- `python3 tests/check_demo_workflow.py`
- `for t in tests/check_*.py; do python3 "$t" || exit 1; done`
- 다음 세션 시작 포인트:
- runner 계열 source_context 중 아직 느슨한 object 로 남은 부분을 추가로 세분화한다.
- 남은 리스크:
- error payload 의 source_context 는 현재 shape 검증 대상이 아니므로 별도 정책이 필요할 수 있다.

## TASK-003 orchestration/session nested object shape 추가 세분화

- 상태: completed
- 우선순위: medium
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `workflow_kit/common/output_contracts.py`
- `schemas/output_sample_contracts.json`
- `schemas/generated_output_schemas.json`
- 작업 내용:
- runner 계열과 session-start 성공 payload 에 남아 있던 느슨한 nested object 를 실제 출력 기준으로 추가 세분화한다.
- 진행 현황:
- `2026-04-23` 기준 `demo_workflow` 와 `existing_project_onboarding` 의 `orchestration_plan.model_split` 이 공통적으로 `orchestrator`, `doc_worker`, `validation_worker` 를 포함하고, demo 에서는 `code_worker` 를 추가로 포함함을 확인했다.
- `2026-04-23` 기준 `session_start.source_documents` 가 `session_handoff_path`, `work_backlog_index_path`, `project_profile_path` 를 안정적으로 반환함을 확인했다.
- `2026-04-23` 기준 runtime output contract 와 정적 sample contract 에 해당 nested object required keys 와 타입을 반영했다.
- `2026-04-23` 기준 runtime 계약에서 `schemas/generated_output_schemas.json` 를 다시 생성했다.
- 완료 기준:
- orchestration/session nested object shape 가 generated JSON Schema 에 반영되고, runner/sample smoke 가 통과한다.
- 작업 결과:
- `orchestration_plan.model_split` 은 `orchestrator`, `doc_worker`, `validation_worker` 를 필수 string 으로 검증하고, `code_worker` 가 있으면 string 으로 검증한다.
- `session_start.source_documents` 는 `session_handoff_path`, `work_backlog_index_path`, `project_profile_path` 를 필수 string 으로 검증한다.
- 검증 완료:
- `python3 tests/check_output_samples.py`
- `python3 tests/check_output_json_schema.py`
- `python3 tests/check_generated_schema_validation.py`
- `python3 tests/check_demo_workflow.py`
- `python3 tests/check_existing_project_onboarding.py`
- `for t in tests/check_*.py; do python3 "$t" || exit 1; done`
- 다음 세션 시작 포인트:
- error payload 의 `source_context` shape 검증 정책을 별도로 정할지 검토한다.
- 남은 리스크:
- `model_split` 의 worker 이름과 모델 라벨 값은 아직 enum 으로 고정하지 않았다.

## TASK-004 error payload source_context shape 검증 정책 적용

- 상태: completed
- 우선순위: medium
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `workflow_kit/common/output_contracts.py`
- `schemas/output_sample_contracts.json`
- `schemas/generated_output_schemas.json`
- `tests/check_output_samples.py`
- `schemas/README.md`
- `core/output_schema_guide.md`
- `tests/README.md`
- 작업 내용:
- 주요 skill/runner error payload 의 `source_context` shape 를 success payload shape 와 분리해 관리하고, 정의된 family 에 한해 runtime validator 와 generated JSON Schema 에 연결한다.
- 진행 현황:
- `2026-04-23` 기준 error sample 들의 `source_context` required key 를 확인했다.
- `2026-04-23` 기준 `ERROR_OUTPUT_FIELD_SHAPES` 를 추가하고 `validate_output_payload_shape()` 가 error 상태에서도 정의된 family 의 shape 를 검증하도록 바꿨다.
- `2026-04-23` 기준 `output_error_field_shapes_schema()` 를 추가하고 `schemas/output_sample_contracts.json` 의 `error_field_shapes` 와 테스트 비교를 연결했다.
- `2026-04-23` 기준 generated JSON Schema 의 error branch 가 family 별 error `source_context` shape 를 사용하도록 연결했다.
- `2026-04-23` 기준 schema/test/output guide 문서를 error shape 정책에 맞춰 보강했다.
- `2026-04-23` 기준 backlog-update 실패 경로의 `target_date` 가 입력에 따라 `null` 일 수 있음을 반영해 nullable 로 조정했다.
- 완료 기준:
- 대표 error sample 이 runtime validator, 정적 계약, generated JSON Schema 를 모두 통과한다.
- 작업 결과:
- 대표 skill/runner error payload 의 `source_context` shape 가 runtime validator, 정적 계약, generated JSON Schema 에서 함께 검증된다.
- read-only entrypoint 내부 오류처럼 family 별 shape 가 아직 안정화되지 않은 경로는 generic object 로 유지했다.
- 검증 완료:
- `python3 tests/check_output_samples.py`
- `python3 tests/check_output_json_schema.py`
- `python3 tests/check_generated_schema_validation.py`
- `python3 tests/check_docs.py`
- `for t in tests/check_*.py; do python3 "$t" || exit 1; done`
- `for t in tests/check_*.py; do python3 "$t" || exit 1; done`
- 다음 세션 시작 포인트:
- read-only entrypoint 내부 오류처럼 generic object 로 남긴 error `source_context` 경로를 별도 family 또는 공통 entrypoint 계약으로 분리할지 검토한다.
- 남은 리스크:
- error shape 는 대표 sample 과 runner/skill prototype 기준이므로 아직 모든 read-only entrypoint 내부 오류를 강제하지는 않는다.

## TASK-005 read-only entrypoint error family 분리

- 상태: completed
- 우선순위: medium
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `workflow_kit/common/output_contracts.py`
- `workflow_kit/server/read_only_entrypoint.py`
- `tests/check_read_only_mcp_server.py`
- `schemas/output_sample_contracts.json`
- `schemas/generated_output_schemas.json`
- `mcp/read_only_bundle.md`
- `schemas/README.md`
- `core/output_schema_guide.md`
- `tests/README.md`
- 작업 내용:
- read-only bundle entrypoint 자체 오류를 개별 tool family 가 아니라 `read_only_entrypoint` output family 로 분리해 source_context shape 를 검증한다.
- 진행 현황:
- `2026-04-23` 기준 unknown tool, missing action, invalid payload schema, missing path 경로가 모두 entrypoint 자체 오류임을 확인했다.
- `2026-04-23` 기준 `read_only_entrypoint` 를 error output family 로 추가하고 `action`, `tool`, `payload_json` 중심의 error `source_context` shape 를 정의했다.
- `2026-04-23` 기준 entrypoint 오류 결과가 `action`, `tool`, `payload_json` 을 공통으로 보존하도록 조정했다.
- `2026-04-23` 기준 read-only MCP smoke 가 entrypoint 자체 오류를 `read_only_entrypoint` family 로 검증하도록 보강했다.
- `2026-04-23` 기준 정적 계약과 generated JSON Schema 를 재생성했다.
- `2026-04-23` 기준 read-only bundle/schema/output guide/test 문서를 전용 error family 정책에 맞춰 정리했다.
- 완료 기준:
- read-only entrypoint 의 대표 자체 오류가 전용 output family 계약을 통과한다.
- 작업 결과:
- read-only entrypoint 자체 오류가 `read_only_entrypoint` output family 로 분리됐고, `source_context.action`, `source_context.tool`, `source_context.payload_json` 을 공통으로 검증한다.
- 검증 완료:
- `python3 tests/check_read_only_mcp_server.py`
- `python3 tests/check_output_samples.py`
- `python3 tests/check_output_json_schema.py`
- `python3 tests/check_generated_schema_validation.py`
- `python3 tests/check_docs.py`
- 다음 세션 시작 포인트:
- read-only MCP transport 계층을 붙일 때 entrypoint error family 를 tool descriptor 또는 server-level error response 와 어떻게 매핑할지 정한다.
- 남은 리스크:
- 정식 MCP SDK transport 를 붙이면 error envelope 구조가 바뀔 수 있으므로 server-level error family 를 다시 조정해야 할 수 있다.

## TASK-006 read-only MCP transport descriptor 경계 추가

- 상태: completed
- 우선순위: medium
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `workflow_kit/server/read_only_registry.py`
- `workflow_kit/server/read_only_entrypoint.py`
- `tests/check_read_only_mcp_server.py`
- `mcp/read_only_bundle.md`
- `workflow_kit/README.md`
- `tests/README.md`
- 작업 내용:
- 정식 MCP SDK 서버 루프를 붙이기 전에, transport 가 소비할 draft tool descriptor 생성 경계를 registry 에 추가한다.
- 진행 현황:
- `2026-04-23` 기준 read-only registry 에 `input_json_schema_for_spec`, `build_transport_tool_descriptor`, `build_transport_tool_descriptors` 를 추가했다.
- `2026-04-23` 기준 entrypoint 에 `--list-transport-tools` 를 추가해 descriptor 목록만 분리 조회할 수 있게 했다.
- `2026-04-23` 기준 manifest 에 transport descriptor metadata 와 tool 별 `transport_descriptor` 를 함께 노출했다.
- `2026-04-23` 기준 read-only MCP smoke 가 descriptor count, `latest_backlog` anyOf input requirements, output schema source, read-only annotation 을 검증하도록 보강했다.
- `2026-04-23` 기준 read-only bundle/package/test 문서를 draft descriptor 경계에 맞춰 갱신했다.
- 완료 기준:
- `--list-transport-tools` 가 read-only 5종의 draft descriptor 를 안정적으로 반환하고, 기존 direct-call entrypoint smoke 가 함께 통과한다.
- 작업 결과:
- read-only bundle registry 가 transport descriptor 를 생성하고, entrypoint 가 `--list-transport-tools` 로 descriptor 목록을 반환한다.
- 검증 완료:
- `python3 tests/check_read_only_mcp_server.py`
- `python3 tests/check_docs.py`
- `python3 tests/check_output_samples.py`
- `python3 tests/check_output_json_schema.py`
- `python3 tests/check_generated_schema_validation.py`
- 다음 세션 시작 포인트:
- 정식 MCP SDK 서버 루프를 연결할지, 아니면 descriptor 를 외부 package/export 산출물로 먼저 내보낼지 결정한다.
- 남은 리스크:
- descriptor shape 는 현재 draft 이며, 실제 SDK 가 요구하는 descriptor 필드와 다를 경우 조정이 필요하다.

## TASK-007 read-only transport descriptor export 산출물 추가

- 상태: completed
- 우선순위: medium
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `scripts/generate_read_only_transport_descriptors.py`
- `schemas/read_only_transport_descriptors.json`
- `tests/check_read_only_transport_descriptors.py`
- `scripts/README.md`
- `schemas/README.md`
- `mcp/read_only_bundle.md`
- `tests/README.md`
- 작업 내용:
- read-only MCP draft transport descriptor 를 외부 package/export 흐름에서 재사용할 수 있도록 체크인 산출물로 추가하고, registry 와 stale 여부를 검증한다.
- 진행 현황:
- `2026-04-23` 기준 descriptor 생성 스크립트 `scripts/generate_read_only_transport_descriptors.py` 를 추가했다.
- `2026-04-23` 기준 체크인 산출물 `schemas/read_only_transport_descriptors.json` 을 추가했다.
- `2026-04-23` 기준 `tests/check_read_only_transport_descriptors.py` 로 산출물, 생성 스크립트, registry 결과가 같은지 검증한다.
- `2026-04-23` 기준 scripts/schema/read-only bundle/tests 문서를 descriptor export 흐름에 맞춰 갱신했다.
- 완료 기준:
- descriptor export 산출물이 registry 결과와 같고, read-only bundle smoke 와 전체 smoke 가 통과한다.
- 작업 결과:
- read-only transport descriptor export 산출물과 stale 검증 테스트가 추가됐다.
- 검증 완료:
- `python3 tests/check_read_only_transport_descriptors.py`
- `python3 tests/check_read_only_mcp_server.py`
- `python3 tests/check_docs.py`
- `python3 tests/check_output_json_schema.py`
- `python3 tests/check_generated_schema_validation.py`
- `for t in tests/check_*.py; do python3 "$t" || exit 1; done`
- 다음 세션 시작 포인트:
- descriptor export 산출물을 harness/package export 또는 정식 MCP SDK 서버 루프 중 어디에 먼저 연결할지 결정한다.
- 남은 리스크:
- descriptor 는 아직 draft 이므로 SDK 호환 필드가 확정되면 산출물 shape 가 바뀔 수 있다.

## TASK-008 harness package export 에 read-only descriptor 포함

- 상태: completed
- 우선순위: medium
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `scripts/export_harness_package.py`
- `tests/check_export_harness_package.py`
- `scripts/README.md`
- `README.md`
- `core/workflow_release_spec.md`
- 작업 내용:
- read-only transport descriptor export 산출물을 하네스 package export bundle 에 포함해, Codex/OpenCode 배포 산출물에서도 MCP/server 연결 지점을 전달한다.
- 진행 현황:
- `2026-04-23` 기준 `workflow_common_sources()` 에 `mcp/read_only_bundle.md` 와 `schemas/read_only_transport_descriptors.json` 를 추가했다.
- `2026-04-23` 기준 harness export smoke 가 manifest 에 read-only descriptor 와 read-only bundle 안내 문서가 포함됐는지 확인하도록 보강했다.
- `2026-04-23` 기준 scripts README, 루트 README, release spec 에 descriptor 포함 export 흐름을 반영했다.
- 완료 기준:
- harness package export 산출물 manifest 가 read-only descriptor 와 관련 문서를 포함하고, 전체 smoke 가 통과한다.
- 작업 결과:
- Codex/OpenCode harness export bundle 이 read-only descriptor 산출물과 read-only bundle 안내 문서를 포함한다.
- 검증 완료:
- `python3 tests/check_export_harness_package.py`
- `python3 tests/check_docs.py`
- `python3 tests/check_read_only_transport_descriptors.py`
- `for t in tests/check_*.py; do python3 "$t" || exit 1; done`
- 다음 세션 시작 포인트:
- 정식 MCP SDK 서버 루프를 붙이기 전에 descriptor 포함 export 구조가 실제 하네스 적용 문서에서 충분히 노출되는지 확인한다.
- 남은 리스크:
- descriptor 는 아직 draft 이므로 실제 SDK 서버 루프 연결 시 export bundle 내 위치나 이름을 다시 조정할 수 있다.

## TASK-009 하네스 적용 문서에 descriptor export 위치 노출

- 상태: completed
- 우선순위: medium
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `harnesses/README.md`
- `harnesses/codex/README.md`
- `harnesses/codex/apply_guide.md`
- `harnesses/opencode/README.md`
- `harnesses/opencode/apply_guide.md`
- 작업 내용:
- Codex/OpenCode 적용 문서에서 export bundle 내 read-only MCP descriptor 위치와 draft 사용 범위를 명시한다.
- 진행 현황:
- `2026-04-23` 기준 하네스 README 와 apply guide 에 descriptor 위치가 직접 드러나지 않음을 확인했다.
- `2026-04-23` 기준 공통 하네스 README 에 `bundle/source-docs/schemas/read_only_transport_descriptors.json` 확인 포인트를 추가했다.
- `2026-04-23` 기준 Codex/OpenCode README 와 apply guide 에 descriptor 는 draft 이며 `transport_ready=false` 상태에서는 참고 산출물로 취급해야 한다는 안내를 추가했다.
- 완료 기준:
- 하네스 적용 문서에서 descriptor 위치와 실제 MCP 연결 전 주의점이 명확히 보이고, 문서 smoke 가 통과한다.
- 작업 결과:
- 하네스 적용 문서가 export bundle 내 read-only descriptor 위치와 draft 사용 범위를 안내한다.
- 검증 완료:
- `python3 tests/check_docs.py`
- `python3 tests/check_export_harness_package.py`
- `python3 tests/check_read_only_transport_descriptors.py`
- `for t in tests/check_*.py; do python3 "$t" || exit 1; done`
- 다음 세션 시작 포인트:
- 정식 MCP SDK 서버 루프 연결 가능성을 검토하거나, descriptor 값을 실제 하네스별 MCP 설정 예시로 변환하는 별도 스크립트를 검토한다.
- 남은 리스크:
- 실제 하네스별 MCP 설정 포맷이 확정되면 현재 descriptor 경로 안내만으로는 부족할 수 있다.

## TASK-010 하네스 템플릿과 scaffold 에 descriptor 안내 반영

- 상태: completed
- 우선순위: medium
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `harnesses/_template/README.md`
- `scripts/scaffold_harness.py`
- `tests/check_scaffold_harness.py`
- 작업 내용:
- 새 하네스를 추가할 때 read-only transport descriptor 위치와 draft 사용 범위를 누락하지 않도록 템플릿과 scaffold 생성물에 안내를 반영한다.
- 진행 현황:
- `2026-04-23` 기준 하네스 템플릿과 scaffold 생성 README/overlay spec 에 descriptor 안내가 없음을 확인했다.
- `2026-04-23` 기준 하네스 템플릿에 descriptor 검토 단계, draft 사용 원칙, export 검증 포인트를 추가했다.
- `2026-04-23` 기준 scaffold 생성 README 와 overlay spec 에 descriptor 위치와 `transport_ready=false` 주의점을 추가했다.
- `2026-04-23` 기준 scaffold smoke 가 생성물에 `read_only_transport_descriptors.json` 문구가 포함되는지 확인하도록 보강했다.
- 완료 기준:
- 새 하네스 scaffold 생성물이 descriptor 위치와 draft 사용 범위를 안내하고, scaffold/docs smoke 가 통과한다.
- 작업 결과:
- 새 하네스 템플릿과 scaffold 생성물이 read-only descriptor 위치와 draft 사용 범위를 안내한다.
- 검증 완료:
- `python3 tests/check_scaffold_harness.py`
- `python3 tests/check_docs.py`
- `python3 tests/check_export_harness_package.py`
- `for t in tests/check_*.py; do python3 "$t" || exit 1; done`
- 다음 세션 시작 포인트:
- descriptor 값을 하네스별 MCP 설정 예시로 변환하는 스크립트를 추가할지 검토한다.
- 남은 리스크:
- 실제 하네스별 MCP 설정 포맷이 확정되면 현재 템플릿 문구를 더 구체적인 설정 예시로 바꿔야 한다.

## TASK-011 read-only descriptor 기반 하네스 MCP 예시 산출물 추가

- 상태: completed
- 우선순위: medium
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `schemas/read_only_harness_mcp_examples.json`
- `scripts/generate_read_only_harness_mcp_examples.py`
- `tests/check_read_only_harness_mcp_examples.py`
- `scripts/export_harness_package.py`
- `tests/check_export_harness_package.py`
- `schemas/README.md`
- `scripts/README.md`
- `tests/README.md`
- `harnesses/README.md`
- `harnesses/codex/README.md`
- `harnesses/opencode/README.md`
- `mcp/read_only_bundle.md`
- `README.md`
- `core/workflow_release_spec.md`
- 작업 내용:
- read-only transport descriptor 를 Codex/OpenCode 하네스별 MCP 설정 검토용 draft 예시로 변환하는 체크인 산출물과 생성 스크립트를 추가한다.
- 진행 현황:
- `2026-04-23` 기준 descriptor 위치 안내는 있었지만 하네스별 설정 예시 산출물은 없음을 확인했다.
- `2026-04-23` 기준 `generate_read_only_harness_mcp_examples.py` 를 추가해 descriptor target, tool 목록, `transport_ready=false` 상태를 유지한 예시 bundle 을 생성하도록 했다.
- `2026-04-23` 기준 체크인 산출물 `schemas/read_only_harness_mcp_examples.json` 을 추가했다.
- `2026-04-23` 기준 export bundle 의 source docs 에 하네스 MCP 예시 산출물이 포함되도록 export 스크립트와 smoke test 를 보강했다.
- `2026-04-23` 기준 관련 README 문서에 새 산출물 위치와 `manual_review_only` 원칙을 반영했다.
- 완료 기준:
- 하네스 MCP 예시 산출물이 registry descriptor 기준과 일치하고, export bundle 에 포함되며, 문서 smoke 가 통과한다.
- 작업 결과:
- Codex/OpenCode 하네스가 read-only descriptor 를 실제 MCP 설정으로 승격하기 전 검토할 수 있는 draft 예시 산출물을 제공한다.
- 검증 완료:
- `python3 tests/check_read_only_harness_mcp_examples.py`
- `python3 tests/check_export_harness_package.py`
- `python3 tests/check_docs.py`
- `for t in tests/check_*.py; do python3 "$t" || exit 1; done`
- `git diff --check`
- 다음 세션 시작 포인트:
- 정식 MCP SDK 서버 루프를 붙일 경우 `read_only_harness_mcp_examples.json` 의 주석 처리된 예시를 실제 활성 설정으로 승격할지 검토한다.
- 남은 리스크:
- Codex/OpenCode 의 MCP 설정 포맷이나 로컬 서버 transport 방식이 바뀌면 예시 내용도 함께 갱신해야 한다.

## TASK-012 read-only JSON-RPC draft bridge 추가

- 상태: completed
- 우선순위: medium
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `workflow_kit/server/read_only_jsonrpc.py`
- `tests/check_read_only_jsonrpc_bridge.py`
- `scripts/generate_read_only_harness_mcp_examples.py`
- `schemas/read_only_harness_mcp_examples.json`
- `tests/check_read_only_harness_mcp_examples.py`
- `workflow_kit/README.md`
- `mcp/read_only_bundle.md`
- `tests/README.md`
- `scripts/README.md`
- `schemas/README.md`
- 작업 내용:
- 정식 MCP SDK 서버 루프를 붙이기 전, read-only descriptor 와 direct-call entrypoint 를 JSON-RPC 형태로 호출할 수 있는 draft bridge 를 추가한다.
- 진행 현황:
- `2026-04-23` 기준 SDK 의존성이나 정식 server transport 는 아직 없으므로, 외부 SDK 대신 newline-delimited JSON-RPC fixture 레이어를 먼저 추가하기로 했다.
- `2026-04-23` 기준 `workflow_kit.server.read_only_jsonrpc` 가 `initialize`, `tools/list`, `tools/call`, unknown method/error mapping 을 처리하도록 추가했다.
- `2026-04-23` 기준 `tools/call` 은 기존 `read_only_entrypoint.invoke_tool` 을 재사용해 payload 검증과 output contract 검증 경계를 유지한다.
- `2026-04-23` 기준 하네스 MCP 예시 산출물이 `workflow_kit.server.read_only_jsonrpc --stdio-lines` draft bridge 를 가리키도록 갱신했다.
- `2026-04-23` 기준 관련 README 문서에 JSON-RPC bridge 는 정식 SDK 서버가 아니라 SDK 연결 전 fixture 라는 점을 명시했다.
- 완료 기준:
- JSON-RPC draft bridge 가 descriptor listing, 성공 tool call, tool-call error mapping 을 검증하고 기존 read-only bundle smoke 와 함께 통과한다.
- 작업 결과:
- read-only bundle 이 정식 MCP SDK transport 로 승격되기 전 request/response 경계를 테스트할 수 있는 draft bridge 를 제공한다.
- 검증 완료:
- `python3 tests/check_read_only_jsonrpc_bridge.py`
- `python3 tests/check_read_only_harness_mcp_examples.py`
- `python3 tests/check_read_only_mcp_server.py`
- `python3 tests/check_docs.py`
- `for t in tests/check_*.py; do python3 "$t" || exit 1; done`
- `git diff --check`
- 다음 세션 시작 포인트:
- JSON-RPC draft bridge 의 request/response envelope 를 실제 MCP SDK transport 가 요구하는 형태와 비교해 승격 차이를 분리한다.
- 남은 리스크:
- 현재 bridge 는 SDK 서버가 아니라 fixture 이므로, 실제 MCP client 와의 호환성을 보장하지 않는다.

## TASK-013 read-only JSON-RPC fixture 산출물 추가

- 상태: completed
- 우선순위: medium
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `schemas/read_only_jsonrpc_fixtures.json`
- `scripts/generate_read_only_jsonrpc_fixtures.py`
- `tests/check_read_only_jsonrpc_fixtures.py`
- `scripts/export_harness_package.py`
- `tests/check_export_harness_package.py`
- `schemas/README.md`
- `scripts/README.md`
- `tests/README.md`
- `mcp/read_only_bundle.md`
- `core/workflow_release_spec.md`
- `harnesses/README.md`
- `README.md`
- 작업 내용:
- JSON-RPC draft bridge 의 대표 request/response envelope 를 체크인 fixture 로 고정한다.
- 진행 현황:
- `2026-04-23` 기준 JSON-RPC draft bridge 는 있었지만, 정식 SDK transport 승격 시 비교할 체크인 fixture 는 없었다.
- `2026-04-23` 기준 `generate_read_only_jsonrpc_fixtures.py` 를 추가해 `initialize`, `tools/list`, 성공 `tools/call`, schema error `tools/call`, unknown method fixture 를 생성하도록 했다.
- `2026-04-23` 기준 `schemas/read_only_jsonrpc_fixtures.json` 을 추가했다.
- `2026-04-23` 기준 export bundle 의 source docs 에 JSON-RPC fixture 산출물이 포함되도록 export 스크립트와 smoke test 를 보강했다.
- `2026-04-23` 기준 관련 README 문서에 fixture 는 SDK transport 승격 전 envelope 비교용 기준선이며 client 호환성 보장이 아니라는 점을 명시했다.
- 완료 기준:
- JSON-RPC fixture 산출물이 runtime bridge 결과와 같고, export bundle 에 포함되며, 문서 smoke 가 통과한다.
- 작업 결과:
- read-only bridge request/response envelope 를 정적 산출물로 고정해 향후 SDK transport 승격 시 diff 기준선을 제공한다.
- 검증 완료:
- `python3 tests/check_read_only_jsonrpc_fixtures.py`
- `python3 tests/check_read_only_jsonrpc_bridge.py`
- `python3 tests/check_export_harness_package.py`
- `python3 tests/check_docs.py`
- `for t in tests/check_*.py; do python3 "$t" || exit 1; done`
- `git diff --check`
- 다음 세션 시작 포인트:
- JSON-RPC fixture 와 실제 MCP SDK server response 형태를 비교해 승격 시 유지/변경할 envelope 필드를 분리한다.
- 남은 리스크:
- fixture 는 현재 draft bridge 의 출력만 고정하므로 실제 MCP SDK 구현 시 일부 필드 이름이나 error envelope 는 바뀔 수 있다.

## TASK-014 read-only MCP transport 승격 기준 spec 추가

- 상태: completed
- 우선순위: medium
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `core/read_only_mcp_transport_promotion.md`
- `tests/check_read_only_transport_promotion_spec.py`
- `core/prototype_promotion_scope.md`
- `mcp/read_only_bundle.md`
- `workflow_kit/README.md`
- `tests/README.md`
- `README.md`
- 작업 내용:
- JSON-RPC draft bridge 를 정식 MCP SDK transport 로 승격할 때 유지할 계약과 바뀔 수 있는 envelope 를 분리한 기준 spec 을 추가한다.
- 진행 현황:
- `2026-04-23` 기준 JSON-RPC fixture 는 있었지만, SDK 승격 시 어떤 field 를 유지하고 어떤 envelope 변경을 허용할지 별도 문서가 없었다.
- `2026-04-23` 기준 `core/read_only_mcp_transport_promotion.md` 를 추가해 descriptor 단일 출처, fixture 기준선, 유지할 계약, 변경 가능한 envelope, 승격 전 검증을 분리했다.
- `2026-04-23` 기준 spec 이 fixture 이름과 핵심 유지 계약을 놓치지 않도록 `check_read_only_transport_promotion_spec.py` 를 추가했다.
- `2026-04-23` 기준 README, prototype promotion scope, read-only bundle, workflow kit, tests 문서에 새 spec 링크를 추가했다.
- 완료 기준:
- transport 승격 기준 spec 이 fixture 이름과 유지 계약을 반영하고, 문서 smoke 와 spec smoke 가 통과한다.
- 작업 결과:
- 정식 MCP SDK transport 구현 전후에 descriptor 계약과 JSON-RPC envelope 변경을 분리해 검토할 기준 문서를 확보했다.
- 검증 완료:
- `python3 tests/check_read_only_transport_promotion_spec.py`
- `python3 tests/check_docs.py`
- `python3 tests/check_read_only_jsonrpc_fixtures.py`
- `for t in tests/check_*.py; do python3 "$t" || exit 1; done`
- `git diff --check`
- 다음 세션 시작 포인트:
- transport 승격 기준을 바탕으로 실제 SDK server loop 구현 후보를 검토하거나, 먼저 input schema dataclass 화를 진행한다.
- 남은 리스크:
- 실제 SDK 구현에서 요구하는 capability/notification 세부 필드는 아직 문서 기준에 반영하지 않았다.

## TASK-015 로드맵 최신 구현 상태 반영

- 상태: completed
- 우선순위: medium
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `core/workflow_kit_roadmap.md`
- `core/prototype_promotion_scope.md`
- 작업 내용:
- 리뷰에서 확인한 로드맵/승격 범위 문서의 오래된 상태 표현을 현재 read-only MCP 구현 상태에 맞게 정리한다.
- 진행 현황:
- `2026-04-23` 기준 로드맵이 `tool_version` 단일 소스화와 read-only MCP descriptor/JSON-RPC fixture/승격 spec 추가를 아직 반영하지 못하고 있음을 확인했다.
- `2026-04-23` 기준 로드맵의 현재 상태, 아직 비어 있는 축, 우선순위, 2주 순서, 다음 작업 목록을 최신 구현 상태 기준으로 갱신했다.
- `2026-04-23` 기준 `prototype_promotion_scope.md` 의 “읽기 전용 MCP 5종 + check_quickstart_stale_links” 표현을 `check_quickstart_stale_links` 를 포함한 5종으로 정리했다.
- 완료 기준:
- 상위 로드맵이 현재 구현 상태와 다음 우선순위를 오해 없이 설명하고, 문서 smoke 가 통과한다.
- 작업 결과:
- 다음 작업 판단 기준이 `tool_version`/descriptor 초안 이전 단계가 아니라 JSON-RPC draft bridge 안정화, SDK transport 승격 준비, input schema 강화 중심으로 정렬됐다.
- 검증 완료:
- `python3 tests/check_docs.py`
- `python3 tests/check_read_only_transport_promotion_spec.py`
- `for t in tests/check_*.py; do python3 "$t" || exit 1; done`
- `git diff --check`
- 다음 세션 시작 포인트:
- read-only JSON-RPC draft bridge 의 malformed input/error envelope 보강 또는 실제 MCP SDK transport loop 후보 검토를 진행한다.
- 남은 리스크:
- 실제 SDK 구현에서 요구하는 세부 capability/notification 필드는 아직 코드 검증으로 확인하지 않았다.

## TASK-016 JSON-RPC malformed input envelope 보강

- 상태: completed
- 우선순위: medium
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `workflow_kit/server/read_only_jsonrpc.py`
- `tests/check_read_only_jsonrpc_bridge.py`
- `scripts/generate_read_only_jsonrpc_fixtures.py`
- `schemas/read_only_jsonrpc_fixtures.json`
- `tests/check_read_only_jsonrpc_fixtures.py`
- `core/read_only_mcp_transport_promotion.md`
- `mcp/read_only_bundle.md`
- `tests/README.md`
- 작업 내용:
- JSON-RPC draft bridge 의 malformed JSON 과 non-object request 경로를 JSON-RPC error envelope 로 고정한다.
- 진행 현황:
- `2026-04-23` 기준 `--request-json` 과 `--stdio-lines` 의 `json.loads` 실패가 프로세스 예외로 빠질 수 있음을 확인했다.
- `2026-04-23` 기준 `parse_request_json` 을 추가해 malformed JSON 은 `-32700 Parse error`, non-object request 는 `-32600 Invalid Request` 로 반환하도록 했다.
- `2026-04-23` 기준 단일 요청과 stdio-lines 모드 모두 malformed line 이후 계속 처리되는지 smoke test 를 추가했다.
- `2026-04-23` 기준 JSON-RPC fixture 에 `malformed_json_parse_error`, `non_object_invalid_request` 를 추가하고 승격 기준 문서에 반영했다.
- 완료 기준:
- malformed input 이 JSON-RPC envelope 로 반환되고, fixture/spec/docs smoke 가 통과한다.
- 작업 결과:
- JSON-RPC draft bridge 의 parse/invalid request 실패 경로가 SDK transport 승격 전 비교 가능한 fixture 로 고정됐다.
- 검증 완료:
- `python3 tests/check_read_only_jsonrpc_bridge.py`
- `python3 tests/check_read_only_jsonrpc_fixtures.py`
- `python3 tests/check_read_only_transport_promotion_spec.py`
- `python3 tests/check_docs.py`
- `for t in tests/check_*.py; do python3 "$t" || exit 1; done`
- `git diff --check`
- 다음 세션 시작 포인트:
- notification/capability edge case 를 추가로 고정하거나 실제 MCP SDK transport loop 후보를 검토한다.
- 남은 리스크:
- 현재 parse error data shape 는 draft bridge 기준이며, 실제 SDK 구현에서는 세부 필드가 달라질 수 있다.

## TASK-017 JSON-RPC notification / initialize edge case 고정

- 상태: completed
- 우선순위: medium
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `workflow_kit/server/read_only_jsonrpc.py`
- `scripts/generate_read_only_jsonrpc_fixtures.py`
- `schemas/read_only_jsonrpc_fixtures.json`
- `tests/check_read_only_jsonrpc_bridge.py`
- `tests/check_read_only_jsonrpc_fixtures.py`
- `core/read_only_mcp_transport_promotion.md`
- `mcp/read_only_bundle.md`
- `tests/README.md`
- `workflow_kit/README.md`
- 작업 내용:
- JSON-RPC draft bridge 의 notification 무응답 규칙과 initialize 최소 입력 shape 를 fixture/test/spec 기준으로 고정한다.
- 진행 현황:
- `2026-04-23` 기준 bridge 가 `notifications/initialized` 한 가지 경우만 무응답 처리하고, 다른 notification 은 error 응답으로 빠질 수 있음을 확인했다.
- `2026-04-23` 기준 initialize request 의 `params`, `capabilities`, `clientInfo`, `protocolVersion` shape 가 사실상 검증되지 않아 draft bridge 기준선이 느슨함을 확인했다.
- `2026-04-23` 기준 모든 `notifications/*` method 를 무응답 처리하도록 일반화하고, initialize params object / capabilities object / clientInfo object / protocolVersion string 최소 검증을 추가했다.
- `2026-04-23` 기준 fixture 에 `initialize_invalid_capabilities`, `notification_initialized_no_response`, `notification_unknown_no_response` 를 추가하고 스모크 테스트와 승격 기준 문서를 함께 갱신했다.
- 완료 기준:
- notification 무응답 규칙과 initialize 최소 입력 검증이 fixture/spec/docs 와 함께 통과한다.
- 작업 결과:
- draft bridge 가 notification 처리와 initialize 입력 경계를 더 명확히 고정해, 실제 SDK transport 승격 시 유지 계약과 바뀔 envelope 를 비교하기 쉬워졌다.
- 검증 완료:
- `python3 tests/check_read_only_jsonrpc_bridge.py`
- `python3 tests/check_read_only_jsonrpc_fixtures.py`
- `python3 tests/check_read_only_transport_promotion_spec.py`
- `python3 tests/check_docs.py`
- 다음 세션 시작 포인트:
- 실제 MCP SDK transport loop 후보를 검토하거나, initialize capability 내부 세부 필드와 client notification 처리 범위를 더 정교하게 문서화한다.
- 남은 리스크:
- notification 과 initialize validation 은 draft bridge 수준의 최소 경계만 고정했으므로, 실제 SDK 가 요구하는 capability 세부 필드와 lifecycle notification 은 추가 조정이 필요할 수 있다.

## TASK-018 initialize capability nested shape 보강

- 상태: completed
- 우선순위: medium
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `workflow_kit/server/read_only_jsonrpc.py`
- `scripts/generate_read_only_jsonrpc_fixtures.py`
- `schemas/read_only_jsonrpc_fixtures.json`
- `tests/check_read_only_jsonrpc_bridge.py`
- `tests/check_read_only_jsonrpc_fixtures.py`
- `core/read_only_mcp_transport_promotion.md`
- `mcp/read_only_bundle.md`
- `tests/README.md`
- `workflow_kit/README.md`
- 작업 내용:
- JSON-RPC draft bridge 의 initialize capability 검증을 nested object / boolean 수준까지 보강해, 허용하는 capability 축과 오류 기준선을 더 구체화한다.
- 진행 현황:
- `2026-04-23` 기준 initialize validation 이 `capabilities` 전체 object 여부만 확인해, `tools.listChanged` 나 `roots` 같은 실제 충돌 가능성이 큰 nested field 는 여전히 느슨하게 통과함을 확인했다.
- `2026-04-23` 기준 `capabilities.tools`, `capabilities.roots`, `capabilities.sampling`, `capabilities.elicitation`, `capabilities.experimental` 를 있으면 object 로 검증하도록 추가했다.
- `2026-04-23` 기준 `capabilities.tools.listChanged`, `capabilities.roots.listChanged` 를 있으면 boolean 으로 검증하도록 추가했다.
- `2026-04-23` 기준 fixture 에 `initialize_with_supported_capabilities`, `initialize_invalid_tools_list_changed`, `initialize_invalid_roots_capability` 를 추가해 성공/실패 기준선을 함께 고정했다.
- 완료 기준:
- initialize capability nested shape 가 fixture/spec/docs 와 함께 고정되고 관련 스모크가 통과한다.
- 작업 결과:
- draft bridge 가 initialize capability 의 허용 object 축과 대표 boolean 필드를 명시적으로 검증하게 되어, 실제 SDK transport 승격 시 어떤 capability 세부 필드가 이미 고정됐는지 더 분명해졌다.
- 검증 완료:
- `python3 tests/check_read_only_jsonrpc_bridge.py`
- `python3 tests/check_read_only_jsonrpc_fixtures.py`
- `python3 tests/check_read_only_transport_promotion_spec.py`
- `python3 tests/check_docs.py`
- 다음 세션 시작 포인트:
- initialize capability 내부에서 아직 generic object 로 남겨둔 field 를 더 좁힐지, 아니면 실제 MCP SDK transport loop 에 필요한 handshake/lifecycle 경계를 구현 후보로 옮길지 결정한다.
- 남은 리스크:
- nested capability validation 은 draft bridge 에서 자주 마주칠 object/bool 경계 위주로만 고정했으므로, 실제 SDK 가 요구하는 capability payload 세부 shape 는 추가 조정이 필요할 수 있다.

## TASK-019 JSON-RPC request/notification lifecycle 경계 고정

- 상태: completed
- 우선순위: medium
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `workflow_kit/server/read_only_jsonrpc.py`
- `scripts/generate_read_only_jsonrpc_fixtures.py`
- `schemas/read_only_jsonrpc_fixtures.json`
- `tests/check_read_only_jsonrpc_bridge.py`
- `tests/check_read_only_jsonrpc_fixtures.py`
- `core/read_only_mcp_transport_promotion.md`
- `mcp/read_only_bundle.md`
- `tests/README.md`
- `workflow_kit/README.md`
- 작업 내용:
- JSON-RPC draft bridge 에서 request 와 notification 의 최소 lifecycle 경계를 고정해, id 검증과 notification 무응답 규칙을 더 명확히 한다.
- 진행 현황:
- `2026-04-23` 기준 `notifications/*` 는 id 유무와 상관없이 무응답 처리되고, 잘못된 JSON-RPC id 타입도 별도 검증하지 않아 request/notification 구분이 느슨함을 확인했다.
- `2026-04-23` 기준 JSON-RPC `id` 는 string/number/null 만 허용하고 boolean/object/array 는 Invalid Request 로 거부하도록 추가했다.
- `2026-04-23` 기준 `notifications/*` request 는 id 가 없을 때만 무응답 처리하고, id 가 있으면 Invalid Request 로 반환하도록 조정했다.
- `2026-04-23` 기준 fixture 에 `notification_with_id_invalid_request`, `invalid_boolean_id` 를 추가하고 lifecycle 관련 테스트와 승격 기준 문서를 함께 갱신했다.
- 완료 기준:
- request/notification lifecycle 경계와 id 검증 규칙이 fixture/spec/docs 와 함께 고정되고 관련 스모크가 통과한다.
- 작업 결과:
- draft bridge 가 request 와 notification 을 구분하는 최소 lifecycle 규칙을 유지하게 되어, 실제 SDK transport 승격 시 바꿔도 되는 envelope 와 유지해야 할 request 경계를 더 분명히 비교할 수 있게 됐다.
- 검증 완료:
- `python3 tests/check_read_only_jsonrpc_bridge.py`
- `python3 tests/check_read_only_jsonrpc_fixtures.py`
- `python3 tests/check_read_only_transport_promotion_spec.py`
- `python3 tests/check_docs.py`
- `git diff --check`
- 다음 세션 시작 포인트:
- stateless draft bridge 를 유지한 채 handshake 순서만 더 고정할지, 아니면 stdio-lines 경로에 initialize 이후 상태를 추적하는 sessionful draft loop 를 도입할지 결정한다.
- 남은 리스크:
- 현재 lifecycle 검증은 id 와 notification 구분까지만 다루므로, 실제 SDK 의 initialize 이후 상태 전이와 추가 lifecycle notification 은 아직 반영하지 않았다.

## TASK-020 stdio sessionful draft loop 도입

- 상태: completed
- 우선순위: medium
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `workflow_kit/server/read_only_jsonrpc.py`
- `scripts/generate_read_only_jsonrpc_fixtures.py`
- `schemas/read_only_jsonrpc_fixtures.json`
- `tests/check_read_only_jsonrpc_bridge.py`
- `tests/check_read_only_jsonrpc_fixtures.py`
- `tests/check_read_only_transport_promotion_spec.py`
- `core/read_only_mcp_transport_promotion.md`
- `mcp/read_only_bundle.md`
- `tests/README.md`
- `workflow_kit/README.md`
- 작업 내용:
- `--stdio-lines` 경로에 initialize 상태를 추적하는 sessionful draft loop 를 도입해, initialize 전 tool request 거부와 duplicate initialize 거부를 고정한다.
- 진행 현황:
- `2026-04-23` 기준 JSON-RPC bridge 는 `--request-json` 와 `--stdio-lines` 모두 완전히 stateless 하게 동작해, stdio session 에서 initialize 없이 `tools/list`/`tools/call` 이 그대로 통과함을 확인했다.
- `2026-04-23` 기준 `JsonRpcSessionState` 를 추가하고 `--stdio-lines` 에서만 session state 를 주입해 initialize 성공 여부와 initialized notification 수신 여부를 추적하도록 조정했다.
- `2026-04-23` 기준 stdio session 에서는 initialize 전 `tools/list`, `tools/call` 을 `Server not initialized (-32002)` 로 거부하고, initialize 는 세션당 한 번만 허용하도록 추가했다.
- `2026-04-23` 기준 checked-in fixture 에 단건 request/response 외에 `stdio_session_requires_initialize`, `stdio_session_rejects_second_initialize` sequence 를 추가해 세션 상태 전이를 함께 고정했다.
- 완료 기준:
- stdio session initialize gating 과 duplicate initialize 거부가 fixture/spec/docs 와 함께 고정되고 관련 스모크가 통과한다.
- 작업 결과:
- draft bridge 가 단일 요청 비교용 stateless 경로와 stdio session 용 stateful 경로를 분리해, 실제 MCP SDK transport 로 승격할 때 필요한 handshake 순서를 더 현실적으로 비교할 수 있게 됐다.
- 검증 완료:
- `python3 tests/check_read_only_jsonrpc_bridge.py`
- `python3 tests/check_read_only_jsonrpc_fixtures.py`
- `python3 tests/check_read_only_transport_promotion_spec.py`
- `python3 tests/check_docs.py`
- `git diff --check`
- 다음 세션 시작 포인트:
- stdio session 에서 `notifications/initialized` 이후만 tool request 를 허용할지까지 handshake 를 더 엄격하게 만들지, 아니면 현재 initialize-only gating 을 유지한 채 실제 SDK transport loop 로 넘어갈지 결정한다.
- 남은 리스크:
- 현재 sessionful draft loop 는 initialize 전후 상태와 duplicate initialize 만 추적하므로, 실제 SDK 의 full handshake 순서와 추가 lifecycle notification/state transition 은 아직 반영하지 않았다.

## TASK-021 optional official MCP SDK stdio candidate 추가

- 상태: completed
- 우선순위: medium
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `workflow_kit/server/read_only_mcp_sdk.py`
- `tests/check_read_only_mcp_sdk_candidate.py`
- `workflow_kit/README.md`
- `mcp/read_only_bundle.md`
- `tests/README.md`
- `core/read_only_mcp_transport_promotion.md`
- `core/workflow_kit_roadmap.md`
- 작업 내용:
- 공식 MCP Python SDK low-level server 패턴을 따르는 optional stdio candidate 모듈을 추가해, 의존성이 있는 환경에서는 실제 SDK server loop 연결 지점을 제공하고 없는 환경에서는 availability metadata 와 오류 경계를 유지한다.
- 진행 현황:
- `2026-04-23` 기준 저장소에는 JSON-RPC draft bridge 와 descriptor/export 산출물은 있었지만, 공식 MCP Python SDK 와 직접 맞닿는 server candidate 모듈은 없음을 확인했다.
- `2026-04-23` 기준 공식 MCP Python SDK v1.x 문서의 low-level server 패턴(`Server`, `stdio_server`, `InitializationOptions`)을 참고해 `workflow_kit.server.read_only_mcp_sdk` 후보 모듈을 추가했다.
- `2026-04-23` 기준 candidate 모듈이 registry descriptor 를 `types.Tool` 목록으로 노출하고, 기존 entrypoint 결과를 `CallToolResult` 로 감싸는 optional stdio server 조립 경로를 추가했다.
- `2026-04-23` 기준 공식 SDK 가 없는 환경에서는 `--print-sdk-runtime` 으로 availability metadata 를 출력하고, `--stdio-sdk` 는 `missing_official_mcp_sdk` 구조화 오류를 반환하도록 추가했다.
- `2026-04-23` 기준 `check_read_only_mcp_sdk_candidate.py` 를 추가해 runtime metadata 와 미설치 환경 fallback 경계를 스모크 테스트로 고정했다.
- 완료 기준:
- optional official SDK candidate 의 runtime metadata 와 fallback 오류 경계가 문서/테스트와 함께 고정된다.
- 작업 결과:
- 저장소가 draft bridge 외에도 공식 MCP Python SDK 기반 stdio server 로 승격할 수 있는 실제 코드 후보를 갖게 됐고, SDK 미설치 환경에서도 안전하게 상태를 보고한다.
- 검증 완료:
- `python3 tests/check_read_only_mcp_sdk_candidate.py`
- `python3 tests/check_read_only_transport_promotion_spec.py`
- `python3 tests/check_docs.py`
- `git diff --check`
- 다음 세션 시작 포인트:
- 공식 MCP Python SDK 를 실제 개발 환경 의존성으로 포함할지 결정하고, 포함한다면 optional candidate 를 CI 또는 로컬 smoke 에서 실제 stdio server 로 기동해 client 상호운용성을 확인한다.
- 남은 리스크:
- 현재 환경에는 공식 MCP Python SDK 가 설치되지 않아 optional candidate 의 실제 stdio loop 기동/상호운용성은 아직 검증하지 못했다.

## TASK-022 official MCP SDK 개발 기준선 보강

- 상태: completed
- 우선순위: medium
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `requirements-dev.txt`
- `.github/workflows/smoke.yml`
- `workflow_kit/server/read_only_mcp_sdk.py`
- `tests/check_read_only_mcp_sdk_stdio.py`
- `tests/README.md`
- `README.md`
- `scripts/README.md`
- `mcp/read_only_bundle.md`
- `workflow_kit/README.md`
- `core/read_only_mcp_transport_promotion.md`
- 작업 내용:
- 본격 MCP 개발 전에 공식 SDK 개발 의존성, CI 재현성, 실제 stdio client/server 왕복 smoke 를 저장소 기준선으로 추가한다.
- 진행 현황:
- `2026-04-23` 기준 공식 MCP Python SDK 는 로컬 `.venv` 에만 설치돼 있고 저장소 의존성 선언이 없어, 새 개발 환경과 CI 에서 같은 기준으로 재현하기 어렵다는 점을 확인했다.
- `2026-04-23` 기준 `requirements-dev.txt` 에 `mcp[cli]==1.27.0` 을 추가하고 GitHub Actions smoke workflow 에 설치 단계를 추가했다.
- `2026-04-23` 기준 official MCP SDK client 로 `workflow_kit.server.read_only_mcp_sdk --stdio-sdk` 를 기동해 `initialize -> tools/list -> tools/call` 을 실제로 왕복하는 `check_read_only_mcp_sdk_stdio.py` 를 추가했다.
- `2026-04-23` 기준 SDK low-level server 의 기본 input validation 이 entrypoint 구조화 error payload 를 가리는 문제를 확인하고 `validate_input=False` 로 조정해 기존 error contract 를 보존하도록 수정했다.
- `2026-04-23` 기준 README, scripts README, tests README, read-only bundle/spec 문서에 SDK 설치와 stdio round-trip smoke 경로를 반영했다.
- 완료 기준:
- 공식 SDK 개발 의존성, stdio interop smoke, CI 설치 경로가 저장소 기준선으로 고정된다.
- 작업 결과:
- 저장소가 로컬 전용 SDK 후보 단계를 넘어, 공식 MCP SDK 를 개발 의존성으로 재현하고 실제 stdio client/server round-trip 까지 확인하는 개발 준비 상태로 올라갔다.
- 검증 완료:
- `./.venv/bin/python tests/check_read_only_mcp_sdk_candidate.py`
- `./.venv/bin/python tests/check_read_only_mcp_sdk_stdio.py`
- `python3 tests/check_docs.py`
- `git diff --check`
- 다음 세션 시작 포인트:
- harness MCP 예시와 export 산출물을 JSON-RPC draft bridge 에서 official SDK stdio candidate 쪽으로 승격할지 결정하거나, 실제 inspector/client 상호운용 smoke 를 추가한다.
- 남은 리스크:
- 현재 stdio interop smoke 는 성공 tool call 과 대표 schema error 경로까지만 확인하므로, pagination/notifications/logging 같은 추가 MCP capability 상호운용성은 아직 검증하지 않았다.

## TASK-023 workflow/skill 중심 릴리즈 우선순위 재정렬

- 상태: completed
- 우선순위: medium
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `core/workflow_kit_roadmap.md`
- `core/workflow_adoption_entrypoints.md`
- `core/workflow_skill_catalog.md`
- `templates/pilot_candidate_checklist.md`
- `templates/pilot_adoption_record_template.md`
- 작업 내용:
- MCP 승격은 다음 릴리즈로 미루고, 이번 릴리즈는 workflow 구성, skill 묶음, 온보딩 및 실적용 준비를 우선하도록 상위 문서 우선순위를 재정렬한다.
- 진행 현황:
- `2026-04-23` 기준 로드맵과 도입 문서가 MCP 승격 준비와 온보딩/실적용 준비를 함께 병렬 우선순위로 두고 있어, 이번 릴리즈 중심축이 문서에서 충분히 선명하지 않음을 확인했다.
- `2026-04-23` 기준 로드맵 우선순위를 기존 프로젝트 온보딩 강화 -> 실제 적용 검증 -> runner/schema 안정화 -> reusable package/MCP 승격 순으로 재배치했다.
- `2026-04-23` 기준 도입 분기 문서에 “이번 릴리즈 기준 권장 도입 묶음” 섹션을 추가해 기존 프로젝트 첫 세션 기본 묶음과 일상 작업 진행 묶음을 명시했다.
- `2026-04-23` 기준 skill 카탈로그에 기존 프로젝트 첫 세션 묶음, 일상 작업 진행 묶음, 병합 후 정리 묶음을 추가하고, MCP 중심 소비 경로는 이번 릴리즈 제외 범위로 명시했다.
- `2026-04-23` 기준 파일럿 후보/기록 템플릿에 skill 사용 순서와 “MCP 없이 workflow/skill 묶음만으로 충분했던 범위” 회고 항목을 추가했다.
- 완료 기준:
- 이번 릴리즈의 중심축이 workflow/skill 온보딩과 실적용 준비라는 점이 로드맵, 도입 문서, 파일럿 템플릿에서 일관되게 보인다.
- 작업 결과:
- 다음 작업 판단 기준이 “MCP를 어디까지 더 붙일까”가 아니라 “기존 프로젝트 첫 세션과 파일럿 적용을 skill 묶음 기준으로 얼마나 자연스럽게 재현할 수 있나”로 정렬됐다.
- 검증 완료:
- `python3 tests/check_docs.py`
- `git diff --check`
- 다음 세션 시작 포인트:
- 실제 파일럿 대상 저장소를 고르거나, 기존 프로젝트 첫 세션용 skill 묶음을 예시 출력/가이드로 더 구체화한다.
- 남은 리스크:
- 상위 우선순위는 정리됐지만, 실제 파일럿 저장소에 적용해 보기 전까지는 skill 묶음 순서와 하네스 소비 순서가 현장에서 자연스러운지 확정할 수 없다.

## TASK-024 workflow/skill 중심 배포 패키징 정렬

- 상태: completed
- 우선순위: medium
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `scripts/export_harness_package.py`
- `tests/check_export_harness_package.py`
- `core/workflow_release_spec.md`
- `core/workflow_harness_distribution.md`
- `harnesses/README.md`
- `scripts/README.md`
- `README.md`
- 작업 내용:
- 이번 릴리즈 방향에 맞춰 하네스 export 패키지가 workflow/skill 온보딩과 파일럿 적용 준비를 우선 드러내도록 manifest 와 배포 문서를 정렬한다.
- 진행 현황:
- `2026-04-23` 기준 export bundle 은 생성 가능했지만, release spec 과 배포 가이드가 이번 릴리즈의 중심이 workflow/skill 온보딩이라는 점을 충분히 드러내지 못함을 확인했다.
- `2026-04-23` 기준 export script manifest 에 `release_focus`, `recommended_entrypoints`, `deferred_release_items` 를 추가하고, source docs 포함 범위에 skill 카탈로그, 온보딩 계약, skill demo, 파일럿 템플릿을 반영했다.
- `2026-04-23` 기준 export smoke test 를 보강해 workflow/skill 진입 문서와 deferred MCP 항목이 manifest 에 기록되는지 검증하도록 맞췄다.
- `2026-04-23` 기준 release spec, harness distribution, harness hub, scripts README, 저장소 README 에 이번 패키지의 기본 소비 진입점과 MCP defer 방침을 반영했다.
- 완료 기준:
- export bundle 과 관련 문서가 이번 릴리즈 배포 단위를 workflow/skill 중심 온보딩 패키지로 일관되게 설명한다.
- 작업 결과:
- dist 산출물을 받은 소비자가 “무엇부터 읽고 적용해야 하는가”를 manifest 와 README 계층에서 바로 파악할 수 있게 됐고, MCP 자료는 참고 자료와 차기 릴리즈 항목으로 분리해 읽히게 됐다.
- 검증 완료:
- `python3 tests/check_export_harness_package.py`
- `python3 tests/check_docs.py`
- `git diff --check`
- 다음 세션 시작 포인트:
- 실제 `dist/harnesses/<target>/` 산출물을 만들어 문서 흐름이 현장 소비자 기준으로 자연스러운지 확인하거나, 파일럿 저장소 1곳에 export bundle 을 적용해본다.
- 남은 리스크:
- 문서와 manifest 는 정렬됐지만, 실제 외부 저장소 소비자가 `recommended_entrypoints` 순서대로 읽을 때 onboarding friction 이 얼마나 줄어드는지는 파일럿 적용 전까지 확정할 수 없다.

## TASK-025 배포 패키지 minimal runtime profile 및 버저닝

- 상태: completed
- 우선순위: high
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `scripts/export_harness_package.py`
- `tests/check_export_harness_package.py`
- `core/workflow_release_spec.md`
- `core/workflow_harness_distribution.md`
- `harnesses/README.md`
- `scripts/README.md`
- `tests/README.md`
- `README.md`
- 작업 내용:
- 배포 패키지가 AI agent 런타임 사용에 필요한 최소 문서만 담도록 slim profile 을 기본값으로 바꾸고, 하네스별 개별 패키지 버저닝을 도입한다.
- 진행 현황:
- `2026-04-23` 기준 기본 export 가 `source-docs/`, global snippet, draft MCP 참고 자료까지 함께 포함해 배포 대상 AI agent 관점에서 컨텍스트 낭비가 크다는 점을 확인했다.
- `2026-04-23` 기준 export 기본 프로필을 `agent_runtime_minimal` 로 바꾸고, bootstrap 결과물과 최소 core 문서만 bundle 에 포함하도록 정리했다.
- `2026-04-23` 기준 `--include-source-docs`, `--include-global-snippets` opt-in 플래그를 추가해 개발 편의용 자료는 배포 타겟에서만 선택적으로 포함되게 했다.
- `2026-04-23` 기준 하네스별 export 경로를 `dist/harnesses/<target>/<version>/` 구조로 바꾸고, zip 파일 이름도 `standard-ai-workflow-<target>-<version>.zip` 형식으로 통일했다.
- `2026-04-23` 기준 manifest 에 `package_name`, `package_version`, `optimization_profile`, `excluded_by_default` 를 추가하고, 기본 export 가 source docs 를 제외하는지 smoke test 로 고정했다.
- `2026-04-23` 기준 export 패키지 루트에 `PACKAGE_CONTENTS.md` 와 `APPLY_GUIDE.md` 를 생성해 다른 환경에서 패키지를 받은 사람이 구성과 적용 순서를 바로 읽을 수 있게 했다.
- 완료 기준:
- 기본 배포 패키지가 하네스별 최소 런타임 파일만 포함하고, 버전 정보와 excluded context 를 manifest 에 남긴다.
- 작업 결과:
- 배포 타겟은 개발 저장소보다 훨씬 얇은 컨텍스트로 구성되고, 하네스별 버전 디렉터리와 zip 이름만으로도 어떤 릴리즈인지 바로 식별할 수 있게 됐다.
- 검증 완료:
- `python3 tests/check_export_harness_package.py`
- `python3 tests/check_docs.py`
- `git diff --check`
- 다음 세션 시작 포인트:
- 실제 `dist/harnesses/<target>/<version>/` 산출물을 생성해 bundle 크기와 첫 진입 문서 흐름을 확인하거나, opt-in source-docs 프로필이 필요한 배포 상황을 구분한다.
- 남은 리스크:
- 최소 런타임 프로필은 컨텍스트 효율에는 유리하지만, 현장 적용자가 추가 배경 문서를 원할 때는 opt-in export 기준을 명확히 안내해야 한다.

## TASK-026 릴리즈 정리 및 pre-release 준비

- 상태: completed
- 우선순위: high
- 요청일: 2026-04-23
- 담당:
- codex
- 호스트명:
- local
- 호스트 IP:
- 127.0.0.1
- 영향 문서:
- `README.md`
- `core/workflow_kit_roadmap.md`
- `releases/prototype-v1-pre-release.md`
- 작업 내용:
- 현재까지의 개발 내용을 릴리즈 관점으로 종합 정리하고, 향후 로드맵과 pre-release 산출물 안내를 문서 기준으로 업데이트한다.
- 진행 현황:
- `2026-04-23` 기준 README 와 로드맵은 세부 작업 기록은 반영됐지만, 이번 릴리즈 결과물을 한 번에 읽을 수 있는 종합 정리와 pre-release 노트가 부족함을 확인했다.
- `2026-04-23` 기준 README 에 개발 종합 정리와 실제 하네스별 pre-release package 위치를 추가했다.
- `2026-04-23` 기준 `releases/prototype-v1-pre-release.md` 를 추가해 릴리즈 성격, 핵심 변경, 산출물, 검증, 다음 릴리즈 이관 항목을 정리했다.
- `2026-04-23` 기준 로드맵에 “이번 릴리즈에서 완료된 것 / 다음 릴리즈로 넘긴 것” 과 릴리즈 운영 정리 축을 추가했다.
- 완료 기준:
- 저장소 문서만 읽어도 이번 pre-release 의 범위와 다음 릴리즈 시작점이 바로 보인다.
- 작업 결과:
- 저장소 루트 README, 로드맵, release note 가 같은 메시지를 내도록 정렬돼서, 개발 정리와 대외 공유용 릴리즈 노트가 분리된 상태로 남게 됐다.
- 검증 완료:
- `python3 tests/check_docs.py`
- `git diff --check`
- 다음 세션 시작 포인트:
- GitHub pre-release 생성 후 외부 저장소 적용 기록을 남기거나, package guide 피드백을 실제 사용 기준으로 반영한다.
- 남은 리스크:
- 릴리즈 노트는 수동 작성 기준이므로, 반복 릴리즈가 늘어나면 changelog 자동화 또는 템플릿 고정이 필요하다.
