# 작업 로그 — 2026-08-25

- 문서 목적: 2026-08-25 세션(외부 하네스 Oh My Pi)의 작업을 기록한다.
- 범위: 원격 동기화, 플러그인 설치 검증(opencode 채널), 발견 항목.
- 대상 독자: 다음 세션 AI agent, 저장소 관리자.
- 상태: active
- 최종 수정일: 2026-09-02
- 관련 문서: `ai-workflow/memory/active/main/session_handoff.md`, `docs/OKF_CONSUMER_QUICKSTART.md` (mkdocs strict build 대상이 아니므로 링크가 아닌 경로 표기 — 61차 수리)

## 1. 세션 요약

외부 하네스(Oh My Pi) 세션에서 저장소 원격 상태 확인·동기화와 플러그인 설치 가능 여부를 실측으로 검증했다. 코드 변경 없음. 산출물은 이 로그와 검증 증거(아래)뿐이다.

## 2. 원격 동기화

- `origin/main` fast-forward: `573f3138` → `4461e08e` (135 커밋, v1.4.0 발행 포함). 새 태그 3개(`v1.2.0-beta`, `v1.3.0`, `v1.4.0`), 새 원격 브랜치 3개(worktree/*) 수신.
- 워크트리 정리: 미추적 `docs/reports/세상일도_무엇_또는_모두_그대로.md` 제거, `__pycache__` 산출물 제거.
- `nul` 파일(Windows 예약 디바이스 이름) — `git clean`/cmd 모두 `Invalid argument` 로 실패 → Python `\\?\` 확장 경로 `os.remove` 로 제거.
- 세션 중 `4e4e8963` (59차 세션 종료, 08-24 18:04) push 수신 → 추가 ff 로 흡수. 종료 시점 로컬 = `origin/main` (0/0).

## 3. 플러그인 설치 검증 (opencode 채널, v1.4.0 source 실측)

환경: Windows 11, Python 3.12/3.13 (`python3` 명령 없음), `opencode` 설치, `claude` CLI 미설치.

검증 방법: 현재 저장소 `workflow-source` (PYTHONPATH 명시) 로 bootstrap 을 임시 디렉터리(`%TEMP%\saw_install_test`) 에 실행하고, MCP 서버를 직접 spawn 해서 JSON-RPC 왕복 확인. 임시 디렉터리는 검증 후 삭제.

### 통과 (실측 증거)

| 항목 | 결과 |
| --- | --- |
| bootstrap `--harness opencode --no-interactive --copy-core-docs --enable-mcp` | exit 0, 339개 파일 |
| `AGENTS.md` · `opencode.json` (orchestrator primary + worker 5종 · permission) · `.opencode/skills/standard-ai-workflow/SKILL.md` · `.opencode/agents/` 5종 · `ai-workflow/` 킷 | 생성 확인 |
| MCP 서버 spawn + `initialize` | `workflow_read_only_bundle` v1.4.0 응답 |
| MCP `tools/list` | 11개 도구 (`latest_backlog`, `check_doc_metadata`, …) |

### 발견 항목 (3) — 다음 세션 착수점

1. **emit MCP 설정의 command 가 항상 `python3`** (`workflow_kit/bootstrap_lib/mcp.py:87` docstring, `:98`). 이 Windows 머신에는 `python3` 이 PATH 에 없어, 생성된 `mcp.opencode.json` 으로 opencode 가 서버를 spawn 하면 실패한다. 서버 코드 자체는 정상(위 실측) — 해석기 선택 문제.
2. **`PYTHONPATH: "workflow-source"` 는 source-checkout 실행 시에만 emit** (`mcp.py:130-131`). 순수 신규 프로젝트는 킷이 `ai-workflow/` 에 있어 emit 경로가 실재하지 않는 디렉터리다. checkout 대상(이 저장소 등)에서는 정상.
3. **이 머신의 전역 Python 이 `workflow_kit` 을 다른 체크아웃 (`D:\01_Development\01_Repos\semcowork\standard_ai_workflow`) 에서 해결** (.pth/sys.path). PATH 의 `wk` 도 `doctor` 명령이 없는 낡은 버전. 전역 도구로 돌리면 이 저장소 코드가 아니라 semcowork 사본이 실행된다.

## 4. 다음 세션 시작 포인트

- ①의 발견 3건을 repo 의 backlog-update 흐름으로 task 등록 (수기 편집 금지 — SSOT 포맷 계약).
- python3 emit 수리 방향 결정: 플랫폼별 해석기(`sys.executable` 파생 등) vs `python` 폴백.
- 관찰 축 이월: `TASK-2026-08-13-main-004` (mypy flake) — 관찰 5차 이후 `gh run list --branch main --workflow smoke.yml` 으로 `19e40ac9` 이후 run 재계수.

## 5. 커밋 상태

- 이 로그 파일만 로컬 커밋 (`docs(work_logs)`). **push 는 이 세션에서 하지 않았다** — 다음 세션의 push 흐름에 포함할 것.

## 6. 61차 세션 (이 세션, Oh My Pi / Windows)

### 6.1 cross-host 분기 병합

세션 시작 시점 로컬 `main`(§5 의 미push `b69a0978`) 과 origin/main 이 `4e4e8963` 에서
분기해 있었다 — 원격 호스트 세션(60차) 이 ADR-027 로드맵 층 M-001~M-006 을 완결하고
**v1.5.0 을 발행**했다. 2개 로컬 커밋을 rebase 로 병합 (state.json 충돌만, 생성물이니
원격 버전 유지 후 재생성). task ID 충돌(원격이 `main-001~016` 사용) 으로 내 task 4건은
`main-017~020` 으로 재배정 — 새 툴(1.5.0) 의 `--wbs exempt --wbs-exempt-reason`
게이트 선언 포함.

### 6.2 main-020 — state.json 백슬래시 결함 수리 (in_progress, CI 대기)

`safe_relpath`(`workflow_kit/common/paths.py:49`) 가 `os.path.relpath` 결과(와
`str(Path)` 폴백)를 그대로 내줘 **Windows 호스트의 state.json 경로 값이 백슬래시
표기**였다(61차 실측: `'docs\\PROJECT_PROFILE.md'`). cross-host federation 의
POSIX 소비자에서 단일 파일명으로 해석되는 결함 — 형식 게이트 부재로 조용히 통과.
수리: 두 분기 `as_posix()` 정규화 + `check_state_json_generated` case_7 신설
(단위 2분기 + 재생성 산출물 형식 대조, 7 cases green) + 되주입 red 실증
(상대 분기 `'docs\\PROJECT_PROFILE.md'` AssertionError) + state.json 재생성(POSIX).
`check_roadmap_state_generated`(5/5) · `check_handoff_next_steps`(2/2) 도 green.

### 6.3 플러그인 설치 작업 확인 (v1.5.0 기준, doctor + live bootstrap)

- **`wk doctor` 실측**: 설치 채널 6개 전부 block — `python3` 부재(5개 채널) +
  `claude` CLI 부재(claude-code). codex/gemini/pi CLI 는 실재. **이 머신에는
  플러그인 설치 캐시가 0**(content_drift caches 빈) — 전역 설정 4종
  (claude/codex/gemini/opencode) 이 존재하나 kit 선언 0. CLAUDE.md 는 포크본
  (v1.0.0-beta fork, 마지막 수동 병합 v1.3.0). `runtime_load` 는 `ps` 부재로 미실측.
- **live bootstrap** (temp 디렉터리, opencode 채널, `--enable-mcp`): v1.5.0 SDLC
  seed(roadmap/ 5종) 정상 도착. emit `mcp.opencode.json` 에서 발견 1·2 **v1.5.0 기준
  재확인** — `command: ["python3", …]`, `PYTHONPATH: "workflow-source"`.
- **spawn 실측 3단계**: ① emit 그대로(`python3`) → command not found ②
  `python3`→`python` 만 교체, env 원상 → **rc 0 이며 module 은 semcowork
  체크아웃(`D:\01_Development\01_Repos\semcowork\…`) 에서 해결** — consumer 가
  다른 프로젝트의 킷 서버를 조용히 실행하는 경로가 실재함(발견 3 실증) ③
  `python` + 이 저장소 `workflow-source` PYTHONPATH → initialize·tools/list
  왕복 성공(serverInfo v1.5.0, tools 11). 서버 코드는 정상 — 결함은 해석기·
  모듈 해결 선택.

### 6.4 mypy flake 관찰 7차

격리(`19e40ac9`) 이후 smoke run 8건 — green 5 · red 3. red 3건
(`b6afe828`·`9e7b2645`·`ff0ac3cc`) 전부 **deterministic · mypy 무관**(schema 샘플
드리프트 · 버전 스탬프 잔재 — annotations 로 규명). mypy 게이트 실패 0
(mypy-strict workflow 도 green). 60차 관찰 6차('실패 run 0') 는 red run
**진행 중**(시작 10:57 → 6차 기록 10:59:39 → failure 확정 11:03 KST) 산출물 —
관찰은 run *완료* 기준이어야 한다. v1.5.0 발행 시점 mcp-sdk-matrix 3셀 red 도
원격이 수리 중(`9feabcd8`).

### 6.5 incident — 탐침의 단위가 아니었다 (main-019 실증)

인터프리터 **시작 후** `os.environ['PYTHONPATH']` 를 세팅한 절차(run_task.py)는
sys.path 에 반영되지 않는다 — 그 절차로 task 4건을 등록했는데 **semcowork
사본(v1.1.8-beta) 의 툴**이 실행됐다. 산출물: legacy 한국어 라벨 task 4건 +
백슬래시 state.json + `planned_items` 키 누락. `git checkout` 원복 + 미추적
제거 후 이 저장소 툴로 재등록. '탐침은 잰 단위가 맞아야 한다'의 **네 번째
사례**(4번째 단위 = *해결되는 패키지의 출처*).

### 6.6 커밋 상태

- 61차: 코드 수정(paths.py·check_state_json_generated.py) + task 4건 + memory
  문서(handoff·backlog·work log) + state.json/roadmap_state 재생성 — 본 세션
  push. 전량 2축(CI) 확인 후 main-020 close.
