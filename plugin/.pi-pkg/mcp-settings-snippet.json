{
  "_comment": "pi (pi-coding-agent)용 MCP 서버 등록 스니펫. ~/.pi/agent/settings.json 의 mcpServers 섹션에 아래 객체를 추가한다. 또는 프로젝트 로컬 .pi/settings.json 에 추가하면 저장소 단위로 공유된다. command 의 cwd 를 plugin 디렉터리로 잡거나, STANDARD_AI_WORKFLOW_ROOT 환경 변수를 워크플로우 루트로 설정한다.",
  "mcpServers": {
    "standardAiWorkflowReadOnly": {
      "type": "stdio",
      "command": "python3",
      "args": [
        "-m",
        "workflow_kit.server.read_only_jsonrpc",
        "--stdio-lines",
        "--bundle",
        "read-only"
      ],
      "env": {
        "STANDARD_AI_WORKFLOW_ROOT": "."
      },
      "trust": true
    }
  }
}
