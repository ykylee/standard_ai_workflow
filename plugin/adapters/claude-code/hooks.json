{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v wk >/dev/null 2>&1 || echo '[standard-ai-workflow] `wk` 를 찾지 못했다 — 스킬은 절차를 안내하지만 메모리 갱신 명령은 돌지 않는다. 설치: docs/INSTALLATION_AND_USAGE.md §3'"
          },
          {
            "type": "command",
            "command": "{ grep -qsF 'generated-from: core/global_workflow_standard.md §1 · §3 · §8 · §11' CLAUDE.md .claude/CLAUDE.md; } || { grep -qsF '@AGENTS.md' CLAUDE.md && grep -qsF 'generated-from: core/global_workflow_standard.md §1 · §3 · §8 · §11' AGENTS.md; } || cat \"${CLAUDE_PLUGIN_ROOT}/adapters/claude-code/rules.md\""
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v wk >/dev/null 2>&1 && wk refresh-state || echo '[standard-ai-workflow] `wk` 를 찾지 못했다 — 스킬은 절차를 안내하지만 메모리 갱신 명령은 돌지 않는다. 설치: docs/INSTALLATION_AND_USAGE.md §3'"
          }
        ]
      }
    ]
  }
}
