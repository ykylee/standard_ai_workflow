{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v wk >/dev/null 2>&1 || echo '[standard-ai-workflow] `wk` not found — the skills still describe the procedure, but the memory-update commands will not run. Install: docs/INSTALLATION_AND_USAGE.md §3'"
          },
          {
            "type": "command",
            "command": "{ grep -qsF 'generated-from: core/global_workflow_standard.md §1 · §3 · §8 · §11' CLAUDE.md .claude/CLAUDE.md; } || { grep -qsF '@AGENTS.md' CLAUDE.md && grep -qsF 'generated-from: core/global_workflow_standard.md §1 · §3 · §8 · §11' AGENTS.md; } || cat \"${CLAUDE_PLUGIN_ROOT}/adapters/claude-code/rules.md\""
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v wk >/dev/null 2>&1 && wk refresh-state || echo '[standard-ai-workflow] `wk` not found — the skills still describe the procedure, but the memory-update commands will not run. Install: docs/INSTALLATION_AND_USAGE.md §3'"
          }
        ]
      }
    ]
  }
}
