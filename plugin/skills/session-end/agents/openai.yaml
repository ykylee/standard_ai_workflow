interface:
  display_name: "Session End"
  short_description: "표준 AI 워크플로우 세션 종료 — handoff 와 backlog 를 갱신하고 state.json 을 재생성해 다음 세션이 그대로 이어받게 남긴다."
  default_prompt: "Use $session-end to follow the standard AI workflow."
policy:
  allow_implicit_invocation: true
