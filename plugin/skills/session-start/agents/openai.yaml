interface:
  display_name: "Session Start"
  short_description: "표준 AI 워크플로우 세션 시작 — state.json + session_handoff.md + backlog 로 현재 기준선을 복원하고 다음 작업 후보를 보고한다."
  default_prompt: "Use $session-start to follow the standard AI workflow."
policy:
  allow_implicit_invocation: true
