# Extension 시스템 v0.7.1+ Roadmap (sub-cat + 4종)

- 문서 목적: standard_ai_workflow 의 Extension 시스템 (v0.7.0 step 7) 의 v0.7.1+ 도입 가이드. sub-cat + 4종 (resiliency) 의 *스케치* + 우리 적응 정책.
- 범위: sub-cat (security/auth/, testing/property-based/) + resiliency-baseline 1종
- 상태: draft (v0.7.1+)
- 최종 수정일: 2026-06-13
- 관련 문서: [`./SCHEMA.md`](./SCHEMA.md) (extension system SSOT), [`./security-baseline.md`](./security-baseline.md), [`./testing-baseline.md`](./testing-baseline.md), [`./performance-baseline.md`](./performance-baseline.md)

## §1 v0.7.1 sub-cat 도입 (스케치)

AIDLC 의 sub-cat 구조 차용:

```
workflow-source/extensions/
├── security/
│   ├── baseline/             # v0.7.0 (current)
│   │   ├── security-baseline.md (6 SEC-WF rule)
│   │   └── security-baseline.opt-in.md
│   └── auth/                 # v0.7.1+ (sub-cat)
│       ├── auth-baseline.md  (6 SEC-AUTH rule)
│       └── auth-baseline.opt-in.md
├── testing/
│   ├── smoke/                # v0.7.0 (current) — current 6 TST-WF rule
│   │   ├── testing-baseline.md
│   │   └── testing-baseline.opt-in.md
│   └── property-based/       # v0.7.1+ (sub-cat)
│       ├── property-based-testing.md (6 PBT-WF rule)
│       └── property-based-testing.opt-in.md
└── performance/
    ├── runtime/              # v0.7.0 (current) — 6 PERF-WF rule
    │   ├── performance-baseline.md
    │   └── performance-baseline.opt-in.md
    └── memory/               # v0.7.1+ (sub-cat)
        ├── memory-baseline.md (6 PERF-MEM rule)
        └── memory-baseline.opt-in.md
```

v0.7.1 의 *본 구현*: directory 구조 변경만. 각 sub-cat 의 *본 구현* (auth-baseline.md, property-based-testing.md, memory-baseline.md) 은 v0.8.0+ follow-up.

## §2 sub-cat 도입 정책

- **migration path**: v0.7.0 의 `security-baseline.md` (flat) → v0.7.1+ 의 `security/baseline/security-baseline.md` (sub-cat). *기존 path 호환* 위해 v0.7.1 release 까지 flat path 유지 후 v0.8.0 에서 sub-cat 으로 migration
- **backward compat**: v0.7.0 의 flat path (e.g. `workflow-source/extensions/security-baseline.md`) 와 v0.7.1+ sub-cat (e.g. `workflow-source/extensions/security/baseline/security-baseline.md`) 동시 지원 (1 release)
- **lint rule 갱신**: `check_extension_system.py` 의 path 매칭이 flat + sub-cat 모두 인식 (commit `0052da1` 의 §7 lint rule)

## §3 4종 (resiliency-baseline) 스케치

AIDLC 의 `extensions/resiliency/baseline/resiliency-baseline.md` (490 line, 16 rule) 의 우리 적응:

| AIDLC Pillar | 우리 적응 | Resiliency-WF rule |
|---|---|---|
| 1. Business Goals | (cloud workload 한정) | N/A — 우리 workflow 는 N/A |
| 2. Change Management & Automation | git + PR review + wiki R-1~R9 | RES-WF-01~02 |
| 3. Integrated Observability | audit log (Step 10) + state.json + health check | RES-WF-03~05 |
| 4. High Availability | (cloud workload 한정) | N/A — local runtime |
| 5. Disaster Recovery | git remote (GitHub + Gitea vault) + auto backup | RES-WF-06~08 |
| 6. Continuous Improvement | smoke test 6종 + drift check + score metric | RES-WF-09~12 |

총 16 AIDLC rule 중 우리 적응 ~6-8 rule (변동 적음). 나머지 8-10 rule 은 cloud-specific (HA / DR / Incident) 로 N/A.

### 3.1 6-8 rule 의 우리 적응 (예상)

| Rule | 우리 운영 |
|---|---|
| RES-WF-01 | Critical workload identification (in-repo 의 ai-workflow vs dist) |
| RES-WF-02 | Change management (PR + commit + vault sync) |
| RES-WF-03 | Audit log observability (v0.7.0 step 10) |
| RES-WF-04 | State.json observability (workflow state) |
| RES-WF-05 | workflow_kit health check (smoke test 6종 PASS rate) |
| RES-WF-06 | Git remote (GitHub) + vault (Gitea) |
| RES-WF-07 | Backlog recovery (mavis-trash + git restore) |
| RES-WF-08 | Session handoff recovery (state.json + handoff.md) |

### 3.2 8-10 rule 의 N/A

| Rule | 이유 |
|---|---|
| RES-09 (RTO/RPO targets) | cloud workload 한정. 우리 local runtime N/A |
| RES-10 (Multi-region) | cloud workload 한정 |
| RES-11 (Active-Active) | cloud workload 한정 |
| RES-12 (Backup frequency) | git 자동화 충분 |
| RES-13 (CD pipeline) | GitHub Actions + Gitea Actions |
| RES-14 (Resiliency testing) | smoke test 가 *부분 커버리지* |
| RES-15 (Incident response) | (옵션) — v0.7.2+ ADR |
| RES-16 (Postmortem) | wiki log 가 유사 역할 |

## §4 v0.7.1 release 시점의 scope

**v0.7.1+ 본 구현** (이 commit):
- 1 doc (이 doc) — sub-cat + resiliency 스케치
- 0 baseline file (v0.8.0+ 에서)
- 0 lint rule 변경 (v0.7.0 의 `check_extension_system.py` 가 flat path + v0.7.1+ sub-cat 모두 인식)

**v0.8.0+ follow-up**:
1. sub-cat directory migration (security/baseline/, security/auth/, testing/smoke/, testing/property-based/, performance/runtime/, performance/memory/)
2. auth-baseline.md (6 SEC-AUTH rule): OAuth / API key / session token 검증
3. property-based-testing.md (6 PBT-WF rule): invariant / round-trip / idempotency 자동 검증 (workflow_kit.common.testing)
4. memory-baseline.md (6 PERF-MEM rule): tracemalloc / gc / RSS 측정 자동화
5. resiliency-baseline.md (8 rule): CI/CD observability + disaster recovery + health check

## §5 우리 운영 metric

v0.7.0 → v0.7.1+ 의 *score 변화*:
- Extension 시스템 coverage: 4 baseline (current 3) → 6-7 baseline (1차 sub-cat) → 12-14 baseline (sub-cat full)
- 1차 sub-cat 만: extension coverage +50%
- 2차 sub-cat full: extension coverage +100%

## §6 References

- 1차 출처: AIDLC `awslabs/aidlc-workflows/aidlc-rules/aws-aidlc-rule-details/extensions/resiliency/baseline/resiliency-baseline.md` (490 line, 16 rule, commit `b19c819`)
- 우리 SSOT: `extensions/SCHEMA.md` (v0.7.0 step 7)
- 우리 1차 출시: `extensions/{security,testing,performance}-baseline.md` (commit `0052da1`)
- 우리 위키: [[topics/aidlc-benchmark-analysis-2026-06-12]] §4.2 B
- 우리 위키: [[concepts/extension-system]] (v0.7.0)
- 우리 검증: `tests/check_extension_system.py` (23 test PASS)
- runtime: `workflow_kit.common.contracts.baselines.evaluate_compliance()` (v0.7.0 follow-up, 18 RuleResult)
