# Business Overview

> Reverse Engineering 9-Artifact #1/9 — generated by `standard_ai_workflow` v0.7.0 step 6
> 1차 출처: AIDLC `inception/reverse-engineering.md` step 2 (business-overview)
> Workflow domain 적응: business transaction = workflow stage transition / business dictionary = workflow vocabulary

## Business Context Diagram

```mermaid
[Business Context mermaid diagram - replace with actual diagram]
```

## Business Description

- **Business Description**: [Overall description of what the workflow system does]
- **Business Transactions**: [List of business transactions (workflow stage transitions, handoffs, approvals) that the system implements]
  - 예: `session-start` → `analysis` → `design` → `implementation` → `review` → `merge`
- **Business Dictionary**: [Workflow domain vocabulary that the system follows]
  - 예: `stage_completion` (stage 완료 상태), `audit_log` (audit 기록), `state.json` (현재 workflow state)

## Component Level Business Descriptions

### [Package/Component Name]

- **Purpose**: [What it does from the business perspective]
- **Responsibilities**: [Key responsibilities]

## Verification

- 본 artifact 가 `workflow-source/reverse-engineering/01-business-overview.md` 에 위치
- Mermaid block (` ```mermaid `) 1개 이상 포함
- Business Transactions ≥ 1 항목
- "Workflow domain 적응" 주석 유지
