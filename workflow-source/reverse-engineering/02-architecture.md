# System Architecture

> Reverse Engineering 9-Artifact #2/9 — generated by `standard_ai_workflow` v0.7.0 step 6
> 1차 출처: AIDLC `inception/reverse-engineering.md` step 3 (architecture)
> Workflow domain 적응: components = harness / skill / MCP / workflow_kit

## System Overview

[High-level description of the standard_ai_workflow system]

## Architecture Diagram

```mermaid
[Architecture mermaid diagram - replace with actual diagram showing all packages, harnesses, skills, MCP servers, relationships]
```

## Component Descriptions

### [Package/Component Name]

- **Purpose**: [What it does]
- **Responsibilities**: [Key responsibilities]
- **Dependencies**: [What it depends on]
- **Type**: [Harness / Skill / MCP / WorkflowKit / Template / Extension]

## Data Flow

```mermaid
[Sequence diagram of key workflow flows: session-start, handoff, audit, version bump]
```

## Integration Points

- **External APIs**: [List with purposes — e.g. git CLI, gh CLI, Gitea API]
- **Internal Stores**: [List with purposes — e.g. state.json, audit.md, PROJECT_PROFILE.md]
- **Third-party Services**: [List with purposes — e.g. GitHub Releases, Gitea]

## Infrastructure Components

- **Harness Stacks**: [List of supported harnesses — Claude, Codex, Gemini, OpenCode, Cursor 등]
- **Distribution Model**: [GitHub Releases + PyPI optional, sub-package 별 zip]
- **Local Paths**: [~/.config/ai-workflow/, ~/wiki/, ai-workflow/memory/active/]

## Verification

- 본 artifact 가 `workflow-source/reverse-engineering/02-architecture.md` 에 위치
- Mermaid block ≥ 2개 (architecture + data flow)
- Component ≥ 1 (Type = Harness/Skill/MCP/WorkflowKit 중 1)
- Integration Points ≥ 1
